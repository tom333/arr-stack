{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "fileMatch": [
        "^charts/arr-stack/values\\.yaml$"
      ],
      "matchStrings": [
        "#\\s*renovate:\\s*image=(?<depName>[^\\s]+)\\s*\\n\\s*repository:\\s*[^\\s]+\\s*\\n\\s*tag:\\s*\"?(?<currentValue>[^\"\\s]+)\"?"
      ],
      "datasourceTemplate": "docker",
      "versioningTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "description": "Automerge minor/patch image bumps tracked by customManagers",
      "matchManagers": [
        "custom.regex"
      ],
      "matchUpdateTypes": [
        "minor",
        "patch",
        "digest"
      ],
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "description": "app-template: NEVER automerge, pinned <5.1.0. The umbrella vendors charts/arr-stack/charts/app-template/ (committed, resolves all 14 aliases). Renovate bumps Chart.yaml/lock but CANNOT regenerate that vendored dir -> version mismatch -> chart renders 6/59 resources. 5.1.0 caused data loss 2026-08-28 (ArgoCD prune deleted PVCs). A bump requires a HUMAN: helm dependency update + unpack app-template into charts/, verify `helm template` = 59 resources, commit the regenerated dir.",
      "matchManagers": [
        "helmv3"
      ],
      "matchPackageNames": [
        "app-template"
      ],
      "automerge": false,
      "allowedVersions": "<5.1.0"
    },
    {
      "description": "Gate major bumps for manual review",
      "matchUpdateTypes": [
        "major"
      ],
      "automerge": false
    },
    {
      "description": "TEMP cap: qbit_manage 4.9.1 max qBittorrent 5.2.2 — remove when qbit_manage supports 5.2.3",
      "matchPackageNames": [
        "lscr.io/linuxserver/qbittorrent"
      ],
      "allowedVersions": "<=5.2.2"
    }
  ],
  "prConcurrentLimit": 20,
  "prHourlyLimit": 5
}
