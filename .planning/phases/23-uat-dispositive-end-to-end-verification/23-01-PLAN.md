---
phase: 23-uat-dispositive-end-to-end-verification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md
  - snapshots/before-phase23-uat-2026-05-27/
  - snapshots/after-phase23-uat-2026-05-27/
autonomous: false
requirements: [CAT-CLEANUP-04]

must_haves:
  truths:
    - "SC#1 — Radarr /api/v3/rootfolder returns Category paths only; legacy /media/films-anime and /media/films-family are ABSENT; valid default /media/films is PRESENT."
    - "SC#2 — Sonarr /api/v3/rootfolder returns Category paths only; legacy /media/anime and /media/family are ABSENT; valid default /media/series is PRESENT."
    - "SC#3 — A fresh Seerr kids-film request lands at /media/films-enfants/<title>/ with qBit category=films-enfants and save_path=/data/torrents/films-enfants/, accepted by the 'qBittorrent - Films - Enfants' DC (NOT the deleted catch-all id=1)."
    - "SC#4 — A non-dry-run arrconf apply on the post-cleanup cluster emits 0 plan_action on root_folders/tags/download_clients for sonarr+radarr; a second back-to-back run is identical (idempotence preserved)."
    - "SC#5 — All 10 Jellyfin Category libraries report ItemCount > 0; no empty lib introduced by the migration."
  artifacts:
    - path: ".planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md"
      provides: "Operator UAT runbook + per-SC result tracking (French, mirrors 22-RUNBOOK.md)"
      contains: "## Étape"
    - path: "snapshots/before-phase23-uat-2026-05-27/"
      provides: "ADR-6 pre-UAT baseline (5/6 apps), committed, secrets auto-redacted"
    - path: "snapshots/after-phase23-uat-2026-05-27/"
      provides: "ADR-6 post-UAT state, committed; diff bounded to the SC#3 new-grab + SC#5 ItemCount delta"
  key_links:
    - from: "Seerr request (SC#3)"
      to: "qBittorrent 'qBittorrent - Films - Enfants' DC"
      via: "Radarr grab routed by film genre/tag, NOT catch-all id=1 (deleted Phase 22)"
      pattern: "category.*films-enfants"
    - from: "arrconf apply (non-dry-run, SC#4)"
      to: "live cluster sonarr+radarr"
      via: "structlog plan_action count=0 across root_folders/tags/download_clients"
      pattern: "plan_action count=0"
---

<objective>
Phase 23 is the dispositive end-to-end UAT for the v0.8.0 Categories cleanup. It proves
in the LIVE my-kluster cluster (running arrconf `:0.15.0` with prune steps active) that
the cleanup executed in Phases 21–22 holds: legacy v0.2.0 root_folders are gone from the
*arr APIs, a fresh Seerr request routes through the correct per-Category download client
(not the now-deleted catch-all), a non-dry-run `arrconf apply` is fully idempotent, and
every Jellyfin Category library still has content.

This is a UAT / verification phase — **NOT a code phase**. No Python code, no chart change,
no new reconciler. The single deliverable is `23-HUMAN-UAT.md`: an operator runbook +
per-SC result tracking, French, mirroring `22-RUNBOOK.md` house style.

Pre-satisfaction state (live cleanup already executed this session — see 22-02-SUMMARY.md):
- **SC#1 PARTIAL** — Radarr legacy roots `/media/films-anime` (id2) + `/media/films-family`
  (id3) already DELETED. This phase RE-CONFIRMS their absence against the live API.
- **SC#2 PARTIAL** — Sonarr legacy roots `/media/anime` (id2) + `/media/family` (id3)
  already DELETED. This phase RE-CONFIRMS their absence.
- **SC#4 PARTIAL** — `arrconf apply --dry-run` already returned 0 plan_action before+after
  cleanup (idempotent). But ROADMAP SC#4 wants a **non-dry-run** apply ×2 = 0 plan_action;
  that real-apply ×2 has NOT been run. This phase executes it.
- **SC#3 FRESH WORK** — the dispositive routing proof. A new live Seerr kids-film request.
- **SC#5 FRESH WORK** — Jellyfin 10-lib ItemCount > 0 check.

Purpose: close CAT-CLEANUP-04 and the v0.8.0 milestone with a documented, repeatable,
operator-executed verification that the cleanup is durable.
Output: `23-HUMAN-UAT.md` + ADR-6 pre/post snapshots committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

# House style to mirror (French runbook: Pré-requis / Étapes / Troubleshooting / Rollback,
# port-forward + arrconf-env sealed-secret extraction, ADR-6 snapshot before/after)
@.planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-RUNBOOK.md

# What the live cleanup already did (SC#1/#2/#4 partially pre-satisfied; catch-all id=1 deleted)
@.planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-02-SUMMARY.md

# DC catch-all full-prune decision (no `unsorted` fallback) — basis for SC#3 routing proof
@.planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-ADR-PLAN-SPLIT.md

<facts>
<!-- Load-bearing constants the executor must copy VERBATIM into the runbook. -->

Cluster access pattern (reuse 22-RUNBOOK Étape 2):
- kubectl namespace: selfhost
- Port-forwards: svc/radarr 7878, svc/sonarr 8989, svc/qbittorrent 8080, svc/seerr 5055, svc/jellyfin 8096
- Credential extraction (sealed-secret arrconf-env):
    eval "$(kubectl -n selfhost get secret arrconf-env -o json \
      | jq -r '.data | to_entries[] | "export \(.key)=\(.value | @base64d)"')"
  Keys: RADARR_API_KEY, SONARR_API_KEY, QBT_USER, QBT_PASS, SEERR_API_KEY, JELLYFIN_API_KEY

Legacy paths (must be ABSENT post-cleanup):
- Radarr: /media/films-anime, /media/films-family
- Sonarr: /media/anime, /media/family

Valid default Categories (must be PRESENT — NOT legacy):
- Radarr: /media/films
- Sonarr: /media/series

The 10 Category names / base_paths (from charts/arr-stack/files/arrconf.yml):
  series (/media/series), series-emilie (/media/series-emilie), series-thomas
  (/media/series-thomas), series-garcons (/media/series-garcons), series-zoe
  (/media/series-zoe), films (/media/films), nouveaux-films (/media/nouveaux-films),
  films-enfants (/media/films-enfants), films-animation-enfants
  (/media/films-animation-enfants), films-zoe (/media/films-zoe)

Jellyfin lib enumeration endpoint (from tools/snapshot/snapshot.sh):
  GET /Library/VirtualFolders   (auth header: Authorization: MediaBrowser Token="$JELLYFIN_API_KEY")
  Each VirtualFolder object has Name + ItemId; per-lib counts via GET /Items?ParentId=<ItemId>&Recursive=true&Limit=0 (TotalRecordCount).

Catch-all DC `qBittorrent` (id=1) was DELETED in Phase 22 (ADR-PLAN-SPLIT Decision 2,
full-prune, no `unsorted` fallback). SC#3 must confirm the new grab went through
`qBittorrent - Films - Enfants`, NOT a resurrected catch-all.

arrconf localhost config pattern (from 20-HUMAN-UAT, for SC#4 apply):
  sed -E 's|http://[^.]+\.selfhost\.svc\.cluster\.local|http://localhost|g' \
    charts/arr-stack/files/arrconf.yml > /tmp/arrconf-localhost.yml
  then: cd tools/arrconf && uv run arrconf --config /tmp/arrconf-localhost.yml apply --apps sonarr,radarr --log-level INFO

ADR-6 snapshot (CLAUDE.md "Workflow snapshot CRITIQUE"):
  tools/snapshot/snapshot.sh --output snapshots/before-phase23-uat-$(date +%F)/
  tools/snapshot/snapshot.sh --output snapshots/after-phase23-uat-$(date +%F)/
  (snapshot.sh covers 6 apps, auto-redacts secrets; commit both, never .gitignore)
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 23-HUMAN-UAT.md runbook (French, mirrors 22-RUNBOOK.md)</name>
  <files>.planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md</files>
  <read_first>
    - @.planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-RUNBOOK.md (structure to mirror: Pré-requis, Étape N, Troubleshooting, Rollback table)
    - @.planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-02-SUMMARY.md (which SC are pre-satisfied)
    - The `<facts>` block in this plan's `<context>` (copy constants verbatim)
  </read_first>
  <action>
    Write `23-HUMAN-UAT.md` — a French operator UAT runbook + per-SC result tracking,
    structurally mirroring `22-RUNBOOK.md`. A scaffold already exists at the target path
    (created by the planner with all 7 Étapes, the Résultats UAT table, Troubleshooting,
    and Rollback); refine/complete it — do NOT discard the scaffold's structure. Required
    sections, in order:

    **Front-matter / intro paragraph** — state this is the dispositive end-to-end UAT
    for v0.8.0 closing CAT-CLEANUP-04; note SC#1/#2/#4 are PARTIALLY pre-satisfied
    (re-confirm) and SC#3/#5 are FRESH work; reference ADR-6 discipline.

    **Pré-requis** — copy from `<facts>`: arrconf `:0.15.0` deployed check
    (`kubectl -n selfhost get cronjob arrconf -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'`
    must be `ghcr.io/tom333/arr-stack-arrconf:0.15.0`; STOP if `0.14.x`); kubectl access
    to `selfhost`; `arrconf` installable via `uv sync`; repo up-to-date on `main`.

    **Étape 1 — Snapshot baseline (ADR-6 pré)** — `tools/snapshot/snapshot.sh --output
    snapshots/before-phase23-uat-$(date +%F)/` then immediate commit
    (`git add snapshots/before-phase23-uat-* && git commit -m "snapshot(23): pre-UAT baseline (ADR-6)"`).
    Mark OBLIGATOIRE before any non-dry-run apply.

    **Étape 2 — Port-forwards + credentials** — all 5 port-forwards (radarr 7878, sonarr
    8989, qbittorrent 8080, seerr 5055, jellyfin 8096) + the `eval "$(kubectl ... arrconf-env
    ...)"` credential block from `<facts>`, plus a sanity-check loop asserting
    RADARR_API_KEY / SONARR_API_KEY / QBT_USER / QBT_PASS / SEERR_API_KEY / JELLYFIN_API_KEY
    are non-empty. Include `umask 077` before extracting creds (threat T-23-03 mitigation).

    **Étape 3 — SC#1 + SC#2 : legacy roots absent (NON-DESTRUCTIF, re-confirm)** — concrete
    curls with exact jq filters (see Task 2 acceptance_criteria for the exact assertions).
    Note these RE-CONFIRM the Phase 22 deletes.

    **Étape 4 — SC#3 : fresh Seerr kids-film request (DISPOSITIVE routing proof)** — the
    full live workflow: pick a known-safe kids title (operator chooses a film that is NOT
    already on disk and HAS available releases — document the chosen title + TMDB id in the
    result block), submit via Seerr, approve, then trace the grab. Include: Seerr request
    POST returns HTTP 201; Radarr `/api/v3/queue` or grab history shows the film with a
    `films-enfants` rootFolderPath; qBit `/api/v2/torrents/info` for the new torrent shows
    `category=films-enfants` + `save_path=/data/torrents/films-enfants/`; and the
    accepting DC is `qBittorrent - Films - Enfants` (NOT a catch-all). Warn operator to
    monitor the single grab and stop if it storms indexers (threat T-23-02). Final disk
    landing `/media/films-enfants/<title>/` is the dispositive truth — note it may complete
    asynchronously (mark "vérifier après import" if grab is still downloading at UAT time).

    **Étape 5 — SC#4 : non-dry-run arrconf apply ×2 (idempotence)** — the `/tmp/arrconf-localhost.yml`
    sed transform + `cd tools/arrconf && uv run arrconf --config /tmp/arrconf-localhost.yml
    apply --apps sonarr,radarr --log-level INFO` run TWICE back-to-back. Expected:
    `plan_action count=0` on root_folders/tags/download_clients for both apps, both runs.
    Include the structlog expected-output block. Warn: this is the ONLY non-dry-run mutation
    in the runbook — it should be a no-op; if plan_action > 0 → STOP, forensic snapshot,
    investigate (threat T-23-01).

    **Étape 6 — SC#5 : Jellyfin 10 libs ItemCount > 0** — `GET /Library/VirtualFolders`
    to enumerate the 10 libs + per-lib `GET /Items?ParentId=<ItemId>&Recursive=true&Limit=0`
    TotalRecordCount > 0 (use the Jellyfin auth header from `<facts>`). List all 10 expected
    lib names verbatim.

    **Étape 7 — Post-check (ADR-6 post + diff + commit)** — `snapshot.sh --output
    snapshots/after-phase23-uat-$(date +%F)/`, bounded `diff -r`, commit both snapshots.

    **Résultats UAT** — a per-SC result table with columns: SC | Description | Attendu |
    Résultat (pass/fail) | Evidence. Five rows (SC#1–SC#5). Leave Résultat/Evidence cells
    blank for the operator to fill during execution.

    **Troubleshooting** — at minimum: qBit 403 (re-login SID), Seerr request stuck in
    pending (manual approve in UI), grab not yet imported at UAT time (mark async, re-check
    later), Jellyfin lib ItemCount 0 (trigger `POST /Library/Refresh` then re-check).

    **Rollback** — table: Étapes 1–3,6 are non-destructive (read-only); Étape 4 SC#3 grab
    can be removed via qBit delete + Radarr unmonitor if the operator wants to revert the
    test download; Étape 5 apply is a no-op (nothing to roll back) — reference
    `snapshots/before-phase23-uat-*` baseline.

    Write in French throughout, matching 22-RUNBOOK tone. Use real fenced bash blocks with
    the exact commands — the operator must execute from the runbook text alone.
  </action>
  <verify>
    <automated>test -f .planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md && [ "$(grep -c '^## Étape' .planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md)" -ge 7 ] && grep -q 'snapshots/before-phase23-uat' .planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md && grep -q 'films-enfants' .planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md && grep -q 'arrconf-env' .planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md && grep -q 'plan_action count=0' .planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md`.
    - `grep -c '^## Étape' 23-HUMAN-UAT.md` returns ≥ 7 (Étapes 1–7).
    - `grep -c 'SC#' 23-HUMAN-UAT.md` returns ≥ 5 (all 5 SC referenced).
    - File contains the string `snapshots/before-phase23-uat` AND `snapshots/after-phase23-uat`.
    - File contains the credential block string `arrconf-env` AND `umask 077`.
    - File contains `films-enfants`, `qBittorrent - Films - Enfants`, and `plan_action count=0`.
    - File contains all 5 port-forward services: `svc/radarr`, `svc/sonarr`, `svc/qbittorrent`, `svc/seerr`, `svc/jellyfin`.
    - A `## Résultats UAT` (or `## Résultats`) table with 5 SC rows is present.
    - Written in French (Pré-requis / Étape / Troubleshooting / Rollback section names present).
  </acceptance_criteria>
  <done>23-HUMAN-UAT.md authored, French, mirrors 22-RUNBOOK.md, all 5 SC covered with concrete commands + per-SC result table.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator executes the UAT runbook against the live cluster</name>
  <action>HUMAN-ACTION (no automation possible from this repo per CLAUDE.md — never deploy/apply directly; always via my-kluster + ArgoCD). Execute `23-HUMAN-UAT.md` (Task 1 output) end to end against the live my-kluster cluster running arrconf :0.15.0: (1) confirm arrconf image is :0.15.0 (Pré-requis); (2) ADR-6 pre-snapshot + commit (Étape 1); (3) 5 port-forwards + arrconf-env creds (Étape 2); (4) SC#1/SC#2 RE-CONFIRM — Radarr/Sonarr /api/v3/rootfolder: legacy paths absent, valid default present (Étape 3); (5) SC#3 DISPOSITIVE — submit ONE known-safe kids-film Seerr request, approve, trace the grab to the `qBittorrent - Films - Enfants` DC (category=films-enfants, save_path=/data/torrents/films-enfants/), disk landing /media/films-enfants/&lt;title&gt;/ (Étape 4); (6) SC#4 — non-dry-run `arrconf apply --apps sonarr,radarr` TWICE, both 0 plan_action (Étape 5); (7) SC#5 — Jellyfin 10 libs each ItemCount &gt; 0 (Étape 6); (8) ADR-6 post-snapshot + bounded diff + commit (Étape 7); (9) fill the Résultats UAT table (pass/fail + evidence per SC) and commit the runbook. See &lt;how-to-verify&gt; for the per-SC assertions and &lt;resume-signal&gt; for the report format. STOP and report if SC#4 RUN 1 shows any plan_action &gt; 0 (do NOT run RUN 2).</action>
  <read_first>
    - .planning/phases/23-uat-dispositive-end-to-end-verification/23-HUMAN-UAT.md (the runbook authored in Task 1)
  </read_first>
  <what-built>
    Task 1 authored `23-HUMAN-UAT.md`. This checkpoint is the operator running it against
    the live my-kluster cluster (arrconf `:0.15.0`), filling the per-SC result table, and
    committing the ADR-6 before/after snapshots. Per CLAUDE.md, the live Seerr request +
    non-dry-run apply CANNOT be automated from this repo — operator-driven, with kubectl
    port-forward + sealed-secret access. This is the dispositive proof for CAT-CLEANUP-04.
  </what-built>
  <how-to-verify>
    Execute `23-HUMAN-UAT.md` end to end (Étapes 1–7), then confirm each SC:

    1. **Pré-requis** — confirm arrconf image is `:0.15.0`:
       `kubectl -n selfhost get cronjob arrconf -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'`
       → must print `ghcr.io/tom333/arr-stack-arrconf:0.15.0`. STOP if `0.14.x`.

    2. **Étape 1** — `tools/snapshot/snapshot.sh --output snapshots/before-phase23-uat-$(date +%F)/`
       runs, output dir created, committed.

    3. **Étape 2** — 5 port-forwards up; `eval "$(kubectl -n selfhost get secret arrconf-env
       -o json | jq -r '.data | to_entries[] | "export \(.key)=\(.value | @base64d)"')"`
       populates all 6 keys non-empty.

    4. **SC#1** — `curl -s -H "X-Api-Key: $RADARR_API_KEY" http://localhost:7878/api/v3/rootfolder
       | jq -r '.[].path'` → list CONTAINS `/media/films`, does NOT contain `/media/films-anime`
       or `/media/films-family`. Assertion:
       `curl -s -H "X-Api-Key: $RADARR_API_KEY" http://localhost:7878/api/v3/rootfolder | jq '[.[].path] | any(. == "/media/films-anime" or . == "/media/films-family")'`
       → returns `false`; and `... | any(. == "/media/films")` → returns `true`.

    5. **SC#2** — `curl -s -H "X-Api-Key: $SONARR_API_KEY" http://localhost:8989/api/v3/rootfolder
       | jq -r '.[].path'` → CONTAINS `/media/series`, does NOT contain `/media/anime` or
       `/media/family`. Assertion mirrors SC#1 against sonarr roots.

    6. **SC#3** — submit a Seerr kids-film request (POST returns HTTP 201), approve, then:
       - qBit: `curl -s -b "SID=$SID" "http://localhost:8080/api/v2/torrents/info" | jq '[.[] | select(.category=="films-enfants")] | .[0] | {name, category, save_path}'`
         → `category == "films-enfants"`, `save_path == "/data/torrents/films-enfants/"`.
       - Radarr grab/queue shows the film on rootFolderPath `/media/films-enfants`.
       - Accepting DC is `qBittorrent - Films - Enfants` (NOT catch-all). Disk landing
         `/media/films-enfants/<title>/` — re-check after import if still downloading.

    7. **SC#4** — `cd tools/arrconf && uv run arrconf --config /tmp/arrconf-localhost.yml
       apply --apps sonarr,radarr --log-level INFO` TWICE → both runs log
       `plan_action count=0` on root_folders/tags/download_clients for sonarr+radarr.

    8. **SC#5** — `curl -s -H 'Authorization: MediaBrowser Token="'"$JELLYFIN_API_KEY"'"'
       http://localhost:8096/Library/VirtualFolders | jq -r '.[].Name'` → 10 libs; each
       lib's `GET /Items?ParentId=<ItemId>&Recursive=true&Limit=0` TotalRecordCount > 0.

    9. **Étape 7** — `snapshot.sh --output snapshots/after-phase23-uat-$(date +%F)/`,
       `diff -r` bounded to SC#3 grab + SC#5 deltas, both snapshots committed.

    10. Fill the `## Résultats UAT` table in `23-HUMAN-UAT.md` (pass/fail + evidence per SC).
        Commit the filled runbook.
  </how-to-verify>
  <acceptance_criteria>
    - SC#1: Radarr rootfolder `any(== "/media/films-anime" or == "/media/films-family")` → `false`; `any(== "/media/films")` → `true`.
    - SC#2: Sonarr rootfolder `any(== "/media/anime" or == "/media/family")` → `false`; `any(== "/media/series")` → `true`.
    - SC#3: Seerr request HTTP 201; new qBit torrent `category == "films-enfants"` AND `save_path == "/data/torrents/films-enfants/"`; accepting DC == `qBittorrent - Films - Enfants` (NOT catch-all id=1); disk landing `/media/films-enfants/<title>/` (async-OK, re-checked after import).
    - SC#4: `arrconf apply` (non-dry-run) ×2 each emits `plan_action count=0` on root_folders/tags/download_clients for sonarr AND radarr.
    - SC#5: `GET /Library/VirtualFolders` returns 10 libs; each of the 10 Category libs (series, series-emilie, series-thomas, series-garcons, series-zoe, films, nouveaux-films, films-enfants, films-animation-enfants, films-zoe) has TotalRecordCount > 0.
    - ADR-6: `snapshots/before-phase23-uat-*` AND `snapshots/after-phase23-uat-*` exist + committed; `diff -r` shows only SC#3 grab + SC#5 ItemCount mutations.
    - `## Résultats UAT` table filled with pass/fail + evidence for all 5 SC; runbook committed.
  </acceptance_criteria>
  <resume-signal>Type "approved: SC#1-5 all pass, snapshots committed" once all 5 SC pass and the before/after ADR-6 snapshots are committed, or describe any failing SC.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → cluster API (port-forward) | sealed-secret `arrconf-env` creds cross into the local shell env; *arr/qBit/Seerr/Jellyfin APIs are reachable on localhost ports |
| Seerr request → indexers/trackers (SC#3) | a fresh request triggers Prowlarr indexer searches + a torrent grab — untrusted external content crosses into the download pipeline |
| arrconf apply (non-dry-run, SC#4) → live *arr config | the ONLY write path in this phase; could mutate root_folders/tags/download_clients if not a no-op |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | non-dry-run `arrconf apply` (SC#4) over-prunes live *arr config | mitigate | SC#4 gate is proven safe: SC#2 dry-run already returned 0 plan_action (22-02-SUMMARY) and the shipped config is `prune: false`. Runbook Étape 5 requires `plan_action count=0` on the FIRST run before the second; if count > 0 → STOP, forensic snapshot, investigate before any further action. ADR-6 pre-snapshot (Étape 1) is the forensic baseline. |
| T-23-02 | Denial of Service | fresh Seerr request (SC#3) storms indexers / grabs an unwanted release | mitigate | Runbook instructs operator to pick a KNOWN-SAFE kids title with available releases, submit ONE request, and monitor the SINGLE grab. No bulk/missing search. If the grab targets an unexpected release or storms indexers → abort the request + qBit delete. |
| T-23-03 | Information Disclosure | sealed-secret `arrconf-env` credential leakage during port-forward / env extraction | mitigate | Creds extracted via `eval` into shell env only (never written to a file); runbook prepends `umask 077`; `snapshot.sh` auto-redacts secrets (per 22-RUNBOOK / ADR-6, 0-leak scan). Port-forwards bound to localhost. |
| T-23-04 | Repudiation | UAT result not reproducible / no forensic trail | mitigate | ADR-6 before/after snapshots committed to Git provide a lossless, versioned forensic record; the `23-HUMAN-UAT.md` result table records pass/fail + evidence per SC. Single-operator project — no multi-party repudiation risk; the Git trail suffices. |
| T-23-05 | Elevation of Privilege | n/a — UAT is read-only except the single Seerr request + no-op apply | accept | No new credentials, no scope expansion, no privilege grant in this phase. Low-value, LAN-only, single-user homelab. |

No HIGH-severity unmitigated threat: the only write path (SC#4 apply) is gated on `plan_action count=0` and backed by an ADR-6 snapshot; SC#3 is a single monitored grab. Proceed.
</threat_model>

<verification>
Phase 23 is verified when the operator (Task 2 checkpoint) confirms all 5 SC pass against
the live cluster and the ADR-6 before/after snapshots are committed:

- SC#1 — Radarr rootfolder: legacy `/media/films-anime` + `/media/films-family` absent; `/media/films` present.
- SC#2 — Sonarr rootfolder: legacy `/media/anime` + `/media/family` absent; `/media/series` present.
- SC#3 — fresh Seerr kids-film request → `qBittorrent - Films - Enfants` DC, category `films-enfants`, save_path `/data/torrents/films-enfants/`, disk `/media/films-enfants/<title>/`.
- SC#4 — non-dry-run `arrconf apply` ×2 → 0 plan_action on root_folders/tags/download_clients (sonarr+radarr).
- SC#5 — all 10 Jellyfin Category libs ItemCount > 0.

`23-HUMAN-UAT.md` result table filled (pass/fail + evidence per SC); both snapshots committed.
</verification>

<success_criteria>
- `23-HUMAN-UAT.md` exists, French, mirrors 22-RUNBOOK.md, covers all 5 ROADMAP SC with concrete operator-executable commands.
- Operator-executed: all 5 SC pass against the live cluster running arrconf `:0.15.0`.
- ADR-6 `snapshots/before-phase23-uat-*` + `snapshots/after-phase23-uat-*` committed; diff bounded.
- CAT-CLEANUP-04 closed; v0.8.0 milestone ready to ship.
</success_criteria>

<output>
After completion, create `.planning/phases/23-uat-dispositive-end-to-end-verification/23-01-SUMMARY.md`
</output>
