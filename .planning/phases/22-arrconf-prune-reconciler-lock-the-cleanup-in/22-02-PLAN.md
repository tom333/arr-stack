---
phase: 22-arrconf-prune-reconciler-lock-the-cleanup-in
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - .planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-RUNBOOK.md
  - .planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-ADR-PLAN-SPLIT.md
autonomous: false
requirements: [CAT-CLEANUP-03]

must_haves:
  truths:
    - "An operator runbook exists that drives the live cleanup of the 3 PRUNE_PHASE_22 orphan torrents and the 10 missing-on-disk *arr records (D-09/D-10/D-11)"
    - "The runbook mandates ADR-6 snapshot before AND after the live operation, and --dry-run before any destructive call"
    - "SC#2 is verified live: arrconf apply --dry-run on the post-Phase-21 cluster (running arrconf :0.15.0) shows 0 plan_action on root_folders/tags/download_clients for Sonarr+Radarr"
    - "The 3 orphan torrents (Zelda ROM, Home Alone 1990, Spy Kids 2001) are deleted from qBit with deleteFiles=true (D-11, irreversible)"
    - "The 10 missing-on-disk *arr records are re-monitored + search-triggered (D-10); not-yet-released 2026 titles stay monitored pending availability"
    - "A phase ADR documents the plan-split decision (code plan + operator plan) per ROADMAP SC#4 intent"
  artifacts:
    - path: ".planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-RUNBOOK.md"
      provides: "operator runbook — port-forwards/creds (Phase 21 pattern), pre-snapshot, dry-run SC#2 gate, orphan delete, missing re-monitor+search, post-snapshot"
      min_lines: 60
    - path: ".planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-ADR-PLAN-SPLIT.md"
      provides: "ADR documenting the 2-plan split (code vs live operator step) and the DC-prune decision rationale"
      min_lines: 20
  key_links:
    - from: "22-RUNBOOK.md"
      to: "qBit /api/v2/torrents/delete"
      via: "POST hashes=<3 orphan hashes>&deleteFiles=true"
      pattern: "deleteFiles=true"
    - from: "22-RUNBOOK.md"
      to: "Radarr/Sonarr /api/v3/command"
      via: "RefreshMovie/RefreshSeries + MissingMoviesSearch / missing-episode search"
      pattern: "MissingMoviesSearch"
---

<objective>
Deliver the live operator cleanup step that Phase 22 folds in by user decision (D-09): remove the 3 PRUNE_PHASE_22 orphan torrents (D-11) and reconcile the 10 missing-on-disk *arr records left by the Phase 21 `both_missing` soft-skip (D-10), then verify SC#2 (dry-run idempotence) against the live cluster now running arrconf `:0.15.0`.

Purpose: Close the v0.2.0 legacy migration completely. The code (Plan 01) prevents drift from returning; this plan cleans the residual live state (orphan torrents on `/data/complete`, missing *arr records) and proves the prune step is idempotent (no over-prune) on the real cluster. Implements CAT-CLEANUP-03 operator portion + verifies ROADMAP SC#2. Documents the plan-split + DC-prune decision in a phase ADR (SC#4 intent).

Output: `22-RUNBOOK.md` (operator runbook, French to match 21-RUNBOOK.md house style), `22-ADR-PLAN-SPLIT.md` (decision record). This plan does NOT touch code and does NOT bump the chart (docs-only — CLAUDE.md §"Release pin co-bump pattern" Exception).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-CONTEXT.md
@.planning/phases/21-filesystem-metadata-migration/21-01-SUMMARY.md
@.planning/phases/21-filesystem-metadata-migration/21-RUNBOOK.md
@.planning/phases/20-categories-cleanup-audit/20-AUDIT.md
@CLAUDE.md

<interfaces>
<!-- Concrete live-operation values the runbook must contain. Extracted from 20-AUDIT.md + 21-RUNBOOK.md. -->

The 3 PRUNE_PHASE_22 orphan torrent hashes (D-11, from 20-AUDIT.md) — exact full hashes:
- eebc5732a40262c8d8f98cbc03c95d3234b99c44 — Legend of Zelda, The - Twilight Princess (.rvz, a game ROM, no *arr match)
- cfb5b5b9bb81a708c197a1f7fd6e55690bd9fcc2 — Maman, j'ai raté l'avion (Home Alone) 1990
- a766daa8f82fd1e1f50c44c1a1c82321e2800afb — Spy Kids 2001
All three are on /data/complete, state stalledUP, match NO Radarr/Sonarr item.

qBit delete endpoint (D-11): POST /api/v2/torrents/delete form body `hashes=<hash>&deleteFiles=true` (deleteFiles=true is IRREVERSIBLE — cleans /data/complete entirely).

The 10 missing-on-disk *arr records (D-10, from 21-01-SUMMARY.md Deviations): the items that were `both_missing` at the Phase 21 live run — their DB records were synced to Category roots but the files are gone. Disposition: re-monitor + search. Not-yet-released 2026 titles (e.g. Mario Galaxy, Hoppers) stay monitored pending availability (no search triggered for unreleased).
*arr commands (D-10): Radarr POST /api/v3/command {"name":"RefreshMovie","movieIds":[...]} then {"name":"MissingMoviesSearch","movieIds":[...]}; Sonarr POST /api/v3/command {"name":"RefreshSeries","seriesId":...} then {"name":"MissingEpisodeSearch"} (or per-series episode search).

Operator access pattern (reuse Phase 21 — 21-RUNBOOK.md Étape 2):
- Port-forwards: kubectl -n selfhost port-forward svc/{radarr 7878 / sonarr 8989 / qbittorrent 8080} :<port>
- Creds: eval "$(kubectl -n selfhost get secret arrconf-env -o json | jq -r '.data | to_entries[] | "export \(.key)=\(.value | @base64d)"')" (umask 077 temp pattern, cleaned post-run)
- Env vars: RADARR_API_KEY, SONARR_API_KEY, QBT_USER, QBT_PASS
- Snapshot tool: tools/snapshot/snapshot.sh --output snapshots/<name>-$(date +%F)/ (ADR-6, committed to git).

SC#2 verification: arrconf apply --dry-run --apps sonarr,radarr against the live cluster (arrconf :0.15.0 deployed via ArgoCD) -> expect 0 plan_action on root_folders/tags/download_clients (the cluster is already Category-aligned post-Phase-21, so the new force_prune step finds nothing to delete).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 22-RUNBOOK.md (orphan delete + missing re-monitor + SC#2 dry-run gate) and 22-ADR-PLAN-SPLIT.md</name>
  <files>.planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-RUNBOOK.md, .planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-ADR-PLAN-SPLIT.md</files>
  <read_first>
    - .planning/phases/21-filesystem-metadata-migration/21-RUNBOOK.md (house style: French, Pré-requis / Étapes / Troubleshooting / Rollback; port-forward + sealed-secret pattern to mirror)
    - .planning/phases/21-filesystem-metadata-migration/21-01-SUMMARY.md Deviations section (the 10 both_missing items + 3 orphans — the carry-forward source)
    - .planning/phases/20-categories-cleanup-audit/20-AUDIT.md lines 413-419, 522-528, 567-573 (the 3 orphan torrent records with full hashes)
    - .planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-CONTEXT.md (D-09/D-10/D-11 decisions)
    - CLAUDE.md section "Workflow snapshot (CRITIQUE)" + section "Filesystem migration" (ADR-6 discipline + snapshot/diff commands)
  </read_first>
  <action>
  Write two documents.

  (A) `22-RUNBOOK.md` — French operator runbook mirroring 21-RUNBOOK.md structure. Sections:
   - Pré-requis: kubectl access; sealed-secret arrconf-env; arrconf :0.15.0 confirmed deployed (ArgoCD synced) — `kubectl -n selfhost get cronjob arrconf -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'` should show :0.15.0.
   - Étape 1 — Snapshot baseline (ADR-6 pre): `tools/snapshot/snapshot.sh --output snapshots/before-phase22-cleanup-$(date +%F)/` then git commit. MANDATORY before any destructive call.
   - Étape 2 — Port-forwards + credentials: copy the Phase 21 pattern verbatim (radarr 7878, sonarr 8989, qbittorrent 8080; `eval "$(kubectl ... arrconf-env ...)"`; sanity-check RADARR_API_KEY/SONARR_API_KEY/QBT_USER/QBT_PASS).
   - Étape 3 — SC#2 dry-run idempotence gate (NON-destructive, run FIRST): `arrconf apply --dry-run --apps sonarr,radarr --log-level INFO`. EXPECTED: 0 plan_action events on root_folders/tags/download_clients for both apps (cluster already Category-aligned post-Phase-21). If ANY plan_action appears -> STOP, snapshot forensic, investigate before proceeding (would mean the cluster drifted or the generator output is wrong).
   - Étape 4 — Delete 3 orphan torrents (D-11, IRREVERSIBLE): for EACH of the 3 hashes, dry-confirm first (GET /api/v2/torrents/info?hashes=<hash> to confirm name/save_path == /data/complete), THEN POST /api/v2/torrents/delete form `hashes=<hash>&deleteFiles=true`. Include the 3 full hashes verbatim:
     - eebc5732a40262c8d8f98cbc03c95d3234b99c44 (Zelda ROM)
     - cfb5b5b9bb81a708c197a1f7fd6e55690bd9fcc2 (Home Alone 1990)
     - a766daa8f82fd1e1f50c44c1a1c82321e2800afb (Spy Kids 2001)
     Warn explicitly: deleteFiles=true permanently removes the data from /data/complete; re-download if ever wanted (re-import historique explicitly out of scope per REQUIREMENTS v0.8.0).
   - Étape 5 — Re-monitor + search the 10 missing-on-disk records (D-10): identify the 10 both_missing movies/series (point at Category roots, no file) — surfaced as MISSING in Radarr/Sonarr. For each: ensure monitored:true (PUT the movie/series if needed), then Radarr POST /api/v3/command {"name":"RefreshMovie","movieIds":[<ids>]} + {"name":"MissingMoviesSearch","movieIds":[<ids>]}; Sonarr RefreshSeries + missing-episode search. EXCEPTION: not-yet-released 2026 titles (Mario Galaxy, Hoppers, etc.) — keep monitored:true but do NOT trigger search (no release available); note them in a table. The operator resolves the exact id list from the live Radarr/Sonarr Wanted/Missing view at run time (the audit ids in 21-01-SUMMARY drifted; treat the live MISSING list as ground truth).
   - Étape 6 — Post-check (ADR-6 post + diff): `tools/snapshot/snapshot.sh --output snapshots/after-phase22-cleanup-$(date +%F)/`; `diff -r snapshots/before-phase22-cleanup-* snapshots/after-phase22-cleanup-*` — EXPECTED: 3 torrents gone from qBit info, the 10 records flipped to monitored/search-queued; no other drift. git commit both snapshots.
   - Troubleshooting: qBit 403 on delete (re-login / cookie expired); a "missing" item that is actually present (skip search); torrent hash not found (already deleted — idempotent, skip).
   - Rollback: orphan torrent delete is IRREVERSIBLE (no rollback — re-add magnet/torrent manually if needed); missing re-monitor is safe (un-monitor to revert). Reference the pre-snapshot for forensics.
  Optionally note (Claude's Discretion per D-09): this runbook may be executed standalone OR appended to the dry-run/apply flow — the operator decides at run time.

  (B) `22-ADR-PLAN-SPLIT.md` — short ADR (markdown, title + Status: Accepted + Context + Decision + Consequences). Record:
   - Decision 1 (plan split): Phase 22 ships as 2 plans — Plan 01 (code: prune reconciler + pydantic guard + tests + chart co-bump, autonomous) and Plan 02 (live operator cleanup, autonomous:false, Wave 2 — depends on :0.15.0 being deployed). Rationale: the operator step (D-09/10/11) talks to the LIVE cluster running the new image, so it genuinely follows the code ship (a later wave / human-action plan), per the planner wave rules. Satisfies ROADMAP SC#4 "decision documented in phase ADR" intent.
   - Decision 2 (DC catch-all): FULL PRUNE of the legacy catch-all download client qBittorrent (id=1, untagged, priority=1) — NOT the rejected `unsorted` low-priority fallback (D-01). End state: only per-Category DCs remain. Implemented via the differ.force_prune path (Plan 01 Task 2) + asserted by respx test test_catch_all_dc_prune_deletes_untagged (Plan 01 Task 3). Rationale: the catch-all intercepted at priority=1 before Category DCs (caused the "La Planète des Alphas" mis-route); a fallback DC would re-introduce the same ambiguity.
  </action>
  <verify>
    <automated>test -f .planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-RUNBOOK.md && test -f .planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-ADR-PLAN-SPLIT.md && grep -q 'deleteFiles=true' .planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-RUNBOOK.md && grep -q 'MissingMoviesSearch' .planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - 22-RUNBOOK.md exists and contains all 3 orphan hashes verbatim: `eebc5732a40262c8d8f98cbc03c95d3234b99c44`, `cfb5b5b9bb81a708c197a1f7fd6e55690bd9fcc2`, `a766daa8f82fd1e1f50c44c1a1c82321e2800afb` (grep each)
    - 22-RUNBOOK.md contains `deleteFiles=true` AND `MissingMoviesSearch` AND `--dry-run` AND `snapshots/before-phase22-cleanup` AND `snapshots/after-phase22-cleanup`
    - 22-RUNBOOK.md contains the SC#2 gate phrasing: `0 plan_action` (or equivalent) on a `arrconf apply --dry-run --apps sonarr,radarr` line
    - 22-ADR-PLAN-SPLIT.md exists, contains `Status: Accepted`, documents both the plan-split AND the full-prune DC decision (grep `Full prune` / `full prune` AND `unsorted`)
    - `git diff --name-only -- charts/arr-stack/values.yaml` is EMPTY for this plan (docs-only, no chart change — D-21-TOOL-02-style exception)
  </acceptance_criteria>
  <done>22-RUNBOOK.md (French, mirrors 21-RUNBOOK.md shape) covers pre-snapshot, port-forward+creds, SC#2 dry-run gate, the 3 orphan-torrent deletes (full hashes, deleteFiles=true), the 10 missing-record re-monitor+search (with 2026-unreleased exception), and post-snapshot+diff; 22-ADR-PLAN-SPLIT.md records the 2-plan split + full-prune DC decision with Status: Accepted. No code, no chart bump.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator live cleanup — execute 22-RUNBOOK.md (orphan deletes + missing re-monitor + SC#2 dry-run gate)</name>
  <action>HUMAN-ACTION (no automation possible from this repo per CLAUDE.md — never deploy/apply directly; always via my-kluster + ArgoCD). Wait until arrconf :0.15.0 is deployed (Renovate PR merged on my-kluster, ArgoCD synced), then execute 22-RUNBOOK.md end to end: (1) ADR-6 pre-snapshot + commit; (2) port-forwards + arrconf-env creds; (3) SC#2 GATE — arrconf apply --dry-run --apps sonarr,radarr must show 0 plan_action on root_folders/tags/download_clients (STOP and report if non-zero); (4) delete the 3 orphan torrents with deleteFiles=true after dry-confirming each hash points at /data/complete; (5) re-monitor + search the 10 missing records, skipping search for unreleased 2026 titles; (6) ADR-6 post-snapshot + diff + commit. See &lt;how-to-verify&gt; for the step detail and &lt;resume-signal&gt; for the report format.</action>
  <what-built>Plan 01 shipped arrconf :0.15.0 (prune reconciler + guard) and this plan's Task 1 produced 22-RUNBOOK.md + 22-ADR-PLAN-SPLIT.md. The runbook drives a LIVE, destructive cleanup against the my-kluster cluster — it cannot be automated from this repo (CLAUDE.md: never deploy/apply directly; always via my-kluster + ArgoCD; live qBit torrent deletion with deleteFiles=true is irreversible and operator-gated).</what-built>
  <how-to-verify>
   Pre-condition: confirm arrconf :0.15.0 is deployed (Renovate PR merged on my-kluster, ArgoCD synced). Then execute 22-RUNBOOK.md end to end:
   1. Étape 1 — run `tools/snapshot/snapshot.sh --output snapshots/before-phase22-cleanup-$(date +%F)/` and commit (ADR-6 pre).
   2. Étape 2 — start the 4 port-forwards + extract arrconf-env creds.
   3. Étape 3 (SC#2 GATE) — run `arrconf apply --dry-run --apps sonarr,radarr`. CONFIRM 0 plan_action on root_folders/tags/download_clients for both apps. If any plan_action appears, STOP and report (do NOT proceed to destructive steps).
   4. Étape 4 — delete the 3 orphan torrents (deleteFiles=true) after dry-confirming each hash points at /data/complete.
   5. Étape 5 — re-monitor + search the 10 missing records (skip search for unreleased 2026 titles, keep monitored).
   6. Étape 6 — post-snapshot + diff (expect only the 3 torrent removals + 10 monitored/search-queued flips), commit both snapshots.
   Report back: the dry-run plan_action count (must be 0), confirmation the 3 hashes are gone from `GET /api/v2/torrents/info`, and the count of re-monitored vs unreleased-deferred records.
  </how-to-verify>
  <resume-signal>Type "applied: SC#2 0 plan_action confirmed, 3 orphans deleted, N re-monitored / M deferred" or describe any plan_action / failure encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live qBit /api/v2/torrents/delete | Destructive + IRREVERSIBLE: deleteFiles=true permanently removes torrent data from /data/complete. |
| operator -> live Radarr/Sonarr /api/v3/command | Search-trigger: re-monitor + MissingMoviesSearch consumes indexer/grab budget; over-triggering could re-acquire unwanted content. |
| arrconf :0.15.0 (deployed) -> live cluster prune at next apply | The force_prune step (Plan 01) now runs in-cluster; the dry-run gate (Étape 3) proves it has nothing to delete before any real apply. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Tampering | irreversible data loss deleting the wrong torrent (deleteFiles=true) | mitigate | Runbook Étape 4 mandates `GET /api/v2/torrents/info?hashes=<hash>` dry-confirm (name + save_path == /data/complete) BEFORE each delete; the 3 hashes are pinned verbatim from 20-AUDIT.md (PRUNE_PHASE_22 flagged). ADR-6 pre-snapshot (Étape 1) is the forensic baseline. |
| T-22-08 | Tampering | the live prune step (arrconf :0.15.0) over-deletes a valid Category root/DC at first cluster apply | mitigate | SC#2 dry-run gate (Étape 3) runs FIRST and BLOCKS: 0 plan_action required before any destructive step; a non-zero plan_action halts the runbook. The allowlist (generator output) protects /media/films + /media/series (Plan 01 threat T-22-01). |
| T-22-09 | Information Disclosure | sealed-secret arrconf-env credentials leak to disk/logs during port-forward setup | mitigate | Reuse the Phase 21 pattern: umask 077 temp file, eval into env (not written to a tracked file), cleaned up post-run; snapshots are sanitized (snapshot.sh strips secrets — Phase 21 verified 0 secret leaks). |
| T-22-10 | Denial of Service | MissingMoviesSearch storms indexers / grabs unwanted releases for unreleased titles | mitigate | Runbook Étape 5 EXPLICITLY excludes not-yet-released 2026 titles from search (monitored only, no search). Operator resolves the live MISSING list as ground truth; per-id movieIds arrays (not a blanket all-missing search). |
| T-22-11 | Repudiation | live destructive run leaves no audit trail | mitigate | ADR-6 pre + post snapshots committed to git (Étape 1 + Étape 6); diff bounded to expected mutations; the human-action checkpoint requires the operator to report the dry-run count + delete confirmations. |
</threat_model>

<verification>
- 22-RUNBOOK.md + 22-ADR-PLAN-SPLIT.md exist; runbook contains the 3 orphan hashes, deleteFiles=true, MissingMoviesSearch, the --dry-run SC#2 gate, and pre/post snapshot commands.
- The human-action checkpoint blocks on the live execution; resume requires operator confirmation of: SC#2 dry-run = 0 plan_action, 3 orphans deleted, missing records re-monitored.
- No code change, no chart change in this plan (docs-only — verify `git diff --name-only` shows only the two .md files under the phase dir).
</verification>

<success_criteria>
- ROADMAP SC#2: arrconf apply --dry-run on the live post-Phase-21 cluster (arrconf :0.15.0) shows 0 plan_action on root_folders/tags/download_clients for Sonarr+Radarr — verified at the human-action checkpoint Étape 3. ✅
- ROADMAP SC#4: DC catch-all full-prune decision documented in a phase ADR (22-ADR-PLAN-SPLIT.md Decision 2). ✅ (implementation + respx test live in Plan 01.)
- Operator portion of CAT-CLEANUP-03 (D-09/10/11): 3 orphan torrents removed, 10 missing records reconciled. ✅ at checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/22-arrconf-prune-reconciler-lock-the-cleanup-in/22-02-SUMMARY.md`
</output>
