---
phase: 32-categories-migration-hard-cut
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/arrconf/arrconf/intent_config.py
  - tools/arrconf/arrconf/config.py
  - tools/arrconf/arrconf/generators/categories.py
  - tools/arrconf/arrconf/__main__.py
  - tools/arrconf/arrconf/diff_cmd.py
  - schemas/arrconf-schema.json
  - schemas/intent-schema.json
autonomous: true
requirements: [CATMIG-01]
must_haves:
  truths:
    - "IntentConfig accepts a top-level categories: list and an apps: pass-through dict"
    - "RootConfig no longer declares a categories field (extra=forbid rejects categories: in arrconf.yml)"
    - "The 5 category generators accept list[MediaCategory] instead of RootConfig"
    - "apply and diff source categories from intent_cfg.categories, not root.categories"
    - "Python triade (ruff format/check + mypy arrconf) passes"
  artifacts:
    - path: "tools/arrconf/arrconf/intent_config.py"
      provides: "IntentConfig.categories + IntentConfig.apps fields"
      contains: "categories: list"
    - path: "tools/arrconf/arrconf/config.py"
      provides: "RootConfig without categories field, legacy guard removed"
    - path: "tools/arrconf/arrconf/generators/categories.py"
      provides: "5 generators taking list[MediaCategory]"
    - path: "schemas/intent-schema.json"
      provides: "regenerated intent schema with categories + apps"
      contains: "categories"
  key_links:
    - from: "tools/arrconf/arrconf/__main__.py apply()/diff()"
      to: "intent_cfg.categories"
      via: "generate_xxx(intent_cfg.categories)"
      pattern: "generate_(sonarr|radarr|qbit|jellyfin|anime)"
    - from: "tools/arrconf/arrconf/diff_cmd.py"
      to: "categories param"
      via: "diff_xxx(client, root, categories)"
      pattern: "categories"
---

<objective>
Migrate the categories contract from RootConfig (arrconf.yml) into IntentConfig (intent.yml), per D-32-01 and D-32-05. This is the indivisible contract-migration plan: it cannot be done half-way because removing `RootConfig.categories` immediately breaks every generator call site and the diff command at runtime. All signature changes, schema regen, and call-site rewiring land atomically so the Python triade stays green at plan end.

Purpose: Make `intent.yml` the single hand-edited owner of `categories[]` (CATMIG-01). After this plan, `categories[]` can no longer be declared in `arrconf.yml` (RootConfig has no such field), and the pure generators are decoupled from both config types — they take a plain `list[MediaCategory]`.

Output: Updated pydantic schemas, generator signatures, wired apply/diff call sites, and regenerated JSON schemas. No file emission yet (that is Plan 02). No data lift of the production categories yet (also Plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-categories-migration-hard-cut/32-CONTEXT.md
@.planning/phases/32-categories-migration-hard-cut/32-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

From tools/arrconf/arrconf/resources/categories.py:
```python
class Category(BaseModel):  # MediaCategory alias in config.py / intent_config.py
    name: str          # kebab-case slug, stable
    kind: str          # "series" | "movies"
    profile: str       # "general" | "family" | "anime"
    display: str
    base_path: str     # MUST equal f"/media/{name}" (D-04 strict invariant, model_validator)
```

From tools/arrconf/arrconf/intent_config.py (IntentConfig root, lines 163-181):
```python
class IntentConfig(BaseModel):
    model_config = ConfigDict(extra="forbid")
    tools: ToolsConfig = Field(default_factory=ToolsConfig, ...)
    sagas: list[SagaEntry] = Field(default_factory=list, ...)
```

From tools/arrconf/arrconf/config.py (RootConfig, lines 644-690):
```python
class RootConfig(BaseModel):
    model_config = ConfigDict(extra="forbid")
    categories: list[MediaCategory] = Field(default_factory=list)   # ← REMOVE
    sonarr: dict[str, SonarrInstance] = Field(default_factory=dict)
    radarr: dict[str, RadarrInstance] = Field(default_factory=dict)
    prowlarr: dict[str, ProwlarrInstance] = Field(default_factory=dict)
    qbittorrent: dict[str, QbittorrentInstance] = Field(default_factory=dict)
    seerr: dict[str, SeerrInstance] = Field(default_factory=dict)
    jellyfin: dict[str, JellyfinInstance] = Field(default_factory=dict)

_LEGACY_CATEGORY_NAMES: frozenset[str] = frozenset({"films-anime", "films-family", "anime", "family"})
def _check_no_legacy_categories(cfg: RootConfig, path: Path) -> None: ...   # ← REMOVE, called in load_config
```

Current generator signatures (tools/arrconf/arrconf/generators/categories.py):
```python
def generate_qbit_categories(cfg: RootConfig) -> list[QbitCategory]
def generate_sonarr_resources(cfg: RootConfig) -> SonarrDerived
def generate_radarr_resources(cfg: RootConfig) -> RadarrDerived
def generate_jellyfin_libraries(cfg: RootConfig) -> list[JellyfinLibrary]
def generate_anime_tag_labels(cfg: RootConfig) -> list[str]
```
All currently access `cfg.categories` internally.

Diff command consumers (tools/arrconf/arrconf/diff_cmd.py):
- `diff_sonarr(client, root_config: RootConfig)` calls `generate_sonarr_resources(root_config)` (line 46)
- `diff_radarr(client, root_config)` calls `generate_radarr_resources(root_config)` (line 66)
- `diff_qbittorrent(client, root_config)` calls `generate_qbit_categories(root_config)` (line 116)
- `diff_jellyfin(client, root_config)` calls `generate_jellyfin_libraries(root_config)` (line 149)
- `diff_prowlarr` does NOT use categories.

apply()/diff() call sites in __main__.py:
- line 67: `all_anime_labels = set(generate_anime_tag_labels(root))`
- line 262: `sonarr_derived = generate_sonarr_resources(root)`
- line 300: `radarr_derived = generate_radarr_resources(root)`
- line 380: `qbit_generated = generate_qbit_categories(root)`
- line 472: `jellyfin_generated = generate_jellyfin_libraries(root)`
- diff branches call `diff_sonarr(client, root)` (834), `diff_radarr(..., root)` (861), `diff_qbittorrent(..., root)` (912), `diff_jellyfin(..., root)` (957)
- apply() already loads `intent_cfg: IntentConfig | None` optionally (lines 222-231); diff() does NOT yet load intent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend IntentConfig, strip RootConfig.categories, retarget generators to list[MediaCategory]</name>
  <files>tools/arrconf/arrconf/intent_config.py, tools/arrconf/arrconf/config.py, tools/arrconf/arrconf/generators/categories.py</files>
  <read_first>
    - tools/arrconf/arrconf/intent_config.py (IntentConfig root + load_intent, lines 163-201)
    - tools/arrconf/arrconf/config.py (RootConfig + _check_no_legacy_categories + load_config, lines 644-712)
    - tools/arrconf/arrconf/generators/categories.py (5 generator signatures + bodies, lines 126-234)
    - tools/arrconf/arrconf/resources/categories.py (Category model — the MediaCategory type)
  </read_first>
  <behavior>
    - Test: IntentConfig.model_validate({"categories": [{name,kind,profile,display,base_path}], "apps": {"sonarr": {...}}}) succeeds and exposes .categories (list[Category]) and .apps (dict[str, Any]).
    - Test: IntentConfig with an unknown top-level key still raises ValidationError (extra="forbid" preserved).
    - Test: RootConfig.model_validate({"categories": [...], "sonarr": {...}}) now raises ValidationError (extra_forbidden on "categories") — categories no longer accepted in arrconf.yml.
    - Test: generate_qbit_categories([Category(...)]) returns list[QbitCategory] (now takes a list, not RootConfig).
    - Test: generate_sonarr_resources([Category(...)]) returns SonarrDerived; generate_radarr_resources, generate_jellyfin_libraries, generate_anime_tag_labels analogous with list input.
  </behavior>
  <action>
    In `tools/arrconf/arrconf/intent_config.py`:
    - Add import: `from typing import Any` (if not present) and `from arrconf.resources.categories import Category as MediaCategory`.
    - In `IntentConfig`, alongside `tools` and `sagas`, add two fields:
      ```python
      categories: list[MediaCategory] = Field(
          default_factory=list,
          description="Cross-cutting media categories (lifted from arrconf.yml, D-32-01).",
      )
      apps: dict[str, Any] = Field(
          default_factory=dict,
          description="App config pass-through (sonarr/radarr/prowlarr/qbittorrent/seerr/jellyfin). "
                      "Emitted verbatim into generated arrconf.yml — no pydantic validation (D-32-01 YAGNI).",
      )
      ```
    - Keep `model_config = ConfigDict(extra="forbid")` untouched. Leave `load_intent` unchanged.

    In `tools/arrconf/arrconf/config.py`:
    - In `RootConfig`, DELETE the line `categories: list[MediaCategory] = Field(default_factory=list)`.
    - DELETE the `_LEGACY_CATEGORY_NAMES` frozenset and the entire `_check_no_legacy_categories` function.
    - In `load_config`, DELETE the call `_check_no_legacy_categories(cfg, path)`. The rest of `load_config` is byte-for-byte unchanged.
    - If `MediaCategory` import is now unused in config.py, remove it (ruff will flag F401 otherwise).

    In `tools/arrconf/arrconf/generators/categories.py`:
    - Change the import `from arrconf.config import RootConfig, TagItem` → drop `RootConfig`, keep `TagItem`. Add `from arrconf.resources.categories import Category as MediaCategory`.
    - Change all 5 signatures from `cfg: RootConfig` to `categories: list[MediaCategory]` (Option 1, KISS per PATTERNS.md):
      `generate_qbit_categories(categories: list[MediaCategory]) -> list[QbitCategory]`
      `generate_sonarr_resources(categories: list[MediaCategory]) -> SonarrDerived`
      `generate_radarr_resources(categories: list[MediaCategory]) -> RadarrDerived`
      `generate_jellyfin_libraries(categories: list[MediaCategory]) -> list[JellyfinLibrary]`
      `generate_anime_tag_labels(categories: list[MediaCategory]) -> list[str]`
    - In each body, replace every `cfg.categories` with `categories` (the param is now the list directly).
    - Update existing unit tests in tools/arrconf/tests/ that call these generators with a RootConfig to pass `.categories` (e.g. `generate_sonarr_resources(root)` → `generate_sonarr_resources(root.categories)`); for IntentConfig-based tests pass `intent_cfg.categories`.

    NOTE: Do NOT touch `__main__.py` or `diff_cmd.py` in this task — that is Task 2. After this task alone, mypy WILL report errors at those call sites; that is expected and fixed in Task 2. Run the per-module tests only (categories generator + config + intent_config tests) to validate behavior; the full triade is gated at end of Task 2.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run pytest tests/ -k "categories or intent_config or config_validation or generators" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "categories: list\[MediaCategory\]" tools/arrconf/arrconf/intent_config.py` returns a match.
    - `grep -n "apps: dict\[str, Any\]" tools/arrconf/arrconf/intent_config.py` returns a match.
    - `grep -c "categories" tools/arrconf/arrconf/config.py` shows no `categories:` field declaration on RootConfig: `grep -n "categories: list" tools/arrconf/arrconf/config.py` returns NO match.
    - `grep -n "_check_no_legacy_categories\|_LEGACY_CATEGORY_NAMES" tools/arrconf/arrconf/config.py` returns NO match.
    - `grep -cE "def generate_(qbit_categories|sonarr_resources|radarr_resources|jellyfin_libraries|anime_tag_labels)\(categories: list\[MediaCategory\]" tools/arrconf/arrconf/generators/categories.py` equals 5.
    - `grep -n "cfg: RootConfig" tools/arrconf/arrconf/generators/categories.py` returns NO match.
  </acceptance_criteria>
  <done>IntentConfig has categories + apps; RootConfig has no categories field and no legacy guard; the 5 generators take list[MediaCategory]; per-module tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire apply/diff call sites to source categories from intent_cfg; require intent</name>
  <files>tools/arrconf/arrconf/__main__.py, tools/arrconf/arrconf/diff_cmd.py</files>
  <read_first>
    - tools/arrconf/arrconf/__main__.py (apply lines 206-280; qbit branch ~375-385; jellyfin branch ~468-476; anime labels ~60-67; diff lines 789-960)
    - tools/arrconf/arrconf/diff_cmd.py (diff_sonarr/radarr/qbittorrent/jellyfin, lines 33-151)
    - tools/arrconf/arrconf/intent_config.py (load_intent — for the new required intent load in diff)
  </read_first>
  <action>
    In `tools/arrconf/arrconf/diff_cmd.py`:
    - Add `categories: list[MediaCategory]` as a parameter to `diff_sonarr`, `diff_radarr`, `diff_qbittorrent`, `diff_jellyfin` (NOT diff_prowlarr — it has no categories use). Add import `from arrconf.resources.categories import Category as MediaCategory`.
    - Replace `generate_sonarr_resources(root_config)` (line 46) → `generate_sonarr_resources(categories)`; `generate_radarr_resources(root_config)` (66) → `generate_radarr_resources(categories)`; `generate_qbit_categories(root_config)` (116) → `generate_qbit_categories(categories)`; `generate_jellyfin_libraries(root_config)` (149) → `generate_jellyfin_libraries(categories)`. Keep `root_config.sonarr["main"]` etc. (instance access) unchanged.

    In `tools/arrconf/arrconf/__main__.py` — apply():
    - After the existing optional intent load (lines 222-231), make intent REQUIRED for categories. Since `apply()` now needs `intent_cfg.categories`, change the guard: if `intent_cfg is None` (intent.yml absent) AND any of sonarr/radarr/qbittorrent/jellyfin is in targets, log `log.error("intent_required_for_categories", intent_path=str(intent_path))` and `raise typer.Exit(code=2)`. (Categories live in intent post-CATMIG; without it the *arr generators have nothing to expand.)
    - Define `cats = intent_cfg.categories if intent_cfg else []` once after the load.
    - Line 67: `all_anime_labels = set(generate_anime_tag_labels(root))` → `set(generate_anime_tag_labels(cats))`.
    - Line 262: `generate_sonarr_resources(root)` → `generate_sonarr_resources(cats)`.
    - Line 300: `generate_radarr_resources(root)` → `generate_radarr_resources(cats)`.
    - Line 380: `generate_qbit_categories(root)` → `generate_qbit_categories(cats)`.
    - Line 472: `generate_jellyfin_libraries(root)` → `generate_jellyfin_libraries(cats)`.

    In `tools/arrconf/arrconf/__main__.py` — diff():
    - diff() currently does NOT load intent. Add the same optional load used in apply(): read `intent_path = ctx.obj["intent_path"]`, `intent_cfg = load_intent(intent_path) if intent_path.exists() else None` wrapped in try/except ConfigError → exit 2 (mirror apply lines 222-231). Apply the same `intent_required_for_categories` guard for sonarr/radarr/qbittorrent/jellyfin targets.
    - `cats = intent_cfg.categories if intent_cfg else []`.
    - Update diff call sites: `diff_sonarr(client, root)` (834) → `diff_sonarr(client, root, cats)`; `diff_radarr(..., root)` (861) → `diff_radarr(..., root, cats)`; `diff_qbittorrent(..., root)` (912) → `diff_qbittorrent(..., root, cats)`; `diff_jellyfin(..., root)` (957) → `diff_jellyfin(..., root, cats)`. diff_prowlarr unchanged.

    Do NOT touch `generate()` (line 1001) — that is Plan 02.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "generate_(sonarr_resources|radarr_resources|qbit_categories|jellyfin_libraries|anime_tag_labels)\(cats\)" tools/arrconf/arrconf/__main__.py` equals 5.
    - `grep -n "generate_.*\(root\)" tools/arrconf/arrconf/__main__.py` returns NO match in apply/diff (lines < 1000).
    - `grep -cE "diff_(sonarr|radarr|qbittorrent|jellyfin)\(.*cats\)" tools/arrconf/arrconf/__main__.py` equals 4.
    - `grep -n "intent_required_for_categories" tools/arrconf/arrconf/__main__.py` returns at least 2 matches (apply + diff).
    - `grep -cE "categories: list\[MediaCategory\]" tools/arrconf/arrconf/diff_cmd.py` is >= 4.
    - mypy arrconf exits 0; full pytest suite passes (run twice if order-flaky per MEMORY note — isolate the known phase10/jellyfin flakies if they reappear).
  </acceptance_criteria>
  <done>apply/diff source categories from intent_cfg.categories; both require intent when *arr targets selected; diff_cmd functions accept categories; triade green; full suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate arrconf + intent JSON schemas</name>
  <files>schemas/arrconf-schema.json, schemas/intent-schema.json</files>
  <read_first>
    - tools/arrconf/arrconf/__main__.py (schema-gen + intent-schema-gen commands, lines 966-986)
    - schemas/arrconf-schema.json (current — categories will be removed)
    - schemas/intent-schema.json (current — categories + apps will be added)
  </read_first>
  <action>
    Regenerate both JSON schemas to reflect the schema changes from Tasks 1-2. From `tools/arrconf/`:
    - `uv run arrconf schema-gen --output ../../schemas/arrconf-schema.json` (RootConfig no longer has `categories` → schema loses that property).
    - `uv run arrconf intent-schema-gen --output ../../schemas/intent-schema.json` (IntentConfig gains `categories` array + `apps` object → schema adds them).
    Commit both regenerated files. The CI step "Verify schema reproducibility" (tests.yml line 54) blocks if these drift, so they MUST be in sync with the code.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run arrconf schema-gen --output ../../schemas/arrconf-schema.json && uv run arrconf intent-schema-gen --output ../../schemas/intent-schema.json && cd ../.. && git diff --exit-code -- schemas/arrconf-schema.json schemas/intent-schema.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"categories"' schemas/arrconf-schema.json` equals 0 (categories removed from RootConfig schema).
    - `grep -c '"categories"' schemas/intent-schema.json` is >= 1 (categories added to IntentConfig schema).
    - `grep -c '"apps"' schemas/intent-schema.json` is >= 1.
    - Re-running both schema-gen commands produces NO git diff (reproducible).
  </acceptance_criteria>
  <done>Both schemas regenerated, deterministic, committed; arrconf schema lost categories, intent schema gained categories + apps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → intent.yml | Hand-edited YAML; now also carries the `apps:` pass-through block that may reference `${ENV}` secret tokens (torznab URLs, qbit creds) |
| intent.yml → IntentConfig (pydantic) | Untrusted YAML parsed into typed model; `apps` is `dict[str, Any]` (no validation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | IntentConfig.apps (untyped dict pass-through) | accept | D-32-01 YAGNI — single trusted operator, no external input; `extra="forbid"` still rejects unknown TOP-LEVEL keys (categories/apps/tools/sagas only). Malformed nested app config surfaces at apply()-time via downstream RootConfig validation of the generated arrconf.yml (Plan 02). |
| T-32-02 | Information disclosure | secrets in intent.yml apps block | mitigate | This plan does NOT emit files. Schema only describes shapes (no values). No secret value is hardcoded into Python or schemas. Env-token interpolation (`${...}`) handled at emit-time in Plan 02 — verified there. |
| T-32-03 | Denial of service | apply/diff with missing intent.yml | accept | New `intent_required_for_categories` guard fails fast with exit 2 + clear structlog event — no silent partial reconcile (consistent with existing missing_api_key gates). |
</threat_model>

<verification>
- `cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf` → exit 0.
- `cd tools/arrconf && uv run pytest -q` → all pass.
- `cd tools/arrconf && uv run arrconf schema-gen --output ../../schemas/arrconf-schema.json && uv run arrconf intent-schema-gen --output ../../schemas/intent-schema.json && cd ../.. && git diff --exit-code -- schemas/` → no drift.
- Negative test: `RootConfig.model_validate({"categories": []})` raises ValidationError (extra_forbidden).
</verification>

<success_criteria>
- IntentConfig owns categories + apps; RootConfig cannot accept categories (CATMIG-01 schema half).
- Generators are config-type-agnostic (take list[MediaCategory]).
- apply/diff functional end-to-end sourcing categories from intent.
- Triade green; both schemas reproducible.
</success_criteria>

<output>
After completion, create `.planning/phases/32-categories-migration-hard-cut/32-01-SUMMARY.md`
</output>
