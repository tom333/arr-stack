---
phase: 29-sagas
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/arr-stack/files/arrconf.yml
autonomous: true
requirements: [SAGAS-03]
must_haves:
  truths:
    - "the tmdbboxsets plugin entry is present in jellyfin.main.plugins.required so the existing two-run _reconcile_plugins installs (Run N) then enables (Run N+1) it"
    - "the plugin GUID is exactly bc4aad2e-d3d0-4725-a5e2-fd07949e5b42 and the repo URL matches an already-declared plugin_repositories entry"
    - "no new Python code and no arrconf.image.tag co-bump (arrconf.yml ConfigMap-only change)"
  artifacts:
    - path: "charts/arr-stack/files/arrconf.yml"
      provides: "tmdbboxsets plugin desired entry under jellyfin.main.plugins.required"
      contains: "bc4aad2e-d3d0-4725-a5e2-fd07949e5b42"
  key_links:
    - from: "charts/arr-stack/files/arrconf.yml jellyfin.main.plugins.required"
      to: "reconcilers/jellyfin.py::_reconcile_plugins (existing, ADR-9)"
      via: "two-run install/enable from the plugins.required list"
      pattern: "install_guid"
---

<objective>
Add the `tmdbboxsets` plugin entry to `charts/arr-stack/files/arrconf.yml` so the existing ADR-9 two-run plugin reconciler (`_reconcile_plugins`) installs and enables it. Zero new Python; the plugin auto-creates movie BoxSets from TMDB collection metadata once active — no per-saga arrconf config needed. This plan touches only the arrconf.yml ConfigMap → NO co-bump of `arrconf.image.tag` (CLAUDE.md exception: `.yml`-only changes outside `tools/arrconf/**` do not bump the image).

Purpose: SAGAS-03 — arrconf presents movie sagas in Jellyfin via the `tmdbboxsets` plugin (best-effort, two-run model).

Output: one plugin entry added under `jellyfin.main.plugins.required`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-sagas/29-CONTEXT.md
@.planning/phases/29-sagas/29-RESEARCH.md
@.planning/phases/29-sagas/29-PATTERNS.md

<interfaces>
<!-- Existing arrconf.yml jellyfin.main.plugins.required (lines 299-316): entries are either bare `- name: "X"`
     or full install entries with install_guid/install_version/install_repo_url (+ optional config).
     The Intro Skipper entry (lines 308-316) is the exact analog for a two-run install entry. -->
<!-- plugin_repositories (lines 291-298) already includes "Jellyfin Stable" → https://repo.jellyfin.org/files/plugin/manifest.json
     — the SAME URL tmdbboxsets uses, so NO new repo entry is needed. -->
<!-- Verified plugin identity (29-RESEARCH §VALIDATE D-04):
     name "TMDb Box Sets", GUID bc4aad2e-d3d0-4725-a5e2-fd07949e5b42, version 13.0.0.0,
     repo URL https://repo.jellyfin.org/files/plugin/manifest.json. No config block needed (plugin auto-configures via defaults). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tmdbboxsets plugin entry to arrconf.yml</name>
  <files>charts/arr-stack/files/arrconf.yml</files>
  <read_first>
    - charts/arr-stack/files/arrconf.yml lines 291-316 (plugin_repositories + the Intro Skipper install entry — exact analog for an install entry shape: name + install_guid + install_version + install_repo_url)
    - 29-RESEARCH.md "[VALIDATE D-04] tmdbboxsets plugin" (GUID, version 13.0.0.0, repo URL, "no config block needed")
    - 29-PATTERNS.md "Two-run plugin model — tmdbboxsets wiring (SAGAS-03)"
    - CLAUDE.md "Release pin co-bump pattern" exception clause (arrconf.yml-only ≠ co-bump)
  </read_first>
  <action>
    In charts/arr-stack/files/arrconf.yml, under `jellyfin.main.plugins.required` (after the Intro Skipper entry that ends ~line 316), append a new install entry, matching the Intro Skipper entry's indentation exactly:

    ```yaml
    # Phase 29 (SAGAS-03): TMDb Box Sets install (two-run, ADR-9). Auto-groups movies
    # sharing a TMDB collection into Jellyfin BoxSets via its scheduled task — no per-saga
    # config needed. Repo URL matches the "Jellyfin Stable" plugin_repositories entry above.
    - name: "TMDb Box Sets"
      install_guid: "bc4aad2e-d3d0-4725-a5e2-fd07949e5b42"
      install_version: "13.0.0.0"
      install_repo_url: "https://repo.jellyfin.org/files/plugin/manifest.json"
    ```

    Do NOT add a `config:` block (the plugin auto-configures via defaults — MinimumNumberOfMovies=2, StripCollectionKeywords=false). Do NOT add a new plugin_repositories entry (the repo URL already matches the "Jellyfin Stable" entry on lines 291-294). Do NOT bump `charts/arr-stack/values.yaml#arrconf.image.tag` — this is an arrconf.yml ConfigMap-only change with zero Python.
  </action>
  <verify>
    <automated>grep -q 'bc4aad2e-d3d0-4725-a5e2-fd07949e5b42' charts/arr-stack/files/arrconf.yml && python3 -c "from ruamel.yaml import YAML; YAML(typ='safe').load(open('charts/arr-stack/files/arrconf.yml'))" 2>/dev/null || python3 -c "import yaml; yaml.safe_load(open('charts/arr-stack/files/arrconf.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'bc4aad2e-d3d0-4725-a5e2-fd07949e5b42' charts/arr-stack/files/arrconf.yml` succeeds.
    - `grep -q 'TMDb Box Sets' charts/arr-stack/files/arrconf.yml` succeeds.
    - `grep -c 'repo.jellyfin.org/files/plugin/manifest.json' charts/arr-stack/files/arrconf.yml` returns ≥2 (the existing Jellyfin Stable repo entry + the new install entry) — no NEW plugin_repositories block was added (still exactly the original repository entries; verify by `grep -c 'plugin_repositories' charts/arr-stack/files/arrconf.yml` == 1).
    - YAML parses without error (verify command exits 0).
    - This task's change set is arrconf.yml-only (no values.yaml co-bump): `git diff --name-only HEAD | grep -qx 'charts/arr-stack/files/arrconf.yml'` succeeds AND `git diff --name-only HEAD | grep -qx 'charts/arr-stack/values.yaml'` returns NO match. (Order-independent — asserts this plan's own diff, not shared working-tree state 29-01 may have committed.)
  </acceptance_criteria>
  <done>tmdbboxsets install entry present under jellyfin.main.plugins.required with the exact GUID/version/repo URL; YAML valid; no co-bump; no new repo entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arrconf.yml desired plugin config → Jellyfin plugin install (existing _reconcile_plugins) | declarative plugin entry drives a network install/enable on Jellyfin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-12 | Tampering | wrong plugin GUID/version installed | mitigate | GUID `bc4aad2e-d3d0-4725-a5e2-fd07949e5b42` and version 13.0.0.0 verified from the official plugin repo (29-RESEARCH §VALIDATE D-04); repo URL matches the already-trusted "Jellyfin Stable" manifest (Task 1). |
| T-29-13 | Elevation of Privilege (scope) | plugin entry config | accept | No config block, no quality_profile interaction; plugin operates only on Jellyfin BoxSet metadata. ADR-5 boundary untouched. |
| T-29-14 | Availability | install not active until Jellyfin restart | accept | ADR-9 two-run model already handles this (Run N install-queued + kubectl rollout restart hint; Run N+1 enable); best-effort, never fails the phase. |
</threat_model>

<verification>
- `grep -q 'bc4aad2e-d3d0-4725-a5e2-fd07949e5b42' charts/arr-stack/files/arrconf.yml` — entry present.
- YAML parses cleanly.
- This plan's `files_modified` frontmatter does NOT list `charts/arr-stack/values.yaml` (no co-bump for an arrconf.yml-only change), and this task touches only `charts/arr-stack/files/arrconf.yml`: `git diff --name-only HEAD | grep -qx 'charts/arr-stack/files/arrconf.yml'` succeeds while `git diff --name-only HEAD | grep -qx 'charts/arr-stack/values.yaml'` returns NO match. (Order-independent: asserts THIS plan's own change set, not a shared working-tree state that 29-01 may have already committed.)
</verification>

<success_criteria>
- SAGAS-03: tmdbboxsets installed/enabled via the existing two-run model; movie BoxSets auto-created from TMDB metadata once active. No new Python, no co-bump.
</success_criteria>

<output>
After completion, create `.planning/phases/29-sagas/29-04-SUMMARY.md`.
</output>
