---
phase: 29-sagas
plan: 03
type: execute
wave: 3
depends_on: [29-01, 29-02]
files_modified:
  - tools/arrconf/arrconf/reconcilers/jellyfin.py
  - tools/arrconf/arrconf/__main__.py
  - tools/arrconf/tests/test_reconcilers_jellyfin_sagas.py
  - charts/arr-stack/values.yaml
autonomous: true
requirements: [SAGAS-04]
must_haves:
  truths:
    - "_reconcile_sagas_boxsets resolves series titles to Jellyfin item GUIDs via exact Name match; unresolved titles log warning + skip (best-effort)"
    - "POST /Collections fires only when no BoxSet with the saga name exists (GET-before-POST, no duplicate BoxSets)"
    - "an existing BoxSet gets members added via POST /Collections/{id}/Items (idempotent add-only)"
    - "member series are tagged arrconf-managed in Sonarr via the existing tag helper"
    - "apply invokes the Jellyfin saga branch only for kind=series sagas when jellyfin is a target"
  artifacts:
    - path: "tools/arrconf/arrconf/reconcilers/jellyfin.py"
      provides: "_reconcile_sagas_boxsets function (GET-before-POST idempotence)"
      contains: "def _reconcile_sagas_boxsets"
  key_links:
    - from: "tools/arrconf/arrconf/reconcilers/jellyfin.py"
      to: "Jellyfin /Items + /Collections"
      via: "GET BoxSet snapshot, POST /Collections only if name absent"
      pattern: "Collections"
    - from: "tools/arrconf/arrconf/__main__.py"
      to: "_reconcile_sagas_boxsets"
      via: "jellyfin saga branch guarded by intent_cfg + jellyfin target"
      pattern: "_reconcile_sagas_boxsets"
---

<objective>
Build the series-saga Jellyfin presentation: `_reconcile_sagas_boxsets()` (GET-before-POST name check so `POST /Collections` never duplicates; idempotent member add via `POST /Collections/{id}/Items`; best-effort title resolution), tag member series `arrconf-managed` in Sonarr, and wire the Jellyfin saga branch into `apply`. Best-effort (ADR-9 spirit): unresolved titles warn-and-continue; phase exits 0.

Purpose: SAGAS-04 — series sagas presented via curated Jellyfin BoxSet + arrconf-managed Sonarr tag (no Sonarr Collections reconciler — Sonarr has none).

Output: new Jellyfin reconciler function + Sonarr tagging sub-step + apply wiring + respx tests + co-bumped tag (idempotent if already 0.19.0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-sagas/29-CONTEXT.md
@.planning/phases/29-sagas/29-RESEARCH.md
@.planning/phases/29-sagas/29-PATTERNS.md
@.planning/phases/29-sagas/29-01-PLAN.md
@.planning/phases/29-sagas/29-02-PLAN.md

<interfaces>
<!-- From 29-01: SagaEntry kind=series has items: list[str] (exact series titles), name = BoxSet name. -->
<!-- From 29-02: __main__.py apply() already has the `if intent_cfg is not None and intent_cfg.sagas:` block with the Radarr branch; this plan ADDS the Jellyfin branch inside the SAME block. -->
<!-- Jellyfin Collections API (verified, 29-RESEARCH §HIGH-RISK VALIDATE D-05):
     - NO GET /Collections. List BoxSets: GET /Items?includeItemTypes=BoxSet&recursive=true&fields=Name,ProviderIds → {"Items":[{Name,Id,...}]}.
     - POST /Collections?name=<str>&ids=<comma-guids>  — NOT idempotent (creates Name2 dupes) → MUST GET-before-POST by name (Pitfall 16-1 mirror, exactly like _reconcile_libraries).
     - POST /Collections/{id}/Items?ids=<comma-guids>  — IS idempotent (AddToCollectionAsync skips existing).
     - Member resolution: GET /Items?includeItemTypes=Series&recursive=true&searchTerm=<title>&fields=Name,ProviderIds → exact Name match (Pitfall 5: searchTerm is fuzzy, filter on Name==title). -->
<!-- A3 (open): POST /Collections/{id}/Items ids format — use lowercase UUID `str(guid)` comma-joined; validate live. Bake "use the Id string Jellyfin returns verbatim" note. -->
<!-- Existing jellyfin.py: _reconcile_libraries (lines 328-381) Pitfall 16-1 by_name snapshot; _reconcile_plugins (503-625) client._request POST with params + dry_run; JellyfinClient.get(path, params={...}) threads params to httpx; structlog `log`. -->
<!-- Sonarr tagging (A3 resolved): tag member series arrconf-managed inside the Jellyfin saga branch using a SonarrClient constructed in apply; reuse _ensure_managed_tag from reconcilers/sonarr.py (or radarr.py — confirm which exposes it for Sonarr). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _reconcile_sagas_boxsets in jellyfin.py</name>
  <files>tools/arrconf/arrconf/reconcilers/jellyfin.py, tools/arrconf/tests/test_reconcilers_jellyfin_sagas.py</files>
  <read_first>
    - tools/arrconf/arrconf/reconcilers/jellyfin.py (analogs: _reconcile_libraries lines 328-381 for by_name GET-before-POST snapshot; _reconcile_plugins lines 503-625 for client._request POST + params + dry_run + action strings; path constants ~lines 59-64; structlog `log`)
    - tools/arrconf/tests/test_reconcilers_jellyfin_plugin_install.py (respx boilerplate + JELLYFIN_BASE + client construction)
    - 29-PATTERNS.md "reconcilers/jellyfin.py — new _reconcile_sagas_boxsets() fn" (verbatim function body) + "Jellyfin BoxSet respx pattern"
    - 29-RESEARCH.md "Pattern 2: Jellyfin Series BoxSet Reconcile" + Pitfall 2 (GET-before-POST) + Pitfall 5 (fuzzy search → exact Name filter)
  </read_first>
  <behavior>
    - Test (created when absent): GET /Items?BoxSet returns {"Items":[]}; series search resolves member ids → POST /Collections fires once; actions contain "saga_boxset:created:<name>".
    - Test (idempotent add when exists): GET /Items?BoxSet returns a BoxSet named like the saga; resolved members → POST /Collections/{id}/Items fires (NOT POST /Collections); actions contain "saga_boxset:items_added:<name>".
    - Test (no duplicate create): existing BoxSet with matching Name → assert POST /Collections route call_count == 0.
    - Test (unresolved title→warn+skip): series search returns no exact Name match → "series_saga_member_unresolved" logged, that title contributes no id, function still returns (best-effort, no raise).
    - Test (no members + exists → no-op): existing BoxSet, all titles unresolved → "saga_boxset_no_op", no POST /{id}/Items.
    - Test (dry_run): absent BoxSet + dry_run=True → no POST; actions contain "saga_boxset:dry_run_create:<name>".
    - Test (empty input): _reconcile_sagas_boxsets(client, [], dry_run=False) returns [].
  </behavior>
  <action>
    In tools/arrconf/arrconf/reconcilers/jellyfin.py: add path constants `ITEMS_PATH = "/Items"` and `COLLECTIONS_PATH = "/Collections"` (only if absent). Add `from arrconf.intent_config import SagaEntry`. Implement `_reconcile_sagas_boxsets(client: JellyfinClient, series_sagas: list[SagaEntry], dry_run: bool) -> list[str]` verbatim per 29-PATTERNS.md: early-return [] if empty; Step 1 GET BoxSets via `client.get(ITEMS_PATH, params={"includeItemTypes":"BoxSet","recursive":"true","fields":"Name,ProviderIds"})` → `existing_by_name: dict[str,str]`; Step 2 per saga resolve member titles via `client.get(ITEMS_PATH, params={"includeItemTypes":"Series","recursive":"true","searchTerm":title,"fields":"Name,ProviderIds"})` filtering `item["Name"] == title` exactly (Pitfall 5), warn+skip unresolved; Step 3 if `saga.name not in existing_by_name` → dry_run/POST `client._request("POST", COLLECTIONS_PATH, params={"name":saga.name,"ids":",".join(resolved_ids)})`, else add-only `client._request("POST", f"{COLLECTIONS_PATH}/{collection_id}/Items", params={"ids":",".join(resolved_ids)})` (skip if no resolved_ids → no-op). Use the resolved Jellyfin `Id` string verbatim from the API response (A3 — do not reformat the GUID).

    Write tools/arrconf/tests/test_reconcilers_jellyfin_sagas.py covering all <behavior> cases, mirroring tests/test_reconcilers_jellyfin_plugin_install.py respx idiom (`@pytest.mark.respx(base_url=JELLYFIN_BASE, assert_all_called=False)`; route GET /Items with different responses by call; POST /Collections + POST /Collections/{id}/Items routes via url__regex; assert call_counts). NO live API calls.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run pytest tests/test_reconcilers_jellyfin_sagas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd tools/arrconf && uv run pytest tests/test_reconcilers_jellyfin_sagas.py -q` exits 0 with ≥7 passing tests.
    - `grep -q 'def _reconcile_sagas_boxsets' tools/arrconf/arrconf/reconcilers/jellyfin.py` succeeds.
    - `grep -q 'includeItemTypes.*BoxSet\|"BoxSet"' tools/arrconf/arrconf/reconcilers/jellyfin.py` succeeds (GET-before-POST snapshot present).
    - `grep -q 'item\["Name"\] == title\|Name.*== title' tools/arrconf/arrconf/reconcilers/jellyfin.py` succeeds (exact-match filter, Pitfall 5).
    - A "no duplicate create" test asserts `POST /Collections` call_count == 0 when the BoxSet name already exists.
    - `cd tools/arrconf && uv run mypy arrconf` reports 0 errors.
  </acceptance_criteria>
  <done>_reconcile_sagas_boxsets implemented with GET-before-POST idempotence + best-effort title resolution; ≥7 respx tests pass; no duplicate-create.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Jellyfin saga branch + Sonarr arrconf-managed tagging into apply + co-bump</name>
  <files>tools/arrconf/arrconf/__main__.py, charts/arr-stack/values.yaml</files>
  <read_first>
    - tools/arrconf/arrconf/__main__.py (analogs: existing Jellyfin branch lines 442-472 for JellyfinClient construction `JellyfinClient(base_url=root.jellyfin["main"].base_url, api_key=settings.jellyfin_api_key.get_secret_value())`; the 29-02 saga `if intent_cfg is not None and intent_cfg.sagas:` block; the sonarr branch for SonarrClient construction + _ensure_managed_tag access)
    - 29-PATTERNS.md "__main__.py" Step 3 (verbatim Jellyfin saga branch)
    - 29-RESEARCH.md Open Question 3 (Sonarr tagging lives in the Jellyfin saga branch, using a SonarrClient; tag only if "sonarr" in targets)
    - charts/arr-stack/values.yaml line 451 (tag)
  </read_first>
  <action>
    In tools/arrconf/arrconf/__main__.py, INSIDE the existing `if intent_cfg is not None and intent_cfg.sagas:` block (added in 29-02), AFTER the Radarr Collections branch, add the Jellyfin saga branch per 29-PATTERNS.md Step 3: guard `if "jellyfin" in targets and "main" in root.jellyfin and settings.jellyfin_api_key:`; lazily import `_reconcile_sagas_boxsets`; construct `JellyfinClient(base_url=root.jellyfin["main"].base_url, api_key=settings.jellyfin_api_key.get_secret_value())`; `series_sagas = [s for s in intent_cfg.sagas if s.kind == "series"]`; call `_reconcile_sagas_boxsets(client, series_sagas, dry_run=dry_run or settings.arrconf_dry_run)`; log `apply_complete app="jellyfin_sagas"`; on `(ApiClientError, ReconcileError) as e` log `app_failed` + `failures.append("jellyfin_sagas")`.

    Sonarr tagging sub-step (A3 resolved): within the same Jellyfin saga branch, if `"sonarr" in targets and settings.sonarr_api_key`, construct a SonarrClient (mirror the sonarr branch construction) and tag each resolved member series with `arrconf-managed` using the existing `_ensure_managed_tag` helper (confirm its exact signature/module — reconcilers/sonarr.py). Resolve series by the same `items` titles. If the helper needs series ids rather than titles, resolve via the Sonarr `GET /series` list matching by `title` exact-match (mirror existing reconciler matching). Best-effort: a tagging failure logs a warning and does not fail the phase beyond the existing failures mechanism. Keep this sub-step minimal — the primary deliverable is the BoxSet; tagging is the secondary tier per the Responsibility Map.

    Co-bump charts/arr-stack/values.yaml line 451 to `tag: "0.19.0"` (idempotent — leave if already set).
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf && uv run pytest tests/test_reconcilers_jellyfin_sagas.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '_reconcile_sagas_boxsets' tools/arrconf/arrconf/__main__.py` succeeds.
    - `grep -q 'jellyfin_sagas' tools/arrconf/arrconf/__main__.py` appears in both apply_complete and failures.append contexts.
    - `grep -q 'arrconf-managed\|_ensure_managed_tag' tools/arrconf/arrconf/__main__.py` succeeds (Sonarr tagging sub-step present).
    - `grep -q 'tag: "0.19.0"' charts/arr-stack/values.yaml` succeeds.
    - `cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf` all green.
  </acceptance_criteria>
  <done>Jellyfin saga branch + Sonarr arrconf-managed tagging wired inside the saga block; failures tracked; tag at 0.19.0; triade green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arrconf → Jellyfin /Items + /Collections (env JELLYFIN_API_KEY) | arrconf creates BoxSets and adds members across the network boundary |
| operator title strings → Jellyfin item resolution | untrusted fuzzy-matched titles resolved to library item GUIDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08 | Tampering / Integrity | POST /Collections duplicate BoxSets | mitigate | GET /Items?BoxSet snapshot + name check BEFORE POST /Collections (Pitfall 16-1 mirror); POST /Collections never fires when name exists (Task 1, asserted by call_count==0 test). |
| T-29-09 | Denial of Service / wrong-item writes | ambiguous/missing title resolution | mitigate | Exact `Name == title` filter after fuzzy searchTerm (Pitfall 5); unresolved titles log warning + skip — no unbounded or wrong-item writes; best-effort, phase still exits 0 (Task 1). |
| T-29-10 | Elevation of Privilege (scope) | Sonarr tagging | accept | Tagging uses arrconf-managed tag via existing helper; no quality_profile/custom_format write; ADR-5 boundary held (Task 2). |
| T-29-11 | Availability | Jellyfin presentation pending plugin/library state | accept | Best-effort per ADR-9 spirit: failures append to `failures` list but unresolved members do not crash apply; D-07 contract (phase exits 0 when presentation pending). |
</threat_model>

<verification>
- `cd tools/arrconf && uv run pytest tests/test_reconcilers_jellyfin_sagas.py -q` — ≥7 pass incl. no-duplicate-create.
- `cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf` — green.
- `grep -q 'tag: "0.19.0"' charts/arr-stack/values.yaml` — co-bump present.
</verification>

<success_criteria>
- SAGAS-04: series sagas presented via curated Jellyfin BoxSet (GET-before-POST, idempotent member add) + arrconf-managed Sonarr tag; Jellyfin presentation is the only automation (no Sonarr Collections reconciler).
- Best-effort contract held: unresolved titles warn-and-continue; phase exits 0 (D-07).
</success_criteria>

<output>
After completion, create `.planning/phases/29-sagas/29-03-SUMMARY.md`.
</output>
