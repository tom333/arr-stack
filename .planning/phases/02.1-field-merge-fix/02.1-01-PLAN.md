---
phase: 02.1-field-merge-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - snapshots/before-phase-2.1-2026-05-08/
autonomous: true
requirements:
  - REQ-idempotence
must_haves:
  truths:
    - "ADR-6 discipline honored: a fresh raw snapshot exists for the scope this phase will touch (sonarr + qbittorrent) BEFORE any code change is shipped to cluster"
    - "snapshot dir `snapshots/before-phase-2.1-2026-05-08/sonarr/` is populated with the 5+ JSON files snapshot.sh emits per app"
    - "snapshot dir `snapshots/before-phase-2.1-2026-05-08/qbittorrent/` is populated (qBit is the Phase 2.1 root-cause counterparty — its credential state is the canary)"
    - "Snapshot is committed in git (NOT gitignored — CLAUDE.md anti-pattern check)"
    - "Pre-fix `tag.json` for sonarr captures the orphan `arrconf-managed` (id=1) entry, so post-PR3 diff can confirm the tag now ATTACHES to the download_client (Phase 2 success #4 closure proof)"
    - "Pre-fix `downloadclient.json` for sonarr shows `tags: []` on the qBit entry — used as the dispositive before/after pair when Plan 04 captures `tags: [1]`"
  artifacts:
    - path: "snapshots/before-phase-2.1-2026-05-08/sonarr/tag.json"
      provides: "Pre-fix Sonarr tag list — must contain the orphan arrconf-managed tag from Phase 2 partial run"
      contains: "arrconf-managed"
    - path: "snapshots/before-phase-2.1-2026-05-08/sonarr/downloadclient.json"
      provides: "Pre-fix Sonarr downloadclient — qBit entry must show `tags: []` (proof the tag never attached pre-fix)"
    - path: "snapshots/before-phase-2.1-2026-05-08/qbittorrent/"
      provides: "Pre-fix qBittorrent state — categories + preferences captured for forensic comparison"
  key_links:
    - from: "snapshot.sh --apps sonarr,qbittorrent"
      to: "snapshots/before-phase-2.1-2026-05-08/"
      via: "shell invocation with --output flag"
      pattern: "tools/snapshot/snapshot\\.sh.*--output snapshots/before-phase-2\\.1-"
    - from: "snapshots/before-phase-2.1-2026-05-08/"
      to: "git history (committed)"
      via: "git add + commit (CLAUDE.md: NEVER ignore snapshots/)"
      pattern: "snapshot\\(02\\.1\\)"
---

<objective>
Wave 1: Re-snapshot the Sonarr + qBittorrent state BEFORE any code change ships to cluster. ADR-6 mandates a fresh forensic baseline whenever a phase touches a new scope OR re-touches a scope where prior runs were partial. Phase 2 closed PARTIAL with `arrconf-managed` orphan tag id=1 in Sonarr and qBit downloadclient unchanged; this snapshot is the dispositive before-pair for the after-pair Plan 04 will capture (closes Phase 2 success #4 + #5 + Phase 1 HUMAN-UAT #3).

Purpose: Lock in the canary state so Plan 04's post-PR3 diff is auditable end-to-end. Without this snapshot, the closure proof for ROADMAP success #4 ("downloadclient gains arrconf-managed tag id") is just an assertion — with it, it becomes a verifiable git diff.

Output: `snapshots/before-phase-2.1-2026-05-08/{sonarr,qbittorrent}/*.json` committed in arr-stack.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/STATE.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-SUMMARY.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh

<interfaces>
<!-- snapshot.sh CLI surface (canonical from tools/snapshot/snapshot.sh L36-50) -->
# Usage:
#   tools/snapshot/snapshot.sh                              # all 6 apps -> snapshots/baseline-<date>/
#   tools/snapshot/snapshot.sh --apps sonarr,qbittorrent    # subset
#   tools/snapshot/snapshot.sh --output snapshots/before-phase-2.1-$(date +%F)/
#
# Refuses to run as root (script L31). Requires KNOWN_APPS env auth — see tools/snapshot/README.md.
# Per-app output: snapshots/<output>/<app>/<resource>.json (one JSON per resource type).
# 5+ JSON files per app for sonarr (tag.json, downloadclient.json, rootfolder.json, system_status.json, ...).

<!-- Phase 2 STATE.md baseline assertions (carry-forward facts to assert in this snapshot) -->
# - Sonarr tag.json should contain {"id": 1, "label": "arrconf-managed"} — orphan from Phase 2 partial run
# - Sonarr downloadclient.json[0].tags should be [] — tag never attached pre-fix
# - qBittorrent state should reflect operational reality (real credentials, real categories) — used as forensic ref
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Capture pre-fix snapshot for sonarr + qbittorrent + commit</name>
  <files>
    snapshots/before-phase-2.1-2026-05-08/
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh (CLI surface, --apps + --output flags, refuses root, 6 known apps)
    - /home/moi/projets/perso/arr-stack/tools/snapshot/README.md (prereqs: port-forwards, env vars SONARR_API_KEY/QBT_USER/QBT_PASS/...)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-SUMMARY.md (cluster state — tag id=1 orphan in sonarr, qBit downloadclient unchanged)
    - /home/moi/projets/perso/arr-stack/CLAUDE.md "Workflow snapshot (CRITIQUE — à respecter avant tout test risqué)" + "Ne pas ignorer snapshots/"
  </read_first>
  <action>
    Step 1 — Establish port-forwards + env vars (same pattern Plan 02-01 Task 1.1 used):
        # In separate terminals or background jobs:
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        SONARR_PF=$!
        kubectl -n selfhost port-forward svc/qbittorrent 8080:8080 &
        QBIT_PF=$!
        sleep 2

        export SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"
        export QBT_USER="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.QBT_USER}' | base64 -d)"
        export QBT_PASS="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.QBT_PASS}' | base64 -d)"

    Step 2 — Capture the snapshot (only the 2 apps in scope for this phase):
        cd /home/moi/projets/perso/arr-stack
        SNAPSHOT_DIR="snapshots/before-phase-2.1-$(date +%F)"
        tools/snapshot/snapshot.sh --apps sonarr,qbittorrent --output "$SNAPSHOT_DIR/"

    Step 3 — Verify the snapshot landed and the carry-forward state from Phase 2 is captured:
        # Sonarr tag.json must contain the orphan arrconf-managed tag (id=1, from Phase 2 partial run):
        jq -e 'map(.label) | any(. == "arrconf-managed")' "$SNAPSHOT_DIR/sonarr/tag.json"
        # Expected: exits 0

        # Sonarr downloadclient.json[0].tags must be [] (tag was never attached pre-fix):
        TAGS_LEN=$(jq -e '.[0].tags | length' "$SNAPSHOT_DIR/sonarr/downloadclient.json")
        echo "qBit downloadclient pre-fix tags length: $TAGS_LEN (expected: 0)"
        [ "$TAGS_LEN" = "0" ] || { echo "ERROR: pre-fix tags should be [] — Phase 2 PARTIAL state mismatch"; exit 1; }

        # qBittorrent dir must exist and be non-empty:
        find "$SNAPSHOT_DIR/qbittorrent" -type f -name '*.json' | head -1 | grep -q .

        # Sanity: at least 5 JSON files in sonarr/ (snapshot.sh emits multiple resources)
        SONARR_FILES=$(find "$SNAPSHOT_DIR/sonarr" -type f -name '*.json' | wc -l)
        echo "sonarr/ JSON file count: $SONARR_FILES (expected >= 5)"
        [ "$SONARR_FILES" -ge 5 ] || { echo "ERROR: snapshot incomplete"; exit 1; }

    Step 4 — Cleanup port-forwards:
        kill $SONARR_PF $QBIT_PF 2>/dev/null || true

    Step 5 — Commit the snapshot in arr-stack git (CLAUDE.md: NEVER add to .gitignore):
        git add "$SNAPSHOT_DIR/"
        git commit -m "snapshot(02.1): pre-fix capture sonarr+qbittorrent (ADR-6 discipline before code change)"
  </action>
  <acceptance_criteria>
    - `test -d /home/moi/projets/perso/arr-stack/snapshots/before-phase-2.1-2026-05-08` exits 0
    - `find /home/moi/projets/perso/arr-stack/snapshots/before-phase-2.1-2026-05-08/sonarr -type f -name '*.json' | wc -l` >= 5
    - `find /home/moi/projets/perso/arr-stack/snapshots/before-phase-2.1-2026-05-08/qbittorrent -type f -name '*.json' | wc -l` >= 1
    - `jq -e 'map(.label) | any(. == "arrconf-managed")' /home/moi/projets/perso/arr-stack/snapshots/before-phase-2.1-2026-05-08/sonarr/tag.json` exits 0 (carry-forward state from Phase 2 PARTIAL)
    - `jq -e '.[0].tags | length == 0' /home/moi/projets/perso/arr-stack/snapshots/before-phase-2.1-2026-05-08/sonarr/downloadclient.json` exits 0 (tag NOT attached pre-fix — dispositive before-pair for Plan 04)
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s | grep -q 'snapshot(02\.1):'` exits 0
    - `git -C /home/moi/projets/perso/arr-stack ls-files snapshots/before-phase-2.1-2026-05-08/ | wc -l` >= 6 (snapshot is committed, NOT gitignored)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack &amp;&amp; test -d snapshots/before-phase-2.1-2026-05-08 &amp;&amp; [ "$(find snapshots/before-phase-2.1-2026-05-08/sonarr -type f -name '*.json' 2&gt;/dev/null | wc -l)" -ge 5 ] &amp;&amp; [ "$(find snapshots/before-phase-2.1-2026-05-08/qbittorrent -type f -name '*.json' 2&gt;/dev/null | wc -l)" -ge 1 ] &amp;&amp; jq -e 'map(.label) | any(. == "arrconf-managed")' snapshots/before-phase-2.1-2026-05-08/sonarr/tag.json &gt; /dev/null &amp;&amp; jq -e '.[0].tags | length == 0' snapshots/before-phase-2.1-2026-05-08/sonarr/downloadclient.json &gt; /dev/null &amp;&amp; git log -1 --pretty=%s | grep -q 'snapshot(02\.1):' &amp;&amp; [ "$(git ls-files snapshots/before-phase-2.1-2026-05-08/ | wc -l)" -ge 6 ]</automated>
  </verify>
  <done>Pre-fix snapshot for sonarr + qbittorrent committed; tag.json shows orphan arrconf-managed; downloadclient.json shows `tags: []`; ADR-6 discipline honored before any code change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation -> Sonarr/qBit (port-forward + read API) | Trusted local action — only GET requests, zero writes |
| Snapshot files -> git history | Once committed, snapshot becomes auditable forever — must NOT contain plaintext secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | Information Disclosure | Snapshot files committed to git could leak credentials | mitigate | snapshot.sh already redacts privacy=password fields to `***REDACTED***` (verified Phase 0) — confirmed by inspection of `tests/fixtures/sonarr/downloadclient.json` line 65. Audit pattern: `grep -rE "(password\|api[_-]?key)" snapshots/before-phase-2.1-*/ | grep -v REDACTED` should produce zero hits. |
| T-02.1-02 | Tampering | Operator could accidentally trigger writes during snapshot | accept | snapshot.sh is a Bash-only read-only capture (curl GET / qBit login + GET only). No mutation primitives in the script. |
| T-02.1-03 | Repudiation | Without this snapshot, no forensic before-pair exists for the Plan 04 closure | mitigate | This task IS the mitigation — Plan 04's drift-test diff would be unauditable without it. |
</threat_model>

<verification>
Wave 1 gate (must all pass before Plan 02 starts):
1. `snapshots/before-phase-2.1-2026-05-08/` directory exists and is committed in git
2. sonarr/ has >= 5 JSON files; qbittorrent/ has >= 1 JSON file
3. Pre-fix carry-forward state verified: `arrconf-managed` tag present in sonarr/tag.json AND `tags: []` in sonarr/downloadclient.json[0]
4. Anti-leak audit: no plaintext credentials present in any snapshot file (`grep -rE "password.*[a-zA-Z0-9]{8,}" snapshots/before-phase-2.1-2026-05-08/ | grep -v REDACTED` returns 0 lines)
</verification>

<success_criteria>
- ADR-6 discipline honored before code change (snapshot present)
- Phase 2 PARTIAL state (orphan tag id=1, empty downloadclient tags) captured as forensic baseline
- Snapshot committed in git, not gitignored
- Anti-leak audit clean
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-field-merge-fix/02.1-01-SUMMARY.md` documenting:
- Snapshot directory path + total file count + per-app file count
- Carry-forward state assertions (tag.json contains arrconf-managed, downloadclient.json shows tags: [])
- Anti-leak audit result (number of REDACTED occurrences, zero plaintext leaks)
- git commit hash + message
- Recommended next step: `/gsd-execute-phase 02.1` Plan 02 (code change + tests)
</output>
