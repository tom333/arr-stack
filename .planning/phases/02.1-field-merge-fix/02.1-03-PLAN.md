---
phase: 02.1-field-merge-fix
plan: 03
type: execute
wave: 3
depends_on:
  - 02.1-02
files_modified:
  - my-kluster/charts/arrconf/values.yaml
  - my-kluster/charts/arrconf/files/arrconf.yml
autonomous: false
requirements:
  - REQ-secret-management
user_setup:
  - service: github-arr-stack
    why: "Tag v0.1.3 must trigger arrconf-image.yml workflow; operator pushes the tag and reviews/merges PR3 in my-kluster"
    dashboard_config:
      - task: "Verify GHCR package `arr-stack-arrconf` is still public after v0.1.3 push (Pitfall 7 sanity check)"
        location: "https://github.com/users/tom333/packages/container/arr-stack-arrconf"
must_haves:
  truths:
    - "Tag `v0.1.3` exists on origin (`git ls-remote --tags origin v0.1.3` returns a sha)"
    - "GitHub Actions workflow `arrconf-image.yml` ran successfully for v0.1.3 (gh run list shows the workflow status as `success`)"
    - "GHCR image `ghcr.io/tom333/arr-stack-arrconf:0.1.3` is publicly pullable (`docker pull` from logged-out daemon succeeds)"
    - "PR3 in my-kluster contains EXACTLY 2 file changes: `charts/arrconf/values.yaml` (1 line: image.tag bump 0.1.2 → 0.1.3) AND `charts/arrconf/files/arrconf.yml` (4-line deletion: drop username + password entries from qBit fields[])"
    - "PR3 merged to my-kluster main; ArgoCD Application `arrconf` is Synced + Healthy after the sync"
    - "ConfigMap `arrconf` in selfhost reflects the YAML cleanup (no `username`/`password` field entries — `kubectl get configmap arrconf -n selfhost -o yaml | grep -E 'username|password' | wc -l` returns 0)"
    - "CronJob spec shows the new image: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'` returns `ghcr.io/tom333/arr-stack-arrconf:0.1.3`"
    - "If CronJob was suspended at Phase 2 close (STATE.md notes manual `kubectl patch`), it is unsuspended after PR3 sync — operator confirms via `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returning `false` (or empty)"
    - "First post-PR3 smoke job (`arrconf-pr3-smoke-<ts>`) completes with exit code 0; logs show: `managed_tag_found` (id=1, carry-forward from Phase 2) + `plan_action action=update` + `merge_field_preserved` events for at least 2 fields (username + password) + a httpx 200 response on PUT /downloadclient/1 (NOT 400)"
    - "Renovate annotation `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` is preserved above the bumped tag (CLAUDE.md MANDATE — never strip)"
    - "Smoke job logs persisted to `evidence/pr3-job-logs-<date>.log` and committed in arr-stack"
  artifacts:
    - path: "my-kluster/charts/arrconf/values.yaml"
      provides: "Helm values — image.tag bumped 0.1.2 → 0.1.3 (D-37); Renovate annotation preserved"
      contains: "tag: \"0.1.3\""
    - path: "my-kluster/charts/arrconf/files/arrconf.yml"
      provides: "Reconciler config — username + password entries removed from qBit fields[] (D-36)"
    - path: ".planning/phases/02.1-field-merge-fix/evidence/pr3-job-logs-2026-05-08.log"
      provides: "Captured kubectl logs from the first PR3 (apply mode) Job — proof of merge_field_preserved + plan_action update + httpx 200"
  key_links:
    - from: "arr-stack git tag v0.1.3"
      to: ".github/workflows/arrconf-image.yml -> GHCR ghcr.io/tom333/arr-stack-arrconf:0.1.3"
      via: "push tag triggers `tags: ['v*']` workflow filter"
      pattern: "ghcr\\.io/tom333/arr-stack-arrconf:0\\.1\\.3"
    - from: "my-kluster PR3"
      to: "ArgoCD Application arrconf -> CronJob/arrconf in selfhost"
      via: "ArgoCD self-heal (~3 min after merge)"
      pattern: "image:.*0\\.1\\.3"
    - from: "Smoke job logs"
      to: "evidence/pr3-job-logs-<date>.log (committed in arr-stack)"
      via: "kubectl logs job/arrconf-pr3-smoke-<ts> > evidence/..."
      pattern: "merge_field_preserved"
---

<objective>
Wave 3: Cut release `v0.1.3`, verify the GHCR image is public + anonymously pullable, then open + merge PR3 in my-kluster bumping `image.tag` and dropping the `username`/`password` placeholder entries from `arrconf.yml`. After ArgoCD sync, force a smoke job and capture logs proving the field-merge works end-to-end (PUT 200, no 400).

This plan is the bridge from Wave 2's code change to Wave 4's closure proof. The two file edits in my-kluster (image bump + YAML cleanup) are atomic — they ship in ONE PR per D-37, ensuring the cluster never sees the old image with the new YAML or vice versa. The smoke job is the dispositive integration signal: if PUT 200 happens (replacing Phase 2 PR2's PUT 400), the merge contract works against a real Sonarr.

Three checkpoint-gated steps:
1. **Release** (autonomous): tag v0.1.3 on arr-stack main, push, wait for GitHub Actions, verify GHCR image is public + anonymously pullable
2. **PR3 author + merge** (human-action): operator opens, reviews, and merges PR3 in my-kluster (2-file diff)
3. **Smoke job + evidence** (autonomous after PR3 merged): force apply-mode job after ArgoCD sync, capture logs, commit evidence in arr-stack

Why non-autonomous: Step 2 requires operator review of PR3 before merge (CLAUDE.md "Ne pas merger une PR avec drift" + my-kluster GitOps strict). The other two steps are fully autonomous but gated by Step 2.

Output: GHCR image v0.1.3 published; my-kluster PR3 merged; CronJob running on v0.1.3 with cleaned YAML; smoke job logs in arr-stack evidence/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/STATE.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-02-SUMMARY.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-04-SUMMARY.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-SUMMARY.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/.github/workflows/arrconf-image.yml
@/home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml
@/home/moi/projets/perso/my-kluster/charts/arrconf/files/arrconf.yml

<interfaces>
<!-- Cross-repo coupling pattern (verbatim from 02-PATTERNS.md "Cross-Repo Coupling") -->
# 1. arr-stack: merge to main (Wave 2 already done) → green CI → tag v0.1.3 + push
# 2. CI arrconf-image.yml runs (verify with `gh run list -R tom333/arr-stack -w arrconf-image.yml`)
# 3. Verify GHCR public anonymous pull: `docker logout ghcr.io && docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.3`
# 4. ONLY THEN open my-kluster PR3 (atomic image-tag + YAML-cleanup)
# 5. Operator merges PR3 → ArgoCD selfHeal → CronJob unsuspended → smoke job → evidence

<!-- Current my-kluster state (must understand before editing) -->
# my-kluster/charts/arrconf/values.yaml (FULL — 30 lines):
#   L1-5: image block — line 4 currently `tag: "0.1.2"` (target: "0.1.3")
#   L7: schedule "0 */4 * * *"
#   L14: arrconfDryRun: false (left untouched — we want apply mode)
#   L26: apiKeysSecret: arrconf-env (untouched)

# my-kluster/charts/arrconf/files/arrconf.yml (FULL — 46 lines):
#   L23-26 to DELETE (4-line block):
#     - name: username
#       value: ''
#     - name: password
#       value: ''
#   L22 (urlBase: '') and L30 (tvImportedCategory: '') stay — they ARE empty in cluster too,
#   not credentials; D-36 covers username + password ONLY (other empties are handled implicitly
#   by D-31 merge logic on apply side and stay in YAML for clarity).

<!-- Phase 2 STATE.md carry-forward (cluster state at start of this plan) -->
# - CronJob arrconf in selfhost: ARRCONF_DRY_RUN=false (PR2 PR #1368 merged), but possibly suspended manually
# - arrconf-managed tag id=1 already exists in Sonarr (orphan from Phase 2 partial run)
# - Sonarr qBit downloadclient: tags=[] (carry-forward state)
# - Every 4h cron fires and fails with 400 — Phase 2.1 fix lands here

<!-- Apply-mode log signature (apply + merge events; W-06 + new merge_field_preserved) -->
# DISPOSITIVE for apply mode (these MUST appear in PR3 logs):
#   - "managed_tag_found"  (sonarr.py L53 — tag id=1 already exists from Phase 2)
#   - "plan_action"          action="update" diff_fields=[fields, tags]
#   - "merge_field_preserved" name="username" + name="password" (NEW from Plan 02 — proof helper ran)
# DISPOSITIVE for the fix working:
#   - httpx PUT /downloadclient/1 returns 200 (NOT 400 like Phase 2 PR2)
# DISPOSITIVE absences (apply-mode markers, NOT dry-run):
#   - "would_create_managed_tag" MUST be 0
#   - "dry_run_skip" MUST be 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Tag v0.1.3 + verify GHCR image public + anonymously pullable</name>
  <files>
    (no file edits — pure release operation; GHCR receives the new image tag)
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.github/workflows/arrconf-image.yml (workflow triggers — `tags: ['v*']`)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-02-SUMMARY.md ("v0.1.0 + v0.1.1 first-push race + uv:0.11 Dockerfile bug — fixed by db0f163; v0.1.2 was the first successful release")
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-CONTEXT.md "D-37 step 1-2"
    - /home/moi/projets/perso/arr-stack/CLAUDE.md "Release" + "Ne pas amender un tag de release publié"
  </read_first>
  <action>
    Step 1 — Verify Wave 2 is fully merged to arr-stack main:
        cd /home/moi/projets/perso/arr-stack
        git fetch origin
        git checkout main && git pull origin main
        # Confirm the Wave 2 commits are present:
        git log --oneline -5 | grep -E "fix\(02\.1\):" || { echo "ERROR: Wave 2 commits missing on main"; exit 1; }

    Step 2 — Tag v0.1.3 (annotated, NOT lightweight per CLAUDE.md release discipline):
        git tag -a v0.1.3 -m "Phase 2.1 — field-merge fix for sensitive YAML values

    - differ.py:merge_fields_for_put() preserves cluster credentials when YAML value is '' / None (D-31/D-32/D-33)
    - sonarr.py:_execute UPDATE branch wired through the merge helper
    - dump.py drops fields[] entries with value=='***REDACTED***' (D-36)
    - Test suite extended: unit (test_differ.py), integration (test_reconcilers_sonarr.py),
      round-trip (test_round_trip.py); existing canary still passes
    - Closes Phase 2 PR2 PUT 400 root cause; unblocks Phase 2 success #4 + #5"

        git push origin v0.1.3

    Step 3 — Wait for arrconf-image.yml workflow to complete:
        # The workflow filter is tags: ['v*'] in .github/workflows/arrconf-image.yml
        sleep 30  # let the trigger fire
        gh run list -R tom333/arr-stack -w arrconf-image.yml --limit 5

        # Wait for the v0.1.3 run to finish (block until status=completed):
        RUN_ID=$(gh run list -R tom333/arr-stack -w arrconf-image.yml --limit 10 --json databaseId,headBranch,headSha,event \
          --jq '.[] | select(.event=="push") | .databaseId' | head -1)
        gh run watch -R tom333/arr-stack "$RUN_ID" --exit-status

        # Verify status:
        gh run view -R tom333/arr-stack "$RUN_ID" --json conclusion --jq .conclusion
        # Expected: "success"

    Step 4 — Verify GHCR image is publicly pullable (Pitfall 7 sanity per 02-02-SUMMARY.md):
        docker logout ghcr.io 2>/dev/null || true
        docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.3
        # Should succeed without auth.

        # Capture digest for the SUMMARY:
        DIGEST=$(docker inspect ghcr.io/tom333/arr-stack-arrconf:0.1.3 --format '{{index .RepoDigests 0}}')
        echo "v0.1.3 image digest: $DIGEST"

    Step 5 — Verify the tag is on origin:
        git ls-remote origin refs/tags/v0.1.3 | grep -q v0.1.3 || { echo "ERROR: tag not on origin"; exit 1; }
  </action>
  <acceptance_criteria>
    - `git -C /home/moi/projets/perso/arr-stack ls-remote origin refs/tags/v0.1.3 | wc -l` returns at least `1`
    - `gh run list -R tom333/arr-stack -w arrconf-image.yml --limit 5 --json conclusion --jq '.[0].conclusion'` returns `"success"`
    - `docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.3` exits 0 from a logged-out daemon (`docker logout ghcr.io` first)
    - `docker manifest inspect ghcr.io/tom333/arr-stack-arrconf:0.1.3` exits 0 anonymously
    - `git -C /home/moi/projets/perso/arr-stack tag -l v0.1.3` returns `v0.1.3`
    - `git -C /home/moi/projets/perso/arr-stack cat-file -t v0.1.3` returns `tag` (annotated, NOT `commit`)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack &amp;&amp; [ "$(git ls-remote origin refs/tags/v0.1.3 | wc -l)" -ge 1 ] &amp;&amp; [ "$(git tag -l v0.1.3)" = "v0.1.3" ] &amp;&amp; [ "$(git cat-file -t v0.1.3)" = "tag" ] &amp;&amp; docker logout ghcr.io 2&gt;/dev/null; docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.3 &amp;&amp; docker manifest inspect ghcr.io/tom333/arr-stack-arrconf:0.1.3 &gt; /dev/null</automated>
  </verify>
  <done>v0.1.3 annotated tag pushed to origin; arrconf-image.yml workflow concluded with status=success; ghcr.io/tom333/arr-stack-arrconf:0.1.3 anonymously pullable; image digest recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3.2: Open + merge PR3 in my-kluster (image.tag bump + YAML cleanup) — operator review required</name>
  <files>
    my-kluster/charts/arrconf/values.yaml
    my-kluster/charts/arrconf/files/arrconf.yml
  </files>
  <read_first>
    - /home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml (verify currently `tag: "0.1.2"`)
    - /home/moi/projets/perso/my-kluster/charts/arrconf/files/arrconf.yml (verify L23-26 currently `username: ''` + `password: ''`)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-PATTERNS.md §9 + §10 (paste-ready diffs)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-CONTEXT.md "D-36 + D-37 step 3-5"
    - /home/moi/projets/perso/arr-stack/CLAUDE.md "Annotations Renovate (CRITIQUE)" — Renovate annotation MUST stay above `repository:` line
  </read_first>
  <what-built>
    Task 3.1 published v0.1.3 to GHCR. This task opens PR3 in my-kluster bumping `image.tag` 0.1.2 → 0.1.3 AND removing the `username: ''`/`password: ''` placeholder entries from `arrconf.yml`. Operator reviews + merges. After ArgoCD sync, the live CronJob runs the new image with cleaned YAML.

    Why non-autonomous: my-kluster is GitOps-strict (CLAUDE.md "GitOps strict — never `kubectl apply` chart resources"). PR review is a hard gate. Auto-merge is NOT permitted on this repo for non-Renovate PRs.
  </what-built>
  <action>
    Step 1 — Open PR3 in my-kluster (atomic 2-file change):
        cd /home/moi/projets/perso/my-kluster
        git checkout main && git pull origin main
        git checkout -b phase-2.1-arrconf-pr3

        # Edit 1 of 2 — bump image.tag in values.yaml (paste-ready diff from 02.1-PATTERNS.md §9):
        sed -i 's/^  tag: "0\.1\.2"$/  tag: "0.1.3"/' charts/arrconf/values.yaml

        # Verify ONLY ONE line changed in values.yaml AND Renovate annotation untouched:
        DIFF_LINES=$(git diff --numstat charts/arrconf/values.yaml | awk '{print $1+$2}')
        echo "values.yaml change lines: $DIFF_LINES (expected: 2 = 1 ins + 1 del)"
        [ "$DIFF_LINES" = "2" ] || { echo "ERROR: values.yaml diff is not exactly 1 line bump"; exit 1; }
        # Renovate annotation MUST still be present:
        grep -q "# renovate: image=ghcr.io/tom333/arr-stack-arrconf" charts/arrconf/values.yaml \
          || { echo "ERROR: Renovate annotation lost"; exit 1; }

        # Edit 2 of 2 — drop username + password entries from arrconf.yml (paste-ready from 02.1-PATTERNS.md §10).
        # Use python for safe YAML manipulation OR sed for the literal block delete:
        python3 - <<'PYEOF'
        from pathlib import Path
        p = Path("charts/arrconf/files/arrconf.yml")
        text = p.read_text()
        # Delete the 4-line block exactly:
        block = "              - name: username\n                value: ''\n              - name: password\n                value: ''\n"
        if block not in text:
            raise SystemExit(f"ERROR: expected block not found in {p}; manual edit required")
        new_text = text.replace(block, "")
        p.write_text(new_text)
        print("arrconf.yml: dropped username + password entries (D-36)")
        PYEOF

        # Verify the block is gone AND no other lines changed:
        ARRCONF_DIFF=$(git diff --numstat charts/arrconf/files/arrconf.yml | awk '{print $1+$2}')
        echo "arrconf.yml change lines: $ARRCONF_DIFF (expected: 4 deletions)"
        [ "$ARRCONF_DIFF" = "4" ] || { echo "ERROR: arrconf.yml diff is not exactly 4 lines deleted"; exit 1; }

        # Negative grep — username/password entries must be gone:
        ! grep -E "name: (username|password)" charts/arrconf/files/arrconf.yml \
          || { echo "ERROR: username/password still in arrconf.yml"; exit 1; }

    Step 2 — Helm lint + render check (catch shape regressions before pushing):
        helm lint charts/arrconf/
        helm template charts/arrconf/ | head -20

    Step 3 — Commit + push + open PR:
        git add charts/arrconf/values.yaml charts/arrconf/files/arrconf.yml
        git commit -m "feat(arrconf): Phase 2.1 PR3 — bump image to 0.1.3 + drop username/password placeholders (D-36/D-37)

    arr-stack v0.1.3 ships the field-merge fix (D-31/D-32/D-33). With the merge
    helper in place, empty-value entries in YAML are preserved from cluster, so
    the username='' / password='' placeholders are now redundant — D-36 removes
    them for cleanliness. Closes Phase 2 PR2 PUT 400."

        git push -u origin phase-2.1-arrconf-pr3

        gh pr create \
          --repo tom333/my-kluster \
          --title "feat(arrconf): Phase 2.1 PR3 — bump image to 0.1.3 + drop placeholder credentials" \
          --body "$(cat <<'BODY'
## Summary
- Bumps arrconf image: `0.1.2` → `0.1.3` (arr-stack release v0.1.3, GHCR public)
- Removes `username: ''` and `password: ''` placeholder entries from qBit fields[] in arrconf.yml (D-36)

## Why
arr-stack v0.1.3 introduces `merge_fields_for_put()` (D-31/D-32/D-33). With the
merge helper, any FieldKV whose YAML value is `''` is preserved from the cluster
state at PUT time. This eliminates the Phase 2 PR2 PUT 400 root cause (Sonarr
rejected the body because qBit credentials were overwritten with empties).

The placeholder entries become redundant under the new contract — D-36 removes
them so the YAML reflects "arrconf does not manage these fields" cleanly.

## Validation
- arr-stack CI green on Wave 2 (ruff/mypy/pytest --cov-fail-under=70)
- `test_round_trip_with_redacted_credentials_is_noop` proves dump+merge contract composes
- arr-stack v0.1.3 image anonymously pullable from GHCR

## Post-merge plan
1. ArgoCD self-heal applies the new image + ConfigMap (~3 min)
2. Force smoke job: `kubectl create job --from=cronjob/arrconf arrconf-pr3-smoke-$(date +%s) -n selfhost`
3. Expect: `merge_field_preserved` events for username/password + PUT 200 on /downloadclient/1
4. arr-stack Plan 02.1-04 re-runs Plan 02-05 Tasks 5.1c + 5.2 for Phase 2 closure
BODY
)"

    Step 4 — OPERATOR: review the PR. Required gates:
        - PR diff is exactly 2 files: `charts/arrconf/values.yaml` (1 line) + `charts/arrconf/files/arrconf.yml` (4 deletions)
        - Renovate annotation `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` is preserved on values.yaml
        - No accidental `:latest` introduced
        - No secret material in the diff
        Once approved, OPERATOR merges PR3 (squash merge per repo convention).

    Step 5 — After PR3 is merged, sync ArgoCD and verify:
        argocd app sync arrconf
        argocd app wait arrconf --health --timeout 180

        # Verify the live CronJob spec carries the new image:
        IMAGE=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}')
        echo "Live CronJob image: $IMAGE (expected: ghcr.io/tom333/arr-stack-arrconf:0.1.3)"
        [ "$IMAGE" = "ghcr.io/tom333/arr-stack-arrconf:0.1.3" ] || { echo "ERROR: ArgoCD did not propagate image bump"; exit 1; }

        # Verify ConfigMap reflects YAML cleanup (no username/password entries):
        REDACTED_LEFTOVERS=$(kubectl get configmap arrconf -n selfhost -o yaml | grep -E "name: (username|password)" | wc -l)
        echo "ConfigMap username/password entries (expected 0): $REDACTED_LEFTOVERS"
        [ "$REDACTED_LEFTOVERS" = "0" ] || { echo "ERROR: ConfigMap still has placeholder credentials"; exit 1; }

        # Unsuspend CronJob if STATE.md flagged it suspended at Phase 2 close:
        SUSPENDED=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')
        if [ "$SUSPENDED" = "true" ]; then
          kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":false}}'
          echo "CronJob unsuspended."
        fi
  </action>
  <how-to-verify>
    1. PR3 has exactly 2 file changes: `gh pr diff <pr#> --repo tom333/my-kluster --name-only | wc -l` returns `2`
    2. PR3 merged: `gh pr view <pr#> --repo tom333/my-kluster --json state --jq .state` returns `"MERGED"`
    3. ArgoCD propagated the image: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'` returns `ghcr.io/tom333/arr-stack-arrconf:0.1.3`
    4. ConfigMap clean: `kubectl get configmap arrconf -n selfhost -o yaml | grep -cE "name: (username|password)"` returns `0`
    5. CronJob NOT suspended: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returns empty or `false`
  </how-to-verify>
  <acceptance_criteria>
    - my-kluster PR3 merged with exactly 2 files changed (1 line in values.yaml; 4 line deletions in arrconf.yml)
    - `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'` returns `ghcr.io/tom333/arr-stack-arrconf:0.1.3`
    - `kubectl get configmap arrconf -n selfhost -o yaml | grep -cE "name: (username|password)"` returns `0`
    - `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returns empty string or `false`
    - `kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status}'` returns `Synced`
    - `kubectl get application arrconf -n argocd -o jsonpath='{.status.health.status}'` returns `Healthy`
    - Renovate annotation preserved: `grep -q "# renovate: image=ghcr.io/tom333/arr-stack-arrconf" /home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml` exits 0
  </acceptance_criteria>
  <verify>
    <automated>[ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}')" = "ghcr.io/tom333/arr-stack-arrconf:0.1.3" ] &amp;&amp; [ "$(kubectl get configmap arrconf -n selfhost -o yaml | grep -cE 'name: (username|password)')" = "0" ] &amp;&amp; SUS=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'); [ -z "$SUS" ] || [ "$SUS" = "false" ] &amp;&amp; [ "$(kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status}')" = "Synced" ] &amp;&amp; [ "$(kubectl get application arrconf -n argocd -o jsonpath='{.status.health.status}')" = "Healthy" ]</automated>
  </verify>
  <resume-signal>Type "approved: PR3 merged, ArgoCD synced, image is 0.1.3, ConfigMap cleaned, CronJob unsuspended" OR describe what failed (helm lint error, PR conflict, ArgoCD did not self-heal, CronJob still suspended).</resume-signal>
  <done>PR3 merged in my-kluster (2-file diff); ArgoCD self-healed; CronJob runs `ghcr.io/tom333/arr-stack-arrconf:0.1.3`; ConfigMap reflects YAML cleanup; CronJob unsuspended; ready for smoke job in Task 3.3.</done>
</task>

<task type="auto">
  <name>Task 3.3: Force first apply-mode smoke job + capture logs proving merge_field_preserved + PUT 200</name>
  <files>
    .planning/phases/02.1-field-merge-fix/evidence/pr3-job-logs-2026-05-08.log
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/reconcilers/sonarr.py (event names: managed_tag_found L53, managed_tag_created L60, dry_run_skip L86)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py (event names: plan_action L82/87/104, prune_skip L96, prune_protected L101)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr2-job-logs-2026-05-08.log (Phase 2 PR2 captured 400 stack — dispositive contrast: PR3 must show 200 instead)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-CONTEXT.md "D-37 step 6"
  </read_first>
  <action>
    Step 1 — Force the first PR3 apply-mode smoke job:
        SMOKE_JOB="arrconf-pr3-smoke-$(date +%s)"
        kubectl create job --from=cronjob/arrconf "$SMOKE_JOB" -n selfhost
        kubectl wait --for=condition=complete "job/$SMOKE_JOB" -n selfhost --timeout=180s

        EVIDENCE="/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/evidence/pr3-job-logs-$(date +%F).log"
        mkdir -p "$(dirname "$EVIDENCE")"
        kubectl logs "job/$SMOKE_JOB" -n selfhost > "$EVIDENCE"

        # Verify exit code 0 (the Job completed; in PR2 it would have been 1 due to PUT 400):
        EXIT_CODE=$(kubectl get job "$SMOKE_JOB" -n selfhost -o jsonpath='{.status.succeeded}')
        [ "$EXIT_CODE" = "1" ] || { echo "ERROR: Job did not succeed (exit_code=$EXIT_CODE); logs in $EVIDENCE"; exit 1; }

    Step 2 — Verify the W-06 carry-forward apply-mode markers (managed_tag + plan_action) are present:
        TAG_FOUND=$(jq -c 'select(.event == "managed_tag_found")' "$EVIDENCE" | wc -l)
        TAG_CREATED=$(jq -c 'select(.event == "managed_tag_created")' "$EVIDENCE" | wc -l)
        echo "managed_tag_found: $TAG_FOUND, managed_tag_created: $TAG_CREATED (expected found>=1 OR created>=1)"
        [ "$TAG_FOUND" -ge 1 ] || [ "$TAG_CREATED" -ge 1 ] \
          || { echo "ERROR: neither managed_tag_found nor managed_tag_created event present"; exit 1; }

        PLAN_UPDATE=$(jq -c 'select(.event == "plan_action" and .action == "update")' "$EVIDENCE" | wc -l)
        echo "plan_action update events: $PLAN_UPDATE (expected >= 1)"
        [ "$PLAN_UPDATE" -ge 1 ] || { echo "ERROR: no plan_action update event"; exit 1; }

    Step 3 — DISPOSITIVE PROOF: `merge_field_preserved` events appear (proves Plan 02 helper ran):
        MERGE_EVENTS=$(jq -c 'select(.event == "merge_field_preserved")' "$EVIDENCE" | wc -l)
        echo "merge_field_preserved events: $MERGE_EVENTS (expected >= 2 — username + password)"
        [ "$MERGE_EVENTS" -ge 2 ] || { echo "ERROR: merge_field_preserved fired fewer than 2 times — helper may not be active"; exit 1; }

        # Check that BOTH username AND password were preserved:
        USERNAME_PRESERVED=$(jq -c 'select(.event == "merge_field_preserved" and .name == "username")' "$EVIDENCE" | wc -l)
        PASSWORD_PRESERVED=$(jq -c 'select(.event == "merge_field_preserved" and .name == "password")' "$EVIDENCE" | wc -l)
        echo "username preserved: $USERNAME_PRESERVED, password preserved: $PASSWORD_PRESERVED (each must be >= 1)"
        [ "$USERNAME_PRESERVED" -ge 1 ] || { echo "ERROR: username field not preserved"; exit 1; }
        [ "$PASSWORD_PRESERVED" -ge 1 ] || { echo "ERROR: password field not preserved"; exit 1; }

    Step 4 — DISPOSITIVE: dry-run markers MUST be absent (apply mode is active):
        DRY_RUN_MARKERS=$(jq -c 'select(.event == "would_create_managed_tag" or .event == "dry_run_skip")' "$EVIDENCE" | wc -l)
        echo "Dry-run markers (must be 0): $DRY_RUN_MARKERS"
        [ "$DRY_RUN_MARKERS" = "0" ] || { echo "CRITICAL: dry-run path event in apply-mode logs"; exit 1; }

    Step 5 — DISPOSITIVE: NO HTTPStatusError 400 in logs (Phase 2 PR2 contrast):
        STATUS_400=$(grep -c "400 Bad Request" "$EVIDENCE" || true)
        echo "400 errors in logs: $STATUS_400 (expected 0 — Phase 2 PR2 had 1+)"
        [ "$STATUS_400" = "0" ] || { echo "ERROR: 400 still present — fix did not land"; exit 1; }

    Step 6 — Cleanup:
        kubectl delete "job/$SMOKE_JOB" -n selfhost --ignore-not-found

    Step 7 — Commit evidence in arr-stack:
        cd /home/moi/projets/perso/arr-stack
        git add .planning/phases/02.1-field-merge-fix/evidence/pr3-job-logs-*.log
        git commit -m "evidence(02.1): capture PR3 apply-mode smoke job — merge helper preserved username+password, PUT 200 (no 400)"
  </action>
  <acceptance_criteria>
    - `find /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/evidence/ -name 'pr3-job-logs-*.log' -size +0c | wc -l` returns at least `1`
    - `EVIDENCE=$(ls -t /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/evidence/pr3-job-logs-*.log | head -1) && head -1 "$EVIDENCE" | jq -e .` exits 0 (valid JSON Lines)
    - **Apply-mode markers present**: `jq -c 'select(.event == "managed_tag_found" or .event == "managed_tag_created")' "$EVIDENCE" | wc -l` >= 1
    - `jq -c 'select(.event == "plan_action" and .action == "update")' "$EVIDENCE" | wc -l` >= 1
    - **DISPOSITIVE (Plan 02 helper ran)**: `jq -c 'select(.event == "merge_field_preserved")' "$EVIDENCE" | wc -l` >= 2
    - `jq -c 'select(.event == "merge_field_preserved" and .name == "username")' "$EVIDENCE" | wc -l` >= 1
    - `jq -c 'select(.event == "merge_field_preserved" and .name == "password")' "$EVIDENCE" | wc -l` >= 1
    - **DISPOSITIVE (apply mode)**: `jq -c 'select(.event == "would_create_managed_tag" or .event == "dry_run_skip")' "$EVIDENCE" | wc -l` == 0
    - **DISPOSITIVE (fix landed — no 400)**: `grep -c "400 Bad Request" "$EVIDENCE"` returns `0`
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s | grep -q "evidence(02\.1):"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>EVIDENCE=$(ls -t /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/evidence/pr3-job-logs-*.log 2&gt;/dev/null | head -1) &amp;&amp; test -n "$EVIDENCE" &amp;&amp; head -1 "$EVIDENCE" | jq -e . &gt; /dev/null &amp;&amp; [ "$(jq -c 'select(.event == "managed_tag_found" or .event == "managed_tag_created")' "$EVIDENCE" | wc -l)" -ge 1 ] &amp;&amp; [ "$(jq -c 'select(.event == "plan_action" and .action == "update")' "$EVIDENCE" | wc -l)" -ge 1 ] &amp;&amp; [ "$(jq -c 'select(.event == "merge_field_preserved")' "$EVIDENCE" | wc -l)" -ge 2 ] &amp;&amp; [ "$(jq -c 'select(.event == "merge_field_preserved" and .name == "username")' "$EVIDENCE" | wc -l)" -ge 1 ] &amp;&amp; [ "$(jq -c 'select(.event == "merge_field_preserved" and .name == "password")' "$EVIDENCE" | wc -l)" -ge 1 ] &amp;&amp; [ "$(jq -c 'select(.event == "would_create_managed_tag" or .event == "dry_run_skip")' "$EVIDENCE" | wc -l)" = "0" ] &amp;&amp; [ "$(grep -c '400 Bad Request' "$EVIDENCE")" = "0" ]</automated>
  </verify>
  <done>Smoke job ran in apply mode under v0.1.3; logs captured; merge_field_preserved fired for username + password (≥2 events); plan_action update emitted; dry-run markers absent; NO 400 errors (Phase 2 PR2 contrast); evidence committed in arr-stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arr-stack git tag → GitHub Actions → GHCR | Tag push triggers image build; cosign signing not yet implemented (OPSV2-03) — accept |
| GHCR → my-kluster CronJob | Anonymous pull; image must remain public after v0.1.3 push (Pitfall 7) |
| my-kluster PR3 → ArgoCD → cluster | Operator review is the integrity gate; the 2-file diff is small enough to audit visually |
| Smoke job logs → arr-stack git | Logs may contain field NAMES but never field VALUES (structlog discipline) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-10 | Tampering (T-image-supply-chain) | v0.1.3 image authenticity | accept | GHCR public anonymous pull is ADR-3 acceptance. cosign signing = OPSV2-03 (post-MVP). |
| T-02.1-11 | Information Disclosure | PR3 diff could accidentally include a real credential if operator copy-pastes from secret | mitigate | Task 3.2 Step 1 negative greps confirm `username`/`password` lines are DELETED (not modified to real values). PR review is the second gate. |
| T-02.1-12 | Tampering (Renovate breakage) | Stripping `# renovate: image=...` annotation would silently break image tracking | mitigate | Task 3.2 Step 1 explicit grep gate enforces annotation presence after edit. CLAUDE.md "Annotations Renovate (CRITIQUE)" is the policy origin. |
| T-02.1-13 | Information Disclosure | Smoke job logs could leak credentials via structlog if helper logs values | mitigate | merge_fields_for_put logs ONLY `name=des_f["name"]` (Plan 02 acceptance criterion T-02.1-04). Audit grep on the captured evidence file: `grep -E "value.*[a-zA-Z]{8,}" evidence/pr3-job-logs-*.log | grep -v REDACTED` should return 0 lines. |
| T-02.1-14 | Repudiation | Without smoke-job evidence, the "PUT 200 instead of 400" claim is unverifiable | mitigate | Task 3.3 commits the log file with explicit assertions (merge_field_preserved fires, no 400 errors). |
</threat_model>

<verification>
Wave 3 gate (must all pass before Plan 04 runs the closure tasks):
1. Tag v0.1.3 on origin; arrconf-image.yml workflow concluded success; image anonymously pullable from GHCR (Task 3.1)
2. PR3 merged in my-kluster (2-file atomic diff); ArgoCD Synced + Healthy; live CronJob runs v0.1.3; ConfigMap clean; CronJob unsuspended (Task 3.2)
3. First post-PR3 smoke job completed; logs captured to evidence/; W-06 markers + merge_field_preserved (≥2) + NO 400 errors (Task 3.3)
4. Renovate annotation preserved on values.yaml (Task 3.2 grep gate)
5. Evidence file committed in arr-stack git history
</verification>

<success_criteria>
- D-36 (PR3 drops username + password placeholders from arrconf.yml) — SATISFIED
- D-37 (v0.1.3 tagged, image public, PR3 merged, smoke job green) — SATISFIED
- REQ-secret-management — formalized at runtime: cluster never sees an arrconf-issued empty credential
- CLAUDE.md "Annotations Renovate (CRITIQUE)" — preserved
- Phase 2 PR2 PUT 400 root cause — closed (smoke job evidence shows PUT 200 / no 400)
- Cluster CronJob restored to operational state (unsuspended, running v0.1.3, fix active)
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-field-merge-fix/02.1-03-SUMMARY.md` documenting:
- v0.1.3 release: tag SHA, image digest, GHCR anonymous-pull verification
- arrconf-image.yml run ID + conclusion timestamp
- my-kluster PR3 number, merge timestamp, exact 2-file diff stats
- ArgoCD sync status post-merge + cluster CronJob image confirmation
- Smoke job name, completion time, exit code (must be 0)
- Excerpts from pr3-job-logs-<date>.log: at least 2 `merge_field_preserved` JSON lines (username + password) + 1 `plan_action update` line
- Negative confirmation: zero `400 Bad Request` occurrences in logs (Phase 2 PR2 had 1+)
- 2 git commits in arr-stack (annotated tag + evidence) + 1 PR in my-kluster
- Recommended next step: `/gsd-execute-phase 02.1` Plan 04 (re-execute Tasks 5.1c + 5.2 for Phase 2 closure)
</output>
