---
status: complete
phase: 02.1-field-merge-fix
source:
  - 02.1-01-SUMMARY.md
  - 02.1-02-SUMMARY.md
  - 02.1-03-SUMMARY.md
  - 02.1-04-SUMMARY.md
started: 2026-05-09T00:50:00.000Z
updated: 2026-05-09T00:55:00.000Z
---

## Current Test

[testing complete]

## Tests

### 1. Cluster CronJob runs v0.1.3 in apply mode, ArgoCD Synced+Healthy
expected: |
  After PR3 + PR4 merged in my-kluster, the cluster's `selfhost/arrconf` CronJob spec
  carries `image: ghcr.io/tom333/arr-stack-arrconf:0.1.3`, is NOT suspended
  (`spec.suspend` is `false` or empty), the ArgoCD `arrconf` Application is `Synced`
  + `Healthy`, and the ConfigMap reflects the PR4 placeholders (re-added `username: ''`
  and `password: ''` entries under qBit fields[]).
result: pass

### 2. Sonarr qBit downloadclient: arrconf-managed tag attached + credentials preserved
expected: |
  After the Phase 2.1 cluster reconcile (Plan 03 PR4 smoke + Plan 04 closure runs),
  Sonarr's qBit downloadclient at `/api/v3/downloadclient/1` shows:
    - `tags: [1]` — the `arrconf-managed` tag (id=1, carry-forward from Phase 2 PARTIAL)
      is now attached. Closes ROADMAP Phase 2 success criterion #4.
    - `fields[].name=="username"` has `value: "moi"` (preserved across PUTs by the
      `merge_fields_for_put` helper).
    - `fields[].name=="password"` has `value: "********"` (privacy=password redaction
      from Sonarr GET; underlying stored value preserved across PUTs).
  And the qBit downloadclient functional test (Sonarr UI → Settings → Download Clients
  → qBittorrent → Test) succeeds (or you've confirmed creds in the UI).
result: pass

### 3. Live round-trip idempotence: `arrconf dump | arrconf diff` exits 0
expected: |
  Running the live round-trip against the cluster (no caching, no fixtures):
    arrconf dump --apps sonarr > /tmp/dump.yml
    arrconf diff --config /tmp/dump.yml --apps sonarr
    echo "exit code: $?"
  produces a non-empty YAML dump (no `***REDACTED***` leaks; `username: ''` and
  `password: ''` placeholders present in dumped YAML), and `arrconf diff` exits 0
  ("no_drift"). Closes Phase 1 HUMAN-UAT #3 and reaffirms REQ-idempotence end-to-end
  against live state.
result: pass

### 4. Drift detection captured + W-04 dispositive value-equality
expected: |
  `.planning/phases/02.1-field-merge-fix/evidence/drift-demo-2026-05-09.log` exists
  and contains:
    - Line 1: a JSON-Lines event with `event=managed_tag_found`, id=1
    - At least 1 JSON event with `event=plan_action` AND `action=update` (drift
      DETECTED by arrconf reconcile)
    - At least 2 `merge_field_preserved` events (helper still active end-to-end)
    - A dispositive comparison block proving `RESTORED_PRIORITY == ORIGINAL_PRIORITY`
      (W-04 value-equality after manual `?forceSave=true` correction — the operator-
      driven nudge that bridges D-02.1-06 until v0.1.4 lands)
  And `snapshots/drift-test-2026-05-09/` has the W-01 forensic before-pair committed
  (`sonarr/` ≥17 files, `qbittorrent/` ≥6 files). Closes ROADMAP Phase 2 success #5
  (REQ-drift-detection — detection half satisfied; correction half deferred).
result: pass

### 5. arrconf test suite green: 60/60 passing, coverage ≥70 % on differ.py + reconcilers/sonarr.py
expected: |
  `cd tools/arrconf && uv run pytest -q` reports `60 passed` (no failures, no skips,
  no errors). The two coverage gates from CLAUDE.md are still met:
    - `differ.py`: ≥70 % line coverage (last run: 98 %)
    - `reconcilers/sonarr.py`: ≥70 % line coverage (last run: 98 %)
  And `ruff check && ruff format --check && mypy .` (run from `tools/arrconf/`)
  all exit 0 — the CI gates from CLAUDE.md hold on Wave 2's code change.
result: pass

### 6. Snapshot before-pair vs after-pair is auditable in git
expected: |
  Both snapshot directories exist in git history (NOT gitignored):
    - `snapshots/before-phase-2.1-2026-05-08/{sonarr,qbittorrent}/` (pre-fix baseline)
    - `snapshots/post-phase2.1-2026-05-09/{sonarr,qbittorrent}/` (post-fix proof)
  And `diff -r snapshots/before-phase-2.1-2026-05-08/sonarr snapshots/post-phase2.1-2026-05-09/sonarr`
  shows ONLY the intentional Phase 2.1 changes:
    - `tag.json` — unchanged (orphan tag id=1 was already there in pre-fix; still id=1 post-fix)
    - `downloadclient.json` — `tags: []` → `tags: [1]` is the dispositive mutation
    - System-level fields like `system_status.json startTime` may differ (noise)
  No surprise mutations on quality_profiles / custom_formats / quality_definitions
  (configarr-owned per ADR-5 frontier).
result: pass

### 7. Carry-forward deferred items documented for Phase 3
expected: |
  `.planning/phases/02.1-field-merge-fix/deferred-items.md` documents 6 items
  (D-02.1-01..D-02.1-06) with severity, workaround, and recommended fix for each.
  D-02.1-06 (forceSave=true gap) is the load-bearing one: it documents a real
  arrconf bug that blocks automated drift correction, with a concrete fix
  (`client.put(..., params={"forceSave": "true"})` in the UPDATE branch) targeted
  at v0.1.4. STATE.md "Carry-forward to Phase 3" section lists D-02.1-05 + D-02.1-06
  as the items Phase 3 must consume or fix.
result: pass

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0

## Gaps

[none yet]
