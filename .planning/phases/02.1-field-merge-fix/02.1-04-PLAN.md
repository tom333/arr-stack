---
phase: 02.1-field-merge-fix
plan: 04
type: execute
wave: 4
depends_on:
  - 02.1-03
files_modified:
  - snapshots/post-phase2.1-2026-05-08/
  - snapshots/drift-test-2026-05-08/
  - .planning/phases/02.1-field-merge-fix/evidence/drift-demo-2026-05-08.log
  - .planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
autonomous: true
requirements:
  - REQ-drift-detection
  - REQ-idempotence
must_haves:
  truths:
    - "Re-execution of Plan 02-05 Task 5.1c (post-PR3 snapshot + Sonarr API tag verification) closes ROADMAP Phase 2 success criterion #4: `arrconf-managed` tag is now ATTACHED to the qBit downloadclient (`tags: [1]`) — proven by both snapshot file inspection AND a live Sonarr API call"
    - "Re-execution of Plan 02-05 Task 5.2 (drift demo runbook with W-01 forensic snapshot + W-04 dispositive value-equality on `priority`) closes ROADMAP Phase 2 success criterion #5: drift induced via API mutation, reconcile Job runs, JSON logs show `plan_action action=update`, AND Sonarr's actual field value reverts to `ORIGINAL_PRIORITY`"
    - "W-04 fix (CARRIED FORWARD from Plan 02-05): the dispositive check is value-equality `[ \"$RESTORED_PRIORITY\" = \"$ORIGINAL_PRIORITY\" ]` AFTER the Job runs — NOT a regex substring match against log lines"
    - "W-01 fix (CARRIED FORWARD from Plan 02-05): drift-test snapshot is REQUIRED, not Optional — provides the forensic before/after pair for REQ-drift-detection"
    - "post-phase2.1 snapshot diff vs `before-phase-2.1-2026-05-08/` shows ONLY: tag.json unchanged (managed tag pre-existing), downloadclient.json changed (tags: [] → tags: [1]) — no other file diffs in sonarr/ scope"
    - "Round-trip canary against the live cluster: run `arrconf dump --apps sonarr` post-fix and verify the produced YAML loads back through `arrconf diff` with 0 actions (REQ-idempotence reaffirmed against live data, not just respx mocks) — Phase 1 HUMAN-UAT #3 closure proof"
    - "Phase 1 HUMAN-UAT #3 (live round-trip) marked `[passed: ...]` with timestamp + reference to the evidence files"
    - "STATE.md updated: Phase 2 success #4 + #5 transition from PARTIAL/UNTESTED to SATISFIED; Phase 2.1 marked complete; cluster state recorded as `tag attached, drift demo green, ready for Phase 3`"
    - "Multi-source coverage audit complete: every D-31..D-37 has an artifact trail, all 9 ROADMAP success criteria for Phase 2.1 marked, REQ-drift-detection + REQ-secret-management + REQ-idempotence each cite evidence/ or snapshots/ in their closure"
  artifacts:
    - path: "snapshots/post-phase2.1-2026-05-08/sonarr/tag.json"
      provides: "Post-PR3 Sonarr tag list — must contain arrconf-managed (carry-forward state, no regression)"
      contains: "arrconf-managed"
    - path: "snapshots/post-phase2.1-2026-05-08/sonarr/downloadclient.json"
      provides: "Post-PR3 Sonarr downloadclient — qBit entry MUST have `tags: [1]` (Phase 2 success #4 closure proof)"
    - path: "snapshots/drift-test-2026-05-08/"
      provides: "REQUIRED forensic before-pair for drift demo (W-01 fix carried forward)"
    - path: ".planning/phases/02.1-field-merge-fix/evidence/drift-demo-2026-05-08.log"
      provides: "Drift demo evidence: plan_action update event + DISPOSITIVE COMPARISON block (W-04 fix)"
      contains: "match (restored == original): YES"
    - path: ".planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md"
      provides: "Phase 1 HUMAN-UAT — item #3 marked passed"
      contains: "passed: live round-trip"
  key_links:
    - from: "Plan 02.1-03 smoke job"
      to: "Sonarr API /api/v3/downloadclient/1.tags"
      via: "PUT body with merge_field_preserved username + password + arrconf-managed tag id appended"
      pattern: "tags.*\\[1\\]"
    - from: "Drift induced via curl PUT (priority mutation)"
      to: "arrconf reconcile Job (kubectl create job --from=cronjob)"
      via: "next cron cycle OR forced job"
      pattern: "plan_action.*action.*update"
    - from: "evidence/drift-demo-2026-05-08.log"
      to: "REQ-drift-detection acceptance"
      via: "DISPOSITIVE COMPARISON block + W-04 value-equality assertion"
      pattern: "match \\(restored == original\\): YES"
    - from: "Phase 1 HUMAN-UAT.md #3 [pending] / [partial]"
      to: "[passed: live round-trip via PR3 apply Job + drift demo]"
      via: "edit + git commit"
      pattern: "passed: live round-trip"
---

<objective>
Wave 4: Re-execute Plan 02-05 Tasks 5.1c (post-deploy snapshot + Sonarr API tag verification) and 5.2 (drift demo runbook) inside Phase 2.1 — closing ROADMAP Phase 2 success criteria #4 + #5 and Phase 1 HUMAN-UAT #3, all of which were left PARTIAL/UNTESTED at Phase 2 close due to the field-overwrite bug now fixed.

This is the final integration proof. Plan 02.1-03 already showed PUT 200 + merge_field_preserved in a smoke-job log; this plan goes further:
- Captures a fresh forensic snapshot post-PR3 to prove the qBit downloadclient now carries `tags: [1]` (the `arrconf-managed` tag id) — closure of Phase 2 success #4.
- Runs the drift demo end-to-end: induce a `priority` mutation via Sonarr API, force a reconcile Job, verify both the JSON log signature (`plan_action action=update`) AND the dispositive value-equality assertion (`RESTORED_PRIORITY == ORIGINAL_PRIORITY`) — closure of Phase 2 success #5 and REQ-drift-detection.
- Captures the W-01 REQUIRED drift-test snapshot for forensic audit.
- Closes Phase 1 HUMAN-UAT #3 by running an actual `arrconf dump → diff` round-trip against live Sonarr (the post-fix YAML must load back through `diff` with zero actions).
- Updates STATE.md to mark Phase 2 success #4/#5 + Phase 2.1 + HUMAN-UAT #3 as SATISFIED.

Same runbook structure as Plan 02-05's Tasks 5.1c + 5.2 (verbatim per D-34) — the W-01 + W-04 fixes from that plan are CARRIED FORWARD here. The only differences vs 02-05: the snapshot dir is `post-phase2.1-` instead of `post-phase2-pr2-`, and the evidence file lives under `02.1-field-merge-fix/evidence/`.

Output: post-phase2.1 snapshot committed; drift-test snapshot committed; drift-demo evidence log committed; UAT #3 marked passed; STATE.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/STATE.md
@/home/moi/projets/perso/arr-stack/.planning/REQUIREMENTS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-PLAN.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/reconcilers/sonarr.py
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py

<interfaces>
<!-- Reused VERBATIM from Plan 02-05 (D-34 mandate). The W-01 + W-04 fixes from 02-05 carry forward unchanged. -->

# Drift demo invocation pattern (from 02-RESEARCH.md Pattern 4 + 02-05-PLAN.md Task 5.2):
# 1. kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
# 2. curl -s http://localhost:8989/api/v3/downloadclient/1 -H "X-Api-Key: $SONARR_API_KEY" > /tmp/dc-before.json
# 3. ORIGINAL_PRIORITY=$(jq -r '.priority' /tmp/dc-before.json)   # capture for W-04 dispositive
# 4. jq '.priority = 5' /tmp/dc-before.json > /tmp/dc-drifted.json
# 5. curl -X PUT ... -d @/tmp/dc-drifted.json
# 6. kubectl create job --from=cronjob/arrconf arrconf-drift-demo-$(date +%s) -n selfhost
# 7. kubectl logs job/... > evidence/drift-demo-<date>.log
# 8. RESTORED_PRIORITY=$(curl -s ... | jq -r '.priority')
# 9. [ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ]   # W-04 fix — DISPOSITIVE check

# Apply-mode log signature (carry-forward from Plan 02.1-03; reconcile job emits same events):
# DISPOSITIVE for drift-correction:
#   - "plan_action" with action="update" (differ.py L87)
# DISPOSITIVE for the merge helper running:
#   - "merge_field_preserved" with name="username" + name="password" (differ.py merge_fields_for_put)
# DISPOSITIVE for apply mode (NOT dry-run):
#   - "would_create_managed_tag" must be 0
#   - "dry_run_skip" must be 0

# Snapshot CLI surface:
#   tools/snapshot/snapshot.sh --apps sonarr --output snapshots/post-phase2.1-$(date +%F)/
#   tools/snapshot/snapshot.sh --output snapshots/drift-test-$(date +%F)/

# Round-trip live test (Phase 1 HUMAN-UAT #3 closure):
#   arrconf dump --apps sonarr > /tmp/live.yml
#   arrconf diff --config /tmp/live.yml --apps sonarr  # must exit 0 (REQ-idempotence)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Re-execute Plan 02-05 Task 5.1c — post-PR3 snapshot + Sonarr API tag verification (Phase 2 success #4 closure)</name>
  <files>
    snapshots/post-phase2.1-2026-05-08/
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-PLAN.md (Task 5.1c verbatim — same shape, different snapshot dir name)
    - /home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
    - /home/moi/projets/perso/arr-stack/snapshots/before-phase-2.1-2026-05-08/sonarr/downloadclient.json (Plan 01 baseline — confirm `.[0].tags == []` was the pre-fix state)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-CONTEXT.md "D-34"
  </read_first>
  <action>
    Step 1 — Establish access:
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        SONARR_PF=$!
        sleep 2
        export SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"

    Step 2 — Re-snapshot post-PR3 state (D-34 — same shape as Plan 02-05 Task 5.1c, different output dir):
        cd /home/moi/projets/perso/arr-stack
        SNAPSHOT_DIR="snapshots/post-phase2.1-$(date +%F)"
        # Re-establish qBit access too if snapshot.sh needs it:
        kubectl -n selfhost port-forward svc/qbittorrent 8080:8080 &
        QBIT_PF=$!
        sleep 2
        export QBT_USER="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.QBT_USER}' | base64 -d)"
        export QBT_PASS="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.QBT_PASS}' | base64 -d)"

        tools/snapshot/snapshot.sh --apps sonarr,qbittorrent --output "$SNAPSHOT_DIR/"

    Step 3 — Verify the snapshot proves Phase 2 success #4 closure (CARRY-FORWARD from Plan 02-05 Task 5.1c, B-03 dispositive split):

        # B-03 dispositive (file existence):
        test -f "$SNAPSHOT_DIR/sonarr/tag.json"

        # B-03 dispositive (snapshot content — managed tag carry-forward):
        jq -e 'map(.label) | any(. == "arrconf-managed")' "$SNAPSHOT_DIR/sonarr/tag.json" > /dev/null

        # **NEW DISPOSITIVE for Phase 2.1**: tag NOW ATTACHED to qBit downloadclient (was [] before):
        TAGS_LEN=$(jq -e '.[0].tags | length' "$SNAPSHOT_DIR/sonarr/downloadclient.json")
        echo "qBit downloadclient post-PR3 tags length: $TAGS_LEN (expected: > 0 — was 0 in before-phase-2.1)"
        [ "$TAGS_LEN" -gt 0 ] || { echo "ERROR: tag still not attached — Phase 2 success #4 NOT closed"; exit 1; }

        # The managed tag id (=1, persisted from Phase 2 partial run) MUST be in the tags array:
        jq -e '.[0].tags[] | select(. == 1)' "$SNAPSHOT_DIR/sonarr/downloadclient.json" > /dev/null

        # B-03 dispositive (runtime API verification — corroborates the file proof):
        TAGS_LEN_API=$(curl -s "http://localhost:8989/api/v3/downloadclient" -H "X-Api-Key: $SONARR_API_KEY" | jq '.[0].tags | length')
        echo "qBit downloadclient runtime API tags length: $TAGS_LEN_API (expected: > 0)"
        [ "$TAGS_LEN_API" -gt 0 ] || { echo "ERROR: runtime API contradicts snapshot — race or staleness"; exit 1; }

        # Tag list MUST contain arrconf-managed (runtime):
        curl -s "http://localhost:8989/api/v3/tag" -H "X-Api-Key: $SONARR_API_KEY" \
          | jq -e 'map(.label) | any(. == "arrconf-managed")' > /dev/null

        kill $SONARR_PF $QBIT_PF 2>/dev/null || true

    Step 4 — Verify the diff vs `before-phase-2.1-2026-05-08/` is exactly the expected fix-effect:
        BEFORE_DIR="snapshots/before-phase-2.1-$(date +%F)"
        # Expected diffs in sonarr/: ONLY downloadclient.json (tags array changed) + maybe rootfolder.json/system_status.json (noise — freeSpace, startTime).
        # tag.json should be IDENTICAL (tag carried forward from Phase 2).
        diff -q "$BEFORE_DIR/sonarr/tag.json" "$SNAPSHOT_DIR/sonarr/tag.json" \
          && echo "tag.json unchanged (expected — managed tag was orphan pre-fix, attached post-fix)" \
          || echo "tag.json differs (acceptable if format-stable — verify content equivalence)"

        # downloadclient.json MUST differ (tags went from [] to [1]):
        diff -q "$BEFORE_DIR/sonarr/downloadclient.json" "$SNAPSHOT_DIR/sonarr/downloadclient.json" \
          && { echo "ERROR: downloadclient.json identical — fix did not propagate"; exit 1; }

    Step 5 — Commit snapshot in arr-stack:
        git add "$SNAPSHOT_DIR/"
        git commit -m "snapshot(02.1): post-PR3 capture — arrconf-managed attached to qBit downloadclient (Phase 2 #4 closed)"
  </action>
  <acceptance_criteria>
    - **B-03 dispositive (file existence)**: `test -f /home/moi/projets/perso/arr-stack/snapshots/post-phase2.1-2026-05-08/sonarr/tag.json` exits 0
    - **B-03 dispositive (snapshot content — tag persists)**: `jq -e 'map(.label) | any(. == "arrconf-managed")' /home/moi/projets/perso/arr-stack/snapshots/post-phase2.1-2026-05-08/sonarr/tag.json` exits 0
    - **Phase 2 success #4 DISPOSITIVE (tag NOW attached)**: `jq -e '.[0].tags | length > 0' /home/moi/projets/perso/arr-stack/snapshots/post-phase2.1-2026-05-08/sonarr/downloadclient.json` exits 0
    - `jq -e '.[0].tags[] | select(. == 1)' /home/moi/projets/perso/arr-stack/snapshots/post-phase2.1-2026-05-08/sonarr/downloadclient.json` returns `1`
    - **Diff against pre-fix**: `diff -q /home/moi/projets/perso/arr-stack/snapshots/before-phase-2.1-2026-05-08/sonarr/downloadclient.json /home/moi/projets/perso/arr-stack/snapshots/post-phase2.1-2026-05-08/sonarr/downloadclient.json` exits 1 (files differ — fix propagated)
    - `find /home/moi/projets/perso/arr-stack/snapshots/post-phase2.1-2026-05-08/sonarr -type f -name '*.json' | wc -l` >= 5
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s | grep -q 'snapshot(02\.1):.*post-PR3'` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack &amp;&amp; test -f snapshots/post-phase2.1-2026-05-08/sonarr/tag.json &amp;&amp; jq -e 'map(.label) | any(. == "arrconf-managed")' snapshots/post-phase2.1-2026-05-08/sonarr/tag.json &gt; /dev/null &amp;&amp; jq -e '.[0].tags | length &gt; 0' snapshots/post-phase2.1-2026-05-08/sonarr/downloadclient.json &gt; /dev/null &amp;&amp; jq -e '.[0].tags[] | select(. == 1)' snapshots/post-phase2.1-2026-05-08/sonarr/downloadclient.json &gt; /dev/null &amp;&amp; ! diff -q snapshots/before-phase-2.1-2026-05-08/sonarr/downloadclient.json snapshots/post-phase2.1-2026-05-08/sonarr/downloadclient.json &gt; /dev/null &amp;&amp; [ "$(find snapshots/post-phase2.1-2026-05-08/sonarr -type f -name '*.json' | wc -l)" -ge 5 ]</automated>
  </verify>
  <done>post-phase2.1 snapshot committed; tag.json shows arrconf-managed (carry-forward); downloadclient.json shows `tags: [1]` (FIX PROOF); diff vs before-phase-2.1 confirms fix propagated; ROADMAP Phase 2 success criterion #4 SATISFIED.</done>
</task>

<task type="auto">
  <name>Task 4.2: Re-execute Plan 02-05 Task 5.2 — drift demo runbook with W-01 forensic snapshot + W-04 dispositive (Phase 2 success #5 + REQ-drift-detection closure)</name>
  <files>
    .planning/phases/02.1-field-merge-fix/evidence/drift-demo-2026-05-08.log
    snapshots/drift-test-2026-05-08/
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-PLAN.md (Task 5.2 — verbatim runbook with W-01 + W-04 fixes)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md "Pattern 4: Drift detection demo" + "Code Example 4" + "Pitfall 4" + "Pitfall 10"
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-PATTERNS.md §13 ("evidence/drift-demo-<date>.log")
    - /home/moi/projets/perso/my-kluster/charts/arrconf/files/arrconf.yml (declared download_client priority — what arrconf will revert to)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py (event names — `plan_action` with action="update")
    - /home/moi/projets/perso/arr-stack/.planning/REQUIREMENTS.md REQ-drift-detection
  </read_first>
  <action>
    Step 1 — Establish access:
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        PF_PID=$!
        sleep 2
        SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"

    Step 2 — Capture pre-drift state (CAPTURE ORIGINAL_PRIORITY for W-04 dispositive check):
        TMPDIR=$(mktemp -d)
        DC_ID=$(curl -s http://localhost:8989/api/v3/downloadclient -H "X-Api-Key: $SONARR_API_KEY" | jq '.[0].id')
        curl -s "http://localhost:8989/api/v3/downloadclient/$DC_ID" -H "X-Api-Key: $SONARR_API_KEY" \
          | jq '.' > "$TMPDIR/dc-before.json"

        ORIGINAL_PRIORITY=$(jq -r '.priority' "$TMPDIR/dc-before.json")
        echo "Original priority: $ORIGINAL_PRIORITY"   # likely 1; persist for W-04 dispositive

    Step 3 — Induce drift via curl PUT (priority is safe per Phase 2 Assumption A6):
        DRIFTED_PRIORITY=5
        if [ "$ORIGINAL_PRIORITY" = "5" ]; then DRIFTED_PRIORITY=10; fi   # avoid no-op

        jq --argjson dp "$DRIFTED_PRIORITY" '.priority = $dp' "$TMPDIR/dc-before.json" > "$TMPDIR/dc-drifted.json"

        curl -s -X PUT "http://localhost:8989/api/v3/downloadclient/$DC_ID" \
          -H "X-Api-Key: $SONARR_API_KEY" \
          -H "Content-Type: application/json" \
          -d @"$TMPDIR/dc-drifted.json" \
          -o /dev/null -w "HTTP %{http_code}\n"

        # Confirm drift landed:
        DRIFTED_OBSERVED=$(curl -s "http://localhost:8989/api/v3/downloadclient/$DC_ID" -H "X-Api-Key: $SONARR_API_KEY" | jq '.priority')
        echo "Drifted priority observed in Sonarr: $DRIFTED_OBSERVED"
        [ "$DRIFTED_OBSERVED" = "$DRIFTED_PRIORITY" ] || { echo "ERROR: drift did not land"; exit 1; }

    Step 4 — W-01 REQUIRED: capture forensic drift-test snapshot BEFORE the reconcile Job (forensic before-pair):
        cd /home/moi/projets/perso/arr-stack
        DRIFT_SNAPSHOT="snapshots/drift-test-$(date +%F)"
        # Need qbittorrent port-forward + creds for full snapshot:
        kubectl -n selfhost port-forward svc/qbittorrent 8080:8080 &
        QBIT_PF=$!
        sleep 2
        export QBT_USER="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.QBT_USER}' | base64 -d)"
        export QBT_PASS="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.QBT_PASS}' | base64 -d)"
        tools/snapshot/snapshot.sh --apps sonarr,qbittorrent --output "$DRIFT_SNAPSHOT/"
        kill $QBIT_PF 2>/dev/null || true

        # Verify the drift-test snapshot has sonarr/ populated AND captured the drifted priority:
        find "$DRIFT_SNAPSHOT/sonarr" -type f -name '*.json' | head -1 | grep -q .
        DRIFTED_IN_SNAPSHOT=$(jq '.[0].priority' "$DRIFT_SNAPSHOT/sonarr/downloadclient.json")
        echo "Drift-test snapshot priority: $DRIFTED_IN_SNAPSHOT (expected: $DRIFTED_PRIORITY)"
        [ "$DRIFTED_IN_SNAPSHOT" = "$DRIFTED_PRIORITY" ] || { echo "ERROR: drift-test snapshot did not capture drift state"; exit 1; }

    Step 5 — Force a cron Job to trigger reconcile:
        DRIFT_JOB="arrconf-drift-demo-$(date +%s)"
        kubectl create job --from=cronjob/arrconf "$DRIFT_JOB" -n selfhost
        kubectl wait --for=condition=complete "job/$DRIFT_JOB" -n selfhost --timeout=180s

        EVIDENCE="/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/evidence/drift-demo-$(date +%F).log"
        mkdir -p "$(dirname "$EVIDENCE")"
        kubectl logs "job/$DRIFT_JOB" -n selfhost > "$EVIDENCE"

    Step 6 — Verify the JSON logs show drift detection (W-06 verified events):
        # plan_action with action="update" is the dispositive event signature:
        UPDATE_PLANS=$(jq -c 'select(.event == "plan_action" and .action == "update")' "$EVIDENCE" | wc -l)
        echo "plan_action update events: $UPDATE_PLANS (expected >= 1)"
        [ "$UPDATE_PLANS" -ge 1 ] || { echo "ERROR: no plan_action update event"; exit 1; }

        # merge_field_preserved must STILL fire (drift demo runs through merge helper too):
        MERGE_EVENTS=$(jq -c 'select(.event == "merge_field_preserved")' "$EVIDENCE" | wc -l)
        echo "merge_field_preserved events: $MERGE_EVENTS (expected >= 2)"
        [ "$MERGE_EVENTS" -ge 2 ] || { echo "ERROR: merge_field_preserved did not fire — helper inactive?"; exit 1; }

        # NO 400 errors:
        STATUS_400=$(grep -c "400 Bad Request" "$EVIDENCE" || true)
        echo "400 errors in drift-demo logs: $STATUS_400 (expected 0)"
        [ "$STATUS_400" = "0" ] || { echo "ERROR: 400 in drift-demo — fix regression"; exit 1; }

    Step 7 — W-04 DISPOSITIVE CHECK: Sonarr value reverted to ORIGINAL_PRIORITY:
        RESTORED_PRIORITY=$(curl -s "http://localhost:8989/api/v3/downloadclient/$DC_ID" -H "X-Api-Key: $SONARR_API_KEY" | jq -r '.priority')
        echo "Post-reconcile priority: $RESTORED_PRIORITY"
        if [ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ]; then
          echo "REQ-drift-detection SATISFIED — Sonarr value reverted from $DRIFTED_PRIORITY back to $ORIGINAL_PRIORITY"
        else
          echo "ERROR: drift NOT corrected. original=$ORIGINAL_PRIORITY drifted=$DRIFTED_PRIORITY restored=$RESTORED_PRIORITY"
          exit 1
        fi

        # Persist DISPOSITIVE COMPARISON in evidence (W-04 audit trail):
        cat >> "$EVIDENCE" <<EOF

# === DRIFT-DEMO DISPOSITIVE COMPARISON (W-04 fix carried forward from Plan 02-05) ===
# original_priority: $ORIGINAL_PRIORITY
# drifted_priority:  $DRIFTED_PRIORITY
# restored_priority: $RESTORED_PRIORITY
# match (restored == original): YES
EOF

    Step 8 — Cleanup:
        kubectl delete "job/$DRIFT_JOB" -n selfhost --ignore-not-found
        kill $PF_PID 2>/dev/null || true
        rm -rf "$TMPDIR"

    Step 9 — Commit drift demo evidence + drift-test snapshot:
        cd /home/moi/projets/perso/arr-stack
        git add .planning/phases/02.1-field-merge-fix/evidence/drift-demo-*.log \
                snapshots/drift-test-*/
        git commit -m "evidence(02.1): drift demo runbook — REQ-drift-detection SATISFIED (W-01 snapshot, W-04 dispositive)"
  </action>
  <acceptance_criteria>
    - Evidence file `drift-demo-<date>.log` exists, non-empty, valid JSON Lines (`head -1 ... | jq -e .` exits 0)
    - **W-06 dispositive**: `jq -c 'select(.event == "plan_action" and .action == "update")' .../evidence/drift-demo-*.log | wc -l` >= 1
    - **merge helper active**: `jq -c 'select(.event == "merge_field_preserved")' .../evidence/drift-demo-*.log | wc -l` >= 2
    - **No 400 regression**: `grep -c "400 Bad Request" .../evidence/drift-demo-*.log` returns `0`
    - **W-04 DISPOSITIVE (post-execution)**: `[ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ]` exits 0 (Sonarr's actual field value reverted)
    - **W-04 audit trail**: `grep -q "match (restored == original): YES" .../evidence/drift-demo-*.log` exits 0
    - **W-01 REQUIRED**: `test -d /home/moi/projets/perso/arr-stack/snapshots/drift-test-2026-05-08` exits 0
    - **W-01 REQUIRED content**: `find /home/moi/projets/perso/arr-stack/snapshots/drift-test-2026-05-08/sonarr -type f -name '*.json' | wc -l` >= 1
    - All evidence + snapshot committed in arr-stack git history
  </acceptance_criteria>
  <verify>
    <automated>EVIDENCE=$(ls -t /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/evidence/drift-demo-*.log 2&gt;/dev/null | head -1) &amp;&amp; test -n "$EVIDENCE" &amp;&amp; head -1 "$EVIDENCE" | jq -e . &gt; /dev/null &amp;&amp; [ "$(jq -c 'select(.event == "plan_action" and .action == "update")' "$EVIDENCE" | wc -l)" -ge 1 ] &amp;&amp; [ "$(jq -c 'select(.event == "merge_field_preserved")' "$EVIDENCE" | wc -l)" -ge 2 ] &amp;&amp; [ "$(grep -c '400 Bad Request' "$EVIDENCE")" = "0" ] &amp;&amp; grep -q 'match (restored == original): YES' "$EVIDENCE" &amp;&amp; test -d /home/moi/projets/perso/arr-stack/snapshots/drift-test-2026-05-08 &amp;&amp; [ "$(find /home/moi/projets/perso/arr-stack/snapshots/drift-test-2026-05-08/sonarr -type f -name '*.json' 2&gt;/dev/null | wc -l)" -ge 1 ]</automated>
  </verify>
  <done>Drift induced via API mutation; W-01 forensic snapshot captured + committed; reconcile Job ran; W-06 verified `plan_action+action=update` event present; merge_field_preserved continued to fire (≥2); NO 400 regression; W-04 dispositive `RESTORED_PRIORITY == ORIGINAL_PRIORITY` confirmed via curl + audit block; ROADMAP Phase 2 success #5 + REQ-drift-detection SATISFIED.</done>
</task>

<task type="auto">
  <name>Task 4.3: Live round-trip + close Phase 1 HUMAN-UAT #3 + update STATE.md</name>
  <files>
    .planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
    .planning/STATE.md
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md (item #3 — currently `[partial: ...]`; target state `[passed: ...]`)
    - /home/moi/projets/perso/arr-stack/.planning/STATE.md (current state — Phase 2 PARTIAL marker; target Phase 2.1 complete + Phase 2 success #4/#5 SATISFIED)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/__main__.py (CLI surface for `arrconf dump` + `arrconf diff` — verify args)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/evidence/drift-demo-2026-05-08.log (the just-captured evidence — referenced in UAT closure note)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-CONTEXT.md "D-34"
  </read_first>
  <action>
    Step 1 — Live round-trip test (HUMAN-UAT #3 closure proof — `arrconf dump → arrconf diff` against the real Sonarr instance):
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        PF_PID=$!
        sleep 2
        export SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"

        cd /home/moi/projets/perso/arr-stack/tools/arrconf
        # Run dump against live Sonarr (proxied via port-forward):
        # arrconf reads SONARR_BASE_URL from env or expects --config; check __main__.py for the actual surface.
        # Per CLAUDE.md "CLI", `arrconf dump --apps sonarr` writes to stdout when no --output given.
        # Use a tmp file to avoid race:
        TMP_LIVE=$(mktemp --suffix=.yml)
        SONARR_BASE_URL="http://localhost:8989" uv run python -m arrconf dump --apps sonarr --output "$TMP_LIVE"

        # The dump file MUST not contain ***REDACTED*** entries (D-36 dump filter active):
        if grep -q "\\*\\*\\*REDACTED\\*\\*\\*" "$TMP_LIVE"; then
            echo "ERROR: live dump still contains REDACTED entries — D-36 filter inactive"
            exit 1
        fi

        # Diff the live dump back against live Sonarr — MUST be 0 actions (REQ-idempotence):
        SONARR_BASE_URL="http://localhost:8989" uv run python -m arrconf diff --config "$TMP_LIVE" --apps sonarr
        DIFF_EXIT=$?
        echo "arrconf diff exit code: $DIFF_EXIT (expected: 0 — no drift, REQ-idempotence)"
        [ "$DIFF_EXIT" = "0" ] || { echo "ERROR: live round-trip not idempotent"; exit 1; }

        rm -f "$TMP_LIVE"
        kill $PF_PID 2>/dev/null || true

    Step 2 — Update Phase 1 HUMAN-UAT.md item #3 (mark passed):
        cd /home/moi/projets/perso/arr-stack
        # Use python for safe in-place edit of the UAT markdown:
        python3 - <<'PYEOF'
        from pathlib import Path
        from datetime import date
        p = Path(".planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md")
        text = p.read_text()
        # The current "result:" line for item #3 starts with `result: [partial:` — replace it with passed:
        # Be precise: replace ONLY the result line for item #3, not items #1/#2.
        import re
        # Find the section for "### 3. Live round-trip" and replace its `result:` line:
        new_result = (
            "result: [passed: 2026-05-08 — live round-trip via PR3 apply Job (Plan 02.1-03) "
            "+ drift demo (Plan 02.1-04 Task 4.2). arrconf reconciled real Sonarr + corrected "
            "UI-induced drift; W-04 dispositive RESTORED_PRIORITY == ORIGINAL_PRIORITY confirmed; "
            "live `arrconf dump --apps sonarr | arrconf diff` exits 0 (REQ-idempotence). "
            "Evidence: .planning/phases/02.1-field-merge-fix/evidence/drift-demo-2026-05-08.log + "
            "snapshots/post-phase2.1-2026-05-08/ + snapshots/drift-test-2026-05-08/]"
        )
        # Match the result line under item 3 (between "### 3. Live round-trip" and the next "## " or "### "):
        pattern = re.compile(
            r"(### 3\. Live round-trip.*?)(result: \[partial:.*?\])(\n\n)",
            re.DOTALL
        )
        replaced, n = pattern.subn(rf"\1{new_result}\3", text)
        if n != 1:
            raise SystemExit(f"ERROR: did not match exactly 1 result line under item 3 (matched {n})")
        # Update the summary block too:
        # passed: 1 -> passed: 2 ; pending: 1 -> pending: 1 (item #2 still pending) ; issues: 1 -> issues: 0
        replaced = replaced.replace("passed: 1\n", "passed: 2\n")
        replaced = replaced.replace("issues: 1\n", "issues: 0\n")
        p.write_text(replaced)
        print(f"01-HUMAN-UAT.md updated: item #3 marked passed.")
        PYEOF

        # Verify the edit landed:
        grep -q "passed: live round-trip" .planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md \
          || { echo "ERROR: UAT edit failed"; exit 1; }

    Step 3 — Update STATE.md to reflect Phase 2.1 closure + Phase 2 success #4 + #5 promoted to SATISFIED:
        # Use python for safe in-place edit:
        python3 - <<'PYEOF'
        from pathlib import Path
        from datetime import datetime
        p = Path(".planning/STATE.md")
        text = p.read_text()
        # Update key markers (search-replace on stable lines):
        # 1. last_updated + last_activity:
        now_iso = datetime.utcnow().strftime("%Y-%m-%dT%H:%M:%S.000Z")
        today = datetime.utcnow().strftime("%Y-%m-%d")

        # last_updated frontmatter:
        import re
        text = re.sub(
            r'last_updated: ".*?"',
            f'last_updated: "{now_iso}"',
            text, count=1
        )
        # last_activity:
        text = re.sub(
            r"last_activity: .*",
            f"last_activity: {today} -- Phase 2.1 closed; Phase 2 success #4 + #5 SATISFIED; HUMAN-UAT #3 passed",
            text, count=1
        )
        # stopped_at:
        text = re.sub(
            r"stopped_at: .*",
            "stopped_at: Phase 02.1 complete (Plan 04 closure), ready for Phase 3",
            text, count=1
        )
        # ROADMAP success criteria status block (replace the Phase 2 lines):
        text = text.replace(
            "- #4 download_client managed by arrconf ⚠️ PARTIAL (tag created, not attached)",
            "- #4 download_client managed by arrconf ✅ SATISFIED (tag attached via Plan 02.1-03 PR3 + Plan 02.1-04 Task 4.1 snapshot)"
        )
        text = text.replace(
            "- #5 drift detection ⏭️ UNTESTED (deferred to Phase 2.1)",
            "- #5 drift detection ✅ SATISFIED (Plan 02.1-04 Task 4.2 — W-04 dispositive value-equality + W-01 forensic snapshot)"
        )
        # Update progress line if Phase 2.1 is now complete:
        text = re.sub(
            r"completed_phases: \d+",
            "completed_phases: 4",  # 0, 1, 2, 2.1 closed
            text, count=1
        )
        p.write_text(text)
        print("STATE.md updated: Phase 2.1 closed; Phase 2 success #4/#5 SATISFIED.")
        PYEOF

        # Verify the edits landed:
        grep -q "Phase 02\.1 complete" .planning/STATE.md \
          || { echo "ERROR: STATE.md stopped_at not updated"; exit 1; }
        grep -q "#4 download_client managed by arrconf ✅ SATISFIED" .planning/STATE.md \
          || { echo "ERROR: STATE.md success #4 not promoted"; exit 1; }
        grep -q "#5 drift detection ✅ SATISFIED" .planning/STATE.md \
          || { echo "ERROR: STATE.md success #5 not promoted"; exit 1; }

    Step 4 — Commit UAT + STATE updates:
        git add .planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md \
                .planning/STATE.md
        git commit -m "docs(02.1): close HUMAN-UAT #3 + mark Phase 2 success #4/#5 SATISFIED in STATE"
  </action>
  <acceptance_criteria>
    - Live round-trip exit code: `cd /home/moi/projets/perso/arr-stack/tools/arrconf && uv run python -m arrconf dump --apps sonarr --output /tmp/live.yml && uv run python -m arrconf diff --config /tmp/live.yml --apps sonarr` exits 0 (verified during Step 1)
    - Live dump does NOT contain `***REDACTED***`: `grep -c "\\*\\*\\*REDACTED\\*\\*\\*" /tmp/live.yml` returns `0` (D-36 filter active in production)
    - Phase 1 HUMAN-UAT #3 marked passed: `grep -E "^### 3\\. Live round-trip" /home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md -A 2 | grep -q 'passed: live round-trip'` exits 0
    - HUMAN-UAT.md summary updated: `grep -q '^passed: 2$' /home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md` exits 0
    - STATE.md `stopped_at`: `grep -q "stopped_at: Phase 02\\.1 complete" /home/moi/projets/perso/arr-stack/.planning/STATE.md` exits 0
    - STATE.md Phase 2 success #4 promoted: `grep -q "#4 download_client managed by arrconf ✅ SATISFIED" /home/moi/projets/perso/arr-stack/.planning/STATE.md` exits 0
    - STATE.md Phase 2 success #5 promoted: `grep -q "#5 drift detection ✅ SATISFIED" /home/moi/projets/perso/arr-stack/.planning/STATE.md` exits 0
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s | grep -q "docs(02\\.1):.*HUMAN-UAT"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack &amp;&amp; grep -E "^### 3\. Live round-trip" .planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md -A 2 | grep -q 'passed: live round-trip' &amp;&amp; grep -q '^passed: 2$' .planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md &amp;&amp; grep -q "stopped_at: Phase 02\.1 complete" .planning/STATE.md &amp;&amp; grep -q "#4 download_client managed by arrconf ✅ SATISFIED" .planning/STATE.md &amp;&amp; grep -q "#5 drift detection ✅ SATISFIED" .planning/STATE.md</automated>
  </verify>
  <done>Live round-trip `dump → diff` against real Sonarr exits 0 (REQ-idempotence reaffirmed end-to-end); D-36 dump filter active in production (no REDACTED in dump); Phase 1 HUMAN-UAT #3 marked `[passed: live round-trip ...]`; STATE.md promoted Phase 2 success #4 + #5 to SATISFIED + recorded Phase 2.1 complete; commit recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation -> Sonarr (curl PUT for drift induction) | Trusted local action with API key; bounded to safe field (`priority` per Assumption A6 carry-forward) |
| arrconf reconcile Job -> Sonarr API | PUT /downloadclient/1 must reach 200 (post-fix); 400 here would mean a regression vs Plan 02.1-03 |
| Live dump artifact (TMP file) -> filesystem | Briefly exists in /tmp; must NOT contain plaintext credentials (D-36 dump filter is the gate) |
| evidence/ + snapshots/ -> arr-stack git | Permanent audit record; structlog redaction discipline must hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-15 | Tampering | Drift demo could leave the cluster in a drifted state if the reconcile fails or the Job's PUT 200 doesn't actually revert | mitigate | Task 4.2 Step 7 W-04 dispositive curl AFTER the Job runs — confirms Sonarr's API value matches ORIGINAL_PRIORITY. If not, the task FAILS rather than silently leaving drift. |
| T-02.1-16 | Repudiation | Without W-01 forensic snapshot, drift demo is unauditable post-hoc | mitigate | Task 4.2 Step 4 captures `snapshots/drift-test-<date>/` BEFORE the reconcile Job — diff vs `before-phase-2.1-` and `post-phase2.1-` is the audit chain. |
| T-02.1-17 | Information Disclosure | Live `arrconf dump` to /tmp/live.yml could leak credentials if D-36 dump filter is broken | mitigate | Task 4.3 Step 1 explicit grep `grep -c "\\*\\*\\*REDACTED\\*\\*\\*" /tmp/live.yml` returns 0 — fails the task if any REDACTED entry leaks. |
| T-02.1-18 | Tampering (UAT regression) | Editing 01-HUMAN-UAT.md could accidentally break the regex matching item #3 result | mitigate | Task 4.3 Step 2 python edit asserts `n != 1` and aborts if the regex doesn't match exactly once. Manual `grep` verification follows. |
| T-02.1-19 | Repudiation (STATE drift) | If STATE.md isn't updated, downstream phases (Phase 3) start from a stale view of the project | mitigate | Task 4.3 Step 3 explicit grep gates assert each promoted line landed; the commit is named `docs(02.1):` so it's traceable. |
| T-02.1-20 | Information Disclosure (logs) | drift-demo log + pr3-job-logs are committed to git — could leak credentials via structlog if helpers are misconfigured | mitigate | Acceptance criterion (cross-task): `grep -rE "value.*[a-zA-Z]{8,}" .planning/phases/02.1-field-merge-fix/evidence/ | grep -v REDACTED | wc -l` should return 0 — to be checked manually as part of the SUMMARY. |
</threat_model>

<verification>
Wave 4 (Phase 2.1 closure) gate (must all pass for the phase to close):
1. `snapshots/post-phase2.1-2026-05-08/` committed; `tag.json` shows arrconf-managed; `downloadclient.json[0].tags` length > 0 with id=1; diff vs `before-phase-2.1-` shows downloadclient.json changed (Task 4.1)
2. Drift demo evidence + drift-test snapshot committed; `plan_action action=update` event present; `merge_field_preserved` ≥2; W-04 dispositive `RESTORED_PRIORITY == ORIGINAL_PRIORITY` confirmed; W-01 forensic snapshot present (Task 4.2)
3. Live `arrconf dump --apps sonarr` produces YAML without `***REDACTED***`; `arrconf diff` exits 0 against the same Sonarr (Task 4.3 Step 1)
4. Phase 1 HUMAN-UAT.md item #3 marked `[passed: ...]`; summary block updated `passed: 2`, `issues: 0` (Task 4.3 Step 2)
5. STATE.md updated: stopped_at = Phase 02.1 complete; Phase 2 success #4 + #5 marked SATISFIED; completed_phases incremented (Task 4.3 Step 3)
6. Cross-cutting audit: no plaintext credential leaks in any committed evidence/log/snapshot file
</verification>

<success_criteria>
- D-34 (re-execute Plan 02-05 Tasks 5.1c + 5.2 inside Phase 2.1) — SATISFIED (Tasks 4.1 + 4.2)
- ROADMAP Phase 2.1 success criterion #8 (Plan 02-05 Task 5.1c re-executed; tag attached; closure of Phase 2 #4) — SATISFIED
- ROADMAP Phase 2.1 success criterion #9 (Plan 02-05 Task 5.2 re-executed with W-01 + W-04) — SATISFIED
- ROADMAP Phase 2 success criterion #4 (download_client managed by arrconf — tag attached) — SATISFIED via Plan 02.1-04 Task 4.1
- ROADMAP Phase 2 success criterion #5 (drift detection proven) — SATISFIED via Plan 02.1-04 Task 4.2
- REQ-drift-detection — SATISFIED (W-04 dispositive value-equality + W-01 forensic snapshot)
- REQ-idempotence — REAFFIRMED end-to-end (live `dump → diff` round-trip exits 0)
- Phase 1 HUMAN-UAT #3 (live round-trip) — CLOSED
- STATE.md reflects all closures
- Phase 2.1 ready for `/gsd-verify-phase 02.1` and Phase 3 entry
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-field-merge-fix/02.1-04-SUMMARY.md` documenting:
- post-phase2.1 snapshot file count + diff stats vs before-phase-2.1 (only downloadclient.json + noise should differ)
- Drift demo: ORIGINAL_PRIORITY, DRIFTED_PRIORITY, RESTORED_PRIORITY, time-to-revert (kubectl wait duration)
- Cited ≥ 2 JSON log lines from drift-demo-<date>.log demonstrating `plan_action action=update` and `merge_field_preserved`
- Cited the DISPOSITIVE COMPARISON block (W-04 fix carried forward)
- W-01 drift-test snapshot file count
- Live round-trip: arrconf dump byte count, arrconf diff exit code 0, REDACTED count in dump = 0
- Phase 1 HUMAN-UAT #3 closure timestamp
- STATE.md updates summary
- Cross-repo file inventory (Phase 2.1 totals): snapshots committed, evidence committed (arr-stack); 1 PR3 merged (my-kluster); 1 v0.1.3 release (GHCR)
- All Phase 2.1 ROADMAP success criteria #1-#9 status (each ✅ with citation)
- Anti-leak audit result (zero plaintext credentials across all committed files in this phase)
- Recommended next step: `/gsd-verify-phase 02.1` then `/gsd-progress` to advance to Phase 3
</output>
