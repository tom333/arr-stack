---
phase: 02-arrconf-cluster-validation
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - my-kluster/charts/arrconf/  # PR1 commits everything authored in 02-03
  - my-kluster/argocd/argocd-apps/arrconf-app.yaml
  - my-kluster/secrets/arrconf-secret.yaml  # committed in my-kluster only (NOT in arr-stack)
  - arr-stack/snapshots/post-phase2-pr1-2026-05-08/
  - arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr1-job-logs-2026-05-08.log
autonomous: false
requirements:
  - REQ-bootstrap-exception
  - REQ-secret-management
must_haves:
  truths:
    - "PR1 opened in my-kluster with the mini-chart + ArgoCD App + arrconfDryRun: true (D-28); 9 files staged from Plan 02-03 working tree (B-01 checkpoint already cleared)"
    - "BEFORE PR1 merges: operator runs `kubectl apply -f my-kluster/secrets/arrconf-secret.yaml` (Pitfall 3 — mandatory for REQ-bootstrap-exception)"
    - "Secret `arrconf-env` exists in `selfhost` namespace before ArgoCD syncs the chart"
    - "Secret has NO `argocd.argoproj.io/tracking-id` annotation (W-05 fix — proves ArgoCD does not own the bootstrap Secret; manual lifecycle preserved per CLAUDE.md 'Bootstrap secrets')"
    - "After PR1 merge: ArgoCD syncs -> CronJob `arrconf` exists in `selfhost` (ROADMAP success criterion #2)"
    - "CronJob has EXACTLY ONE volumeMount entry, named 'config' (the ConfigMap mount for arrconf.yml) — NO Secret volumeMount (B-02 fix — proves bootstrap-from-env injection is via envFrom alone, no file-mounted secret path)"
    - "First Job run (scheduled or forced via `kubectl create job --from=cronjob/arrconf`) completes successfully with ARRCONF_DRY_RUN=true (D-28 PR1 protocol)"
    - "Job pod logs show JSON structlog output with the EXACT event names emitted by Phase 1 reconciler dry-run path: `managed_tag_found` OR `would_create_managed_tag` (from sonarr.py _ensure_managed_tag), `plan_action` (from differ.py reconcile), AND `dry_run_skip` (from sonarr.py _execute when dry_run=true) — captured by reading reconciler source per W-06 fix"
    - "NOTE: Phase 1 reconciler does NOT emit an explicit env-vs-file source marker; that limitation is documented here per B-02 partial — the volumeMount inspection is the dispositive cluster-level proof of env-only injection"
    - "Job logs persisted to `arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr1-job-logs-<date>.log` for audit trail"
    - "Re-snapshot `arr-stack/snapshots/post-phase2-pr1-<date>/` captured after PR1 first-run cycle (D-30 #2)"
    - "diff -rq snapshots/before-phase-2-2026-05-08/sonarr/ snapshots/post-phase2-pr1-<date>/sonarr/ produces ZERO output (ROADMAP success criterion #3 — proves zero writes during dry-run)"
    - "arrconf binary's apply --dry-run guarantee (Phase 1 unit-tested via 52 respx-mocked tests) is corroborated at cluster level"
    - "Task 4.1 verify is a checkpoint:human-action gate; the &&-chain in <verify><automated> is acceptable as-is — when any sub-check fails, the operator inspects manually using the per-step echo lines in <action> Steps 1/7/8 (W-NEW-04 addressed by leaving as-is)"
  artifacts:
    - path: "arr-stack/snapshots/post-phase2-pr1-2026-05-08/sonarr/downloadclient.json"
      provides: "Post-PR1 dry-run Sonarr download_client snapshot — must equal pre-deploy baseline"
    - path: "arr-stack/snapshots/post-phase2-pr1-2026-05-08/sonarr/tag.json"
      provides: "Post-PR1 dry-run Sonarr tag snapshot — arrconf-managed tag MUST NOT be present yet (dry-run = no writes)"
    - path: "arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr1-job-logs-2026-05-08.log"
      provides: "Captured kubectl logs from the first PR1 cron Job — proof of dry-run path events (managed_tag_found / would_create_managed_tag / plan_action / dry_run_skip — exact names from sonarr.py + differ.py per W-06)"
  key_links:
    - from: "my-kluster/charts/arrconf/templates/cronjob.yaml"
      to: "my-kluster/secrets/arrconf-secret.yaml"
      via: "envFrom: secretRef: name: arrconf-env (cluster-bound at Job pod start)"
      pattern: "envFrom"
    - from: "Cluster CronJob `arrconf` in selfhost"
      to: "Sonarr Service `sonarr.selfhost.svc.cluster.local:8989`"
      via: "HTTP GET only (dry-run); arrconf reads, plans, logs, exits"
      pattern: "GET .*api/v3/(downloadclient|tag)"
    - from: "arr-stack/snapshots/post-phase2-pr1-<date>/sonarr/"
      to: "arr-stack/snapshots/before-phase-2-2026-05-08/sonarr/"
      via: "diff -rq must produce zero output"
      pattern: "identical"
    - from: "kubectl get cronjob arrconf volumeMounts"
      to: "EXACTLY 1 entry named 'config' (no Secret-derived volume)"
      via: "jq filter on the volumeMounts array — proves env-only secret injection (B-02 fix)"
      pattern: "length == 1 and .[0].name == \"config\""
---

<objective>
Wave 3: PR1 dry-run deployment and observation. (W-NEW-02 fix: wave bumped 2 -> 3 because 02-03 was bumped 1 -> 2 to match its `depends_on: [02-02]`. Logically this is still the "PR1 dry-run" value stream.) This plan opens **PR1** in my-kluster (the chart authored in Plan 02-03), bootstraps the secret manually, lets ArgoCD sync, observes the first cron cycle (or forces a smoke run), captures the JSON logs as evidence, verifies env-only secret injection at the cluster level (B-02), and re-snapshots Sonarr to prove `diff -rq` against the baseline = ZERO output.

Purpose: Decouple "did the chart deploy correctly" from "did arrconf write what we expected" (D-28). PR1 ONLY proves the chart deploys and arrconf runs in dry-run mode without writing anything. The Phase 1 unit-level zero-write guarantee (52 respx-mocked tests, 99% coverage on differ.py + reconcilers/sonarr.py) is corroborated at cluster level.

This plan addresses checker findings:
- B-02 (REQ-bootstrap-exception runtime guarantee): adds volumeMounts inspection acceptance — proves NO Secret-mounted file path, only the ConfigMap mount for arrconf.yml
- W-05 (ArgoCD tracking-id leak): adds acceptance asserting the bootstrap Secret has NO `argocd.argoproj.io/tracking-id` annotation
- W-06 (event names inferred): replaces inferred regex with EXACT event names captured from sonarr.py + differ.py source — `managed_tag_found`, `would_create_managed_tag`, `plan_action`, `dry_run_skip`

Output: PR1 merged in my-kluster; CronJob arrconf running in selfhost in dry-run mode; post-PR1 snapshot identical to baseline; JSON log evidence with verified event names committed to arr-stack `.planning/phases/02-arrconf-cluster-validation/evidence/`.

Satisfies ROADMAP success criteria #1 (already satisfied by Wave 1 Plan 02-01), #2 (CronJob exists, secret bound), #3 (zero writes during dry-run).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-VALIDATION.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-03-PLAN.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-03-SUMMARY.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/reconcilers/sonarr.py
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py
@/home/moi/projets/perso/my-kluster/CLAUDE.md
@/home/moi/projets/perso/my-kluster/charts/configarr/templates/cronjob.yaml

<interfaces>
<!-- Cluster-side interfaces this plan exercises. No new file interfaces are created in arr-stack. -->

# Cluster resources expected to exist after PR1 merge + ArgoCD sync:
# - Secret/arrconf-env in namespace selfhost (manually applied BEFORE merge — Pitfall 3)
# - ConfigMap/arrconf in namespace selfhost (rendered from charts/arrconf/files/arrconf.yml)
# - CronJob/arrconf in namespace selfhost (schedule "0 */4 * * *", Forbid concurrency)
# - Application/arrconf in namespace argocd (Synced + Healthy)

# Forced-run pattern (no waiting for cron):
# kubectl create job --from=cronjob/arrconf arrconf-pr1-smoke -n selfhost
# kubectl wait --for=condition=complete job/arrconf-pr1-smoke -n selfhost --timeout=120s
# kubectl logs job/arrconf-pr1-smoke -n selfhost

# === EXACT event names emitted by Phase 1 reconciler dry-run path (W-06 fix) ===
# Captured from /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/reconcilers/sonarr.py:
#   - "managed_tag_found"          (line 53; emitted when arrconf-managed tag already exists)
#   - "would_create_managed_tag"   (line 56; emitted when dry_run=true AND tag missing)
#   - "managed_tag_created"        (line 60; APPLY MODE ONLY — should NOT appear in PR1 logs)
#   - "dry_run_skip"               (line 86; emitted for each ADD/UPDATE/DELETE action when dry_run=true)
#
# Captured from /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py:
#   - "plan_action"                (lines 82, 87, 104; emitted with action=add|update|delete by reconcile())
#   - "prune_skip"                 (line 96; warning level, when prune=False and item not in YAML)
#   - "prune_protected"            (line 101; warning level, when prune=True but managed tag missing)
#
# IMPORTANT: There is NO `dry_run=true` field on these events; the dry-run signal is the EVENT NAME itself
# (`would_create_managed_tag`, `dry_run_skip`). There is NO explicit env-vs-file source marker — that B-02
# limitation is documented in must_haves.truths and the dispositive cluster-level proof is the volumeMounts
# inspection (only `name: config` ConfigMap mount; no Secret-mounted path).

# Expected log shape for PR1 (dry-run, baseline-sonarr.yml = current Sonarr state -> NO_OPs only):
# {"event": "managed_tag_found", "id": <int>, "logger": "arrconf.reconcilers.sonarr"}      ← if tag exists
#   OR
# {"event": "would_create_managed_tag", "logger": "arrconf.reconcilers.sonarr"}            ← if tag missing AND dry_run
# {"event": "plan_action", "action": "no-op", "name": "qBittorrent", ...}                  ← from differ.py (or "update" if drift)
# {"event": "dry_run_skip", "action": "<update|add|delete>", "name": "qBittorrent", ...}   ← only if non-NO_OP

# Snapshot diff invariant for success criterion #3:
# diff -rq snapshots/before-phase-2-2026-05-08/sonarr/ snapshots/post-phase2-pr1-<date>/sonarr/
# Expected: zero output (identical contents)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4.1: Apply arrconf-secret.yaml manually + Open PR1 in my-kluster</name>
  <files>
    my-kluster/  # commits the chart + ArgoCD App on a feature branch
  </files>
  <read_first>
    - /home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml (verify arrconfDryRun: true; image.tag pinned)
    - /home/moi/projets/perso/my-kluster/secrets/arrconf-secret.yaml (the secret being applied manually; verify SONARR_API_KEY value present)
    - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arrconf-app.yaml (verify metadata.name=arrconf, path=charts/arrconf)
    - /home/moi/projets/perso/my-kluster/CLAUDE.md "Bootstrap secrets" section (manual lifecycle until ESO/Phase 8)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md "Pitfall 3" + "Pitfall 1" + "Pattern 3"
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-03-SUMMARY.md (B-01 checkpoint outcome — confirms 9 files in working tree)
  </read_first>
  <what-built>
    Plan 02-03 authored the mini-chart + ArgoCD App + bootstrap Secret in my-kluster's working tree and B-01 checkpoint confirmed 9 files staged. This task is the human-gated cross-repo step: apply the secret manually (BEFORE ArgoCD sees the chart, per Pitfall 3), then open PR1 in my-kluster with the chart files. After merge, ArgoCD will detect the new App and sync.
  </what-built>
  <action>
    Step 1 — Apply the secret BEFORE anything else (Pitfall 3 — non-negotiable order):
        kubectl apply -f /home/moi/projets/perso/my-kluster/secrets/arrconf-secret.yaml

        # Verify the secret landed:
        kubectl get secret arrconf-env -n selfhost
        # Expected: NAME=arrconf-env, TYPE=Opaque, DATA=1, AGE=<recent>

        # Confirm the SONARR_API_KEY data is non-empty (without revealing it):
        kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d | wc -c
        # Expected: > 20 (a real API key)

        # W-05 fix: confirm the Secret has NO ArgoCD tracking-id annotation (proves manual lifecycle, not GitOps-managed):
        TRACKING_ID=$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.metadata.annotations.argocd\.argoproj\.io/tracking-id}')
        echo "ArgoCD tracking-id: '$TRACKING_ID' (expected: empty string)"
        if [ -n "$TRACKING_ID" ]; then
          echo "ERROR: bootstrap Secret has ArgoCD tracking-id — it should NOT be GitOps-managed."
          echo "Investigate: ArgoCD project may be auto-importing secrets/ — review my-kluster/argocd/argocd-appprojects/."
          exit 1
        fi

    Step 2 — Create a feature branch in my-kluster and stage all Plan 02-03 files:
        cd /home/moi/projets/perso/my-kluster
        git checkout main && git pull origin main
        git checkout -b phase-2-arrconf-pr1
        git add charts/arrconf/ argocd/argocd-apps/arrconf-app.yaml secrets/arrconf-secret.yaml

        # Verify staged set:
        STAGED_COUNT=$(git diff --cached --name-only | wc -l)
        echo "Staged files: $STAGED_COUNT (expected: 9)"
        [ "$STAGED_COUNT" -eq 9 ] || { echo "ERROR: expected 9 staged files, got $STAGED_COUNT"; git diff --cached --name-only; exit 1; }

    Step 3 — Pre-commit secret hygiene check (defense-in-depth, W-02 fix — POSIX class):
        cd /home/moi/projets/perso/arr-stack
        ! grep -rE 'SONARR_API_KEY[[:space:]]*[:=][[:space:]]*"[A-Za-z0-9]{16,}"' --exclude-dir='.git' --exclude-dir='snapshots' . 2>/dev/null
        # If this returns matches: STOP, that's a regression of REQ-secret-management.

    Step 4 — Commit + push the branch in my-kluster:
        cd /home/moi/projets/perso/my-kluster
        git commit -m "$(cat <<'EOF'
feat(arrconf): add Phase 2 mini-chart with arrconfDryRun=true (PR1)

Phase 2 PR1 of two-PR protocol (D-28 in arr-stack).
- Mini-chart mirrors charts/configarr/ (D-24); replaced by arr-stack umbrella in Phase 4.
- arrconfDryRun: true — observe one cron cycle, prove zero writes, then PR2 flips to false.
- Bootstrap secret arrconf-env (SONARR_API_KEY only — D-29).
- ArgoCD App in selfhost-project, automated selfHeal+prune (mirror configarr-app.yaml).

Pre-merge requirements (already done):
- kubectl apply -f secrets/arrconf-secret.yaml (Pitfall 3)
- GHCR package visibility = Public (Pitfall 1, resolved Phase 1 HUMAN-UAT #1)
- Image tag verified pullable anonymously (Pitfall 2)

Refs: arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md
EOF
)"
        git push -u origin phase-2-arrconf-pr1

    Step 5 — Open the PR via gh CLI:
        gh pr create \
          --repo tom333/my-kluster \
          --title "feat(arrconf): Phase 2 PR1 — mini-chart with arrconfDryRun=true" \
          --body "$(cat <<'EOF'
## Summary

Phase 2 PR1 of the two-PR dry-run protocol (D-28 in arr-stack).

This PR adds the arrconf mini-chart to my-kluster, mirroring the configarr pattern (D-24). The chart deploys `arrconfDryRun: true`, so the first cron cycle will observe what arrconf would do without writing to Sonarr. PR2 (separate) will flip the flag to `false` after I confirm zero-write behaviour via snapshot diff.

## What's in this PR

- `charts/arrconf/` — Helm chart (Chart.yaml, values.yaml, files/arrconf.yml, templates/_helpers.tpl, templates/cronjob.yaml, templates/configmap.yaml, README.md)
- `argocd/argocd-apps/arrconf-app.yaml` — ArgoCD Application for selfhost-project
- `secrets/arrconf-secret.yaml` — Bootstrap secret with SONARR_API_KEY only (D-29; same value as configarr-secret.yaml — same Sonarr instance)

## Pre-merge checklist (all completed)

- [x] `kubectl apply -f my-kluster/secrets/arrconf-secret.yaml` (Pitfall 3 — secret MUST exist before ArgoCD sync)
- [x] `kubectl get secret arrconf-env -n selfhost` returns the secret with DATA=1
- [x] `kubectl get secret arrconf-env -n selfhost -o jsonpath='{.metadata.annotations.argocd\.argoproj\.io/tracking-id}'` returns empty (W-05 — manual lifecycle)
- [x] GHCR package `tom333/arr-stack-arrconf` visibility set to Public (Pitfall 1, closes Phase 1 HUMAN-UAT #1)
- [x] Anonymous `docker pull` from logged-out client succeeds (Pitfall 2 image-tag-verified)
- [x] `helm lint my-kluster/charts/arrconf/` exits 0
- [x] arr-stack `snapshots/before-phase-2-2026-05-08/` re-captured and committed (ADR-6, D-30 #1)

## Post-merge plan

1. ArgoCD will sync; CronJob `arrconf` appears in `selfhost`.
2. Wait for first cron cycle (<= 4 h) OR force a run via `kubectl create job --from=cronjob/arrconf arrconf-pr1-smoke -n selfhost`.
3. Capture `kubectl logs` (JSON structlog) — expect `managed_tag_found`/`would_create_managed_tag` + `plan_action` + `dry_run_skip` events (exact names from sonarr.py + differ.py source).
4. Verify volumeMounts has only ONE entry (the ConfigMap; no Secret-mounted file path).
5. Re-snapshot Sonarr in arr-stack: `tools/snapshot/snapshot.sh --output snapshots/post-phase2-pr1-<date>/`.
6. `diff -rq snapshots/before-phase-2-<date>/sonarr/ snapshots/post-phase2-pr1-<date>/sonarr/` MUST produce zero output (success criterion #3).
7. THEN open PR2 to flip `arrconfDryRun: false`.

## Operational notes

- `concurrencyPolicy: Forbid` only blocks scheduler-created Jobs, NOT `kubectl create job --from=cronjob/...` (Pitfall 4 — k/k#107827).
- `selfHeal: true` reverts manual `kubectl edit` within ~3 min — all changes via PR (Pitfall 8).
- Mini-chart will be replaced by the arr-stack umbrella in Phase 4 (Pitfall 6).

## Refs

- arr-stack Phase 2 CONTEXT: `.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md` (D-23 to D-30)
- arr-stack Phase 2 RESEARCH: `.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md` (Pitfalls 1-10)
EOF
)"

    Step 6 — Wait for the operator (you) to merge the PR. DO NOT auto-merge.

    Step 7 — After merge, verify ArgoCD sees the App and syncs:
        argocd app sync arrconf
        argocd app wait arrconf --health --timeout 180

        kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status}'   # expect "Synced"
        kubectl get application arrconf -n argocd -o jsonpath='{.status.health.status}' # expect "Healthy"

    Step 8 — Verify cluster resources exist + the B-02 fix volumeMount inspection:
        kubectl get cronjob arrconf -n selfhost
        kubectl get configmap arrconf -n selfhost
        kubectl get secret arrconf-env -n selfhost
        kubectl describe cronjob arrconf -n selfhost | grep -E 'Schedule|Concurrency|Image|envFrom'

        # B-02 fix: volumeMounts must contain EXACTLY ONE entry, named 'config' (the ConfigMap mount).
        # Any Secret-mounted path indicates the bootstrap-from-env contract was violated.
        VOLUME_MOUNTS_OK=$(kubectl get cronjob arrconf -n selfhost \
          -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].volumeMounts}' \
          | jq 'length == 1 and .[0].name == "config"')
        echo "volumeMounts inspection: $VOLUME_MOUNTS_OK (expected: true)"
        [ "$VOLUME_MOUNTS_OK" = "true" ] || { echo "ERROR: B-02 fix failed — extra volumeMount detected"; kubectl get cronjob arrconf -n selfhost -o yaml; exit 1; }

    DO NOT proceed to Task 4.2 until all 4 cluster resources exist + ArgoCD reports Synced+Healthy + volumeMount inspection passes.
  </action>
  <how-to-verify>
    1. Secret applied: `kubectl get secret arrconf-env -n selfhost -o jsonpath='{.metadata.name}'` returns `arrconf-env`
    2. Secret has data: `kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d | wc -c` > 20
    3. **W-05** Secret has NO ArgoCD tracking-id: `kubectl get secret arrconf-env -n selfhost -o jsonpath='{.metadata.annotations.argocd\.argoproj\.io/tracking-id}'` returns empty string (not present)
    4. arr-stack still secret-clean: `! grep -rE 'SONARR_API_KEY[[:space:]]*[:=][[:space:]]*"[A-Za-z0-9]{16,}"' --exclude-dir='.git' --exclude-dir='snapshots' /home/moi/projets/perso/arr-stack/` exits 0 (W-02 POSIX class)
    5. PR opened: `gh pr list --repo tom333/my-kluster --state open --search 'Phase 2 PR1'` returns 1 row
    6. PR merged (after operator review): `gh pr view <pr-number> --repo tom333/my-kluster --json state --jq .state` returns `MERGED`
    7. ArgoCD App synced: `kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status}'` returns `Synced`
    8. ArgoCD App healthy: `kubectl get application arrconf -n argocd -o jsonpath='{.status.health.status}'` returns `Healthy`
    9. CronJob exists: `kubectl get cronjob arrconf -n selfhost -o name` returns `cronjob.batch/arrconf`
    10. CronJob has envFrom: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].envFrom[0].secretRef.name}'` returns `arrconf-env`
    11. CronJob has ARRCONF_DRY_RUN=true: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].env[?(@.name=="ARRCONF_DRY_RUN")].value}'` returns `true`
    12. **B-02** CronJob volumeMounts has EXACTLY 1 entry named `config`: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].volumeMounts}' | jq -e 'length == 1 and .[0].name == "config"'` exits 0
  </how-to-verify>
  <acceptance_criteria>
    - `kubectl get secret arrconf-env -n selfhost` exits 0 with DATA=1
    - **W-05**: `kubectl get secret arrconf-env -n selfhost -o jsonpath='{.metadata.annotations.argocd\.argoproj\.io/tracking-id}'` returns empty string (annotation absent)
    - my-kluster PR1 merged on main (gh pr view returns state=MERGED)
    - `kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status}'` = `Synced`
    - `kubectl get application arrconf -n argocd -o jsonpath='{.status.health.status}'` = `Healthy`
    - `kubectl get cronjob arrconf -n selfhost` exits 0
    - `kubectl get configmap arrconf -n selfhost` exits 0
    - CronJob has envFrom: arrconf-env, ARRCONF_DRY_RUN=true (verifications 10–11)
    - **B-02**: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].volumeMounts}' | jq -e 'length == 1 and .[0].name == "config"'` exits 0 (only ConfigMap mount; NO Secret-derived volume)
    - arr-stack repo still has zero plaintext API keys (W-02 POSIX-class grep)
  </acceptance_criteria>
  <verify>
    <automated>kubectl get secret arrconf-env -n selfhost -o name &amp;&amp; kubectl get cronjob arrconf -n selfhost -o name &amp;&amp; kubectl get configmap arrconf -n selfhost -o name &amp;&amp; [ "$(kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status}')" = "Synced" ] &amp;&amp; [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].envFrom[0].secretRef.name}')" = "arrconf-env" ] &amp;&amp; [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].env[?(@.name==\"ARRCONF_DRY_RUN\")].value}')" = "true" ] &amp;&amp; [ -z "$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.metadata.annotations.argocd\.argoproj\.io/tracking-id}')" ] &amp;&amp; kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].volumeMounts}' | jq -e 'length == 1 and .[0].name == "config"'</automated>
  </verify>
  <resume-signal>Type "approved: PR1 merged, ArgoCD synced, CronJob present, volumeMount=1, no tracking-id" OR describe what failed (PR review blocked, ArgoCD sync failure, secret missing, CrashLoop, extra volumeMount detected, tracking-id annotation present, etc.). If `ImagePullBackOff` happens: re-check Pitfall 1 (GHCR public) and Pitfall 2 (tag format).</resume-signal>
  <done>Secret manually applied (NO ArgoCD tracking-id — W-05); PR1 merged; ArgoCD synced+healthy; CronJob `arrconf` present in selfhost with envFrom+ARRCONF_DRY_RUN=true; volumeMounts has EXACTLY 1 entry named 'config' (B-02 — env-only secret injection proven); ROADMAP success criterion #2 SATISFIED.</done>
</task>

<task type="auto">
  <name>Task 4.2: Force first dry-run Job + capture JSON logs as evidence (W-06 — exact event names)</name>
  <files>
    arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr1-job-logs-2026-05-08.log
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md "Pattern 4" + "Pitfall 4" + "Pitfall 10" sections
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-VALIDATION.md "Per-Task Verification Map" rows for `pr1-deploy-dryrun` and `snapshot-post-pr1`
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/__main__.py (verify CLI accepts the args from cronjob.yaml)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/reconcilers/sonarr.py (W-06 — verified event names: `managed_tag_found`, `would_create_managed_tag`, `dry_run_skip`)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py (W-06 — verified event names: `plan_action`)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md "D-07" (structlog auto JSON on non-TTY)
  </read_first>
  <action>
    Step 1 — Force a smoke run instead of waiting for cron (Pattern 4 in RESEARCH.md):
        SMOKE_JOB="arrconf-pr1-smoke-$(date +%s)"
        kubectl create job --from=cronjob/arrconf "$SMOKE_JOB" -n selfhost
        kubectl wait --for=condition=complete "job/$SMOKE_JOB" -n selfhost --timeout=180s

        # If it fails (timeout or condition=failed): collect diagnostics.
        # kubectl describe job/$SMOKE_JOB -n selfhost
        # kubectl logs job/$SMOKE_JOB -n selfhost
        # Common failure modes:
        #   - ImagePullBackOff -> Pitfall 1 (GHCR not public) or Pitfall 2 (wrong tag pinned)
        #   - "Error: secret arrconf-env not found" -> Pitfall 3 (secret was not applied)
        #   - "missing SONARR_API_KEY" inside arrconf -> secret applied but key empty
        #   - 401 Unauthorized from Sonarr -> wrong/expired API key

    Step 2 — Capture JSON logs to evidence file:
        mkdir -p /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/
        EVIDENCE="/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr1-job-logs-$(date +%F).log"
        kubectl logs "job/$SMOKE_JOB" -n selfhost > "$EVIDENCE"

        # Sanity-check: file is non-empty and parses as JSON Lines:
        wc -l "$EVIDENCE"     # expect at least 3 lines
        head -1 "$EVIDENCE" | jq -e .  # first line must be valid JSON (Pitfall 10 — kubectl logs is non-TTY -> JSON activates)

    Step 3 — Verify the dry-run signal in the logs using EXACT event names from reconciler source (W-06 fix):
        # The logs MUST show events from the dry-run code path. Per W-06 audit of sonarr.py + differ.py:
        # - REQUIRED: at least one of `managed_tag_found` OR `would_create_managed_tag` (sonarr.py _ensure_managed_tag)
        # - REQUIRED: at least one `plan_action` event (differ.py reconcile, lines 82/87/104)
        # - DISPOSITIVE for dry-run: `dry_run_skip` event ONLY appears in dry-run path (sonarr.py line 86)
        #   — but ONLY if there is something to skip; if all actions are NO_OP (baseline-sonarr.yml = current state),
        #     `dry_run_skip` won't appear (NO_OP is filtered before the dry_run branch in _execute).
        # - DISPOSITIVE for apply mode (must NOT appear in PR1): `managed_tag_created` (sonarr.py line 60)

        # Check 1: at least one tag-related event from the dry-run path:
        TAG_EVENTS=$(jq -c 'select(.event == "managed_tag_found" or .event == "would_create_managed_tag")' "$EVIDENCE" | wc -l)
        echo "Tag-related dry-run events: $TAG_EVENTS (expected >= 1)"
        [ "$TAG_EVENTS" -ge 1 ] || { echo "ERROR: no managed_tag_found or would_create_managed_tag event"; exit 1; }

        # Check 2: at least one plan_action event from differ.py:
        PLAN_EVENTS=$(jq -c 'select(.event == "plan_action")' "$EVIDENCE" | wc -l)
        echo "plan_action events: $PLAN_EVENTS (expected >= 1)"
        [ "$PLAN_EVENTS" -ge 1 ] || { echo "ERROR: no plan_action event"; exit 1; }

        # Check 3: NO managed_tag_created event (apply-mode marker — would indicate dry-run was bypassed):
        APPLY_EVENTS=$(jq -c 'select(.event == "managed_tag_created")' "$EVIDENCE" | wc -l)
        echo "managed_tag_created events (apply-mode marker): $APPLY_EVENTS (expected 0)"
        [ "$APPLY_EVENTS" -eq 0 ] || { echo "CRITICAL: managed_tag_created event found in PR1 dry-run logs — dry_run was bypassed!"; exit 1; }

        # By exit code: arrconf binary should exit 0 (success):
        kubectl get "job/$SMOKE_JOB" -n selfhost -o jsonpath='{.status.conditions[?(@.type=="Complete")].status}'
        # Expected: True

    Step 4 — Cleanup the smoke job (optional — failedJobsHistoryLimit and successfulJobsHistoryLimit handle retention):
        kubectl delete "job/$SMOKE_JOB" -n selfhost --ignore-not-found

    Step 5 — Commit the evidence in arr-stack:
        cd /home/moi/projets/perso/arr-stack
        git add .planning/phases/02-arrconf-cluster-validation/evidence/pr1-job-logs-*.log
        git commit -m "evidence(02): capture PR1 dry-run cron Job logs (success criterion #2; W-06 verified events)"

    Constraint: DO NOT modify the my-kluster chart or values via `kubectl edit`. ArgoCD's `selfHeal: true` will revert within ~3 min (Pitfall 8). All chart changes go via PR.
  </action>
  <acceptance_criteria>
    - `find /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/ -name 'pr1-job-logs-*.log' -size +0c` returns at least 1 file
    - `wc -l .../evidence/pr1-job-logs-*.log` shows >= 3 lines
    - `head -1 .../evidence/pr1-job-logs-*.log | jq -e . > /dev/null` exits 0 (valid JSON Lines)
    - **W-06 dispositive**: `jq -c 'select(.event == "managed_tag_found" or .event == "would_create_managed_tag")' .../evidence/pr1-job-logs-*.log | wc -l` >= 1 (verified event names from sonarr.py)
    - **W-06 dispositive**: `jq -c 'select(.event == "plan_action")' .../evidence/pr1-job-logs-*.log | wc -l` >= 1 (verified event from differ.py)
    - **W-06 dispositive**: `jq -c 'select(.event == "managed_tag_created")' .../evidence/pr1-job-logs-*.log | wc -l` == 0 (apply-mode marker MUST be absent in PR1 dry-run)
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s` matches `evidence(02):` (commit landed)
  </acceptance_criteria>
  <verify>
    <automated>EVIDENCE=$(ls -t /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr1-job-logs-*.log 2&gt;/dev/null | head -1) &amp;&amp; test -n "$EVIDENCE" &amp;&amp; head -1 "$EVIDENCE" | jq -e . &gt; /dev/null &amp;&amp; [ "$(jq -c 'select(.event == "managed_tag_found" or .event == "would_create_managed_tag")' "$EVIDENCE" | wc -l)" -ge 1 ] &amp;&amp; [ "$(jq -c 'select(.event == "plan_action")' "$EVIDENCE" | wc -l)" -ge 1 ] &amp;&amp; [ "$(jq -c 'select(.event == "managed_tag_created")' "$EVIDENCE" | wc -l)" = "0" ]</automated>
  </verify>
  <done>Smoke job created via `kubectl create job --from=cronjob/arrconf`; completed successfully; logs captured to evidence/pr1-job-logs-<date>.log as valid JSON Lines; verified event names from sonarr.py + differ.py present (W-06): managed_tag_found OR would_create_managed_tag, plan_action; apply-mode marker `managed_tag_created` absent (dispositive proof of dry-run); evidence committed in arr-stack.</done>
</task>

<task type="auto">
  <name>Task 4.3: Re-snapshot post-PR1 + diff vs baseline (success criterion #3)</name>
  <files>
    arr-stack/snapshots/post-phase2-pr1-2026-05-08/
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
    - /home/moi/projets/perso/arr-stack/snapshots/before-phase-2-2026-05-08/sonarr/ (the baseline being compared against — produced in Plan 02-01)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md "Pattern 5"
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md "D-30 #2"
  </read_first>
  <action>
    Step 1 — Re-snapshot Sonarr (the only app PR1 could affect — only sonarr reconciler is active per D-25). Snapshot all 6 apps as in Plan 02-01, but the diff gate focuses on sonarr.
        cd /home/moi/projets/perso/arr-stack

        # Re-use the same env vars + port-forwards from Plan 02-01 Task 1.1.
        SNAPSHOT_DIR="snapshots/post-phase2-pr1-$(date +%F)"
        tools/snapshot/snapshot.sh --output "$SNAPSHOT_DIR/"

    Step 2 — Diff against the baseline. Success criterion #3 requires ZERO output:
        BASELINE="snapshots/before-phase-2-2026-05-08"
        DIFF_OUTPUT=$(diff -rq "$BASELINE/sonarr/" "$SNAPSHOT_DIR/sonarr/")
        if [ -z "$DIFF_OUTPUT" ]; then
          echo "PASS — zero writes during dry-run (success criterion #3 SATISFIED)"
        else
          echo "FAIL — diff detected during dry-run; arrconf wrote despite ARRCONF_DRY_RUN=true:"
          echo "$DIFF_OUTPUT"
          exit 1
        fi

    Step 3 — Commit the snapshot:
        git add "$SNAPSHOT_DIR/"
        git commit -m "$(cat <<'EOF'
snapshot(02): post-phase2-pr1 dry-run capture (D-30 #2)

PR1 deployed with arrconfDryRun: true; first cron Job ran successfully.
diff -rq vs snapshots/before-phase-2-2026-05-08/sonarr/ produced zero output.

ROADMAP success criterion #3 SATISFIED: zero writes during dry-run, confirmed
at cluster level. Phase 1's unit-tested apply --dry-run guarantee (52 respx-mocked
tests, 99% coverage on differ.py + reconcilers/sonarr.py) is corroborated end-to-end.

Evidence: .planning/phases/02-arrconf-cluster-validation/evidence/pr1-job-logs-<date>.log
Next: PR2 in my-kluster will flip arrconfDryRun: false (Wave 4).
EOF
)"
  </action>
  <acceptance_criteria>
    - `find /home/moi/projets/perso/arr-stack/snapshots/post-phase2-pr1-2026-05-08/sonarr -type f -name '*.json' | wc -l` >= 5
    - `test -f .../snapshots/post-phase2-pr1-2026-05-08/sonarr/downloadclient.json` exits 0
    - `test -f .../snapshots/post-phase2-pr1-2026-05-08/sonarr/tag.json` exits 0
    - `diff -rq .../before-phase-2-2026-05-08/sonarr/ .../post-phase2-pr1-2026-05-08/sonarr/ | wc -l` = 0 (CRITICAL — success criterion #3)
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s` matches `snapshot(02):`
  </acceptance_criteria>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack &amp;&amp; test -f snapshots/post-phase2-pr1-2026-05-08/sonarr/downloadclient.json &amp;&amp; test -f snapshots/post-phase2-pr1-2026-05-08/sonarr/tag.json &amp;&amp; [ "$(diff -rq snapshots/before-phase-2-2026-05-08/sonarr/ snapshots/post-phase2-pr1-2026-05-08/sonarr/ | wc -l)" = "0" ]</automated>
  </verify>
  <done>post-phase2-pr1-<date>/ snapshot captured for all 6 apps; diff -rq vs baseline for sonarr/ is empty (zero writes during dry-run); ROADMAP success criterion #3 SATISFIED; snapshot committed in arr-stack.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| my-kluster repo -> ArgoCD -> cluster | PR1 merge causes ArgoCD to apply chart manifests; trust = "the right human approved the PR" |
| operator workstation -> cluster (manual `kubectl apply` of secret) | Bootstrap secret applied imperatively; ArgoCD does NOT manage `secrets/` (W-05 verifies tracking-id absent) |
| Cluster CronJob pod -> Sonarr Service | In-cluster HTTP with X-Api-Key (from envFrom secret) — Phase 2 dry-run = GET only, no PUT/POST/DELETE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure (T-secret-leak) | `kubectl describe pod` env dump | mitigate | `envFrom: secretRef` (NOT `valueFrom: secretKeyRef`); kubectl describe shows only the secret name. Verified Phase 1 T-01-01. |
| T-02-02 | Information Disclosure (T-secret-leak) | Job pod logs (kubectl logs) | mitigate | structlog SecretStr masking from Phase 1 ensures `repr()` never emits API keys. |
| T-02-03 | Tampering (T-bootstrap-trust) | Bootstrap Secret could be silently adopted by ArgoCD if project sync rules change | mitigate | W-05 fix: acceptance criterion explicitly checks `argocd.argoproj.io/tracking-id` annotation is absent; if it ever appears, the manual lifecycle contract is broken and CLAUDE.md "Bootstrap secrets" is violated. |
| T-02-04 | Tampering (T-env-source-bypass) | Pod could read SONARR_API_KEY from a mounted file instead of env | mitigate | B-02 fix: acceptance criterion `volumeMounts has length == 1 and .[0].name == "config"` — only the ConfigMap mount allowed; any Secret-mounted volumeMount fails the gate. |
| T-02-05 | Confused Deputy (T-scope-bypass) | arrconf inadvertently writes during dry-run | mitigate | Phase 1 unit tests (52 respx-mocked) + W-06 fix: cluster logs MUST contain dry-run path event names AND MUST NOT contain `managed_tag_created` (apply-mode marker). Snapshot diff at Task 4.3 corroborates. |
| T-02-06 | Repudiation | Drift between scheduled cron + manual smoke job | accept | Task 4.2 forces an explicit smoke job with a unique name; kubectl logs persist to `evidence/`. |
| T-02-07 | Tampering (T-image-supply-chain) | Pod actually pulls the verified image | accept | GHCR public anonymous pull is ADR-3 acceptance. cosign signing = OPSV2-03. |
| T-02-08 | Elevation of Privilege | Pod escapes with extra K8s permissions | mitigate | Default ServiceAccount in selfhost has no extra RoleBindings; runAsNonRoot:true uid:1000; no privileged, no hostNetwork. |
</threat_model>

<verification>
End-of-plan gate (must all pass before Wave 4 starts):
1. PR1 merged in my-kluster; ArgoCD App `arrconf` Synced + Healthy
2. CronJob `arrconf` exists in selfhost with envFrom: arrconf-env, ARRCONF_DRY_RUN=true
3. **B-02**: CronJob volumeMounts = [{name: "config", ...}] exactly (length=1, only ConfigMap mount)
4. **W-05**: Secret arrconf-env has NO ArgoCD tracking-id annotation
5. At least one Job has run with kubectl logs containing the EXACT W-06 event names: `managed_tag_found` OR `would_create_managed_tag` + `plan_action`
6. Apply-mode marker `managed_tag_created` is ABSENT in PR1 logs (dispositive dry-run proof)
7. Evidence file `pr1-job-logs-<date>.log` committed in arr-stack
8. `snapshots/post-phase2-pr1-<date>/` committed in arr-stack
9. `diff -rq snapshots/before-phase-2-<date>/sonarr/ snapshots/post-phase2-pr1-<date>/sonarr/` produces ZERO output
</verification>

<success_criteria>
- ROADMAP success criterion #2 (CronJob exists in selfhost with envFrom secretRef) — SATISFIED by Task 4.1
- ROADMAP success criterion #3 (zero writes during dry-run, snapshot diff empty) — SATISFIED by Task 4.3
- REQ-bootstrap-exception (manual secret apply, env-only injection) — SATISFIED by Task 4.1 (B-02 volumeMount inspection + W-05 tracking-id check)
- REQ-secret-management (no secrets in arr-stack) — RE-VERIFIED by Task 4.1 step 3 (W-02 POSIX class)
- D-23 (schedule, concurrencyPolicy, history limits in cluster) — SATISFIED + observed
- D-26 (ArgoCD App Synced) — SATISFIED
- D-27 (ConfigMap mounted at /app/config/arrconf.yml) — SATISFIED + the smoke-job pod successfully reads it
- D-28 (PR1 with arrconfDryRun: true; observation cycle complete) — SATISFIED
- D-29 (Secret bound via envFrom; only SONARR_API_KEY) — SATISFIED + observed in cluster
- D-30 #2 (post-phase2-pr1-<date>/ committed) — SATISFIED
- B-02 fix (env-only secret injection proven at cluster level via volumeMounts inspection) — SATISFIED
- W-05 fix (bootstrap Secret manual lifecycle proven via tracking-id annotation absence) — SATISFIED
- W-06 fix (event names verified against reconciler source) — SATISFIED
</success_criteria>

<output>
After completion, create `/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-04-SUMMARY.md` documenting:
- PR1 number + merge timestamp
- Smoke job name + completion time + exit code
- Snapshot diff result (must be "ZERO output" — record as such)
- volumeMounts inspection result (B-02): expected `[{name: "config", ...}]`, observed: <result>
- ArgoCD tracking-id annotation check (W-05): expected empty, observed: <result>
- Citation of >= 2 JSON log lines from evidence file showing W-06 verified event names
- Confirmation that `managed_tag_created` is absent in PR1 logs (dispositive dry-run proof)
- Wave 4 readiness checklist
</output>
</content>
</invoke>