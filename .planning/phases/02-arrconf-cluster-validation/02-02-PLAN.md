---
phase: 02-arrconf-cluster-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
autonomous: false
requirements:
  - REQ-bootstrap-exception
user_setup:
  - service: GHCR
    why: "Phase 1 image must be publicly pullable for cluster anonymous pull (ADR-3); GHCR has no CLI for visibility toggle"
    dashboard_config:
      - task: "Toggle package visibility to Public"
        location: "https://github.com/users/tom333/packages/container/arr-stack-arrconf/settings -> Danger Zone -> Change visibility -> Public"
must_haves:
  truths:
    - "Git tag v0.1.0 pushed to arr-stack/main -> arrconf-image.yml workflow ran successfully -> ghcr.io/tom333/arr-stack-arrconf:0.1.0 (or :v0.1.0) exists on GHCR (resolves Phase 1 HUMAN-UAT #1)"
    - "GHCR package visibility = Public; `docker pull ghcr.io/tom333/arr-stack-arrconf:<tag>` succeeds anonymously (Pitfall 1, Phase 1 HUMAN-UAT #1)"
    - "Empirical image tag string is RECORDED (either '0.1.0' from metadata-action {{version}} default, or 'v0.1.0' from {{raw}}) — downstream Plan 02-03 substitutes this verified literal into values.yaml (Pitfall 2)"
    - "Image is the immutable artifact that runs the bootstrap-from-env binary in cluster (REQ-bootstrap-exception runtime guarantee — the image must be reachable for the secret-as-env injection to ever execute)"
    - "Phase 1 HUMAN-UAT #1 marked passed in 01-HUMAN-UAT.md"
    - "02-02-SUMMARY.md emits image_tag_verified inside a fenced code block under '## Machine-readable outputs' so Plan 02-03's grep consumer reads a clean key:value line (no markdown bullet/bold wrapper) — B-NEW-01 fix"
  artifacts:
    - path: "ghcr.io/tom333/arr-stack-arrconf:<recorded-tag>"
      provides: "Public, immutable Docker image consumed by my-kluster CronJob (cluster anonymous pull)"
    - path: "arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md"
      provides: "Phase 1 HUMAN-UAT closure record — item #1 marked passed with timestamp + recorded tag"
    - path: "arr-stack/.planning/phases/02-arrconf-cluster-validation/02-02-SUMMARY.md"
      provides: "Records image_tag_verified literal value (e.g. '0.1.0' or 'v0.1.0') consumed by Plan 02-03 — emitted as a clean `key: value` line inside a fenced code block under '## Machine-readable outputs' (B-NEW-01 fix)"
      contains: "image_tag_verified:"
  key_links:
    - from: "Git tag v0.1.0 in arr-stack"
      to: "ghcr.io/tom333/arr-stack-arrconf:<tag>"
      via: "GitHub Actions workflow .github/workflows/arrconf-image.yml triggered on tags: ['v*']"
      pattern: "tags:\\s*\\['v\\*'\\]"
    - from: "GHCR package visibility = Public"
      to: "Cluster anonymous pull at Pod startup (no imagePullSecrets per ADR-3)"
      via: "operator dashboard toggle, verified by `docker pull` from a logged-out client"
      pattern: "docker pull ghcr.io/tom333/arr-stack-arrconf"
    - from: "02-02-SUMMARY.md `## Machine-readable outputs` fenced block"
      to: "Plan 02-03 values.yaml image.tag substitution"
      via: "downstream plan greps `^image_tag_verified:` from inside the fenced code block (no markdown wrapping)"
      pattern: "image_tag_verified"
---

<deviation_note status="2026-05-08">
**Retargeted v0.1.0 → v0.1.2 (v0.1.1 also consumed by latent Phase 1 Dockerfile bug — astral-sh/uv:0.11 didn't exist; pinned to 0.9 in 76e2c97 follow-up).** First-push race: when initial `git push -u origin main` + `git push origin v0.1.0` were issued, the workflow file `arrconf-image.yml` was not yet indexed on the default branch, so no CI run fired. v0.1.0 tag is left in history as the bootstrap artifact (per CLAUDE.md: never `git tag -f` a published tag — always create a new tag). All references below to "v0.1.0" / "0.1.0" should be read as "v0.1.1" / "0.1.1" for the actually delivered image. Plan 02-03's regex patterns updated in lockstep to match the new literal.
</deviation_note>

<objective>
Wave 1 — Plan 02 of 2 (parallel with 02-01; sequential gate before 02-03 in Wave 2 — Pitfall 2 means 02-03 must read this plan's recorded image_tag_verified): Phase 1 IMAGE RELEASE.

Single deliverable: release `ghcr.io/tom333/arr-stack-arrconf:v0.1.0` on GHCR with Public visibility, verified pullable anonymously, with the empirical tag string recorded for Plan 02-03 to substitute into the chart's values.yaml.

This plan is cross-repo (arr-stack: tag push; GHCR: package visibility), checkpoint-gated (the GHCR public-visibility toggle has no CLI). Plan 02-03 depends on this plan because it needs the verified literal tag string to bake into values.yaml.image.tag — Pitfall 2 in RESEARCH.md is explicit that `metadata-action`'s default template strips the `v`, so the tag could be either `0.1.0` or `v0.1.0` — guessing wrong means PR1 fails with `ImagePullBackOff`.

Purpose: Close Phase 1 HUMAN-UAT #1 (GHCR public toggle) and produce the exact image tag literal that Plan 02-03 must use (no inference allowed downstream).

Output: v0.1.0 git tag pushed; CI workflow successful; GHCR package public; anonymous pull verified; image_tag_verified field recorded in 02-02-SUMMARY.md (as a clean fenced-code line — B-NEW-01 fix); Phase 1 HUMAN-UAT #1 marked passed.

Cross-repo lifecycle: arr-stack repo (tag push, UAT update commit). No my-kluster changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-VALIDATION.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
@/home/moi/projets/perso/arr-stack/.github/workflows/arrconf-image.yml
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Push v0.1.0 git tag + watch CI + record GHCR tag string</name>
  <files>
    arr-stack/  # Git tag v0.1.0 (no checked-in file artifact in this task)
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.github/workflows/arrconf-image.yml (verify tag -> image pipeline; metadata-action template — does it use `{{version}}` (strips v) or `{{raw}}` (keeps v)?)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md "Pitfall 2" (tag format empirical verification)
    - /home/moi/projets/perso/arr-stack/CLAUDE.md "Release" + "Ne pas faire" sections (no :latest in production)
  </read_first>
  <action>
    Step 1 — Push the tag (in arr-stack):
        cd /home/moi/projets/perso/arr-stack
        # Verify tag does not already exist locally or on remote:
        git tag -l v0.1.0   # expect empty
        git ls-remote --tags origin v0.1.0   # expect empty

        git tag v0.1.0
        git push origin v0.1.0

    Step 2 — Wait for CI (arrconf-image.yml triggers on `tags: ['v*']`):
        gh run list --workflow=arrconf-image.yml --limit 1
        # Block until completed:
        gh run watch
        # Confirm conclusion=success:
        CONCLUSION=$(gh run list --workflow=arrconf-image.yml --limit 1 --json conclusion --jq '.[0].conclusion')
        echo "CI conclusion: $CONCLUSION"
        if [ "$CONCLUSION" != "success" ]; then
          echo "ERROR: CI failed; investigate before proceeding to GHCR toggle"
          gh run view --log-failed
          exit 1
        fi

    Step 3 — Record the empirical image tag string from GHCR (Pitfall 2):
        gh api -H "Accept: application/vnd.github+json" \
          /users/tom333/packages/container/arr-stack-arrconf/versions \
          | jq -r '.[] | .metadata.container.tags[]' | sort -u
        # Expected output:
        #   - either `0.1.0` (most likely — metadata-action default template strips the `v`)
        #   - or `v0.1.0` (only if workflow uses `{{raw}}` template)
        #   - plus: `latest`, `sha-<short>`, `branch-main`

        # Capture the verified tag (the one matching ^v?0\.1\.0$):
        RECORDED_TAG=$(gh api -H "Accept: application/vnd.github+json" \
          /users/tom333/packages/container/arr-stack-arrconf/versions \
          | jq -r '[.[].metadata.container.tags[]] | map(select(test("^v?0\\.1\\.0$")))[0]')
        echo "Recorded tag: $RECORDED_TAG"

    Constraints:
    - DO NOT amend the tag if CI fails (CLAUDE.md: never `git tag -f` a published tag — create v0.1.1 instead).
    - DO NOT use :latest in any downstream config (CLAUDE.md hard rule).
    - DO NOT toggle GHCR visibility yet (Task 2.2 — separate checkpoint with anonymous-pull verification).

    Stash $RECORDED_TAG for Task 2.2 (e.g., write to a temp shell variable file or remember to type the value back at the checkpoint).
  </action>
  <acceptance_criteria>
    - `git -C /home/moi/projets/perso/arr-stack tag -l v0.1.0` returns `v0.1.0` (tag exists locally)
    - `git -C /home/moi/projets/perso/arr-stack ls-remote --tags origin v0.1.0 | wc -l` >= 1 (tag pushed to origin)
    - `gh run list --workflow=arrconf-image.yml --limit 1 --json conclusion --jq '.[0].conclusion'` returns `success`
    - `gh api /users/tom333/packages/container/arr-stack-arrconf/versions | jq -r '[.[].metadata.container.tags[]] | map(select(test("^v?0\\.1\\.0$"))) | length'` >= 1
    - The recorded tag literal matches `^v?0\.1\.0$` (i.e., either `0.1.0` or `v0.1.0`)
  </acceptance_criteria>
  <verify>
    <automated>git -C /home/moi/projets/perso/arr-stack tag -l v0.1.0 | grep -q '^v0\.1\.0$' &amp;&amp; gh api /users/tom333/packages/container/arr-stack-arrconf/versions 2&gt;/dev/null | jq -e '[.[].metadata.container.tags[]] | any(test("^v?0\\.1\\.0$"))' &gt; /dev/null &amp;&amp; [ "$(gh run list --workflow=arrconf-image.yml --limit 1 --json conclusion --jq '.[0].conclusion')" = "success" ]</automated>
  </verify>
  <done>v0.1.0 tag pushed to origin; arrconf-image.yml CI run completed=success; image visible on GHCR with recorded tag literal matching ^v?0.1.0$; recorded tag captured (in shell context or notes) for Task 2.2 anonymous-pull verification.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2.2: Toggle GHCR package visibility -> Public + verify anonymous pull + close Phase 1 HUMAN-UAT #1</name>
  <files>
    arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md (item #1 — GHCR public toggle; this task closes it)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md "Pitfall 1" + "Pitfall 9" sections
  </read_first>
  <what-built>
    Task 2.1 already pushed v0.1.0 and confirmed the image is on GHCR with a recorded tag literal. This checkpoint requires the operator to (a) toggle GHCR package visibility to Public via the dashboard (no CLI exists for this), and (b) confirm anonymous pull works. After resume, the executor closes Phase 1 HUMAN-UAT #1.
  </what-built>
  <action>
    Step 1 — Manual: Toggle GHCR package visibility to Public (Pitfall 1):
        Visit https://github.com/users/tom333/packages/container/arr-stack-arrconf/settings
        Scroll to "Danger Zone" -> "Change package visibility" -> select "Public" -> confirm.

    Step 2 — Verify anonymous pull from a logged-out daemon (automated, runs after toggle):
        # Use the recorded tag from Task 2.1:
        RECORDED_TAG="<paste the recorded tag literal: 0.1.0 or v0.1.0>"

        # Force-logout from ghcr.io (idempotent):
        docker logout ghcr.io 2>/dev/null || true

        # Pull anonymously (must succeed):
        docker pull "ghcr.io/tom333/arr-stack-arrconf:$RECORDED_TAG"
        # Expected: success, no "denied" / "unauthorized" error.

        # Cleanup:
        docker rmi "ghcr.io/tom333/arr-stack-arrconf:$RECORDED_TAG"

    Step 3 — Update Phase 1 HUMAN-UAT (mark item #1 resolved + add recorded tag):
        Edit /home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
        Change item #1 result line to:
            [passed: GHCR public toggle confirmed 2026-05-08; tag <RECORDED_TAG> pulls anonymously]
        Update the Summary count if needed (passed=N+1).

    Step 4 — Commit the UAT update in arr-stack:
        cd /home/moi/projets/perso/arr-stack
        git add .planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
        git commit -m "docs(01): mark HUMAN-UAT #1 (GHCR public toggle) resolved by Phase 2 v0.1.0 release"

    DO NOT proceed to Plan 02-03 (chart authoring) until anonymous pull is confirmed AND the recorded tag is in 02-02-SUMMARY.md (next plan reads it).
  </action>
  <how-to-verify>
    1. `gh api /users/tom333/packages/container/arr-stack-arrconf | jq -r '.visibility'` returns `public`
    2. `docker logout ghcr.io 2>/dev/null; docker pull ghcr.io/tom333/arr-stack-arrconf:<RECORDED_TAG>` exits 0
    3. `grep -A2 -E '^### 1\.|^## 1\. GHCR' /home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md | grep -q 'passed'` exits 0
    4. `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s | grep -q 'docs(01):'` exits 0
  </how-to-verify>
  <acceptance_criteria>
    - GHCR package visibility = `public` (verified via gh api)
    - Anonymous `docker pull ghcr.io/tom333/arr-stack-arrconf:<RECORDED_TAG>` succeeds from a logged-out client
    - 01-HUMAN-UAT.md item #1 marked `passed` with timestamp + recorded tag
    - Commit landed: `docs(01): mark HUMAN-UAT #1 ...`
  </acceptance_criteria>
  <verify>
    <automated>[ "$(gh api /users/tom333/packages/container/arr-stack-arrconf 2&gt;/dev/null | jq -r '.visibility')" = "public" ] &amp;&amp; grep -E '^### 1\.|^## 1\. GHCR' /home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md -A 5 | grep -q 'passed'</automated>
  </verify>
  <resume-signal>Type "approved: <RECORDED_TAG>" (e.g. "approved: 0.1.0") with the empirical tag string captured in Task 2.1, OR describe what failed (CI error, GHCR not pullable, tag format unexpected, dashboard toggle blocked).</resume-signal>
  <done>GHCR visibility=public; anonymous pull verified; 01-HUMAN-UAT #1 closed with recorded tag literal; commit landed in arr-stack; recorded tag will be captured into 02-02-SUMMARY.md so Plan 02-03 can substitute it into values.yaml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arr-stack repo (tag push) -> GHCR (CI publishes image) | Trust = "the right human pushed v0.1.0"; CI runs in GitHub-hosted runners; provenance attestation is in workflow run logs |
| GHCR (Public visibility) -> any internet client | Anonymous pull = ADR-3 acceptance; no signature verification (cosign deferred to OPSV2-03) |
| Operator workstation -> GHCR dashboard (visibility toggle) | UI-only; trust = GitHub session of `tom333` account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (T-image-supply-chain) | ghcr.io/tom333/arr-stack-arrconf:0.1.0 | mitigate | Pin to immutable semver tag (no :latest) per CLAUDE.md hard rule. metadata-action emits SHA tag for post-hoc integrity audit. cosign signing = OPSV2-03 (v2). |
| T-02-02 | Repudiation | Tag amendment | mitigate | CLAUDE.md prohibits `git tag -f`; if v0.1.0 fails, create v0.1.1 instead. Git tag history preserves audit trail. |
| T-02-03 | Information Disclosure | Image content | mitigate | Phase 1 image is built from arr-stack `tools/arrconf/` Python source — no secrets baked in (env-only injection at runtime). The Dockerfile uses USER 1000:1000, no embedded credentials, no `.env` files. |
| T-02-04 | Confused Deputy | Empirical tag literal mismatch (Pitfall 2) | mitigate | Task 2.1 reads back the actual tag from GHCR API after CI completes; Task 2.2 verifies anonymous pull with that exact literal. Plan 02-03 reads the recorded value from 02-02-SUMMARY.md (not inferred). |
</threat_model>

<verification>
End-of-plan gate (must all pass before Plan 02-03 can run):
1. v0.1.0 tag exists on origin
2. arrconf-image.yml CI conclusion=success
3. GHCR package visibility=public
4. Anonymous `docker pull` succeeds with recorded tag
5. 01-HUMAN-UAT #1 marked passed
6. Recorded tag literal documented in 02-02-SUMMARY.md inside the `## Machine-readable outputs` fenced code block (Plan 02-03 grep-consumer input)
</verification>

<success_criteria>
- Phase 1 HUMAN-UAT #1 (GHCR public toggle) — SATISFIED by Task 2.2
- REQ-bootstrap-exception (image is the runtime artifact for the env-injection Pod) — SATISFIED (image exists, is pullable, will run as the bootstrap-from-env binary in Wave 3)
- D-01 carry-forward (release manuelle, image GHCR :vX.Y.Z, never :latest) — SATISFIED + grep-verifiable downstream in Plan 02-03
- ADR-3 (anonymous pull design) — SATISFIED + verified
- Pitfall 2 resolved (empirical tag literal recorded, not inferred) — SATISFIED
- B-NEW-01 fix (machine-readable SUMMARY emission format compatible with 02-03's grep consumer) — SATISFIED via the `## Machine-readable outputs` fenced-block contract below
</success_criteria>

<output>
After completion, create `/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-02-SUMMARY.md` documenting:

1. **CRITICAL — Machine-readable section (B-NEW-01 contract).** The SUMMARY MUST contain a section with this EXACT shape (heading + fenced code block). Plan 02-03 Task 3.1 greps `^image_tag_verified:` from inside this fence; any markdown wrapping (bullets, bold, inline code) breaks the consumer:

   ```
   ## Machine-readable outputs

   ```
   image_tag_verified: 0.1.0
   ```
   ```

   - The literal value (`0.1.0` here) MUST be replaced with whatever Task 2.1 verified on GHCR — either `0.1.0` (metadata-action default) or `v0.1.0` (raw template). MUST match `^v?0\.1\.0$`.
   - The line inside the fence MUST start with `image_tag_verified:` (no `- `, no `**`, no leading whitespace). One space after the colon, then the value, then end of line.
   - Only ONE `image_tag_verified:` line in the SUMMARY (consumer takes the first match).
   - The fence is a triple-backtick block with no language tag (plain text content). The closing fence is on its own line.

2. **Human-readable narrative section** documenting:
   - v0.1.0 tag SHA + push timestamp
   - arrconf-image.yml CI run id + duration
   - GHCR package visibility toggle timestamp + verifying client (logged-out docker daemon)
   - 01-HUMAN-UAT #1 closure timestamp
   - Any non-fatal CI warnings (deprecation notices etc. — for monitoring)
</output>
</content>
</invoke>
