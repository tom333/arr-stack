---
phase: 02-arrconf-cluster-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 02-02
files_modified:
  - arr-stack/.planning/phases/02-arrconf-cluster-validation/.cluster-services
  - my-kluster/charts/arrconf/Chart.yaml
  - my-kluster/charts/arrconf/values.yaml
  - my-kluster/charts/arrconf/files/arrconf.yml
  - my-kluster/charts/arrconf/templates/_helpers.tpl
  - my-kluster/charts/arrconf/templates/cronjob.yaml
  - my-kluster/charts/arrconf/templates/configmap.yaml
  - my-kluster/charts/arrconf/README.md
  - my-kluster/argocd/argocd-apps/arrconf-app.yaml
  - my-kluster/secrets/arrconf-secret.yaml
autonomous: false
requirements:
  - REQ-bootstrap-exception
  - REQ-secret-management
must_haves:
  truths:
    - "Sonarr Service hostname captured from my-kluster ArgoCD app yaml: literal value `sonarr.selfhost.svc.cluster.local:8989` (verified W-03)"
    - "qBittorrent Service hostname captured from my-kluster ArgoCD app yaml: literal value `qbittorrent.selfhost.svc.cluster.local:8080` (verified W-03)"
    - "Cluster service hostnames recorded in `.planning/phases/02-arrconf-cluster-validation/.cluster-services` (committed in arr-stack as the canonical capture-source for downstream chart-authoring)"
    - "my-kluster/charts/arrconf/ mini-chart authored: Chart.yaml, values.yaml, files/arrconf.yml, templates/_helpers.tpl, templates/cronjob.yaml, templates/configmap.yaml, README.md (D-24, D-25, D-27)"
    - "my-kluster/charts/arrconf/values.yaml ships with arrconfDryRun: true (D-28 PR1 default) and image.tag pinned to the empirically-verified Phase 1 tag from 02-02-SUMMARY.md (never :latest per CLAUDE.md, D-01 carry-forward)"
    - "my-kluster/argocd/argocd-apps/arrconf-app.yaml authored mirroring configarr-app.yaml (D-26)"
    - "my-kluster/secrets/arrconf-secret.yaml authored with ONLY SONARR_API_KEY in stringData (D-29 least-privilege; REQ-bootstrap-exception, REQ-secret-management)"
    - "helm lint my-kluster/charts/arrconf/ exits 0; helm template renders valid manifests with envFrom + ARRCONF_DRY_RUN env + image pinned"
    - "No plaintext API keys committed in arr-stack repo (CLAUDE.md anti-secret discipline; REQ-secret-management) — grep audit passes"
    - "End-of-plan checkpoint: operator confirms my-kluster working tree state matches expected file list before Wave 3 starts (B-01 fix — guards against partial-write resume / stale staged leftovers; W-NEW-01 fix — also asserts no untracked/modified out-of-scope files in my-kluster)"
  artifacts:
    - path: "arr-stack/.planning/phases/02-arrconf-cluster-validation/.cluster-services"
      provides: "Verified Sonarr + qBittorrent Service hostname literals captured from my-kluster ArgoCD app yamls; downstream chart-authoring substitutes from this file (W-03 fix)"
      contains: "sonarr.selfhost.svc.cluster.local:8989"
    - path: "my-kluster/charts/arrconf/Chart.yaml"
      provides: "Helm chart manifest (mirror configarr/Chart.yaml)"
      contains: "name: arrconf"
    - path: "my-kluster/charts/arrconf/values.yaml"
      provides: "Helm values with arrconfDryRun: true (PR1 default), image.tag pinned to image_tag_verified from 02-02-SUMMARY.md, Renovate annotation"
      contains: "arrconfDryRun: true"
    - path: "my-kluster/charts/arrconf/files/arrconf.yml"
      provides: "Reconciler config — Sonarr-only with 1 download_client (qBittorrent), prune: false, base_url + qBit host literal from .cluster-services"
      contains: "apps:"
    - path: "my-kluster/charts/arrconf/templates/cronjob.yaml"
      provides: "CronJob template with envFrom: secretRef, ARRCONF_DRY_RUN env, securityContext, args explicit, NO tty:true (Pitfall 10 — JSON logs)"
    - path: "my-kluster/charts/arrconf/templates/configmap.yaml"
      provides: "ConfigMap template injecting files/arrconf.yml via .Files.Get"
    - path: "my-kluster/argocd/argocd-apps/arrconf-app.yaml"
      provides: "ArgoCD Application targeting selfhost-project/selfhost namespace"
      contains: "name: arrconf"
    - path: "my-kluster/secrets/arrconf-secret.yaml"
      provides: "Bootstrap Secret with SONARR_API_KEY only (D-29)"
      contains: "name: arrconf-env"
  key_links:
    - from: "my-kluster/charts/arrconf/values.yaml"
      to: "ghcr.io/tom333/arr-stack-arrconf:<image_tag_verified>"
      via: "image.repository + image.tag (literal value read from 02-02-SUMMARY.md `## Machine-readable outputs` fenced block)"
      pattern: "ghcr\\.io/tom333/arr-stack-arrconf"
    - from: "my-kluster/charts/arrconf/templates/cronjob.yaml"
      to: "my-kluster/secrets/arrconf-secret.yaml"
      via: "envFrom: secretRef: name: {{ .Values.apiKeysSecret }}"
      pattern: "envFrom"
    - from: "my-kluster/charts/arrconf/templates/configmap.yaml"
      to: "my-kluster/charts/arrconf/files/arrconf.yml"
      via: ".Files.Get \"files/arrconf.yml\""
      pattern: "\\.Files\\.Get"
    - from: "my-kluster/argocd/argocd-apps/arrconf-app.yaml"
      to: "my-kluster/charts/arrconf/"
      via: "spec.source.path: charts/arrconf"
      pattern: "path: charts/arrconf"
    - from: "my-kluster/charts/arrconf/files/arrconf.yml"
      to: "Sonarr Service in selfhost"
      via: "base_url: http://sonarr.selfhost.svc.cluster.local:8989 (literal from .cluster-services)"
      pattern: "sonarr\\.selfhost\\.svc\\.cluster\\.local:8989"
---

<objective>
Wave 2 — Plan 03 of 5 (depends on 02-02 for image_tag_verified): MY-KLUSTER CHART AUTHORING.

Note (W-NEW-02 fix): wave bumped from 1 → 2 because this plan's `depends_on: ["02-02"]` makes wave: 1 inconsistent with parallel-scheduling semantics. /gsd-execute-phase treats wave as authoritative; depends_on is the fallback. Bumping wave preserves the documented dependency edge.

Single deliverable: author the full `my-kluster/charts/arrconf/` mini-chart (D-24) + ArgoCD App + bootstrap Secret YAML; capture verified Sonarr + qBittorrent service hostnames; helm lint + helm template + arr-stack secret-leak audit; end-of-plan checkpoint guarding cross-repo working-tree state before Wave 3 begins.

Depends on Plan 02-02 because values.yaml.image.tag MUST use the empirical tag literal recorded in 02-02-SUMMARY.md (Pitfall 2 — guessing wrong = ImagePullBackOff in Wave 3).

This plan addresses checker findings:
- B-01 (cross-repo authoring lifecycle gap): final checkpoint shows `git -C my-kluster status --short` and requires operator acknowledgment before Wave 3
- B-04 (scope_sanity): split off from original 02-01 — chart authoring is its own value-stream
- B-NEW-01 (cross-plan data contract): Task 3.1 Step 2 reads `image_tag_verified` from inside the `## Machine-readable outputs` fenced block in 02-02-SUMMARY.md (not from a markdown-wrapped bullet) — round-trip with 02-02 emission format is now clean
- W-02 (`\s` in regex): replaced with `[[:space:]]` POSIX class throughout
- W-03 (qBit/Sonarr hostnames unverified): upfront task captures literal hostnames, baked into substitution map
- W-NEW-01 (cross-repo verify scope leak): Task 3.4 augmented to also assert NO untracked/modified out-of-scope files in my-kluster — catches stale leftovers at unrelated paths (e.g., `charts/arrconf-old/`)
- W-NEW-02 (wave/depends_on inconsistency): wave: 2 (was 1) — see note above

Purpose: produce a fully-authored, lint-validated mini-chart in my-kluster's working tree, ready for Wave 3 (Plan 02-04, PR1) to commit and PR. The end-of-plan checkpoint ensures the tree state is exactly what 02-04 Task 4.1 will stage.

Output: 9 files in my-kluster working tree (uncommitted) + 1 file in arr-stack (.cluster-services, committed) + helm lint + helm template + grep audit all green + operator acknowledgment of cross-repo tree state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-VALIDATION.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-02-SUMMARY.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/examples/baseline-sonarr.yml
@/home/moi/projets/perso/my-kluster/CLAUDE.md
@/home/moi/projets/perso/my-kluster/charts/configarr/Chart.yaml
@/home/moi/projets/perso/my-kluster/charts/configarr/values.yaml
@/home/moi/projets/perso/my-kluster/charts/configarr/templates/_helpers.tpl
@/home/moi/projets/perso/my-kluster/charts/configarr/templates/cronjob.yaml
@/home/moi/projets/perso/my-kluster/charts/configarr/templates/configmap.yaml
@/home/moi/projets/perso/my-kluster/argocd/argocd-apps/configarr-app.yaml
@/home/moi/projets/perso/my-kluster/argocd/argocd-apps/sonarr-app.yaml
@/home/moi/projets/perso/my-kluster/argocd/argocd-apps/qbittorrent-app.yaml
@/home/moi/projets/perso/my-kluster/argocd/argocd-appprojects/selfhost-project.yaml
@/home/moi/projets/perso/my-kluster/secrets/configarr-secret.yaml

<interfaces>
<!-- Concrete substitution map for the mini-chart, sourced from 02-PATTERNS.md. -->
<!-- Executor should NOT need to re-read PATTERNS.md to author files. -->

# Verified Sonarr + qBittorrent Service hostnames (W-03 fix — captured from my-kluster ArgoCD app yamls):
#   sonarr.selfhost.svc.cluster.local:8989       (from sonarr-app.yaml: service.main.ports.http.port=8989)
#   qbittorrent.selfhost.svc.cluster.local:8080  (from qbittorrent-app.yaml: service.main.ports.http.port=8080)
# These are the literal values to bake into files/arrconf.yml — no inference, no port-forward to discover.

# Image tag (W-03 / Pitfall 2 — read from 02-02-SUMMARY.md `## Machine-readable outputs` fenced block, B-NEW-01):
#   image_tag_verified: <0.1.1 OR v0.1.1> (verified empirically by Plan 02-02 anonymous pull test; v0.1.0 superseded — 02-02 deviation_note)

# my-kluster/charts/arrconf/Chart.yaml (final shape, 6 lines)
apiVersion: v2
name: arrconf
description: arrconf — config-as-code reconciler for Sonarr/Radarr/Prowlarr/qBit/Seerr (Phase 2 scope: Sonarr download_clients only)
type: application
version: 0.1.1
appVersion: "0.1.1"

# my-kluster/charts/arrconf/values.yaml (final shape — copy literally; substitute <IMAGE_TAG_VERIFIED> from 02-02-SUMMARY.md)
image:
  # renovate: image=ghcr.io/tom333/arr-stack-arrconf
  repository: ghcr.io/tom333/arr-stack-arrconf
  tag: "<IMAGE_TAG_VERIFIED>"   # literal value from 02-02-SUMMARY.md image_tag_verified field (read from fenced block)
  pullPolicy: IfNotPresent

schedule: "0 */4 * * *"
timezone: "Europe/Paris"
startingDeadlineSeconds: 600

# Two-PR dry-run protocol (D-28):
#   PR1: arrconfDryRun: true   -> observe logs, prove zero writes
#   PR2: arrconfDryRun: false  -> enable apply
arrconfDryRun: true

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 50m
    memory: 128Mi

# Manual bootstrap secret (D-29):
#   kubectl apply -f my-kluster/secrets/arrconf-secret.yaml BEFORE merging PR1
apiKeysSecret: arrconf-env

successfulJobsHistoryLimit: 1
failedJobsHistoryLimit: 2

# my-kluster/charts/arrconf/templates/_helpers.tpl
# Mechanical rename: copy configarr/templates/_helpers.tpl, then global s/configarr\./arrconf./g
# 5 helpers: arrconf.name, arrconf.fullname, arrconf.chart, arrconf.labels, arrconf.selectorLabels

# my-kluster/charts/arrconf/templates/configmap.yaml (final shape, 9 lines)
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "arrconf.fullname" . }}
  labels:
    {{- include "arrconf.labels" . | nindent 4 }}
data:
  arrconf.yml: |-
{{ .Files.Get "files/arrconf.yml" | indent 4 }}

# my-kluster/charts/arrconf/templates/cronjob.yaml (final shape — copy literally)
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "arrconf.fullname" . }}
  labels:
    {{- include "arrconf.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.schedule | quote }}
  successfulJobsHistoryLimit: {{ .Values.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.failedJobsHistoryLimit }}
  startingDeadlineSeconds: {{ .Values.startingDeadlineSeconds | default 600 }}
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            {{- include "arrconf.selectorLabels" . | nindent 12 }}
          annotations:
            checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        spec:
          restartPolicy: Never
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
          containers:
            - name: arrconf
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              args: ["apply", "--config", "/app/config/arrconf.yml", "--apps", "sonarr"]
              env:
                - name: TZ
                  value: {{ .Values.timezone | quote }}
                - name: ARRCONF_DRY_RUN
                  value: {{ .Values.arrconfDryRun | quote }}
              envFrom:
                - secretRef:
                    name: {{ .Values.apiKeysSecret }}
              volumeMounts:
                - name: config
                  mountPath: /app/config/arrconf.yml
                  subPath: arrconf.yml
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
          volumes:
            - name: config
              configMap:
                name: {{ include "arrconf.fullname" . }}

# my-kluster/argocd/argocd-apps/arrconf-app.yaml (final shape — 23 lines)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: arrconf
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: selfhost
    server: https://kubernetes.default.svc
  project: selfhost-project
  source:
    repoURL: https://github.com/tom333/my-kluster.git
    targetRevision: HEAD
    path: charts/arrconf
  syncPolicy:
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
    automated:
      selfHeal: true
      prune: true

# my-kluster/secrets/arrconf-secret.yaml (final shape — 8 lines)
apiVersion: v1
kind: Secret
metadata:
  name: arrconf-env
  namespace: selfhost
type: Opaque
stringData:
  SONARR_API_KEY: "<copy from my-kluster/secrets/configarr-secret.yaml SONARR_API_KEY value — same Sonarr instance, RESEARCH.md Open Q3>"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Capture verified Sonarr + qBittorrent Service hostnames into .cluster-services (W-03 + B-NEW-01 fix)</name>
  <files>
    arr-stack/.planning/phases/02-arrconf-cluster-validation/.cluster-services
  </files>
  <read_first>
    - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/sonarr-app.yaml (capture metadata.name + spec.source.helm.values service.main.ports.http.port)
    - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/qbittorrent-app.yaml (capture metadata.name + spec.source.helm.values service.main.ports.http.port)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-02-SUMMARY.md (image_tag_verified field — captured from Plan 02-02 inside the `## Machine-readable outputs` fenced code block per B-NEW-01 contract)
  </read_first>
  <action>
    Step 1 — Read the my-kluster ArgoCD app yamls and extract the literal Service name + port:
        SONARR_APP=/home/moi/projets/perso/my-kluster/argocd/argocd-apps/sonarr-app.yaml
        QBT_APP=/home/moi/projets/perso/my-kluster/argocd/argocd-apps/qbittorrent-app.yaml

        # Verify the metadata.name (== Service name when the app-template chart creates a service called "main"):
        # In bjw-s/app-template, with `service.main.controller: main`, the resulting Service is named after the Application (release name).
        # Sonarr Service: `sonarr` ; port: from service.main.ports.http.port = 8989
        # qBit Service:   `qbittorrent` ; port: from service.main.ports.http.port = 8080
        SONARR_NAME=$(grep -A2 '^metadata:' "$SONARR_APP" | grep '  name:' | awk '{print $2}')
        QBT_NAME=$(grep -A2 '^metadata:' "$QBT_APP" | grep '  name:' | awk '{print $2}')
        SONARR_PORT=8989   # verified via grep '  port: 8989' on $SONARR_APP — the service.main.ports.http.port literal
        QBT_PORT=8080      # verified via grep '  port: 8080' on $QBT_APP

        echo "Sonarr Service: ${SONARR_NAME}.selfhost.svc.cluster.local:${SONARR_PORT}"
        echo "qBit Service:   ${QBT_NAME}.selfhost.svc.cluster.local:${QBT_PORT}"

    Step 2 — Read image_tag_verified from 02-02-SUMMARY.md (B-NEW-01 fix — read from inside fenced code block, NOT from a markdown-wrapped bullet/bold/inline-code line):

        SUMMARY=/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-02-SUMMARY.md

        # The B-NEW-01 contract emits the value as a clean line inside a fenced code block:
        #     ## Machine-readable outputs
        #     ```
        #     image_tag_verified: 0.1.1
        #     ```
        # So inside the fence the line literally starts with `image_tag_verified:` (no `- `, no `**`).
        # The simpler grep below works because it anchors `^image_tag_verified:` AND requires the value
        # to match a semver shape — which the markdown wrapper line in the old format would not satisfy.
        IMAGE_TAG_VERIFIED=$(grep -E '^image_tag_verified:[[:space:]]*v?[0-9]+\.[0-9]+\.[0-9]+$' "$SUMMARY" | head -1 | awk '{print $2}')

        # Belt-and-braces: if the simpler grep fails for any reason (stray indentation), fall back to an
        # awk that walks the fenced block explicitly:
        if [ -z "$IMAGE_TAG_VERIFIED" ]; then
          IMAGE_TAG_VERIFIED=$(awk '
            /^## Machine-readable outputs/{flag=1; next}
            /^```$/{
              if(flag==1){flag=2; next}
              else if(flag==2){exit}
            }
            flag==2 && /^image_tag_verified:/{print $2; exit}
          ' "$SUMMARY")
        fi
        echo "Verified image tag: $IMAGE_TAG_VERIFIED"

        # Sanity check (REQUIRED — fail-loud per B-NEW-01 fix). Tag must be present AND match ^v?0\.1\.[01]$ (0.1.0 superseded by 0.1.1 per 02-02 deviation_note):
        if [ -z "$IMAGE_TAG_VERIFIED" ]; then
          echo "ERROR: image_tag_verified missing from 02-02-SUMMARY.md."
          echo "Expected format (B-NEW-01 contract):"
          echo "  ## Machine-readable outputs"
          echo "  \`\`\`"
          echo "  image_tag_verified: 0.1.1"
          echo "  \`\`\`"
          exit 1
        fi
        echo "$IMAGE_TAG_VERIFIED" | grep -qE '^v?0\.1\.[01]$' || {
          echo "ERROR: image_tag_verified value '$IMAGE_TAG_VERIFIED' does not match ^v?0\\.1\\.[01]$"
          exit 1
        }

    Step 3 — Write the .cluster-services capture file in arr-stack:
        cat > /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/.cluster-services <<EOF
# Captured 2026-05-08 from my-kluster ArgoCD app yamls (W-03 fix).
# Source: argocd/argocd-apps/{sonarr,qbittorrent}-app.yaml metadata.name + service.main.ports.http.port.
# These literal values MUST be substituted into files/arrconf.yml in Task 3.2.

SONARR_HOST=sonarr.selfhost.svc.cluster.local
SONARR_PORT=8989
SONARR_BASE_URL=http://sonarr.selfhost.svc.cluster.local:8989

QBT_HOST=qbittorrent.selfhost.svc.cluster.local
QBT_PORT=8080

# From 02-02-SUMMARY.md image_tag_verified field, fenced block (Pitfall 2; B-NEW-01 contract):
IMAGE_TAG_VERIFIED=$IMAGE_TAG_VERIFIED
EOF

    Step 4 — Commit .cluster-services in arr-stack (small canonical capture, gitignored=NO):
        cd /home/moi/projets/perso/arr-stack
        git add .planning/phases/02-arrconf-cluster-validation/.cluster-services
        git commit -m "docs(02): capture verified cluster service hostnames + image tag (W-03, B-NEW-01)"

    Constraints:
    - DO NOT use `kubectl get svc` to discover hostnames — read from the my-kluster yamls instead (deterministic, version-controlled, no live-cluster dependency).
    - DO NOT commit secrets in this file — only hostnames + ports + image tag (no API keys).
    - DO NOT silently default `IMAGE_TAG_VERIFIED` to `0.1.1` (or `0.1.0`) if extraction fails — the script must hard-fail so 02-02's emit format and 02-03's consumer round-trip remains an enforced contract.
  </action>
  <acceptance_criteria>
    - `test -f /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/.cluster-services` exits 0
    - `grep -E '^SONARR_HOST=sonarr\.selfhost\.svc\.cluster\.local$' /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/.cluster-services` returns 1 match
    - `grep -E '^SONARR_PORT=8989$' .../.cluster-services` returns 1 match
    - `grep -E '^QBT_HOST=qbittorrent\.selfhost\.svc\.cluster\.local$' .../.cluster-services` returns 1 match
    - `grep -E '^QBT_PORT=8080$' .../.cluster-services` returns 1 match
    - `grep -E '^IMAGE_TAG_VERIFIED=v?0\.1\.[01]$' .../.cluster-services` returns 1 match
    - **B-NEW-01 dispositive (round-trip)**: `[ -n "$IMAGE_TAG_VERIFIED" ] && echo "$IMAGE_TAG_VERIFIED" | grep -qE '^v?0\.1\.[01]$'` — i.e., the value extracted from 02-02-SUMMARY.md's fenced `## Machine-readable outputs` block is non-empty and matches the expected semver
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s | grep -q 'docs(02):.*W-03'` exits 0
  </acceptance_criteria>
  <verify>
    <automated>F=/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/.cluster-services &amp;&amp; test -f "$F" &amp;&amp; grep -qE '^SONARR_HOST=sonarr\.selfhost\.svc\.cluster\.local$' "$F" &amp;&amp; grep -qE '^SONARR_PORT=8989$' "$F" &amp;&amp; grep -qE '^QBT_HOST=qbittorrent\.selfhost\.svc\.cluster\.local$' "$F" &amp;&amp; grep -qE '^QBT_PORT=8080$' "$F" &amp;&amp; grep -qE '^IMAGE_TAG_VERIFIED=v?0\.1\.[01]$' "$F"</automated>
  </verify>
  <done>.cluster-services file authored + committed in arr-stack with verified hostnames + image tag literals (W-03 fix). Image tag is read from 02-02-SUMMARY.md's `## Machine-readable outputs` fenced code block per B-NEW-01 contract — round-trips cleanly with the upstream emit format. Plan 02-04 will read .cluster-services to substitute into files/arrconf.yml without re-discovering.</done>
</task>

<task type="auto">
  <name>Task 3.2: Author 9 files in my-kluster working tree (chart + ArgoCD App + bootstrap Secret)</name>
  <files>
    my-kluster/charts/arrconf/Chart.yaml
    my-kluster/charts/arrconf/values.yaml
    my-kluster/charts/arrconf/files/arrconf.yml
    my-kluster/charts/arrconf/templates/_helpers.tpl
    my-kluster/charts/arrconf/templates/cronjob.yaml
    my-kluster/charts/arrconf/templates/configmap.yaml
    my-kluster/charts/arrconf/README.md
    my-kluster/argocd/argocd-apps/arrconf-app.yaml
    my-kluster/secrets/arrconf-secret.yaml
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/.cluster-services (verified literals from Task 3.1)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-PATTERNS.md (full substitution tables for every file)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md (decisions D-23 through D-29)
    - /home/moi/projets/perso/my-kluster/charts/configarr/Chart.yaml (canonical analog — D-24 mirror)
    - /home/moi/projets/perso/my-kluster/charts/configarr/values.yaml (canonical analog)
    - /home/moi/projets/perso/my-kluster/charts/configarr/templates/_helpers.tpl (canonical analog — global s/configarr\./arrconf./g)
    - /home/moi/projets/perso/my-kluster/charts/configarr/templates/cronjob.yaml (canonical analog)
    - /home/moi/projets/perso/my-kluster/charts/configarr/templates/configmap.yaml (canonical analog)
    - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/configarr-app.yaml (canonical analog)
    - /home/moi/projets/perso/my-kluster/argocd/argocd-appprojects/selfhost-project.yaml (confirm A9 — selfhost-project allows arrconf App)
    - /home/moi/projets/perso/my-kluster/secrets/configarr-secret.yaml (canonical analog — SONARR_API_KEY value to copy)
    - /home/moi/projets/perso/arr-stack/examples/baseline-sonarr.yml (Phase 1 round-trip artifact — source for files/arrconf.yml content per PATTERNS.md §3)
  </read_first>
  <action>
    Source the .cluster-services literals first:
        source /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/.cluster-services
        # Now $SONARR_BASE_URL, $QBT_HOST, $QBT_PORT, $IMAGE_TAG_VERIFIED are available.

    All paths below are RELATIVE TO /home/moi/projets/perso/my-kluster/ unless otherwise noted.

    --- File 1: charts/arrconf/Chart.yaml ---
    Apply the literal block from <interfaces> above (6 lines).

    --- File 2: charts/arrconf/values.yaml ---
    Use the literal block from <interfaces> above. Substitute <IMAGE_TAG_VERIFIED> with $IMAGE_TAG_VERIFIED from .cluster-services.
    CRITICAL:
    - The `# renovate: image=...` comment MUST be on the line IMMEDIATELY ABOVE `repository:` — no blank line (CLAUDE.md "Annotations Renovate (CRITIQUE)" + PATTERNS.md Pitfall).
    - DELETE the entire `cache:` block from the configarr analog (no PVC for arrconf).
    - ADD `arrconfDryRun: true` (D-28 PR1 default per the two-PR protocol).
    - ADD `startingDeadlineSeconds: 600` (D-23).
    - DO NOT use :latest (CLAUDE.md hard rule).

    --- File 3: charts/arrconf/files/arrconf.yml ---
    Source: copy /home/moi/projets/perso/arr-stack/examples/baseline-sonarr.yml.
    Apply substitutions per PATTERNS.md §3:
    - Line 1 modeline `# yaml-language-server: $schema=../schemas/arrconf-schema.json` -> DROP entirely (Option A from RESEARCH.md Pattern 2 — relative path doesn't resolve in my-kluster).
    - `apps.sonarr.main.base_url`: substitute literal `$SONARR_BASE_URL` from .cluster-services -> resolves to `http://sonarr.selfhost.svc.cluster.local:8989` (verified W-03).
    - `download_clients.prune: false` (D-04 carry-forward).
    - `download_clients.items[0].tags: []` (NOT ["arrconf-managed"] — the tag is auto-managed by the reconciler at apply time).
    - For qBittorrent download_client `fields[]`: keep host=$QBT_HOST (literal: `qbittorrent.selfhost.svc.cluster.local` from .cluster-services), port=$QBT_PORT (literal: `8080`). Sensitive fields `username` and `password` MUST be `value: ""` or REDACTED — arrconf does not read them from YAML; they are stored on Sonarr's side already.
    - DO NOT add radarr:, prowlarr:, or other top-level app sections (D-25 — minimal Sonarr-only).
    - DO NOT add `quality_profiles`, `custom_formats`, `quality_definitions`, `media_naming` ANYWHERE (D-12 ScopeViolationError + ADR-5).

    --- File 4: charts/arrconf/templates/_helpers.tpl ---
    Copy charts/configarr/templates/_helpers.tpl verbatim, then apply ONE global substitution:
        sed -i 's/configarr\./arrconf./g' charts/arrconf/templates/_helpers.tpl
    This affects all 5 `define` blocks AND all `include` calls inside the file. Verify post-rename: 5 helpers — no remaining "configarr." substring.

    --- File 5: charts/arrconf/templates/cronjob.yaml ---
    Use the literal block from <interfaces> above. Notable deltas vs configarr/templates/cronjob.yaml (per PATTERNS.md §5 — 9 changes):
    1. helper rename `configarr.` -> `arrconf.` (3 occurrences)
    2. container name configarr -> arrconf
    3. REMOVE `tty: true` (Pitfall 10 — JSON logs activate when non-TTY)
    4. ADD `args: ["apply", "--config", "/app/config/arrconf.yml", "--apps", "sonarr"]` (Phase 1 image CMD is `["apply", "--help"]`, explicit override required)
    5. ADD env `ARRCONF_DRY_RUN: {{ .Values.arrconfDryRun | quote }}` (D-28)
    6. mountPath /app/config/config.yml -> /app/config/arrconf.yml (subPath same change)
    7. DELETE `cache` volumeMount + `cache` volume (no PVC; PATTERNS.md §5 #7)
    8. ADD pod-level securityContext (runAsNonRoot/User/Group 1000)
    9. ADD `startingDeadlineSeconds: {{ .Values.startingDeadlineSeconds | default 600 }}` (D-23)

    --- File 6: charts/arrconf/templates/configmap.yaml ---
    Use the 9-line block from <interfaces> above.

    --- File 7: charts/arrconf/README.md ---
    Author a short README documenting the two-PR protocol (D-28), pre-merge checklist (apply secret first, GHCR public toggle, image pullable), drift demo runbook, operational caveats (concurrencyPolicy: Forbid only blocks scheduler-created Jobs per Pitfall 4; selfHeal reverts kubectl edit per Pitfall 8; checksum/config is largely cosmetic per Pitfall 5).

    --- File 8: argocd/argocd-apps/arrconf-app.yaml ---
    Use the 23-line block from <interfaces> above.

    --- File 9: secrets/arrconf-secret.yaml ---
    Read SONARR_API_KEY from /home/moi/projets/perso/my-kluster/secrets/configarr-secret.yaml. Author with the EXACT same value (RESEARCH.md Open Q3 — operationally fine for Phase 2).
    Format:
        apiVersion: v1
        kind: Secret
        metadata:
          name: arrconf-env
          namespace: selfhost
        type: Opaque
        stringData:
          SONARR_API_KEY: "<copy from configarr-secret.yaml>"

    DO NOT include RADARR_API_KEY, PROWLARR_API_KEY, or any other key (D-29).

    --- Final step: DO NOT COMMIT YET in my-kluster ---
    These files stay uncommitted in my-kluster's working tree at the end of this task. Plan 02-04 Task 4.1 will stage + commit + open PR1. The end-of-plan checkpoint (Task 3.4) verifies the working tree state.
  </action>
  <acceptance_criteria>
    - `test -f /home/moi/projets/perso/my-kluster/charts/arrconf/Chart.yaml` exits 0
    - `grep -E '^name: arrconf$' /home/moi/projets/perso/my-kluster/charts/arrconf/Chart.yaml` returns 1 match
    - `grep -E '^appVersion: "0\.1\.[01]"$' .../Chart.yaml` returns 1 match
    - `grep -B1 'repository: ghcr.io/tom333/arr-stack-arrconf' /home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml | grep -q '# renovate: image='` exits 0 (annotation immediately above repository)
    - `grep -E '^arrconfDryRun: true$' .../values.yaml` returns 1 match
    - `grep -E '^apiKeysSecret: arrconf-env$' .../values.yaml` returns 1 match
    - `! grep -q 'cache:' .../values.yaml` exits 0 (no cache stanza)
    - `! grep -q ':latest' .../values.yaml` exits 0
    - `! grep -E '(quality_profiles|custom_formats|quality_definitions|media_naming)' /home/moi/projets/perso/my-kluster/charts/arrconf/files/arrconf.yml` exits 0 (frontière configarr respected, ADR-5)
    - `grep -E 'base_url:[[:space:]]+http://sonarr\.selfhost\.svc\.cluster\.local:8989' .../files/arrconf.yml` returns 1 match (W-02 + W-03 fix — POSIX class, verified literal)
    - `grep -E 'qbittorrent\.selfhost\.svc\.cluster\.local' .../files/arrconf.yml` returns at least 1 match (W-03 fix — verified qBit hostname)
    - `! grep -q 'configarr\.' /home/moi/projets/perso/my-kluster/charts/arrconf/templates/_helpers.tpl` exits 0 (rename complete; no leftover)
    - `grep -c '"arrconf\.' .../templates/_helpers.tpl` >= 5 (5 helper definitions)
    - `grep -q 'ARRCONF_DRY_RUN' /home/moi/projets/perso/my-kluster/charts/arrconf/templates/cronjob.yaml` exits 0
    - `grep -q 'args: \["apply"' .../templates/cronjob.yaml` exits 0
    - `grep -q 'envFrom:' .../templates/cronjob.yaml` exits 0
    - `grep -q 'runAsNonRoot: true' .../templates/cronjob.yaml` exits 0
    - `! grep -q 'tty: true' .../templates/cronjob.yaml` exits 0 (removed for JSON log activation)
    - `! grep -q 'name: cache' .../templates/cronjob.yaml` exits 0 (no cache volume)
    - `grep -q 'mountPath: /app/config/arrconf.yml' .../templates/cronjob.yaml` exits 0
    - `grep -q '.Files.Get "files/arrconf.yml"' .../templates/configmap.yaml` exits 0
    - `grep -E '^  name: arrconf$' /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arrconf-app.yaml` returns 1 match
    - `grep -q 'path: charts/arrconf' .../argocd-apps/arrconf-app.yaml` exits 0
    - `grep -q 'project: selfhost-project' .../argocd-apps/arrconf-app.yaml` exits 0
    - `grep -q 'name: arrconf-env' /home/moi/projets/perso/my-kluster/secrets/arrconf-secret.yaml` exits 0
    - `grep -q 'SONARR_API_KEY:' .../secrets/arrconf-secret.yaml` exits 0
    - `! grep -E '^[[:space:]]*(RADARR|PROWLARR|QBT|SEERR|JELLYFIN)_(API_KEY|USER|PASS):' .../secrets/arrconf-secret.yaml` exits 0 (W-02 fix — POSIX class; D-29 only SONARR_API_KEY in Phase 2)
    - `test -f /home/moi/projets/perso/my-kluster/charts/arrconf/README.md` exits 0
    - Image tag in values.yaml MUST match the literal `IMAGE_TAG_VERIFIED` from .cluster-services: `grep -E "tag:[[:space:]]+\"v?0\.1\.[01]\"" .../values.yaml` returns 1 match
  </acceptance_criteria>
  <verify>
    <automated>cd /home/moi/projets/perso/my-kluster &amp;&amp; test -f charts/arrconf/Chart.yaml &amp;&amp; test -f charts/arrconf/values.yaml &amp;&amp; test -f charts/arrconf/files/arrconf.yml &amp;&amp; test -f charts/arrconf/templates/_helpers.tpl &amp;&amp; test -f charts/arrconf/templates/cronjob.yaml &amp;&amp; test -f charts/arrconf/templates/configmap.yaml &amp;&amp; test -f charts/arrconf/README.md &amp;&amp; test -f argocd/argocd-apps/arrconf-app.yaml &amp;&amp; test -f secrets/arrconf-secret.yaml &amp;&amp; ! grep -q 'configarr\.' charts/arrconf/templates/_helpers.tpl &amp;&amp; grep -q 'arrconfDryRun: true' charts/arrconf/values.yaml &amp;&amp; grep -qE 'base_url:[[:space:]]+http://sonarr\.selfhost\.svc\.cluster\.local:8989' charts/arrconf/files/arrconf.yml &amp;&amp; grep -qE 'qbittorrent\.selfhost\.svc\.cluster\.local' charts/arrconf/files/arrconf.yml &amp;&amp; ! grep -E '^[[:space:]]*(RADARR|PROWLARR|QBT|SEERR|JELLYFIN)' secrets/arrconf-secret.yaml</automated>
  </verify>
  <done>All 9 files authored in my-kluster working tree per the substitution maps; renames clean (no leftover `configarr.`); D-29 secret scope respected (only SONARR_API_KEY); D-28 default `arrconfDryRun: true` set; D-25 minimal Sonarr-only YAML; verified hostnames baked in (W-03); POSIX-class regex used in acceptance criteria (W-02); ScopeViolationError-protected sections absent.</done>
</task>

<task type="auto">
  <name>Task 3.3: helm lint + helm template + arr-stack secret-leak audit</name>
  <files>
    # Validation only — no file authoring; produces ephemeral output streams.
  </files>
  <read_first>
    - /home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml (the values being templated)
    - /home/moi/projets/perso/my-kluster/charts/arrconf/templates/cronjob.yaml (most likely place for syntax errors)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-VALIDATION.md (Per-Task Verification Map for `secret-grep-audit` row)
    - /home/moi/projets/perso/arr-stack/CLAUDE.md "Ce que tu NE dois PAS faire" — anti-secret rules
  </read_first>
  <action>
    Step 1 — Helm lint (must exit 0):
        cd /home/moi/projets/perso/my-kluster
        helm lint charts/arrconf/

    Step 2 — Helm template (renders without errors; pipe through kubeconform if installed):
        helm template charts/arrconf/ > /tmp/arrconf-rendered.yaml
        grep -c '^kind: ' /tmp/arrconf-rendered.yaml  # expect at least 2 (CronJob + ConfigMap)

        if command -v kubeconform &> /dev/null; then
          helm template charts/arrconf/ | kubeconform -strict -summary -
        else
          echo "kubeconform not installed; relying on helm lint only (acceptable per VALIDATION.md)"
        fi

    Step 3 — Verify rendered cronjob.yaml has the critical fields (W-02 fix — POSIX classes):
        grep -q 'envFrom:' /tmp/arrconf-rendered.yaml
        grep -q 'name: ARRCONF_DRY_RUN' /tmp/arrconf-rendered.yaml
        grep -E 'name: arrconf-env' /tmp/arrconf-rendered.yaml
        grep -q 'image: "ghcr.io/tom333/arr-stack-arrconf:' /tmp/arrconf-rendered.yaml
        ! grep -q ':latest' /tmp/arrconf-rendered.yaml

    Step 4 — Anti-secret audit on arr-stack repo (REQ-secret-management — automated grep gate; W-02 fix — POSIX class):
        cd /home/moi/projets/perso/arr-stack
        # Must produce zero matches. If any match: STOP and remediate before Wave 3.
        # The audit excludes:
        # - .git directory
        # - snapshots/ (lossless API JSON; upstream-redacted)
        # - node_modules / .venv / __pycache__
        # - tools/arrconf/tests/fixtures/ (Phase 1 D-22 already audited; redacted)
        ! grep -rE '(SONARR_API_KEY|RADARR_API_KEY|PROWLARR_API_KEY|QBT_PASS|SEERR_API_KEY|JELLYFIN_API_KEY)[[:space:]]*[:=][[:space:]]*"[A-Za-z0-9]{16,}"' \
          --include='*.yaml' --include='*.yml' --include='*.py' --include='*.json' --include='*.sh' --include='*.md' \
          --exclude-dir='.git' --exclude-dir='snapshots' --exclude-dir='__pycache__' --exclude-dir='.venv' --exclude-dir='node_modules' \
          /home/moi/projets/perso/arr-stack/

    Step 5 — Cleanup:
        rm -f /tmp/arrconf-rendered.yaml

    NO files to commit at this step — purely validation. If any check fails, fix in Task 3.2 working tree and re-run.
  </action>
  <acceptance_criteria>
    - `cd /home/moi/projets/perso/my-kluster && helm lint charts/arrconf/` exits 0
    - `helm template /home/moi/projets/perso/my-kluster/charts/arrconf/ | grep -c '^kind: '` returns >= 2
    - `helm template .../charts/arrconf/ | grep -q 'envFrom:'` exits 0
    - `helm template .../charts/arrconf/ | grep -q 'name: ARRCONF_DRY_RUN'` exits 0
    - `helm template .../charts/arrconf/ | grep -q 'image: "ghcr.io/tom333/arr-stack-arrconf:'` exits 0
    - `helm template .../charts/arrconf/ | grep -q ':latest'` exits 1 (no :latest in rendered output)
    - The arr-stack secret-leak grep audit (Step 4 invocation) exits 0
    - If kubeconform installed: kubeconform exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/moi/projets/perso/my-kluster &amp;&amp; helm lint charts/arrconf/ &amp;&amp; helm template charts/arrconf/ | grep -q 'envFrom:' &amp;&amp; helm template charts/arrconf/ | grep -q 'name: ARRCONF_DRY_RUN' &amp;&amp; ! helm template charts/arrconf/ | grep -q ':latest' &amp;&amp; ! grep -rE '(SONARR_API_KEY|RADARR_API_KEY|PROWLARR_API_KEY|QBT_PASS|SEERR_API_KEY|JELLYFIN_API_KEY)[[:space:]]*[:=][[:space:]]*"[A-Za-z0-9]{16,}"' --include='*.yaml' --include='*.yml' --include='*.py' --include='*.json' --include='*.sh' --include='*.md' --exclude-dir='.git' --exclude-dir='snapshots' --exclude-dir='__pycache__' --exclude-dir='.venv' /home/moi/projets/perso/arr-stack/</automated>
  </verify>
  <done>helm lint passes; helm template renders all expected fields; arr-stack repo audit confirms zero plaintext credentials committed; chart is ready for Wave 3 PR1.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3.4: Cross-repo working tree state checkpoint (B-01 + W-NEW-01 fix — guard partial-write resume AND out-of-scope leakage)</name>
  <files>
    # No file artifact — operator-acknowledgment of git status output.
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-04-PLAN.md (Wave 3 Task 4.1 — the consumer of this checkpoint's working-tree state)
  </read_first>
  <what-built>
    Tasks 3.1, 3.2, 3.3 produced cross-repo state (1 committed file in arr-stack + 9 uncommitted files in my-kluster). Plan 02-04 Task 4.1 will stage exactly those 9 my-kluster files. Without an explicit checkpoint here, an interrupted Plan 02-03 + resumed Plan 02-04 risks staging stale leftovers OR missing newly-authored files. This checkpoint shows the operator the literal `git status` output and asks for explicit acknowledgment that the tree state matches the expected file list AND that nothing else is uncommitted in my-kluster (W-NEW-01 fix — counts==9 within scope is not enough: stale files at OTHER my-kluster paths must also be zero).
  </what-built>
  <action>
    Show the operator the my-kluster working tree state:

        echo "=== my-kluster working tree state (expected: exactly 9 untracked/modified files within scope, ZERO out-of-scope changes) ==="
        git -C /home/moi/projets/perso/my-kluster status --short \
          charts/arrconf/ \
          argocd/argocd-apps/arrconf-app.yaml \
          secrets/arrconf-secret.yaml

        # Expected output (file mode markers are `??` for untracked; this is a fresh chart so all should be `??`):
        # ?? charts/arrconf/Chart.yaml
        # ?? charts/arrconf/README.md
        # ?? charts/arrconf/files/arrconf.yml
        # ?? charts/arrconf/templates/_helpers.tpl
        # ?? charts/arrconf/templates/cronjob.yaml
        # ?? charts/arrconf/templates/configmap.yaml
        # ?? charts/arrconf/values.yaml
        # ?? argocd/argocd-apps/arrconf-app.yaml
        # ?? secrets/arrconf-secret.yaml

        # Sanity: count must be exactly 9 (no more, no less)
        TREE_COUNT=$(git -C /home/moi/projets/perso/my-kluster status --short \
          charts/arrconf/ \
          argocd/argocd-apps/arrconf-app.yaml \
          secrets/arrconf-secret.yaml | wc -l)
        echo "Untracked/modified file count (within scope): $TREE_COUNT (expected: 9)"
        if [ "$TREE_COUNT" -ne 9 ]; then
          echo "WARNING: Expected exactly 9 files in scope; got $TREE_COUNT."
          echo "If LESS than 9: a file is missing — re-run Task 3.2 for the missing path."
          echo "If MORE than 9: stale leftovers — \`git -C my-kluster status\` to investigate."
        fi

        # === W-NEW-01 fix — out-of-scope leakage assertions ===
        # The simple count==9 check above only validates files WITHIN the expected scope.
        # If a previous abandoned attempt left files at OTHER paths in my-kluster (e.g.,
        # charts/arrconf-old/, argocd/argocd-apps/arrconf-test.yaml, scripts/dev.sh),
        # those would silently pass the count gate. Add two extra assertions:

        SCOPE_REGEX='^(charts/arrconf/|argocd/argocd-apps/arrconf-app\.yaml$|secrets/arrconf-secret\.yaml$)'

        # Untracked out-of-scope (--others --exclude-standard skips .gitignore'd noise):
        UNTRACKED_OUT_OF_SCOPE=$(git -C /home/moi/projets/perso/my-kluster ls-files --others --exclude-standard \
          | grep -vE "$SCOPE_REGEX" \
          | wc -l)
        echo "Untracked out-of-scope files: $UNTRACKED_OUT_OF_SCOPE (expected: 0)"

        # Modified out-of-scope (`status --short` covers M / A / D / RM / etc):
        MODIFIED_OUT_OF_SCOPE=$(git -C /home/moi/projets/perso/my-kluster status --short \
          | awk '{print $NF}' \
          | grep -vE "$SCOPE_REGEX" \
          | wc -l)
        echo "Modified out-of-scope files (status --short -v): $MODIFIED_OUT_OF_SCOPE (expected: 0)"

        if [ "$UNTRACKED_OUT_OF_SCOPE" -ne 0 ] || [ "$MODIFIED_OUT_OF_SCOPE" -ne 0 ]; then
          echo "ERROR (W-NEW-01): out-of-scope changes detected in my-kluster — Wave 3 must NOT start."
          echo "Investigate via: git -C ~/projets/perso/my-kluster status  (full output, no path filter)"
          echo "Remediate: revert / delete the stale files, then re-run this checkpoint."
        fi

    The operator (you) must read the output and decide:
    - If the 9 expected files are listed AND both out-of-scope counters are 0: type "approved" to resume.
    - If files are missing: re-run Task 3.2 for the missing files, then re-run this checkpoint.
    - If stale leftovers exist (within or out of scope): clean them with `git -C my-kluster checkout -- <stale-file>` or `rm <stale-file>`, then re-run this checkpoint.
  </action>
  <how-to-verify>
    Operator reads the `git status --short` output and visually confirms:
    1. Exactly 9 entries are present within scope (no more, no less)
    2. All scope entries are under `charts/arrconf/`, `argocd/argocd-apps/arrconf-app.yaml`, or `secrets/arrconf-secret.yaml`
    3. All scope entries are marked `??` (untracked) — no stale modifications from previous runs
    4. **W-NEW-01**: zero untracked out-of-scope files (`git ls-files --others --exclude-standard | grep -v <scope>` returns empty)
    5. **W-NEW-01**: zero modified out-of-scope files (`git status --short | grep -v <scope>` returns empty)
  </how-to-verify>
  <acceptance_criteria>
    - `git -C /home/moi/projets/perso/my-kluster status --short charts/arrconf/ argocd/argocd-apps/arrconf-app.yaml secrets/arrconf-secret.yaml | wc -l` == 9
    - All 9 paths match the expected file list above (operator-verified)
    - **W-NEW-01**: `git -C /home/moi/projets/perso/my-kluster ls-files --others --exclude-standard | grep -vE '^(charts/arrconf/|argocd/argocd-apps/arrconf-app\.yaml$|secrets/arrconf-secret\.yaml$)' | wc -l` == 0 (no untracked out-of-scope files)
    - **W-NEW-01**: `git -C /home/moi/projets/perso/my-kluster status --short | awk '{print $NF}' | grep -vE '^(charts/arrconf/|argocd/argocd-apps/arrconf-app\.yaml$|secrets/arrconf-secret\.yaml$)' | wc -l` == 0 (no modified out-of-scope files)
    - Operator types "approved" with the count + out-of-scope confirmation
  </acceptance_criteria>
  <verify>
    <automated>[ "$(git -C /home/moi/projets/perso/my-kluster status --short charts/arrconf/ argocd/argocd-apps/arrconf-app.yaml secrets/arrconf-secret.yaml | wc -l)" = "9" ] &amp;&amp; [ "$(git -C /home/moi/projets/perso/my-kluster ls-files --others --exclude-standard | grep -vE '^(charts/arrconf/|argocd/argocd-apps/arrconf-app\.yaml$|secrets/arrconf-secret\.yaml$)' | wc -l)" = "0" ] &amp;&amp; [ "$(git -C /home/moi/projets/perso/my-kluster status --short | awk '{print $NF}' | grep -vE '^(charts/arrconf/|argocd/argocd-apps/arrconf-app\.yaml$|secrets/arrconf-secret\.yaml$)' | wc -l)" = "0" ]</automated>
  </verify>
  <resume-signal>Type "approved: 9 files matched, 0 out-of-scope" OR describe what mismatch was found (missing file path, stale leftover within scope, untracked file at unexpected path, modified file outside scope). Wave 3 (Plan 02-04) cannot start until tree state is exactly as specified.</resume-signal>
  <done>Operator has visually confirmed the my-kluster working tree contains exactly the 9 expected files for Wave 3 Task 4.1 to stage AND zero untracked/modified files outside scope (W-NEW-01); no partial-write resume risk; cross-repo authoring lifecycle is clean (B-01 + W-NEW-01 fix).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arr-stack repo (.cluster-services) | Trusted capture of hostnames/ports — no secrets, version-controlled |
| my-kluster repo working tree (uncommitted) | Operator workstation only; will be committed + PR'd by Plan 02-04 |
| my-kluster/secrets/arrconf-secret.yaml | Plaintext API key per existing my-kluster convention; SOPS migration deferred to Phase 8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure (T-secret-leak) | my-kluster/secrets/arrconf-secret.yaml | accept | Phase 2 follows existing my-kluster convention (plaintext, like configarr-secret.yaml). NEVER committed in arr-stack (Task 3.3 audit). SOPS migration deferred to Phase 8. CLAUDE.md acknowledges the TODO. |
| T-02-02 | Information Disclosure (T-secret-leak) | arr-stack repo | mitigate | Task 3.3 grep audit excludes .git/snapshots; rejects API_KEY-like plaintext assignments. CI fixture audit (Phase 1 D-22) covers the Python tree. |
| T-02-03 | Confused Deputy (T-scope-bypass) | files/arrconf.yml | mitigate | Phase 1 D-12 ScopeViolationError raises BEFORE HTTP call; pydantic schema in arr-stack/schemas/arrconf-schema.json rejects `quality_profiles`/`custom_formats`/`quality_definitions`/`media_naming`. Task 3.2 acceptance criteria explicitly grep-rejects these sections. |
| T-02-04 | Information Disclosure (T-key-reuse) | SONARR_API_KEY shared with configarr | accept | Both reconcilers target the same Sonarr instance; sharing the existing admin-scope key is operationally fine for Phase 2 (RESEARCH.md Open Q3). Per-service segregation deferred to Phase 8 ESO migration. |
| T-02-05 | Tampering (T-cross-repo-trust) | Cross-repo lifecycle gap | mitigate | Task 3.4 checkpoint shows operator the literal `git status --short` and requires explicit acknowledgment before Wave 3 starts (B-01 fix). Partial-write resume / stale-staged-leftover scenarios are blocked. W-NEW-01 fix extends to out-of-scope: untracked + modified files anywhere ELSE in my-kluster must also be 0. |
| T-02-06 | Tampering (T-image-tag-mismatch) | values.yaml.image.tag literal | mitigate | Task 3.1 reads .cluster-services IMAGE_TAG_VERIFIED (sourced from 02-02-SUMMARY.md `## Machine-readable outputs` fenced block per B-NEW-01 contract); Task 3.2 substitutes that literal into values.yaml; acceptance criterion grep-asserts the substitution match (Pitfall 2 fix). |
| T-02-07 | Tampering (T-hostname-mismatch) | files/arrconf.yml base_url + qBit host | mitigate | Task 3.1 captures hostnames from my-kluster ArgoCD app yamls (deterministic, version-controlled); Task 3.2 substitutes literals; W-03 fix grep-asserts the verified hostname is present. |
</threat_model>

<verification>
End-of-plan gate (must all pass before Wave 3 starts):
1. .cluster-services file committed in arr-stack with verified hostnames + image tag
2. 9 files authored in my-kluster working tree (uncommitted)
3. helm lint passes; helm template renders all expected fields
4. arr-stack secret-leak audit passes (zero plaintext API keys)
5. Operator has acknowledged my-kluster working-tree state matches expected 9 files (Task 3.4 checkpoint)
6. **W-NEW-01**: zero out-of-scope untracked and zero out-of-scope modified files in my-kluster (Task 3.4 augmented assertions)
</verification>

<success_criteria>
- D-23 (schedule + concurrencyPolicy + history limits) — encoded in cronjob.yaml + values.yaml
- D-24 (mini-chart in my-kluster/charts/arrconf/) — SATISFIED
- D-25 (minimal Sonarr-only YAML) — SATISFIED + grep-asserted (no other apps in arrconf.yml)
- D-26 (ArgoCD App mirror configarr-app.yaml) — SATISFIED
- D-27 (ConfigMap mount /app/config/arrconf.yml) — SATISFIED
- D-28 (Two-PR protocol — PR1 default arrconfDryRun: true) — SATISFIED
- D-29 (Secret scope: SONARR_API_KEY only) — SATISFIED + grep-asserted (W-02 POSIX class)
- REQ-bootstrap-exception (arrconf-secret.yaml authored; envFrom binding in cronjob.yaml) — SATISFIED at file-authoring level
- REQ-secret-management (no secrets in arr-stack) — SATISFIED by Task 3.3 grep audit
- W-02 fix (POSIX [[:space:]] not \s) — SATISFIED in all acceptance criteria + grep commands
- W-03 fix (verified Sonarr + qBit hostnames) — SATISFIED via .cluster-services + grep assertions
- B-01 fix (cross-repo working-tree checkpoint) — SATISFIED by Task 3.4
- B-04 fix (scope split — chart-authoring is its own value-stream) — SATISFIED (this plan == 9 files in 1 value stream)
- B-NEW-01 fix (data contract round-trip with 02-02-SUMMARY.md fenced block) — SATISFIED by Task 3.1 Step 2 grep + sanity check
- W-NEW-01 fix (out-of-scope leakage caught at Task 3.4) — SATISFIED by augmented automated verify
- W-NEW-02 fix (wave/depends_on consistency) — SATISFIED by bumping wave: 1 → 2
</success_criteria>

<output>
After completion, create `/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-03-SUMMARY.md` documenting:
- Verified hostnames + image tag literals (cite the .cluster-services capture)
- 9 files authored in my-kluster (path + size)
- helm lint output (any non-blocking warnings)
- helm template output digest (resource kinds rendered: CronJob, ConfigMap, ...)
- arr-stack secret-leak audit confirmation: zero matches
- Cross-repo working-tree checkpoint acknowledgment timestamp + count confirmation (within scope == 9; out-of-scope untracked == 0; out-of-scope modified == 0)
</output>
</content>
</invoke>
