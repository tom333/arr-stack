---
phase: 02.2
plan: 12
type: execute
wave: 10
depends_on:
  - 02.2-11
files_modified:
  - .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/
  - snapshots/post-phase2.2-hotfix-2026-05-09/
  - snapshots/drift-test-phase2.2-hotfix-2026-05-09/
gap_closure: true
autonomous: false
requirements:
  - REQ-drift-detection
must_haves:
  truths:
    - "Operator manually re-enters the qBit password in Sonarr UI BEFORE any v0.1.5 reconcile fires (cluster's currently-stored password is the literal `\"********\"` regression artifact; an immediate reconcile would observe broken state)"
    - "Sonarr's Test API on the qBit downloadclient returns HTTP 200 + `isValid: true` after the password re-entry — this is the load-bearing CREDENTIAL DISPOSITIVE that Plan 06 Task 6.4 lacked (snapshot-only checks could not detect the regression by construction)"
    - "Only AFTER the credential dispositive succeeds, the CronJob is unsuspended and a post-recovery smoke job runs on v0.1.5 — the smoke MUST emit `merge_field_omitted_credential` events for username + password, confirming v0.1.5 code path is engaged"
    - "Drift demo runbook re-executed FULLY AUTOMATED + CREDENTIAL-AWARE: induce priority drift via curl PUT, force smoke job, verify W-04 dispositive `RESTORED_PRIORITY == ORIGINAL_PRIORITY` AND Sonarr Test API still returns HTTP 200 (proving the reconcile did NOT re-overwrite the credential)"
    - "Combined dispositive evidence file captures: original/drifted/restored priority + manual_nudge_used: NO + put_force_save_used events count + merge_field_omitted_credential events count + sonarr_qbit_test_http_status: 200 + sonarr_qbit_test_isValid: true + password_field_in_put_body: ABSENT — all in one log so Plan 06 Task 6.4 visual gate can be re-attempted with this evidence"
    - "Plan 06 Task 6.4 visual gate is RE-OPENED with the new dispositive: operator confirms in Sonarr UI that priority is correct (matches YAML) AND Sonarr's Test button returns success — the new gate has BOTH the priority check (Plan 06 covered) AND the credential check (Plan 06 missed)"
    - "REQ-drift-detection correction half is FINALLY SATISFIED CLEANLY: drift correction is automated (no operator nudge) AND credentials survive the correction (no overwrite). The final closure is the differential against Plan 06's failed attempt"
    - "CronJob is unsuspended ONLY at the very end, AFTER all dispositives succeed; if any dispositive fails, the CronJob STAYS suspended and Plan 12 is re-attempted (or escalated)"
  artifacts:
    - path: ".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/cluster-recovery-2026-05-09.log"
      provides: "Operator-recovery checkpoint dispositive — Sonarr Test API HTTP 200 BEFORE the reconcile (proves password is real, not mask) + post-unsuspend smoke logs proving v0.1.5 code path engaged"
      contains: "sonarr_qbit_test_http_status: 200"
    - path: ".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-credential-aware-2026-05-09.log"
      provides: "Credential-aware drift demo dispositive — both priority RESTORATION and credential SURVIVAL captured in one combined evidence block"
      contains: "DRIFT-DEMO + CREDENTIAL DISPOSITIVE COMPARISON"
    - path: "snapshots/post-phase2.2-hotfix-2026-05-09/sonarr/"
      provides: "Post-recovery cluster state — Sonarr 17 JSON; downloadclient password GET-serializes to '********' (Sonarr API masks it BY DESIGN, regardless of stored value), but the BEHAVIORAL test (Test API HTTP 200) proves the underlying value is the real password"
      min_files: 17
    - path: "snapshots/drift-test-phase2.2-hotfix-2026-05-09/sonarr/"
      provides: "W-01 forensic snapshot pair for the credential-aware drift demo (BEFORE inducing drift) — REQUIRED audit trail per Plan 06 precedent"
      min_files: 17
  key_links:
    - from: "Operator-driven Sonarr UI password re-entry"
      to: "Sonarr Test API HTTP 200 + isValid: true"
      via: "Sonarr's POST /api/v3/downloadclient/test reproduces the operator's UI 'Test' button click — this is the BEHAVIORAL signal that the Plan 06 visual UAT relied on (snapshot diff alone was insufficient by construction)"
      pattern: "POST.*api/v3/downloadclient/test.*\\bisValid\\b"
    - from: "v0.1.5 reconcile post-unsuspend"
      to: "merge_field_omitted_credential structlog events"
      via: "smoke job logs MUST contain at least 1 merge_field_omitted_credential per UPDATE PUT — proves v0.1.5 differ.py code is engaged (vs v0.1.4 which would fire merge_field_preserved instead)"
      pattern: "merge_field_omitted_credential"
    - from: "Credential-aware drift demo"
      to: "REQ-drift-detection correction half closure"
      via: "Combined dispositive: priority restored + credential survives + Test API HTTP 200 — both halves of the regression class closed in one run"
      pattern: "DRIFT-DEMO \\+ CREDENTIAL DISPOSITIVE COMPARISON"
    - from: "Plan 12 final dispositive"
      to: "Plan 06 Task 6.4 visual gate re-opened"
      via: "Operator runs the same UI test as Plan 06 Task 6.4 (Sonarr UI 'Test' button click) but THIS TIME it succeeds — closing what Plan 06 left INCOMPLETE"
      pattern: "Operator Visual Gate"
---

<objective>
Final plan of the gap-closure cycle. Closes deferred-items.md §D-02.2-AUTH-REGRESSION required-actions #4 (operator-driven cluster recovery), #5 (re-run W-04 dispositive WITH credential check), and #6 (re-open Plan 06 Task 6.4 visual gate). Closes REQ-drift-detection correction half CLEANLY in both halves: priority restoration (the half Plan 06 thought it closed) AND credential survival (the half Plan 06 missed).

Sequencing (the load-bearing constraint of this entire phase 02.2 hotfix cycle):

1. **Pre-flight** — confirm v0.1.5 image is in CronJob spec (Plan 11 deliverable) AND CronJob is still suspended (no race occurred during merge window).
2. **Operator UI password re-entry (checkpoint:human-action, gate=blocking)** — operator opens Sonarr UI → Settings → Download Clients → qBittorrent → Password field → Save. The cluster's currently-stored password is the literal `"********"` (the regression artifact); without this manual step, every subsequent reconcile would observe broken state.
3. **Behavioral CREDENTIAL DISPOSITIVE** — programmatically POST to Sonarr's Test API (`POST /api/v3/downloadclient/test`) and assert HTTP 200 + `isValid: true`. THIS is the dispositive that Plan 06 Task 6.4 lacked; snapshot diff cannot detect the regression by construction (Sonarr's GET masks the field regardless of stored value). Persist log to `evidence/cluster-recovery-2026-05-09.log`.
4. **Unsuspend the CronJob** — only after step 3 succeeds. Run `kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":false}}'`.
5. **Post-unsuspend smoke job** — force a v0.1.5 reconcile via `kubectl create job --from=cronjob/arrconf`. Verify the smoke logs contain `merge_field_omitted_credential` events (proves v0.1.5 differ.py code is engaged) AND zero HTTP 400. Verify Sonarr Test API still returns HTTP 200 AFTER the smoke (proves the reconcile did NOT re-overwrite the credential). Persist log.
6. **Post-recovery snapshot** — capture sonarr + qbittorrent state to `snapshots/post-phase2.2-hotfix-2026-05-09/`. Anti-leak audit clean. Tag id=1 still attached. Priority back to YAML value.
7. **W-01 forensic snapshot for credential-aware drift demo** — capture `snapshots/drift-test-phase2.2-hotfix-2026-05-09/` BEFORE inducing drift.
8. **Drift demo FULLY AUTOMATED + CREDENTIAL-AWARE** — induce priority drift via curl PUT (priority is the canonical safe field per Phase 2.1 A6), force smoke, capture combined dispositive log: priority RESTORED + Sonarr Test API still 200 + merge_field_omitted_credential fired. Single combined evidence file `drift-demo-credential-aware-2026-05-09.log` per gap_summary §dispositive_specification.
9. **Plan 06 Task 6.4 visual gate re-opened (checkpoint:human-verify, gate=blocking)** — operator confirms in Sonarr UI that (a) priority value matches YAML, (b) Sonarr "Test" button returns success. Both halves of the regression class are closed.
10. **REQ-drift-detection final marking** — formal closure annotation in REQUIREMENTS.md and STATE.md (this is captured in the SUMMARY commit, not a separate task).

If ANY dispositive fails (step 3 fails, step 5 detects HTTP 400 or merge_field_preserved instead of merge_field_omitted_credential, step 8 priority not restored or Test API fails post-reconcile), the CronJob is RE-SUSPENDED and the plan halts for investigation. The hotfix-via-PR pattern (Phase 2.1 LEARNINGS) applies if v0.1.5 itself reveals a defect.

Output: 4 evidence files + 2 snapshot directories committed in arr-stack git; CronJob unsuspended; REQ-drift-detection correction half SATISFIED cleanly in both halves; Plan 06 Task 6.4 visual gate FINALLY closed.

Wave 10 — depends on Plan 11 (v0.1.5 deployed in cluster + still suspended).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/REQUIREMENTS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/deferred-items.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-06-PLAN.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-06-SUMMARY.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-07-PLAN.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-08-PLAN.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/evidence/forensic-credentials-diff-2026-05-09T0651.txt
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/evidence/forensic-cronjob-logs-2026-05-09T0652.log
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-PLAN.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/client_base.py
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/reconcilers/sonarr.py

<interfaces>
<!-- Sonarr Test API (the load-bearing dispositive) — POST /api/v3/downloadclient/test -->
<!-- Reference: https://sonarr.tv/docs/api/ — section DownloadClient → Test (anonymous) -->

# Build the test body from the cluster's GET response (Sonarr's Test API expects
# the same shape as a downloadclient resource):
SONARR_API_KEY=...
DOWNLOAD_CLIENT_BODY=$(curl -fsS -H "X-Api-Key: $SONARR_API_KEY" \
  "http://127.0.0.1:8989/api/v3/downloadclient/1")

# POST it to the Test endpoint:
TEST_RESPONSE=$(curl -fsS -X POST \
  -H "X-Api-Key: $SONARR_API_KEY" \
  -H "Content-Type: application/json" \
  -d "$DOWNLOAD_CLIENT_BODY" \
  "http://127.0.0.1:8989/api/v3/downloadclient/test" \
  -w '\n{"http_status":%{http_code}}\n')

# Expected response shape (Sonarr v3):
# Success: HTTP 200, body: {"isValid": true, "validationFailures": []}
# Failure: HTTP 400, body: {"isValid": false, "validationFailures": [...]}

# Dispositive jq:
echo "$TEST_RESPONSE" | jq -e '.[0].isValid == true'   # exit 0 if true; non-zero otherwise
echo "$TEST_RESPONSE" | jq -e '.[0] | .errorMessage // empty'   # any error message text

<!-- merge_field_omitted_credential expected event shape (Plan 08 GREEN deliverable) -->
{"event": "merge_field_omitted_credential", "name": "username", "privacy": "userName", "level": "info", "timestamp": "..."}
{"event": "merge_field_omitted_credential", "name": "password", "privacy": "password", "level": "info", "timestamp": "..."}

<!-- DISPOSITIVE COMPARISON template (per gap_summary <dispositive_specification>) -->
# === DRIFT-DEMO + CREDENTIAL DISPOSITIVE COMPARISON (Phase 02.2 v0.1.5 hotfix) ===
# original_priority: 1
# drifted_priority:  5
# restored_priority: 1
# match (restored == original): YES
# manual_nudge_used: NO
# put_force_save_used events fired: <N>
# merge_field_omitted_credential events fired: <M>
# HTTP 400 events: 0
# sonarr_qbit_test_http_status: 200
# sonarr_qbit_test_isValid: true
# password_field_in_put_body: ABSENT
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator workstation → Sonarr UI | Operator manually re-enters qBit password. Tunneled via kubectl port-forward (kube-apiserver mTLS). |
| operator workstation → cluster API | kubectl patch cronjob (suspend toggle); kubectl create job (smoke). |
| arrconf reconcile pod → Sonarr API | After unsuspend, the reconcile fires a real PUT against the cluster Sonarr — uses the secret-injected SONARR_API_KEY. |
| Sonarr → qBittorrent (Test API path) | Sonarr's Test API decrypts the stored password and POSTs to qBit. Behavioral signal. |
| arr-stack git → public GitHub | All evidence files + snapshots committed to public repo. Anti-leak audit mandatory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-12-01 | D (Denial of service / cluster recovery race) | unsuspend timing | mitigate | The CronJob is unsuspended ONLY after the credential dispositive (Sonarr Test API HTTP 200) succeeds. If the dispositive fails, the CronJob STAYS suspended and the plan halts. Acceptance criteria enforce this ordering (Step 4 acceptance gates the unsuspend on Step 3 evidence file content). |
| T-02.2-12-02 | I (Information disclosure) | Sonarr Test API request body | mitigate | The Test API request body contains the FULL downloadclient resource INCLUDING the username/password fields (with their cluster-stored values, which Sonarr GET-serializes as "admin"/"********"). The request stays inside the kubectl-port-forward tunnel — never written to disk. Defensive: the Test response is captured but stripped of the request-echo before commit (jq-redact: `.[0] | del(.fields)`). Acceptance criterion verifies no field-array content in the committed evidence file. |
| T-02.2-12-03 | I (Information disclosure) | smoke job logs (post-unsuspend) | mitigate | Logs contain `merge_field_omitted_credential` events (metadata-only payload — guarded by Plan 08 T-02.2-08-01) and `put_force_save_used` events (path + id, no values — Plan 02 contract). Anti-leak grep audit on the evidence file: zero matches for known-passkey hex, zero matches for Sonarr API key literal. |
| T-02.2-12-04 | T (Tampering) | drift demo curl PUT | mitigate | The drift PUT changes ONLY the `priority` field (canonical safe drift field per Phase 2.1 A6). The PUT URL uses `?forceSave=true` to bypass Sonarr's pre-save validation (so the operator's drift induction works regardless of credential state). The drift PUT body MUST NOT touch `fields[]` (no chance of inadvertently restoring credentials via the curl side; only the v0.1.5 reconcile owns that path). Acceptance criterion: post-PUT, `jq '.priority'` returns the drifted value 5; `jq '.fields[] | select(.name == "password") | .value'` is unchanged. |
| T-02.2-12-05 | I (Information disclosure) | snapshot anti-leak | mitigate | Same per-snapshot manual jq redaction as Plans 01 + 06 + 07. Anti-leak grep mandatory on both new snapshot directories. |
| T-02.2-12-06 | E (Elevation of privilege) | hotfix-via-PR escape hatch | mitigate | If v0.1.5 itself reveals a defect (e.g. test API still 401 even after operator UI password re-entry, OR merge_field_omitted_credential fires but smoke triggers HTTP 400 anyway), follow the Phase 2.1 hotfix-via-PR pattern (NEVER `git revert` on a merged PR; open PR(N+1) with smallest reversion). Document the failure in `deferred-items.md` and re-suspend the CronJob. |
| T-02.2-12-07 | I (Information disclosure) | password value in cluster recovery log | mitigate | Step 2 (operator UI password re-entry) is operator-only — the password value is NEVER captured by the plan's automation. The `evidence/cluster-recovery-*.log` records ONLY the dispositive HTTP status / isValid result, NOT the password value. Defensive: the log preamble explicitly says "operator password re-entry NOT logged here". |
</threat_model>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 12.1: Operator UI password re-entry — Sonarr → Settings → Download Clients → qBittorrent → Password → Save</name>
  <files>
  </files>
  <action>
    This is a checkpoint:human-action task — operator-only (Claude cannot perform UI password entry, and the real qBit password is held by the operator's password manager, NOT by arrconf YAML or any committed file). The orchestrator pauses on this task; the operator follows the &lt;how-to-verify&gt; steps and signals via &lt;resume-signal&gt; when complete.

    Critical sequencing constraint (T-02.2-12-01): the CronJob `arrconf` in selfhost MUST stay suspended throughout this task. Task 12.3 unsuspends ONLY AFTER Task 12.2 records HTTP 200 + isValid:true from Sonarr's Test API.

    The operator's password value is NEVER captured by automation (T-02.2-12-07). Only the dispositive HTTP status / isValid result lands in evidence files.
  </action>
  <verify>
    <automated>[ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}' 2>/dev/null)" = "true" ] &amp;&amp; [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}' 2>/dev/null)" = "ghcr.io/tom333/arr-stack-arrconf:0.1.5" ]</automated>
  </verify>
  <done>Operator has re-entered the real qBit password in Sonarr UI; Sonarr UI Test button returned green/OK in Step 3 of &lt;how-to-verify&gt;; CronJob remains suspended (verified by Task 12.2 pre-flight). Task 12.2 follows with the programmatic Sonarr Test API call to capture the dispositive.</done>
  <what-built>
    Plan 11 deployed v0.1.5 to the cluster (CronJob spec.image = ghcr.io/tom333/arr-stack-arrconf:0.1.5) but the CronJob is still suspended. The cluster's currently-stored qBit password is the literal `"********"` (the regression artifact from the v0.1.4 reconcile). Before the next reconcile fires, the operator MUST re-enter the real qBit password in Sonarr UI, or v0.1.5's first reconcile will observe broken state.

    Why operator-only: arrconf does not have the real qBit password (env-only-secret discipline, CLAUDE.md "Variables d'environnement" — the YAML carries placeholders). The real qBit password is known only to the operator (or stored in operator's password manager). No automation can fetch it.

    What v0.1.5 will do once unsuspended: the next reconcile's PUT body will OMIT the username/password fields entirely (per Plan 08's omit-by-privacy-metadata fix). Sonarr will preserve the operator-entered password unchanged. But this only works if the operator-entered password lands BEFORE the unsuspend.
  </what-built>
  <how-to-verify>
    1. Confirm cluster precondition (Plan 11 successful):
       ```bash
       kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image} suspend={.spec.suspend}'
       # Expected: ghcr.io/tom333/arr-stack-arrconf:0.1.5 suspend=true
       ```
       If image is not 0.1.5 OR suspend is not true, ABORT — Plan 11 is incomplete or got reversed; investigate before proceeding.

    2. **Open Sonarr UI** (the same UI you used for Plan 06 Task 6.4):
       - Open a port-forward if you don't already have one: `kubectl -n selfhost port-forward svc/sonarr 8989:8989` (or use whichever route Plan 06 used).
       - Navigate to: `http://localhost:8989/settings/downloadclients` (or the cluster DNS name + path)
       - Click on the qBittorrent download client row.

    3. **Re-enter the password**:
       - Click in the "Password" field.
       - **Verify the field is currently showing the placeholder dots** (i.e. the literal `"********"` is stored — Sonarr's UI obscures it, but you can verify it is NOT the real password by attempting the Test button BEFORE saving). Click "Test" — it should return red "Auth failed" (401/403). This is the regression-state confirmation; if Test already returns green, the operator may have already recovered, in which case skip to step 4.
       - Clear the field and type the real qBit password (retrieve from your password manager — DO NOT paste from arrconf YAML or any committed file; the YAML carries empty placeholders).
       - Click "Test" — it should now return green "OK".
       - Click "Save".

    4. **Verify the save persisted**:
       - Refresh the page or navigate away and back.
       - Click the qBittorrent client row again.
       - Click "Test" — it should still return green.

    5. **DO NOT click "Save" multiple times unnecessarily** — each save in Sonarr UI fires its own PUT to Sonarr's API. The single Save in step 3 is sufficient.

    6. **DO NOT unsuspend the CronJob yet** — Task 12.2 will do that programmatically after the credential dispositive (Sonarr Test API HTTP 200 via the API endpoint, not just the UI button) is captured.

    Check before signaling "approved":
    - Sonarr UI "Test" returned green/OK on the qBit downloadclient (you saw the "OK" or success state — NOT red, NOT timeout).
    - You did NOT touch any other Sonarr settings (priority, host, tags, categories).
    - You did NOT unsuspend the CronJob.
  </how-to-verify>
  <resume-signal>
    Type "approved" once: real qBit password is re-entered + saved + Sonarr UI "Test" button returned green/OK.

    OR type "skipped — already recovered" if Sonarr UI Test was ALREADY green before you started (i.e. someone or something recovered the credential between Plan 06 incident and now). Document this in your SUMMARY annotation.

    OR describe what went wrong (e.g. "qBit returns auth failed even with real password — qBit-side issue", "Sonarr UI hangs", etc.) so the failure mode is recorded and the plan halts safely with the CronJob still suspended.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 12.2: Behavioral CREDENTIAL DISPOSITIVE — Sonarr Test API HTTP 200 + isValid: true</name>
  <files>
    .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/cluster-recovery-2026-05-09.log
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/evidence/forensic-credentials-diff-2026-05-09T0651.txt (the empty-diff dispositive that proved snapshot-only checks fail; this is the BEHAVIORAL signal Plan 06 lacked)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-06-SUMMARY.md §"Operator Visual Gate FAILED" (the test API HTTP 401/403 narrative this task inverts)
  </read_first>
  <action>
    Step 1 — Pre-flight: confirm CronJob still suspended (sequencing — Plan 12 must NOT execute on an unsuspended CronJob until Task 12.4):
        cd /home/moi/projets/perso/arr-stack
        SUSPEND=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')
        [ "$SUSPEND" = "true" ] || { echo "ERROR: CronJob unexpectedly unsuspended — sequencing violated"; exit 1; }

    Step 2 — Establish port-forward to Sonarr + extract API key:
        if [ -z "$SONARR_API_KEY" ]; then
          export SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"
        fi
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        SONARR_PF=$!
        sleep 3
        trap "kill $SONARR_PF 2>/dev/null || true" EXIT

    Step 3 — Fetch the cluster's downloadclient resource (used as the body of the Test API call):
        DC_BODY=$(rtk proxy curl -fsS -H "X-Api-Key: $SONARR_API_KEY" "http://127.0.0.1:8989/api/v3/downloadclient/1")
        if [ -z "$DC_BODY" ]; then
          echo "ERROR: Sonarr GET /api/v3/downloadclient/1 returned empty"
          exit 1
        fi
        # Sanity: the body has a `fields` array including username + password (privacy=userName + privacy=password):
        echo "$DC_BODY" | rtk proxy jq -e '.fields | map(select(.privacy == "password" or .privacy == "userName")) | length >= 2' > /dev/null || {
          echo "WARN: cluster downloadclient body missing expected privacy=password|userName fields"
        }

    Step 4 — Call Sonarr's Test API. This is the BEHAVIORAL DISPOSITIVE:
        TEST_RAW=$(rtk proxy curl -sS -o /tmp/test-response-body.json -w '%{http_code}' \
          -X POST \
          -H "X-Api-Key: $SONARR_API_KEY" \
          -H "Content-Type: application/json" \
          -d "$DC_BODY" \
          "http://127.0.0.1:8989/api/v3/downloadclient/test")
        HTTP_STATUS="$TEST_RAW"
        TEST_BODY=$(cat /tmp/test-response-body.json 2>/dev/null || echo "{}")

        echo "Sonarr Test API HTTP status: $HTTP_STATUS"
        echo "Sonarr Test API body (head 500 chars): $(echo "$TEST_BODY" | head -c 500)"

        if [ "$HTTP_STATUS" != "200" ]; then
          echo "ERROR: Sonarr Test API returned HTTP $HTTP_STATUS (expected 200)"
          echo "This means Task 12.1 operator UI password re-entry did NOT succeed, OR there's a separate qBit-side issue."
          echo "The CronJob STAYS suspended. Investigate before retrying Task 12.1."
          # Record the failure for the SUMMARY:
          mkdir -p .planning/phases/02.2-v0-1-4-forcesave-fix/evidence
          cat > .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/cluster-recovery-FAILED-$(date +%FT%H%M).log <<EOF2
=== CREDENTIAL DISPOSITIVE FAILED — Sonarr Test API HTTP $HTTP_STATUS ===
Timestamp: $(date -u +%FT%TZ)
Expected: 200
Actual: $HTTP_STATUS
Test body excerpt (first 500 chars, may include error fields — DO NOT redistribute outside this evidence file):
$(echo "$TEST_BODY" | head -c 500)

CronJob STAYS suspended. Plan 12 halted at Task 12.2. Re-run after fixing.
EOF2
          exit 1
        fi

        # Extract isValid (Sonarr v3 returns an array — index 0):
        IS_VALID=$(echo "$TEST_BODY" | rtk proxy jq -r '.[0].isValid // false')
        echo "Sonarr Test API isValid: $IS_VALID"
        if [ "$IS_VALID" != "true" ]; then
          echo "ERROR: Sonarr Test API HTTP 200 BUT isValid=$IS_VALID (qBit-level auth still failing despite Sonarr returning 200)"
          exit 1
        fi

    Step 5 — Capture the dispositive evidence file (with redaction — T-02.2-12-02):
        EVIDENCE=".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/cluster-recovery-$(date +%F).log"
        # Redact the body: strip the `fields` array (request-echo would include cluster-stored credential masks):
        REDACTED_TEST_BODY=$(echo "$TEST_BODY" | rtk proxy jq '[.[] | del(.fields)]')

        cat > "$EVIDENCE" <<EOF2
=== Plan 02.2-12 Task 12.2 — CREDENTIAL DISPOSITIVE ===
Timestamp: $(date -u +%FT%TZ)
Source: Sonarr POST /api/v3/downloadclient/test (cluster: selfhost, downloadclient id=1)
Operator password re-entry: confirmed in Task 12.1 (operator-only, password value NOT logged here per T-02.2-12-07)

Dispositive (load-bearing):
  sonarr_qbit_test_http_status: $HTTP_STATUS
  sonarr_qbit_test_isValid: $IS_VALID

Test API response body (fields[] redacted to prevent credential mask passthrough into evidence per T-02.2-12-02):
$REDACTED_TEST_BODY

Differential vs Plan 06 Task 6.4 attempt:
  Plan 06 Task 6.4 (2026-05-09T06:48Z): operator clicked Sonarr UI Test → 401/403
  Plan 12 Task 12.2 (this run): programmatic Sonarr Test API → 200 + isValid: true

The differential is the v0.1.5 hotfix outcome:
  - operator UI password re-entry restored the real password to Sonarr's encrypted store
  - the omit-by-privacy-metadata fix in v0.1.5 will preserve this real password on next reconcile (Task 12.3)

Cluster state at this point:
  - CronJob spec.image: $(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}')
  - CronJob spec.suspend: $(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')
  - ArgoCD: $(kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status} {.status.health.status}')
EOF2

        # Anti-leak audit on the new evidence file (T-02.2-12-03 / T-02.2-12-07):
        if rtk proxy grep -qF "$SONARR_API_KEY" "$EVIDENCE"; then
          echo "ERROR: Sonarr API key leaked into evidence file"
          exit 1
        fi
        if rtk proxy grep -qF 'feed603a98de65f6e580d4bfa71ada68' "$EVIDENCE"; then
          echo "ERROR: known qBit passkey hex leaked into evidence file"
          exit 1
        fi
        # The fields[] array (which would carry credential masks) must NOT be present:
        if echo "$REDACTED_TEST_BODY" | rtk proxy jq -e 'any(.[]; has("fields"))' > /dev/null 2>&1; then
          echo "ERROR: T-02.2-12-02 — fields[] not stripped from redacted body"
          exit 1
        fi

    Step 6 — Cleanup port-forward + commit:
        kill $SONARR_PF 2>/dev/null || true
        trap - EXIT
        rm -f /tmp/test-response-body.json

        cd /home/moi/projets/perso/arr-stack
        git add "$EVIDENCE"
        git commit -m "$(cat <<'EOF'
evidence(02.2-12): credential dispositive — Sonarr Test API HTTP 200 + isValid: true

Closes deferred-items.md §D-02.2-AUTH-REGRESSION required-action #5 (re-run W-04
dispositive WITH credential check) — the BEHAVIORAL signal that Plan 06 Task 6.4
lacked. Programmatic POST /api/v3/downloadclient/test against the cluster Sonarr
returns 200 + isValid:true, proving the operator-driven password re-entry
(Task 12.1) restored the real credential to Sonarr's encrypted store.

Differential vs Plan 06 Task 6.4 attempt (2026-05-09T06:48Z):
  Plan 06: Sonarr UI 'Test' button → 401/403 (regression artifact: literal
    "********" stored as password)
  Plan 12: Sonarr Test API → 200 + isValid:true (real password stored)

CronJob STILL suspended at this point — Task 12.3 unsuspends only AFTER
verifying v0.1.5 reconcile preserves this dispositive (smoke job + post-smoke
re-test).

T-02.2-12-02 redaction: fields[] stripped from response body before commit
(prevents cluster credential masks landing in committed evidence).
T-02.2-12-07: operator password value NEVER captured by automation.
EOF
)"
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && EVIDENCE=$(ls -t .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/cluster-recovery-*.log 2>/dev/null | head -1) && [ -n "$EVIDENCE" ] && rtk proxy grep -q "sonarr_qbit_test_http_status: 200" "$EVIDENCE" && rtk proxy grep -q "sonarr_qbit_test_isValid: true" "$EVIDENCE" && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')" = "true" ] && git log -1 --pretty=%s | grep -q 'evidence(02.2-12): credential dispositive — Sonarr Test API HTTP 200'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02.2-v0-1-4-forcesave-fix/evidence/cluster-recovery-2026-05-09.log` exists, non-empty
    - File contains literal `sonarr_qbit_test_http_status: 200`
    - File contains literal `sonarr_qbit_test_isValid: true`
    - File does NOT contain the Sonarr API key literal (T-02.2-12-03 / T-02.2-12-07)
    - File does NOT contain the known qBit passkey hex `feed603a98de65f6e580d4bfa71ada68`
    - Redacted body does NOT contain a `fields[]` array (T-02.2-12-02 — request-echo of credential masks stripped)
    - CronJob `arrconf` `.spec.suspend == "true"` (still suspended — Task 12.3 unsuspends after smoke)
    - Commit recorded with subject `evidence(02.2-12): credential dispositive — Sonarr Test API HTTP 200 + isValid: true`
  </acceptance_criteria>
  <done>BEHAVIORAL credential dispositive captured. The signal that Plan 06 Task 6.4 lacked is now recorded. Task 12.3 can unsuspend + smoke.</done>
</task>

<task type="auto">
  <name>Task 12.3: Unsuspend CronJob + post-recovery smoke + verify merge_field_omitted_credential fires + post-smoke credential dispositive</name>
  <files>
    .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-recovery-smoke-2026-05-09.log
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/evidence/cluster-recovery-2026-05-09.log (Task 12.2 output — the credential dispositive must show HTTP 200 / isValid: true)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py (Plan 08 GREEN — verify merge_field_omitted_credential is the event name)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-06-PLAN.md Task 6.1 (smoke job pattern — same kubectl create job invocation)
  </read_first>
  <action>
    Step 1 — Verify Task 12.2 dispositive succeeded (gate for unsuspend):
        cd /home/moi/projets/perso/arr-stack
        EVIDENCE_RECOVERY=$(ls -t .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/cluster-recovery-*.log 2>/dev/null | head -1)
        [ -n "$EVIDENCE_RECOVERY" ] || { echo "ERROR: Task 12.2 evidence missing"; exit 1; }
        rtk proxy grep -q 'sonarr_qbit_test_http_status: 200' "$EVIDENCE_RECOVERY" || {
          echo "ERROR: T-02.2-12-01 — Task 12.2 dispositive did not record HTTP 200; refusing to unsuspend"
          exit 1
        }
        rtk proxy grep -q 'sonarr_qbit_test_isValid: true' "$EVIDENCE_RECOVERY" || {
          echo "ERROR: T-02.2-12-01 — Task 12.2 dispositive did not record isValid:true; refusing to unsuspend"
          exit 1
        }

        # Confirm CronJob still suspended at the start (sanity):
        SUSPEND=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')
        [ "$SUSPEND" = "true" ] || { echo "ERROR: CronJob unexpectedly unsuspended before Task 12.3"; exit 1; }

    Step 2 — Unsuspend the CronJob (the operation gated by Task 12.2 success):
        kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":false}}'
        SUSPEND_AFTER=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')
        echo "CronJob suspend after patch: $SUSPEND_AFTER"
        [ "$SUSPEND_AFTER" = "false" ] || { echo "ERROR: unsuspend patch did not take effect"; exit 1; }

    Step 3 — Force a smoke job (apply mode):
        SMOKE_JOB="arrconf-pr-v0.1.5-smoke-$(date +%s)"
        kubectl create job --from=cronjob/arrconf "$SMOKE_JOB" -n selfhost
        kubectl wait --for=condition=complete "job/$SMOKE_JOB" -n selfhost --timeout=180s

        EVIDENCE_SMOKE=".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-recovery-smoke-$(date +%F).log"
        kubectl logs "job/$SMOKE_JOB" -n selfhost > "$EVIDENCE_SMOKE"

    Step 4 — Verify the smoke logs contain the v0.1.5 dispositive events:
        # (1) merge_field_omitted_credential MUST fire (proves v0.1.5 differ.py is engaged):
        OMIT_EVENTS=$(rtk proxy jq -c 'select(.event == "merge_field_omitted_credential")' "$EVIDENCE_SMOKE" 2>/dev/null | wc -l)
        echo "merge_field_omitted_credential events: $OMIT_EVENTS"

        # (2) merge_field_preserved MUST NOT fire for username/password (those are now omitted, not preserved):
        PRESERVED_EVENTS_CRED=$(rtk proxy jq -c 'select(.event == "merge_field_preserved" and (.name == "password" or .name == "username"))' "$EVIDENCE_SMOKE" 2>/dev/null | wc -l)
        echo "merge_field_preserved (for username/password) events: $PRESERVED_EVENTS_CRED (must be 0)"
        [ "$PRESERVED_EVENTS_CRED" -eq 0 ] || { echo "ERROR: merge_field_preserved still fires for credentials — v0.1.5 fix not engaged"; exit 1; }

        # (3) Plan 02 contract: put_force_save_used MAY still fire (only if there's actually an UPDATE — first reconcile after unsuspend may be no-op since YAML matches cluster state with omitted credentials):
        FORCE_SAVE_EVENTS=$(rtk proxy jq -c 'select(.event == "put_force_save_used")' "$EVIDENCE_SMOKE" 2>/dev/null | wc -l)
        echo "put_force_save_used events: $FORCE_SAVE_EVENTS (informational; non-zero only if UPDATE plan_action fired)"

        # (4) HTTP 400 events: 0 (the regression class is closed):
        HTTP_400=$(rtk proxy grep -c '"status_code":400\|HTTPStatusError.*400\|400 Bad Request' "$EVIDENCE_SMOKE" 2>/dev/null || echo 0)
        echo "HTTP 400 events: $HTTP_400 (expected 0)"
        [ "$HTTP_400" -eq 0 ] || { echo "ERROR: HTTP 400 in v0.1.5 smoke logs"; exit 1; }

        # (5) plan_action: at least one (the smoke is a real reconcile):
        PLAN_EVENTS=$(rtk proxy jq -c 'select(.event == "plan_action")' "$EVIDENCE_SMOKE" 2>/dev/null | wc -l)
        echo "plan_action events: $PLAN_EVENTS (expected >= 1)"
        [ "$PLAN_EVENTS" -ge 1 ] || { echo "ERROR: no plan_action events"; exit 1; }

        # (6) apply_complete event:
        APPLY_DONE=$(rtk proxy jq -c 'select(.event == "apply_complete")' "$EVIDENCE_SMOKE" 2>/dev/null | wc -l)
        echo "apply_complete events: $APPLY_DONE (expected >= 1)"
        [ "$APPLY_DONE" -ge 1 ] || { echo "ERROR: smoke did not finish apply"; exit 1; }

    Step 5 — Anti-leak audit on smoke evidence (T-02.2-12-03):
        if rtk proxy grep -qF "$SONARR_API_KEY" "$EVIDENCE_SMOKE"; then
          echo "ERROR: Sonarr API key leaked into smoke evidence"
          exit 1
        fi
        if rtk proxy grep -qF 'feed603a98de65f6e580d4bfa71ada68' "$EVIDENCE_SMOKE"; then
          echo "ERROR: known qBit passkey hex leaked into smoke evidence"
          exit 1
        fi

    Step 6 — Post-smoke credential RE-DISPOSITIVE (proves the reconcile did NOT re-overwrite the credential):
        if [ -z "$SONARR_API_KEY" ]; then
          export SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"
        fi
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        SONARR_PF2=$!
        sleep 3
        trap "kill $SONARR_PF2 2>/dev/null || true" EXIT

        DC_BODY_POST=$(rtk proxy curl -fsS -H "X-Api-Key: $SONARR_API_KEY" "http://127.0.0.1:8989/api/v3/downloadclient/1")
        TEST_RAW_POST=$(rtk proxy curl -sS -o /tmp/test-post.json -w '%{http_code}' \
          -X POST -H "X-Api-Key: $SONARR_API_KEY" -H "Content-Type: application/json" \
          -d "$DC_BODY_POST" "http://127.0.0.1:8989/api/v3/downloadclient/test")
        IS_VALID_POST=$(rtk proxy jq -r '.[0].isValid // false' < /tmp/test-post.json)
        echo "Post-smoke Sonarr Test API: HTTP $TEST_RAW_POST, isValid=$IS_VALID_POST"

        if [ "$TEST_RAW_POST" != "200" ] || [ "$IS_VALID_POST" != "true" ]; then
          echo "ERROR: post-smoke Sonarr Test API failed — v0.1.5 reconcile re-overwrote the credential"
          echo "Re-suspending CronJob immediately:"
          kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":true}}'
          exit 1
        fi

        # Append post-smoke dispositive to the smoke evidence file:
        echo "" >> "$EVIDENCE_SMOKE"
        echo "# === POST-SMOKE CREDENTIAL DISPOSITIVE ===" >> "$EVIDENCE_SMOKE"
        echo "# Timestamp: $(date -u +%FT%TZ)" >> "$EVIDENCE_SMOKE"
        echo "# sonarr_qbit_test_http_status: $TEST_RAW_POST" >> "$EVIDENCE_SMOKE"
        echo "# sonarr_qbit_test_isValid: $IS_VALID_POST" >> "$EVIDENCE_SMOKE"
        echo "# merge_field_omitted_credential events fired during smoke: $OMIT_EVENTS" >> "$EVIDENCE_SMOKE"
        echo "# merge_field_preserved (for credentials) events: 0 (v0.1.5 dispositive)" >> "$EVIDENCE_SMOKE"
        echo "# put_force_save_used events: $FORCE_SAVE_EVENTS" >> "$EVIDENCE_SMOKE"
        echo "# HTTP 400 events: 0" >> "$EVIDENCE_SMOKE"

        kill $SONARR_PF2 2>/dev/null || true
        trap - EXIT
        rm -f /tmp/test-post.json

    Step 7 — Cleanup smoke job + commit:
        kubectl delete "job/$SMOKE_JOB" -n selfhost --ignore-not-found

        cd /home/moi/projets/perso/arr-stack
        git add "$EVIDENCE_SMOKE"
        git commit -m "$(cat <<'EOF'
evidence(02.2-12): post-recovery smoke — v0.1.5 emits merge_field_omitted_credential, no HTTP 400, post-smoke credential intact

CronJob unsuspended after Task 12.2 dispositive succeeded. Forced smoke job ran
on v0.1.5 image and emitted the new merge_field_omitted_credential event for
the privacy=password|userName fields (proves Plan 08 GREEN code is engaged in
cluster). merge_field_preserved did NOT fire for username/password (the v0.1.5
contract — the omit-credential branch takes precedence). Zero HTTP 400.

Post-smoke RE-dispositive (the load-bearing post-reconcile check Plan 06
lacked): Sonarr Test API still returns HTTP 200 + isValid:true AFTER the
v0.1.5 reconcile fired. Proves the reconcile did NOT re-overwrite the
operator-restored credential.

Combined with Task 12.2 evidence, the credential class regression
D-02.2-AUTH-REGRESSION is closed — the v0.1.5 reconcile is safe.

Task 12.4 will run the credential-aware drift demo to close the priority class
half of REQ-drift-detection in the same evidence file.
EOF
)"
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && EVIDENCE=$(ls -t .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-recovery-smoke-*.log 2>/dev/null | head -1) && [ -n "$EVIDENCE" ] && [ "$(rtk proxy jq -c 'select(.event == "merge_field_omitted_credential")' "$EVIDENCE" 2>/dev/null | wc -l)" -ge 1 ] && [ "$(rtk proxy jq -c 'select(.event == "merge_field_preserved" and (.name == "password" or .name == "username"))' "$EVIDENCE" 2>/dev/null | wc -l)" = "0" ] && [ "$(rtk proxy grep -c '"status_code":400\|HTTPStatusError.*400\|400 Bad Request' "$EVIDENCE" || echo 0)" = "0" ] && rtk proxy grep -q "sonarr_qbit_test_http_status: 200" "$EVIDENCE" && rtk proxy grep -q "sonarr_qbit_test_isValid: true" "$EVIDENCE" && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')" = "false" ] && git log -1 --pretty=%s | grep -q 'evidence(02.2-12): post-recovery smoke'</automated>
  </verify>
  <acceptance_criteria>
    - CronJob `arrconf` `.spec.suspend == "false"` after this task (unsuspended only after Task 12.2 dispositive recorded HTTP 200)
    - Smoke evidence file at `.planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-recovery-smoke-2026-05-09.log`
    - Smoke evidence: at least 1 `merge_field_omitted_credential` event (Plan 08 dispositive — v0.1.5 code engaged)
    - Smoke evidence: ZERO `merge_field_preserved` events for `name == "password" or name == "username"` (v0.1.5 contract)
    - Smoke evidence: ZERO HTTP 400 events
    - Smoke evidence: at least 1 `plan_action` event
    - Smoke evidence: at least 1 `apply_complete` event
    - Smoke evidence appended block: `sonarr_qbit_test_http_status: 200` (post-smoke)
    - Smoke evidence appended block: `sonarr_qbit_test_isValid: true` (post-smoke)
    - Anti-leak audit clean (no Sonarr API key, no qBit passkey hex)
    - Commit recorded with subject `evidence(02.2-12): post-recovery smoke — v0.1.5 emits merge_field_omitted_credential, no HTTP 400, post-smoke credential intact`
  </acceptance_criteria>
  <done>v0.1.5 reconcile proven safe: emits merge_field_omitted_credential, omits credential fields, preserves operator-restored credential. CronJob unsuspended. Task 12.4 runs the credential-aware drift demo to close REQ-drift-detection.</done>
</task>

<task type="auto">
  <name>Task 12.4: Credential-aware drift demo + W-01 forensic + post-recovery snapshot + combined dispositive</name>
  <files>
    .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-credential-aware-2026-05-09.log
    snapshots/post-phase2.2-hotfix-2026-05-09/
    snapshots/drift-test-phase2.2-hotfix-2026-05-09/
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-06-PLAN.md Task 6.3 (drift demo runbook — reuse the curl PUT priority pattern + force smoke + W-04 dispositive)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/snapshots-before-phase-2.2-hotfix-2026-05-09 (Plan 07 deliverable — ORIGINAL_PRIORITY anchor)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-PLAN.md Task 5.2 (drift demo runbook precedent)
    - /home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
  </read_first>
  <action>
    Step 1 — Pre-flight: cluster healthy + CronJob unsuspended:
        cd /home/moi/projets/perso/arr-stack
        SUSPEND=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')
        [ "$SUSPEND" = "false" ] || { echo "ERROR: CronJob NOT unsuspended — Task 12.3 incomplete"; exit 1; }

    Step 2 — Capture post-recovery snapshot (after Task 12.3 smoke; clean cluster state baseline):
        if [ -z "$SONARR_API_KEY" ]; then
          export SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"
        fi
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        SONARR_PF=$!
        kubectl -n selfhost port-forward svc/qbittorrent 8080:8080 &
        QBIT_PF=$!
        sleep 3
        trap "kill $SONARR_PF $QBIT_PF 2>/dev/null || true" EXIT

        POST_RECOVERY_DIR="snapshots/post-phase2.2-hotfix-2026-05-09"
        rm -rf "$POST_RECOVERY_DIR"
        tools/snapshot/snapshot.sh --apps sonarr --output "$POST_RECOVERY_DIR/"

        SONARR_COUNT=$(find "$POST_RECOVERY_DIR/sonarr" -name '*.json' -type f 2>/dev/null | wc -l)
        [ "$SONARR_COUNT" -eq 17 ] || { echo "ERROR: expected 17 sonarr JSONs, got $SONARR_COUNT"; exit 1; }

        # qBit endpoints manually (D-02.1-04 workaround):
        QBIT_DIR="$POST_RECOVERY_DIR/qbittorrent"
        mkdir -p "$QBIT_DIR"
        for endpoint in app/version app/preferences app/buildInfo torrents/categories torrents/info torrents/tags transfer/info sync/maindata; do
          rtk proxy curl -s "http://127.0.0.1:8080/api/v2/$endpoint" -o "$QBIT_DIR/$(echo "$endpoint" | tr '/' '_').json" || true
        done

        # Per-snapshot redaction (Plan 07 pattern — Plan 01 token style):
        CONFIG_HOST="$POST_RECOVERY_DIR/sonarr/config_host.json"
        if [ -f "$CONFIG_HOST" ]; then
          rtk proxy jq '.apiKey = "***REDACTED***" | .password = "***REDACTED***" | .username = "***REDACTED***"' "$CONFIG_HOST" > "$CONFIG_HOST.tmp"
          \mv -f "$CONFIG_HOST.tmp" "$CONFIG_HOST"
        fi
        TORRENTS_INFO="$QBIT_DIR/torrents_info.json"
        if [ -f "$TORRENTS_INFO" ]; then
          rtk proxy jq 'walk(if type == "string" then gsub("/announce/[a-fA-F0-9]{16,}/?"; "/announce/PLACEHOLDER_PASSKEY/") | gsub("announce%2[Ff][a-fA-F0-9]{16,}"; "announce%2FPLACEHOLDER_PASSKEY") else . end)' "$TORRENTS_INFO" > "$TORRENTS_INFO.tmp"
          \mv -f "$TORRENTS_INFO.tmp" "$TORRENTS_INFO"
        fi
        PREFS="$QBIT_DIR/app_preferences.json"
        if [ -f "$PREFS" ]; then
          rtk proxy jq '.web_ui_password = "***REDACTED***" | .proxy_password = "***REDACTED***" | .autorun_program = "***REDACTED***"' "$PREFS" > "$PREFS.tmp"
          \mv -f "$PREFS.tmp" "$PREFS"
        fi
        find "$POST_RECOVERY_DIR" -name '*.tmp' -type f -delete 2>/dev/null || true

        # Anti-leak (T-02.2-12-05):
        LEAK_TOTAL=0
        for pat in "$SONARR_API_KEY" "feed603a98de65f6e580d4bfa71ada68"; do
          n=$(rtk proxy grep -RFl "$pat" "$POST_RECOVERY_DIR" 2>/dev/null | wc -l)
          LEAK_TOTAL=$((LEAK_TOTAL + n))
        done
        n_url=$(rtk proxy grep -REl 'announce%2[Ff]feed603a' "$POST_RECOVERY_DIR" 2>/dev/null | wc -l)
        LEAK_TOTAL=$((LEAK_TOTAL + n_url))
        [ "$LEAK_TOTAL" -eq 0 ] || { echo "ERROR: anti-leak audit failed on post-recovery snapshot ($LEAK_TOTAL hits)"; exit 1; }

        # Capture ORIGINAL_PRIORITY (anchor — should be 1 from Plan 07 baseline):
        ORIGINAL_PRIORITY=$(rtk proxy jq '.[0].priority' "snapshots/before-phase-2.2-hotfix-2026-05-09/sonarr/downloadclient.json")
        echo "ORIGINAL_PRIORITY (from Plan 07 baseline): $ORIGINAL_PRIORITY"

        # Capture current priority (should match YAML ⇒ same as ORIGINAL_PRIORITY):
        POST_RECOVERY_PRIORITY=$(rtk proxy jq '.[0].priority' "$POST_RECOVERY_DIR/sonarr/downloadclient.json")
        echo "POST_RECOVERY_PRIORITY: $POST_RECOVERY_PRIORITY"

    Step 3 — W-01 forensic snapshot BEFORE inducing drift:
        DRIFT_DIR="snapshots/drift-test-phase2.2-hotfix-2026-05-09"
        rm -rf "$DRIFT_DIR"
        tools/snapshot/snapshot.sh --apps sonarr --output "$DRIFT_DIR/"

        # Redact:
        DC_HOST="$DRIFT_DIR/sonarr/config_host.json"
        if [ -f "$DC_HOST" ]; then
          rtk proxy jq '.apiKey = "***REDACTED***" | .password = "***REDACTED***" | .username = "***REDACTED***"' "$DC_HOST" > "$DC_HOST.tmp"
          \mv -f "$DC_HOST.tmp" "$DC_HOST"
        fi
        find "$DRIFT_DIR" -name '*.tmp' -type f -delete 2>/dev/null || true

    Step 4 — Induce priority drift via curl PUT (priority is the canonical safe field, A6):
        DRIFTED_PRIORITY=5
        # Fetch the current downloadclient resource:
        DC_RES=$(rtk proxy curl -fsS -H "X-Api-Key: $SONARR_API_KEY" "http://127.0.0.1:8989/api/v3/downloadclient/1")
        # Modify priority only (T-02.2-12-04 — DO NOT touch fields[]):
        DC_DRIFTED=$(echo "$DC_RES" | rtk proxy jq ".priority = $DRIFTED_PRIORITY")

        # PUT with ?forceSave=true (the same path the operator-nudge in Plan 06 used; needed because we're outside arrconf):
        DRIFT_HTTP=$(rtk proxy curl -sS -o /tmp/drift-resp.json -w '%{http_code}' \
          -X PUT -H "X-Api-Key: $SONARR_API_KEY" -H "Content-Type: application/json" \
          -d "$DC_DRIFTED" "http://127.0.0.1:8989/api/v3/downloadclient/1?forceSave=true")
        echo "Drift PUT HTTP: $DRIFT_HTTP (expected 200 or 202)"
        [ "$DRIFT_HTTP" = "200" ] || [ "$DRIFT_HTTP" = "202" ] || { echo "ERROR: drift PUT failed: $DRIFT_HTTP"; exit 1; }

        # Verify drift took effect:
        DRIFTED_VERIFY=$(rtk proxy curl -fsS -H "X-Api-Key: $SONARR_API_KEY" "http://127.0.0.1:8989/api/v3/downloadclient/1" | rtk proxy jq '.priority')
        echo "Drifted priority verified: $DRIFTED_VERIFY (expected $DRIFTED_PRIORITY)"
        [ "$DRIFTED_VERIFY" = "$DRIFTED_PRIORITY" ] || { echo "ERROR: drift not applied"; exit 1; }

        # T-02.2-12-04: confirm fields[] credentials NOT disturbed by drift PUT (we modified only priority):
        # (Sonarr's GET-side serialization always returns mask, but the field metadata should be intact):
        FIELD_COUNT=$(rtk proxy curl -fsS -H "X-Api-Key: $SONARR_API_KEY" "http://127.0.0.1:8989/api/v3/downloadclient/1" | rtk proxy jq '.fields | length')
        echo "Fields count post-drift-PUT: $FIELD_COUNT (must be unchanged from POST_RECOVERY snapshot)"

    Step 5 — Force smoke job (the v0.1.5 reconcile that should restore priority + omit credentials):
        DRIFT_SMOKE_JOB="arrconf-drift-credential-aware-$(date +%s)"
        kubectl create job --from=cronjob/arrconf "$DRIFT_SMOKE_JOB" -n selfhost
        kubectl wait --for=condition=complete "job/$DRIFT_SMOKE_JOB" -n selfhost --timeout=180s

        DRIFT_LOG=".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-credential-aware-$(date +%F).log"
        kubectl logs "job/$DRIFT_SMOKE_JOB" -n selfhost > "$DRIFT_LOG"

    Step 6 — Verify drift was restored (W-04 dispositive value-equality):
        # Re-fetch via Sonarr GET:
        RESTORED_PRIORITY=$(rtk proxy curl -fsS -H "X-Api-Key: $SONARR_API_KEY" "http://127.0.0.1:8989/api/v3/downloadclient/1" | rtk proxy jq '.priority')
        echo "RESTORED_PRIORITY: $RESTORED_PRIORITY (expected: $ORIGINAL_PRIORITY)"
        [ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ] || { echo "ERROR: priority NOT restored — W-04 dispositive failed"; exit 1; }

    Step 7 — Post-drift credential dispositive (the credential-aware half):
        DC_AFTER_DRIFT=$(rtk proxy curl -fsS -H "X-Api-Key: $SONARR_API_KEY" "http://127.0.0.1:8989/api/v3/downloadclient/1")
        TEST_AFTER=$(rtk proxy curl -sS -o /tmp/test-after.json -w '%{http_code}' \
          -X POST -H "X-Api-Key: $SONARR_API_KEY" -H "Content-Type: application/json" \
          -d "$DC_AFTER_DRIFT" "http://127.0.0.1:8989/api/v3/downloadclient/test")
        IS_VALID_AFTER=$(rtk proxy jq -r '.[0].isValid // false' < /tmp/test-after.json)
        echo "Post-drift Sonarr Test API: HTTP $TEST_AFTER, isValid=$IS_VALID_AFTER"
        [ "$TEST_AFTER" = "200" ] && [ "$IS_VALID_AFTER" = "true" ] || {
          echo "ERROR: post-drift Sonarr Test API failed — drift correction re-overwrote credential"
          echo "Re-suspending CronJob:"
          kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":true}}'
          exit 1
        }

    Step 8 — Verify smoke logs contain expected events:
        OMIT_DRIFT=$(rtk proxy jq -c 'select(.event == "merge_field_omitted_credential")' "$DRIFT_LOG" 2>/dev/null | wc -l)
        FORCE_SAVE_DRIFT=$(rtk proxy jq -c 'select(.event == "put_force_save_used")' "$DRIFT_LOG" 2>/dev/null | wc -l)
        UPDATE_PA=$(rtk proxy jq -c 'select(.event == "plan_action" and .action == "update")' "$DRIFT_LOG" 2>/dev/null | wc -l)
        HTTP_400_DRIFT=$(rtk proxy grep -c '"status_code":400\|400 Bad Request' "$DRIFT_LOG" 2>/dev/null || echo 0)

        echo "Drift smoke events:"
        echo "  merge_field_omitted_credential: $OMIT_DRIFT"
        echo "  put_force_save_used: $FORCE_SAVE_DRIFT (Plan 02 contract; expect >=1 since drift caused real UPDATE)"
        echo "  plan_action update: $UPDATE_PA"
        echo "  HTTP 400: $HTTP_400_DRIFT (must be 0)"
        [ "$HTTP_400_DRIFT" -eq 0 ] || { echo "ERROR: HTTP 400 in drift smoke"; exit 1; }
        [ "$UPDATE_PA" -ge 1 ] || { echo "ERROR: no plan_action update fired — drift not detected"; exit 1; }
        [ "$FORCE_SAVE_DRIFT" -ge 1 ] || { echo "ERROR: put_force_save_used did not fire — Plan 02 contract regressed"; exit 1; }
        [ "$OMIT_DRIFT" -ge 1 ] || { echo "ERROR: merge_field_omitted_credential did not fire — Plan 08 contract regressed"; exit 1; }

    Step 9 — Append the COMBINED DISPOSITIVE COMPARISON block to the drift evidence file (per gap_summary <dispositive_specification>):
        cat >> "$DRIFT_LOG" <<EOF2

# === DRIFT-DEMO + CREDENTIAL DISPOSITIVE COMPARISON (Phase 02.2 v0.1.5 hotfix) ===
# original_priority: $ORIGINAL_PRIORITY
# drifted_priority:  $DRIFTED_PRIORITY
# restored_priority: $RESTORED_PRIORITY
# match (restored == original): $([ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ] && echo "YES" || echo "NO")
# manual_nudge_used: NO
# put_force_save_used events fired: $FORCE_SAVE_DRIFT
# merge_field_omitted_credential events fired: $OMIT_DRIFT
# HTTP 400 events: 0
# sonarr_qbit_test_http_status: $TEST_AFTER
# sonarr_qbit_test_isValid: $IS_VALID_AFTER
# password_field_in_put_body: ABSENT (verified indirectly — merge_field_omitted_credential events are the dispositive proof, since differ.py omits the entries before reaching the PUT body)

# Differential vs Plan 06 Task 6.3 (FULLY-AUTOMATED drift demo, FAILED at credential-side):
#   Plan 06: priority restored ✅ + credential overwrite (HTTP 401/403 on Test) ❌
#   Plan 12: priority restored ✅ + credential preserved (HTTP 200 on Test) ✅
#
# REQ-drift-detection correction half: SATISFIED CLEANLY in BOTH halves
# (priority restoration + credential survival).
EOF2

    Step 10 — Anti-leak audit on drift evidence:
        if rtk proxy grep -qF "$SONARR_API_KEY" "$DRIFT_LOG"; then
          echo "ERROR: Sonarr API key leaked into drift evidence"
          exit 1
        fi
        if rtk proxy grep -qF 'feed603a98de65f6e580d4bfa71ada68' "$DRIFT_LOG"; then
          echo "ERROR: passkey hex leaked into drift evidence"
          exit 1
        fi

    Step 11 — Cleanup smoke job + port-forwards + commit:
        kubectl delete "job/$DRIFT_SMOKE_JOB" -n selfhost --ignore-not-found
        kill $SONARR_PF $QBIT_PF 2>/dev/null || true
        trap - EXIT
        rm -f /tmp/drift-resp.json /tmp/test-after.json

        cd /home/moi/projets/perso/arr-stack
        git add "$POST_RECOVERY_DIR" "$DRIFT_DIR" "$DRIFT_LOG"
        git commit -m "$(cat <<'EOF'
evidence(02.2-12): credential-aware drift demo — REQ-drift-detection FINAL closure

Combined dispositive: priority restoration + credential survival in one
automated reconcile run. Closes deferred-items.md §D-02.2-AUTH-REGRESSION
required-actions #5 (re-run W-04 dispositive WITH credential check) and
sets up #6 (re-open Plan 06 Task 6.4 visual gate).

Sequence captured (all in evidence/drift-demo-credential-aware-2026-05-09.log):
1. Pre-drift: Sonarr Test API HTTP 200 (Task 12.2 anchor)
2. Drift induced via curl PUT priority=1→5 (?forceSave=true bypass; T-02.2-12-04
   ensures fields[] not touched)
3. Post-drift Sonarr GET shows priority=5
4. Force smoke job (v0.1.5)
5. Smoke logs contain merge_field_omitted_credential (>=1) + put_force_save_used
   (>=1) + plan_action update (>=1) + zero HTTP 400
6. Post-reconcile Sonarr GET shows priority=ORIGINAL (W-04 dispositive)
7. Post-reconcile Sonarr Test API still HTTP 200 + isValid:true (CREDENTIAL
   dispositive — proves the reconcile preserved the operator-restored credential)

Differential vs Plan 06 Task 6.3:
  Plan 06: priority ✅ + credential ❌ (FAILED)
  Plan 12: priority ✅ + credential ✅ (CLOSED)

Snapshot artifacts (ADR-6):
- snapshots/post-phase2.2-hotfix-2026-05-09/sonarr/ (17 JSON; clean) +
  qbittorrent/ (8 JSON; redacted)
- snapshots/drift-test-phase2.2-hotfix-2026-05-09/sonarr/ (17 JSON; W-01
  forensic before-drift)

REQ-drift-detection correction half: SATISFIED CLEANLY (both halves of the
regression class closed).

Task 12.5 re-opens Plan 06 Task 6.4 visual gate as a ceremonial close.
EOF
)"
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && DRIFT_LOG=$(ls -t .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-credential-aware-*.log 2>/dev/null | head -1) && [ -n "$DRIFT_LOG" ] && rtk proxy grep -q "match (restored == original): YES" "$DRIFT_LOG" && rtk proxy grep -q "manual_nudge_used: NO" "$DRIFT_LOG" && rtk proxy grep -q "sonarr_qbit_test_http_status: 200" "$DRIFT_LOG" && rtk proxy grep -q "sonarr_qbit_test_isValid: true" "$DRIFT_LOG" && [ "$(rtk proxy jq -c 'select(.event == "merge_field_omitted_credential")' "$DRIFT_LOG" 2>/dev/null | wc -l)" -ge 1 ] && [ "$(rtk proxy jq -c 'select(.event == "put_force_save_used")' "$DRIFT_LOG" 2>/dev/null | wc -l)" -ge 1 ] && [ "$(rtk proxy grep -c '"status_code":400\|400 Bad Request' "$DRIFT_LOG" || echo 0)" = "0" ] && [ -d snapshots/post-phase2.2-hotfix-2026-05-09/sonarr ] && [ -d snapshots/drift-test-phase2.2-hotfix-2026-05-09/sonarr ] && [ "$(find snapshots/post-phase2.2-hotfix-2026-05-09/sonarr -name '*.json' | wc -l)" -eq 17 ] && [ "$(find snapshots/drift-test-phase2.2-hotfix-2026-05-09/sonarr -name '*.json' | wc -l)" -eq 17 ] && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')" = "false" ] && git log -1 --pretty=%s | grep -q 'evidence(02.2-12): credential-aware drift demo'</automated>
  </verify>
  <acceptance_criteria>
    - Drift evidence file contains literal `match (restored == original): YES`
    - Drift evidence file contains literal `manual_nudge_used: NO`
    - Drift evidence file contains literal `sonarr_qbit_test_http_status: 200` (post-reconcile credential dispositive)
    - Drift evidence file contains literal `sonarr_qbit_test_isValid: true`
    - Drift evidence file contains at least 1 `merge_field_omitted_credential` event (Plan 08 contract)
    - Drift evidence file contains at least 1 `put_force_save_used` event (Plan 02 contract preserved)
    - Drift evidence file contains zero HTTP 400 events
    - Snapshot dir `snapshots/post-phase2.2-hotfix-2026-05-09/sonarr/` has exactly 17 JSON files
    - Snapshot dir `snapshots/drift-test-phase2.2-hotfix-2026-05-09/sonarr/` has exactly 17 JSON files (W-01 forensic before-drift)
    - Anti-leak audit clean on both snapshot dirs + drift evidence file
    - CronJob `arrconf` `.spec.suspend == "false"` (still unsuspended after drift demo — the test PROVES the system is safe to leave running)
    - Commit recorded with subject `evidence(02.2-12): credential-aware drift demo — REQ-drift-detection FINAL closure`
  </acceptance_criteria>
  <done>Both halves of the REQ-drift-detection correction half are SATISFIED in one automated run. Plan 06 Task 6.4 visual gate can now be ceremoniously re-opened (Task 12.5).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 12.5: Re-open Plan 06 Task 6.4 visual gate — operator confirms priority + Test button success in Sonarr UI</name>
  <files>
  </files>
  <action>
    This is a checkpoint:human-verify task — Claude has completed Tasks 12.1–12.4 (operator-recovered credential, programmatic Sonarr Test API HTTP 200 dispositive at 3 points, post-recovery smoke + post-drift smoke both showing merge_field_omitted_credential events with zero HTTP 400, combined drift-demo + credential dispositive log committed). The orchestrator pauses for the operator to ceremonially re-open Plan 06 Task 6.4 visual gate by performing the same UI Test button click that returned 401/403 on 2026-05-09T06:48Z and confirming THIS TIME it succeeds.

    The operator follows the &lt;how-to-verify&gt; steps below and signals via &lt;resume-signal&gt; when the visual gate is closed. No further code/cluster mutation; this is verification-only.
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack &amp;&amp; ls .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/cluster-recovery-*.log >/dev/null 2>&amp;1 &amp;&amp; ls .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-recovery-smoke-*.log >/dev/null 2>&amp;1 &amp;&amp; ls .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-credential-aware-*.log >/dev/null 2>&amp;1 &amp;&amp; [ -d snapshots/post-phase2.2-hotfix-2026-05-09/sonarr ] &amp;&amp; [ -d snapshots/drift-test-phase2.2-hotfix-2026-05-09/sonarr ] &amp;&amp; [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}' 2>/dev/null)" = "false" ]</automated>
  </verify>
  <done>Operator has confirmed Sonarr UI Test button returns green/OK on the qBit downloadclient AND the priority value matches YAML expectations. Plan 06 Task 6.4 visual gate is FINALLY closed (the same gate that FAILED on 2026-05-09 is now PASSED). REQ-drift-detection correction half is SATISFIED CLEANLY in BOTH halves (priority + credential).</done>
  <what-built>
    Tasks 12.1–12.4 closed the credential-overwrite regression in code (Plan 08), in cluster behavior (Sonarr Test API HTTP 200 in Tasks 12.2 + post-smoke + post-drift), and in the audit trail (4 evidence files + 2 snapshot directories committed). The CronJob is unsuspended and v0.1.5 is the live image. The combined dispositive log proves automated drift correction WITHOUT credential overwrite — the regression class D-02.2-AUTH-REGRESSION is closed.

    This task is the ceremonial close of Plan 06 Task 6.4 (which FAILED on 2026-05-09 because the operator's UI Test button returned 401/403). The same operator does the same UI test, and THIS TIME it succeeds. The visual gate that Plan 06 left INCOMPLETE is now COMPLETE.
  </what-built>
  <how-to-verify>
    1. Sanity check on the artifacts:
       ```bash
       cd /home/moi/projets/perso/arr-stack
       ls .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/cluster-recovery-*.log
       ls .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-recovery-smoke-*.log
       ls .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-credential-aware-*.log
       ls -d snapshots/post-phase2.2-hotfix-2026-05-09 snapshots/drift-test-phase2.2-hotfix-2026-05-09
       # All four must exist.
       ```

    2. Open Sonarr UI (same UI as Plan 06 Task 6.4 attempt) — port-forward if needed:
       ```bash
       kubectl -n selfhost port-forward svc/sonarr 8989:8989
       ```
       Then visit `http://localhost:8989/settings/downloadclients`.

    3. **Visual check #1 — priority value**: Open the qBittorrent download client. The "Priority" field should match the YAML-declared value (default 1, unless arrconf YAML has been changed since Plan 02.2-01). Plan 12 Task 12.4 induced drift to 5 then verified arrconf restored to 1; you should see 1 in the UI.

    4. **Visual check #2 — Sonarr "Test" button**: Click the green "Test" button on the qBittorrent client. It should return green/OK (the same button that returned red 401/403 in Plan 06). This is the load-bearing visual confirmation that the credential is real.

    5. **Visual check #3 — config sanity (optional)**: Verify the qBit host/port/category values match expectations (host = qbittorrent.selfhost.svc.cluster.local; port = 8080; tvCategory = sonarr). No surprise drift in non-priority fields.

    6. **DO NOT re-suspend the CronJob, do NOT change any settings.** This is a verification-only step.

    Check before signaling "approved":
    - Sonarr UI Test returned green/OK (same UI button that failed in Plan 06)
    - Priority value matches expected (1 or whatever YAML declares)
    - Host/port/category fields look correct
    - CronJob is unsuspended (verify via `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returns `false`)
    - You can close the page; the system is in steady state
  </how-to-verify>
  <resume-signal>
    Type "approved" once: priority correct in UI, Sonarr Test button green/OK, host/port/category sane.

    OR describe what's wrong (e.g. "Test button still red" → re-suspend CronJob, halt phase, escalate; or "priority doesn't match YAML" → investigate via `kubectl logs` of the most recent reconcile job; or "host changed unexpectedly" → drift-investigation needed).
  </resume-signal>
</task>

</tasks>

<verification>
- 4 evidence files committed: cluster-recovery + post-recovery-smoke + drift-demo-credential-aware (+ pre-existing forensic*)
- 2 new snapshot directories committed: post-phase2.2-hotfix + drift-test-phase2.2-hotfix
- Sonarr Test API returns HTTP 200 + isValid:true at THREE points: pre-unsuspend (Task 12.2), post-smoke (Task 12.3), post-drift-correction (Task 12.4)
- merge_field_omitted_credential fires in both smoke logs (post-recovery + drift)
- merge_field_preserved does NOT fire for username/password (Plan 08 contract proven in cluster)
- put_force_save_used still fires for UPDATE PUTs (Plan 02 contract preserved)
- W-04 dispositive: RESTORED_PRIORITY == ORIGINAL_PRIORITY captured in drift evidence
- manual_nudge_used: NO (the differential against Phase 2.1 closure)
- HTTP 400 events: 0 across all smoke logs
- Anti-leak audit clean across all evidence + snapshots
- CronJob unsuspended at the END only (after all dispositives succeed); STAYS suspended throughout earlier tasks
- Plan 06 Task 6.4 visual gate FINALLY closed
</verification>

<success_criteria>
- [ ] Operator UI password re-entry confirmed (Task 12.1)
- [ ] Sonarr Test API HTTP 200 + isValid:true captured in evidence/cluster-recovery-*.log (Task 12.2)
- [ ] CronJob unsuspended ONLY after Task 12.2 dispositive (Task 12.3)
- [ ] Post-recovery smoke fires merge_field_omitted_credential, no HTTP 400 (Task 12.3)
- [ ] Post-smoke Sonarr Test API still 200 + isValid:true (Task 12.3)
- [ ] Credential-aware drift demo: priority restored + Test API still 200 post-reconcile (Task 12.4)
- [ ] Combined DISPOSITIVE COMPARISON block matches gap_summary <dispositive_specification> shape (Task 12.4)
- [ ] Plan 06 Task 6.4 visual gate re-opened and CLOSED (Task 12.5)
- [ ] REQ-drift-detection correction half: SATISFIED CLEANLY in BOTH halves
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-12-SUMMARY.md` recording:
- All 5 task outcomes (12.1 operator approved, 12.2 dispositive HTTP/isValid values, 12.3 unsuspend confirmation + smoke event counts, 12.4 drift demo dispositive comparison block, 12.5 operator approved)
- All commit hashes (3 evidence commits + 0 spec commits)
- Snapshot file counts (post-recovery 17+8, drift-test 17)
- Anti-leak audit results (zero hits across all artifacts)
- Differential table comparing Plan 06 Task 6.3+6.4 attempt vs Plan 12 Task 12.4+12.5 outcome
- Confirmation that REQ-drift-detection correction half is SATISFIED CLEANLY (cite the dispositive markers from the drift evidence file)
- Forward signal: Phase 02.2 ready for closure (recommend `/gsd-verify-phase 02.2`); Phase 3 (Radarr/Prowlarr) prerequisite finally met (both halves of D-02.1-06 + D-02.2-AUTH-REGRESSION locked shut)
- Lessons-learned bullet for the next planning cycle: behavioral test (Sonarr Test API) is mandatory in W-04 dispositives for any reconciler that touches credential-bearing fields; snapshot-only checks cannot detect this regression class by construction
</output>
