---
phase: 02.2
plan: 06
type: execute
wave: 5
depends_on:
  - 02.2-05
files_modified:
  - .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/
  - snapshots/post-phase2.2-2026-05-09/
  - snapshots/drift-test-phase2.2-2026-05-09/
autonomous: false
requirements:
  - REQ-drift-detection
must_haves:
  truths:
    - "Forced apply-mode smoke job confirms the v0.1.4 image is running and reconciles cleanly: PUT 200 (no more 400)"
    - "Smoke job logs contain the new `put_force_save_used` event for any UPDATE PUT (D-02.2-05)"
    - "Smoke job logs contain `plan_action` events as before (D-31/D-32 carry-forward)"
    - "Post-deploy snapshot diff against pre-deploy baseline shows ONLY the expected noise (rootfolder.json freeSpace, system_status.json startTime) — no spurious clears, no credentials disturbed"
    - "Drift demo runbook re-executed FULLY AUTOMATED: induce priority drift via curl PUT (no operator nudge), force smoke job, verify W-04 dispositive RESTORED_PRIORITY == ORIGINAL_PRIORITY"
    - "W-01 forensic snapshot captured BEFORE inducing drift; both before-pair and after-pair committed in arr-stack git"
    - "REQ-drift-detection correction half closes CLEANLY (no manual nudge — the differential proof against Phase 2.1 closure)"
  artifacts:
    - path: ".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-deploy-smoke-2026-05-09.log"
      provides: "Captured kubectl logs from the first v0.1.4 smoke job — proves PUT 200 + put_force_save_used event"
      contains: "put_force_save_used"
    - path: ".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-2026-05-09.log"
      provides: "Drift demo log + DISPOSITIVE COMPARISON block — proves automated drift correction"
      contains: "match (restored == original): YES"
    - path: "snapshots/post-phase2.2-2026-05-09/sonarr/"
      provides: "Post-deploy cluster state — diff against before-phase-2.2 must show no unexpected clears"
    - path: "snapshots/drift-test-phase2.2-2026-05-09/sonarr/"
      provides: "W-01 forensic snapshot pair for the drift demo — REQUIRED audit trail"
  key_links:
    - from: "Cluster CronJob `arrconf` (v0.1.4)"
      to: "Sonarr API PUT /api/v3/downloadclient/{id}?forceSave=true"
      via: "_ArrV3Client.put() injects forceSave=true on every UPDATE"
      pattern: "PUT.*api/v3/downloadclient.*forceSave"
    - from: "evidence/drift-demo-2026-05-09.log"
      to: "REQ-drift-detection acceptance (correction half)"
      via: "W-04 dispositive: RESTORED_PRIORITY == ORIGINAL_PRIORITY computed from Sonarr API, captured in DISPOSITIVE COMPARISON block — fully automated, no operator nudge"
      pattern: "match \\(restored == original\\): YES"
    - from: "Phase 2.1 W-04 dispositive (manual nudge)"
      to: "Phase 2.2 W-04 dispositive (automated)"
      via: "differential proof — same runbook, same dispositive, but Phase 2.2 omits the operator `?forceSave=true` curl nudge"
      pattern: "drift-demo.*log"
---

<objective>
Final verification — close REQ-drift-detection's correction half cleanly via fully-automated reconcile (no operator manual nudge).

Three sequential phases inside this plan:
1. **Post-deploy smoke** — force a non-drift apply-mode job, capture logs, verify the new `put_force_save_used` event fires alongside the existing `plan_action` events. Proves v0.1.4 is actually running and PUT 200 happens in the normal reconcile path.
2. **Post-deploy snapshot** — capture sonarr + qbittorrent state and diff against `snapshots/before-phase-2.2-2026-05-09/`. Confirm only expected noise (rootfolder.json freeSpace, system_status.json startTime); no credentials disturbed; tags=[1] still attached.
3. **Drift demo runbook (FULLY AUTOMATED)** — re-execute Plan 02-05 / Plan 02.1-04 drift demo, this time WITHOUT the operator-driven `curl PUT .../?forceSave=true` manual nudge. The differential against Phase 2.1's closure (which used the manual nudge) is the dispositive proof that v0.1.4 closes the correction half of REQ-drift-detection cleanly.

Sequence: W-01 forensic snapshot → induce priority drift via curl PUT (priority is the canonical safe field per A6) → force smoke job → capture log + W-04 dispositive `RESTORED_PRIORITY == ORIGINAL_PRIORITY` value-equality.

Purpose: Lock the architectural finding D-02.1-06 shut. After this plan, Phase 3 (Radarr/Prowlarr) starts with a clean REQ-drift-detection (BOTH halves: detection ✅ and correction ✅).

Output: Three evidence files committed, two snapshot directories committed, REQ-drift-detection SATISFIED in its entirety.

Wave 5 — depends on Plan 05 (v0.1.4 actually running in cluster).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/REQUIREMENTS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-04-PLAN.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-PLAN.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/client_base.py
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/reconcilers/sonarr.py
</context>

<tasks>

<task type="auto">
  <name>Task 6.1: Force post-deploy apply-mode smoke job + capture logs (PUT 200 + put_force_save_used)</name>
  <files>
    .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-deploy-smoke-2026-05-09.log
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/client_base.py (verify _ArrV3Client.put() emits put_force_save_used — Plan 02 GREEN commit)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/reconcilers/sonarr.py (W-06 — verified apply-mode events: managed_tag_found should fire since tag id=1 already exists from Phase 2.1)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py (W-06 — verified `plan_action` event)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-PLAN.md Task 5.1b (smoke-job pattern)
  </read_first>
  <action>
    Step 1 — Create evidence directory if missing:
        cd /home/moi/projets/perso/arr-stack
        mkdir -p .planning/phases/02.2-v0-1-4-forcesave-fix/evidence

    Step 2 — Force a smoke job (apply mode):
        SMOKE_JOB="arrconf-pr-v0.1.4-smoke-$(date +%s)"
        kubectl create job --from=cronjob/arrconf "$SMOKE_JOB" -n selfhost
        kubectl wait --for=condition=complete "job/$SMOKE_JOB" -n selfhost --timeout=180s

        EVIDENCE=".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-deploy-smoke-$(date +%F).log"
        kubectl logs "job/$SMOKE_JOB" -n selfhost > "$EVIDENCE"

    Step 3 — Verify the apply-mode signal AND the new put_force_save_used event:
        # The smoke is a normal reconcile (tags+credentials should already match cluster from Phase 2.1 closure).
        # Expected events:
        # - managed_tag_found (tag id=1 exists from Phase 2.1)
        # - plan_action (at least one — should be no-op or update if any drift accumulated)
        # - put_force_save_used (NEW — fires on every UPDATE PUT; if no real drift, may not fire if PlannedAction is NO_OP)

        TAG_FOUND=$(jq -c 'select(.event == "managed_tag_found")' "$EVIDENCE" 2>/dev/null | wc -l)
        TAG_CREATED=$(jq -c 'select(.event == "managed_tag_created")' "$EVIDENCE" 2>/dev/null | wc -l)
        echo "managed_tag_found: $TAG_FOUND (expected >= 1 since tag id=1 carry-forward)"
        if [ "$TAG_FOUND" -lt 1 ] && [ "$TAG_CREATED" -lt 1 ]; then
          echo "ERROR: neither managed_tag_found nor managed_tag_created event present — pipeline broken"
          exit 1
        fi

        PLAN_EVENTS=$(jq -c 'select(.event == "plan_action")' "$EVIDENCE" 2>/dev/null | wc -l)
        echo "plan_action events: $PLAN_EVENTS (expected >= 1)"
        [ "$PLAN_EVENTS" -ge 1 ] || { echo "ERROR: no plan_action events"; exit 1; }

        # Dry-run markers MUST be absent (apply mode):
        DRY_RUN_MARKERS=$(jq -c 'select(.event == "would_create_managed_tag" or .event == "dry_run_skip")' "$EVIDENCE" 2>/dev/null | wc -l)
        echo "Dry-run markers (must be 0): $DRY_RUN_MARKERS"
        [ "$DRY_RUN_MARKERS" -eq 0 ] || { echo "ERROR: dry-run path event in apply-mode logs"; exit 1; }

        # If plan_action included an UPDATE, put_force_save_used MUST also be present.
        UPDATE_EVENTS=$(jq -c 'select(.event == "plan_action" and .action == "update")' "$EVIDENCE" 2>/dev/null | wc -l)
        FORCE_SAVE_EVENTS=$(jq -c 'select(.event == "put_force_save_used")' "$EVIDENCE" 2>/dev/null | wc -l)
        echo "plan_action update events: $UPDATE_EVENTS"
        echo "put_force_save_used events: $FORCE_SAVE_EVENTS"
        if [ "$UPDATE_EVENTS" -ge 1 ] && [ "$FORCE_SAVE_EVENTS" -lt 1 ]; then
          echo "ERROR: plan_action update fired but put_force_save_used did NOT — _ArrV3Client.put() not engaged"
          exit 1
        fi
        # Note: if no UPDATE (steady state already converged), put_force_save_used may legitimately be 0.
        # This is fine — Task 6.3 induces a drift to force an UPDATE PUT and re-checks.

        # No HTTP 400 errors:
        HTTP_400=$(grep -c '"status_code":400\|HTTPStatusError.*400\|400 Bad Request' "$EVIDENCE" 2>/dev/null || echo 0)
        echo "HTTP 400 events: $HTTP_400 (expected 0)"
        [ "$HTTP_400" -eq 0 ] || { echo "ERROR: HTTP 400 in v0.1.4 smoke logs — fix did not deploy"; exit 1; }

    Step 4 — Cleanup smoke job:
        kubectl delete "job/$SMOKE_JOB" -n selfhost --ignore-not-found

    Step 5 — Commit evidence:
        cd /home/moi/projets/perso/arr-stack
        git add .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-deploy-smoke-*.log
        git commit -m "evidence(02.2): v0.1.4 post-deploy smoke — no HTTP 400, put_force_save_used emits"
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && EVIDENCE=$(ls -t .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-deploy-smoke-*.log 2>/dev/null | head -1) && test -n "$EVIDENCE" && head -1 "$EVIDENCE" | jq -e . > /dev/null && [ "$(jq -c 'select(.event == "managed_tag_found" or .event == "managed_tag_created")' "$EVIDENCE" | wc -l)" -ge 1 ] && [ "$(jq -c 'select(.event == "plan_action")' "$EVIDENCE" | wc -l)" -ge 1 ] && [ "$(jq -c 'select(.event == "would_create_managed_tag" or .event == "dry_run_skip")' "$EVIDENCE" | wc -l)" = "0" ] && [ "$(grep -c '"status_code":400\|HTTPStatusError.*400\|400 Bad Request' "$EVIDENCE" || echo 0)" = "0" ] && git log -1 --pretty=%s | grep -q 'evidence(02.2):.*post-deploy-smoke'</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-deploy-smoke-2026-05-09.log` exists, non-empty
    - First line of evidence file is valid JSON: `head -1 ... | jq -e . > /dev/null` exits 0
    - At least 1 `managed_tag_found` OR `managed_tag_created` event in evidence
    - At least 1 `plan_action` event in evidence
    - Zero `would_create_managed_tag` or `dry_run_skip` events (apply mode confirmed)
    - Zero HTTP 400 events in evidence
    - If any `plan_action action=update` events fire, at least 1 `put_force_save_used` event also fires (the new event for D-02.2-05)
    - Commit recorded with `evidence(02.2):` prefix
  </acceptance_criteria>
  <done>v0.1.4 smoke job ran in apply mode; logs captured; no HTTP 400 (vs Phase 2.1 partial which had them); pipeline events match expected; ready for Task 6.2 snapshot diff.</done>
</task>

<task type="auto">
  <name>Task 6.2: Capture post-deploy snapshot + diff vs pre-deploy baseline (no spurious clears)</name>
  <files>
    snapshots/post-phase2.2-2026-05-09/
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/snapshots/before-phase-2.2-2026-05-09/sonarr/downloadclient.json (Plan 01 baseline — used as ORIGINAL anchor)
    - /home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-04-SUMMARY.md "Snapshot diff: only 2 noise files" (expected noise pattern)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md "Worktree isolation can leak files"
  </read_first>
  <action>
    Step 1 — Establish port-forwards + env:
        if [ -z "$SONARR_API_KEY" ]; then
          export SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"
        fi
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        SONARR_PF=$!
        kubectl -n selfhost port-forward svc/qbittorrent 8080:8080 &
        QBIT_PF=$!
        sleep 3

    Step 2 — Capture post-deploy snapshot:
        cd /home/moi/projets/perso/arr-stack
        POST_DIR="snapshots/post-phase2.2-$(date +%F)"
        tools/snapshot/snapshot.sh --apps sonarr,qbittorrent --output "$POST_DIR/"

    Step 3 — Apply same redaction workaround as Plan 01 (D-02.1-01, D-02.1-02 carry-forward):
        if [ -f "$POST_DIR/sonarr/config_host.json" ]; then
          jq '.apiKey = "***REDACTED***" | .password = "***REDACTED***" | .username = "***REDACTED***"' \
            "$POST_DIR/sonarr/config_host.json" > "$POST_DIR/sonarr/config_host.json.tmp" \
            && mv "$POST_DIR/sonarr/config_host.json.tmp" "$POST_DIR/sonarr/config_host.json"
        fi
        if [ -f "$POST_DIR/qbittorrent/torrents_info.json" ]; then
          jq 'walk(if type == "string" then gsub("/announce/[a-fA-F0-9]{16,}"; "/announce/***REDACTED-PASSKEY***") else . end)' \
            "$POST_DIR/qbittorrent/torrents_info.json" > "$POST_DIR/qbittorrent/torrents_info.json.tmp" \
            && mv "$POST_DIR/qbittorrent/torrents_info.json.tmp" "$POST_DIR/qbittorrent/torrents_info.json"
        fi

    Step 4 — Anti-leak audit:
        ! grep -rE '/announce/[a-fA-F0-9]{16,}' "$POST_DIR/" || { echo "LEAK"; exit 1; }
        ! grep -rE '(apiKey|api_key|API_KEY)" *: *"[^*]' "$POST_DIR/" || { echo "LEAK"; exit 1; }

    Step 5 — Cleanup port-forwards:
        kill $SONARR_PF $QBIT_PF 2>/dev/null || true

    Step 6 — Diff against pre-deploy baseline (only 2 noise files expected):
        BEFORE_DIR="snapshots/before-phase-2.2-2026-05-09"
        DIFF_OUT="/tmp/snapshot-diff-02.2.txt"
        diff -rq "$BEFORE_DIR/sonarr/" "$POST_DIR/sonarr/" > "$DIFF_OUT" 2>&1 || true

        # Expected diffs: rootfolder.json (freeSpace fluctuates), system_status.json (startTime)
        # Anything else == regression.
        UNEXPECTED=$(grep -v -E '(rootfolder|system_status|qualityprofile|languageprofile)\.json' "$DIFF_OUT" | grep -v '^$' || true)
        if [ -n "$UNEXPECTED" ]; then
          echo "UNEXPECTED DIFFS DETECTED:"
          echo "$UNEXPECTED"
          # Don't fail outright — surface for manual review. Capture in evidence.
        fi

    Step 7 — Confirm credentials NOT disturbed (the dispositive Phase 2.1 carry-forward check):
        # downloadclient.json[0].fields password value MUST be the same in both snapshots
        # (either both ***REDACTED*** or both ********; Sonarr's GET-side mask is consistent)
        BEFORE_PWD=$(jq -c '.[0].fields[]? | select(.name=="password") | .value' "$BEFORE_DIR/sonarr/downloadclient.json")
        AFTER_PWD=$(jq -c '.[0].fields[]? | select(.name=="password") | .value' "$POST_DIR/sonarr/downloadclient.json")
        echo "Before password value: $BEFORE_PWD"
        echo "After password value: $AFTER_PWD"
        [ "$BEFORE_PWD" = "$AFTER_PWD" ] || { echo "ERROR: password value changed between snapshots — possible regression"; exit 1; }

        # Tags must still be [1] (or non-empty)
        AFTER_TAGS=$(jq -c '.[0].tags' "$POST_DIR/sonarr/downloadclient.json")
        echo "After tags: $AFTER_TAGS"
        echo "$AFTER_TAGS" | jq -e 'length > 0' > /dev/null || { echo "ERROR: tags array became empty"; exit 1; }

    Step 8 — Commit:
        git add snapshots/post-phase2.2-*/
        git commit -m "snapshot(02.2): post-deploy capture confirming v0.1.4 + forceSave on UPDATE"
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && POST_DIR=$(ls -d snapshots/post-phase2.2-* 2>/dev/null | head -1) && test -d "$POST_DIR/sonarr" && test -d "$POST_DIR/qbittorrent" && [ "$(find $POST_DIR/sonarr -type f -name '*.json' | wc -l)" -ge 5 ] && jq -e '.[0].tags | length > 0' "$POST_DIR/sonarr/downloadclient.json" > /dev/null && [ "$(jq -c '.[0].fields[]? | select(.name=="password") | .value' "$POST_DIR/sonarr/downloadclient.json")" = "$(jq -c '.[0].fields[]? | select(.name=="password") | .value' snapshots/before-phase-2.2-2026-05-09/sonarr/downloadclient.json)" ] && ! grep -rE '/announce/[a-fA-F0-9]{16,}' "$POST_DIR/" && git log -1 --pretty=%s | grep -q 'snapshot(02.2):.*post-deploy'</automated>
  </verify>
  <acceptance_criteria>
    - `snapshots/post-phase2.2-2026-05-09/sonarr/` directory exists with >= 5 JSON files
    - `snapshots/post-phase2.2-2026-05-09/qbittorrent/` directory exists
    - `jq -e '.[0].tags | length > 0' snapshots/post-phase2.2-2026-05-09/sonarr/downloadclient.json` exits 0 (tag still attached)
    - Password field value is IDENTICAL between before and after snapshots (no spurious clear)
    - No leaked tracker passkeys, no raw API keys
    - Git commit recorded with `snapshot(02.2):` prefix
  </acceptance_criteria>
  <done>Post-deploy snapshot captured + redacted + audited; diff confirms only expected noise; credentials + tags intact; ready for Task 6.3 drift demo dispositive.</done>
</task>

<task type="auto">
  <name>Task 6.3: Drift demo runbook FULLY AUTOMATED — induce priority drift, force smoke, verify W-04 dispositive (REQ-drift-detection correction half closure)</name>
  <files>
    .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-2026-05-09.log
    snapshots/drift-test-phase2.2-2026-05-09/
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-PLAN.md Task 5.2 (full runbook — REUSED verbatim except: NO operator manual `?forceSave=true` curl nudge)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-04-PLAN.md (Phase 2.1 closure precedent — used the manual nudge; Phase 2.2 omits it)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md "W-01 forensic snapshot + W-04 dispositive value-equality (drift demo runbook)"
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md "Decisions §D-02.2-03 Verification Depth" + "Specifics §W-04 dispositive shape"
    - /home/moi/projets/perso/arr-stack/snapshots/before-phase-2.2-2026-05-09/sonarr/downloadclient.json (ORIGINAL_PRIORITY anchor)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/client_base.py (verify _ArrV3Client.put() injects forceSave)
  </read_first>
  <action>
    Step 1 — Establish access:
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        PF_PID=$!
        sleep 2
        if [ -z "$SONARR_API_KEY" ]; then
          export SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"
        fi

    Step 2 — Capture pre-drift state (ORIGINAL_PRIORITY anchor for W-04):
        TMPDIR=$(mktemp -d)
        DC_ID=$(curl -s http://localhost:8989/api/v3/downloadclient -H "X-Api-Key: $SONARR_API_KEY" | jq '.[0].id')
        curl -s "http://localhost:8989/api/v3/downloadclient/$DC_ID" -H "X-Api-Key: $SONARR_API_KEY" \
          | jq '.' > "$TMPDIR/dc-before.json"

        ORIGINAL_PRIORITY=$(jq -r '.priority' "$TMPDIR/dc-before.json")
        echo "Original priority: $ORIGINAL_PRIORITY"

    Step 3 — W-01 forensic snapshot BEFORE inducing drift (REQUIRED per CONTEXT.md):
        cd /home/moi/projets/perso/arr-stack
        DRIFT_SNAPSHOT="snapshots/drift-test-phase2.2-$(date +%F)"
        tools/snapshot/snapshot.sh --apps sonarr --output "$DRIFT_SNAPSHOT/"

        # Apply the same redaction (config_host)
        if [ -f "$DRIFT_SNAPSHOT/sonarr/config_host.json" ]; then
          jq '.apiKey = "***REDACTED***" | .password = "***REDACTED***" | .username = "***REDACTED***"' \
            "$DRIFT_SNAPSHOT/sonarr/config_host.json" > "$DRIFT_SNAPSHOT/sonarr/config_host.json.tmp" \
            && mv "$DRIFT_SNAPSHOT/sonarr/config_host.json.tmp" "$DRIFT_SNAPSHOT/sonarr/config_host.json"
        fi

    Step 4 — Induce drift via curl PUT (priority is the canonical safe field per A6):
        DRIFTED_PRIORITY=5
        if [ "$ORIGINAL_PRIORITY" = "5" ]; then DRIFTED_PRIORITY=10; fi   # avoid no-op

        jq --argjson dp "$DRIFTED_PRIORITY" '.priority = $dp' "$TMPDIR/dc-before.json" > "$TMPDIR/dc-drifted.json"

        # CRITICAL: induce drift WITH ?forceSave=true (operator side — bypasses Sonarr pre-save)
        # This is the operator's curl, NOT the test of arrconf — arrconf's reconcile in Step 5 must NOT need a manual ?forceSave nudge.
        curl -s -X PUT "http://localhost:8989/api/v3/downloadclient/$DC_ID?forceSave=true" \
          -H "X-Api-Key: $SONARR_API_KEY" \
          -H "Content-Type: application/json" \
          -d @"$TMPDIR/dc-drifted.json" \
          -o /dev/null -w "HTTP %{http_code}\n"

        # Confirm drift landed:
        DRIFTED_OBSERVED=$(curl -s "http://localhost:8989/api/v3/downloadclient/$DC_ID" -H "X-Api-Key: $SONARR_API_KEY" | jq '.priority')
        echo "Drifted priority observed in Sonarr: $DRIFTED_OBSERVED"
        [ "$DRIFTED_OBSERVED" = "$DRIFTED_PRIORITY" ] || { echo "ERROR: drift did not land"; exit 1; }

    Step 5 — Force a cron Job to trigger arrconf reconcile (this is the v0.1.4 fully-automated test):
        DRIFT_JOB="arrconf-drift-demo-$(date +%s)"
        kubectl create job --from=cronjob/arrconf "$DRIFT_JOB" -n selfhost
        kubectl wait --for=condition=complete "job/$DRIFT_JOB" -n selfhost --timeout=180s

        EVIDENCE=".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-$(date +%F).log"
        kubectl logs "job/$DRIFT_JOB" -n selfhost > "$EVIDENCE"

    Step 6 — Verify the logs show: detection (plan_action update) + the new put_force_save_used event:
        UPDATE_PLANS=$(jq -c 'select(.event == "plan_action" and .action == "update")' "$EVIDENCE" | wc -l)
        echo "plan_action update events: $UPDATE_PLANS (expected >= 1)"
        [ "$UPDATE_PLANS" -ge 1 ] || { echo "ERROR: no plan_action update event"; exit 1; }

        # The dispositive Phase 02.2 contract: put_force_save_used MUST fire when an UPDATE PUT happens.
        FORCE_SAVE_EVENTS=$(jq -c 'select(.event == "put_force_save_used")' "$EVIDENCE" | wc -l)
        echo "put_force_save_used events: $FORCE_SAVE_EVENTS (expected >= 1 — DISPOSITIVE for D-02.2-05)"
        [ "$FORCE_SAVE_EVENTS" -ge 1 ] || { echo "ERROR: put_force_save_used did NOT fire — _ArrV3Client.put() not engaged"; exit 1; }

        # No HTTP 400:
        HTTP_400=$(grep -c '"status_code":400\|HTTPStatusError.*400\|400 Bad Request' "$EVIDENCE" 2>/dev/null || echo 0)
        echo "HTTP 400 events: $HTTP_400 (expected 0 — DISPOSITIVE that v0.1.4 fix works)"
        [ "$HTTP_400" -eq 0 ] || { echo "ERROR: HTTP 400 in drift-demo logs — fix did NOT close the correction half"; exit 1; }

    Step 7 — W-04 DISPOSITIVE: Sonarr value reverted to ORIGINAL_PRIORITY (FULLY AUTOMATED — no operator nudge):
        sleep 3   # give Sonarr a moment after the smoke job finishes
        RESTORED_PRIORITY=$(curl -s "http://localhost:8989/api/v3/downloadclient/$DC_ID" -H "X-Api-Key: $SONARR_API_KEY" | jq -r '.priority')
        echo "Post-reconcile priority observed in Sonarr: $RESTORED_PRIORITY"

        if [ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ]; then
          echo "REQ-drift-detection correction half SATISFIED — Sonarr value reverted from $DRIFTED_PRIORITY back to $ORIGINAL_PRIORITY (FULLY AUTOMATED, no operator nudge)"
        else
          echo "ERROR: drift was NOT corrected by arrconf alone — REQ-drift-detection FAILS"
          echo "  Original: $ORIGINAL_PRIORITY"
          echo "  Drifted:  $DRIFTED_PRIORITY"
          echo "  Restored: $RESTORED_PRIORITY"
          exit 1
        fi

        # Persist DISPOSITIVE COMPARISON block for audit (mirrors Phase 2.1 format):
        cat >> "$EVIDENCE" <<EOF

# === DRIFT-DEMO DISPOSITIVE COMPARISON (Phase 02.2 — FULLY AUTOMATED) ===
# original_priority: $ORIGINAL_PRIORITY
# drifted_priority:  $DRIFTED_PRIORITY
# restored_priority: $RESTORED_PRIORITY
# match (restored == original): YES
# manual_nudge_used: NO (vs Phase 2.1 which used a manual ?forceSave=true curl)
# put_force_save_used events fired: $FORCE_SAVE_EVENTS
# plan_action update events: $UPDATE_PLANS
# HTTP 400 events: $HTTP_400
EOF

    Step 8 — Cleanup:
        kubectl delete "job/$DRIFT_JOB" -n selfhost --ignore-not-found
        kill $PF_PID 2>/dev/null || true
        rm -rf "$TMPDIR"

    Step 9 — Commit drift demo evidence + W-01 snapshot:
        cd /home/moi/projets/perso/arr-stack
        git add .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-*.log \
                snapshots/drift-test-phase2.2-*/
        git commit -m "evidence(02.2): drift demo FULLY AUTOMATED — REQ-drift-detection correction half SATISFIED (W-01 snapshot, W-04 dispositive, no operator nudge)"
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && EVIDENCE=$(ls -t .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-*.log 2>/dev/null | head -1) && test -n "$EVIDENCE" && head -1 "$EVIDENCE" | jq -e . > /dev/null && [ "$(jq -c 'select(.event == "plan_action" and .action == "update")' "$EVIDENCE" | wc -l)" -ge 1 ] && [ "$(jq -c 'select(.event == "put_force_save_used")' "$EVIDENCE" | wc -l)" -ge 1 ] && [ "$(grep -c '"status_code":400\|HTTPStatusError.*400\|400 Bad Request' "$EVIDENCE" || echo 0)" = "0" ] && grep -q 'match (restored == original): YES' "$EVIDENCE" && grep -q 'manual_nudge_used: NO' "$EVIDENCE" && DRIFT_SNAP=$(ls -d snapshots/drift-test-phase2.2-* 2>/dev/null | head -1) && test -d "$DRIFT_SNAP/sonarr" && [ "$(find $DRIFT_SNAP/sonarr -type f -name '*.json' | wc -l)" -ge 1 ] && git log -1 --pretty=%s | grep -q 'evidence(02.2):.*FULLY AUTOMATED'</automated>
  </verify>
  <acceptance_criteria>
    - Evidence file `drift-demo-2026-05-09.log` exists, non-empty, valid JSON Lines on first line
    - **W-06 dispositive**: `jq -c 'select(.event == "plan_action" and .action == "update")' .../drift-demo-*.log | wc -l` >= 1
    - **D-02.2-05 dispositive (NEW)**: `jq -c 'select(.event == "put_force_save_used")' .../drift-demo-*.log | wc -l` >= 1
    - **D-02.1-06 closure dispositive**: ZERO HTTP 400 events in drift-demo log
    - **W-04 dispositive**: Evidence file contains literal `match (restored == original): YES`
    - **Differential proof**: Evidence file contains literal `manual_nudge_used: NO` (vs Phase 2.1 which used the manual nudge)
    - **W-01 REQUIRED**: `snapshots/drift-test-phase2.2-2026-05-09/sonarr/` exists with >= 1 JSON file
    - All evidence + snapshot committed in arr-stack git history with `evidence(02.2):` prefix
  </acceptance_criteria>
  <done>Drift induced via curl PUT; arrconf reconcile job fired; v0.1.4 reconcile detected drift (`plan_action action=update`) AND emitted `put_force_save_used` AND completed without HTTP 400 (vs Phase 2.1 which would have failed); Sonarr field restored to YAML value (W-04 dispositive `RESTORED_PRIORITY == ORIGINAL_PRIORITY`); W-01 forensic snapshot committed; REQ-drift-detection correction half SATISFIED CLEANLY (fully automated, no manual nudge); Phase 2.2 closure ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6.4: Operator confirmation — REQ-drift-detection closure + suspend CronJob if any anomaly</name>
  <files>
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-2026-05-09.log
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-deploy-smoke-2026-05-09.log
    - /home/moi/projets/perso/arr-stack/snapshots/post-phase2.2-2026-05-09/sonarr/downloadclient.json
    - /home/moi/projets/perso/arr-stack/snapshots/drift-test-phase2.2-2026-05-09/sonarr/downloadclient.json
  </read_first>
  <action>
    This is a checkpoint:human-verify task. The automated tasks (6.1, 6.2, 6.3) have completed and
    produced evidence files + snapshots. The operator MUST visually confirm the cluster state and
    decide whether Phase 02.2 closes cleanly or requires a hotfix-via-PR rollback.

    Operator manual steps (executed by human, NOT by Claude):

    Step 1 — Open Sonarr UI in browser:
        Settings -> Download Clients -> qBittorrent
        Confirm:
        - priority value matches the YAML-declared value (the original ORIGINAL_PRIORITY from before drift demo)
        - username field is populated correctly
        - password field is masked (shows ********) but functional
        - Click Sonarr's "Test" button on the qBittorrent download client
        - Expected: Test succeeds (proves credentials weren't disturbed by v0.1.4 deployment)

    Step 2 — Re-confirm cluster state from terminal:
        argocd app get arrconf | grep -E "(Status|Health|Sync)"
        # Expected: Health: Healthy, Sync: Synced

        kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}{"\n"}'
        # Expected: false (or empty) — CronJob is unsuspended

        kubectl get pods -n selfhost -l job-name=arrconf-drift-demo-* 2>/dev/null
        # Expected: empty (job was deleted) or Completed

    Step 3 — Spot-check evidence files:
        cd /home/moi/projets/perso/arr-stack
        jq '.event' .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-*.log 2>/dev/null | sort | uniq -c
        # Expected event types include: managed_tag_found, plan_action, merge_field_preserved, put_force_save_used

        grep -A 10 'DRIFT-DEMO DISPOSITIVE COMPARISON' .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-*.log
        # Expected: 'match (restored == original): YES' AND 'manual_nudge_used: NO'

    Step 4 — Decision branch:

    A. If all green (no anomaly):
        Type "approved: REQ-drift-detection correction half closed cleanly, Sonarr UI looks right, ArgoCD healthy"
        Phase 02.2 ready for orchestrator-side closure (STATE.md / ROADMAP.md update + verify-phase).

    B. If anomaly detected:
        Step 4.B.1 — Suspend the CronJob to freeze cluster state:
            kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":true}}'

        Step 4.B.2 — Open hotfix PR per Phase 2.1 hotfix-via-PR pattern (NEVER `git revert` on a merged PR):
            # See 02.1-LEARNINGS.md "Hotfix-via-PR pattern when a deployed change reveals a defect"
            # Smallest possible reversion: bump my-kluster image.tag back to "0.1.3" while debugging.

        Step 4.B.3 — Resume signal: describe the anomaly + the suspend action taken; orchestrator escalates.

    Resume to Claude: type "approved" or describe the anomaly + suspend action.
  </action>
  <what-built>
    Tasks 6.1, 6.2, 6.3 executed automatically:
    - Post-deploy smoke job ran on v0.1.4: PUT 200 (no HTTP 400), put_force_save_used event present
    - Post-deploy snapshot: credentials + tag intact, no spurious clears
    - Drift demo: induced priority drift; arrconf detected + corrected via fully-automated reconcile (no operator nudge); W-04 dispositive `RESTORED_PRIORITY == ORIGINAL_PRIORITY` confirmed

    What's still needed from a human:
    - Open the Sonarr UI and visually confirm the qBit downloadclient priority looks right (matches YAML-declared value)
    - Spot-check the evidence files for any unexpected JSON noise
    - Spot-check `argocd app get arrconf` for Healthy + Synced
    - If any anomaly: suspend the CronJob (`kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":true}}'`) and open a hotfix-PR per Phase 2.1 hotfix-via-PR pattern (NEVER `git revert` on a merged PR)
    - If all clean: signal that Phase 02.2 is ready to close
  </what-built>
  <how-to-verify>
    1. Open Sonarr UI in browser:
       https://sonarr.tom333.fr (or however the operator accesses it)
       Settings → Download Clients → qBittorrent
       Confirm: priority matches YAML, username present, password masked but functional (Sonarr's "Test" button should succeed — that's the proof the credentials weren't disturbed by v0.1.4)

    2. Re-confirm cluster state:
       argocd app get arrconf | grep -E "(Status|Health|Sync)"
       # Expected: Health: Healthy, Sync: Synced

       kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}{"\n"}'
       # Expected: false (or empty)

       kubectl get pods -n selfhost -l job-name=arrconf-drift-demo-* 2>/dev/null
       # Expected: empty (job was deleted) or Completed

    3. Spot-check evidence files:
       cd /home/moi/projets/perso/arr-stack
       jq '.event' .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/drift-demo-*.log 2>/dev/null | sort | uniq -c
       # Expected event types include: managed_tag_found, plan_action, merge_field_preserved, put_force_save_used

    4. If anomaly:
       kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":true}}'
       # Then open a hotfix PR per the Phase 2.1 hotfix-via-PR pattern

    5. If clean:
       Type "approved" to signal closure of Phase 02.2.
  </how-to-verify>
  <acceptance_criteria>
    - Operator visually confirms Sonarr UI shows the qBit downloadclient with priority = ORIGINAL_PRIORITY value (matching YAML)
    - Operator confirms ArgoCD arrconf Application is Synced + Healthy
    - Operator confirms CronJob is unsuspended (or suspends + escalates if anomaly)
    - Operator types "approved" or describes the anomaly
  </acceptance_criteria>
  <verify>
    <automated>argocd app get arrconf -o json 2>/dev/null | jq -e '.status.sync.status == "Synced" and .status.health.status == "Healthy"' > /dev/null && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')" != "true" ]</automated>
  </verify>
  <resume-signal>Type "approved: REQ-drift-detection correction half closed cleanly, Sonarr UI looks right, ArgoCD healthy" OR describe the anomaly + suspend action taken.</resume-signal>
  <done>Operator confirms cluster state; if approved, Phase 02.2 ready for orchestrator-side closure (STATE.md / ROADMAP.md update + verify-phase).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation -> Sonarr API (curl PUT to induce drift) | Trusted local action; operator-side `?forceSave=true` is acceptable BECAUSE it's the test driver, NOT the test of arrconf. arrconf's reconcile is the system under test |
| Cluster CronJob (v0.1.4) -> Sonarr API PUT | The system under test — arrconf must perform the corrective PUT 200 WITHOUT operator intervention |
| Evidence files -> arr-stack git history | Permanent audit trail for REQ-drift-detection closure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-17 | Tampering (T-drift-test-stimulus) | Operator-side ?forceSave=true on Step 4 induction PUT could mask a real arrconf failure if the system under test still depended on operator nudge | mitigate | Step 4 nudge applies ONLY to drift induction, NOT to arrconf reconcile. The test of arrconf is Step 5+ (reconcile job runs without any operator interaction). Step 6 dispositive: `jq put_force_save_used >= 1 AND HTTP 400 == 0` — proves arrconf's PUT alone closed the correction half |
| T-02.2-18 | Repudiation | Without DISPOSITIVE COMPARISON block, the manual_nudge_used differential vs Phase 2.1 is not preserved | mitigate | Step 7 appends a structured block to evidence file that explicitly records `manual_nudge_used: NO` and the original/drifted/restored priority triple. Auditable forever in the committed log |
| T-02.2-19 | Information Disclosure | Evidence file content | accept | Phase 1 SecretStr masking ensures structlog never emits API key values; the JSON log lines contain event names + diff fields + ids, never credentials. Same as Phase 2 + Phase 2.1 evidence files |
| T-02.2-20 | Denial of Service | Drift demo perturbs cluster state if arrconf fails to reconcile | mitigate | Step 4 uses `priority` (canonical safe field per A6 — no functional impact); Task 6.4 checkpoint allows operator to suspend CronJob and open hotfix PR if anomaly. Phase 2.1 hotfix-via-PR pattern is the safety net |
| T-02.2-21 | Elevation of Privilege (W-04 false positive) | Regex substring matching could falsely accept a benign log event | mitigate | Step 7 W-04 check is a value-equality assertion (`[ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ]`) on actual Sonarr API field values, not regex on logs. Identical mitigation pattern to Phase 2.1 |
</threat_model>

<verification>
End-of-plan gate (Plan 06 complete when all pass):
1. Post-deploy smoke job ran on v0.1.4 with no HTTP 400 (Task 6.1)
2. Post-deploy snapshot diff confirms only expected noise; credentials + tags intact (Task 6.2)
3. Drift demo runbook fully automated; W-04 dispositive RESTORED == ORIGINAL captured in DISPOSITIVE COMPARISON block; put_force_save_used event fires (Task 6.3)
4. W-01 forensic snapshot pair committed (Task 6.3)
5. Operator visually confirms Sonarr UI + cluster health (Task 6.4)
6. REQ-drift-detection correction half closes CLEANLY (no manual nudge — differential vs Phase 2.1)
</verification>

<success_criteria>
- D-02.2-03 (Verification depth: full drift demo dispositive) SATISFIED
- D-02.2-05 (put_force_save_used event observable in cluster) SATISFIED
- W-01 forensic snapshot REQUIRED (drift-test-phase2.2-<date>/) SATISFIED
- W-04 dispositive value-equality SATISFIED (FULLY AUTOMATED — closes Phase 2.1's open half)
- D-02.1-06 architectural finding LOCKED SHUT
- REQ-drift-detection (correction half) CLOSED CLEANLY
- Phase 3 (Radarr/Prowlarr) prerequisite satisfied — automated drift correction proven for *arr v3
</success_criteria>

<output>
After completion, create `/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-06-SUMMARY.md` documenting:
- Smoke job ID + completion time
- Post-deploy snapshot diff summary (expected noise only)
- Drift demo: ORIGINAL_PRIORITY, DRIFTED_PRIORITY, RESTORED_PRIORITY
- Cited >= 2 JSON log lines from drift-demo-2026-05-09.log demonstrating put_force_save_used + plan_action update
- Cited DISPOSITIVE COMPARISON block (W-04 + manual_nudge_used: NO differential)
- W-01 drift-test snapshot file count
- Phase 02.2 ROADMAP success status (REQ-drift-detection correction half SATISFIED)
- Differential vs Phase 2.1 closure: Phase 2.1 used operator manual nudge; Phase 2.2 closes via fully-automated reconcile
- Recommended next step: `/gsd-verify-phase 02.2` to formalize phase completion + STATE.md / ROADMAP.md update
</output>
