---
phase: 02.2
plan: 03
type: execute
wave: 2
depends_on:
  - 02.2-01
files_modified:
  - spec.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "ADR-8 added to spec.md §11 documenting the trusted-controller stance"
    - "ADR-8 prose is in French (project convention — ADRs 1-7 are all French)"
    - "Load-bearing claim present: 'arrconf is a trusted controller — bypasses *arr UI-grade pre-save validation via ?forceSave=true on UPDATE PUTs'"
    - "Conséquences section documents qBittorrent (Phase 5) + Jellyfin (Phase 7) scope-out"
    - "Alternatives rejetées section captures Option B (conditional on body) + Option C (reconciler layer override)"
    - "ADR-1 through ADR-7 are NOT renumbered; ADR-8 appends after ADR-7"
  artifacts:
    - path: "spec.md"
      provides: "ADR-8 entry in §11 documenting the architectural stance for Phase 3+ contributors"
      contains: "ADR-8"
  key_links:
    - from: "spec.md §11 ADR-8"
      to: "tools/arrconf/arrconf/client_base.py:_ArrV3Client.put()"
      via: "trusted-controller decision realized in code (Plan 02) and documented in spec (this plan)"
      pattern: "ADR-8.*trusted controller"
---

<objective>
Add ADR-8 to `spec.md` §11 documenting the architectural stance: "arrconf is a trusted controller — bypasses *arr UI-grade pre-save validation via `?forceSave=true` on UPDATE PUTs."

Captures the load-bearing constraint for Phase 3+ contributors. Phase 3 (Radarr/Prowlarr inheriting from `_ArrV3Client`), Phase 5 (qBittorrent — explicitly out of scope for forceSave), Phase 6 (Seerr — TBD), Phase 7 (Jellyfin — TBD) all need to know this stance is in effect for *arr v3 PUTs.

The ADR explains WHY the bypass is safe in arrconf's context: (a) arrconf's own `diff` already determined a real change is needed; (b) the merged PUT body preserves cluster's stored credentials by design (D-31/D-32 contract); (c) arrconf is the trusted controller — Sonarr's UI-grade validation is meant for human-driven misconfigs.

Purpose: Permanent architectural record. The ADR is the durable artifact; the README addition is deferred to a docs-pass phase per CONTEXT.md "Deferred §Whether to ship v0.1.4 docs change".

Output: spec.md updated with new ADR-8 section appended after ADR-7 (line 851-878), preserving the existing format (Contexte / Décision / Raisons / Conséquences / Alternatives rejetées). Single git commit.

This plan runs in Wave 2 in parallel with Plan 02 (different file: spec.md vs client_base.py). It depends on Plan 01 (snapshot baseline) only for ADR-6 phase ordering, not for any data dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/deferred-items.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Append ADR-8 to spec.md §11 documenting trusted-controller stance</name>
  <files>
    spec.md
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/spec.md (lines 740-880 — full §11 ADR list ADR-1..ADR-7, format template)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-PATTERNS.md §6 (full ADR-8 French-prose draft to adopt verbatim per planner's call)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md "Decisions §D-02.2-04" (load-bearing claim + 3 reasons)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/deferred-items.md §D-02.1-06 (the architectural finding being documented)
  </read_first>
  <action>
    Step 1 — Locate the insertion point. ADR-7 ends at line 878 with `**Alternatives rejetées** :` block. Line 879 is `---`. Line 881 begins `## 12. Références`.

    Insert ADR-8 BETWEEN ADR-7's `**Alternatives rejetées**` block and the `---` separator, OR replace the `---` and re-add it after ADR-8. Recommended: append AFTER line 878 and BEFORE line 879 (or 880).

    Step 2 — Write the ADR-8 prose (verbatim from PATTERNS.md §6, in French per project convention — ADRs 1-7 are all French):

    ```markdown
    ### ADR-8 — arrconf is a trusted controller — bypasses *arr UI-grade pre-save validation

    **Contexte** : Sonarr (et par extension Radarr/Prowlarr en *arr v3+) effectue une pré-validation côté serveur sur tout PUT modifiant une ressource avec credentials (download_client.password, indexer.apiKey, etc.). Cette pré-validation est conçue pour rattraper les misconfigs UI : elle re-authentifie contre le service externe (qBittorrent, indexer) en utilisant la valeur littérale du champ. Côté arrconf, le helper `merge_fields_for_put` (Phase 2.1, D-31) préserve le mask `********` pour les champs `privacy=password` parce que arrconf YAML ne porte jamais de vrai secret (CLAUDE.md "Variables d'environnement"). Résultat : le PUT atomique d'arrconf échoue en 400 dès qu'un champ top-level change vraiment, parce que Sonarr essaie d'auth qBit avec `********` comme mot de passe.

    **Décision** : tout client *arr v3 d'arrconf (Sonarr, Radarr, Prowlarr) envoie systématiquement `?forceSave=true` sur les PUT UPDATE. Cela demande à Sonarr de skipper la pré-validation et d'écrire la ressource telle quelle. arrconf est un **trusted controller** — le scénario que la pré-validation protège (humain qui se trompe via UI) ne s'applique pas. L'implémentation vit au layer HTTP client (`_ArrV3Client.put()` dans `tools/arrconf/arrconf/client_base.py`) — Phase 3's `RadarrClient` et `ProwlarrClient` héritent par construction.

    **Raisons** :
    - arrconf a déjà décidé qu'un changement réel est nécessaire via son propre `diff` (idempotence golden rule, CLAUDE.md)
    - Le body PUT est construit pour préserver les credentials du cluster par design (D-31/D-32 — value-based merge contract de Phase 2.1)
    - Le pre-save validation Sonarr est une pré-validation UX pour utilisateurs humains ; arrconf n'est pas un humain
    - Sans `forceSave=true`, la moitié correction de REQ-drift-detection est inatteignable sans intervention opérateur manuelle (constaté Phase 2.1 W-04 dispositive — D-02.1-06)
    - Centraliser au layer HTTP client (D-02.2-02) plutôt qu'au layer reconciler évite le mode d'échec "j'ai oublié le kwarg" pour Phase 3+

    **Conséquences** :
    - Un nouveau log event `put_force_save_used` (event=info, payload `{path, id}`) est émis à chaque UPDATE PUT — auditable côté cluster JSON logs aux côtés de `merge_field_preserved` et `plan_action`
    - Phase 3 Radarr/Prowlarr héritent par construction du comportement (intermediate class `_ArrV3Client`) — pas de discipline contributeur
    - qBittorrent (Phase 5) et Jellyfin (Phase 7) ont des sémantiques PUT différentes ; ils restent **OUT OF SCOPE** de `forceSave` (leur classe parent reste `ArrApiClient` directement, pas `_ArrV3Client`)
    - Si Sonarr introduit un jour un check pre-save vraiment utile, arrconf le skippera. Trade-off acceptable : les checks utiles seraient une régression upstream, pas une feature ; on les détectera par la perte d'idempotence (qui resterait visible dans les logs `plan_action`)
    - Le trigger est l'action (UPDATE), pas le contenu du body — ADD (POST) et DELETE ne portent pas `forceSave`

    **Alternatives rejetées** :
    - **Trigger conditionnel sur mask dans body** (Option B de D-02.1-06) : ajoute un body scan + maintenance d'un alphabet de mask Sonarr (`********`, `***REDACTED***`, autres ?) — coût de maintenance inutile pour un gain de sécurité négligeable (la pré-validation étant inutile dans le contexte arrconf de toute façon)
    - **Trigger conditionnel sur événements `merge_field_preserved` émis** : couple le layer HTTP au layer merge — inversion de responsabilité
    - **Override au layer reconciler `_execute`** : crée une discipline par-reconciler ("n'oublie pas `params={"forceSave": "true"}`") — exactement le mode d'échec qu'on veut éviter pour Phase 3
    - **Class flag `force_save_on_put: bool`** sur `ArrApiClient` (Option a) : plus court mais inverse le défaut — qBit/Jellyfin doivent se rappeler de remettre `False`. Le sous-typage explicite (`_ArrV3Client`) est plus auto-documenté pour Phase 5/7 contributeurs
    ```

    Step 3 — Use the Edit tool (NOT heredoc/sed) to insert this block into spec.md immediately after ADR-7's "Alternatives rejetées" section (line 878) and before the `---` line. If the `---` separator is on line 879, insert ADR-8 prose followed by a blank line BEFORE the `---`. The expected file structure after edit:
        ...
        878: - **Multi-instance** ... (ADR-7 last line)
        879: (blank)
        880: ### ADR-8 — arrconf is a trusted controller ...
        ... (ADR-8 body, ~30 lines)
        ... (blank)
        ... ---
        ... (blank)
        ... ## 12. Références

    Step 4 — Verify the edit:
        # Confirm ADR-8 header exists
        grep -c "^### ADR-8" /home/moi/projets/perso/arr-stack/spec.md
        # Expected: 1

        # Confirm ADRs 1-7 still exist (not renumbered)
        for n in 1 2 3 4 5 6 7; do
          grep -q "^### ADR-$n " /home/moi/projets/perso/arr-stack/spec.md || { echo "REGRESSION: ADR-$n missing"; exit 1; }
        done

        # Confirm load-bearing claim present
        grep -q "trusted controller" /home/moi/projets/perso/arr-stack/spec.md
        grep -q "forceSave=true" /home/moi/projets/perso/arr-stack/spec.md

        # Confirm Conséquences mentions qBit + Jellyfin scope-out
        grep -A 20 "^### ADR-8" /home/moi/projets/perso/arr-stack/spec.md | grep -q "qBittorrent"
        grep -A 20 "^### ADR-8" /home/moi/projets/perso/arr-stack/spec.md | grep -q "Jellyfin"

        # Confirm Alternatives rejetées block has 4 entries (Option B, événements, reconciler layer, class flag)
        grep -A 60 "^### ADR-8" /home/moi/projets/perso/arr-stack/spec.md | grep -A 30 "Alternatives rejetées" | grep -c "^- " | awk '{ if ($1 < 3) exit 1 }'

    Step 5 — Commit:
        cd /home/moi/projets/perso/arr-stack
        git add spec.md
        git commit -m "docs(spec): add ADR-8 — arrconf trusted controller forceSave bypass (D-02.2-04)"
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && [ "$(grep -c '^### ADR-8' spec.md)" = "1" ] && [ "$(grep -c '^### ADR-' spec.md)" = "8" ] && grep -q "trusted controller" spec.md && grep -q "forceSave=true" spec.md && grep -A 30 '^### ADR-8' spec.md | grep -q "qBittorrent" && grep -A 30 '^### ADR-8' spec.md | grep -q "Jellyfin" && grep -A 60 '^### ADR-8' spec.md | grep -q "Alternatives rejetées" && git log -1 --pretty=%s | grep -q 'docs(spec):.*ADR-8'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### ADR-8' /home/moi/projets/perso/arr-stack/spec.md` returns `1` (exactly one ADR-8 header)
    - `grep -c '^### ADR-' /home/moi/projets/perso/arr-stack/spec.md` returns `8` (no renumbering of ADRs 1-7)
    - `grep -q '^### ADR-1 ' spec.md && grep -q '^### ADR-7 ' spec.md` exits 0 (ADR-1 and ADR-7 still present)
    - spec.md contains the literal substring `trusted controller`
    - spec.md contains the literal substring `forceSave=true`
    - spec.md contains the literal substring `_ArrV3Client` (links ADR to code)
    - spec.md contains the literal substring `D-02.1-06` (links to Phase 2.1 finding)
    - `grep -A 30 '^### ADR-8' spec.md | grep -q 'qBittorrent'` exits 0 (Conséquences mentions qBit scope-out)
    - `grep -A 30 '^### ADR-8' spec.md | grep -q 'Jellyfin'` exits 0 (Conséquences mentions Jellyfin scope-out)
    - `grep -A 60 '^### ADR-8' spec.md | grep -q 'Alternatives rejetées'` exits 0
    - `grep -A 30 '^### ADR-8' spec.md | grep -qE '(Contexte|Décision|Raisons|Conséquences|Alternatives)'` exits 0 (French headers preserved per project convention)
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s | grep -q 'docs(spec):.*ADR-8'` exits 0
  </acceptance_criteria>
  <done>ADR-8 appended to spec.md §11; ADR-1..7 unchanged; load-bearing trusted-controller claim present; qBit/Jellyfin scope-out documented; alternatives rejetées block populated; commit recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec.md (durable architectural record) -> Phase 3+ contributors | ADR-8 is the long-lived contract surface for the trusted-controller decision; subsequent phase planners read §11 to understand constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-08 | Repudiation | Without ADR-8, the forceSave bypass is "magical" code with no architectural justification | mitigate | This plan adds ADR-8 with full Contexte / Décision / Raisons / Conséquences / Alternatives rejetées block matching ADRs 1-7 format. The link to D-02.1-06 (Phase 2.1 finding) and to `_ArrV3Client` code provides bidirectional traceability |
| T-02.2-09 | Tampering | A future contributor renames or removes ADR-8 silently | accept | Git history is the audit trail; ADR removal would surface in `git log spec.md`. Project convention is "ADRs are append-only; superseded ADRs are marked, not deleted" (precedent: ADRs 1-7 all preserved across phases) |
</threat_model>

<verification>
End-of-plan gate (Plan 03 complete when all pass):
1. ADR-8 header present exactly once
2. Total ADR count = 8 (no renumbering)
3. Load-bearing claim "trusted controller" + "forceSave=true" present in body
4. Code linkage `_ArrV3Client` + finding linkage `D-02.1-06` both present
5. qBit + Jellyfin scope-out documented in Conséquences
6. Alternatives rejetées block populated
7. Commit recorded with `docs(spec):` prefix
</verification>

<success_criteria>
- D-02.2-04 satisfied (ADR-8 in spec.md §11)
- Phase 3+ contributors have a self-contained reference for the architectural decision
- Bidirectional traceability: ADR ↔ code (`_ArrV3Client`) ↔ finding (D-02.1-06) ↔ implementation (Plan 02 GREEN commit)
</success_criteria>

<output>
After completion, create `/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-03-SUMMARY.md` documenting:
- Lines added to spec.md (delta from `git diff --stat`)
- ADR-8 ordinal confirmation (no renumbering)
- One-line summary of the load-bearing claim
- Commit SHA
- Recommended next step: Plan 04 (release v0.1.4) can run after Plans 02 + 03 are merged
</output>
