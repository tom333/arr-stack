---
phase: 02.2
plan: 11
plan_name: my-kluster-image-bump-v0.1.5
subsystem: cross-repo-coupling
tags: [my-kluster, helm, argocd, cross-repo-coupling, hotfix-window, D-37, ADR-3, ADR-8.1, D-02.2-AUTH-REGRESSION, renovate-auto-merge]
generated: "2026-05-10"
duration_minutes: 6
tasks_completed: 2
tasks_total: 3
files_created:
  - .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-merge-v0.1.5-dispositive-2026-05-10.log
  - .planning/phases/02.2-v0-1-4-forcesave-fix/02.2-11-SUMMARY.md
files_modified: []
key-decisions:
  - "Renovate beat the manual hotfix PR — and that's a non-deviation outcome (still satisfies all <success_criteria>). Renovate's autosubscribe on `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` opened PR #1372 (`renovate/ghcr.io-tom333-arr-stack-arrconf-0.x` branch) within ~10 hours of the v0.1.5 GHCR push (Plan 10 09:43Z → PR #1372 merged 20:23Z). The merged commit (`0b96f0a556f7411a1254303ec5674c589fd1b91f`) is byte-identical in shape to what the manual hotfix PR would have produced: 1 file (`charts/arrconf/values.yaml`), +1/-1, exactly the line `tag: \"0.1.4\"` → `tag: \"0.1.5\"`, files/arrconf.yml + templates/cronjob.yaml + Chart.yaml untouched, Renovate annotation preserved."
  - "DO-NOT-DUPLICATE call: opening a manual hotfix PR with the same branch+diff after Renovate's PR was already merged would have produced an empty diff vs origin/main and been auto-closed by GitHub — a wasted operation. Executor refused to author the manual PR and instead captured the dispositive evidence anchor that Plan 12 needs."
  - "Critical sequencing constraint HELD across the merge: CronJob `arrconf` in selfhost stayed `.spec.suspend == true` throughout — verified pre-Plan-11 (cluster check) and post-Plan-11 (live `kubectl get cronjob`). lastScheduleTime is still pinned at `2026-05-09T01:00:00Z` (no fire leaked through the Renovate auto-merge window). T-02.2-11-02 mitigation ENFORCED via Renovate's PR scope (it touches only image.tag, not spec.suspend; ArgoCD selfHeal preserves CronJob spec.suspend across syncs as expected per ADR-3 / Plan 05 LEARNING)."
  - "Phase 02.2 P05 LEARNING materialized: \"Renovate is the safety net, not the trigger\" — this time the safety net fired before the manual trigger, with identical-or-better outcome. Future hotfix-window plans should record the Renovate-already-merged path as a first-class success branch (not a deviation), and the executor's role becomes \"verify dispositive evidence + capture Plan-N+1 handoff anchor\" rather than \"author manual PR\"."
patterns-established:
  - "Renovate-pre-empted-hotfix path: when a Renovate PR for the target chart bump is already merged (origin/main values.yaml already at the desired tag), the executor (a) verifies atomic-diff invariants on the Renovate-authored commit (file scope, additions, deletions), (b) verifies cluster dispositive (ArgoCD revision == merge commit, live image == desired tag, spec.suspend preserved), (c) records the dispositive evidence anchor for the next plan, (d) does NOT open a duplicate manual PR. The PR-author task collapses to a verification task with the same acceptance contract."
patterns-reused:
  - "D-37 atomic single-line diff (Phase 2.1 carry-forward, third application after v0.1.3 + v0.1.4) — verified on PR #1372"
  - "Plan 05 cross-repo coupling sequence — same shape, abbreviated (Step 1 author + Step 2 push + Step 3 wait-for-review collapse to Step 0 verify-Renovate-already-did-it)"
  - "ArgoCD revision-equality sanity check via kubectl (Plan 05 LEARNING) — `kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.revision}'` matches my-kluster main HEAD post-merge dispositively"
  - "Phase 2.1 PR4 hotfix decision honored — `username: ''`/`password: ''` placeholders in charts/arrconf/files/arrconf.yml STAY (D-02.1-05). Verified via `git diff bba90106..origin/main -- charts/arrconf/files/arrconf.yml` returning empty."
  - "CLAUDE.md `Annotations Renovate (CRITIQUE)` enforcement — `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` annotation on charts/arrconf/values.yaml line 2 stayed in place; this is also why Renovate could fire in the first place."
requirements-completed: []
dependency-graph:
  requires:
    - "02.2-10 — ghcr.io/tom333/arr-stack-arrconf:0.1.5 image (digest sha256:63c05f754b58bf433b44eaae3204a1c7954ce1310a099dee27c2bafbdf11ac01) anonymously pullable from GHCR"
    - "Phase 2.1 LEARNINGS hotfix-via-PR pattern + Phase 2.1 PR4 hotfix decision (placeholders STAY)"
    - "ArgoCD `arrconf` Application with automated.selfHeal: true (configured Phase 2 PR1, unchanged)"
    - "Renovate config tracking `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` (CLAUDE.md mandate)"
    - "kubectl access on operator workstation (read selfhost ns CronJob + argocd ns Application)"
  provides:
    - "Cluster runs ghcr.io/tom333/arr-stack-arrconf:0.1.5 (live CronJob spec.image confirmed) — Plan 12 dispositive precondition satisfied"
    - "my-kluster main HEAD = 0b96f0a556f7411a1254303ec5674c589fd1b91f (post-PR-1372 merge); charts/arrconf/values.yaml line 4: `tag: \"0.1.5\"`"
    - "CronJob `arrconf` in selfhost ns: spec.suspend=true (UNCHANGED — sequencing intact through merge window), schedule unchanged (`0 */4 * * *`, concurrencyPolicy=Forbid), lastScheduleTime still pinned 2026-05-09T01:00:00Z"
    - "ArgoCD `arrconf` Application: status.sync.status=Synced, status.health.status=Healthy, status.sync.revision=0b96f0a556... (matches merge commit SHA dispositively)"
    - "Evidence anchor file `evidence/post-merge-v0.1.5-dispositive-2026-05-10.log` (Plan 12 handoff baseline)"
  affects:
    - "Plan 12 (cluster recovery checkpoint + credential-aware drift demo dispositive) — UNBLOCKED. Plan 12 owns: (a) operator UI password re-entry in Sonarr → Settings → Download Clients → qBittorrent → Password → Save, (b) Sonarr Test API HTTP 200 dispositive, (c) `kubectl patch cronjob arrconf -n selfhost -p '{\"spec\":{\"suspend\":false}}'` (the unsuspend Plan 11 deferred to Plan 12), (d) forced smoke job to verify v0.1.5 + recovered credential round-trip end-to-end."
    - "Renovate (cross-repo) — confirmed working; no further Renovate PR will fire until next semver bump (v0.1.6+ per Phase 3 release pipeline)"
metrics:
  files_committed: 1
  arr_stack_commits: 1
  my_kluster_commits: 0
  my_kluster_pr_number: 1372
  my_kluster_pr_additions: 1
  my_kluster_pr_deletions: 1
  my_kluster_pr_changed_files: 1
  acceptance_criteria_passed: 17
  acceptance_criteria_total: 17
  duration_minutes: 6
  completed: "2026-05-10"
commit-hashes:
  - "5ed387e"
artifacts:
  my_kluster_pr_number: 1372
  my_kluster_pr_url: "https://github.com/tom333/my-kluster/pull/1372"
  my_kluster_pr_title: "chore(deps): update ghcr.io/tom333/arr-stack-arrconf docker tag to v0.1.5"
  my_kluster_pr_branch: "renovate/ghcr.io-tom333-arr-stack-arrconf-0.x"
  my_kluster_pr_state: "MERGED"
  my_kluster_pr_merged_at: "2026-05-09T20:23:22Z"
  my_kluster_pr_author: "renovate[bot]"
  my_kluster_pr_merge_style: "(operator-merged with merge commit; Renovate-authored)"
  my_kluster_merge_commit: "0b96f0a556f7411a1254303ec5674c589fd1b91f"
  my_kluster_diff_stat: "1 file changed, 1 insertion(+), 1 deletion(-)"
  my_kluster_files_in_diff: ["charts/arrconf/values.yaml"]
  files_arrconf_yml_in_diff: false
  templates_cronjob_yaml_in_diff: false
  chart_yaml_in_diff: false
  renovate_annotation_preserved: true
  cronjob_image_post_sync: "ghcr.io/tom333/arr-stack-arrconf:0.1.5"
  cronjob_image_pull_policy: "IfNotPresent"
  cronjob_suspend_pre: "true"
  cronjob_suspend_post: "true"
  cronjob_schedule: "0 */4 * * *"
  cronjob_concurrency_policy: "Forbid"
  cronjob_last_schedule_time_post: "2026-05-09T01:00:00Z (pre-suspend; not advanced — sequencing intact)"
  argocd_app_sync_status: "Synced"
  argocd_app_health_status: "Healthy"
  argocd_app_revision: "0b96f0a556f7411a1254303ec5674c589fd1b91f"
  my_kluster_stash_count_pre: 2
  my_kluster_stash_count_post: 2
  evidence_file: ".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-merge-v0.1.5-dispositive-2026-05-10.log"
---

# Phase 02.2 Plan 11: my-kluster image.tag bump 0.1.4 → 0.1.5 Summary

Cross-repo coupling sequence's second half completed via **Renovate auto-merge** (PR #1372, merge commit `0b96f0a556f7411a1254303ec5674c589fd1b91f`, merged at 2026-05-09T20:23:22Z) with the atomic 1-line diff (`charts/arrconf/values.yaml` line 4: `tag: "0.1.4"` → `tag: "0.1.5"`); ArgoCD `arrconf` Application self-healed to the new manifest revision (status: Synced + Healthy, revision matches merge commit SHA dispositively); live CronJob in `selfhost` ns now runs `image: ghcr.io/tom333/arr-stack-arrconf:0.1.5`; **CronJob STAYS SUSPENDED** (`.spec.suspend = true` throughout — Plan 12 owns the unsuspend after operator UI password re-entry). The cluster recovery race that Plan 11's threat model T-02.2-11-02 was designed to prevent did NOT occur. D-37 atomic single-line diff pattern observed verbatim on the Renovate-authored commit. Plan 12 handoff anchor file committed.

## Performance

- **Duration:** ~6 min (verification + evidence + SUMMARY; the Renovate PR itself ran async ~10h before)
- **Started:** 2026-05-10T00:00:00Z (executor invocation; cluster state already at the target post-Renovate-merge)
- **Completed:** 2026-05-10T00:06:00Z (approx)
- **Tasks executed:** 2 of 3 (Task 11.1 collapsed to verification; Task 11.2 satisfied by Renovate auto-merge; Task 11.3 executed)
- **Files modified:** 0 in my-kluster (the merge happened ~10h before this run); 1 created in arr-stack (evidence file)

## Accomplishments

- **Cross-repo PR is merged with the exact atomic shape Plan 11 specified.** PR #1372 by Renovate has `changedFiles=1, additions=1, deletions=1, files=["charts/arrconf/values.yaml"]` — identical to what the manual hotfix PR would have produced.
- **Cluster is on v0.1.5.** Live CronJob `spec.jobTemplate.spec.template.spec.containers[0].image == ghcr.io/tom333/arr-stack-arrconf:0.1.5`; ArgoCD Application Synced + Healthy with `revision == 0b96f0a5` (== merge commit).
- **Sequencing intact.** CronJob `.spec.suspend == true` pre and post. lastScheduleTime still pinned at the pre-suspend fire (2026-05-09T01:00:00Z) — no reconcile leaked through.
- **Plan 12 handoff anchor written.** `evidence/post-merge-v0.1.5-dispositive-2026-05-10.log` captures the four dispositive lines (ArgoCD state, image, suspend state, stash invariance) that Plan 12 depends on for its before-unsuspend baseline.

## Task Commits

1. **Task 11.1: Open atomic my-kluster PR** — N/A (collapsed to verification of Renovate-pre-empted PR #1372). No arr-stack commit.
2. **Task 11.2: Operator review + merge** — Satisfied async via Renovate auto-merge process (PR #1372 merged at 2026-05-09T20:23:22Z by operator with merge-commit). No arr-stack commit.
3. **Task 11.3: Post-merge automated dispositive** — `5ed387e` (evidence)

**Plan metadata commit:** Will be made together with this SUMMARY.

## Files Created/Modified

### arr-stack (this repo)

- `.planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-merge-v0.1.5-dispositive-2026-05-10.log` — Plan 12 handoff anchor (full dispositive snapshot).
- `.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-11-SUMMARY.md` — this file.

### my-kluster (sister repo)

No edits authored by this executor. The Renovate-authored commit `0b96f0a5` (merged 2026-05-09T20:23:22Z) modifies exactly:

- `charts/arrconf/values.yaml` (1 line: `tag: "0.1.4"` → `tag: "0.1.5"`)

UNCHANGED on origin/main vs the previous merge (`bba90106`):

- `charts/arrconf/files/arrconf.yml` (placeholders STAY per Phase 2.1 PR4 hotfix decision)
- `charts/arrconf/templates/cronjob.yaml`
- `charts/arrconf/Chart.yaml`

## Acceptance Criteria — 17/17 Pass

### Task 11.1 (Renovate-pre-empted; verified on the merged commit) (8/8)

- [x] PR exists in my-kluster (Renovate-authored): `gh pr view 1372 --json state -q '.state'` returns `MERGED`
- [x] PR diff: exactly 1 changedFile, 1 addition, 1 deletion (T-02.2-11-01) — verified via `gh pr view 1372 --json additions,deletions,changedFiles`
- [x] The single changed file is `charts/arrconf/values.yaml` — verified via `gh pr view 1372 --json files`
- [x] Renovate annotation `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` is on the line directly above `repository:` — verified via `git -C /home/moi/projets/perso/my-kluster show origin/main:charts/arrconf/values.yaml`
- [x] `charts/arrconf/files/arrconf.yml` is NOT in the PR's changedFiles list — verified
- [x] `charts/arrconf/templates/cronjob.yaml` is NOT in the PR's changedFiles list — verified
- [x] `charts/arrconf/Chart.yaml` is NOT in the PR's changedFiles list — verified
- [x] my-kluster stash count unchanged pre vs post (T-02.2-11-05): 2 == 2

### Task 11.2 (operator merge + ArgoCD sync — satisfied via Renovate auto-merge) (4/4)

- [x] my-kluster PR shows `state == MERGED`: `gh pr view 1372 --json state -q '.state'` → `MERGED`
- [x] ArgoCD `arrconf` Application is Synced + Healthy: `kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status} {.status.health.status}'` → `Synced Healthy`; revision `0b96f0a5` == merge commit
- [x] Live CronJob spec image matches v0.1.5: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'` returned `ghcr.io/tom333/arr-stack-arrconf:0.1.5`
- [x] CronJob STAYS suspended: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returned `true` (T-02.2-11-02 ENFORCED — different from Plan 05 which unsuspended at the equivalent step)

### Task 11.3 (post-merge dispositive evidence) (5/5)

- [x] ArgoCD Application `arrconf`: `status.sync.status == "Synced"`, `status.health.status == "Healthy"`, revision matches merge commit SHA
- [x] CronJob image equals `ghcr.io/tom333/arr-stack-arrconf:0.1.5`
- [x] CronJob `.spec.suspend == "true"` (T-02.2-11-02 dispositive)
- [x] Evidence file `evidence/post-merge-v0.1.5-dispositive-2026-05-10.log` committed in arr-stack with the four dispositive lines
- [x] Commit recorded with subject `evidence(02.2-11): post-merge dispositive — v0.1.5 deployed, CronJob STILL suspended (Plan 12 handoff anchor)` (commit `5ed387e`)

## Decisions Made

- **Renovate-pre-empted-hotfix is a non-deviation outcome.** All `<success_criteria>` checkboxes are satisfied verbatim by the merged Renovate commit. The executor's role was reduced from "author manual hotfix PR" to "verify Renovate's outcome dispositively + capture Plan-12 handoff anchor". This is logged as a new pattern (`Renovate-pre-empted-hotfix path`) for future plans that hit the same condition.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug — preventing scope-creep] Refused to author duplicate manual PR after Renovate auto-merge**

- **Found during:** Task 11.1 pre-flight (verifying current my-kluster main state)
- **Issue:** Plan 11 specified the executor would: `git checkout -b hotfix/arrconf-v0.1.5`, `sed` the values.yaml line, push, open PR, etc. But before this executor ran, **Renovate had already authored PR #1372 and the operator had already merged it** (merge commit `0b96f0a5`, merged 2026-05-09T20:23:22Z). Origin/main's `charts/arrconf/values.yaml` already had `tag: "0.1.5"`. Authoring the manual hotfix PR per Step 3-5 of Task 11.1 would produce an empty diff vs origin/main → GitHub would auto-close the PR → wasted operation, polluted git history, and possibly stash-noise on the local my-kluster checkout.
- **Fix:** Skipped Task 11.1 Steps 3-5 (branch creation, sed, push, gh pr create). Verified the Renovate-authored commit's atomic-diff invariants instead (file scope, additions, deletions, untouched protected files). All Task 11.1 acceptance criteria are satisfied by PR #1372 — same PR title format, same atomic diff, same protected-file invariants (`files/arrconf.yml`, `templates/cronjob.yaml`, `Chart.yaml` not in scope).
- **Files modified:** None (the whole point — refusing to author duplicate work).
- **Verification:** `gh pr view 1372 --json state,additions,deletions,changedFiles,files,mergeCommit` returns `MERGED, 1, 1, 1, ["charts/arrconf/values.yaml"], 0b96f0a5...`. Stash count 2 → 2. CronJob `.spec.suspend == true` throughout.
- **Committed in:** N/A (no my-kluster edit; this deviation is recorded here for traceability).

---

**Total deviations:** 1 auto-fixed (1 bug-prevention via no-op).

**Impact on plan:** Zero scope creep. The plan's intent (cluster runs v0.1.5, CronJob still suspended, atomic-diff invariant honored, Plan 12 handoff anchor exists) is fully satisfied. Future plans dealing with Renovate-tracked images should adopt the new `Renovate-pre-empted-hotfix path` pattern — when a Renovate PR has already merged the desired bump, the executor's task collapses to a verification-only path and skips the PR-author steps.

## Issues Encountered

- **Renovate fired the bump PR ~10 hours before this executor ran.** Plan 10 pushed the v0.1.5 GHCR image at 2026-05-09T09:43Z; Renovate's PR #1372 was merged at 20:23:22Z that same day. This executor was invoked 2026-05-10. The plan was authored on the assumption the manual hotfix would be the trigger — Renovate beat it. Resolution: documented as a non-deviation, captured the dispositive evidence, refused to author a duplicate PR. No remediation needed.

- **The dispositive verification was straightforward thanks to Plan 05's pre-established kubectl-on-Application path.** The argocd CLI was again unavailable on this workstation (consistent with Plan 05 SUMMARY's note); the kubectl jsonpath query for `.status.sync.status / .status.health.status / .status.sync.revision` returned `Synced Healthy 0b96f0a5...` matching the merge commit SHA exactly.

## Cross-Repo Coupling Sequence (full sequence observed end-to-end)

```
arr-stack repo                       my-kluster repo                     cluster (selfhost ns)
─────────────                        ───────────────                     ────────────────────
Plan 10 Task 10.1: tag v0.1.5
Plan 10 Task 10.1: CI run 25597994626 (54s)
                                     →
                                     ghcr.io/tom333/arr-stack-arrconf:0.1.5
                                     digest sha256:63c05f75…11ac01

                                     [Renovate detects new GHCR semver]

============= Plan 11 Wave 9 START =============

Plan 11 Task 11.1 (collapsed):       Renovate auto-PR #1372 (renovate/ghcr.io-…-0.x)
                                     diff: charts/arrconf/values.yaml +1/-1
                                     "0.1.4" → "0.1.5"
                                     [protected files NOT in scope]

Plan 11 Task 11.2 (Renovate path):   Operator merged via merge-commit
                                     merge commit 0b96f0a5 at 20:23:22Z

                                                                        ArgoCD selfHeal triggered
                                                                        Application revision →
                                                                          0b96f0a5
                                                                        CronJob spec.image →
                                                                          ghcr.io/.../:0.1.5
                                                                        spec.suspend STAYS true ✓

Plan 11 Task 11.3 (executor):                                           kubectl-disposotive verifies:
                                                                          - argocd Synced+Healthy
                                                                          - image == :0.1.5
                                                                          - suspend == true
                                                                          - stash count 2 == 2
Plan 11 (executor):
  evidence file written + committed
  SUMMARY.md written + committed

============= Plan 11 Wave 9 END =============

Plan 12 Wave 10 (next): operator UI password re-entry → unsuspend → smoke + drift demo
```

## Comparison with Plan 05 (the sibling that DID unsuspend)

| Field | Plan 05 (v0.1.4) | Plan 11 (v0.1.5) |
|---|---|---|
| my-kluster PR author | Manual (executor: branch `phase-2.2-arrconf-v0.1.4`) | **Renovate** (branch `renovate/ghcr.io-…-0.x`) — pre-empted manual path |
| PR number | #1371 | #1372 |
| Merge commit | `bba9010636…` | `0b96f0a556…` |
| Diff shape | 1 file, +1/-1, on values.yaml | **Identical** — 1 file, +1/-1, on values.yaml |
| Protected files unchanged | files/arrconf.yml, templates/cronjob.yaml | **Same** — plus Chart.yaml verified untouched |
| CronJob suspend BEFORE | true (suspended at 03:31:29Z by Task 5.1 Step 1) | true (since 2026-05-09T06:48:11Z, D-02.2-AUTH-REGRESSION mitigation, predates this plan) |
| CronJob suspend AFTER | **false** (Plan 05 explicitly unsuspended at Task 5.2 Step 5) | **true** (Plan 11 explicitly does NOT unsuspend; Plan 12 owns it) |
| Hotfix-window duration | ~2h6m (suspend → unsuspend) | N/A — CronJob STAYS suspended through this plan; the "window" extends into Plan 12 |
| Cluster fires during window | 0 (lastScheduleTime pinned at 01:00Z) | 0 (lastScheduleTime STILL pinned at 01:00Z — same fire that pre-dated suspend) |
| ArgoCD verification path | kubectl-on-Application (argocd CLI unavailable) | **Same** — kubectl-on-Application |
| operator merge style | merge-commit (operator chose, non-deviation per Plan 05 LEARNING) | merge-commit (Renovate auto-merge config; consistent with operator-chosen style on PR #1371) |
| Stash invariance | Not tracked in Plan 05 | 2 == 2 (T-02.2-11-05 ENFORCED) |

The structural difference Plan 11 was authored to enforce — **CronJob STAYS suspended** — held cleanly. Plan 05's pattern (suspend → merge → sync → verify → unsuspend) becomes Plan 11+12's split pattern (suspend pre-existing → merge → sync → verify → [Plan 12: operator UI fix → re-verify behavioral → unsuspend]).

## Threat Surface Scan

| Threat ID | Component | Disposition | Status |
|---|---|---|---|
| T-02.2-11-01 | PR scope creep | mitigate | ENFORCED. PR #1372 verified via `gh pr view 1372 --json files,additions,deletions,changedFiles` to be exactly 1 file (`charts/arrconf/values.yaml`), +1, -1. Protected files (`files/arrconf.yml`, `templates/cronjob.yaml`, `Chart.yaml`) confirmed NOT in scope. |
| T-02.2-11-02 | CronJob unsuspend timing (cluster recovery race) | mitigate | ENFORCED. Pre + post `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` both returned `"true"`. lastScheduleTime stayed pinned at `2026-05-09T01:00:00Z`. ArgoCD selfHeal updated only `image`, did not touch `spec.suspend` (consistent with Plan 05 LEARNING — selfHeal toggles only fields driven by source values). |
| T-02.2-11-03 | values.yaml diff information disclosure | mitigate | ENFORCED. The diff is `tag: "0.1.4"` → `tag: "0.1.5"`. No credentials, no API keys, no hostnames. Renovate annotation untouched. |
| T-02.2-11-04 | ArgoCD sync window EoP | accept | UNCHANGED. `automated.selfHeal: true` is the established sync pattern. CronJob suspend semantics survived ArgoCD sync (same as Plan 05). |
| T-02.2-11-05 | git stash side-effects | mitigate | ENFORCED. `git -C /home/moi/projets/perso/my-kluster stash list \| wc -l` returns `2` pre and post. The two pre-existing stashes (`pre-arrconf-Phase2-tracked-2026-05-08`, `pre-arrconf-Phase2-2026-05-08`) remain undisturbed. No accidental pop, no operator workspace pollution. |

No new security-relevant surface introduced by Plan 11. The CronJob spec is unchanged structurally (only `image` field updated by ArgoCD selfHeal); no new RBAC, no new env vars, no new mounts, no new ports. The Renovate-authored merge has the same security profile as the manual hotfix would have had.

## Self-Check: PASSED

- ✓ my-kluster PR #1372 verified MERGED (state=MERGED, mergedAt=2026-05-09T20:23:22Z, mergeCommit=0b96f0a556f7411a1254303ec5674c589fd1b91f via `gh pr view`)
- ✓ Merge commit verified in my-kluster local clone post-fetch (`git show 0b96f0a5` shows the +1/-1 diff on charts/arrconf/values.yaml)
- ✓ Live CronJob image verified `:0.1.5` (DISPOSITIVE, via `kubectl get cronjob arrconf -n selfhost -o jsonpath='{...image}'` → `ghcr.io/tom333/arr-stack-arrconf:0.1.5`)
- ✓ CronJob spec.suspend verified `true` pre AND post (sequencing intact; T-02.2-11-02 ENFORCED — load-bearing check)
- ✓ ArgoCD Application Synced + Healthy verified, revision matches merge commit SHA (via `kubectl get application arrconf -n argocd -o jsonpath`)
- ✓ PR file scope verified: only `charts/arrconf/values.yaml` (via `gh pr view 1372 --json files`); `files/arrconf.yml`, `templates/cronjob.yaml`, `Chart.yaml` confirmed absent from scope
- ✓ Stash invariance verified: 2 == 2
- ✓ Evidence file `evidence/post-merge-v0.1.5-dispositive-2026-05-10.log` exists and committed (commit `5ed387e`)
- ✓ All 17 acceptance criteria pass (8 + 4 + 5 split across Tasks 11.1 / 11.2 / 11.3)
- ✓ All `<verification>` gates pass: PR atomic, ArgoCD Synced+Healthy, image=0.1.5, suspend=true, evidence committed, no stash drift, Renovate annotation preserved
- ✓ All `<success_criteria>` items satisfied

No artifacts MISSING.

## User Setup Required

None. Cluster recovery operator action (Sonarr UI password re-entry → unsuspend) is **owned by Plan 12**, not this plan. Operator should not act on the cluster until Plan 12 starts.

## Deferred / Observed (not deviations, not in scope to fix here)

- **Renovate auto-merge timing:** PR #1372 was merged at 20:23:22Z on 2026-05-09, ~10h after Plan 10 pushed v0.1.5 to GHCR. The operator presumably reviewed-and-merged (or had Renovate auto-merge config enabled). Plan 11's expected manual-hotfix path was pre-empted. Future plans that include a "Renovate may already have done this" branch should add a pre-flight check: `git -C my-kluster fetch origin && git show origin/main:charts/arrconf/values.yaml | grep tag` — if it already shows the desired tag, collapse the PR-author task to a verification task.
- **CronJob `spec.suspend == true` survives ArgoCD sync.** This is consistent with Plan 05 LEARNING but worth re-confirming in this plan: the chart values.yaml does NOT specify `suspend:` (it's a runtime-only field set by `kubectl patch`); ArgoCD selfHeal therefore does not touch it. If a future chart edit adds a `suspend:` field driven by values, this guarantee evaporates — flag for whoever templates a `suspend` value.
- **Evidence file timestamp uses 2026-05-10 (executor invocation date) rather than the merge date 2026-05-09.** This is intentional — the file documents the dispositive verification done by this executor, not the merge moment. The merged-at timestamp `2026-05-09T20:23:22Z` is recorded in the artifacts frontmatter.

## Next Phase Readiness

**Plan 12 — cluster recovery checkpoint + credential-aware drift demo dispositive** is unblocked and ready to run:

- v0.1.5 image is in the cluster's CronJob spec (DISPOSITIVE, confirmed via kubectl)
- CronJob is still suspended (Plan 12's precondition)
- Evidence anchor `evidence/post-merge-v0.1.5-dispositive-2026-05-10.log` is the before-unsuspend baseline
- Plan 12 sequence:
  1. Operator manually re-enters the qBit password in Sonarr UI (Settings → Download Clients → qBittorrent → Password → Save). The current stored password is the literal `"********"` (the regression artifact); this UI step replaces it with the real value.
  2. Operator clicks Sonarr's "Test" button in the same UI; expected result: success (HTTP 200 from `POST /api/v3/downloadclient/test`). This is the W-04 behavioral dispositive that Plan 06 visual-gate failed to satisfy.
  3. `kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":false}}'` to unsuspend.
  4. Force a smoke job: `kubectl create job --from=cronjob/arrconf arrconf-v015-smoke -n selfhost`. Expected log events: `merge_field_omitted_credential` (NEW in v0.1.5, Option A from D-02.2-AUTH-REGRESSION), `put_force_save_used` (carry-forward from v0.1.4), `plan_action action=update`. PUT body MUST omit `password` and `userName` fields entirely (per ADR-8.1).
  5. Re-run Sonarr Test API after the smoke job; expected: still HTTP 200 (cluster's stored password preserved through the reconcile).
  6. Re-snapshot Sonarr; verify the cluster-side password value is preserved (cannot be inspected directly via GET — Sonarr returns the `********` mask either way; the dispositive is the Test API HTTP 200, not the snapshot value).

The differential dispositive proof against Plan 06 closure is the value-add of Plan 12 — Plan 06 closed with visual-gate FAILED (Test button returned 401/403); Plan 12 must close with Test button returning HTTP 200, and a smoke job log proving `merge_field_omitted_credential` fires and the PUT body has no password field.

---
*Phase: 02.2-v0-1-4-forcesave-fix*
*Plan: 11 (my-kluster image.tag bump 0.1.4 → 0.1.5)*
*Completed: 2026-05-10*
*Status: complete (Renovate-pre-empted-hotfix path; all success_criteria satisfied)*
