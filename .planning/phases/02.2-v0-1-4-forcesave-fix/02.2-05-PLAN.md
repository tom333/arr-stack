---
phase: 02.2
plan: 05
type: execute
wave: 4
depends_on:
  - 02.2-04
files_modified:
  - my-kluster/charts/arrconf/values.yaml
autonomous: false
requirements: []
must_haves:
  truths:
    - "PR opened in my-kluster with EXACTLY one line changed: image.tag '0.1.3' -> '0.1.4' in charts/arrconf/values.yaml"
    - "charts/arrconf/files/arrconf.yml is UNCHANGED (placeholders username: ''/password: '' STAY per Phase 2.1 PR4 hotfix decision — D-02.1-05)"
    - "charts/arrconf/templates/cronjob.yaml is UNCHANGED"
    - "CronJob is suspended BEFORE the PR merge so no in-flight reconcile fires during ArgoCD sync window"
    - "PR is reviewed and merged by the operator (checkpoint:human-action gate)"
    - "After merge: ArgoCD self-heals to v0.1.4 image; CronJob is unsuspended; CronJob spec shows the new image tag"
  artifacts:
    - path: "my-kluster/charts/arrconf/values.yaml (post-merge)"
      provides: "Cluster runs ghcr.io/tom333/arr-stack-arrconf:0.1.4 after ArgoCD sync"
      contains: "tag: \"0.1.4\""
  key_links:
    - from: "my-kluster PR (image.tag bump)"
      to: "ArgoCD selfHeal -> live CronJob image"
      via: "automated.selfHeal: true on the arrconf ArgoCD Application"
      pattern: "image:\\s*ghcr.io/tom333/arr-stack-arrconf:0.1.4"
    - from: "kubectl patch cronjob arrconf -n selfhost suspend"
      to: "Hotfix-via-PR safety pattern"
      via: "Suspend CronJob during merge → ArgoCD sync → operator-driven verification window"
      pattern: "suspend.*true"
---

<objective>
Cross-repo coupling sequence's second half: open the my-kluster PR bumping `charts/arrconf/values.yaml` `image.tag: "0.1.3" -> "0.1.4"`, suspend the CronJob during the merge window (per Phase 2.1 hotfix-window pattern), wait for operator review + merge, verify ArgoCD self-heals the new image into the CronJob spec, and unsuspend.

This plan is the smallest atomic diff (D-37). The placeholder `username: ''`/`password: ''` entries in `charts/arrconf/files/arrconf.yml` STAY (per Phase 2.1 PR4 hotfix decision documented in CONTEXT.md L24 and 02.1-LEARNINGS D-36 reversal).

Purpose: Make the cluster actually run v0.1.4 image without disturbing the active reconcile loop during the merge window.

Output: my-kluster PR merged; ArgoCD shows arrconf Application Synced + Healthy with the v0.1.4 image; CronJob unsuspended and ready for Plan 06 cluster smoke + drift demo.

Wave 4 — depends on Plan 04 (v0.1.4 image actually pullable from GHCR).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-03-PLAN.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml

<interfaces>
<!-- Single-line change in my-kluster — D-37 atomic diff -->

# Existing values.yaml (lines 1-5) — CURRENT STATE:
image:
  # renovate: image=ghcr.io/tom333/arr-stack-arrconf
  repository: ghcr.io/tom333/arr-stack-arrconf
  tag: "0.1.3"
  pullPolicy: IfNotPresent

# DESIRED diff (1 line):
-    tag: "0.1.3"
+    tag: "0.1.4"

# UNCHANGED in this PR:
# - charts/arrconf/files/arrconf.yml (placeholders STAY per Phase 2.1 PR4)
# - charts/arrconf/templates/cronjob.yaml
# - charts/arrconf/Chart.yaml
# - any other my-kluster file

# CronJob suspend command (Phase 2.1 hotfix-window pattern):
# kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":true}}'
# kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":false}}'

# ArgoCD verification:
# argocd app get arrconf
# argocd app sync arrconf
# argocd app wait arrconf --health --timeout 180

# Live CronJob image verification (post-sync):
# kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'
# Expected: ghcr.io/tom333/arr-stack-arrconf:0.1.4
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Suspend CronJob + open my-kluster PR (atomic image.tag bump 0.1.3 → 0.1.4)</name>
  <files>
    my-kluster/charts/arrconf/values.yaml
  </files>
  <read_first>
    - /home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml (current state — confirm `tag: "0.1.3"` on line 4)
    - /home/moi/projets/perso/my-kluster/charts/arrconf/files/arrconf.yml (confirm placeholders `username: ''` and `password: ''` are present and KEPT — DO NOT EDIT this file)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md "Suspend CronJob during hotfix windows" + "Hotfix-via-PR pattern when a deployed change reveals a defect"
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-PATTERNS.md §8 (single-line diff template)
  </read_first>
  <action>
    Step 1 — Suspend the CronJob in cluster (Phase 2.1 hotfix-window pattern):
        kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":true}}'
        kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'
        # Expected: "true"

    Step 2 — Author the my-kluster PR (atomic 1-line diff per D-37):
        cd /home/moi/projets/perso/my-kluster
        git checkout main && git pull origin main
        git checkout -b phase-2.2-arrconf-v0.1.4

        # Atomic edit: change ONLY the tag line.
        sed -i 's/^    tag: "0.1.3"$/    tag: "0.1.4"/' charts/arrconf/values.yaml

        # Verify exactly 1 file, 1 line changed:
        DIFF_STAT=$(git diff --stat)
        echo "$DIFF_STAT"
        echo "$DIFF_STAT" | grep -q '1 file changed, 1 insertion(+), 1 deletion(-)' \
          || { echo "ERROR: diff is not a single-line change"; git diff; exit 1; }

        # Confirm files/arrconf.yml is UNTOUCHED (placeholders STAY per Phase 2.1 PR4):
        git diff charts/arrconf/files/arrconf.yml | grep -q . \
          && { echo "ERROR: files/arrconf.yml MUST NOT change in this PR"; exit 1; } || true

        # Confirm cronjob.yaml is UNTOUCHED:
        git diff charts/arrconf/templates/cronjob.yaml | grep -q . \
          && { echo "ERROR: templates/cronjob.yaml MUST NOT change in this PR"; exit 1; } || true

    Step 3 — Commit + push + open the PR:
        git add charts/arrconf/values.yaml
        git commit -m "feat(arrconf): bump image v0.1.3 -> v0.1.4 (forceSave fix, D-02.1-06)

Closes D-02.1-06: Sonarr's pre-save qBit auth re-validation against the
masked '********' password preserved by merge_fields_for_put causes HTTP 400
on real-change PUTs. v0.1.4 ships ?forceSave=true on UPDATE PUTs via the
new _ArrV3Client mixin (arr-stack ADR-8 — trusted controller stance).

Required prerequisite before Phase 3 (Radarr/Prowlarr automated drift correction).

See: arr-stack v0.1.4 release notes + spec.md §11 ADR-8."
        git push -u origin phase-2.2-arrconf-v0.1.4

        gh pr create \
          --repo tom333/my-kluster \
          --title "feat(arrconf): bump image v0.1.3 -> v0.1.4 (forceSave fix)" \
          --body "$(cat <<'EOF'
## Summary

Atomic image-tag bump only. Closes D-02.1-06 (forceSave fix).

- `charts/arrconf/values.yaml`: `tag: "0.1.3"` → `tag: "0.1.4"`
- `charts/arrconf/files/arrconf.yml`: **UNCHANGED** (placeholders stay per Phase 2.1 PR4)
- `charts/arrconf/templates/cronjob.yaml`: **UNCHANGED**

## Why

Sonarr's GET /api/v3/downloadclient/{id} masks `privacy=password` fields with the literal `********`. Phase 2.1's `merge_fields_for_put` faithfully preserves this mask in the PUT body. Sonarr's pre-save validation re-authenticates against `********` and rejects the PUT with HTTP 400 on any real-change PUT (e.g. priority drift). v0.1.4 ships `?forceSave=true` on UPDATE PUTs — arrconf is the trusted controller per ADR-8.

Required prerequisite before Phase 3 (Radarr/Prowlarr automated drift correction would face the identical 400 failure mode).

## Test plan

- [ ] CronJob suspended pre-merge (already done by operator workflow)
- [ ] After merge: ArgoCD self-heals; live CronJob shows `image: ghcr.io/tom333/arr-stack-arrconf:0.1.4`
- [ ] CronJob unsuspended after ArgoCD sync OK
- [ ] Forced smoke job (next phase) shows: `put_force_save_used` event + `plan_action action=update` + PUT 200 (no more 400)
- [ ] Drift demo runbook re-executed WITHOUT operator manual `?forceSave=true` nudge — closes REQ-drift-detection cleanly

See arr-stack `.planning/phases/02.2-v0-1-4-forcesave-fix/` for full phase context.
EOF
)"

        PR_NUMBER=$(gh pr view --repo tom333/my-kluster --json number --jq .number)
        echo "PR opened: #$PR_NUMBER"
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/my-kluster && [ "$(git diff main..phase-2.2-arrconf-v0.1.4 --stat | tail -1 | awk '{print $1}')" = "1" ] && grep -q '^    tag: "0.1.4"$' charts/arrconf/values.yaml && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')" = "true" ] && [ -n "$(gh pr list --repo tom333/my-kluster --head phase-2.2-arrconf-v0.1.4 --json number --jq '.[0].number')" ]</automated>
  </verify>
  <acceptance_criteria>
    - `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returns `true` (CronJob is suspended)
    - Branch `phase-2.2-arrconf-v0.1.4` exists in my-kluster origin
    - `git -C /home/moi/projets/perso/my-kluster diff main..phase-2.2-arrconf-v0.1.4 --stat` shows exactly 1 file, 1 line changed
    - `grep -q '^    tag: "0.1.4"$' /home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml` exits 0 (on PR branch)
    - `git -C /home/moi/projets/perso/my-kluster diff main..phase-2.2-arrconf-v0.1.4 -- charts/arrconf/files/arrconf.yml | wc -l` returns `0` (placeholders stay)
    - `git -C /home/moi/projets/perso/my-kluster diff main..phase-2.2-arrconf-v0.1.4 -- charts/arrconf/templates/cronjob.yaml | wc -l` returns `0` (templates stay)
    - PR exists in my-kluster: `gh pr list --repo tom333/my-kluster --head phase-2.2-arrconf-v0.1.4 --json number --jq '.[0].number'` returns a number
    - PR title contains `feat(arrconf):` and `v0.1.4`
  </acceptance_criteria>
  <done>CronJob suspended; my-kluster PR opened with exactly 1-line diff; placeholders + templates untouched; ready for operator review + merge in Task 5.2.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5.2: Operator review + merge of my-kluster PR; verify ArgoCD self-heals to v0.1.4</name>
  <files>
  </files>
  <read_first>
    - PR description (auto-loaded by gh pr view in the action)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md "Decisions §Inherited from Phase 2.1 §Hotfix-via-PR pattern"
  </read_first>
  <action>
    This is a checkpoint:human-action task. The operator MUST complete the steps below manually
    (PR merge requires GitHub UI / gh CLI auth; ArgoCD sync requires cluster credentials).

    Operator manual steps (executed by human, NOT by Claude):

    Step 1 — Review the PR (should be exactly 1 file, 1 line — verify the diff in GitHub UI):
        gh pr view <PR-NUMBER> --repo tom333/my-kluster

    Step 2 — Merge the PR (squash recommended for atomic single-commit history):
        gh pr merge <PR-NUMBER> --repo tom333/my-kluster --squash --delete-branch

    Step 3 — Wait for ArgoCD to detect the change (~30-60s) then force-sync if needed:
        argocd app sync arrconf
        argocd app wait arrconf --health --timeout 180

    Step 4 — Verify the live CronJob spec shows v0.1.4 image (DISPOSITIVE):
        LIVE_IMAGE=$(kubectl get cronjob arrconf -n selfhost \
          -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}')
        echo "Live CronJob image: $LIVE_IMAGE"
        echo "$LIVE_IMAGE" | grep -q ':0.1.4$' || { echo "ERROR: image did not roll forward"; exit 1; }

    Step 5 — Unsuspend the CronJob:
        kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":false}}'
        kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'
        # Expected: "false" (or empty)

    Step 6 — Confirm via argocd app:
        argocd app get arrconf | grep -E "(Status|Health|Sync)"
        # Expected: Health: Healthy, Sync: Synced

    Step 7 — Spot-check next-fire time hasn't fired yet:
        kubectl get cronjob arrconf -n selfhost \
          -o jsonpath='{.status.lastScheduleTime}{"\n"}{.spec.schedule}{"\n"}'
        # Note: next CronJob fires every 4h per D-23. Plan 06 will create a forced smoke job manually.

    Operator escalation paths:
    - PR merge fails (conflict) → resolve in PR branch, push, retry
    - ArgoCD shows OutOfSync after sync → check `argocd app diff arrconf` for hidden drift
    - Live image doesn't match v0.1.4 → ArgoCD selfHeal may not be enabled; force `argocd app sync arrconf --force`
    - Cluster network issue blocks GHCR pull → ImagePullBackOff in next pod; verify Plan 04 anonymous-pull check first

    Resume to Claude: type "approved: PR merged, ArgoCD synced to v0.1.4, CronJob unsuspended"
    OR describe the failure (PR conflict / ArgoCD sync error / image rollout failure / cluster network issue).
  </action>
  <what-built>
    Task 5.1 opened a my-kluster PR with a single 1-line diff (`image.tag: 0.1.3 -> 0.1.4`) and suspended the CronJob to freeze cluster state during the merge window. This checkpoint pauses for operator review, merge, and ArgoCD sync verification before unsuspending.

    What's still needed from a human:
    1. Review the PR diff (1 file, 1 line — should be trivial to verify)
    2. Merge the PR
    3. Confirm ArgoCD picked up the change and shows arrconf Application Synced + Healthy with the new image
    4. Unsuspend the CronJob
    5. Resume signal back to Claude
  </what-built>
  <how-to-verify>
    Operator manual steps:

    1. Review the PR (should be 1 file, 1 line — verify the diff in GitHub UI):
       gh pr view <PR-NUMBER> --repo tom333/my-kluster

    2. Merge the PR (squash recommended for atomic single-commit history):
       gh pr merge <PR-NUMBER> --repo tom333/my-kluster --squash --delete-branch

    3. Wait for ArgoCD to detect the change (~30-60s) then force-sync if needed:
       argocd app sync arrconf
       argocd app wait arrconf --health --timeout 180

    4. Verify the live CronJob spec shows v0.1.4 image:
       LIVE_IMAGE=$(kubectl get cronjob arrconf -n selfhost \
         -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}')
       echo "Live CronJob image: $LIVE_IMAGE"
       echo "$LIVE_IMAGE" | grep -q ':0.1.4$' || { echo "ERROR: image did not roll forward"; exit 1; }

    5. Unsuspend the CronJob:
       kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":false}}'
       kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'
       # Expected: "false" (or empty)

    6. Confirm via argocd app:
       argocd app get arrconf | grep -E "(Status|Health|Sync)"
       # Expected: Health: Healthy, Sync: Synced

    7. Spot-check next-fire time hasn't fired yet:
       kubectl get cronjob arrconf -n selfhost \
         -o jsonpath='{.status.lastScheduleTime}{"\n"}{.spec.schedule}{"\n"}'
       # Note: next CronJob fires every 4h per D-23. Plan 06 will create a forced smoke job manually.

    Operator escalation paths:
    - PR merge fails (conflict) → resolve in PR branch, push, retry
    - ArgoCD shows OutOfSync after sync → check argocd app diff arrconf for hidden drift
    - Live image doesn't match v0.1.4 → ArgoCD selfHeal may not be enabled; force `argocd app sync arrconf --force`
    - Cluster network issue blocks GHCR pull → ImagePullBackOff in next pod; verify Plan 04 anonymous-pull check first
  </how-to-verify>
  <acceptance_criteria>
    - my-kluster PR shows state=MERGED: `gh pr view <PR-NUMBER> --repo tom333/my-kluster --json state --jq .state` returns `MERGED`
    - ArgoCD arrconf Application is Synced + Healthy: `argocd app get arrconf -o json | jq -e '.status.sync.status == "Synced" and .status.health.status == "Healthy"'` exits 0
    - Live CronJob spec image matches v0.1.4: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}' | grep -q ':0.1.4$'` exits 0
    - CronJob is unsuspended: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returns `false` or empty
  </acceptance_criteria>
  <verify>
    <automated>[ "$(gh pr list --repo tom333/my-kluster --head phase-2.2-arrconf-v0.1.4 --state merged --json number --jq '.[0].number')" != "null" -a -n "$(gh pr list --repo tom333/my-kluster --head phase-2.2-arrconf-v0.1.4 --state merged --json number --jq '.[0].number')" ] && kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}' | grep -q ':0.1.4$' && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')" != "true" ]</automated>
  </verify>
  <resume-signal>Type "approved: PR merged, ArgoCD synced to v0.1.4, CronJob unsuspended" OR describe what failed (PR conflict, ArgoCD sync error, image rollout failure, cluster network issue).</resume-signal>
  <done>my-kluster PR merged; ArgoCD synced + healthy on v0.1.4; live CronJob spec image = `ghcr.io/tom333/arr-stack-arrconf:0.1.4`; CronJob unsuspended; ready for Plan 06 cluster smoke + drift demo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| my-kluster main branch -> ArgoCD selfHeal -> live CronJob spec | The atomic 1-line diff is the integrity gate; smaller diff = easier review = lower risk of accidental drift |
| Operator workstation -> kubectl patch (suspend/unsuspend) | Trusted local action with a precise namespace + resource scope; reversible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-13 | Tampering | PR could include unintended changes (placeholder removal, template tweak) | mitigate | Task 5.1 Step 2 verifies `git diff --stat` shows exactly 1 file, 1 insertion, 1 deletion before push; explicit `git diff` checks on `files/arrconf.yml` and `templates/cronjob.yaml` MUST be empty |
| T-02.2-14 | Denial of Service | CronJob fires during merge window between bad transient state | mitigate | Task 5.1 Step 1 suspends CronJob BEFORE merge; Task 5.2 Step 5 unsuspends only AFTER ArgoCD sync confirmed. Phase 2.1 LEARNINGS pattern reused verbatim |
| T-02.2-15 | Elevation of Privilege | If GHCR image was hijacked, pulling :0.1.4 could run untrusted code | accept | Plan 04 Task 4.2 anonymous-pull verification gates this plan; the image was just verified to come from the GHCR namespace owned by `tom333`. cosign signing (OPSV2-03) is the future stronger guarantee |
| T-02.2-16 | Repudiation | Multiple PRs landing in close succession could confuse audit trail | mitigate | The PR title + commit message both reference `v0.1.4` and `D-02.1-06`. The branch name `phase-2.2-arrconf-v0.1.4` is also self-describing. Squash merge keeps a single audit anchor |
</threat_model>

<verification>
End-of-plan gate (Plan 05 complete when all pass):
1. CronJob was suspended before PR merge (Task 5.1)
2. PR was a single 1-line diff (Task 5.1)
3. PR was reviewed and merged by operator (Task 5.2 checkpoint)
4. ArgoCD synced + healthy with v0.1.4 image rolled forward
5. CronJob unsuspended after sync confirmed
6. Live CronJob spec shows the new image tag
</verification>

<success_criteria>
- D-37 atomic-PR pattern observed (single-line diff)
- Hotfix-window discipline observed (suspend → merge → sync → unsuspend)
- Phase 2.1 PR4 hotfix decision honored (placeholders stay)
- Cluster ready for Plan 06 smoke + drift demo
</success_criteria>

<output>
After completion, create `/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-05-SUMMARY.md` documenting:
- my-kluster PR number + merge timestamp + 1-line diff confirmation
- CronJob suspend/unsuspend timestamps (window duration)
- ArgoCD sync timestamp + final Health + Sync status
- Live CronJob image string (post-sync)
- Recommended next step: Plan 06 (cluster smoke + drift demo W-04 dispositive — closes REQ-drift-detection)
</output>
