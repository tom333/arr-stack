---
phase: 31-qbit-manage
plan: "02"
subsystem: helm-chart
tags: [helm, cronjob, qbit-manage, chart-lint, renovate]
dependency_graph:
  requires: ["31-01"]
  provides: ["QBM-03"]
  affects: ["charts/arr-stack", ".github/workflows/chart-lint.yml"]
tech_stack:
  added: []
  patterns: ["app-template 5.0.0 CronJob alias", "ConfigMap .Files.Get mount", "Helm 4 multi-alias workaround"]
key_files:
  created:
    - charts/arr-stack/templates/qbit-manage-configmap.yaml
  modified:
    - charts/arr-stack/Chart.yaml
    - charts/arr-stack/Chart.lock
    - charts/arr-stack/values.yaml
    - charts/arr-stack/values.schema.json
    - .github/workflows/chart-lint.yml
    - README.md
decisions:
  - "values.schema.json required a new qbit-manage entry (additionalProperties: true) to pass helm lint — schema had additionalProperties: false at root (auto-fix Rule 1)"
  - "Chart.lock regenerated via helm dependency update after adding 13th alias"
  - "arrconf.image.tag left at 0.20.0 — no Python changes in this Helm-only plan"
metrics:
  duration: "~15 minutes"
  completed: "2026-05-31"
  tasks_completed: 2
  files_changed: 6
---

# Phase 31 Plan 02: qbit-manage Helm Deployment Summary

qbit-manage deployed as the 13th app-template CronJob alias with run-once env (QBT_RUN/QBT_SCHEDULE), arrconf-env credential injection, and generated config.yml mounted read-only via ConfigMap.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | 13th Chart.yaml alias + ConfigMap template + values.yaml CronJob block | d862df0 | Chart.yaml, Chart.lock, values.yaml, values.schema.json, templates/qbit-manage-configmap.yaml |
| 2 | chart-lint.yml unpack loop + annotation guard (12→14) + README alias loop | 7103103 | .github/workflows/chart-lint.yml, README.md |

## What Was Built

**Chart.yaml** — added the 13th `app-template@5.0.0` dependency with `alias: qbit-manage`. Chart.lock regenerated.

**qbit-manage-configmap.yaml** — ConfigMap template that renders `files/qbit_manage/config.yml` (generated by Plan 31-01) via `.Files.Get "files/qbit_manage/config.yml"`. Named `qbit-manage-config` to match the values.yaml persistence mount.

**values.yaml** — `qbit-manage` CronJob block:
- Schedule `0 */4 * * *` (every 4 hours, same cadence as arrconf/configarr)
- `concurrencyPolicy: Forbid` prevents overlap
- `QBT_RUN: "true"` + `QBT_SCHEDULE: "0"` — run-once-and-exit mode (RESEARCH Pitfall 2 mitigated)
- `envFrom: arrconf-env` — injects QBT_USER/QBT_PASS for `!ENV` tag resolution in config.yml
- ConfigMap mount at `/config/config.yml` (qbit_manage Docker convention)
- Renovate annotation `# renovate: image=ghcr.io/stuffanthings/qbit_manage` above repository line
- Image pinned at `v4.6.6`
- arrconf.image.tag unchanged at `0.20.0`

**values.schema.json** — added `"qbit-manage": {"type": "object", "additionalProperties": true}` to allow the new CronJob block (schema had `additionalProperties: false` at root).

**chart-lint.yml** — unpack loop updated from 12 to 13 aliases (added `qbit-manage`); Renovate annotation guard bumped from 12 to 14 (confirmed 14 actual matches); step name updated to `>= 14 matches in values.yaml`.

**README.md** — local verification alias loop updated to include `qbit-manage`, comment updated to "13 aliases".

## Verification Results

- `helm lint charts/arr-stack/`: PASS (1 chart linted, 0 failed)
- `helm template | kubeconform -ignore-missing-schemas -`: PASS (exit 0)
- Rendered CronJob: `qbit-manage`, schedule `0 */4 * * *`, `QBT_RUN=true`, `QBT_SCHEDULE=0`, `envFrom: arrconf-env`, volume `qbit-manage-config`
- Renovate annotation synthetic test: 14 matches (>= 14 required): PASS
- All 19 acceptance criteria: PASS

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] values.schema.json missing qbit-manage entry**
- **Found during:** Task 1 verification (helm lint)
- **Issue:** `values.schema.json` has `additionalProperties: false` at the root level and explicitly lists each alias. Adding `qbit-manage` to `values.yaml` caused helm lint to fail with "additional properties 'qbit-manage' not allowed".
- **Fix:** Added `"qbit-manage": {"type": "object", "additionalProperties": true}` alongside the existing `cross-seed` entry in the schema.
- **Files modified:** `charts/arr-stack/values.schema.json`
- **Commit:** d862df0

**2. [Rule 3 - Blocking] Chart.lock out of sync after adding 13th alias**
- **Found during:** Task 1 verification (helm dependency build)
- **Issue:** `Chart.lock` was out of sync with the updated `Chart.yaml` after adding the qbit-manage dependency entry.
- **Fix:** Ran `helm dependency update charts/arr-stack/` to regenerate `Chart.lock`.
- **Files modified:** `charts/arr-stack/Chart.lock`
- **Commit:** d862df0

## Known Stubs

None — this plan wires the generated config.yml (from Plan 31-01) into the chart; no placeholder data.

## Threat Surface Scan

No new threat surface beyond what was documented in the plan's threat model. The ConfigMap contains no secrets (only `!ENV` references); secrets arrive at runtime via `envFrom: arrconf-env` (T-31-06 mitigated). The `cat_update: false` in the mounted config.yml ensures qbit_manage cannot write categories (T-31-08 mitigated).

## Self-Check

Checking created files exist:
- `charts/arr-stack/templates/qbit-manage-configmap.yaml`: FOUND
- `charts/arr-stack/Chart.yaml` (alias: qbit-manage): FOUND
- `charts/arr-stack/values.yaml` (qbit-manage block): FOUND

Checking commits exist:
- d862df0: FOUND
- 7103103: FOUND

## Self-Check: PASSED
