---
phase: 27-trash-cf-picker-recyclarr-reference
plan: 04
type: execute
wave: 4
depends_on: [27-03]
files_modified:
  - tools/arrconf-ui/web/src/lib/RecyclarrReferencePicker.svelte
  - tools/arrconf-ui/web/src/lib/AppSection.svelte
  - tools/arrconf-ui/web/src/App.svelte
autonomous: false
requirements: [CFGUI-05, CFGUI-06, CFGUI-08]

must_haves:
  truths:
    - "Each app's configarr section shows a read-only Recyclarr template dropdown with a copy-name button"
    - "The Recyclarr panel never writes include: or any config key"
    - "The CF, QP, and Recyclarr pickers are mounted in the configarr form (sonarr + radarr) and not in the arrconf form"
    - "Picker onChange flows through the existing AppSection onChange → PUT path with no tag corruption"
  artifacts:
    - path: "tools/arrconf-ui/web/src/lib/RecyclarrReferencePicker.svelte"
      provides: "Read-only Recyclarr template dropdown + clipboard copy"
      min_lines: 40
    - path: "tools/arrconf-ui/web/src/lib/AppSection.svelte"
      provides: "configarrMode gate + 3 picker mount points"
      contains: "configarrMode"
    - path: "tools/arrconf-ui/web/src/App.svelte"
      provides: "passes configarrMode + customFormatDefinitions to AppSection"
      contains: "configarrMode"
  key_links:
    - from: "tools/arrconf-ui/web/src/App.svelte"
      to: "tools/arrconf-ui/web/src/lib/AppSection.svelte"
      via: "configarrMode={true} on the configarr branch only"
      pattern: "configarrMode"
    - from: "tools/arrconf-ui/web/src/lib/AppSection.svelte"
      to: "TrashCFPicker / TrashQPPicker / RecyclarrReferencePicker"
      via: "{#if configarrMode && (sonarr|radarr)}"
      pattern: "RecyclarrReferencePicker"
---

<objective>
Build the read-only Recyclarr reference picker (CFGUI-06), wire all three pickers
into `AppSection.svelte` behind a `configarrMode` gate, pass the required props
(customFormatDefinitions, quality-profile names) from `App.svelte`, then a human
checkpoint to confirm the end-to-end UX + the MEDIUM-confidence QP field mapping.

Purpose: CFGUI-06 (Recyclarr reference-only), and the final integration that makes
SC#1/#3/#4/#5 observable in the running UI. The save path is the existing Phase 25
PUT — no tag corruption.
Output: 1 new component + 2 modified files + 1 verified human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-trash-cf-picker-recyclarr-reference/27-RESEARCH.md
@.planning/phases/27-trash-cf-picker-recyclarr-reference/27-PATTERNS.md
@.planning/phases/27-trash-cf-picker-recyclarr-reference/27-CONTEXT.md

<interfaces>
<!-- App.svelte configarr branch (lines 180-198) renders AppSection per section: -->
<!--   value={configState[sectionName]}  onChange={(next)=>updateAppSection(sectionName,next)} -->
<!--   activeConfig $state is 'arrconf' | 'configarr' -->
<!--   configState holds top-level customFormatDefinitions (configarr) -->

<!-- AppSection.svelte Props (lines 6-14): -->
<!--   sectionName, sectionSchema, root, value, onChange, errors -->
<!-- section value shape: { main: { quality_profiles:[...], custom_formats:[...] } } -->

<!-- RecyclarrReferencePicker is READ-ONLY: no onChange prop, no config write. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RecyclarrReferencePicker.svelte — read-only dropdown + copy-name (CFGUI-06)</name>
  <files>tools/arrconf-ui/web/src/lib/RecyclarrReferencePicker.svelte</files>
  <read_first>
    - tools/arrconf-ui/web/src/lib/HelpTooltip.svelte + SuggestArrBadge.svelte (read-only display + badge analogs) — 27-PATTERNS.md lines 638-706
    - tools/arrconf-ui/web/src/lib/FieldInput.svelte lines 378-382 (.lock-badge CSS)
    - 27-CONTEXT.md D-12, D-13 + research correction #1 (id-only, NO description field)
    - 27-RESEARCH.md "Recyclarr config-templates Structure" (lines 245-279) + Open Question 1 (lines 585-588)
  </read_first>
  <action>
    Create `tools/arrconf-ui/web/src/lib/RecyclarrReferencePicker.svelte` (Svelte 5 runes).

    Props: `{ app: 'sonarr' | 'radarr' }` ONLY. NO `onChange`, NO `existingProfiles` —
    this component NEVER writes to config state (D-13).

    Load in `$effect`: `getRecyclarrTemplates(app)` → `templates = $state<RecyclarrTemplateEntry[]>([])`.
    A `<select>` (or searchable list) bound to `selected = $state<RecyclarrTemplateEntry|null>(null)`.

    Display per research correction #1: there is NO description field. Show the `id` string
    as the primary label (the ids are self-descriptive, e.g.
    `sonarr-v4-quality-profile-bluray-web-1080p-french-multi-vf`). Show `template` (the
    relative path) as a greyed subtitle/`<code>` for reference. Add a header label:
    `RECYCLARR_REFERENCE_LABEL` ("Référence uniquement — aucun `include:` inséré.") + a
    `.lock-badge` 🔒 with the same title. Document the no-description deviation in a short
    code comment referencing research correction #1.

    The ONLY action (D-13): a "Copier le nom" button →
    `async function copyId(id: string) { await navigator.clipboard.writeText(id); }`.
    NO other button, NO config mutation, NO `include:` string ever written. The component
    has no `onChange` and imports nothing from the config write path.

    Style with CSS tokens only. Use `Spinner` while loading.
  </action>
  <verify>
    <automated>cd tools/arrconf-ui/web && npm run check && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd tools/arrconf-ui/web && npm run check && npm run typecheck && npm run build` all exit 0
    - `grep -c 'getRecyclarrTemplates' tools/arrconf-ui/web/src/lib/RecyclarrReferencePicker.svelte` returns >= 1
    - `grep -c 'navigator.clipboard.writeText' tools/arrconf-ui/web/src/lib/RecyclarrReferencePicker.svelte` returns 1
    - `grep -c 'onChange' tools/arrconf-ui/web/src/lib/RecyclarrReferencePicker.svelte` returns 0 (read-only, no write prop)
    - `grep -c 'include:' tools/arrconf-ui/web/src/lib/RecyclarrReferencePicker.svelte` returns 0 (never inserts include)
    - `grep -c 'RECYCLARR_REFERENCE_LABEL' tools/arrconf-ui/web/src/lib/RecyclarrReferencePicker.svelte` returns >= 1
    - `grep -c 'description' tools/arrconf-ui/web/src/lib/RecyclarrReferencePicker.svelte` returns 0 (no fabricated description — correction #1)
  </acceptance_criteria>
  <done>Recyclarr picker shows id-only entries with copy-name button, is read-only (no onChange/include:), labelled reference-only; quad gates pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire 3 pickers into AppSection + pass props from App.svelte (configarr only)</name>
  <files>tools/arrconf-ui/web/src/lib/AppSection.svelte, tools/arrconf-ui/web/src/App.svelte</files>
  <read_first>
    - tools/arrconf-ui/web/src/lib/AppSection.svelte (full — props + .section-body; 27-PATTERNS.md lines 364-432)
    - tools/arrconf-ui/web/src/App.svelte lines 160-198 (arrconf vs configarr branches; configState + updateAppSection)
    - tools/arrconf-ui/web/src/lib/TrashCFPicker.svelte + TrashQPPicker.svelte (Plan 03 — their Props contracts)
  </read_first>
  <action>
    AppSection.svelte — add optional prop `configarrMode?: boolean` (default `false`) per
    27-PATTERNS.md lines 384-393. Import the three picker components. After the existing
    `{#each instances}` block, inside `.section-body`, add the mount block (27-PATTERNS.md
    lines 397-415):
    ```svelte
    {#if configarrMode && (sectionName === 'sonarr' || sectionName === 'radarr')}
      <div class="picker-section">
        <TrashCFPicker
          app={sectionName as 'sonarr' | 'radarr'}
          existingCustomFormats={mainCustomFormats}
          localDefinitions={localDefs}
          profileNames={profileNames}
          onChange={(next) => updateMain('custom_formats', next)}
        />
        <TrashQPPicker
          app={sectionName as 'sonarr' | 'radarr'}
          existingProfiles={mainProfiles}
          onChange={(next) => updateMain('quality_profiles', next)}
        />
        <RecyclarrReferencePicker app={sectionName as 'sonarr' | 'radarr'} />
      </div>
    {/if}
    ```
    Derive helpers in the script block (the configarr section value is `{ main: {...} }`):
    - `const main = $derived((value?.main ?? {}) as Record<string, unknown>);`
    - `const mainCustomFormats = $derived((main.custom_formats ?? []) as CustomFormatEntry[]);`
    - `const mainProfiles = $derived((main.quality_profiles ?? []) as Record<string, unknown>[]);`
    - `const profileNames = $derived(mainProfiles.map(p => (p as {name?:string}).name).filter(Boolean) as string[]);`
    - `localDefs` prop (see App.svelte below) for customFormatDefinitions; add it as another
      optional prop `localDefinitions?: { trash_id: string; name: string }[]` defaulting `[]`.
    - `function updateMain(key, next) { onChange({ ...value, main: { ...main, [key]: next } }); }`
      — this reuses the EXISTING onChange → updateAppSection → PUT path (no new write path,
      no second YAML instance; ruyaml tag safety preserved by Phase 25's write).
    Add `.picker-section` CSS per 27-PATTERNS.md lines 419-429 (token-based).

    App.svelte — on the configarr branch ONLY (the `{:else}` block, lines ~180-197), pass
    two new props to `<AppSection>`:
    - `configarrMode={true}`
    - `localDefinitions={(configState.customFormatDefinitions ?? []) as { trash_id: string; name: string }[]}`
    Do NOT pass these on the arrconf branch (lines ~170-178) — pickers must NOT appear in
    the arrconf form (Pitfall: app-context / RESEARCH A3). `customFormatDefinitions` is a
    top-level configarr key, identical for sonarr+radarr; pass the same list to both.

    Verify ADR-5: no *arr URL anywhere. Verify the save still round-trips (the picker
    onChange only touches `main.custom_formats` / `main.quality_profiles`; tags live
    elsewhere and are untouched).
  </action>
  <verify>
    <automated>cd tools/arrconf-ui/web && npm run check && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd tools/arrconf-ui/web && npm run check && npm run typecheck && npm run build` all exit 0
    - `grep -c 'configarrMode' tools/arrconf-ui/web/src/lib/AppSection.svelte` returns >= 2 (prop + guard)
    - `grep -E 'TrashCFPicker|TrashQPPicker|RecyclarrReferencePicker' tools/arrconf-ui/web/src/lib/AppSection.svelte` matches all 3 imports/usages
    - `grep -c 'configarrMode={true}' tools/arrconf-ui/web/src/App.svelte` returns >= 1 (configarr branch only)
    - `grep -c 'customFormatDefinitions' tools/arrconf-ui/web/src/App.svelte` returns >= 1
    - The arrconf branch does NOT set configarrMode: `sed -n '160,180p' tools/arrconf-ui/web/src/App.svelte | grep -c configarrMode` returns 0
    - ADR-5: `grep -rE "8989|7878|9696|sonarr\.selfhost|radarr\.selfhost|prowlarr\.|//sonarr|//radarr" tools/arrconf-ui/web/src/` returns no *arr API URL
    - No new dep: `git diff --name-only HEAD | grep -c 'package.json\|package-lock.json'` returns 0
  </acceptance_criteria>
  <done>3 pickers mount in the configarr form (sonarr+radarr) behind configarrMode, receive correct props, write via the existing onChange path; not present in arrconf form; quad gates + ADR-5 grep pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — end-to-end picker UX + QP field-mapping confidence check</name>
  <action>Operator-driven verification in the running arrconf-ui — see what-built and how-to-verify. No code is written in this task; the operator exercises the CF/QP/Recyclarr pickers, confirms the 3 hand-rolled profiles are untouched, validates the MEDIUM-confidence QP field mapping, and confirms zero GitHub network calls (SC#2).</action>
  <what-built>
    The three TRaSH/Recyclarr pickers wired into the configarr form: CF searchable
    multi-select with add/remove + known/custom/unknown classification, QP add-as-new with
    collision block, Recyclarr read-only reference dropdown with copy-name. All backed by
    the baked catalog (no GitHub at runtime).
  </what-built>
  <how-to-verify>
    1. Start the UI locally: `cd tools/arrconf-ui && uv run uvicorn arrconf_ui.app:app --reload`
       in one terminal, and `cd tools/arrconf-ui/web && npm run dev` in another (or
       `npm run build` then serve via the backend StaticFiles mount). Open the printed URL.
    2. Switch the config selector to `configarr.yml`. Expand the `sonarr` section.
    3. CF picker (SC#1/#4): type "French" (or another name) in the CF search — confirm
       results filter by name. Tick 1-2 CFs, click "Ajouter les formats sélectionnés".
       Confirm each appears as a chip. Confirm the existing `fr-vff`/`fr-multi` style French
       CF entries show a "custom" badge (NOT a warning). If a fabricated unknown id exists,
       confirm it shows the ⚠ inconnu warning and is NOT removed on its own.
    4. QP picker (SC#5): pick a TRaSH quality profile by name. Try a name that collides with
       "MULTi.VF" — confirm the insert button is disabled and the collision warning shows.
       Rename to something unique, insert, and confirm a NEW entry appears AFTER the 3
       existing profiles (MULTi.VF / Anime / Family unchanged, same order).
    5. Recyclarr (SC#3): open the Recyclarr reference dropdown — confirm it lists template
       ids, shows the "Référence uniquement" label + 🔒, and "Copier le nom" copies the id.
       Confirm NO config change occurs from interacting with it.
    6. Save: click save. Open the diff preview — confirm only the new CF/QP entries appear
       as additions, the 3 hand-rolled profiles are untouched, and any `!env`/`!secret`
       tags elsewhere are intact (no tag drop).
    7. QP MAPPING CONFIDENCE (research correction #4, MEDIUM): inspect the saved
       `quality_profiles[]` entry — confirm `upgrade.until_quality` == TRaSH `cutoff`,
       `qualities[]` reflects the `items[allowed!=false]` set in the SAME order as the
       baked QP json (Feb-2026 ordering, not re-sorted). If the mapping looks wrong, report
       it — this is the one MEDIUM-confidence area in the phase.
    8. Network check (SC#2): with DevTools Network open during all of the above, confirm
       ZERO requests to github.com / raw.githubusercontent.com — only `/api/trash/*` and
       `/api/configarr/*` relative calls.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass, or describe issues (especially any QP mapping discrepancy in step 7).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ FastAPI (relative /api only) | LAN-trusted operator UI; no external host should ever be dialed |
| Recyclarr panel → clipboard | one-way copy of an id string; no config write |
| picker onChange → configarr.yml (PUT) | new entries persisted via Phase 25 tag-safe write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-13 | Tampering | Recyclarr panel inadvertently mutating config | mitigate | Component has no onChange prop; grep gate `onChange==0` and `include:==0`; clipboard-only action (D-13) |
| T-27-14 | XSS | clipboard / copied id reflected unsafely | accept | Copied value is a static catalog id string written to clipboard via `writeText` (no DOM injection); ids are `[a-z0-9-]` from baked data |
| T-27-15 | Spoofing (ADR-5) | *arr URL introduced during wiring | mitigate | Pickers call relative `/api/trash/*` only; grep gate across `web/src/` for *arr hosts returns none; step 8 network check confirms no external host |
| T-27-16 | Information disclosure | `!env`/`!secret` tag drop on save | mitigate | Picker onChange touches only `main.custom_formats`/`main.quality_profiles`; reuses Phase 25 ruyaml round-trip write; human step 6 confirms tags intact |
</threat_model>

<verification>
- Frontend quad passes for all modified files.
- Grep gates: Recyclarr is write-free; configarrMode only on configarr branch; no *arr URL in web/src.
- Human checkpoint: 8-step UX + QP-mapping confidence + network (no GitHub) confirmation.
</verification>

<success_criteria>
SC#3 (Recyclarr read-only reference, no include:), and the full integration making
SC#1/#4/#5 observable end-to-end. Human-verified UX + MEDIUM-confidence QP field
mapping confirmed. CFGUI-05/06/08 satisfied in the running UI.
</success_criteria>

<output>
After completion, create `.planning/phases/27-trash-cf-picker-recyclarr-reference/27-04-SUMMARY.md`
</output>
