---
phase: 30-cross-seed
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - .github/workflows/chart-lint.yml
  - README.md
  - .planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md
autonomous: true
requirements: [XSEED-03]
must_haves:
  truths:
    - "the CI multi-alias unpack step creates charts/arr-stack/charts/cross-seed from app-template so helm template resolves the 12th alias"
    - "the README local-verification alias loop includes cross-seed"
    - "the customManagers regex synthetic test threshold accounts for the 2 new cross-seed image matches"
    - "an operator runbook documents the cross-seed-config PVC + host directory pre-reqs and the out-of-stack teardown"
  artifacts:
    - path: ".github/workflows/chart-lint.yml"
      provides: "cross-seed alias unpack + updated image-count threshold"
      contains: "cross-seed"
    - path: "README.md"
      provides: "cross-seed in local alias loop"
      contains: "cross-seed"
    - path: ".planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md"
      provides: "PVC + host dir pre-reqs + teardown steps"
      contains: "cross-seed-config"
  key_links:
    - from: ".github/workflows/chart-lint.yml"
      to: "charts/arr-stack/charts/cross-seed"
      via: "cp -r app-template"
      pattern: "charts/cross-seed"
---

<objective>
Close the CI + operator surface for XSEED-03: make chart-lint.yml resolve the new 12th alias
(Helm 4 multi-alias unpack workaround), update the README local-verification loop, raise the
renovate synthetic-test image-count threshold for the 2 new cross-seed images, and deliver the
operator runbook for the cluster pre-reqs (PVC + host dir + secret keys) and out-of-stack teardown.

Purpose: Plan 02 adds the alias to Chart.yaml + values.yaml, but CI helm template may fail for
the unresolved cross-seed chart dir unless the vendor step copies app-template to it, and the
renovate synthetic count guard must not under-count. The deployment also depends on operator
pre-reqs (PVC, host dir, secret keys) that arr-stack does not automate.

Output: CI step + README loop updated, operator runbook created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cross-seed/30-CONTEXT.md
@.planning/phases/30-cross-seed/30-RESEARCH.md
@.planning/phases/30-cross-seed/30-PATTERNS.md
@.planning/phases/30-cross-seed/30-02-SUMMARY.md

<interfaces>
CI vendor step (chart-lint.yml lines 40-47): currently only unpacks the tarball; has NO
per-alias copy loop. RESEARCH lines 339-352 + PATTERNS lines 343-361: adding the explicit
cp -r for cross-seed is the safe, idempotent fix.

chart-lint.yml synthetic guard (line ~129): "if total_matches < 10:" must become "< 12"
(cross-seed adds 2 renovate-tracked repository lines — main container + initContainer).

README local loop (line ~184):
"for alias in sonarr radarr prowlarr qbittorrent cleanuparr seerr flaresolverr jellyfin suggestarr arrconf configarr; do"
must append "cross-seed".

Operator pre-reqs (RESEARCH Environment Availability + Out-of-Stack Replacement Runbook):
- cross-seed-config PVC must exist in my-kluster BEFORE ArgoCD sync (existingClaim).
- host dir /media/data/torrents/cross-seed must exist (mkdir -p) for linkDirs hardlinks.
- arrconf-env SealedSecret must contain QBT_USER, QBT_PASS, PROWLARR_API_KEY (operator adds missing keys in a separate my-kluster PR before merge).
- optional: copy old config.db to the new PVC to preserve search history; stop the out-of-stack instance.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update chart-lint.yml (alias unpack + image-count threshold) and README local loop</name>
  <files>.github/workflows/chart-lint.yml, README.md</files>
  <read_first>
    - .github/workflows/chart-lint.yml lines 40-47 (vendor step) and lines 108-133 (customManagers synthetic test)
    - README.md lines 169-190 (Verification locale alias loop)
    - .planning/phases/30-cross-seed/30-PATTERNS.md (chart-lint.yml section lines 343-364)
    - .planning/phases/30-cross-seed/30-RESEARCH.md (CI alias unpack lines 330-352; Pitfall 2 lines 410-420)
  </read_first>
  <action>
    1. In .github/workflows/chart-lint.yml, in the "Vendor app-template (Helm 4 multi-alias workaround)" step, after the existing tar line and the "test -f ... app-template/Chart.yaml" line, add an explicit per-alias copy loop so helm resolves the 12th alias. The loop iterates: sonarr radarr prowlarr qbittorrent cleanuparr seerr flaresolverr jellyfin suggestarr arrconf configarr cross-seed, and for each runs `[ ! -d "charts/arr-stack/charts/$alias" ] && cp -r charts/arr-stack/charts/app-template "charts/arr-stack/charts/$alias"`. Also update the comment "so all 10 aliases find their chart copy" to "so all 12 aliases find their chart copy".

    2. In the "customManagers regex synthetic test" step, change every "10" floor reference to "12": the step name "(>= 10 matches in values.yaml)" to "(>= 12 matches in values.yaml)"; the condition "if total_matches < 10:" to "if total_matches < 12:"; the error print "expected >= 10 image matches" to "expected >= 12 image matches"; the OK print "image matches (>= 10 required)" to "image matches (>= 12 required)". Rationale: cross-seed adds 2 renovate-tracked images (main container + initContainer, both ghcr.io/cross-seed/cross-seed).

    3. In README.md, update the local-verification alias loop (line ~184): change "for alias in sonarr radarr prowlarr qbittorrent cleanuparr seerr flaresolverr jellyfin suggestarr arrconf configarr; do" to append " cross-seed" before "; do". Update the comment "11 aliases du meme chart" to "12 aliases du meme chart" if present (line ~182).
  </action>
  <verify>
    <automated>grep -q "configarr cross-seed; do" .github/workflows/chart-lint.yml && grep -q "total_matches < 12" .github/workflows/chart-lint.yml && grep -q "configarr cross-seed; do" README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "configarr cross-seed; do" .github/workflows/chart-lint.yml == 1
    - grep -c "total_matches < 12" .github/workflows/chart-lint.yml == 1
    - grep -c "total_matches < 10" .github/workflows/chart-lint.yml == 0
    - grep -c "configarr cross-seed; do" README.md == 1
    - the CI loop body copies app-template to charts/arr-stack/charts/cross-seed for the cross-seed alias
  </acceptance_criteria>
  <done>CI vendor step copies app-template to charts/arr-stack/charts/cross-seed, image-count threshold is 12, README loop includes cross-seed.</done>
</task>

<task type="auto">
  <name>Task 2: Create operator runbook for PVC + host dir pre-reqs and out-of-stack teardown</name>
  <files>.planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/30-cross-seed/30-RESEARCH.md (Environment Availability lines 642-654; Out-of-Stack Replacement Runbook lines 590-599)
    - .planning/phases/30-cross-seed/30-PATTERNS.md (Key Constraints lines 424-441 — PVC + host dir pre-reqs)
    - CLAUDE.md "Integration avec my-kluster" section (PVC + SealedSecret bootstrap, never deploy from this repo)
  </read_first>
  <action>
    Create .planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md documenting the manual cluster prerequisites and teardown (operator-driven; arr-stack does NOT automate them). Include these sections and exact commands:

    Section "Pre-reqs BEFORE ArgoCD sync (my-kluster side)":
    1. Verify the arrconf-env SealedSecret carries PROWLARR_API_KEY, QBT_USER, QBT_PASS. If any key is missing, add it in a separate my-kluster SealedSecret PR and merge it BEFORE the arr-stack PR (SuggestArr D-02 precedent).
    2. Create the cross-seed-config PVC in the selfhost namespace (ReadWriteOnce, size ~1Gi) — the cross-seed alias mounts it via existingClaim cross-seed-config and starts empty.
    3. Create the host directory for hardlinks on the MicroK8s node with the command: mkdir -p /media/data/torrents/cross-seed (linkDirs /data/torrents/cross-seed resolves under the hostPath /media/data/torrents to /data).

    Section "Verify the consolidated deployment (post-sync, Success Criterion 2)":
    4. kubectl -n selfhost get pod -l app.kubernetes.io/name=cross-seed shows Running (initContainer config-init completed).
    5. kubectl -n selfhost logs deploy/cross-seed shows cross-seed authenticated to the configured torznab with NO auth error (confirms PROWLARR_API_KEY / QBT_PASS were resolved by the initContainer).
    6. Optionally confirm no unresolved tokens remain inside the pod (ephemeral emptyDir): kubectl -n selfhost exec deploy/cross-seed -- cat /config/config.js, then check it contains no dollar-brace tokens.

    Section "Out-of-stack teardown (after consolidated instance verified)":
    7. Optional — preserve search history: copy the old instance config.db into the new PVC at /config/config.db before first heavy use.
    8. Stop the old out-of-stack instance: docker stop cross-seed (or docker-compose down cross-seed) on the host.
    9. This teardown is an operator action — arr-stack does NOT automate it (ADR-10 deployer boundary; never deploy/teardown from this repo).

    Section "Rollback":
    10. If the consolidated instance misbehaves: scale deploy/cross-seed to 0 in cluster and restart the out-of-stack instance; investigate logs; no data loss since the PVC is dedicated and the old config.db is untouched.
  </action>
  <verify>
    <automated>test -f .planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md && grep -q "cross-seed-config" .planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md && grep -q "media/data/torrents/cross-seed" .planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md && grep -q "arrconf-env" .planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md exists
    - it contains "cross-seed-config" (PVC pre-req)
    - it contains "mkdir -p /media/data/torrents/cross-seed" (host dir pre-req)
    - it contains "arrconf-env" with PROWLARR_API_KEY/QBT_USER/QBT_PASS key-presence check
    - it contains the out-of-stack teardown step (docker stop cross-seed) and the rollback step
  </acceptance_criteria>
  <done>Operator runbook documents PVC + host dir + secret-key pre-reqs, post-sync verification, out-of-stack teardown, and rollback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow (chart-lint.yml) | Read-only validation; no secrets handled; vendor step operates on public app-template chart |
| Operator runbook (documentation) | Describes manual cluster actions; references secret KEY NAMES only, never values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09 | Information Disclosure | operator runbook in git | mitigate | Runbook references secret key NAMES (PROWLARR_API_KEY, QBT_PASS) only; the verification step instructs checking for unresolved tokens, never printing resolved secret values into any committed log |
| T-30-10 | Tampering | CI alias unpack copies wrong chart | accept | cp -r sources only the verified app-template (Chart.yaml pinned 5.0.0); idempotent guard `[ ! -d ... ]` prevents clobbering an existing dir |
| T-30-11 | Denial of Service | missing PVC / host dir at sync time | mitigate | Runbook pre-reqs (PVC + mkdir + secret keys) are listed as BEFORE-sync gates; failure surfaces as a clear Pending pod, not silent secret leakage |
</threat_model>

<verification>
- grep "configarr cross-seed; do" .github/workflows/chart-lint.yml returns the updated loop.
- grep "total_matches < 12" .github/workflows/chart-lint.yml matches; "total_matches < 10" returns nothing.
- grep "configarr cross-seed; do" README.md matches.
- .planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md exists with PVC + host dir + secret-key pre-reqs + teardown + rollback.
- No secret value appears in any modified file (only key names).
</verification>

<success_criteria>
XSEED-03 (CI + operator surface): CI resolves the 12th alias and validates the 2 new images;
README local workflow includes cross-seed; the operator has a runbook for the PVC + host dir +
secret-key pre-reqs and the out-of-stack teardown enabling Success Criterion 3 (single ArgoCD
sync replaces the out-of-stack instance).
</success_criteria>

<output>
After completion, create `.planning/phases/30-cross-seed/30-03-SUMMARY.md`
</output>
