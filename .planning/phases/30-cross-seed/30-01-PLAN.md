---
phase: 30-cross-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - charts/arr-stack/files/intent.yml
  - charts/arr-stack/files/cross-seed/config.js
  - tools/arrconf/tests/test_generate_cross_seed.py
  - charts/arr-stack/values.yaml
autonomous: true
requirements: [XSEED-01, XSEED-02]
must_haves:
  truths:
    - "intent.yml tools.cross_seed carries distinct env tokens ${PROWLARR_API_KEY} and ${QBT_PASS}, not the shared PLACEHOLDER"
    - "arrconf generate emits a cross-seed/config.js that preserves the ${...} tokens verbatim"
    - "arrconf generate --check exits 0 (committed config.js matches regenerated output — CI reproducibility gate green)"
    - "the cross-seed generator test suite passes against the new token strings"
    - "arrconf.image.tag is co-bumped 0.19.0 -> 0.19.1 in the same commit as the tools/arrconf/** test change"
  artifacts:
    - path: "charts/arr-stack/files/intent.yml"
      provides: "hand-edited source with distinct secret tokens"
      contains: "${PROWLARR_API_KEY}"
    - path: "charts/arr-stack/files/cross-seed/config.js"
      provides: "regenerated config.js with module.exports literal carrying the tokens"
      contains: "module.exports = {"
    - path: "tools/arrconf/tests/test_generate_cross_seed.py"
      provides: "token-distinction assertion"
      contains: "${PROWLARR_API_KEY}"
    - path: "charts/arr-stack/values.yaml"
      provides: "co-bumped arrconf image tag"
      contains: "0.19.1"
  key_links:
    - from: "charts/arr-stack/files/intent.yml"
      to: "charts/arr-stack/files/cross-seed/config.js"
      via: "arrconf generate"
      pattern: "PROWLARR_API_KEY"
---

<objective>
Close out XSEED-01 / XSEED-02 by replacing the ambiguous shared `PLACEHOLDER` in the
cross-seed intent source with two *distinct* env-substitution tokens — `${PROWLARR_API_KEY}`
in the torznab URL and `${QBT_USER}` / `${QBT_PASS}` in the qBittorrent client string — then
regenerate the committed `config.js` and update the generator test that hard-codes `PLACEHOLDER`.

Purpose: the deploy-time initContainer (Plan 02) substitutes secrets by token name. A single
shared `PLACEHOLDER` cannot be expanded to two different secret values, so the tokens MUST be
distinct before any Helm wiring can work. This is the foundational generator-side change.

Output: updated `intent.yml`, regenerated `cross-seed/config.js`, updated test, and the
mandatory `arrconf.image.tag` co-bump (test file under `tools/arrconf/**`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cross-seed/30-CONTEXT.md
@.planning/phases/30-cross-seed/30-RESEARCH.md
@.planning/phases/30-cross-seed/30-PATTERNS.md

<interfaces>
<!-- generate_cross_seed is a pure function; NO code change is needed in intent.py. -->
<!-- json.dumps preserves ${...} unescaped (verified: only " and \ are escaped). -->

From tools/arrconf/arrconf/generators/intent.py:
```python
def generate_cross_seed(cfg: CrossSeedConfig) -> str:
    # data["torznab"], data["torrentClients"], data["linkDirs"] set from cfg
    body = json.dumps(data, indent="\t", sort_keys=True, ensure_ascii=False)
    return f"{_HEADER}module.exports = {body};\n"
```

CLI: `python -m arrconf generate` regenerates `charts/arr-stack/files/cross-seed/config.js`.
`python -m arrconf generate --check` is the CI drift gate (exit 0 = no drift, exit 1 = drift).

Co-bump anchor — charts/arr-stack/values.yaml line ~451:
```yaml
          image:
            # renovate: image=ghcr.io/tom333/arr-stack-arrconf
            repository: ghcr.io/tom333/arr-stack-arrconf
            tag: "0.19.0"   # <- bump to "0.19.1"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap PLACEHOLDER for distinct env tokens in intent.yml and regenerate config.js</name>
  <files>charts/arr-stack/files/intent.yml, charts/arr-stack/files/cross-seed/config.js</files>
  <read_first>
    - charts/arr-stack/files/intent.yml (the file being edited)
    - charts/arr-stack/files/cross-seed/config.js (the generated artifact to be regenerated)
    - tools/arrconf/arrconf/generators/intent.py (confirm NO code change — json.dumps passes ${} through)
    - .planning/phases/30-cross-seed/30-PATTERNS.md (intent.yml token-update section, lines 232-264)
    - .planning/phases/30-cross-seed/30-RESEARCH.md (Generator Tweak D-01, lines 293-313; Open Question 2 on QBT_USER, lines 624-627)
  </read_first>
  <behavior>
    - After edit, intent.yml torznab entry ends with `apikey=${PROWLARR_API_KEY}` (no `PLACEHOLDER`)
    - After edit, intent.yml torrent_clients entry is `qbittorrent:http://${QBT_USER}:${QBT_PASS}@qbittorrent.selfhost.svc.cluster.local:8080` (no `PLACEHOLDER`, no hard-coded `admin`)
    - After `arrconf generate`, config.js contains both `${PROWLARR_API_KEY}` and `${QBT_PASS}` and `${QBT_USER}`, the word `PLACEHOLDER` is absent
    - config.js still starts with the GENERATED header and `module.exports = {`
  </behavior>
  <action>
    In `charts/arr-stack/files/intent.yml`, under `tools.cross_seed`, replace the two PLACEHOLDER lines verbatim (per D-01 + RESEARCH Open Question 2 — use ${QBT_USER} not hard-coded admin since QBT_USER exists in arrconf-env):

    torznab line:
      FROM: `      - "http://prowlarr.selfhost.svc.cluster.local:9696/1/api?apikey=PLACEHOLDER"`
      TO:   `      - "http://prowlarr.selfhost.svc.cluster.local:9696/1/api?apikey=${PROWLARR_API_KEY}"`

    torrent_clients line:
      FROM: `      - "qbittorrent:http://admin:PLACEHOLDER@qbittorrent.selfhost.svc.cluster.local:8080"`
      TO:   `      - "qbittorrent:http://${QBT_USER}:${QBT_PASS}@qbittorrent.selfhost.svc.cluster.local:8080"`

    Leave `link_dirs: ["/data/torrents/cross-seed"]`, `link_type: hardlink`, `action: inject`, and `sagas: []` unchanged.

    Then regenerate the committed artifact (do NOT hand-edit config.js):
      `cd tools/arrconf && python -m arrconf generate --intent ../../charts/arr-stack/files/intent.yml --output-dir ../../charts/arr-stack/files`
    (If the CLI flag names differ, read the `generate` command signature in `tools/arrconf/arrconf/__main__.py` near line 1018 — the command writes `<output-dir>/cross-seed/config.js`.)

    NO change to `tools/arrconf/arrconf/generators/intent.py` — `json.dumps` preserves `${...}` verbatim (verified: only `"` and `\` are JSON-escaped).
  </action>
  <verify>
    <automated>grep -q 'apikey=${PROWLARR_API_KEY}' charts/arr-stack/files/intent.yml && grep -q '${QBT_USER}:${QBT_PASS}' charts/arr-stack/files/intent.yml && ! grep -q PLACEHOLDER charts/arr-stack/files/intent.yml && grep -q '${PROWLARR_API_KEY}' charts/arr-stack/files/cross-seed/config.js && ! grep -q PLACEHOLDER charts/arr-stack/files/cross-seed/config.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep PLACEHOLDER charts/arr-stack/files/intent.yml` returns nothing
    - `grep PLACEHOLDER charts/arr-stack/files/cross-seed/config.js` returns nothing
    - `grep -c '${PROWLARR_API_KEY}' charts/arr-stack/files/cross-seed/config.js` >= 1
    - `grep -c '${QBT_PASS}' charts/arr-stack/files/cross-seed/config.js` >= 1
    - config.js first line is `// GENERATED by 'arrconf generate' from intent.yml — DO NOT EDIT BY HAND`
    - `cd tools/arrconf && python -m arrconf generate --intent ../../charts/arr-stack/files/intent.yml --output-dir ../../charts/arr-stack/files --check` exits 0 (no drift between committed config.js and regenerated output)
  </acceptance_criteria>
  <done>intent.yml carries distinct tokens, config.js is regenerated to match, the drift `--check` gate is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update generator test for new token + co-bump arrconf.image.tag</name>
  <files>tools/arrconf/tests/test_generate_cross_seed.py, charts/arr-stack/values.yaml</files>
  <read_first>
    - tools/arrconf/tests/test_generate_cross_seed.py (the test being edited — `test_generate_cross_seed_minimal`)
    - .planning/phases/30-cross-seed/30-PATTERNS.md (test update section, lines 319-340)
    - CLAUDE.md §"Release pin co-bump pattern" (test file change is a tools/arrconf/** change → MUST co-bump in same commit)
    - charts/arr-stack/values.yaml lines 448-452 (arrconf image block)
  </read_first>
  <behavior>
    - test_generate_cross_seed_minimal asserts against `${PROWLARR_API_KEY}` token, not `PLACEHOLDER`
    - All 5 cross-seed generator tests pass (deterministic, sort_keys, omits_empty, camelcase unchanged)
    - mypy + ruff triad stays green for tools/arrconf
  </behavior>
  <action>
    In `tools/arrconf/tests/test_generate_cross_seed.py`, line 17, change the `CrossSeedConfig` torznab URL in `test_generate_cross_seed_minimal`:
      FROM: `cfg = CrossSeedConfig(torznab=["http://prowlarr.test/1/api?apikey=PLACEHOLDER"])`
      TO:   `cfg = CrossSeedConfig(torznab=["http://prowlarr.test/1/api?apikey=${PROWLARR_API_KEY}"])`
    Do NOT touch `test_generate_cross_seed_deterministic`, `_sort_keys`, `_omits_empty`, `_camelcase` (none reference PLACEHOLDER).

    Co-bump (CLAUDE.md release-pin rule — this commit touches tools/arrconf/**): in `charts/arr-stack/values.yaml` change the arrconf image tag (under the `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` block, ~line 451):
      FROM: `            tag: "0.19.0"`
      TO:   `            tag: "0.19.1"`
    This is a patch bump (token-emission correction, per RESEARCH Pitfall 7). Do NOT remove the renovate annotation line.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf && uv run pytest tests/test_generate_cross_seed.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep PLACEHOLDER tools/arrconf/tests/test_generate_cross_seed.py` returns nothing
    - `grep -c '${PROWLARR_API_KEY}' tools/arrconf/tests/test_generate_cross_seed.py` >= 1
    - `grep 'tag: "0.19.1"' charts/arr-stack/values.yaml` matches under the arrconf-arrconf image block
    - `grep -c 'tag: "0.19.0"' charts/arr-stack/values.yaml` == 0
    - `cd tools/arrconf && uv run pytest tests/test_generate_cross_seed.py -q` reports all 5 tests passing
    - `cd tools/arrconf && uv run mypy arrconf` exits 0
  </acceptance_criteria>
  <done>Generator test passes against the new token, arrconf image tag co-bumped to 0.19.1 in the same change set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| intent.yml (committed) → config.js (committed) → git | Both files are version-controlled and CI-reproduced; NO real secret may cross into either |
| operator hand-edit → generator | intent.yml is validated by pydantic CrossSeedConfig (extra="forbid") |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | config.js committed to git | mitigate | config.js carries ONLY `${PROWLARR_API_KEY}` / `${QBT_PASS}` / `${QBT_USER}` literal tokens — never resolved values. Acceptance grep asserts `PLACEHOLDER` absent AND no real-looking secret present; real values are injected at runtime in Plan 02 only |
| T-30-02 | Tampering | hand-edited intent.yml | mitigate | pydantic CrossSeedConfig with extra="forbid" rejects unknown fields; `generate --check` CI gate detects any drift between source and committed artifact |
| T-30-03 | Repudiation | generator non-determinism | accept | `json.dumps(sort_keys=True)` guarantees byte-identical output; `test_generate_cross_seed_deterministic` enforces it |
</threat_model>

<verification>
- `grep -r PLACEHOLDER charts/arr-stack/files/intent.yml charts/arr-stack/files/cross-seed/config.js` returns nothing.
- `cd tools/arrconf && python -m arrconf generate --intent ../../charts/arr-stack/files/intent.yml --output-dir ../../charts/arr-stack/files --check` exits 0.
- `cd tools/arrconf && uv run pytest tests/test_generate_cross_seed.py -q` all pass.
- `charts/arr-stack/values.yaml` arrconf image tag is `0.19.1`.
</verification>

<success_criteria>
XSEED-01: operator declares `tools.cross_seed` with distinct tokens in intent.yml — DONE.
XSEED-02: `arrconf generate` emits a valid `cross-seed/config.js` with `module.exports = {...}` carrying the tokens, drift gate green — DONE.
Co-bump rule satisfied (arrconf 0.19.0 → 0.19.1 in the same commit as the test change).
</success_criteria>

<output>
After completion, create `.planning/phases/30-cross-seed/30-01-SUMMARY.md`
</output>
