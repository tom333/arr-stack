---
phase: 28-generate-foundation
plan: 03
type: execute
wave: 3
depends_on: [28-01, 28-02]
files_modified:
  - tools/arrconf/arrconf/__main__.py
  - tools/arrconf/tests/test_generate_cmd.py
  - tools/arrconf/tests/test_cli.py
  - charts/arr-stack/values.yaml
autonomous: true
requirements: [INTENT-02, INTENT-03]
must_haves:
  truths:
    - "arrconf generate writes charts/arr-stack/files/cross-seed/config.js from intent.yml"
    - "arrconf generate --check writes nothing and exits 1 on drift, 0 when in sync"
    - "arrconf generate exits 2 on missing/invalid intent.yml (ConfigError)"
    - "generate is decoupled from apply (apply never runs generate)"
  artifacts:
    - path: "tools/arrconf/arrconf/__main__.py"
      provides: "generate @app.command with --intent/--output-dir/--check"
      contains: "def generate"
    - path: "tools/arrconf/tests/test_generate_cmd.py"
      provides: "CLI tests: write, --check pass, --check drift, missing-intent exit 2"
      contains: "generate"
  key_links:
    - from: "tools/arrconf/arrconf/__main__.py"
      to: "tools/arrconf/arrconf/generators/intent.py"
      via: "generate subcommand calls generate_cross_seed"
      pattern: "generate_cross_seed"
    - from: "tools/arrconf/arrconf/__main__.py"
      to: "tools/arrconf/arrconf/intent_config.py"
      via: "generate subcommand calls load_intent"
      pattern: "load_intent"
---

<objective>
Wire the `generate` CLI subcommand into `__main__.py`: bare `arrconf generate` writes the generated `config.js` under `charts/arr-stack/files/cross-seed/`; `--check` writes nothing and exits 1 on drift (the CI/idempotence realization of INTENT-03, D-07); `ConfigError` → exit 2. Decoupled from `apply` (D-06). Update the CLI tests including the help-list assertion.

Purpose: This is the wiring task — it connects the contract (Plan 01) and the generator (Plan 02) to a single CLI code path the operator and CI both invoke.
Output: `generate` subcommand, CLI tests, updated help-list test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-generate-foundation/28-CONTEXT.md
@.planning/phases/28-generate-foundation/28-PATTERNS.md
@.planning/phases/28-generate-foundation/28-01-SUMMARY.md
@.planning/phases/28-generate-foundation/28-02-SUMMARY.md

<interfaces>
<!-- Consumes Plan 01 + Plan 02 contracts. -->
From arrconf.intent_config: load_intent(path) -> IntentConfig ; IntentConfig.tools.cross_seed: CrossSeedConfig | None
From arrconf.generators.intent: generate_cross_seed(cfg: CrossSeedConfig) -> str

generate subcommand contract:
  arrconf generate [--intent PATH=charts/arr-stack/files/intent.yml] [--output-dir PATH=charts/arr-stack/files/] [--check]
  exit 0 = wrote files OR --check found no drift
  exit 1 = --check drift detected
  exit 2 = ConfigError (missing/invalid intent.yml)
  writes: {output_dir}/cross-seed/config.js
</interfaces>

<analog-source>
schema-gen @app.command pattern: tools/arrconf/arrconf/__main__.py lines 819-833.
apply error-handling (ConfigError → exit 2): __main__.py ~204-212.
CliRunner test idiom + help-list test: tools/arrconf/tests/test_cli.py (lines 1-34, esp. line 33 the four-subcommand assertion).
schema-gen CLI test: test_cli.py lines 119-125.
Full generate skeleton: 28-PATTERNS.md lines 272-313 (use verbatim).
</analog-source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generate @app.command to __main__.py</name>
  <files>tools/arrconf/arrconf/__main__.py</files>
  <read_first>
    - tools/arrconf/arrconf/__main__.py (READ lines 819-833 for the schema-gen command pattern; READ ~204-212 for the apply ConfigError→exit 2 handling; confirm where imports live at top and where commands are defined relative to `if __name__ == "__main__":`)
    - tools/arrconf/arrconf/intent_config.py (load_intent signature, Plan 01)
    - tools/arrconf/arrconf/generators/intent.py (generate_cross_seed signature, Plan 02)
    - .planning/phases/28-generate-foundation/28-PATTERNS.md (lines 272-313 — the verbatim generate skeleton)
  </read_first>
  <behavior>
    - generate --help lists --intent, --output-dir, --check
    - bare generate with a valid intent writes {output_dir}/cross-seed/config.js (creating cross-seed/ if absent), exit 0
    - generate --check with committed file == rendered exits 0, writes nothing
    - generate --check with committed file != rendered (or absent) exits 1, writes nothing
    - generate with missing intent.yml exits 2
    - when intent.tools.cross_seed is None, generate is a no-op exit 0 (nothing to render)
  </behavior>
  <action>
Add imports near the existing imports in `__main__.py`: `from arrconf.intent_config import load_intent` and `from arrconf.generators.intent import generate_cross_seed`. (ConfigError and typer/structlog/Path are already imported — confirm and reuse.)

Add a new `@app.command()` function named `generate` immediately after `schema_gen_cmd` and before `if __name__ == "__main__":`. Use the verbatim skeleton from 28-PATTERNS.md lines 272-313:
- `intent: Path = typer.Option(Path("charts/arr-stack/files/intent.yml"), "--intent", "-i", help="Path to intent.yml (hand-edited source of truth).")`
- `output_dir: Path = typer.Option(Path("charts/arr-stack/files/"), "--output-dir", "-o", help="Directory for generated output files (co-located with arrconf.yml).")`
- `check: bool = typer.Option(False, "--check", help="Verify committed files match intent; exit 1 on drift (CI mode).")`
- Docstring: "Generate committed configs from intent.yml. Use --check in CI (INTENT-02/INTENT-03, D-06/D-07)."
- Body: `load_intent(intent)` wrapped in try/except ConfigError → `log.error("intent_config_error", error=str(e)); raise typer.Exit(code=2) from e`. Then `drift = False`; if `intent_cfg.tools.cross_seed is not None`: `rendered = generate_cross_seed(intent_cfg.tools.cross_seed)`; `target = output_dir / "cross-seed" / "config.js"`. In `--check` mode: if `not target.exists() or target.read_text(encoding="utf-8") != rendered`: `log.error("generate_drift", file=str(target)); drift = True` else `log.info("generate_ok", ...)`. Else (write mode): `target.parent.mkdir(parents=True, exist_ok=True); target.write_text(rendered, encoding="utf-8"); log.info("generate_written", ...)`. End: `raise typer.Exit(code=1 if drift else 0)`.

DO NOT call generate from the apply command — apply stays decoupled (D-06). The `if intent_cfg.tools.cross_seed is not None` guard narrows the `CrossSeedConfig | None` type so mypy accepts the call.

This file is under `tools/arrconf/**` → co-bump applies (Task 3).
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run python -c "from arrconf.__main__ import app" && uv run arrconf generate --help</automated>
  </verify>
  <acceptance_criteria>
    - `arrconf generate --help` exits 0 and output contains `--intent`, `--output-dir`, `--check`
    - `grep -q "def generate" tools/arrconf/arrconf/__main__.py`
    - `grep -q "generate_cross_seed" tools/arrconf/arrconf/__main__.py`
    - `grep -q 'raise typer.Exit(code=2)' tools/arrconf/arrconf/__main__.py` (ConfigError path present)
  </acceptance_criteria>
  <done>generate subcommand registered with --intent/--output-dir/--check; ConfigError→exit 2; decoupled from apply.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI tests for generate (test_generate_cmd.py) + update help-list test</name>
  <files>tools/arrconf/tests/test_generate_cmd.py, tools/arrconf/tests/test_cli.py</files>
  <read_first>
    - tools/arrconf/tests/test_cli.py (READ lines 1-34 — CliRunner setup + the help-list test at ~line 33 asserting ["apply","dump","diff","schema-gen"]; lines 119-125 schema-gen CLI test)
    - tools/arrconf/tests/test_schema_gen.py (lines 54-66 — reproducibility/tmp_path comparison pattern)
    - tools/arrconf/arrconf/__main__.py (the generate command from Task 1)
  </read_first>
  <behavior>
    - generate --help shows --intent, --output-dir, --check
    - generate with valid temp intent writes cross-seed/config.js under output dir, exit 0
    - generate with missing intent path exits 2
    - generate --check matching committed content exits 0
    - generate --check with stale/absent committed content exits 1 (writes nothing)
    - top-level --help lists "generate" alongside apply/dump/diff/schema-gen
  </behavior>
  <action>
Create `tools/arrconf/tests/test_generate_cmd.py`. Header: `from __future__ import annotations`, `from pathlib import Path`, `from typer.testing import CliRunner`, `from arrconf.__main__ import app`, `runner = CliRunner()`. Write one test per `<behavior>` case (the first five). Use `tmp_path: Path`. For the write test: write a valid intent.yml to tmp_path, run `["generate", "--intent", str(intent), "--output-dir", str(out)]`, assert exit_code 0 and `(out / "cross-seed" / "config.js").exists()`. For the --check drift test, mirror 28-PATTERNS.md lines 438-450 (write stale "stale content" to the committed path, assert exit_code 1). For the missing-intent test, point `--intent` at a nonexistent path, assert exit_code 2.

THEN update `tools/arrconf/tests/test_cli.py`: the existing help-list test (currently asserts `["apply", "dump", "diff", "schema-gen"]`, ~line 33) must add `"generate"` to that list. If the test name is `test_help_lists_four_subcommands`, rename it to `test_help_lists_subcommands` (the count changed) and update its assertion list to include `"generate"`. This is mandatory — PATTERNS gotcha #5: the test passes vacuously otherwise.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run pytest tests/test_generate_cmd.py tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd tools/arrconf && uv run pytest tests/test_generate_cmd.py tests/test_cli.py -q` exits 0
    - `grep -q '"generate"' tools/arrconf/tests/test_cli.py` (help-list updated)
    - `grep -c "def test_" tools/arrconf/tests/test_generate_cmd.py` returns >= 5
  </acceptance_criteria>
  <done>generate CLI tests pass; help-list test asserts "generate".</done>
</task>

<task type="auto">
  <name>Task 3: Co-bump arrconf image tag</name>
  <files>charts/arr-stack/values.yaml</files>
  <read_first>
    - charts/arr-stack/values.yaml (READ line 451 — arrconf image tag)
    - CLAUDE.md ("Release pin co-bump pattern" — minor bump for new feature)
  </read_first>
  <action>
CO-BUMP (CLAUDE.md): this plan touched `tools/arrconf/**`. Ensure `charts/arr-stack/values.yaml` line 451 reads `tag: "0.18.0"`. If a sibling Python plan (01 or 02) already set it, leave it — the target end-state for the whole generate feature is one minor bump to `0.18.0`. Do not touch the `# renovate: image=...` annotation.
  </action>
  <verify>
    <automated>grep -q 'tag: "0.18.0"' charts/arr-stack/values.yaml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'tag: "0.18.0"' charts/arr-stack/values.yaml` matches
    - `grep -q "renovate: image=ghcr.io/tom333/arr-stack-arrconf" charts/arr-stack/values.yaml`
  </acceptance_criteria>
  <done>arrconf.image.tag == 0.18.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/CI invocation → filesystem | generate writes committed files; --check is read-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06 | Tampering | generate write path | mitigate | Output confined to `{output_dir}/cross-seed/config.js`; no arbitrary path interpolation from intent.yml; rendered content header-marked read-only |
| T-28-07 | Repudiation | drift undetected | mitigate | `--check` compares committed bytes to rendered bytes and exits 1 on any difference (CI gate in Plan 04) |
| T-28-08 | Tampering | apply auto-running generate | accept | D-06 forbids it; apply code is NOT modified by this plan (verified: no generate call added to apply) |
</threat_model>

<verification>
- `cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf` (triade)
- `cd tools/arrconf && uv run pytest tests/test_generate_cmd.py tests/test_cli.py -q` passes
- `arrconf generate --check` semantics: exit 1 on drift, 0 in sync, 2 on ConfigError (covered by tests)
</verification>

<success_criteria>
- generate subcommand: writes config.js (bare), exits 1 on drift (--check), exits 2 on ConfigError (INTENT-02/INTENT-03, D-06/D-07)
- generate decoupled from apply
- CLI tests pass incl. updated help-list; tag co-bumped to 0.18.0
</success_criteria>

<output>
After completion, create `.planning/phases/28-generate-foundation/28-03-SUMMARY.md`
</output>
