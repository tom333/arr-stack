# Phase 28: Generate foundation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-31
**Phase:** 28-generate-foundation
**Areas discussed:** Bootstrap emission, intent.yml shape, generate CLI + apply, CI guard mechanics

---

## Bootstrap emission

### Q1 — First thing `generate` emits

| Option | Description | Selected |
|--------|-------------|----------|
| New blocks stubs only | generate wired end-to-end, emits only new target files; arrconf.yml/categories untouched | ✓ |
| Re-emit categories slice | Route in-memory categories[] expansion to a committed arrconf.yml fragment (starts deferred migration) | |
| One full block now | Pull a whole block into P28 as proving slice (borrows P31 scope) | |

**User's choice:** New blocks stubs only.
**Notes:** Keeps the deferred categories→intent migration truly deferred.

### Q2 — How P28 proves the loop isn't vacuous

| Option | Description | Selected |
|--------|-------------|----------|
| Seed sample intent | Ship real intent.yml with one sample entry → committed non-empty config; CI guard gets real bytes; deploy waits for P30/31 | ✓ |
| Empty-but-valid | Emit structurally-valid-but-empty target files; thinner proof | |
| Self-test fixture | Golden intent→config pair in tests/; guard pre-positioned on real paths | |

**User's choice:** Seed sample intent.

### Q3 — Which block seeds the proving slice

| Option | Description | Selected |
|--------|-------------|----------|
| cross-seed (JS literal) | Forces literal-JS emission in the foundation; de-risks P30; proves framework not YAML-only | ✓ |
| qbit_manage (YAML) | Closest to existing patterns, lowest friction; defers JS question | |
| You decide | Planner picks | |

**User's choice:** cross-seed (JS literal).
**Notes:** Prove the hard output format in the foundation.

---

## intent.yml shape

### Q1 — Location & relation to arrconf.yml

| Option | Description | Selected |
|--------|-------------|----------|
| Separate intent.yml | New charts/arr-stack/files/intent.yml; holds only new blocks; arrconf.yml stays hand-edited | ✓ |
| intent.yml absorbs categories | Move categories[] in now (contradicts deferred INTENT-CATMIG-01) | |
| Section in arrconf.yml | `intent:` block inside arrconf.yml | |

**User's choice:** Separate intent.yml.

### Q2 — Top-level layout

| Option | Description | Selected |
|--------|-------------|----------|
| tools{} + sagas[] | Two top-level keys mirroring DESIGN §3; both schemas shipped, only tools.cross_seed exercised | ✓ |
| Flat per-tool keys | cross_seed/qbit_manage/sagas all top-level, no tools wrapper | |
| Only what P28 uses | Ship just tools.cross_seed schema now | |

**User's choice:** tools{} + sagas[].

---

## generate CLI + apply

### Q1 — Relationship to apply

| Option | Description | Selected |
|--------|-------------|----------|
| Fully decoupled | generate standalone; apply reads committed files; apply never auto-generates | ✓ |
| apply auto-generates | apply runs generate in-memory then reconciles (loses committed-diff guarantee) | |
| generate calls apply | generate --apply one-shot | |

**User's choice:** Fully decoupled.

### Q2 — CI drift-detection mode

| Option | Description | Selected |
|--------|-------------|----------|
| --check flag | generate --check writes nothing, exits non-zero on drift, prints diff; single code path | ✓ |
| Plain generate + git diff | Literal `generate && git diff --exit-code`; mutates ephemeral CI tree | |
| You decide | Planner picks | |

**User's choice:** --check flag.

---

## CI guard mechanics

### Q1 — Where the guard runs

| Option | Description | Selected |
|--------|-------------|----------|
| Extend tests.yml | New idempotence job in tests.yml; isolated from chart-lint auto-tagger | ✓ |
| New workflow file | Dedicated generate-guard.yml with own path triggers | |
| Into chart-lint.yml | Co-locate with chart validation (entangles with auto-tagger) | |

**User's choice:** Extend tests.yml.
**Notes:** Keeps the guard away from the mathieudutour auto-tagger that fires on push to main.

### Q2 — Output file location

| Option | Description | Selected |
|--------|-------------|----------|
| charts/.../files/ | Generated outputs under charts/arr-stack/files/, ConfigMap-mount-ready | ✓ |
| Subdir per tool | cross-seed/config.js, qbit_manage/config.yml nested | |
| You decide | Planner picks | |

**User's choice:** charts/.../files/.
**Notes:** Generic config.yml filename ambiguity flagged for planner (ConfigMap key/subPath rename or per-tool subdir).

---

## Read-only marking + ADR (locked with defaults, not separately discussed)

- Read-only header: `# GENERATED by 'arrconf generate' from intent.yml — DO NOT EDIT BY HAND` on generated targets only (config.js in P28); arrconf.yml/configarr.yml stay hand-edited.
- ADR: new ADR in .planning/ (next free number) formalizing intention layer + absorber/déployer boundary + ADR-5 extension (configarr sole TRaSH applier). Content from DESIGN §2/§5.

## Claude's Discretion

- Subcommand flag surface beyond --check.
- Exact output sub-paths/filenames under charts/arr-stack/files/.
- IntentConfig pydantic model placement (new module vs extend config.py).
- Generate determinism/ordering mechanism.

## Deferred Ideas

- Categories[] → intent.yml migration (INTENT-CATMIG-01, v2).
- configarr.yml generated from intent (INTENT-CFGARR-01, v2).
- UI on intention (INTENT-UI-01, v2).
- Block deploy logic — Phases 29/30/31.
- Generic config.yml filename collision — planner-flag.
- Reviewed-not-folded todo: 2026-05-27-migrer-mediatheque-existante (v0.8.0 ops task, keyword-noise match).
