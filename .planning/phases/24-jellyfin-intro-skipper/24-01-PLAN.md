---
phase: 24-jellyfin-intro-skipper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/arrconf/arrconf/resources/jellyfin/plugin.py
  - tools/arrconf/arrconf/resources/jellyfin/library.py
  - tools/arrconf/arrconf/config.py
  - tools/arrconf/arrconf/generators/categories.py
  - tools/arrconf/arrconf/reconcilers/jellyfin.py
  - charts/arr-stack/files/arrconf.yml
  - tools/arrconf/tests/test_reconcilers_jellyfin_chapter_extraction.py
autonomous: true
requirements: [JFSKIP-01, JFSKIP-04]
must_haves:
  truths:
    - "arrconf apply (dry-run) logs the Intro Skipper plugin repository (https://intro-skipper.org/manifest.json) is registered, idempotent on second run"
    - "EnableChapterImageExtraction:true flows from the categories generator to all 10 libraries via reconciler"
  artifacts:
    - path: "tools/arrconf/arrconf/resources/jellyfin/plugin.py"
      provides: "Extended PluginEntry with install_guid/install_version/install_repo_url + config + IntroSkipperConfig model"
      contains: "install_guid"
    - path: "tools/arrconf/arrconf/config.py"
      provides: "JellyfinLibrariesSection.enable_chapter_image_extraction field"
      contains: "enable_chapter_image_extraction"
    - path: "tools/arrconf/arrconf/reconcilers/jellyfin.py"
      provides: "_update_library_options helper + _create_library LibraryOptions body"
      contains: "_update_library_options"
    - path: "charts/arr-stack/files/arrconf.yml"
      provides: "Intro Skipper plugin_repositories entry + libraries.enable_chapter_image_extraction:true"
      contains: "Intro Skipper"
  key_links:
    - from: "tools/arrconf/arrconf/generators/categories.py"
      to: "tools/arrconf/arrconf/reconcilers/jellyfin.py"
      via: "JellyfinLibrary.enable_chapter_image_extraction drives _create_library / _update_library_options"
      pattern: "enable_chapter_image_extraction"
---

<objective>
Lay the declarative + reconciler foundation for Phase 24: extend the pydantic models (PluginEntry install/config fields, JellyfinLibrary + JellyfinLibrariesSection chapter-extraction flag), wire chapter-image extraction through the generator and the library reconciler, and register the Intro Skipper plugin repository in arrconf.yml.

Purpose: JFSKIP-01 (repo registration, zero-code via existing set-by-URL path — D-03) and JFSKIP-04 (EnableChapterImageExtraction on all 10 Category libs — D-06) are independent of the install/enable two-run model, so they ship first as a fully-autonomous code+config wave.
Output: Extended models, a _update_library_options() reconciler helper, a chapter-aware _create_library() body, the YAML repo entry, and respx tests — all verified via dry-run and unit tests with NO live cluster writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-jellyfin-intro-skipper/24-CONTEXT.md
@.planning/phases/24-jellyfin-intro-skipper/24-PATTERNS.md

<interfaces>
From tools/arrconf/arrconf/resources/jellyfin/plugin.py (current):
```
class PluginEntry(BaseModel):
    model_config = ConfigDict(extra="allow")
    name: str
    id: str | None = Field(default=None)
```

From tools/arrconf/arrconf/resources/jellyfin/library.py (current):
```
class JellyfinLibrary(BaseModel):
    model_config = ConfigDict(extra="allow")
    name: str
    collection_type: str
    paths: list[str] = Field(default_factory=list)
```

From tools/arrconf/arrconf/reconcilers/jellyfin.py: _create_library() posts json={} today (line 148). _add_missing_paths() reads cluster_lib["LibraryOptions"]["PathInfos"]. Reconcile order is fixed: libraries -> users -> server_config -> plugins.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend pydantic models + categories generator for chapter extraction and plugin install/config</name>
  <files>tools/arrconf/arrconf/resources/jellyfin/plugin.py, tools/arrconf/arrconf/resources/jellyfin/library.py, tools/arrconf/arrconf/config.py, tools/arrconf/arrconf/generators/categories.py</files>
  <read_first>
    - tools/arrconf/arrconf/resources/jellyfin/plugin.py (the file being extended)
    - tools/arrconf/arrconf/resources/jellyfin/library.py (the file being extended)
    - tools/arrconf/arrconf/resources/jellyfin/server_config.py (PluginRepository extra="allow" pattern)
    - tools/arrconf/arrconf/config.py lines 519-543 (JellyfinLibrariesSection) and 587-605 (JellyfinPluginsSection)
    - tools/arrconf/arrconf/generators/categories.py lines 199-220 (generate_jellyfin_libraries)
    - .planning/phases/24-jellyfin-intro-skipper/24-PATTERNS.md (model + generator extension blocks)
  </read_first>
  <behavior>
    - PluginEntry accepts install_guid/install_version/install_repo_url/config=None and still parses a bare {name} entry (backward-compatible)
    - IntroSkipperConfig defaults: AutoSkip=False, AutoSkipCredits=False, MaxParallelism=1
    - JellyfinLibrariesSection.enable_chapter_image_extraction defaults False; extra="forbid" still rejects unknown keys
    - JellyfinLibrary.enable_chapter_image_extraction defaults False
    - generate_jellyfin_libraries() emits enable_chapter_image_extraction=True on every JellyfinLibrary (all 10 categories)
  </behavior>
  <action>
In tools/arrconf/arrconf/resources/jellyfin/plugin.py, keep the existing imports (from pydantic import BaseModel, ConfigDict, Field). Add a new model IntroSkipperConfig(BaseModel) with model_config = ConfigDict(extra="allow") and fields AutoSkip: bool = Field(default=False), AutoSkipCredits: bool = Field(default=False), MaxParallelism: int = Field(default=1). Extend PluginEntry (keep extra="allow", name, id) with four new optional fields: install_guid: str | None = Field(default=None), install_version: str | None = Field(default=None), install_repo_url: str | None = Field(default=None), config: IntroSkipperConfig | None = Field(default=None). Update the module docstring to note D-01 (install-capable, reverses D-07-PLUGINS-01).

In tools/arrconf/arrconf/resources/jellyfin/library.py, add enable_chapter_image_extraction: bool = Field(default=False) to JellyfinLibrary (it has extra="allow", so non-breaking).

In tools/arrconf/arrconf/config.py, add to JellyfinLibrariesSection (keep extra="forbid"): enable_chapter_image_extraction: bool = Field(default=False, description="D-06: when True, EnableChapterImageExtraction=true on all 10 Category libs (uniform)."). Do NOT touch JellyfinPluginsSection body — required: list[PluginEntry] already covers the new fields. Do NOT touch JellyfinServerConfigSection — PluginRepositories already covers JFSKIP-01.

In tools/arrconf/arrconf/generators/categories.py, in generate_jellyfin_libraries(), add enable_chapter_image_extraction=True to the JellyfinLibrary(...) constructor inside the list comprehension. Update the docstring to cite "Phase 24 D-06: uniform chapter extraction on all 10 libs".

If IntroSkipperConfig or PluginEntry is exported from tools/arrconf/arrconf/resources/jellyfin/__init__.py, add IntroSkipperConfig to that __all__/imports so "from arrconf.resources.jellyfin import IntroSkipperConfig" works in tests.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run python -c "from arrconf.resources.jellyfin.plugin import PluginEntry, IntroSkipperConfig; from arrconf.config import JellyfinLibrariesSection; from arrconf.generators.categories import generate_jellyfin_libraries; e=PluginEntry(name='X', install_guid='g', config={'AutoSkip': False, 'MaxParallelism': 1}); assert e.config.MaxParallelism==1; assert PluginEntry(name='Y').install_guid is None; assert JellyfinLibrariesSection().enable_chapter_image_extraction is False; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - "grep -c install_guid tools/arrconf/arrconf/resources/jellyfin/plugin.py" returns >= 1
    - "grep -c 'class IntroSkipperConfig' tools/arrconf/arrconf/resources/jellyfin/plugin.py" returns 1
    - "grep -c enable_chapter_image_extraction tools/arrconf/arrconf/config.py" returns >= 1
    - "grep -c 'enable_chapter_image_extraction=True' tools/arrconf/arrconf/generators/categories.py" returns 1
    - The verify python one-liner prints OK (exit 0)
  </acceptance_criteria>
  <done>PluginEntry has install/config fields, IntroSkipperConfig exists with MaxParallelism=1 default, JellyfinLibrary + JellyfinLibrariesSection carry enable_chapter_image_extraction, and the generator emits True on all libs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chapter-extraction reconciler logic (_create_library body + _update_library_options) + respx tests</name>
  <files>tools/arrconf/arrconf/reconcilers/jellyfin.py, tools/arrconf/tests/test_reconcilers_jellyfin_chapter_extraction.py</files>
  <read_first>
    - tools/arrconf/arrconf/reconcilers/jellyfin.py lines 116-339 (_create_library, _add_missing_paths, _reconcile_libraries)
    - tools/arrconf/tests/test_reconcilers_jellyfin.py (full fixture/_mock_all_gets/_make_instance scaffold)
    - .planning/phases/24-jellyfin-intro-skipper/24-PATTERNS.md (Extension point C + test patterns + dry-run convention)
  </read_first>
  <behavior>
    - New library with enable_chapter_image_extraction=True: POST /Library/VirtualFolders body carries {"LibraryOptions": {"EnableChapterImageExtraction": true}}
    - Existing library, cluster LibraryOptions.EnableChapterImageExtraction=False, desired=True: POST /Library/VirtualFolders/LibraryOptions with {Id, LibraryOptions: {EnableChapterImageExtraction: true}}
    - Existing library already True: no LibraryOptions POST (no-op)
    - dry_run=True: zero writes, action strings contain dry_run
  </behavior>
  <action>
In tools/arrconf/arrconf/reconcilers/jellyfin.py:

1. In _create_library() (line ~139), change the json={} body so that when desired_lib.enable_chapter_image_extraction is True it posts json={"LibraryOptions": {"EnableChapterImageExtraction": True}}, else keep json={}. Keep the existing params={...} query string unchanged.

2. Add a new helper _update_library_options(client: JellyfinClient, desired_lib: JellyfinLibrary, cluster_lib: dict[str, Any], dry_run: bool) -> list[str] following the _add_missing_paths pattern. Read cluster_value = (cluster_lib.get("LibraryOptions") or {}).get("EnableChapterImageExtraction", False). If cluster_value == desired_lib.enable_chapter_image_extraction return []. On dry_run: log.info("dry_run_skip", resource="library_options", name=desired_lib.name) and return [f"library_options:dry_run:{desired_lib.name}"]. Otherwise client._request("POST", "/Library/VirtualFolders/LibraryOptions", json={"Id": cluster_lib.get("ItemId"), "LibraryOptions": {"EnableChapterImageExtraction": desired_lib.enable_chapter_image_extraction}}), log.info("library_options_updated", name=desired_lib.name), return [f"library_options_updated:{desired_lib.name}"].

3. In _reconcile_libraries(), in the existing-lib branch (after _add_missing_paths / _prune_library_paths, around line 334), call actions += _update_library_options(client, desired_lib, cluster_lib, dry_run). Do NOT change the topological reconcile order (libraries stays step_index=1).

Create tools/arrconf/tests/test_reconcilers_jellyfin_chapter_extraction.py. Copy the fixture/helper scaffold from tests/test_reconcilers_jellyfin.py (imports, JELLYFIN_BASE, _mock_all_gets, _make_instance/_DEFAULT_LIBRARIES helpers). Add tests:
- test_chapter_extraction_enabled_on_library_create: new lib with enable_chapter_image_extraction=True; captured POST /Library/VirtualFolders body equals {"LibraryOptions": {"EnableChapterImageExtraction": True}} (use the body-capture side_effect pattern).
- test_chapter_extraction_update_existing_library: cluster lib present with LibraryOptions.EnableChapterImageExtraction=False, desired True; POST /Library/VirtualFolders/LibraryOptions called, action library_options_updated:<name> present.
- test_chapter_extraction_no_op_when_already_enabled: cluster lib EnableChapterImageExtraction=True; no LibraryOptions POST, no library_options_updated action.
- test_chapter_extraction_dry_run_no_post: dry_run=True; no POST, every chapter action contains dry_run.

Mock every Jellyfin endpoint via respx (@pytest.mark.respx(base_url=JELLYFIN_BASE, assert_all_called=False) or @respx.mock). NEVER call a real Jellyfin.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run pytest tests/test_reconcilers_jellyfin_chapter_extraction.py -x -q && uv run pytest tests/test_reconcilers_jellyfin.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - "grep -c _update_library_options tools/arrconf/arrconf/reconcilers/jellyfin.py" returns >= 2 (def + call site)
    - "grep -c '/Library/VirtualFolders/LibraryOptions' tools/arrconf/arrconf/reconcilers/jellyfin.py" returns 1
    - "grep -c EnableChapterImageExtraction tools/arrconf/arrconf/reconcilers/jellyfin.py" returns >= 2
    - pytest tests/test_reconcilers_jellyfin_chapter_extraction.py exits 0 with the 4 named tests collected
    - Pre-existing tests/test_reconcilers_jellyfin.py still exits 0 (no regression to library reconcile order)
  </acceptance_criteria>
  <done>Chapter extraction flows through create + update paths, is idempotent (no-op when already enabled), dry-run-safe, and covered by 4 respx tests with the existing jellyfin tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: Register Intro Skipper plugin repository + libraries chapter flag in arrconf.yml (JFSKIP-01)</name>
  <files>charts/arr-stack/files/arrconf.yml</files>
  <read_first>
    - charts/arr-stack/files/arrconf.yml lines 245-303 (jellyfin.main block)
    - tools/arrconf/arrconf/reconcilers/jellyfin.py lines 87-113 (_server_config_equivalent set-by-URL)
    - .planning/phases/24-jellyfin-intro-skipper/24-PATTERNS.md (arrconf.yml YAML block)
  </read_first>
  <action>
In charts/arr-stack/files/arrconf.yml, under jellyfin.main.server_config.plugin_repositories: (after the existing "Jellyfin Stable" entry at line 293), append a second entry verbatim:
```
        # D-03 (JFSKIP-01): Intro Skipper manifest — idempotent set-by-URL via _server_config_equivalent()
        - Name: "Intro Skipper"
          Url: "https://intro-skipper.org/manifest.json"
          Enabled: true
```
Under jellyfin.main.libraries: (lines 248-250), add the chapter flag after the existing prune: false line:
```
      enable_chapter_image_extraction: true  # D-06 (JFSKIP-04): uniform on all 10 Category libs
```
Do NOT add any secret/API-key value. Do NOT add the plugin INSTALL block here — that ships in Plan 02 (this plan is repo registration + chapter flag only). Verify the file still parses by loading it through arrconf config.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run python -c "from arrconf.config import RootConfig; import ruyaml; y=ruyaml.YAML(); d=y.load(open('../../charts/arr-stack/files/arrconf.yml')); c=RootConfig.model_validate(d); jf=c.jellyfin['main']; assert any(r.Url=='https://intro-skipper.org/manifest.json' for r in jf.server_config.plugin_repositories); assert jf.libraries.enable_chapter_image_extraction is True; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - "grep -c 'https://intro-skipper.org/manifest.json' charts/arr-stack/files/arrconf.yml" returns 1
    - "grep -c 'enable_chapter_image_extraction: true' charts/arr-stack/files/arrconf.yml" returns 1
    - "grep -c install_guid charts/arr-stack/files/arrconf.yml" returns 0 (install block deferred to Plan 02)
    - The verify one-liner prints OK (RootConfig parses; no ConfigError)
  </acceptance_criteria>
  <done>arrconf.yml has the Intro Skipper repo entry and the libraries chapter-extraction flag; the config parses cleanly with no install block present yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arrconf -> Jellyfin /System/Configuration | arrconf writes the plugin repository URL into Jellyfin server config (full-replace POST) |
| Jellyfin -> remote plugin manifest | Jellyfin will later fetch https://intro-skipper.org/manifest.json (install is Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | Intro Skipper manifest URL in arrconf.yml | mitigate | URL is a literal HTTPS string committed in-repo and reviewed in PR; set-by-URL idempotence (_server_config_equivalent) prevents silent drift; no install triggered in this plan |
| T-24-02 | Information Disclosure | arrconf.yml repo entry | accept | Entry contains only Name/Url/Enabled — no secrets or API keys; grep for credentials in arrconf.yml stays empty (project rule: no secrets in YAML) |
| T-24-03 | Spoofing | LibraryOptions POST body | mitigate | Only Id (from cluster GET) and EnableChapterImageExtraction boolean are sent; no user-controlled free-form fields; respx tests assert exact body shape |
</threat_model>

<verification>
- cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf passes (triade; mypy gate is the arrconf package, NOT tests/).
- cd tools/arrconf && uv run pytest -q passes with the new chapter-extraction tests; coverage on reconcilers stays >= 70%.
- RootConfig.model_validate accepts the updated arrconf.yml.
</verification>

<success_criteria>
- JFSKIP-01: Intro Skipper repo registered declaratively in arrconf.yml via the existing set-by-URL path (no new reconciler code).
- JFSKIP-04: EnableChapterImageExtraction:true flows generator -> JellyfinLibrary -> reconciler (create body + update helper) on all 10 libs, idempotent and dry-run-safe.
- All new code is pure/respx-tested with zero live Jellyfin writes; triade green.
</success_criteria>

<output>
After completion, create .planning/phases/24-jellyfin-intro-skipper/24-01-SUMMARY.md.
</output>
