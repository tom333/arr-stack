---
phase: 24-jellyfin-intro-skipper
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - tools/arrconf/arrconf/reconcilers/jellyfin.py
  - charts/arr-stack/files/arrconf.yml
  - charts/arr-stack/values.yaml
  - schemas/arrconf-schema.json
  - .planning/PROJECT.md
  - tools/arrconf/tests/test_reconcilers_jellyfin_plugin_install.py
autonomous: true
requirements: [JFSKIP-02, JFSKIP-03]
must_haves:
  truths:
    - "arrconf apply (dry-run) logs the Intro Skipper plugin (GUID c83d86bb-a1e0-4c35-a113-e2101cf4ee6b) queued-for-install when absent, idempotent on second run"
    - "When the plugin is present+Active, arrconf applies intro+credits config with MaxParallelism=1 (off-peak concurrency cap)"
    - "Install and enable NEVER happen in the same run (two-run model, D-02)"
  artifacts:
    - path: "tools/arrconf/arrconf/reconcilers/jellyfin.py"
      provides: "Two-run install step + plugin-config step in _reconcile_plugins"
      contains: "plugin_install_queued"
    - path: "charts/arr-stack/files/arrconf.yml"
      provides: "Intro Skipper install_guid/install_version/install_repo_url + config block"
      contains: "c83d86bb-a1e0-4c35-a113-e2101cf4ee6b"
    - path: "charts/arr-stack/values.yaml"
      provides: "arrconf.image.tag minor bump to 0.16.0 (co-bump, same commit as Python)"
      contains: "0.16.0"
    - path: ".planning/PROJECT.md"
      provides: "New ADR recording D-07-PLUGINS-01 reversal (activation-only to install-capable)"
      contains: "install-capable"
  key_links:
    - from: "charts/arr-stack/files/arrconf.yml"
      to: "tools/arrconf/arrconf/reconcilers/jellyfin.py"
      via: "PluginEntry.install_guid drives POST /Packages/Installed; PluginEntry.config drives POST /Plugins/{id}/Configuration"
      pattern: "PACKAGES_INSTALLED_PATH"
---

<objective>
Implement the Intro Skipper install + enable + plugin-config reconciler logic (two-run model, D-01/D-02/D-04/D-05), declare the install+config block in arrconf.yml, co-bump the arrconf image tag, regenerate the JSON schema, and record the ADR reversal that moves arrconf from activation-only to install-capable for Jellyfin plugins.

Purpose: JFSKIP-02 (install when absent, distinguish queued-install vs active) and JFSKIP-03 (intro+credits detection + off-peak concurrency cap MaxParallelism=1). This is pure reconciler code + config + tests — fully autonomous and respx-mocked. The actual live two-run/restart verification is the operator's job in Plan 03.
Output: Extended _reconcile_plugins(), arrconf.yml install/config block, values.yaml tag 0.16.0, regenerated schemas/arrconf-schema.json, the new ADR, and respx tests proving install-queued / idempotence / config-applied / two-run separation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-jellyfin-intro-skipper/24-CONTEXT.md
@.planning/phases/24-jellyfin-intro-skipper/24-PATTERNS.md
@.planning/research/STACK.md

<interfaces>
From tools/arrconf/arrconf/reconcilers/jellyfin.py (current, after Plan 01):
```
PLUGINS_PATH = "/Plugins"
_ACTIVE_PLUGIN_STATUSES = frozenset({"Active", "Restart"})
def _reconcile_plugins(client, section, dry_run):
    # GET /Plugins -> by_name / by_id -> for each entry: warn if absent, else POST Enable
    # client._request("POST", f"{PLUGINS_PATH}/{plugin_id}/{plugin_version}/Enable")
```
PluginEntry (extended in Plan 01) has: name, id, install_guid, install_version, install_repo_url, config (IntroSkipperConfig).
JellyfinClient uses MediaBrowser token auth; call shape is client._request("POST", path, params=..., json=...) and client.get(path).
</interfaces>

CRITICAL constraints (PATTERNS.md / PITFALLS):
- Two-run model: when an install POST just fired, append plugin_install_queued:<name> and continue — do NOT attempt enable or config in the same run (Jellyfin loads plugins at boot only).
- POST /Packages/Installed/{name} takes QUERY PARAMS {assemblyGuid, version, repositoryUrl}, json=None.
- Enable path keeps version in path: POST /Plugins/{id}/{version}/Enable.
- Plugin config is a DIFFERENT endpoint family: GET /Plugins/{id}/Configuration then POST /Plugins/{id}/Configuration — never folded into /System/Configuration.
- NEVER automate the Jellyfin pod restart from arrconf — only log a plugin_install_queued warning with hint: kubectl rollout restart deployment/jellyfin -n selfhost.
- ADR-8 forceSave is *arr-v3 ONLY, NEVER Jellyfin.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-run install + plugin-config logic in _reconcile_plugins + respx tests</name>
  <files>tools/arrconf/arrconf/reconcilers/jellyfin.py, tools/arrconf/tests/test_reconcilers_jellyfin_plugin_install.py</files>
  <read_first>
    - tools/arrconf/arrconf/reconcilers/jellyfin.py lines 58-77 (constants) and 461-529 (_reconcile_plugins)
    - tools/arrconf/tests/test_reconcilers_jellyfin.py (fixture/_mock_all_gets/_make_instance scaffold)
    - .planning/phases/24-jellyfin-intro-skipper/24-PATTERNS.md (Extension point A + B + test cases 1-7)
    - .planning/research/STACK.md (B1-B5 endpoint shapes — MEDIUM confidence flags)
  </read_first>
  <behavior>
    - Plugin absent + install fields set: POST /Packages/Installed/Intro Skipper with params assemblyGuid=c83d86bb-..., version=1.10.11.19, repositoryUrl=https://intro-skipper.org/manifest.json; action plugin_install_queued:Intro Skipper; warning logged with kubectl hint; NO enable/config same run
    - Plugin absent + NO install fields (bare name): existing plugin_missing_skip warning (unchanged)
    - Plugin present (Run N+1): no install POST; enable if not Active; then config GET+diff+POST if config block present
    - config no-op: cluster config already matches desired -> no POST
    - config skipped when plugin present but not Active (post-install, pre-restart)
    - dry_run=True: zero writes; every action contains dry_run
  </behavior>
  <action>
In tools/arrconf/arrconf/reconcilers/jellyfin.py:

1. After the existing constants (line ~63) add PACKAGES_INSTALLED_PATH = "/Packages/Installed".

2. Extend _reconcile_plugins(). In the "if cluster is None:" branch (line 487), BEFORE the existing plugin_missing_skip warning, check "if entry.install_guid and entry.install_version and entry.install_repo_url:". On dry_run: log.info("dry_run_skip", resource="plugin_install", name=entry.name), actions.append(f"plugin_install:dry_run:{entry.name}"), continue. Otherwise: client._request("POST", f"{PACKAGES_INSTALLED_PATH}/{entry.name}", params={"assemblyGuid": entry.install_guid, "version": entry.install_version, "repositoryUrl": entry.install_repo_url}), then log.warning("plugin_install_queued", name=entry.name, guid=entry.install_guid, version=entry.install_version, hint="Jellyfin restart required: kubectl rollout restart deployment/jellyfin -n selfhost"), actions.append(f"plugin_install_queued:{entry.name}"), continue. If install fields are absent, fall through to the existing plugin_missing_skip warning (unchanged). Keep the existing enable loop (lines 499-527) untouched.

3. After the enable loop, before "return actions", add a second loop over section.required for the plugin-config step: skip if entry.config is None; resolve cluster = by_id.get(entry.id) if entry.id else by_name.get(entry.name); skip if cluster is None or cluster.get("Status") not in _ACTIVE_PLUGIN_STATUSES (two-run: config only after restart+active). Then plugin_id = cluster["Id"]; cluster_config = client.get(f"{PLUGINS_PATH}/{plugin_id}/Configuration"); desired_config = entry.config.model_dump(exclude_none=True). If all(cluster_config.get(k) == v for k, v in desired_config.items()): log.info("plugin_config_no_op", name=entry.name), continue. On dry_run: actions.append(f"plugin_config:dry_run:{entry.name}"), continue. Otherwise client._request("POST", f"{PLUGINS_PATH}/{plugin_id}/Configuration", json=desired_config), log.info("plugin_config_applied", name=entry.name), actions.append(f"plugin_config_applied:{entry.name}"). Keep step_index=4 and the topological order in reconcile_jellyfin() unchanged.

Create tools/arrconf/tests/test_reconcilers_jellyfin_plugin_install.py. Copy scaffold from tests/test_reconcilers_jellyfin.py and add constants INTRO_SKIPPER_GUID="c83d86bb-a1e0-4c35-a113-e2101cf4ee6b", INTRO_SKIPPER_VERSION="1.10.11.19", INTRO_SKIPPER_REPO="https://intro-skipper.org/manifest.json". Implement the 7 plugin tests from PATTERNS.md: test_plugin_install_queued_when_absent (assert install route called + params + action + warning), test_plugin_install_idempotent_when_present (plugin present -> install route NOT called), test_plugin_install_no_action_when_install_fields_absent (bare name -> plugin_missing_skip path, no install POST), test_plugin_install_dry_run_no_post, test_plugin_config_applied_when_plugin_active (config GET+POST, body has AutoSkip/AutoSkipCredits/MaxParallelism), test_plugin_config_no_op_when_already_matches, test_plugin_config_skipped_when_plugin_not_active. Mock ALL endpoints via respx — never call a real Jellyfin.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run pytest tests/test_reconcilers_jellyfin_plugin_install.py -x -q && uv run pytest tests/test_reconcilers_jellyfin.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - "grep -c PACKAGES_INSTALLED_PATH tools/arrconf/arrconf/reconcilers/jellyfin.py" returns >= 2 (const + use)
    - "grep -c plugin_install_queued tools/arrconf/arrconf/reconcilers/jellyfin.py" returns >= 1
    - "grep -c plugin_config_applied tools/arrconf/arrconf/reconcilers/jellyfin.py" returns >= 1
    - "grep -c Configuration tools/arrconf/arrconf/reconcilers/jellyfin.py" returns >= 2 (GET + POST plugin config)
    - "grep -v '^#' tools/arrconf/arrconf/reconcilers/jellyfin.py | grep -c 'rollout restart deployment/jellyfin'" returns >= 1 (kubectl hint in code, not just comment)
    - pytest tests/test_reconcilers_jellyfin_plugin_install.py exits 0 with all 7 tests collected
    - Pre-existing tests/test_reconcilers_jellyfin.py still exits 0
  </acceptance_criteria>
  <done>Install fires only when absent+install-fields-present, never same-run as enable; config applies only when Active and only on diff; idempotent + dry-run-safe; 7 respx tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Declare install+config block in arrconf.yml, co-bump image tag, regenerate schema</name>
  <files>charts/arr-stack/files/arrconf.yml, charts/arr-stack/values.yaml, schemas/arrconf-schema.json</files>
  <read_first>
    - charts/arr-stack/files/arrconf.yml lines 294-303 (jellyfin.main.plugins.required, after Plan 01 edits)
    - charts/arr-stack/values.yaml lines 448-452 (arrconf.image.tag = "0.15.0")
    - CLAUDE.md "Release pin co-bump pattern" + "Accumulated-bumps escape hatch"
    - .planning/phases/24-jellyfin-intro-skipper/24-PATTERNS.md (arrconf.yml plugins block)
  </read_first>
  <action>
In charts/arr-stack/files/arrconf.yml, under jellyfin.main.plugins.required: (after the existing "Kodi Sync Queue" entry at line 302), append verbatim:
```
        # D-01/D-02/D-04/D-05 (JFSKIP-02/03): Intro Skipper install (two-run) + intro+credits config
        - name: "Intro Skipper"
          install_guid: "c83d86bb-a1e0-4c35-a113-e2101cf4ee6b"
          install_version: "1.10.11.19"
          install_repo_url: "https://intro-skipper.org/manifest.json"
          config:
            AutoSkip: false          # show skip button only; auto-skip is Out of Scope (PROJECT.md)
            AutoSkipCredits: false
            MaxParallelism: 1        # D-05: off-peak concurrency cap for single-node MicroK8s
```
Do NOT add any API key or secret.

In charts/arr-stack/values.yaml, bump arrconf.image.tag from "0.15.0" to "0.16.0" (MINOR — new feature, per CLAUDE.md co-bump rule; MUST land in the same commit as the Python reconciler change). Do NOT touch or move the "# renovate: image=ghcr.io/tom333/arr-stack-arrconf" annotation above repository:.

Regenerate the JSON schema since PluginEntry and JellyfinLibrariesSection changed (Plan 01): cd tools/arrconf && uv run arrconf schema-gen --output ../../schemas/arrconf-schema.json. Commit the regenerated file (CI blocks if stale).
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run python -c "from arrconf.config import RootConfig; import ruyaml; y=ruyaml.YAML(); d=y.load(open('../../charts/arr-stack/files/arrconf.yml')); c=RootConfig.model_validate(d); p=[e for e in c.jellyfin['main'].plugins.required if e.name=='Intro Skipper'][0]; assert p.install_guid=='c83d86bb-a1e0-4c35-a113-e2101cf4ee6b'; assert p.install_version=='1.10.11.19'; assert p.config.MaxParallelism==1; assert p.config.AutoSkip is False; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - "grep -c c83d86bb-a1e0-4c35-a113-e2101cf4ee6b charts/arr-stack/files/arrconf.yml" returns 1
    - "grep -c '1.10.11.19' charts/arr-stack/files/arrconf.yml" returns 1
    - "grep -c 'MaxParallelism: 1' charts/arr-stack/files/arrconf.yml" returns 1
    - "grep -c 'tag: \"0.16.0\"' charts/arr-stack/values.yaml" returns 1
    - "grep -c 'renovate: image=ghcr.io/tom333/arr-stack-arrconf' charts/arr-stack/values.yaml" returns 1 (annotation preserved)
    - "cd tools/arrconf && uv run python -c \"import json; json.load(open('../../schemas/arrconf-schema.json'))\"" exits 0 (valid JSON, regenerated)
    - The verify one-liner prints OK
  </acceptance_criteria>
  <done>arrconf.yml carries the install+config block with concrete GUID/version/URL; values.yaml tag is 0.16.0 with the renovate annotation intact; schema regenerated and valid JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Record ADR reversing D-07-PLUGINS-01 (activation-only to install-capable)</name>
  <files>.planning/PROJECT.md, spec.md</files>
  <read_first>
    - .planning/PROJECT.md line 215 (decisions block) — find D-07-PLUGINS-01
    - spec.md section 11 "Decisions cles (ADR-like)" line 742 (ADR list, ADR-8 at 875)
    - .planning/phases/24-jellyfin-intro-skipper/24-CONTEXT.md (D-01 reversal rationale)
  </read_first>
  <action>
Record the ADR reversal in .planning/PROJECT.md inside the decisions block (line 215+). Add a new decision entry documenting that arrconf's Jellyfin plugin reconciler moves from activation-only (D-07-PLUGINS-01) to install-capable. Content must state: (1) the reversal of D-07-PLUGINS-01; (2) the mechanism: POST /Packages/Installed/{name}?assemblyGuid&version&repositoryUrl when the plugin is absent, gated on the new optional install_guid/install_version/install_repo_url fields (absent = old activation-only behavior preserved, backward compatible); (3) the two-run model (D-02) requiring a manual operator Jellyfin restart between install and enable; (4) install only — NO uninstall, NO prune (operator removes plugins via UI). Reference Phase 24 / JFSKIP-02 and the pinned version 1.10.11.19 + GUID c83d86bb-a1e0-4c35-a113-e2101cf4ee6b.

Also append a short cross-reference note to spec.md section 11 (after ADR-8, near line 900) pointing to the PROJECT.md ADR (one or two sentences — spec.md is the long-form ADR home; keep the full text in PROJECT.md to avoid duplication). Use the existing ADR numbering convention in spec.md (next free ADR number).

Use the word "install-capable" verbatim so traceability checks pass.
  </action>
  <verify>
    <automated>grep -c "install-capable" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - "grep -c install-capable .planning/PROJECT.md" returns >= 1
    - "grep -c D-07-PLUGINS-01 .planning/PROJECT.md" returns >= 1 (reversal references the original decision)
    - "grep -c 'Packages/Installed' .planning/PROJECT.md" returns >= 1
    - spec.md section 11 contains a cross-reference to the PROJECT.md ADR (grep for "install-capable" OR "Intro Skipper" in spec.md returns >= 1)
  </acceptance_criteria>
  <done>PROJECT.md records the D-07-PLUGINS-01 reversal to install-capable with mechanism + two-run model + no-uninstall scope; spec.md section 11 cross-references it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arrconf -> Jellyfin /Packages/Installed | arrconf triggers Jellyfin to install a plugin from a remote manifest URL |
| Jellyfin -> remote manifest (intro-skipper.org) | Jellyfin fetches and installs the plugin package over HTTPS |
| arrconf -> Jellyfin /Plugins/{id}/Configuration | arrconf writes plugin config (AutoSkip/MaxParallelism) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering / Supply-chain | Plugin install from remote manifest (wrong-fork crash class, PITFALLS B2) | mitigate | Version pinned to 1.10.11.19 + GUID c83d86bb-a1e0-4c35-a113-e2101cf4ee6b verbatim in arrconf.yml; no floating "latest"; manifest URL is the official intro-skipper.org fork; values committed and PR-reviewed |
| T-24-05 | Elevation of Privilege | install-capable reconciler (reverses activation-only) | mitigate | Install gated on explicit install_guid/install_version/install_repo_url fields (absent = old safe activation-only behavior); NO uninstall/prune path; two-run model requires deliberate operator restart |
| T-24-06 | Information Disclosure | arrconf.yml install/config block | accept | Block contains only GUID/version/URL/boolean config — no API keys or secrets (project rule: no secrets in YAML); JellyfinClient token comes from env, never YAML |
| T-24-07 | Denial of Service | first fingerprint task CPU spike on single-node MicroK8s | mitigate | MaxParallelism=1 (D-05) caps concurrency; config schedules off-peak; AutoSkip=false avoids forced behavior |
</threat_model>

<verification>
- cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf passes (triade; mypy gate is the arrconf package, NOT tests/).
- cd tools/arrconf && uv run pytest -q passes with the new plugin-install tests; coverage on reconcilers stays >= 70%.
- arrconf.yml parses via RootConfig.model_validate; schemas/arrconf-schema.json is valid JSON and not stale.
- values.yaml arrconf.image.tag == 0.16.0 with the renovate annotation intact (co-bump rule).
</verification>

<success_criteria>
- JFSKIP-02: arrconf installs Intro Skipper when absent via POST /Packages/Installed, distinguishes queued-install vs active, never auto-restarts (logs kubectl hint).
- JFSKIP-03: intro+credits config applied with MaxParallelism=1, idempotent and dry-run-safe.
- co-bump done (0.16.0, same commit as Python); ADR reversal recorded.
- All logic respx-tested with zero live Jellyfin writes; triade green.
</success_criteria>

<output>
After completion, create .planning/phases/24-jellyfin-intro-skipper/24-02-SUMMARY.md.
</output>
