---
phase: 24-jellyfin-intro-skipper
plan: 03
type: execute
wave: 3
depends_on: ["24-02"]
files_modified:
  - .planning/phases/24-jellyfin-intro-skipper/INTRO-SKIPPER-RUNBOOK.md
  - snapshots/
autonomous: false
requirements: [JFSKIP-05]
must_haves:
  truths:
    - "Operator runbook documents the single Jellyfin restart (kubectl rollout restart deployment/jellyfin -n selfhost) between install run N and enable run N+1"
    - "After restart + second arrconf apply, the plugin appears active in GET /Plugins with no duplicate repository entries"
    - "Jellyfin web UI shows a skip-intro/skip-credits button during playback on at least one series episode"
    - "EnableChapterImageExtraction:true confirmed on all 10 libraries via GET /Library/VirtualFolders"
    - "Kodi/JellyCon spike result documented with a binary accept/reject for service.jellyskip on LibreELEC (non-gating)"
  artifacts:
    - path: ".planning/phases/24-jellyfin-intro-skipper/INTRO-SKIPPER-RUNBOOK.md"
      provides: "Operator runbook: two-run install/restart, live SC verification, Kodi service.jellyskip spike + result"
      contains: "kubectl rollout restart deployment/jellyfin"
  key_links:
    - from: "arrconf install run N (plugin_install_queued)"
      to: "arrconf enable run N+1 (plugin active)"
      via: "manual operator Jellyfin restart documented in runbook"
      pattern: "rollout restart deployment/jellyfin"
---

<objective>
Author the operator runbook and run the live two-run verification of the Intro Skipper feature against the production Jellyfin (10.11.8), confirming the dispositive web/app skip-button criterion, chapter extraction on all 10 libraries, and recording the Kodi/JellyCon spike result with a binary accept/reject.

Purpose: JFSKIP-05 (skip-intro works on web/app/Swiftfin — committed; Kodi best-effort via documented runbook, non-gating) and the live confirmation of ROADMAP Phase 24 Success Criteria 2-4 which can ONLY be verified after the manual Jellyfin restart that splits the two-run install model (D-02). The Kodi spike result is required-but-non-blocking before the phase is declared complete.
Output: INTRO-SKIPPER-RUNBOOK.md (two-run + restart + Kodi spike) and an ADR-6 forensic snapshot pair around the live writes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-jellyfin-intro-skipper/24-CONTEXT.md
@.planning/research/PITFALLS.md

Reference: ROADMAP Phase 24 Success Criteria 2-5 (live-cluster, dispositive). CLAUDE.md "Workflow snapshot" (ADR-6: snapshot before any live Jellyfin write — snapshots/before-phase-24-$(date +%F)/). The two-run model (D-02): install run N queues install + logs restart_needed; operator restarts Jellyfin once; enable run N+1 activates + configs. Kodi is non-gating (D-07): JellyCon has no native Media Segments (issue #953); workaround is the operator-installed service.jellyskip addon on the LibreELEC salon box.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the Intro Skipper operator runbook (two-run + restart + chapter + Kodi spike)</name>
  <files>.planning/phases/24-jellyfin-intro-skipper/INTRO-SKIPPER-RUNBOOK.md</files>
  <read_first>
    - .planning/phases/24-jellyfin-intro-skipper/24-CONTEXT.md (D-02 two-run model, D-07 Kodi runbook-only)
    - .planning/research/PITFALLS.md (restart requirement, plugin state persistence PVC vs ephemeral, fingerprint CPU)
    - CLAUDE.md "Workflow snapshot" section (ADR-6 snapshot.sh usage)
  </read_first>
  <action>
Create .planning/phases/24-jellyfin-intro-skipper/INTRO-SKIPPER-RUNBOOK.md documenting the full operator procedure. Sections:

1. Pre-phase snapshot (ADR-6): tools/snapshot/snapshot.sh --apps jellyfin --output snapshots/before-phase-24-$(date +%F)/ ; git add + commit before any live write.
2. Run N (install): after the v0.16.0 image deploys via ArgoCD, the arrconf CronJob (or manual arrconf apply --apps jellyfin) logs plugin_install_queued for "Intro Skipper" with the kubectl hint. Document how to confirm: kubectl -n selfhost logs job/<arrconf-job> | grep plugin_install_queued.
3. Manual restart (the one operator step that splits the two-run model): kubectl rollout restart deployment/jellyfin -n selfhost ; wait for rollout to complete (kubectl rollout status deployment/jellyfin -n selfhost). Note the PVC-persistence check: confirm the Jellyfin plugin directory is on a PVC (not ephemeral emptyDir) so the install survives the restart — if ephemeral, install is lost on every pod recreate (PITFALLS risk flag).
4. Run N+1 (enable + config): next arrconf apply activates the plugin (POST /Plugins/{id}/{version}/Enable) and applies the intro+credits config (MaxParallelism=1). Confirm: curl GET /Plugins shows "Intro Skipper" Status=Active; no duplicate PluginRepositories (GET /System/Configuration PluginRepositories has exactly one intro-skipper.org Url).
5. Chapter extraction verification: GET /Library/VirtualFolders and confirm EnableChapterImageExtraction:true on all 10 libraries.
6. Idempotence: a third arrconf apply --dry-run produces zero actions for install/enable/config/library_options.
7. Kodi/JellyCon salon spike (non-gating): document the service.jellyskip install steps on the LibreELEC salon box and a binary accept/reject decision field (accept = service.jellyskip works on LibreELEC 10.11.8; reject = unsupported, operator-manual only). Note JellyCon issue #953 (no native Media Segments).

Use concrete commands and the real GUID c83d86bb-a1e0-4c35-a113-e2101cf4ee6b / version 1.10.11.19 / URL https://intro-skipper.org/manifest.json throughout. NEVER instruct arrconf to automate the restart.
  </action>
  <verify>
    <automated>test -f .planning/phases/24-jellyfin-intro-skipper/INTRO-SKIPPER-RUNBOOK.md && grep -c "rollout restart deployment/jellyfin" .planning/phases/24-jellyfin-intro-skipper/INTRO-SKIPPER-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - File .planning/phases/24-jellyfin-intro-skipper/INTRO-SKIPPER-RUNBOOK.md exists
    - "grep -c 'rollout restart deployment/jellyfin' INTRO-SKIPPER-RUNBOOK.md" returns >= 1
    - "grep -c 'plugin_install_queued' INTRO-SKIPPER-RUNBOOK.md" returns >= 1
    - "grep -c 'EnableChapterImageExtraction' INTRO-SKIPPER-RUNBOOK.md" returns >= 1
    - "grep -c 'service.jellyskip' INTRO-SKIPPER-RUNBOOK.md" returns >= 1
    - "grep -c 'c83d86bb-a1e0-4c35-a113-e2101cf4ee6b' INTRO-SKIPPER-RUNBOOK.md" returns >= 1
    - Runbook contains a binary accept/reject field for the Kodi spike
  </acceptance_criteria>
  <done>The runbook fully documents the two-run install/restart, the live SC verification curls, idempotence check, and the Kodi service.jellyskip spike with a binary accept/reject decision field.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live two-run verification + Kodi spike (operator checkpoint)</name>
  <action>Operator-driven live verification on the production cluster — see what-built / how-to-verify below. No autonomous code action; arrconf must NEVER automate the restart.</action>
  <what-built>arrconf v0.16.0 reconciler that registers the Intro Skipper repo (JFSKIP-01), installs the plugin via two-run model (JFSKIP-02), applies intro+credits config with MaxParallelism=1 (JFSKIP-03), and sets EnableChapterImageExtraction on all 10 libraries (JFSKIP-04), plus the operator runbook (JFSKIP-05).</what-built>
  <how-to-verify>
1. Take the ADR-6 baseline snapshot: tools/snapshot/snapshot.sh --apps jellyfin --output snapshots/before-phase-24-$(date +%F)/ ; commit it.
2. Deploy v0.16.0 (Renovate PR on my-kluster bumps targetRevision; ArgoCD sync). Confirm the arrconf CronJob runs Run N and logs plugin_install_queued for "Intro Skipper" (kubectl -n selfhost logs job/<arrconf-job> | grep plugin_install_queued). [SC#1]
3. Restart Jellyfin once: kubectl rollout restart deployment/jellyfin -n selfhost ; kubectl rollout status deployment/jellyfin -n selfhost.
4. Trigger Run N+1 (next CronJob tick or manual). Confirm via GET /Plugins that "Intro Skipper" Status=Active and that GET /System/Configuration PluginRepositories has exactly ONE intro-skipper.org entry (no duplicates). [SC#2]
5. Play a series episode with a known intro in the Jellyfin web client (after the fingerprint task has run — may take hours on first run). Confirm a Skip Intro / Skip Credits button appears. [SC#3 — dispositive]
6. GET /Library/VirtualFolders and confirm EnableChapterImageExtraction:true on all 10 libraries; confirm seek-bar thumbnails in at least one client. [SC#4]
7. Run arrconf apply --dry-run a third time: expect ZERO install/enable/config/library_options actions (idempotence).
8. (Non-gating) Attempt service.jellyskip on the LibreELEC salon box per the runbook; record accept or reject in the runbook. [SC#5]
9. Take the post snapshot: tools/snapshot/snapshot.sh --apps jellyfin --output snapshots/after-phase-24-$(date +%F)/ ; diff against baseline; commit.
  </how-to-verify>
  <resume-signal>Type "approved" if SC#1-4 pass and the Kodi spike result (SC#5) is recorded (accept or reject), or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live Jellyfin (kubectl) | operator triggers the restart and live arrconf apply against production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | Tampering | live cluster state during two-run + restart | mitigate | ADR-6 snapshot before any live write + post snapshot diff; rollback = restore from snapshot; no destructive ops (install only, no uninstall/prune) |
| T-24-09 | Repudiation | manual operator restart between runs | accept | Documented runbook step with kubectl commands captured in git; arrconf logs plugin_install_queued (audit trail) |

This plan is documentation + operator-driven live verification; the only new code (the reconciler) ships in Plan 02 and carries its own threat model. No new attack surface beyond the already-mitigated supply-chain/install threats (T-24-04..07).
</threat_model>

<verification>
- INTRO-SKIPPER-RUNBOOK.md exists with the two-run restart, live SC verification, idempotence check, and Kodi spike accept/reject field.
- Live checkpoint: SC#1 (install queued) -> restart -> SC#2 (active, no duplicate repo) -> SC#3 (skip button, dispositive) -> SC#4 (chapter extraction on 10 libs) all pass; SC#5 (Kodi spike) recorded.
- ADR-6 before/after snapshots committed.
</verification>

<success_criteria>
- JFSKIP-05: skip-intro confirmed on web/app/Swiftfin (committed); Kodi service.jellyskip spike documented with binary accept/reject (non-gating).
- ROADMAP Phase 24 SC#2, SC#3, SC#4 verified live after the operator restart; idempotence confirmed on the third run.
</success_criteria>

<output>
After completion, create .planning/phases/24-jellyfin-intro-skipper/24-03-SUMMARY.md.
</output>
