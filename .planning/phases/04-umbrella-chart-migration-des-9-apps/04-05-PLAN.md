---
phase: 04-umbrella-chart-migration-des-9-apps
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - charts/arr-stack/values.yaml
  - charts/arr-stack/files/arrconf.yml  # NOTE: Plan 04-02 ported the my-kluster version verbatim (sonarr-only); this plan does NOT mutate the file — D-04-CRON-03 is satisfied by the CronJob `args:` flag in values.yaml, not by changing the config file. The file stays sonarr-only for Phase 4; radarr/prowlarr top-level keys would be added in a follow-up (Phase 5 reconciler-config work).
  - charts/arr-stack/values.schema.json
  - examples/values-prod.yaml
autonomous: true
requirements:
  - REQ-umbrella-deployment
  - REQ-helm-validation
  - REQ-config-as-code
  - REQ-pr-to-cluster-latency

must_haves:
  truths:
    - "arrconf and configarr CronJob aliases render as Kubernetes CronJob resources via app-template 5.0.0"
    - "Both CronJobs have `concurrencyPolicy: Forbid` (D-04-CRON-02 MANDATORY)"
    - "arrconf args are `[--config, /app/config/arrconf.yml, apply, --apps, sonarr,radarr,prowlarr]` (D-04-CRON-03 functional change vs production)"
    - "configarr ships `tty: true` (D-04-CRON-04 — without it, the npm process exits with code 1)"
    - "configarr references existing PVC `configarr-cache` via `existingClaim` (no PVC re-creation)"
    - "Both CronJob aliases mount the ConfigMaps authored in Plan 04-02 by exact name (`arrconf-config`, `configarr-config`)"
    - "`values.yaml` does NOT contain the v4-era key `cronJobConfig:` anywhere — the v5 key name is `cronjob:` (RESEARCH Pitfall 5; W3 carry-forward)"
    - "`values.schema.json` exists and `helm lint` validates `values.yaml` against it"
    - "`examples/values-prod.yaml` mirrors `values.yaml` (D-04-VALUES-03)"
  artifacts:
    - path: charts/arr-stack/values.yaml
      provides: "arrconf + configarr CronJob alias bodies"
      contains: "concurrencyPolicy: Forbid"
    - path: charts/arr-stack/values.schema.json
      provides: "Strict JSON Schema generated from values.yaml"
      contains: '"$schema"'
    - path: examples/values-prod.yaml
      provides: "Production values reference (D-04-VALUES-03)"
  key_links:
    - from: charts/arr-stack/values.yaml (arrconf alias persistence.config)
      to: charts/arr-stack/templates/arrconf-configmap.yaml (metadata.name = arrconf-config)
      via: "persistence.config.name: arrconf-config"
      pattern: 'name: arrconf-config'
    - from: charts/arr-stack/values.yaml (configarr alias persistence.cache)
      to: existing PVC `configarr-cache` in selfhost namespace
      via: "existingClaim"
      pattern: "existingClaim: configarr-cache"
---

<objective>
Populate the two CronJob aliases (`arrconf`, `configarr`) using app-template 5.0.0's CronJob shape, generate `values.schema.json` for the now-complete `values.yaml`, and ship `examples/values-prod.yaml` (per D-04-VALUES-03 — the arr-stack-app.yaml in my-kluster references this path).

The CronJob shape uses the v5 key `cronjob:` (not the v4-era `cronJobConfig:`) and the v5 history-limit names `successfulJobsHistory:` / `failedJobsHistory:` (not the K8s field names `successfulJobsHistoryLimit:` etc — app-template adds the `Limit` suffix at render time). RESEARCH §"Anti-Patterns to Avoid" and Pitfall 5 are the authoritative references.

Key functional changes vs production:
- **arrconf `--apps` flag**: production runs `--apps sonarr`; Phase 4 expands to `--apps sonarr,radarr,prowlarr` (D-04-CRON-03 — Phase 3 reconcilers are GA after v0.2.0). This is a deliberate non-byte-equivalent change documented in the cutover plan (04-08).
- **configarr image tag**: `1.28.0` (unchanged from my-kluster main).

Purpose: This plan finishes the umbrella's value-level work. After this, the chart is feature-complete and ready for CI hardening (Plan 04-06) and documentation (Plan 04-07).

Output: `values.yaml` is fully populated (10/10 aliases); `values.schema.json` exists and is wired to lint; `examples/values-prod.yaml` is in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-CONTEXT.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-PATTERNS.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-02-SUMMARY.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-04-SUMMARY.md
@CLAUDE.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/arrconf.yaml
@.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/configarr.yaml

<interfaces>
Source: `git -C /home/moi/projets/perso/my-kluster show main:charts/arrconf/values.yaml` + `charts/arrconf/templates/cronjob.yaml`
  Used by: arrconf alias body (translated from custom-chart shape to app-template 5.0.0 shape)
  Key fields:
    image: ghcr.io/tom333/arr-stack-arrconf:0.2.1 (per my-kluster main)
    schedule: "0 */4 * * *"
    concurrencyPolicy: Forbid
    args: ["--config", "/app/config/arrconf.yml", "apply", "--apps", "sonarr,radarr,prowlarr"]   # FUNCTIONAL CHANGE per D-04-CRON-03
    envFrom: arrconf-env (existing Secret in selfhost — bootstrap convention unchanged)
    securityContext: runAsNonRoot true, runAsUser 1000, runAsGroup 1000 (preserved from custom chart)
    successfulJobsHistoryLimit: 1, failedJobsHistoryLimit: 2 (custom-chart values)
    startingDeadlineSeconds: 600

Source: `git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/values.yaml` + `charts/configarr/templates/cronjob.yaml` + `charts/configarr/templates/pvc.yaml`
  Used by: configarr alias body
  Key fields:
    image: ghcr.io/raydak-labs/configarr:1.28.0
    schedule: "0 */4 * * *"   # configarr also 4-hourly in production (RESEARCH Open Q #2 — confirmed from custom-chart values.yaml)
    tty: true   # D-04-CRON-04 MANDATORY
    envFrom: configarr-env
    cache PVC: existing `configarr-cache` (1Gi, microk8s-hostpath storageclass — PVC pre-exists, do NOT re-create)
    NO securityContext on the configarr pod (custom chart has none)
    successfulJobsHistoryLimit: 1, failedJobsHistoryLimit: 2

CronJob key naming in app-template 5.0.0 (RESEARCH Pitfall 5):
  - Container container path: controllers.main.containers.main.{image, env, args, envFrom}
  - CronJob path: controllers.main.cronjob.{schedule, concurrencyPolicy, successfulJobsHistory, failedJobsHistory, startingDeadlineSeconds, restartPolicy}
  - controllers.main.type: cronjob
  - persistence: existingClaim for the cache; type: configMap with name reference for the ConfigMap mount
</interfaces>
</context>

<executor_note>
`values.yaml` append-only contract — this plan REPLACES the last two `{}` placeholders (`arrconf: {}` and `configarr: {}`) with full bodies. After this task runs, every top-level alias key in `values.yaml` is populated.

The `examples/values-prod.yaml` artifact is a content copy of `values.yaml` (D-04-VALUES-03). It is NOT a symlink because:
1. CI runners on Windows or strict mount setups break on symlinks
2. Renovate edits `values.yaml`; the example file is a documentation reference

Keep `examples/values-prod.yaml` in sync by copying after each `values.yaml` edit until/unless we automate it (out of Phase 4 scope).
</executor_note>

<tasks>

<task type="auto">
  <name>Task 5.1: Append arrconf CronJob alias body</name>
  <files>charts/arr-stack/values.yaml</files>
  <read_first>
    - `git -C /home/moi/projets/perso/my-kluster show main:charts/arrconf/values.yaml`
    - `git -C /home/moi/projets/perso/my-kluster show main:charts/arrconf/templates/cronjob.yaml`
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Pattern 2: arrconf CronJob Alias" (verbatim shape — copy with the few field tweaks below)
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Common Pitfalls" Pitfall 5 (cronjob key name) + Pitfall 10 (hardcoded ConfigMap name)
  </read_first>
  <action>
Replace `arrconf: {}` with the body below. The shape matches RESEARCH Pattern 2 with:
- `controllers.main.type: cronjob` (lowercase — app-template 5.0.0 schema)
- `controllers.main.cronjob.*` (v5 key — NOT `cronJobConfig:`)
- `successfulJobsHistory: 1` and `failedJobsHistory: 2` (custom-chart values from my-kluster, also v5 key names without `Limit` suffix)
- `concurrencyPolicy: Forbid` MANDATORY per D-04-CRON-02
- args reflect D-04-CRON-03 expansion: `apply --apps sonarr,radarr,prowlarr`
- securityContext preserved (runAsNonRoot, runAsUser 1000, runAsGroup 1000) — the my-kluster cronjob.yaml carried this, must be preserved for byte-equivalence
- persistence references the hardcoded ConfigMap `arrconf-config` from Plan 04-02

```yaml
arrconf:
  global:
    fullnameOverride: arrconf
  controllers:
    main:
      type: cronjob
      cronjob:
        schedule: "0 */4 * * *"
        concurrencyPolicy: Forbid
        successfulJobsHistory: 1
        failedJobsHistory: 2
        startingDeadlineSeconds: 600
        restartPolicy: Never
      pod:
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 1000
      containers:
        main:
          image:
            # renovate: image=ghcr.io/tom333/arr-stack-arrconf
            repository: ghcr.io/tom333/arr-stack-arrconf
            tag: "0.2.1"
            pullPolicy: IfNotPresent
          args:
            - "--config"
            - "/app/config/arrconf.yml"
            - "apply"
            - "--apps"
            - "sonarr,radarr,prowlarr"   # D-04-CRON-03: Phase 4 expands beyond production's sonarr-only
          env:
            TZ: "Europe/Paris"
            ARRCONF_DRY_RUN: "false"
            ARRCONF_LOG_LEVEL: INFO
          envFrom:
            - secretRef:
                name: arrconf-env
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 50m
              memory: 128Mi
  persistence:
    config:
      type: configMap
      name: arrconf-config
      globalMounts:
        - path: /app/config/arrconf.yml
          subPath: arrconf.yml
          readOnly: true
```

Note: the mountPath uses `subPath: arrconf.yml` so the file mounts at `/app/config/arrconf.yml` (matching the my-kluster custom-chart cronjob.yaml `volumeMounts:` block).

Render and inspect the produced CronJob:
```bash
helm template arr-stack charts/arr-stack/ > /tmp/w4a.yaml
awk '/^---$/{p=0} /name: arrconf$/ && /kind: CronJob/{p=1} p' /tmp/w4a.yaml | head -60
# OR:
yq 'select(.kind=="CronJob" and .metadata.name=="arrconf")' /tmp/w4a.yaml
```

The rendered CronJob must show: `schedule: "0 */4 * * *"`, `concurrencyPolicy: Forbid`, `successfulJobsHistoryLimit: 1`, `failedJobsHistoryLimit: 2`, container args containing `apply` + `--apps` + `sonarr,radarr,prowlarr`.
  </action>
  <verify>
    <automated>bash -c 'helm lint charts/arr-stack/ && helm template arr-stack charts/arr-stack/ > /tmp/w4a.yaml && grep -c "kind: CronJob" /tmp/w4a.yaml | xargs test 1 -le && ! grep -q "cronJobConfig:" charts/arr-stack/values.yaml && grep -q "successfulJobsHistory: 1" charts/arr-stack/values.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - `helm lint charts/arr-stack/` exits 0
    - At least one CronJob rendered: `grep -c '^kind: CronJob$' /tmp/w4a.yaml` >= 1
    - **W3 carry-forward — no v4-era `cronJobConfig` key anywhere**: `! grep -q 'cronJobConfig:' charts/arr-stack/values.yaml`
    - v5 key in use: `grep -c 'cronjob:' charts/arr-stack/values.yaml` >= 1
    - History limit names use v5 form (no `Limit` suffix): `grep -q 'successfulJobsHistory: 1' charts/arr-stack/values.yaml && grep -q 'failedJobsHistory: 2' charts/arr-stack/values.yaml`
    - arrconf CronJob renders with concurrencyPolicy Forbid: `awk '/kind: CronJob/{p=1} p && /---/{p=0} p' /tmp/w4a.yaml | grep -A20 'name: arrconf$' | grep -q 'concurrencyPolicy: Forbid'`
    - arrconf CronJob schedule: `awk '/kind: CronJob/{p=1} p && /---/{p=0} p' /tmp/w4a.yaml | grep -A20 'name: arrconf$' | grep -q 'schedule: 0 \*/4 \* \* \*\|schedule: "0 \*/4 \* \* \*"'`
    - D-04-CRON-03 functional change visible in rendered args: `grep -q 'sonarr,radarr,prowlarr' /tmp/w4a.yaml`
    - ConfigMap reference correct (must point at hardcoded `arrconf-config`): `grep -A3 'persistence:' charts/arr-stack/values.yaml | grep -q 'name: arrconf-config' || grep -B5 'name: arrconf-config' charts/arr-stack/values.yaml | grep -q persistence`
    - secretRef present: `grep -q 'name: arrconf-env' charts/arr-stack/values.yaml`
    - Renovate annotation: `grep -c '# renovate: image=ghcr.io/tom333/arr-stack-arrconf' charts/arr-stack/values.yaml` == 1
    - configarr placeholder still `{}`: `grep -q '^configarr: {}$' charts/arr-stack/values.yaml`
  </acceptance_criteria>
  <done>arrconf CronJob alias renders with the v5 schema, Forbid concurrency, the Phase 4 args expansion, and the hardcoded ConfigMap binding. No `cronJobConfig:` regression.</done>
</task>

<task type="auto">
  <name>Task 5.2: Append configarr CronJob alias body (tty:true + existingClaim cache PVC)</name>
  <files>charts/arr-stack/values.yaml</files>
  <read_first>
    - `git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/values.yaml`
    - `git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/templates/cronjob.yaml`
    - `git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/templates/pvc.yaml`
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Pattern 3: configarr CronJob Alias" + §"Common Pitfalls" Pitfall 7 (tty:true mandatory)
  </read_first>
  <action>
Replace `configarr: {}` with the body below. configarr's quirks:
- `tty: true` MUST be set on the container (Pitfall 7 — without it the npm process exits with code 1)
- The cache PVC `configarr-cache` pre-exists in cluster (created by the my-kluster custom chart). Reference via `existingClaim` — do NOT re-declare a PVC in the umbrella (Pitfall 3).
- NO securityContext at pod level (production has none — preserve)
- Schedule `0 */4 * * *` per `git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/values.yaml` (closes RESEARCH Open Q #2)

```yaml
configarr:
  global:
    fullnameOverride: configarr
  controllers:
    main:
      type: cronjob
      cronjob:
        schedule: "0 */4 * * *"
        concurrencyPolicy: Forbid
        successfulJobsHistory: 1
        failedJobsHistory: 2
        restartPolicy: Never
      containers:
        main:
          image:
            # renovate: image=ghcr.io/raydak-labs/configarr
            repository: ghcr.io/raydak-labs/configarr
            tag: "1.28.0"
            pullPolicy: IfNotPresent
          tty: true  # D-04-CRON-04 MANDATORY (npm process exits without TTY)
          env:
            TZ: "Europe/Paris"
          envFrom:
            - secretRef:
                name: configarr-env
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 50m
              memory: 128Mi
  persistence:
    config:
      type: configMap
      name: configarr-config
      globalMounts:
        - path: /app/config/config.yml
          subPath: config.yml
          readOnly: true
    cache:
      type: persistentVolumeClaim
      existingClaim: configarr-cache
      globalMounts:
        - path: /app/repos
```

Note the persistence shape:
- `config` is a ConfigMap mount → file at `/app/config/config.yml` (configarr binary's expected path)
- `cache` is the pre-existing PVC `configarr-cache` → mounted at `/app/repos` (TRaSH-Guides git clone target)

Render the full chart and verify both CronJobs:
```bash
helm template arr-stack charts/arr-stack/ > /tmp/w4-full.yaml
grep -c '^kind: CronJob$' /tmp/w4-full.yaml  # expect: 2
yq 'select(.kind=="CronJob") | .metadata.name + ": " + .spec.schedule + " | concurrency: " + .spec.concurrencyPolicy' /tmp/w4-full.yaml
# expect 2 lines:
#   arrconf: 0 */4 * * * | concurrency: Forbid
#   configarr: 0 */4 * * * | concurrency: Forbid
```
  </action>
  <verify>
    <automated>bash -c 'helm lint charts/arr-stack/ && helm template arr-stack charts/arr-stack/ > /tmp/w4-full.yaml && test "$(grep -c \"^kind: CronJob$\" /tmp/w4-full.yaml)" = "2" && grep -q "tty: true" /tmp/w4-full.yaml && grep -q "existingClaim: configarr-cache" charts/arr-stack/values.yaml && ! grep -q "cronJobConfig:" charts/arr-stack/values.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - `helm lint charts/arr-stack/` exits 0
    - Exactly 2 CronJobs rendered: `grep -c '^kind: CronJob$' /tmp/w4-full.yaml` == 2
    - Both CronJobs have Forbid: `awk '/kind: CronJob/{p=1} p && /---/{p=0} p' /tmp/w4-full.yaml | grep -c 'concurrencyPolicy: Forbid'` == 2
    - configarr tty:true in render: `awk '/configarr/{p=1} p && /---/{p=0} p' /tmp/w4-full.yaml | grep -q 'tty: true'`
    - configarr cache PVC referenced via existingClaim: `grep -q 'existingClaim: configarr-cache' charts/arr-stack/values.yaml`
    - configarr image: `grep -q 'image: ghcr.io/raydak-labs/configarr:1.28.0' /tmp/w4-full.yaml`
    - **No v4 key**: `! grep -q 'cronJobConfig:' charts/arr-stack/values.yaml` (W3 carry-forward)
    - All 10 aliases now populated (no more `{}` placeholders): `! grep -qE '^[a-z][a-zA-Z]*: \{\}$' charts/arr-stack/values.yaml`
    - **fullnameOverride contract for all 10 aliases**: `grep -c '^[[:space:]]\{2,4\}fullnameOverride:' charts/arr-stack/values.yaml` == 10 (one per alias)
    - All 10 expected fullnameOverride values match alias names: `for a in sonarr radarr prowlarr cleanuparr qbittorrent seerr flaresolverr jellyfin arrconf configarr; do grep -q "fullnameOverride: $a$" charts/arr-stack/values.yaml || exit 1; done`
    - `! grep -q 'tag: latest' charts/arr-stack/values.yaml` (no :latest anywhere; 10/10 aliases pinned)
    - `tools/scripts/check-renovate-annotations.sh charts/arr-stack/values.yaml` exits 0 (all 10 `repository:` lines have annotations)
    - Cumulative render shape: `grep -c '^kind: Deployment$' /tmp/w4-full.yaml` == 8, `grep -c '^kind: CronJob$' /tmp/w4-full.yaml` == 2
  </acceptance_criteria>
  <done>Both CronJob aliases populated with v5 schema and concurrency Forbid. `values.yaml` now has all 10 alias bodies, no `:latest` tags, all renovate annotations in place, no v4-era keys. The chart's value-level work is complete.</done>
</task>

<task type="auto">
  <name>Task 5.3: Generate values.schema.json + ship examples/values-prod.yaml</name>
  <files>
    charts/arr-stack/values.schema.json
    examples/values-prod.yaml
  </files>
  <read_first>
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Standard Stack §Supporting" (losisin/helm-values-schema-json v2.4.0 — flagged MEDIUM confidence; check current GitHub release at execute time)
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-CONTEXT.md` D-04-VALUES-04 (full strict schema) + D-04-VALUES-03 (`examples/values-prod.yaml` ships as copy)
    - `CLAUDE.md` "Conventions Helm — umbrella chart" → `values.schema.json` posture: "À écrire dès qu'on ajoute des sections complexes."
  </read_first>
  <action>
**1) Install the helm plugin (if not present):**
```bash
helm plugin list | grep -q 'schema' || helm plugin install https://github.com/losisin/helm-values-schema-json
```

If the plugin command name is `helm schema` (as in the losisin repo), use that. If the version flagged (v2.4.0) is unreleased or missing at execute time, fall back to the latest stable from `https://github.com/losisin/helm-values-schema-json/releases` — record the version used in the SUMMARY.

**2) Generate the initial schema:**
```bash
helm schema -input charts/arr-stack/values.yaml -output charts/arr-stack/values.schema.json
```

This produces a JSON Schema Draft 2020-12 file that:
- Sets `additionalProperties: false` at the top level (strict, per D-04-VALUES-04)
- Enumerates the 10 expected top-level alias keys
- Captures every leaf field's type

**3) Hand-tighten (minimal scope for Phase 4):** Open `values.schema.json` and verify:
- Top level `additionalProperties` is `false` (if `true`, change to `false` — strict mode)
- Each alias is `type: object` with `additionalProperties: true` (app-template's deep value structure means we cannot enumerate all the sub-keys without exploding the schema; surface-level strictness is enough for Phase 4)

If the generator produces a schema that breaks `helm lint`, regenerate with looser settings and document the gap in the SUMMARY.

**4) Verify Helm picks up the schema:** When `values.schema.json` is present, `helm lint` automatically validates `values.yaml` against it. Run:
```bash
helm lint charts/arr-stack/
# expect: exits 0; output includes "values.yaml ... valid"
```

If the lint fails because of schema mismatches in the generated schema, regenerate the schema (the generated output is the canonical shape; the values are the source of truth).

**5) Ship `examples/values-prod.yaml`:** Per D-04-VALUES-03 — content copy of `values.yaml`:
```bash
mkdir -p examples
cp charts/arr-stack/values.yaml examples/values-prod.yaml
```

Add a header comment to `examples/values-prod.yaml`:
```yaml
# examples/values-prod.yaml
# This file is the production values reference for my-kluster's arr-stack-app.yaml.
# It is a content-copy of charts/arr-stack/values.yaml (D-04-VALUES-03).
# Keep them in sync; CI does NOT auto-validate divergence in Phase 4 (deferred to a follow-up).
```

The header is informational only — `examples/values-prod.yaml` content below the header MUST be byte-identical to `values.yaml` body. If a future PR drifts them, Renovate-edited `values.yaml` won't propagate to my-kluster's arr-stack-app.yaml valueFile path. A CI check for this drift is in Plan 04-06 scope.

**6) Final full-chart render with schema validation:**
```bash
helm lint charts/arr-stack/ -f examples/values-prod.yaml
helm template arr-stack charts/arr-stack/ -f examples/values-prod.yaml > /tmp/final-render.yaml
grep -c '^kind:' /tmp/final-render.yaml
# expect: ~30+ K8s objects (8 Deployments + 8 Services + 7 Ingresses + 8 PVCs implicit + ServiceAccounts + 2 CronJobs + 2 ConfigMaps)
```
  </action>
  <verify>
    <automated>bash -c 'helm lint charts/arr-stack/ -f examples/values-prod.yaml && test -f charts/arr-stack/values.schema.json && python3 -c "import json; json.load(open(\"charts/arr-stack/values.schema.json\"))" && test -f examples/values-prod.yaml && helm template arr-stack charts/arr-stack/ -f examples/values-prod.yaml > /tmp/final.yaml && test "$(grep -c \"^kind: CronJob$\" /tmp/final.yaml)" = "2"'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f charts/arr-stack/values.schema.json`
    - Schema is valid JSON: `python3 -c "import json; json.load(open('charts/arr-stack/values.schema.json'))"` exits 0
    - Schema declares Draft 2020-12 (or whichever Draft the plugin emits — capture in SUMMARY): `grep -q '$schema' charts/arr-stack/values.schema.json`
    - `helm lint charts/arr-stack/ -f examples/values-prod.yaml` exits 0 (lint with schema enabled passes)
    - `test -f examples/values-prod.yaml`
    - examples/values-prod.yaml content matches values.yaml body (ignoring the header comment): `diff <(sed '/^# /d' examples/values-prod.yaml) <(sed '/^# /d' charts/arr-stack/values.yaml) | head -5` shows minor diffs limited to comments/blank lines OR is empty
    - `helm template arr-stack charts/arr-stack/ -f examples/values-prod.yaml > /tmp/final.yaml` exits 0
    - Final render counts: `grep -c '^kind: Deployment$' /tmp/final.yaml` == 8
    - `grep -c '^kind: CronJob$' /tmp/final.yaml` == 2
    - `grep -c '^kind: Service$' /tmp/final.yaml` == 8 (one per media app)
    - `grep -c '^kind: Ingress$' /tmp/final.yaml` == 7 (flaresolverr no ingress)
    - `grep -c '^kind: ConfigMap$' /tmp/final.yaml` == 2 (arrconf-config + configarr-config)
    - **W3 carry-forward final assertion**: `! grep -q 'cronJobConfig:' charts/arr-stack/values.yaml`
    - **No :latest invariant**: `! grep -q 'tag: latest' charts/arr-stack/values.yaml`
    - **fullnameOverride contract for all 10 aliases**: `grep -c '^[[:space:]]\{2,4\}fullnameOverride:' charts/arr-stack/values.yaml` == 10
  </acceptance_criteria>
  <done>`values.schema.json` exists, is valid JSON, and `helm lint -f examples/values-prod.yaml` validates against it. The umbrella chart is feature-complete: 8 Deployments + 8 Services + 7 Ingresses + 2 CronJobs + 2 ConfigMaps rendered.</done>
</task>

</tasks>

<verification>
- `helm lint charts/arr-stack/ -f examples/values-prod.yaml` exits 0
- `helm template arr-stack charts/arr-stack/ -f examples/values-prod.yaml` produces 8 Deployments, 8 Services, 7 Ingresses, 2 CronJobs, 2 ConfigMaps
- `! grep -q 'tag: latest' charts/arr-stack/values.yaml`
- `! grep -q 'cronJobConfig:' charts/arr-stack/values.yaml`
- All 10 alias top-level keys populated with bodies, no remaining `{}` placeholders
- `values.schema.json` validates and is enforced by `helm lint`
- `examples/values-prod.yaml` body matches `values.yaml`
</verification>

<success_criteria>
- The umbrella is feature-complete at the values level
- CronJob aliases use the v5 schema (`cronjob:`, `successfulJobsHistory:`) — no v4 artefacts
- All Phase 4 deliberate functional changes are encoded (arrconf args expansion, qbittorrent/cleanuparr/flaresolverr pins)
- Schema enforcement is wired and green
- `examples/values-prod.yaml` is in place for my-kluster's arr-stack-app.yaml valueFile reference
</success_criteria>

<output>
After completion, create `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-05-SUMMARY.md` recording: (1) the losisin plugin version actually used, (2) any hand-tightening of the schema, (3) the final render shape (Deployments / Services / Ingresses / CronJobs / ConfigMaps), (4) confirmation of the 3 v5-specific invariants (no `cronJobConfig:`, no `tag: latest`, 10 `fullnameOverride` entries), (5) any deferred items for a follow-up (e.g. CI drift check between `values.yaml` and `examples/values-prod.yaml`).
</output>
