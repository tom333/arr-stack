---
phase: 04-umbrella-chart-migration-des-9-apps
plan: 08
type: execute
wave: 6
depends_on: [06, 07]
cross_repo: true
files_modified:
  # arr-stack repo
  - .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/umbrella-render.yaml
  - .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/byte-equivalence-diff.txt
  - .planning/phases/04-umbrella-chart-migration-des-9-apps/04-08-CUTOVER-LOG.md
  # my-kluster repo (cross-repo) — these are REAL files modified via a PR in the sister repo, NOT YAML comments
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/sonarr-app.yaml
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/radarr-app.yaml
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/prowlarr-app.yaml
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/cleanuparr-app.yaml
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/qbittorrent-app.yaml
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/seerr-app.yaml
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/flaresolverr-app.yaml
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/jellyfin-app.yaml
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arrconf-app.yaml
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/configarr-app.yaml
  - /home/moi/projets/perso/my-kluster/charts/arrconf/  # directory deletion
  - /home/moi/projets/perso/my-kluster/charts/configarr/  # directory deletion
autonomous: false
requirements:
  - REQ-umbrella-deployment
  - REQ-pr-to-cluster-latency
  - REQ-helm-validation

must_haves:
  truths:
    - "Local byte-equivalence diff captured in `evidence/byte-equivalence-diff.txt` showing only D-04-CUTOVER-04 approved diffs (instance label, name label) — no unexpected drift"
    - "Atomic my-kluster PR opened: 1 file added (`arr-stack-app.yaml` WITHOUT `automated:` block), 10 unit `*-app.yaml` deleted, 2 chart directories deleted (`charts/arrconf/`, `charts/configarr/`)"
    - "`arr-stack-app.yaml` ships with `syncOptions: [CreateNamespace=true, ServerSideApply=true, Replace=true]` — `Replace=true` is REQUIRED (D-04-CUTOVER-05; Deployment selector immutability)"
    - "Operator runs the kubectl-fallback cutover sequence (since `argocd` CLI is absent — confirmed in STATE.md Phase 02.2 P05 lesson)"
    - "10-minute soak passes: all 8 media-app pods reach `Running` + `Ready 1/1`; both CronJobs are `NotScheduled` until their next 4h tick"
    - "Ingress smoke test passes for the 7 publicly-exposed apps (sonarr/radarr/prowlarr/cleanuparr/qbittorrent/seerr/jellyfin → HTTP 200/302/401)"
    - "ArgoCD reports `Synced + Healthy` on `arr-stack` Application"
    - "Brief pod restarts (5-10s per app) observed at cutover and accepted (Replace=true semantics, RESEARCH §Critical Naming Constraint)"
  artifacts:
    - path: .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/umbrella-render.yaml
      provides: "The exact rendered manifests applied at cutover"
    - path: .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/byte-equivalence-diff.txt
      provides: "Diff output between umbrella render and Wave 0 pre-cutover-argocd baseline"
    - path: .planning/phases/04-umbrella-chart-migration-des-9-apps/04-08-CUTOVER-LOG.md
      provides: "Operator-authored runbook completion record: PR URL, sync timing, soak observations"
  key_links:
    - from: /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml
      to: charts/arr-stack/ in this repo
      via: "ArgoCD spec.source.repoURL + path + targetRevision"
      pattern: "path: charts/arr-stack"
    - from: arr-stack-app.yaml syncOptions
      to: Deployment selector mutation tolerance
      via: "Replace=true"
      pattern: "Replace=true"
---

<objective>
Execute the cutover: render the umbrella locally, diff against the Wave 0 baseline, then open an **atomic cross-repo PR** in `my-kluster` that (a) adds `arr-stack-app.yaml` WITHOUT the `automated:` block, (b) deletes the 10 unit ArgoCD Application YAMLs, (c) deletes `charts/arrconf/` and `charts/configarr/`. Operator drives `kubectl apply` and `kubectl get application … -w` (argocd CLI is absent — STATE.md Phase 02.2 P05).

Brief pod restarts (5-10s per app) are expected and accepted (D-04-CUTOVER-05 + RESEARCH §"Critical Naming Constraint"): the cutover changes `app.kubernetes.io/instance` from per-app release names to `arr-stack`, and Deployment selectors are immutable — Replace=true delete-recreates affected resources.

Plan 04-09 (Wave 7) handles the follow-up PR re-enabling `automated.{selfHeal,prune}` and captures the SC#2 latency evidence on the first Renovate-driven image bump.

Output: cutover succeeds, ArgoCD reports `Synced + Healthy`, the 7 public hostnames respond, both CronJobs are wired (next fire within 4h).

**Cross-repo discipline (B2 carry-forward):** This plan modifies files in `/home/moi/projets/perso/my-kluster/`. The `files_modified` list above enumerates ALL 13 cross-repo paths as REAL entries. Operator must `cd /home/moi/projets/perso/my-kluster` to perform the file mutations and open the PR there.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-CONTEXT.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-PATTERNS.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-05-SUMMARY.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-06-SUMMARY.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-07-SUMMARY.md
@CLAUDE.md
@/home/moi/projets/perso/my-kluster/CLAUDE.md

<interfaces>
Source: `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/sonarr-app.yaml` (and 9 sibling files — all to be DELETED in this PR)
Source: `git -C /home/moi/projets/perso/my-kluster show main:charts/arrconf/` and `charts/configarr/` (both to be DELETED)

Cutover-target file shape — `my-kluster/argocd/argocd-apps/arr-stack-app.yaml`:
  apiVersion: argoproj.io/v1alpha1
  kind: Application
  metadata:
    name: arr-stack
    namespace: argocd
    finalizers:
      - resources-finalizer.argocd.argoproj.io
  spec:
    project: selfhost-project
    destination:
      server: https://kubernetes.default.svc
      namespace: selfhost
    source:
      repoURL: https://github.com/tom333/arr-stack.git
      targetRevision: v0.1.0   # the auto-tag from Plan 04-06's chart-lint job
      path: charts/arr-stack
      helm:
        valueFiles:
          - ../../examples/values-prod.yaml  # path relative to chart dir per my-kluster ArgoCD pattern
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
        - Replace=true
      # NO automated: block at cutover (D-04-CUTOVER-02) — Plan 04-09 adds it after first sync clears

Cluster API (read-only / apply only — no port-forward needed):
  kubectl -n argocd get application arr-stack -o yaml
  kubectl -n argocd get application arr-stack -o jsonpath='{.status.sync.status} {.status.health.status} rev={.status.sync.revision}'
  kubectl -n selfhost get pods -o wide -w
  kubectl -n selfhost get all,ingress,configmap

Helper scripts (Plan 04-01):
  tools/scripts/byte-equivalence-diff.sh  (will be exercised in Task 8.1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 8.1: Render umbrella locally + run byte-equivalence diff vs Wave 0 baseline</name>
  <files>
    .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/umbrella-render.yaml
    .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/byte-equivalence-diff.txt
  </files>
  <read_first>
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Cutover Sequence" + §"Critical Naming Constraint" (the 2 expected diffs: instance label, name label)
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-CONTEXT.md` D-04-CUTOVER-03 (byte-equivalence gate)
    - `tools/scripts/byte-equivalence-diff.sh` (helper from Plan 04-01)
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/` (10 baseline YAMLs — the diff target)
  </read_first>
  <action>
1) Render the umbrella deterministically:
```bash
cd /home/moi/projets/perso/arr-stack
helm dependency update charts/arr-stack/
helm template arr-stack charts/arr-stack/ -f examples/values-prod.yaml \
  > .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/umbrella-render.yaml
```

2) Run the helper diff against the Wave 0 baseline:
```bash
tools/scripts/byte-equivalence-diff.sh \
  .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/ \
  .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/umbrella-render.yaml \
  > .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/byte-equivalence-diff.txt 2>&1 || true
```

(The `|| true` is intentional: the script exits 1 on detected diffs, and we EXPECT a non-empty diff because Replace=true semantics + selector relabeling produce the D-04-CUTOVER-04 approved diffs.)

3) Manually classify the diff output. Expected approved diffs:
- `app.kubernetes.io/instance: arr-stack` (was: per-app release name)
- `app.kubernetes.io/name: arr-stack` (was: per-app release name, when `fullnameOverride` is in play)
- `helm.sh/chart: app-template-5.0.0` — IDENTICAL before/after (both at 5.0.0 — no diff)
- arrconf CronJob args: `--apps sonarr,radarr,prowlarr` vs old `--apps sonarr` (D-04-CRON-03 deliberate functional change)
- qbittorrent image tag `5.2.0` vs `latest` — semantically identical (digest match) but textually different (D-04-PIN-01)
- cleanuparr image tag `2.3.3` vs `latest` — same
- flaresolverr image tag `v3.4.6` vs `latest` — same

UNEXPECTED diffs (any of these blocks the cutover):
- Service name change (e.g. `arr-stack-sonarr` instead of `sonarr` — would mean fullnameOverride is missing)
- Port number changes
- Ingress hostname changes
- PVC name changes (would mean existingClaim was omitted somewhere)
- ConfigMap name changes (`arrconf-config` or `configarr-config` must be hardcoded)

Write a classification summary at the bottom of `evidence/byte-equivalence-diff.txt`:

```
=== CUTOVER DIFF CLASSIFICATION ===
APPROVED (D-04-CUTOVER-04):
  - app.kubernetes.io/instance label: <count> occurrences
  - app.kubernetes.io/name label: <count> occurrences
APPROVED (D-04-CRON-03 deliberate functional change):
  - arrconf args expansion: sonarr → sonarr,radarr,prowlarr
APPROVED (D-04-PIN-01 image pinning, semantically identical via digest match):
  - qbittorrent: latest → 5.2.0
  - cleanuparr: latest → 2.3.3
  - flaresolverr: latest → v3.4.6
UNEXPECTED:
  - <empty — if non-empty, STOP cutover>

VERDICT: <PROCEED | STOP>
```

If VERDICT=STOP, surface to operator and do NOT proceed to Task 8.2.
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/umbrella-render.yaml && test -f .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/byte-equivalence-diff.txt && grep -q "VERDICT: PROCEED" .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/byte-equivalence-diff.txt'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/umbrella-render.yaml` (render artifact exists)
    - `grep -c '^kind: Deployment$' .planning/phases/04-*/evidence/umbrella-render.yaml` == 8
    - `grep -c '^kind: CronJob$' .planning/phases/04-*/evidence/umbrella-render.yaml` == 2
    - `grep -c '^kind: Ingress$' .planning/phases/04-*/evidence/umbrella-render.yaml` == 7
    - `test -f .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/byte-equivalence-diff.txt`
    - Classification block present: `grep -q "VERDICT:" .planning/phases/04-*/evidence/byte-equivalence-diff.txt`
    - Verdict explicitly PROCEED (operator-authored): `grep -q "VERDICT: PROCEED" .planning/phases/04-*/evidence/byte-equivalence-diff.txt`
    - Approved diff categories enumerated: `grep -q "app.kubernetes.io/instance" .planning/phases/04-*/evidence/byte-equivalence-diff.txt`
    - No unexpected entries: `! grep -qE 'UNEXPECTED:[[:space:]]*\n[[:space:]]*-[[:space:]]+[A-Za-z]' .planning/phases/04-*/evidence/byte-equivalence-diff.txt` (the UNEXPECTED block, if present, has no bulleted entries beneath it)
  </acceptance_criteria>
  <done>The local render is captured, diffed against the baseline, classified, and operator-approved with VERDICT: PROCEED. If the verdict is STOP, the cutover is halted before any my-kluster mutation.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8.2: Open atomic cross-repo PR in my-kluster (1 add + 10 delete + 2 dir delete)</name>
  <files>
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/sonarr-app.yaml
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/radarr-app.yaml
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/prowlarr-app.yaml
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/cleanuparr-app.yaml
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/qbittorrent-app.yaml
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/seerr-app.yaml
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/flaresolverr-app.yaml
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/jellyfin-app.yaml
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arrconf-app.yaml
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/configarr-app.yaml
    /home/moi/projets/perso/my-kluster/charts/arrconf/
    /home/moi/projets/perso/my-kluster/charts/configarr/
  </files>
  <action>
    Operator-driven cross-repo PR creation. Decision context:
    - Wave 0 baseline confirmed against actual production state (commit 2a94257, app-template 5.0.0).
    - Local byte-equivalence diff captured and classified PROCEED (Task 8.1).
    - The `arr-stack-app.yaml` ships WITHOUT the `automated:` block at this stage (D-04-CUTOVER-02). Plan 04-09 adds it after first sync clears.

    Options:
    - OPEN: Open the PR + apply — closes Phase 4 cutover; 5-10s pod restart per app accepted.
    - DEFER: Defer the cutover — more observation time, but Phase 4 stays open and Phases 5/6/7 are blocked.

    Operator runbook:

    Step A — Switch to my-kluster repo, fast-forward main:
        cd /home/moi/projets/perso/my-kluster
        git checkout main && git pull --ff-only origin main
        git checkout -b cutover/arr-stack-umbrella-phase-4

    Step B — Create the new ArgoCD Application file at argocd/argocd-apps/arr-stack-app.yaml with the exact body below:
        apiVersion: argoproj.io/v1alpha1
        kind: Application
        metadata:
          name: arr-stack
          namespace: argocd
          finalizers:
            - resources-finalizer.argocd.argoproj.io
        spec:
          project: selfhost-project
          destination:
            server: https://kubernetes.default.svc
            namespace: selfhost
          source:
            repoURL: https://github.com/tom333/arr-stack.git
            targetRevision: v0.1.0    # bump to the latest tag if v0.1.0 is not the auto-tag from Plan 04-06
            path: charts/arr-stack
            helm:
              valueFiles:
                - ../../examples/values-prod.yaml
          syncPolicy:
            syncOptions:
              - CreateNamespace=true
              - ServerSideApply=true
              - Replace=true
            # automated: block deliberately omitted (D-04-CUTOVER-02)
            # — added in follow-up PR by Plan 04-09 once Wave 6 sync is green.

    Step C — Delete the 10 unit Applications:
        rm argocd/argocd-apps/sonarr-app.yaml
        rm argocd/argocd-apps/radarr-app.yaml
        rm argocd/argocd-apps/prowlarr-app.yaml
        rm argocd/argocd-apps/cleanuparr-app.yaml
        rm argocd/argocd-apps/qbittorrent-app.yaml
        rm argocd/argocd-apps/seerr-app.yaml
        rm argocd/argocd-apps/flaresolverr-app.yaml
        rm argocd/argocd-apps/jellyfin-app.yaml
        rm argocd/argocd-apps/arrconf-app.yaml
        rm argocd/argocd-apps/configarr-app.yaml

    Step D — Delete the 2 chart directories:
        rm -rf charts/arrconf/
        rm -rf charts/configarr/

    Step E — Commit + push + open PR:
        git add -A argocd/argocd-apps/ charts/
        git status   # verify: 1 new file, 10 deleted files, 2 deleted directories
        git commit -m "chore(arr-stack): cutover to umbrella chart (Phase 4)"
        git push -u origin cutover/arr-stack-umbrella-phase-4
        gh pr create --title "arr-stack umbrella cutover (Phase 4)" --body "<see Plan 04-08 PLAN.md for full body>"

    Step F — Capture the PR URL and paste it as the resume-signal `approved &lt;PR-URL&gt;`.

    Resume signal:
    - "approved &lt;PR-URL&gt;" once the PR is open
    - "deferred" to keep Phase 4 open
  </action>
  <verify>
    <automated>bash -c 'test -f /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml && grep -q "Replace=true" /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml && ! grep -qE "^[[:space:]]*automated:" /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml'</automated>
  </verify>
  <done>PR open in my-kluster with the 13-path diff (1 add + 10 unit-app deletes + 2 chart-dir deletes); arr-stack-app.yaml carries syncOptions Replace=true but NO automated block; operator returns the PR URL.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 8.3: Operator merges PR + kubectl-driven cutover sync + 10-min soak + ingress smoke</name>
  <files>
    /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml
    .planning/phases/04-umbrella-chart-migration-des-9-apps/04-08-CUTOVER-LOG.md
  </files>
  <action>
    Operator-driven sync. `argocd` CLI is absent (STATE.md Phase 02.2 P05), so the operator uses the kubectl fallback path. The merge itself is operator-only (one-button merge in GitHub UI or `gh pr merge`).

    Options:
    - MERGE: Merge + drive the sync — Phase 4 cutover closes; 5-10s pod restart per app accepted.
    - ABORT: Close the PR + git revert (rollback) — production stays on old unit Apps; root-cause the blocker.

    Operator runbook:

    Step A — Merge the PR:
        cd /home/moi/projets/perso/my-kluster
        gh pr merge --merge cutover/arr-stack-umbrella-phase-4   # or --squash per operator preference

    Step B — Wait for ArgoCD-AppSet to pick up the new file (~3 min default refresh):
        kubectl -n argocd get application arr-stack -w  # waits for Application to appear
        # OR force a project refresh:
        kubectl annotate application -n argocd &lt;appset-name&gt; argocd.argoproj.io/refresh=hard --overwrite

    Step C — First sync (operator-driven, since `automated:` is omitted):
        # Force a sync via the kubectl Application patch path:
        kubectl -n argocd patch application arr-stack --type=merge \
          -p '{"operation":{"sync":{"syncStrategy":{"apply":{"force":false}}}}}'
        # OR use the ArgoCD UI "Sync" button.

    Step D — Watch the sync progress:
        kubectl -n argocd get application arr-stack \
          -o jsonpath='{.status.sync.status} {.status.health.status} rev={.status.sync.revision}{"
"}' \
          | tee /tmp/cutover-sync-progress.log
        # Expected sequence: OutOfSync+Progressing → Syncing+Progressing → Synced+Progressing → Synced+Healthy

    Step E — 10-minute soak (cumulative observation of all pods reaching Ready):
        kubectl -n selfhost get pods -o wide -w
        # Verify: all 8 media-app pods Ready 1/1, both CronJobs NotScheduled (until next 4h tick)

    Step F — Per-app rollout status sanity check:
        for app in sonarr radarr prowlarr cleanuparr qbittorrent seerr flaresolverr jellyfin; do
          echo "=== $app ==="
          kubectl rollout status -n selfhost deployment/$app --timeout=60s
        done
        # All must report 'successfully rolled out'

    Step G — Ingress smoke test (the 7 publicly-exposed apps):
        for h in sonarr radarr prowlarr cleanuparr qbittorrent seerr jellyfin; do
          code=$(curl -I -s -o /dev/null -w "%{http_code}" "https://${h}.tgu.ovh")
          echo "$h: HTTP $code"
        done
        # Expected: 200/302/401 for each. Flaresolverr is in-cluster only — no ingress to test.

    Step H — CronJob sanity check:
        kubectl -n selfhost get cronjob arrconf configarr -o wide
        # Verify: SCHEDULE 0 */4 * * *, SUSPEND False, ACTIVE 0

    Step I — Write `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-08-CUTOVER-LOG.md` with PR URL, sync timing, per-pod restart durations, ingress smoke table, CronJob state, deviations.

    Resume signal:
    - "approved &lt;PR-URL&gt; synced+healthy" once `kubectl get application arr-stack -n argocd` reports `Synced + Healthy` AND the 7 ingresses respond AND both CronJobs are scheduled.
    - "rollback &lt;reason&gt;" to escalate.
  </action>
  <verify>
    <automated>bash -c 'kubectl -n argocd get application arr-stack -o jsonpath="{.status.sync.status} {.status.health.status}" | grep -E "Synced[[:space:]]+Healthy" && for a in sonarr radarr prowlarr cleanuparr qbittorrent seerr flaresolverr jellyfin; do test ! -f /home/moi/projets/perso/my-kluster/argocd/argocd-apps/${a}-app.yaml || exit 1; done && ! test -d /home/moi/projets/perso/my-kluster/charts/arrconf && ! test -d /home/moi/projets/perso/my-kluster/charts/configarr && test -f .planning/phases/04-umbrella-chart-migration-des-9-apps/04-08-CUTOVER-LOG.md'</automated>
  </verify>
  <done>arr-stack ArgoCD Application Synced+Healthy; 10 unit Apps gone from my-kluster main; 2 chart directories gone; cutover log committed in arr-stack repo; brief 5-10s pod restart observed per app and accepted.</done>
</task>

</tasks>

<verification>
- Byte-equivalence diff classified VERDICT: PROCEED
- Cross-repo PR opened, merged
- arr-stack Application synced + healthy
- 7 public ingresses respond
- 2 CronJobs wired correctly
- 10 unit Apps + 2 chart dirs removed from my-kluster
- `arr-stack-app.yaml` ships WITHOUT `automated:` block (D-04-CUTOVER-02)
- `Replace=true` in syncOptions (D-04-CUTOVER-05)
- Cutover log committed
</verification>

<success_criteria>
- REQ-umbrella-deployment: single ArgoCD App deploys all 11 components
- D-04-CUTOVER-* satisfied (1: atomic PR, 2: no automated at cutover, 3: byte-equivalence verified, 5: Replace=true)
- No regression on internal DNS (`fullnameOverride` discipline confirmed by sync success)
- No regression on storage (existingClaim discipline preserved PVCs)
- Brief 5-10s pod restart per media app accepted (RESEARCH §"Critical Naming Constraint")
- Plan 04-09 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-08-SUMMARY.md` referencing:
- PR URL + merge SHA
- Cutover timing: T0 (merge), T+sync (Synced+Healthy), T+soak (10-min observation closed)
- Replace=true semantics observed (pods restarted as expected)
- Any deviation worth flagging in Plan 04-09 narrative
</output>
