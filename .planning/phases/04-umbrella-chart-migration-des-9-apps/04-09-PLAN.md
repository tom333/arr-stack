---
phase: 04-umbrella-chart-migration-des-9-apps
plan: 09
type: execute
wave: 7
depends_on: [08]
cross_repo: true
files_modified:
  # arr-stack repo
  - .planning/phases/04-umbrella-chart-migration-des-9-apps/04-09-SC2-EVIDENCE.md
  - .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/sc2-renovate-pr.txt
  # my-kluster repo (cross-repo)
  - /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml
autonomous: false
requirements:
  - REQ-umbrella-deployment
  - REQ-pr-to-cluster-latency
  - REQ-renovate-image-tracking

must_haves:
  truths:
    - "Follow-up PR opened + merged in my-kluster re-enabling `automated.{selfHeal,prune}` on `arr-stack-app.yaml`"
    - "ArgoCD remains `Synced + Healthy` after the automated block is re-applied"
    - "SC#2 (REQ-pr-to-cluster-latency) reaches a definitive verdict within 72h of cutover — either by natural Renovate bump (0-48h) or by D-04-PIN-04 Path B forced bump (48-72h)"
    - "Auto-tag job from chart-lint.yml fires on the values.yaml change → my-kluster Renovate scan opens a `targetRevision` bump PR within the same Renovate cycle → ArgoCD sync < 1 h post-merge"
  artifacts:
    - path: .planning/phases/04-umbrella-chart-migration-des-9-apps/04-09-SC2-EVIDENCE.md
      provides: "Timeline of the first Renovate-driven image bump end-to-end"
    - path: .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/sc2-renovate-pr.txt
      provides: "Captured Renovate PR metadata (URL, dependency, timing)"
  key_links:
    - from: /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml
      to: ArgoCD automated reconciliation
      via: "syncPolicy.automated.{selfHeal: true, prune: true}"
      pattern: "automated:"
---

<objective>
Close the cutover with a one-line follow-up PR that re-enables `automated.{selfHeal, prune}` on `arr-stack-app.yaml`, then watch for the first Renovate-driven image bump to land end-to-end. SC#2 (REQ-pr-to-cluster-latency) requires the bump to materialize in cluster within 1 hour of merge — that's the latency target.

Three milestones (M1 / M2 / M3 — B4 carry-forward):
- **M1 (T+0h → T+48h)**: Watch the Renovate cycle. arr-stack has 10 tracked images; even minor bumps on any one count. If a bump opens naturally during this window, capture timing evidence and proceed to M3 verification.
- **M2 (T+48h sharp)**: If no natural bump has opened by the 48h mark, execute D-04-PIN-04 Path B — force a downgrade on one image (e.g. cleanuparr `2.3.3` → `2.3.2`) in a manual PR to **arr-stack/values.yaml**, push, merge. Renovate will immediately propose a rebump back to `2.3.3` (or the latest available). This validates the customManagers regex + auto-merge pipeline without waiting indefinitely for upstream releases.
- **M3 (T+72h sharp)**: SC#2 verdict captured — either GREEN (bump completed < 1h, evidence committed) or RED (bump did not close, root cause documented, escalation path noted). Phase 4 cannot remain open past M3.

Output: `automated:` re-enabled (one-line PR), evidence file capturing the SC#2 timeline, 72h hard decision gate respected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-CONTEXT.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-08-SUMMARY.md
@CLAUDE.md
@/home/moi/projets/perso/my-kluster/CLAUDE.md

<interfaces>
Source: `/home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml` (created in Plan 04-08 WITHOUT automated block)
  Target shape after Plan 04-09 follow-up PR:
    syncPolicy:
      syncOptions:
        - CreateNamespace=true
        - ServerSideApply=true
        - Replace=true
      automated:           # ← NEW (this plan's diff)
        prune: true
        selfHeal: true

Cluster observation:
  kubectl -n argocd get application arr-stack -o jsonpath='{.status.sync.status} {.status.health.status}'
  kubectl -n selfhost get pods -o wide

SC#2 target metric: merge of Renovate PR (either arr-stack values.yaml bump OR my-kluster targetRevision bump) → ArgoCD sync visible in cluster < 1 h.

The auto-tag job (B3, Plan 04-06) collapses what was previously T1→T2 of the chain: Renovate-PR-merge → auto-tag → my-kluster-Renovate-detection. The remaining bottleneck is my-kluster's Renovate scan cycle (default 1h-ish). Plan 04-09's narrative reflects this T1→T2 collapse.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 9.1: Operator opens follow-up PR re-enabling `automated.{selfHeal, prune}`</name>
  <files>/home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml</files>
  <action>
    Operator-driven one-line PR in my-kluster. The diff is 4 added lines.

    Decision context:
    - Plan 04-08 cutover succeeded; arr-stack-app.yaml is in main without the `automated:` block.
    - Production ran in operator-driven sync mode during the soak. Time to flip on automated.{selfHeal,prune}.

    Options:
    - OPEN: 1-line PR + merge — closes Phase 4 config-drift gate; ArgoCD becomes self-healing again.
    - DEFER: keep operator-driven mode — more control but departs from my-kluster's long-standing convention; inconsistent with the rest of the cluster.

    Operator runbook:
        cd /home/moi/projets/perso/my-kluster
        git checkout main && git pull --ff-only origin main
        git checkout -b chore/arr-stack-enable-automated

    Edit `argocd/argocd-apps/arr-stack-app.yaml` — under `spec.syncPolicy`, add the `automated:` block:
        syncPolicy:
          syncOptions:
            - CreateNamespace=true
            - ServerSideApply=true
            - Replace=true
          automated:           # &lt;-- NEW
            prune: true        # &lt;-- NEW
            selfHeal: true     # &lt;-- NEW

    Then:
        git add argocd/argocd-apps/arr-stack-app.yaml
        git diff --staged   # verify: 4 lines added, no other change
        git commit -m "chore(arr-stack): re-enable automated.{selfHeal,prune} (Phase 4 closure)"
        git push -u origin chore/arr-stack-enable-automated
        gh pr create --title "chore(arr-stack): re-enable automated reconciliation" --body "1-line follow-up after Phase 4 cutover."

    After merge, verify cluster picks it up cleanly:
        kubectl -n argocd get application arr-stack -o yaml | grep -A3 'automated:'
        # Expected: automated: prune: true selfHeal: true
        kubectl -n argocd get application arr-stack \
          -o jsonpath='{.status.sync.status} {.status.health.status}'
        # Expected: Synced Healthy (no regression)

    Resume signal:
    - "approved &lt;PR-URL&gt;" once the PR is merged.
    - "deferred &lt;reason&gt;" to leave automated off.
  </action>
  <verify>
    <automated>bash -c 'grep -A2 "automated:" /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml | grep -q "selfHeal: true" && grep -A2 "automated:" /home/moi/projets/perso/my-kluster/argocd/argocd-apps/arr-stack-app.yaml | grep -q "prune: true" && kubectl -n argocd get application arr-stack -o jsonpath="{.status.sync.status} {.status.health.status}" | grep -E "Synced[[:space:]]+Healthy"'</automated>
  </verify>
  <done>Follow-up PR merged on my-kluster main; automated.{selfHeal,prune} re-enabled on arr-stack-app.yaml; ArgoCD remains Synced + Healthy after the change applies.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 9.2: Watch + capture SC#2 (REQ-pr-to-cluster-latency) — 72h hard decision gate (M1 / M2 / M3)</name>
  <files>
    .planning/phases/04-umbrella-chart-migration-des-9-apps/04-09-SC2-EVIDENCE.md
    .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/sc2-renovate-pr.txt
  </files>
  <action>
    Watch the natural Renovate cycle for 48h. If no bump opens by M2, execute D-04-PIN-04 Path B (force a manual rebump-trigger). At M3 (T+72h sharp), record a definitive verdict — Phase 4 cannot remain open past M3.

    Three milestones (B4 carry-forward):

    M1 (T+0h → T+48h) — watch loop:
        cd /home/moi/projets/perso/arr-stack
        gh pr list --state open --label renovate
        # Or by title:
        gh pr list --state open --search "in:title chore(deps)"

    When a bump PR opens:
        gh pr view &lt;PR-NUMBER&gt; --json title,body,createdAt,mergedAt > /tmp/sc2-arr-stack-pr.json
        gh pr view &lt;PR-NUMBER&gt; --json state,mergedAt
        cd /home/moi/projets/perso/my-kluster
        gh pr list --state open --search "in:title chore(deps) targetRevision arr-stack"
        gh pr view &lt;PR-NUMBER&gt; --json title,body,createdAt
        # After merge:
        kubectl -n argocd get application arr-stack \
          -o jsonpath='{.status.sync.revision}{"
"}'
        # Compare against the new release tag. Time the gap (PR-merge to revision-update) — should be < 1h.

    Record in `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-09-SC2-EVIDENCE.md`:
    - T0: arr-stack Renovate PR opened (UTC)
    - T1: arr-stack PR merged + auto-tag fired
    - T2: my-kluster Renovate PR opened
    - T3: my-kluster PR merged
    - T4: ArgoCD shows new sync.revision
    - T5: new pod observed in cluster
    - Total latency T3 → T5 (the SC#2 metric)

    M2 (T+48h sharp) — only if no natural M1 bump:
        cd /home/moi/projets/perso/arr-stack
        git checkout main && git pull --ff-only origin main
        git checkout -b chore/force-rebump-sc2-evidence
        # Edit charts/arr-stack/values.yaml — cleanuparr tag "2.3.3" → "2.3.2" (forced downgrade D-04-PIN-04 Path B)
        # ALSO update examples/values-prod.yaml to stay in sync
        git diff   # verify minimal diff
        git add charts/arr-stack/values.yaml examples/values-prod.yaml
        git commit -m "chore(deps): force cleanuparr downgrade to trigger SC#2 evidence (D-04-PIN-04 Path B)"
        git push -u origin chore/force-rebump-sc2-evidence
        gh pr create --title "chore(deps): force cleanuparr downgrade (SC#2 evidence)" --body "D-04-PIN-04 Path B — forced rebump trigger for Phase 4 SC#2 latency capture."
        gh pr merge --merge &lt;PR-NUMBER&gt;
        # Renovate immediately proposes 2.3.2 → 2.3.3 — proceed with M1 capture flow.

    M3 (T+72h sharp) — verdict:
    - GREEN: `04-09-SC2-EVIDENCE.md` shows a full T0→T5 timeline with `T3→T5 < 1h`. SC#2 satisfied.
    - RED: Timeline shows `T3→T5 >= 1h` OR the chain broke. Root cause documented; Phase 4 closes with SC#2 as a deferred concern carried into Phase 5 retrospective.

    Resume signal (sequential):
    - "M1 bump observed PR=&lt;URL&gt; sync_time=&lt;minutes&gt;" — natural bump happened, record timing
    - OR "M2 forced PR=&lt;URL&gt;" — force-rebump triggered at T+48h
    - "M3 verdict &lt;GREEN|RED&gt; details=&lt;file-path&gt;" — final closure
  </action>
  <verify>
    <automated>bash -c 'test -f .planning/phases/04-umbrella-chart-migration-des-9-apps/04-09-SC2-EVIDENCE.md && grep -cE "T[0-5]:" .planning/phases/04-*/04-09-SC2-EVIDENCE.md | xargs test 6 -le && grep -qE "VERDICT:[[:space:]]+(GREEN|RED)" .planning/phases/04-*/04-09-SC2-EVIDENCE.md'</automated>
  </verify>
  <done>04-09-SC2-EVIDENCE.md contains T0-T5 timestamps and an explicit GREEN/RED verdict; the verdict was captured at or before T+72h relative to the cutover timestamp in 04-08-CUTOVER-LOG.md; a Renovate PR URL is recorded.</done>
</task>

</tasks>

<verification>
- Follow-up PR opened + merged + ArgoCD still Synced + Healthy with `automated:` re-enabled
- SC#2 evidence file exists with T0-T5 timestamps and an explicit GREEN/RED verdict
- 72h hard decision gate respected (verdict captured at or before T+72h)
- Auto-tag job (B3) observed firing at least once (either via natural M1 or forced M2)
</verification>

<success_criteria>
- REQ-pr-to-cluster-latency: definitive verdict captured within 72h
- D-04-CUTOVER-02 hand-off complete (`automated:` re-enabled in follow-up PR)
- T1→T2 collapse (auto-tag) observed in practice
- Phase 4 closure unblocked (gsd-verify-work can fire next)
</success_criteria>

<output>
After completion, create `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-09-SUMMARY.md` referencing:
- The 1-line follow-up PR URL + merge SHA
- The SC#2 verdict (GREEN/RED)
- The captured T3→T5 latency in minutes
- Whether M1 (natural) or M2 (forced) path produced the evidence
- Any deferred items routed to Phase 5 (e.g. Renovate scan cycle tuning if SC#2 came back RED)
</output>
