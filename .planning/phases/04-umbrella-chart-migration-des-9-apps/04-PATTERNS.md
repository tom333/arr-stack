# Phase 4: Umbrella chart + migration des 9 apps — Pattern Map

**Mapped:** 2026-05-12
**Files analyzed:** 27 new/modified files (arr-stack) + 12 cross-repo deletions (my-kluster)
**Analogs found:** 24 / 27 (3 are new with no codebase analog)

---

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `charts/arr-stack/Chart.yaml` | chart-manifest | config | `my-kluster/charts/arrconf/Chart.yaml` | role-match |
| `charts/arr-stack/Chart.lock` | chart-manifest | config | none — generated by `helm dependency update` | generated |
| `charts/arr-stack/charts/` (tarballs) | chart-manifest | config | none — downloaded artifacts | generated |
| `charts/arr-stack/values.yaml` (sonarr alias) | chart-values | request-response | `my-kluster/argocd/argocd-apps/sonarr-app.yaml` helm.values | exact |
| `charts/arr-stack/values.yaml` (radarr alias) | chart-values | request-response | `my-kluster/argocd/argocd-apps/radarr-app.yaml` helm.values | exact |
| `charts/arr-stack/values.yaml` (prowlarr alias) | chart-values | request-response | `my-kluster/argocd/argocd-apps/prowlarr-app.yaml` helm.values | exact |
| `charts/arr-stack/values.yaml` (cleanuparr alias) | chart-values | request-response | `my-kluster/argocd/argocd-apps/cleanuparr-app.yaml` helm.values | exact |
| `charts/arr-stack/values.yaml` (qbittorrent alias) | chart-values | request-response | `my-kluster/argocd/argocd-apps/qbittorrent-app.yaml` helm.values | exact |
| `charts/arr-stack/values.yaml` (seerr alias) | chart-values | request-response | `my-kluster/argocd/argocd-apps/seerr-app.yaml` helm.values | exact |
| `charts/arr-stack/values.yaml` (flaresolverr alias) | chart-values | request-response | `my-kluster/argocd/argocd-apps/flaresolverr-app.yaml` helm.values | exact |
| `charts/arr-stack/values.yaml` (jellyfin alias) | chart-values | request-response | `my-kluster/argocd/argocd-apps/jellyfin-app.yaml` helm.values | exact |
| `charts/arr-stack/values.yaml` (arrconf alias) | chart-values | batch | `my-kluster/charts/arrconf/values.yaml` + cronjob.yaml | role-match |
| `charts/arr-stack/values.yaml` (configarr alias) | chart-values | batch | `my-kluster/charts/configarr/values.yaml` + cronjob.yaml | role-match |
| `charts/arr-stack/values.schema.json` | config | config | none — generated by losisin/helm-values-schema-json | generated |
| `charts/arr-stack/templates/_helpers.tpl` | helm-helper | config | `my-kluster/charts/arrconf/templates/_helpers.tpl` | role-match |
| `charts/arr-stack/templates/arrconf-configmap.yaml` | chart-manifest | config | `my-kluster/charts/arrconf/templates/configmap.yaml` | exact |
| `charts/arr-stack/templates/configarr-configmap.yaml` | chart-manifest | config | `my-kluster/charts/configarr/templates/configmap.yaml` | exact |
| `charts/arr-stack/files/arrconf.yml` | config-file | config | `my-kluster/charts/arrconf/files/arrconf.yml` | exact |
| `charts/arr-stack/files/configarr.yml` | config-file | config | `my-kluster/charts/configarr/files/config.yml` | exact |
| `examples/values-prod.yaml` | config-file | config | `charts/arr-stack/values.yaml` (copy/symlink) | exact |
| `.github/workflows/chart-lint.yml` | ci-workflow | request-response | `.github/workflows/tests.yml` | role-match |
| `renovate.json` (modified) | config-bot | config | `renovate.json` (current 3-line file) | role-match |
| `README.md` (rewritten) | docs | config | `README.md` (current 29-line file) | role-match |
| `CLAUDE.md` (rewritten) | docs | config | `CLAUDE.md` (current file) | role-match |
| `tools/scripts/check-renovate-annotations.sh` | script | batch | none — new | no analog |
| `tools/scripts/byte-equivalence-diff.sh` | script | batch | none — new | no analog |
| `.planning/phases/04-*/evidence/current-image-tags.txt` | evidence | config | none — operator-captured | no analog |
| **my-kluster cross-repo:** `argocd/argocd-apps/arr-stack-app.yaml` | argocd-app | request-response | `my-kluster/argocd/argocd-apps/configarr-app.yaml` + `spec.md` §9.2 | exact |
| **my-kluster cross-repo:** delete 10 unit `*-app.yaml` files | cross-repo-delete | — | source files listed in Shared Patterns | delete |
| **my-kluster cross-repo:** delete `charts/configarr/` | cross-repo-delete | — | `my-kluster/charts/configarr/` | delete |
| **my-kluster cross-repo:** delete `charts/arrconf/` | cross-repo-delete | — | `my-kluster/charts/arrconf/` | delete |

---

## Pattern Assignments (grouped by Wave)

---

### Wave 0 — Pre-plan operator gate (evidence capture)

#### `.planning/phases/04-*/evidence/current-image-tags.txt`

**Analog:** none — operator-run kubectl command, output committed as-is.

**Capture command** (from CONTEXT.md D-04-PIN-02):
```bash
for app in qbittorrent flaresolverr cleanuparr; do
  kubectl -n selfhost get pod -l app.kubernetes.io/name=$app \
    -o jsonpath='{.spec.containers[0].image}{"\n"}{.status.containerStatuses[0].imageID}{"\n---\n"}'
done > .planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/current-image-tags.txt
```

**Known data** (from RESEARCH.md §Running Image Digests):
- qbittorrent: resolved to semver `5.2.0` (digest `sha256:2e0148428b6769e2ee1eb6781246b6fca4b70cd680edfcb16e7113d9d6cb1631`)
- flaresolverr: digest `sha256:7962759d99d7e125e108e0f5e7f3cdbcd36161776d058d1d9b7153b92ef1af9e` — semver tag UNKNOWN, planner uses `@sha256:` syntax or latest semver (`v3.4.6`) with annotation
- cleanuparr: digest `sha256:9b8f7a5f740c6cdc8f799a1d4b367ea560c0ce60799100afc3e14b6e3468cb5e` — semver tag UNKNOWN, planner uses `@sha256:` syntax or latest semver (`2.3.3`)

---

### Wave 1 — Chart skeleton

#### `charts/arr-stack/Chart.yaml`

**Analog:** `my-kluster/charts/arrconf/Chart.yaml` (role-match) — simple chart with no dependencies. Phase 4 target adds 10 `dependencies:` alias entries.

**Translation:** The exact dependencies block is verified and documented in RESEARCH.md §"Chart.yaml Dependencies Block (Exact)". Copy verbatim:

```yaml
apiVersion: v2
name: arr-stack
description: "Helm umbrella chart for the self-hosted media stack"
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0-0"
dependencies:
  - name: app-template
    alias: sonarr
    version: 4.6.2
    repository: https://bjw-s-labs.github.io/helm-charts
  - name: app-template
    alias: radarr
    version: 4.6.2
    repository: https://bjw-s-labs.github.io/helm-charts
  # ... repeat for prowlarr, cleanuparr, qbittorrent, seerr, flaresolverr, jellyfin, arrconf, configarr
```

**Translation notes:** No `appVersion` field in old single-chart analogs. Add it here (`0.1.0`). The `dependencies:` block is entirely new — no analog for multi-alias pattern. Use exact structure from RESEARCH.md §Chart.yaml.

---

#### `charts/arr-stack/templates/_helpers.tpl`

**Analog:** `my-kluster/charts/arrconf/templates/_helpers.tpl` (lines 1-49) — standard bjw-s naming helpers pattern.

**Analog source** (`my-kluster/charts/arrconf/templates/_helpers.tpl` lines 1-49):
```yaml
{{- define "arrconf.name" -}}
{{- default .Chart.Name .Values.nameOverride | trunc 63 | trimSuffix "-" }}
{{- end }}

{{- define "arrconf.fullname" -}}
{{- if .Values.fullnameOverride }}
{{- .Values.fullnameOverride | trunc 63 | trimSuffix "-" }}
{{- else }}
{{- $name := default .Chart.Name .Values.nameOverride }}
{{- if contains $name .Release.Name }}
{{- .Release.Name | trunc 63 | trimSuffix "-" }}
{{- else }}
{{- printf "%s-%s" .Release.Name $name | trunc 63 | trimSuffix "-" }}
{{- end }}
{{- end }}
{{- end }}
```

**Translation — umbrella version:** Replace per-chart named templates (`arrconf.name`, `arrconf.labels`) with umbrella-scoped names (`arr-stack.labels`). Add NEW annotation fragment templates NOT in the analog (per RESEARCH.md §Unknown #2):

```yaml
# templates/_helpers.tpl

{{- define "arr-stack.labels" -}}
helm.sh/chart: {{ printf "%s-%s" .Chart.Name .Chart.Version | replace "+" "_" | trunc 63 | trimSuffix "-" }}
app.kubernetes.io/managed-by: {{ .Release.Service }}
{{- end }}

{{/*
oauth2-proxy ingress annotations — used by 7 of 9 ingress apps (NOT Jellyfin, NOT Prowlarr).
Call: {{- include "arr-stack.oauth2ProxyAnnotations" (dict "hostname" "sonarr.tgu.ovh") | nindent 8 }}
*/}}
{{- define "arr-stack.oauth2ProxyAnnotations" -}}
nginx.ingress.kubernetes.io/auth-url: "https://auth.tgu.ovh/oauth2/auth"
nginx.ingress.kubernetes.io/auth-signin: "https://auth.tgu.ovh/oauth2/start?rd=https://{{ .hostname }}"
{{- end }}

{{/*
cert-manager annotation — used by all 8 ingress apps.
*/}}
{{- define "arr-stack.certManagerAnnotation" -}}
cert-manager.io/cluster-issuer: "letsencrypt-prod"
{{- end }}
```

**Translation notes:**
- The `_helpers.tpl` indirection is for authoring ergonomics only — rendered output is identical to having annotations inline (D-04-CUTOVER-03).
- RESEARCH.md Assumption A2 warns that `include "arr-stack.oauth2ProxyAnnotations"` may not resolve from within app-template sub-chart rendering. If CI fails, fall back to inlining annotations directly in each alias `ingress.main.annotations` block in `values.yaml`.
- Custom ConfigMap templates (`arrconf-configmap.yaml`, `configarr-configmap.yaml`) need only `arr-stack.labels` from this helper.

---

#### `charts/arr-stack/templates/arrconf-configmap.yaml`

**Analog:** `my-kluster/charts/arrconf/templates/configmap.yaml` (lines 1-9) — exact pattern.

**Analog source** (`my-kluster/charts/arrconf/templates/configmap.yaml` lines 1-9):
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "arrconf.fullname" . }}
  labels:
    {{- include "arrconf.labels" . | nindent 4 }}
data:
  arrconf.yml: |-
{{ .Files.Get "files/arrconf.yml" | indent 4 }}
```

**Translation — umbrella version:**
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: arrconf-config
  labels:
    {{- include "arr-stack.labels" . | nindent 4 }}
data:
  arrconf.yml: |-
{{ .Files.Get "files/arrconf.yml" | indent 4 }}
```

**Translation notes:**
- Hardcode `name: arrconf-config` (not `.Release.Name`-based) so the arrconf alias can reference it by exact name via `persistence.config.name: arrconf-config`.
- Replace `arrconf.labels` with `arr-stack.labels`.

---

#### `charts/arr-stack/templates/configarr-configmap.yaml`

**Analog:** `my-kluster/charts/configarr/templates/configmap.yaml` (lines 1-9) — exact pattern.

**Analog source** (`my-kluster/charts/configarr/templates/configmap.yaml` lines 1-9):
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "configarr.fullname" . }}
  labels:
    {{- include "configarr.labels" . | nindent 4 }}
data:
  config.yml: |-
{{ .Files.Get "files/config.yml" | indent 4 }}
```

**Translation — umbrella version:**
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: configarr-config
  labels:
    {{- include "arr-stack.labels" . | nindent 4 }}
data:
  config.yml: |-
{{ .Files.Get "files/configarr.yml" | indent 4 }}
```

**Translation notes:**
- Source file is `files/config.yml` in analog; target is `files/configarr.yml` (renamed to avoid ambiguity in the umbrella's `files/` dir).
- Hardcode `name: configarr-config` for the alias `persistence.config.name: configarr-config` reference.

---

#### `charts/arr-stack/files/arrconf.yml`

**Analog:** `my-kluster/charts/arrconf/files/arrconf.yml` — verbatim port.

**Source** (`my-kluster/charts/arrconf/files/arrconf.yml` lines 1-44): Full arrconf config with sonarr download_clients block. Copy verbatim. Phase 4 does not extend scope.

**Translation notes:** No changes. File content is frozen at Phase 3 state. The `args` in the arrconf CronJob alias (`apply --apps sonarr,radarr,prowlarr`) drives what gets reconciled, not this file's structure.

---

#### `charts/arr-stack/files/configarr.yml`

**Analog:** `my-kluster/charts/configarr/files/config.yml` — verbatim port, renamed.

**Source** (`my-kluster/charts/configarr/files/config.yml` lines 1-328): Full configarr YAML with trashGuideUrl, customFormatDefinitions, sonarr and radarr sections. Copy verbatim.

**Translation notes:** Rename file from `config.yml` to `configarr.yml` (distinguishes it from arrconf.yml in the shared `files/` directory). No content changes.

---

### Wave 2 — 8 media app aliases in values.yaml

Each alias section in `charts/arr-stack/values.yaml` is a verbatim lift of the corresponding `helm.values:` block from the unit ArgoCD Application, with one addition: the `# renovate: image=<repo>` annotation above each `repository:` line.

#### `values.yaml` — sonarr alias

**Analog:** `my-kluster/argocd/argocd-apps/sonarr-app.yaml` lines 18-80 (the entire `helm.values:` block).

**Byte-equivalence source** (`sonarr-app.yaml` lines 18-80):
```yaml
controllers:
  main:
    containers:
      main:
        image:
          repository: lscr.io/linuxserver/sonarr
          tag: 4.0.17
        env:
          TZ: "Europe/Paris"
          PUID: "1000"
          PGID: "1000"
service:
  main:
    controller: main
    ports:
      http:
        port: 8989
ingress:
  main:
    className: nginx
    annotations:
      cert-manager.io/cluster-issuer: "letsencrypt-prod"
      nginx.ingress.kubernetes.io/auth-url: "https://auth.tgu.ovh/oauth2/auth"
      nginx.ingress.kubernetes.io/auth-signin: "https://auth.tgu.ovh/oauth2/start?rd=https://sonarr.tgu.ovh"
    hosts:
      - host: sonarr.tgu.ovh
        paths:
          - path: /
            pathType: Prefix
            service:
              identifier: main
              port: http
    tls:
      - secretName: sonarr-tls
        hosts:
          - sonarr.tgu.ovh
persistence:
  config:
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 2Gi
    globalMounts:
      - path: /config
  torrents:
    type: hostPath
    hostPath: /opt/media-stack/torrents
    hostPathType: DirectoryOrCreate
    globalMounts:
      - path: /data/torrents
  media:
    type: persistentVolumeClaim
    existingClaim: media-nas-pvc
    globalMounts:
      - path: /media
```

**Translation notes:**
- Wrap in top-level `sonarr:` key.
- Add `# renovate: image=lscr.io/linuxserver/sonarr` above `repository:` line.
- Strip the YAML comments (`# Même hostPath que qBittorrent...`, etc.) — they belong in the source unit Apps, not the umbrella.
- Annotations are inlined verbatim (not via `_helpers.tpl` include) to avoid Assumption A2 risk.

---

#### `values.yaml` — radarr alias

**Analog:** `my-kluster/argocd/argocd-apps/radarr-app.yaml` lines 18-80.

**Byte-equivalence source** (key differences from sonarr):
- `tag: 6.1.1`, port `7878`, hostname `radarr.tgu.ovh`, TLS secret `radarr-tls`, PVC size `2Gi`, path `/data/torrents`, media NFS mount.

**Translation notes:** Same as sonarr. Add `# renovate: image=lscr.io/linuxserver/radarr`. Wrap in `radarr:` key.

---

#### `values.yaml` — prowlarr alias

**Analog:** `my-kluster/argocd/argocd-apps/prowlarr-app.yaml` lines 18-67.

**Byte-equivalence source** (key differences):
- `tag: 2.3.5`, port `9696`, hostname `prowlarr.tgu.ovh`.
- oauth2-proxy annotations are **commented out** in source (lines 41-42). Preserve the comment or omit entirely — byte-equivalence requires omitting the annotations, not inlining them.
- No `torrents` or `media` persistence — only `config` PVC (`1Gi`).

**Translation notes:**
- Wrap in `prowlarr:` key.
- Add `# renovate: image=lscr.io/linuxserver/prowlarr`.
- No oauth2-proxy annotations (prowlarr opts out — commented in source, omit in umbrella).

---

#### `values.yaml` — cleanuparr alias

**Analog:** `my-kluster/argocd/argocd-apps/cleanuparr-app.yaml` lines 18-65.

**Byte-equivalence source** (key differences):
- `repository: ghcr.io/cleanuparr/cleanuparr`, `tag: latest` — must be pinned to digest.
- No `PUID`/`PGID` env vars (only `TZ`).
- Port `11011`, hostname `cleanuparr.tgu.ovh`, `config` PVC `1Gi`.

**Translation notes:**
- Wrap in `cleanuparr:` key.
- Add `# renovate: image=ghcr.io/cleanuparr/cleanuparr`.
- Replace `tag: latest` with pinned value from `evidence/current-image-tags.txt` (digest `sha256:9b8f7a5f...` or resolved semver `2.3.3`).

---

#### `values.yaml` — qbittorrent alias

**Analog:** `my-kluster/argocd/argocd-apps/qbittorrent-app.yaml` lines 18-76.

**Byte-equivalence source** (key differences):
- `repository: lscr.io/linuxserver/qbittorrent`, `tag: latest` — must be pinned to `5.2.0`.
- Extra env var `WEBUI_PORT: "8080"`.
- Port `8080`.
- `torrents` hostPath mount path is `/data` (NOT `/data/torrents` like sonarr/radarr — qbittorrent is the source).
- No `media` NFS mount.

**Translation notes:**
- Wrap in `qbittorrent:` key.
- Add `# renovate: image=lscr.io/linuxserver/qbittorrent`.
- Replace `tag: latest` with `tag: "5.2.0"` (verified from RESEARCH.md §Running Image Digests).

---

#### `values.yaml` — seerr alias

**Analog:** `my-kluster/argocd/argocd-apps/seerr-app.yaml` lines 18-65.

**Byte-equivalence source** (key differences):
- `repository: ghcr.io/seerr-team/seerr`, `tag: v3.2.0`.
- No `PUID`/`PGID` (only `TZ`).
- Port `5055`, hostname `seerr.tgu.ovh`, config path `/app/config` (not `/config`).

**Translation notes:**
- Wrap in `seerr:` key.
- Add `# renovate: image=ghcr.io/seerr-team/seerr`.

---

#### `values.yaml` — flaresolverr alias

**Analog:** `my-kluster/argocd/argocd-apps/flaresolverr-app.yaml` lines 18-42.

**Byte-equivalence source** (key differences):
- `repository: ghcr.io/flaresolverr/flaresolverr`, `tag: latest` — must be pinned to digest.
- Extra env var `LOG_LEVEL: "info"`.
- Port `8191`.
- **No ingress block** (internal-only service, comment in source: `# Pas d'ingress : accès interne uniquement`).
- No persistence.

**Translation notes:**
- Wrap in `flaresolverr:` key.
- Add `# renovate: image=ghcr.io/flaresolverr/flaresolverr`.
- Replace `tag: latest` with pinned digest from `evidence/current-image-tags.txt`.
- Omit `ingress:` block entirely — flaresolverr is internal-only (RESEARCH.md Pitfall 10).

---

#### `values.yaml` — jellyfin alias

**Analog:** `my-kluster/argocd/argocd-apps/jellyfin-app.yaml` lines 18-75.

**Byte-equivalence source** (key differences):
- `repository: lscr.io/linuxserver/jellyfin`, `tag: 10.11.8`.
- Port `8096`, hostname `jellyfin.tgu.ovh`.
- Ingress has **no oauth2-proxy annotations** (Jellyfin handles its own auth) but has `nginx.ingress.kubernetes.io/proxy-body-size: "0"`.
- No `torrents` mount.
- Config PVC `10Gi`.

**Translation notes:**
- Wrap in `jellyfin:` key.
- Add `# renovate: image=lscr.io/linuxserver/jellyfin`.
- No oauth2-proxy annotations — preserve `proxy-body-size: "0"` annotation.

---

### Wave 3 — CronJob aliases + values.schema.json

#### `values.yaml` — arrconf alias

**Analog:** `my-kluster/charts/arrconf/templates/cronjob.yaml` (lines 1-49) + `my-kluster/charts/arrconf/values.yaml` (lines 1-29).

**Source — production CronJob spec** (`my-kluster/charts/arrconf/templates/cronjob.yaml` lines 21-49):
```yaml
# Source fields to translate:
spec.schedule: "0 */4 * * *"                          # → controllers.main.cronjob.schedule
spec.concurrencyPolicy: Forbid                          # → controllers.main.cronjob.concurrencyPolicy
spec.successfulJobsHistoryLimit: 1                      # → controllers.main.cronjob.successfulJobsHistory
spec.failedJobsHistoryLimit: 2                          # → controllers.main.cronjob.failedJobsHistory
spec.startingDeadlineSeconds: 600                       # → controllers.main.cronjob.startingDeadlineSeconds
spec.jobTemplate.spec.template.spec.restartPolicy: Never  # auto in app-template type: cronjob
spec.jobTemplate.spec.template.spec.securityContext:    # → defaultPodOptions.securityContext
  runAsNonRoot: true
  runAsUser: 1000
  runAsGroup: 1000
containers[0].image: ghcr.io/tom333/arr-stack-arrconf:0.1.4  # → containers.main.image
containers[0].args: ["--config", "/app/config/arrconf.yml", "apply", "--apps", "sonarr"]  # Phase 3 value
containers[0].envFrom.secretRef.name: arrconf-env       # → containers.main.envFrom
volumeMounts[0]: /app/config/arrconf.yml subPath arrconf.yml  # → persistence.config.globalMounts
# DROPPED: checksum/config annotation (D-04-CRON-02)
```

**Target — app-template alias** (from RESEARCH.md Pattern 1):
```yaml
arrconf:
  controllers:
    main:
      type: CronJob
      cronjob:
        schedule: "0 */4 * * *"
        concurrencyPolicy: Forbid
        successfulJobsHistory: 1
        failedJobsHistory: 2
        startingDeadlineSeconds: 600
      containers:
        main:
          image:
            # renovate: image=ghcr.io/tom333/arr-stack-arrconf
            repository: ghcr.io/tom333/arr-stack-arrconf
            tag: "0.2.0"
            pullPolicy: IfNotPresent
          args:
            - --config
            - /app/config/arrconf.yml
            - apply
            - --apps
            - sonarr,radarr,prowlarr
          envFrom:
            - secretRef:
                name: arrconf-env
  defaultPodOptions:
    securityContext:
      runAsNonRoot: true
      runAsUser: 1000
      runAsGroup: 1000
  persistence:
    config:
      type: configMap
      name: arrconf-config
      globalMounts:
        - path: /app/config/arrconf.yml
          subPath: arrconf.yml
          readOnly: true
```

**Translation notes:**
- `cronJobConfig:` is WRONG — use `cronjob:` (RESEARCH.md Pitfall 1, Common Library values.yaml verified).
- `checksum/config` Pod annotation DROPPED (D-04-CRON-02).
- args updated from `--apps sonarr` (Phase 2/3 state) to `--apps sonarr,radarr,prowlarr` (D-04-CRON-03, Phase 3 reconcilers live).
- `envFrom.secretRef.name: arrconf-env` (NOT `identifier:`) — external pre-existing Secret (RESEARCH.md Pitfall 7).
- arrconf source image in `my-kluster/charts/arrconf/values.yaml` is `0.1.4`; current arrconf is at `0.2.0` (or check current GHCR tag — use `0.2.0` per Phase 3 completion).

---

#### `values.yaml` — configarr alias

**Analog:** `my-kluster/charts/configarr/templates/cronjob.yaml` (lines 1-48) + `my-kluster/charts/configarr/values.yaml` (lines 1-24).

**Source — production CronJob spec** (`my-kluster/charts/configarr/templates/cronjob.yaml` lines 21-48):
```yaml
# Source fields to translate:
spec.schedule: "0 */4 * * *"                          # → controllers.main.cronjob.schedule
spec.concurrencyPolicy: Forbid                          # → cronjob.concurrencyPolicy
spec.successfulJobsHistoryLimit: 1                      # → cronjob.successfulJobsHistory
spec.failedJobsHistoryLimit: 2                          # → cronjob.failedJobsHistory
# NO securityContext on pod (configarr does NOT run as non-root — RESEARCH.md Pattern 2)
containers[0].tty: true                                 # → containers.main.tty: true (RESEARCH.md Pitfall 3)
containers[0].image: ghcr.io/raydak-labs/configarr:1.16.0
containers[0].envFrom.secretRef.name: configarr-env
volumeMounts[0]: /app/config/config.yml subPath config.yml
volumeMounts[1]: /app/repos (cache PVC)
volumes[1]: PVC configarr-cache
# DROPPED: checksum/config annotation (D-04-CRON-02)
```

**Target — app-template alias** (from RESEARCH.md Pattern 2):
```yaml
configarr:
  controllers:
    main:
      type: CronJob
      cronjob:
        schedule: "0 */4 * * *"
        concurrencyPolicy: Forbid
        successfulJobsHistory: 1
        failedJobsHistory: 2
      containers:
        main:
          image:
            # renovate: image=ghcr.io/raydak-labs/configarr
            repository: ghcr.io/raydak-labs/configarr
            tag: "1.16.0"
            pullPolicy: IfNotPresent
          tty: true
          envFrom:
            - secretRef:
                name: configarr-env
  defaultPodOptions:
    securityContext: {}
  persistence:
    config:
      type: configMap
      name: configarr-config
      globalMounts:
        - path: /app/config/config.yml
          subPath: config.yml
          readOnly: true
    cache:
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 1Gi
      storageClass: microk8s-hostpath
      globalMounts:
        - path: /app/repos
```

**Translation notes:**
- `tty: true` MUST be preserved (RESEARCH.md Pitfall 3).
- No `runAsNonRoot` / `runAsUser` on configarr Pod (RESEARCH.md Pitfall 4 — configarr does NOT have securityContext in production).
- `persistence.cache.type: persistentVolumeClaim` with `storageClass: microk8s-hostpath` and `size: 1Gi` replaces the separate `pvc.yaml` template (analog: `my-kluster/charts/configarr/templates/pvc.yaml` lines 1-14).
- config mount path is `/app/config/config.yml` (configarr convention) — NOT `/app/config/configarr.yml`.

---

#### `charts/arr-stack/values.schema.json`

**Analog:** none — generated artifact.

**Generation workflow** (from RESEARCH.md §Unknown #3):
```bash
helm plugin install https://github.com/losisin/helm-values-schema-json
helm schema -input charts/arr-stack/values.yaml -output charts/arr-stack/values.schema.json
# Then hand-tighten with enum/pattern constraints
```

**Translation notes:**
- `helm-schema-gen` by karuppiah7890 is UNMAINTAINED — use `losisin/helm-values-schema-json` only.
- Schema validates automatically during `helm template` and `helm lint` (built into Helm 3).
- CI gate via `losisin/helm-values-schema-json@v1` GitHub Action.

---

### Wave 4 — CI + Renovate

#### `.github/workflows/chart-lint.yml`

**Analog:** `.github/workflows/tests.yml` (lines 1-78) — same workflow structure (checkout → setup → run → no matrix).

**Analog pattern** (`tests.yml` lines 1-30):
```yaml
name: tests
on:
  pull_request:
    paths:
      - 'tools/arrconf/**'
  push:
    branches: [main]
    paths:
      - 'tools/arrconf/**'
jobs:
  test:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Setup uv
        uses: astral-sh/setup-uv@v4
```

**Target** (from RESEARCH.md §Unknown #6 — exact workflow):
```yaml
name: chart-lint
on:
  pull_request:
    paths:
      - 'charts/arr-stack/**'
      - 'examples/**'
      - 'renovate.json'
      - '.github/workflows/chart-lint.yml'
  push:
    branches: [main]
    paths:
      - 'charts/arr-stack/**'
      - 'examples/**'
jobs:
  lint:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: 'latest'
      - name: Add bjw-s chart repo
        run: helm repo add bjw-s-labs https://bjw-s-labs.github.io/helm-charts
      - name: Helm dependency update
        run: helm dependency update charts/arr-stack/
      - name: Helm lint
        run: helm lint charts/arr-stack/ -f examples/values-prod.yaml
      - name: Install kubeconform
        run: |
          curl -sL https://github.com/yannh/kubeconform/releases/latest/download/kubeconform-linux-amd64.tar.gz \
            | tar xz -C /usr/local/bin
      - name: Validate rendered manifests
        run: |
          helm template arr-stack charts/arr-stack/ \
            -f examples/values-prod.yaml \
            | kubeconform \
              -strict \
              -ignore-missing-schemas \
              -kubernetes-version 1.33.0
      - name: Validate values.schema.json
        uses: losisin/helm-values-schema-json@v1
        with:
          input: charts/arr-stack/values.yaml
          schema: charts/arr-stack/values.schema.json
          fail-on-errors: true
      - name: Validate Renovate config
        run: npx --yes renovate-config-validator renovate.json
```

**Translation notes:**
- Replace `astral-sh/setup-uv` with `azure/setup-helm@v4`.
- Add `helm repo add bjw-s-labs` BEFORE `helm dependency update` (RESEARCH.md Pitfall 9).
- `kubeconform -kubernetes-version 1.33.0` matches cluster version (K8s 1.33.9 from RESEARCH.md).
- No `defaults.run.working-directory` unlike tests.yml — chart operations run from repo root.

---

#### `renovate.json` (modified)

**Analog:** `renovate.json` (current, lines 1-4) — 3-line extend-only config.

**Analog source** (current `renovate.json` lines 1-4):
```json
{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"]
}
```

**Target** (from RESEARCH.md §Unknown #4 — exact JSON):
```json
{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["/charts/arr-stack/values\\.yaml$"],
      "matchStringsStrategy": "combination",
      "matchStrings": [
        "#\\s*renovate:\\s*image=(?<depName>[^\\s]+)\\s*\\n\\s*repository:\\s*(?<registryUrl>.*?)\\/",
        "\\s*tag:\\s*[\"']?(?<currentValue>[^\\s\"']+)[\"']?"
      ],
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "matchManagers": ["regex", "helmv3", "helm-values"],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "automerge": true
    },
    {
      "matchManagers": ["regex", "helmv3", "helm-values"],
      "matchUpdateTypes": ["major"],
      "automerge": false,
      "labels": ["major-update"]
    }
  ]
}
```

**Translation notes:**
- Use `combination` strategy (NOT the greedy `[\\s\\S]*?` single-string alternative) to prevent regex crossing alias boundaries (RESEARCH.md Pitfall 8).
- The `# renovate: image=<repo>` annotation must be on the line immediately above `repository:` in `values.yaml`.
- Verify with `npx --yes renovate-config-validator renovate.json` after writing.

---

#### `tools/scripts/check-renovate-annotations.sh`

**Analog:** none — new script. Derive from RESEARCH.md commands.

**Purpose:** Verify every `repository:` line in `values.yaml` has a `# renovate: image=` annotation on the preceding line.

**Pattern to implement** (shell script, no codebase analog):
```bash
#!/usr/bin/env bash
# Check that every 'repository:' line in values.yaml has a preceding renovate annotation.
set -euo pipefail
FILE="charts/arr-stack/values.yaml"
violations=0
while IFS= read -r -d '' match; do
  line_num=$(grep -n "repository:" "$FILE" | grep -F "$match" | cut -d: -f1)
  echo "MISSING annotation at line $line_num: $match"
  violations=$((violations + 1))
done < <(awk '
  /repository:/ { if (prev !~ /# renovate: image=/) print $0 }
  { prev = $0 }
' "$FILE" | tr '\n' '\0')
[ $violations -eq 0 ] || exit 1
echo "All repository: lines have renovate annotations."
```

---

#### `tools/scripts/byte-equivalence-diff.sh`

**Analog:** none — new script. Derive from RESEARCH.md §Unknown #9 commands.

**Purpose:** Automate the diff between `argocd app manifests` exports and `helm template` output for byte-equivalence verification (D-04-CUTOVER-03).

**Pattern to implement** (from RESEARCH.md §Unknown #9):
```bash
#!/usr/bin/env bash
# Usage: ./tools/scripts/byte-equivalence-diff.sh <evidence-dir>
set -euo pipefail
EVIDENCE_DIR="${1:-.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence}"
APPS="sonarr radarr prowlarr cleanuparr qbittorrent seerr flaresolverr jellyfin arrconf configarr"
EXCLUDE_PATTERN="argocd.argoproj.io|app.kubernetes.io/instance|helm.sh/chart"

helm template arr-stack charts/arr-stack/ \
  -f examples/values-prod.yaml \
  --namespace selfhost \
  > "$EVIDENCE_DIR/umbrella-rendered.yaml"

for app in $APPS; do
  diff \
    <(grep -v -E "$EXCLUDE_PATTERN" "$EVIDENCE_DIR/pre-cutover-argocd-${app}.yaml" | sort) \
    <(grep -v -E "$EXCLUDE_PATTERN" "$EVIDENCE_DIR/umbrella-rendered.yaml" | sort) \
    && echo "OK: $app byte-equivalent" \
    || echo "DIFF: $app — review above output"
done
```

---

### Wave 5 — Documentation refresh

#### `examples/values-prod.yaml`

**Analog:** `charts/arr-stack/values.yaml` — identical content (copy or symlink per D-04-VALUES-03).

**Translation notes:** Copy verbatim from `values.yaml` after Wave 2-3 are complete. This is purely a documentation artifact so operators forking the repo see a complete example. No values changes.

---

#### `README.md` (rewritten)

**Analog:** current `README.md` (lines 1-29) — Phase 0-3 state, 29-line stub.

**Current state** (analog lines 1-5):
```markdown
# arr-stack

Plateforme média fully-as-code ... déployée sur le cluster MicroK8s personnel `my-kluster`.

> **Statut** : en cours de bootstrap (Phase 0). Le code arrconf et le chart umbrella n'existent pas encore ...
```

**Target structure** (from CONTEXT.md D-04-DOCS-01, REQ-readme-onboarding < 30 min):
The rewritten README must cover:
1. What is arr-stack (one paragraph — what it deploys, how it works)
2. Stack overview table (apps + versions + ports)
3. Prerequisites (kubectl, helm, argocd CLI or fallback, GHCR access)
4. Umbrella chart section (`helm lint`, `helm template | kubeconform`)
5. Deploy section (how ArgoCD picks up a release — git tag → Renovate PR my-kluster → ArgoCD sync)
6. Operator runbook (snapshot before changes, cutover procedure summary, rollback = git revert)
7. Development (arrconf Python dev loop, `pytest`, `ruff`, chart dev loop)
8. Links to spec.md, CLAUDE.md, `.planning/`

**Translation notes:** Full rewrite, not patch. Remove "en cours de bootstrap (Phase 0)" stale status. The snapshot section is preserved and expanded.

---

#### `CLAUDE.md` (rewritten)

**Analog:** current `CLAUDE.md` — full project conventions file.

**Sections to update** (D-04-DOCS-01 + CONTEXT.md specifics):

| Section | Change |
|---|---|
| "Structure cible" → "Structure actuelle" | Remove anticipatory language; reflect that chart exists and templates are minimal (no arrconf-cronjob.yaml — those are in app-template alias) |
| "Bootstrap (état actuel — 2026-05-07)" → "Historical bootstrap (Phase 0-3)" | Archive, do not delete |
| "Intégration avec my-kluster" | Rewrite for single ArgoCD App: `arr-stack-app.yaml` points to `charts/arr-stack`, `targetRevision: vX.Y.Z`, no per-app Application files |
| "Comment ajouter une nouvelle app à arrconf" | Keep but note chart alias must also be added to Chart.yaml + values.yaml |
| "Ce que tu NE dois PAS faire" | Add: "Ne pas oublier d'ajouter l'alias dans Chart.yaml + values.yaml quand tu ajoutes une app" |

**Translation notes:** Rewrite is end-to-end against Phase 4 ground truth. Anticipatory sections (`templates/arrconf-cronjob.yaml`, `templates/arrconf-configmap.yaml` as separate files) are contradicted by D-04-CRON-01 — the umbrella has only `templates/_helpers.tpl` + two ConfigMap templates. The "Structure cible" block must reflect this.

---

### Wave 6 — Cross-repo my-kluster PR

#### `my-kluster/argocd/argocd-apps/arr-stack-app.yaml` (new)

**Analog (primary):** `my-kluster/argocd/argocd-apps/configarr-app.yaml` (lines 1-24) — git-path source structure (not Helm repo), `ServerSideApply=true` already set.

**Analog source** (`configarr-app.yaml` lines 1-24):
```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: configarr
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: selfhost
    server: https://kubernetes.default.svc
  project: selfhost-project
  source:
    repoURL: https://github.com/tom333/my-kluster.git
    targetRevision: HEAD
    path: charts/configarr
  syncPolicy:
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
    automated:
      selfHeal: true
      prune: true
```

**Target** (from `spec.md` §9.2 lines 682-706, the authoritative literal example):
```yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: arr-stack
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: selfhost
    server: https://kubernetes.default.svc
  project: selfhost-project
  source:
    repoURL: https://github.com/tom333/arr-stack.git
    targetRevision: v0.1.0
    path: charts/arr-stack
  syncPolicy:
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
    # automated: block OMITTED at first merge — D-04-CUTOVER-02
    # Re-add in follow-up PR after manual sync verified:
    # automated:
    #   selfHeal: true
    #   prune: true
```

**Translation notes:**
- `repoURL` changes from `my-kluster.git` to `arr-stack.git`.
- `targetRevision` changes from `HEAD` to `v0.1.0` (pinned semver git tag).
- `path` changes from `charts/configarr` to `charts/arr-stack`.
- `automated:` block REMOVED at first merge (D-04-CUTOVER-02). Add back in Wave 7 follow-up PR.
- `ServerSideApply=true` is already in the analog and must be preserved — it enables field manager migration for adopted resources.
- Verify that `selfhost-project` AppProject `sourceRepos:` whitelist includes `https://github.com/tom333/arr-stack.git` (RESEARCH.md Open Question 4).

---

#### Cross-repo deletions (10 unit App files + 2 chart dirs)

**Files to delete from `my-kluster`:**
- `argocd/argocd-apps/sonarr-app.yaml`
- `argocd/argocd-apps/radarr-app.yaml`
- `argocd/argocd-apps/prowlarr-app.yaml`
- `argocd/argocd-apps/cleanuparr-app.yaml`
- `argocd/argocd-apps/qbittorrent-app.yaml`
- `argocd/argocd-apps/seerr-app.yaml`
- `argocd/argocd-apps/flaresolverr-app.yaml`
- `argocd/argocd-apps/jellyfin-app.yaml`
- `argocd/argocd-apps/configarr-app.yaml`
- `argocd/argocd-apps/arrconf-app.yaml`
- `charts/configarr/` (entire directory)
- `charts/arrconf/` (entire directory)

**Translation notes:** These are git deletes (`git rm -r`). No content migration — content has been absorbed into `charts/arr-stack/`. The atomic deletion is D-04-CUTOVER-01.

---

### Wave 7 — Post-cutover stabilization

#### Follow-up PR: re-enable `automated:` in `arr-stack-app.yaml`

**Analog:** `arr-stack-app.yaml` written in Wave 6 — one-line re-addition.

**Change:** Add back the `automated:` block that was removed for first sync:
```yaml
    automated:
      selfHeal: true
      prune: true
```

---

## Shared Patterns

### Renovate `# renovate: image=` annotation
**Source:** `my-kluster/charts/arrconf/values.yaml` lines 2-3 (first use of this convention in the ecosystem).
**Apply to:** Every `repository:` line in `charts/arr-stack/values.yaml` (10 aliases × 1 image each = 10 annotations).
```yaml
# renovate: image=ghcr.io/tom333/arr-stack-arrconf
repository: ghcr.io/tom333/arr-stack-arrconf
tag: "0.2.0"
```

### `envFrom: secretRef: name:` (external Secret syntax)
**Source:** RESEARCH.md §Pattern 6 (verified: `my-kluster/charts/arrconf/values.yaml` line 26 `apiKeysSecret: arrconf-env`).
**Apply to:** `arrconf` and `configarr` aliases in `values.yaml`.
```yaml
envFrom:
  - secretRef:
      name: arrconf-env    # NOT identifier: — this is a pre-existing external Secret
```

### `ServerSideApply=true` syncOption
**Source:** `my-kluster/argocd/argocd-apps/configarr-app.yaml` line 20.
**Apply to:** `arr-stack-app.yaml` in `my-kluster`.
```yaml
syncOptions:
  - CreateNamespace=false
  - ServerSideApply=true
```

### `concurrencyPolicy: Forbid` on CronJob controllers
**Source:** `my-kluster/charts/arrconf/templates/cronjob.yaml` line 12 + `my-kluster/charts/configarr/templates/cronjob.yaml` line 12.
**Apply to:** Both `arrconf` and `configarr` aliases (D-04-CRON-02 MANDATORY).
```yaml
controllers:
  main:
    cronjob:
      concurrencyPolicy: Forbid
```

### `.Files.Get` ConfigMap template
**Source:** `my-kluster/charts/arrconf/templates/configmap.yaml` (lines 1-9) and `my-kluster/charts/configarr/templates/configmap.yaml` (lines 1-9).
**Apply to:** `templates/arrconf-configmap.yaml` and `templates/configarr-configmap.yaml`.
```yaml
data:
  arrconf.yml: |-
{{ .Files.Get "files/arrconf.yml" | indent 4 }}
```

### `resources-finalizer.argocd.argoproj.io` on ArgoCD Applications
**Source:** all 10 unit `*-app.yaml` files (lines 6-7 in each).
**Apply to:** `arr-stack-app.yaml`.
```yaml
finalizers:
  - resources-finalizer.argocd.argoproj.io
```

---

## No Analog Found

| File | Role | Data Flow | Reason |
|---|---|---|---|
| `tools/scripts/check-renovate-annotations.sh` | script | batch | No shell scripts exist in arr-stack (Phase 0-3 added only Python + snapshot.sh) |
| `tools/scripts/byte-equivalence-diff.sh` | script | batch | No diff scripts exist; pattern derived from RESEARCH.md §Unknown #9 commands |
| `charts/arr-stack/values.schema.json` | config | config | Generated artifact — no schema files exist in arr-stack yet; use `losisin/helm-values-schema-json` |

---

## Critical Anti-Patterns (Copy from RESEARCH.md)

These are patterns present in the **analog** source that must NOT be copied to the target:

| Anti-Pattern | Where in Analog | What to Do Instead |
|---|---|---|
| `cronJobConfig:` key | CONTEXT.md shorthand | Use `cronjob:` under `controllers.<name>` |
| `checksum/config:` Pod annotation | `my-kluster/charts/arrconf/templates/cronjob.yaml` line 20 | DROP (D-04-CRON-02) |
| `automated: selfHeal/prune` in first sync | `configarr-app.yaml` (template) | OMIT for first merge; add back in Wave 7 |
| `tag: latest` | `cleanuparr-app.yaml` line 25, `qbittorrent-app.yaml` line 25, `flaresolverr-app.yaml` line 25 | Pin to semver/digest from evidence/current-image-tags.txt |
| `identifier: arrconf-env` | (none in codebase, potential error) | Use `name: arrconf-env` (external Secret, not app-template identifier) |
| `helm.sh/chart` in byte-equivalence diff | unit App ArgoCD manifests | Exclude from diff (will change from `app-template-4.6.2` to `arr-stack-0.1.0`) |

---

## Metadata

**Analog search scope:** `/home/moi/projets/perso/my-kluster/argocd/argocd-apps/`, `/home/moi/projets/perso/my-kluster/charts/arrconf/`, `/home/moi/projets/perso/my-kluster/charts/configarr/`, `/home/moi/projets/perso/arr-stack/.github/workflows/`, `/home/moi/projets/perso/arr-stack/renovate.json`
**Files scanned:** 19 analog files read directly
**Pattern extraction date:** 2026-05-12
