---
phase: 04-umbrella-chart-migration-des-9-apps
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - charts/arr-stack/Chart.yaml
  - charts/arr-stack/Chart.lock
  - charts/arr-stack/values.yaml
  - charts/arr-stack/templates/_helpers.tpl
  - charts/arr-stack/templates/arrconf-configmap.yaml
  - charts/arr-stack/templates/configarr-configmap.yaml
  - charts/arr-stack/files/arrconf.yml
  - charts/arr-stack/files/configarr.yml
  - charts/arr-stack/.helmignore
autonomous: true
requirements:
  - REQ-umbrella-deployment
  - REQ-helm-validation
  - REQ-config-as-code

must_haves:
  truths:
    - "`helm lint charts/arr-stack/` exits 0"
    - "`helm dependency update` resolves 10 app-template@5.0.0 deps from the OCI registry and writes Chart.lock"
    - "`values.yaml` parses as valid YAML and ships an empty stanza per alias (filled in Waves 2-4)"
    - "The two `files/` payloads (arrconf.yml + configarr.yml) are byte-equivalent to the my-kluster main-branch source"
    - "ConfigMap templates emit ConfigMaps named `arrconf-config` and `configarr-config` (hardcoded names, not release-prefixed) so CronJob aliases can mount them by exact name"
  artifacts:
    - path: charts/arr-stack/Chart.yaml
      provides: "10 app-template@5.0.0 dependency aliases + umbrella version 0.1.0"
      contains: "version: 5.0.0"
      min_lines: 50
    - path: charts/arr-stack/Chart.lock
      provides: "Frozen dependency tarball digests"
      contains: "app-template"
    - path: charts/arr-stack/values.yaml
      provides: "10 alias section placeholders (Waves 2-4 fill them)"
      contains: "sonarr:"
    - path: charts/arr-stack/templates/_helpers.tpl
      provides: "Common labels helper (arr-stack.labels)"
      contains: "arr-stack.labels"
    - path: charts/arr-stack/templates/arrconf-configmap.yaml
      provides: "ConfigMap mounting files/arrconf.yml at /app/config/arrconf.yml"
      contains: "arrconf-config"
    - path: charts/arr-stack/templates/configarr-configmap.yaml
      provides: "ConfigMap mounting files/configarr.yml at /app/config/config.yml"
      contains: "configarr-config"
    - path: charts/arr-stack/files/arrconf.yml
      provides: "arrconf runtime config (Phase 4 scope: sonarr only — port from my-kluster main)"
    - path: charts/arr-stack/files/configarr.yml
      provides: "configarr runtime config (TRaSH-Guides custom formats + quality profiles)"
  key_links:
    - from: charts/arr-stack/Chart.yaml
      to: oci://ghcr.io/bjw-s/helm/app-template
      via: "dependency repository field"
      pattern: "oci://ghcr.io/bjw-s/helm"
    - from: charts/arr-stack/templates/arrconf-configmap.yaml
      to: charts/arr-stack/files/arrconf.yml
      via: ".Files.Get"
      pattern: '\.Files\.Get "files/arrconf.yml"'
    - from: charts/arr-stack/templates/configarr-configmap.yaml
      to: charts/arr-stack/files/configarr.yml
      via: ".Files.Get"
      pattern: '\.Files\.Get "files/configarr.yml"'
---

<objective>
Lay down the umbrella chart skeleton: `Chart.yaml` with 10 app-template@5.0.0 alias dependencies, `_helpers.tpl` for shared labels, the two ConfigMap templates that mount the arrconf + configarr config files, and the `files/` payloads (verbatim port from `my-kluster/charts/{arrconf,configarr}/files/`). `values.yaml` ships as a skeleton — each alias section is a single empty top-level key that Waves 2-4 fill in.

Purpose: This is the load-bearing wave. Without a green `helm lint` on a chart with 10 deps resolved against the OCI registry, every subsequent wave is uncheckable.

Output: A `charts/arr-stack/` directory that passes `helm lint` and produces a `Chart.lock` pinning all 10 deps to the same v5.0.0 tarball.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-CONTEXT.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-PATTERNS.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Read these from my-kluster main branch via `git -C /home/moi/projets/perso/my-kluster show main:<path>` — the working tree is on a stale branch and will mislead. -->

Source: `git -C /home/moi/projets/perso/my-kluster show main:charts/arrconf/files/arrconf.yml`
  Used by: charts/arr-stack/files/arrconf.yml (verbatim port — single source of truth for arrconf runtime config)

Source: `git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/files/config.yml`
  Used by: charts/arr-stack/files/configarr.yml (verbatim port; note filename change `config.yml` → `configarr.yml` to match D-04-VALUES-02 layout)

Source: app-template@5.0.0 chart at `oci://ghcr.io/bjw-s/helm/app-template:5.0.0`
  Used by: all 10 Chart.yaml dependencies
  Verified: RESEARCH.md §"Standard Stack §Core" — pulled from registry, common 5.0.0 dependency confirmed
</interfaces>
</context>

<executor_note>
`values.yaml` is APPEND-ONLY across waves 2-4. This plan creates the file as a skeleton with one empty top-level alias key per app (10 lines), in the same order as `Chart.yaml dependencies[]`. Plans 04-03, 04-04, 04-05 append per-alias bodies under their respective top-level keys without rewriting the file. (W5 carry-forward.)
</executor_note>

<tasks>

<task type="auto">
  <name>Task 2.1: Author Chart.yaml + Chart.lock + values.yaml skeleton + _helpers.tpl</name>
  <files>
    charts/arr-stack/Chart.yaml
    charts/arr-stack/Chart.lock
    charts/arr-stack/values.yaml
    charts/arr-stack/templates/_helpers.tpl
    charts/arr-stack/.helmignore
  </files>
  <read_first>
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Pattern 4: Chart.yaml with 10 Aliases" (verbatim Chart.yaml body — copy)
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Code Examples → _helpers.tpl" (verbatim helper body — copy, then simplify per below)
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-PATTERNS.md` (analog: `my-kluster/charts/arrconf/templates/_helpers.tpl` — used for shape comparison only; do NOT copy its `arrconf.fullname` template into the umbrella since the umbrella is a multi-alias chart, not a single-app chart)
  </read_first>
  <action>
**1) `charts/arr-stack/Chart.yaml`** — copy RESEARCH §"Pattern 4" verbatim. The full dependency block lists all 10 aliases pinned to `version: 5.0.0` with `repository: oci://ghcr.io/bjw-s/helm` (per D-04-PIN-01). Final body:

```yaml
apiVersion: v2
name: arr-stack
description: Helm umbrella chart for the arr-stack self-hosted media stack
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.28.0-0"

dependencies:
  - name: app-template
    alias: sonarr
    version: 5.0.0
    repository: oci://ghcr.io/bjw-s/helm
  - name: app-template
    alias: radarr
    version: 5.0.0
    repository: oci://ghcr.io/bjw-s/helm
  - name: app-template
    alias: prowlarr
    version: 5.0.0
    repository: oci://ghcr.io/bjw-s/helm
  - name: app-template
    alias: cleanuparr
    version: 5.0.0
    repository: oci://ghcr.io/bjw-s/helm
  - name: app-template
    alias: qbittorrent
    version: 5.0.0
    repository: oci://ghcr.io/bjw-s/helm
  - name: app-template
    alias: seerr
    version: 5.0.0
    repository: oci://ghcr.io/bjw-s/helm
  - name: app-template
    alias: flaresolverr
    version: 5.0.0
    repository: oci://ghcr.io/bjw-s/helm
  - name: app-template
    alias: jellyfin
    version: 5.0.0
    repository: oci://ghcr.io/bjw-s/helm
  - name: app-template
    alias: arrconf
    version: 5.0.0
    repository: oci://ghcr.io/bjw-s/helm
  - name: app-template
    alias: configarr
    version: 5.0.0
    repository: oci://ghcr.io/bjw-s/helm
```

**2) `charts/arr-stack/values.yaml`** — skeleton only. One empty top-level key per alias, in dependency order, with a comment marking which wave will populate each section:

```yaml
# charts/arr-stack/values.yaml
# Each top-level key matches a dependency alias in Chart.yaml.
# Per-app bodies are populated by Plans 04-03 (sonarr/radarr/prowlarr/qbittorrent),
# 04-04 (cleanuparr/seerr/flaresolverr/jellyfin), and 04-05 (arrconf/configarr).

# Wave 2 (Plan 04-03) populates the next 4:
sonarr: {}
radarr: {}
prowlarr: {}
qbittorrent: {}

# Wave 3 (Plan 04-04) populates the next 4:
cleanuparr: {}
seerr: {}
flaresolverr: {}
jellyfin: {}

# Wave 4 (Plan 04-05) populates the next 2:
arrconf: {}
configarr: {}
```

Note: empty maps `{}` are valid YAML and pass `helm lint` because app-template renders no resources when its values are empty.

**3) `charts/arr-stack/templates/_helpers.tpl`** — minimal helper providing one named template `arr-stack.labels`, used by both ConfigMap templates. Body:

```yaml
{{/*
Common labels for arr-stack umbrella-owned objects (ConfigMaps for arrconf + configarr).
Per-alias app-template renders carry their own labels — this helper applies only
to objects authored directly in charts/arr-stack/templates/.
*/}}
{{- define "arr-stack.labels" -}}
helm.sh/chart: {{ printf "%s-%s" .Chart.Name .Chart.Version | replace "+" "_" | trunc 63 | trimSuffix "-" }}
app.kubernetes.io/managed-by: {{ .Release.Service }}
app.kubernetes.io/part-of: arr-stack
{{- end }}
```

Do NOT copy the `arrconf.fullname` / `arrconf.name` templates from the my-kluster analog — they only made sense for a single-app chart. The umbrella authors only 2 ConfigMaps directly; both use hardcoded names per D-04-CRON-01 derivative reasoning (RESEARCH Pitfall 10).

**4) `charts/arr-stack/.helmignore`** — standard ignore list:

```
.DS_Store
.git/
.gitignore
*.tgz
.planning/
```

**5) `charts/arr-stack/Chart.lock`** — run `helm dependency update charts/arr-stack/` from the repo root after authoring Chart.yaml. This pulls all 10 deps (same 5.0.0 tarball cached once) and writes `Chart.lock`. If `helm dependency update` errors on the OCI registry (RESEARCH Pitfall 6), fall back to:

```bash
helm pull oci://ghcr.io/bjw-s/helm/app-template --version 5.0.0 -d charts/arr-stack/charts/
helm dependency update charts/arr-stack/
```

Commit the resulting `Chart.lock`. Do NOT commit `charts/arr-stack/charts/*.tgz` — `.helmignore` excludes them from the chart package but `.gitignore` should also list `charts/arr-stack/charts/` to keep the repo lean (add to root `.gitignore` if not already present).
  </action>
  <verify>
    <automated>bash -c 'helm lint charts/arr-stack/ && grep -c "version: 5.0.0" charts/arr-stack/Chart.yaml && python3 -c "import yaml; yaml.safe_load(open(\"charts/arr-stack/values.yaml\"))" && test -f charts/arr-stack/Chart.lock'</automated>
  </verify>
  <acceptance_criteria>
    - `helm lint charts/arr-stack/` exits 0
    - `grep -c '^    version: 5.0.0$' charts/arr-stack/Chart.yaml` == 10 (one per alias)
    - `grep -c '^  - name: app-template$' charts/arr-stack/Chart.yaml` == 10
    - `grep -c 'alias: ' charts/arr-stack/Chart.yaml` == 10
    - The 10 expected aliases all present: `for a in sonarr radarr prowlarr cleanuparr qbittorrent seerr flaresolverr jellyfin arrconf configarr; do grep -q "alias: $a$" charts/arr-stack/Chart.yaml || exit 1; done`
    - Chart.yaml has umbrella version 0.1.0: `grep -q '^version: 0.1.0$' charts/arr-stack/Chart.yaml`
    - `python3 -c "import yaml; yaml.safe_load(open('charts/arr-stack/values.yaml'))"` exits 0 (valid YAML)
    - All 10 alias keys present at top level: `for a in sonarr radarr prowlarr cleanuparr qbittorrent seerr flaresolverr jellyfin arrconf configarr; do grep -q "^${a}: {}" charts/arr-stack/values.yaml || exit 1; done`
    - `test -f charts/arr-stack/Chart.lock`
    - `grep -q 'app-template' charts/arr-stack/Chart.lock` (Chart.lock has dependency entries)
    - `grep -q 'arr-stack.labels' charts/arr-stack/templates/_helpers.tpl` (helper template defined)
    - `test -f charts/arr-stack/.helmignore`
    - `helm template arr-stack charts/arr-stack/ > /tmp/skeleton-render.yaml` exits 0 (renders cleanly with empty alias values)
    - The skeleton render produces no app-template resources (empty values means no resources): `grep -c '^kind: Deployment' /tmp/skeleton-render.yaml` == 0 (none rendered yet)
  </acceptance_criteria>
  <done>Chart skeleton renders cleanly with `helm lint` green and `helm template` produces only the labels helper output (no rendered K8s resources yet — those come when alias values are populated in Plans 04-03/04/05).</done>
</task>

<task type="auto">
  <name>Task 2.2: Port `files/` payloads + author the two ConfigMap templates</name>
  <files>
    charts/arr-stack/files/arrconf.yml
    charts/arr-stack/files/configarr.yml
    charts/arr-stack/templates/arrconf-configmap.yaml
    charts/arr-stack/templates/configarr-configmap.yaml
  </files>
  <read_first>
    - `git -C /home/moi/projets/perso/my-kluster show main:charts/arrconf/files/arrconf.yml` (source for arrconf.yml — port verbatim, single source of truth)
    - `git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/files/config.yml` (source for configarr.yml — port verbatim; note the file is renamed)
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Code Examples → arrconf-configmap.yaml template" and §"configarr-configmap.yaml template" (verbatim ConfigMap bodies — copy)
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Common Pitfalls → Pitfall 10" (hardcoded ConfigMap name is mandatory so CronJob aliases can `persistence.config.name: arrconf-config` by exact name)
  </read_first>
  <action>
**1) Port `files/arrconf.yml`** — copy `git -C /home/moi/projets/perso/my-kluster show main:charts/arrconf/files/arrconf.yml` verbatim to `charts/arr-stack/files/arrconf.yml`.

This is the Phase 3-completed config covering Sonarr only. Plan 04-05 will REPLACE this with the Phase 4 expanded config (sonarr + radarr + prowlarr per D-04-CRON-03). For this plan, we ship the my-kluster source unchanged so the byte-equivalence baseline (Wave 6) measures only the chart skeleton — not the arrconf scope expansion which is intentional non-byte-equivalence (Plan 04-05 documents it as a deliberate functional change).

Verify the port is exact:
```bash
diff <(git -C /home/moi/projets/perso/my-kluster show main:charts/arrconf/files/arrconf.yml) charts/arr-stack/files/arrconf.yml
# expected: empty (zero diff)
```

**2) Port `files/configarr.yml`** — copy `git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/files/config.yml` verbatim to `charts/arr-stack/files/configarr.yml`. Filename change `config.yml` → `configarr.yml` is per D-04-VALUES-02 (each `files/` entry is named for its consumer, not its source).

Verify:
```bash
diff <(git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/files/config.yml) charts/arr-stack/files/configarr.yml
# expected: empty
```

**3) `charts/arr-stack/templates/arrconf-configmap.yaml`** — body per RESEARCH §"arrconf-configmap.yaml template", with the hardcoded name `arrconf-config` (per Pitfall 10 — the CronJob alias references this exact name via `persistence.config.name: arrconf-config`):

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: arrconf-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "arr-stack.labels" . | nindent 4 }}
data:
  arrconf.yml: |
    {{- .Files.Get "files/arrconf.yml" | nindent 4 }}
```

**4) `charts/arr-stack/templates/configarr-configmap.yaml`** — analogous to arrconf, but the rendered data key is `config.yml` (not `configarr.yml`) because the configarr binary expects to find its config at `/app/config/config.yml`. The umbrella mounts ConfigMap key `config.yml` from the source file `files/configarr.yml`:

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: configarr-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "arr-stack.labels" . | nindent 4 }}
data:
  config.yml: |
    {{- .Files.Get "files/configarr.yml" | nindent 4 }}
```

Confirm by inspecting `git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/templates/cronjob.yaml`: `mountPath: /app/config/config.yml subPath: config.yml` — so the ConfigMap key must literally be `config.yml`.

Render both ConfigMaps and verify shape:

```bash
helm template arr-stack charts/arr-stack/ > /tmp/skeleton-render.yaml
grep -A2 '^kind: ConfigMap' /tmp/skeleton-render.yaml
# expected: 2 ConfigMaps named arrconf-config and configarr-config
```
  </action>
  <verify>
    <automated>bash -c 'test -f charts/arr-stack/files/arrconf.yml && test -f charts/arr-stack/files/configarr.yml && python3 -c "import yaml; yaml.safe_load(open(\"charts/arr-stack/files/arrconf.yml\")); yaml.safe_load(open(\"charts/arr-stack/files/configarr.yml\"))" && helm template arr-stack charts/arr-stack/ > /tmp/skeleton.yaml && grep -c "^  name: arrconf-config$" /tmp/skeleton.yaml && grep -c "^  name: configarr-config$" /tmp/skeleton.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import yaml; yaml.safe_load(open('charts/arr-stack/files/arrconf.yml'))"` exits 0
    - `python3 -c "import yaml; yaml.safe_load(open('charts/arr-stack/files/configarr.yml'))"` exits 0
    - Verbatim port assertion: `diff <(git -C /home/moi/projets/perso/my-kluster show main:charts/arrconf/files/arrconf.yml) charts/arr-stack/files/arrconf.yml | wc -l` == 0
    - Verbatim port assertion: `diff <(git -C /home/moi/projets/perso/my-kluster show main:charts/configarr/files/config.yml) charts/arr-stack/files/configarr.yml | wc -l` == 0
    - `helm template arr-stack charts/arr-stack/ > /tmp/skeleton.yaml` exits 0
    - `grep -c '^  name: arrconf-config$' /tmp/skeleton.yaml` == 1 (exactly one ConfigMap with this name)
    - `grep -c '^  name: configarr-config$' /tmp/skeleton.yaml` == 1
    - `grep -c '^kind: ConfigMap$' /tmp/skeleton.yaml` == 2 (only the 2 we author; alias bodies still empty)
    - `grep -c '^kind: Deployment$' /tmp/skeleton.yaml` == 0 (no app-template Deployments yet — alias values are still `{}`)
    - `grep -c 'app.kubernetes.io/part-of: arr-stack' /tmp/skeleton.yaml` >= 2 (both ConfigMaps carry the helper-injected label)
    - The configarr ConfigMap data key is `config.yml` (not `configarr.yml`): `grep -A1 'name: configarr-config' /tmp/skeleton.yaml | grep -q 'config.yml:' || grep -B1 'config.yml: |' /tmp/skeleton.yaml | head -5`
    - `helm lint charts/arr-stack/` still exits 0
  </acceptance_criteria>
  <done>The two ConfigMaps render with hardcoded names that match Plan 04-05's references. `files/` payloads are exact ports of my-kluster main and parse as valid YAML. The skeleton renders only 2 ConfigMaps and 0 app-template resources (alias values are still empty placeholders).</done>
</task>

</tasks>

<verification>
- `helm lint charts/arr-stack/` exits 0
- `helm dependency update charts/arr-stack/` writes Chart.lock with 10 app-template@5.0.0 entries
- `helm template arr-stack charts/arr-stack/` renders 2 ConfigMaps (arrconf-config, configarr-config), 0 Deployments (alias bodies are populated in Plans 04-03/04/05)
- `files/arrconf.yml` and `files/configarr.yml` are byte-equivalent ports of the my-kluster main-branch source
- `values.yaml` is valid YAML with 10 top-level alias keys ready for append-only population
</verification>

<success_criteria>
- Chart skeleton is consumable by Plans 04-03, 04-04, 04-05 (which append per-alias values to `values.yaml`) without further structural work
- `Chart.lock` pins all deps so subsequent `helm dependency update` runs are deterministic
- ConfigMap names match the names Plan 04-05's CronJob aliases will reference
- No regressions on the 5.0.0 dependency target
</success_criteria>

<output>
After completion, create `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-02-SUMMARY.md` recording: (1) helm version in use, (2) `Chart.lock` digest line for app-template-5.0.0, (3) the 2 hardcoded ConfigMap names confirmed in the render, (4) any deviation from the verbatim port of `files/arrconf.yml` (should be zero — flag if not).
</output>
