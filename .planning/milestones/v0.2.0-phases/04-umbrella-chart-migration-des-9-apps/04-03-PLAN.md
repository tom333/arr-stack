---
phase: 04-umbrella-chart-migration-des-9-apps
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - charts/arr-stack/values.yaml
autonomous: true
requirements:
  - REQ-umbrella-deployment
  - REQ-renovate-image-tracking
  - REQ-helm-validation
  - REQ-config-as-code

must_haves:
  truths:
    - "values.yaml carries verbatim per-alias bodies for sonarr / radarr / prowlarr / qbittorrent ported from my-kluster main"
    - "Each of the 4 aliases has `global.fullnameOverride: <alias>` so the rendered Service name preserves DNS compatibility (Sonarr → http://sonarr.selfhost.svc.cluster.local:8989 keeps working)"
    - "Every `repository:` line in the 4 sections has a `# renovate: image=<repo>` annotation directly above it (CLAUDE.md mandatory convention)"
    - "qbittorrent ships with `tag: 5.2.0` (pinned semver from the running cluster digest), NOT `:latest`"
    - "`helm template arr-stack charts/arr-stack/` produces 4 Deployments + 4 Services + 4 Ingresses + 4 PVCs (or `existingClaim` references) corresponding to these 4 aliases"
  artifacts:
    - path: charts/arr-stack/values.yaml
      provides: "sonarr / radarr / prowlarr / qbittorrent alias bodies"
      contains: "fullnameOverride: sonarr"
      min_lines: 200
  key_links:
    - from: charts/arr-stack/values.yaml (sonarr.service.main.ports.http.port)
      to: http://sonarr.selfhost.svc.cluster.local:8989
      via: "Service name preserved by fullnameOverride"
      pattern: "port: 8989"
    - from: charts/arr-stack/values.yaml (qbittorrent image)
      to: lscr.io/linuxserver/qbittorrent:5.2.0
      via: "Pinned semver replaces production :latest"
      pattern: 'tag: "5.2.0"'
---

<objective>
Populate `values.yaml` with the verbatim per-alias bodies for the four arr-family apps that ship with explicit semver tags: **sonarr** (4.0.17), **radarr** (6.1.1), **prowlarr** (2.3.5), and **qbittorrent** (pinned to 5.2.0 per D-04-PIN-01 from the Wave 0 digest). The shape of each body is lifted verbatim from `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/<app>-app.yaml`'s `helm.values` block — these are already at app-template 5.0.0 shape, so no schema translation is required.

The only deltas vs the verbatim my-kluster source are:
1. Add `global.fullnameOverride: <alias>` at the top of each section (RESEARCH §"Critical Naming Constraint: Deployment Selector" — without this, the umbrella renders Service names like `arr-stack-sonarr` and breaks internal DNS).
2. Add `# renovate: image=<repo>` above every `repository:` line (D-04-PIN-03).
3. Pin qbittorrent's `tag: latest` → `tag: "5.2.0"` (D-04-PIN-01; resolved from `evidence/current-image-tags.txt`).

Purpose: These 4 apps form the core *arr stack (Sonarr + Radarr + Prowlarr + qBittorrent). Getting them right unblocks the byte-equivalence diff for the cutover (Plan 04-08).

Output: `values.yaml` grows by ~200 lines of per-alias values bodies. `helm template` now produces actual K8s resources for these 4 apps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-CONTEXT.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-PATTERNS.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-01-SUMMARY.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-02-SUMMARY.md
@CLAUDE.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/sonarr.yaml
@.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/radarr.yaml
@.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/prowlarr.yaml
@.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/qbittorrent.yaml

<interfaces>
<!-- Source-of-truth: my-kluster main-branch helm.values for each app. Already in app-template 5.0.0 shape — port directly. -->

Source: `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/sonarr-app.yaml` (helm.values field)
  Used by: values.yaml sonarr section (verbatim port + 3 deltas)

Source: `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/radarr-app.yaml`
  Used by: values.yaml radarr section

Source: `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/prowlarr-app.yaml`
  Used by: values.yaml prowlarr section

Source: `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/qbittorrent-app.yaml`
  Used by: values.yaml qbittorrent section (port + pin `latest` → `5.2.0`)
</interfaces>
</context>

<executor_note>
`values.yaml` is APPEND-ONLY in this wave. Plan 04-02 created the skeleton with empty `{}` placeholders. This plan REPLACES `sonarr: {}` with the full sonarr body (and same for radarr/prowlarr/qbittorrent), preserving the existing entries for cleanuparr/seerr/flaresolverr/jellyfin/arrconf/configarr (which Plans 04-04 and 04-05 will fill).

When making edits, prefer `Edit` over `Write` to preserve unrelated content. Each alias body is a self-contained YAML subtree under its top-level key; replacing `<alias>: {}` with the full body is a localised change.
</executor_note>

<tasks>

<task type="auto">
  <name>Task 3.1: Append sonarr + radarr alias bodies</name>
  <files>charts/arr-stack/values.yaml</files>
  <read_first>
    - `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/sonarr-app.yaml` (helm.values is the source of truth)
    - `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/radarr-app.yaml`
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Pattern 1: Media App Alias (Sonarr Example)" (shape reference — confirm fullnameOverride placement)
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Common Pitfalls" Pitfalls 1 + 2 + 3 (fullnameOverride; no YAML anchors; existingClaim discipline)
  </read_first>
  <action>
Replace `sonarr: {}` in `charts/arr-stack/values.yaml` with the sonarr body below, and `radarr: {}` with the radarr body. Both bodies are derived directly from the my-kluster helm.values fields — same shape, just lifted into the umbrella values context.

**sonarr body** (replace `sonarr: {}` with):

```yaml
sonarr:
  global:
    fullnameOverride: sonarr
  controllers:
    main:
      containers:
        main:
          image:
            # renovate: image=lscr.io/linuxserver/sonarr
            repository: lscr.io/linuxserver/sonarr
            tag: "4.0.17"
          env:
            TZ: "Europe/Paris"
            PUID: "1000"
            PGID: "1000"
  service:
    main:
      controller: main
      ports:
        http:
          port: 8989
  ingress:
    main:
      className: nginx
      annotations:
        cert-manager.io/cluster-issuer: "letsencrypt-prod"
        nginx.ingress.kubernetes.io/auth-url: "https://auth.tgu.ovh/oauth2/auth"
        nginx.ingress.kubernetes.io/auth-signin: "https://auth.tgu.ovh/oauth2/start?rd=https://sonarr.tgu.ovh"
      hosts:
        - host: sonarr.tgu.ovh
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: sonarr-tls
          hosts:
            - sonarr.tgu.ovh
  persistence:
    config:
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 2Gi
      globalMounts:
        - path: /config
    torrents:
      # Même hostPath que qBittorrent pour accéder aux fichiers téléchargés
      type: hostPath
      hostPath: /opt/media-stack/torrents
      hostPathType: DirectoryOrCreate
      globalMounts:
        - path: /data/torrents
    media:
      # NAS NFS — destination finale des séries
      type: persistentVolumeClaim
      existingClaim: media-nas-pvc
      globalMounts:
        - path: /media
```

**radarr body** (replace `radarr: {}` with):

```yaml
radarr:
  global:
    fullnameOverride: radarr
  controllers:
    main:
      containers:
        main:
          image:
            # renovate: image=lscr.io/linuxserver/radarr
            repository: lscr.io/linuxserver/radarr
            tag: "6.1.1"
          env:
            TZ: "Europe/Paris"
            PUID: "1000"
            PGID: "1000"
  service:
    main:
      controller: main
      ports:
        http:
          port: 7878
  ingress:
    main:
      className: nginx
      annotations:
        cert-manager.io/cluster-issuer: "letsencrypt-prod"
        nginx.ingress.kubernetes.io/auth-url: "https://auth.tgu.ovh/oauth2/auth"
        nginx.ingress.kubernetes.io/auth-signin: "https://auth.tgu.ovh/oauth2/start?rd=https://radarr.tgu.ovh"
      hosts:
        - host: radarr.tgu.ovh
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: radarr-tls
          hosts:
            - radarr.tgu.ovh
  persistence:
    config:
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 2Gi
      globalMounts:
        - path: /config
    torrents:
      type: hostPath
      hostPath: /opt/media-stack/torrents
      hostPathType: DirectoryOrCreate
      globalMounts:
        - path: /data/torrents
    media:
      type: persistentVolumeClaim
      existingClaim: media-nas-pvc
      globalMounts:
        - path: /media
```

**3 deltas vs the my-kluster source for each (encoded in the bodies above):**
1. Added `global.fullnameOverride: <alias>` block at the top (RESEARCH §"Critical Naming Constraint")
2. Added `# renovate: image=lscr.io/linuxserver/<alias>` comment immediately above each `repository:` line (D-04-PIN-03)
3. Quoted the version `tag:` value as a string ("4.0.17", "6.1.1") to prevent YAML number-coercion edge cases

Render and verify:
```bash
helm template arr-stack charts/arr-stack/ > /tmp/wave2-after-sonarr-radarr.yaml
grep -c '^kind: Deployment$' /tmp/wave2-after-sonarr-radarr.yaml  # expect: 2
grep -c '^kind: Service$' /tmp/wave2-after-sonarr-radarr.yaml     # expect: 2
grep -c '^  name: sonarr$' /tmp/wave2-after-sonarr-radarr.yaml    # >=2 (Deployment+Service+Ingress+PVC use name: sonarr)
```
  </action>
  <verify>
    <automated>bash -c 'helm lint charts/arr-stack/ && helm template arr-stack charts/arr-stack/ > /tmp/w2-sr.yaml && grep -c "^  name: sonarr$" /tmp/w2-sr.yaml | xargs test 4 -le && grep -c "^  name: radarr$" /tmp/w2-sr.yaml | xargs test 4 -le && tools/scripts/check-renovate-annotations.sh charts/arr-stack/values.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - `helm lint charts/arr-stack/` exits 0
    - `helm template arr-stack charts/arr-stack/ > /tmp/w2-sr.yaml` exits 0
    - 2 Deployments rendered: `grep -c '^kind: Deployment$' /tmp/w2-sr.yaml` >= 2 (more allowed once we wire downstream waves)
    - Sonarr Service preserves DNS: `grep -c '^  name: sonarr$' /tmp/w2-sr.yaml` >= 4 (Deployment + Service + Ingress + PVC all named `sonarr` due to fullnameOverride)
    - Radarr Service preserves DNS: `grep -c '^  name: radarr$' /tmp/w2-sr.yaml` >= 4
    - No release-prefixed Services (regression sentinel): `! grep -q '^  name: arr-stack-sonarr' /tmp/w2-sr.yaml && ! grep -q '^  name: arr-stack-radarr' /tmp/w2-sr.yaml`
    - Renovate annotations present: `grep -c '# renovate: image=lscr.io/linuxserver/sonarr' charts/arr-stack/values.yaml` == 1
    - `grep -c '# renovate: image=lscr.io/linuxserver/radarr' charts/arr-stack/values.yaml` == 1
    - `tools/scripts/check-renovate-annotations.sh charts/arr-stack/values.yaml` exits 0 (annotation contract holds for what's been added so far)
    - existingClaim `media-nas-pvc` referenced in both sections: `grep -c 'existingClaim: media-nas-pvc' charts/arr-stack/values.yaml` >= 2
    - No regression on Plan 04-02's empty placeholders for the remaining 6 aliases: `grep -c '^prowlarr: {}$' charts/arr-stack/values.yaml` == 1 (Task 3.2 replaces it)
    - The sonarr port renders correctly: `grep -A2 'name: sonarr' /tmp/w2-sr.yaml | grep -q 'port: 8989' || grep -B5 'port: 8989' /tmp/w2-sr.yaml | grep -q sonarr`
  </acceptance_criteria>
  <done>Sonarr and Radarr aliases render correctly: Service names preserve internal DNS, Renovate annotations are in place, no YAML errors, no release-prefixed regression.</done>
</task>

<task type="auto">
  <name>Task 3.2: Append prowlarr + qbittorrent alias bodies (qbittorrent pin :latest → 5.2.0)</name>
  <files>charts/arr-stack/values.yaml</files>
  <read_first>
    - `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/prowlarr-app.yaml`
    - `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/qbittorrent-app.yaml`
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Per-App Values Blocks → prowlarr" and §"qbittorrent"
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-01-SUMMARY.md` (resolved qbittorrent semver from the Wave 0 digest = 5.2.0)
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-01-DRIFT-NOTE.md` (qbittorrent digest sha256:2e0148... → 5.2.0 per RESEARCH §"Running Image Digests")
  </read_first>
  <action>
Replace `prowlarr: {}` and `qbittorrent: {}` in `charts/arr-stack/values.yaml` with the bodies below. Two important details:

- **prowlarr** has its oauth2-proxy annotations **commented out** in the my-kluster source (likely so Prowlarr can be called by Sonarr/Radarr without an oauth flow). Preserve that comment exactly — byte-equivalence is the goal.
- **qbittorrent** ships as `tag: latest` in production. Pin it to `"5.2.0"` per D-04-PIN-01 (resolved from `evidence/current-image-tags.txt` digest `sha256:2e0148...`).

**prowlarr body** (replace `prowlarr: {}` with):

```yaml
prowlarr:
  global:
    fullnameOverride: prowlarr
  controllers:
    main:
      containers:
        main:
          image:
            # renovate: image=lscr.io/linuxserver/prowlarr
            repository: lscr.io/linuxserver/prowlarr
            tag: "2.3.5"
          env:
            TZ: "Europe/Paris"
            PUID: "1000"
            PGID: "1000"
  service:
    main:
      controller: main
      ports:
        http:
          port: 9696
  ingress:
    main:
      className: nginx
      annotations:
        cert-manager.io/cluster-issuer: "letsencrypt-prod"
        # nginx.ingress.kubernetes.io/auth-url: "https://auth.tgu.ovh/oauth2/auth"
        # nginx.ingress.kubernetes.io/auth-signin: "https://auth.tgu.ovh/oauth2/start?rd=https://prowlarr.tgu.ovh"
      hosts:
        - host: prowlarr.tgu.ovh
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: prowlarr-tls
          hosts:
            - prowlarr.tgu.ovh
  persistence:
    config:
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 1Gi
      globalMounts:
        - path: /config
```

**qbittorrent body** (replace `qbittorrent: {}` with — note `tag: "5.2.0"` not `tag: latest`):

```yaml
qbittorrent:
  global:
    fullnameOverride: qbittorrent
  controllers:
    main:
      containers:
        main:
          image:
            # renovate: image=lscr.io/linuxserver/qbittorrent
            repository: lscr.io/linuxserver/qbittorrent
            tag: "5.2.0"  # pinned per D-04-PIN-01 from evidence/current-image-tags.txt digest sha256:2e0148... (was: latest)
          env:
            TZ: "Europe/Paris"
            PUID: "1000"
            PGID: "1000"
            WEBUI_PORT: "8080"
  service:
    main:
      controller: main
      ports:
        http:
          port: 8080
  ingress:
    main:
      className: nginx
      annotations:
        cert-manager.io/cluster-issuer: "letsencrypt-prod"
        nginx.ingress.kubernetes.io/auth-url: "https://auth.tgu.ovh/oauth2/auth"
        nginx.ingress.kubernetes.io/auth-signin: "https://auth.tgu.ovh/oauth2/start?rd=https://qbittorrent.tgu.ovh"
      hosts:
        - host: qbittorrent.tgu.ovh
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: qbittorrent-tls
          hosts:
            - qbittorrent.tgu.ovh
  persistence:
    config:
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 1Gi
      globalMounts:
        - path: /config
    torrents:
      # Stockage local sur le nœud MicroK8s
      # Partage le même path que Radarr/Sonarr pour accéder aux fichiers téléchargés
      type: hostPath
      hostPath: /opt/media-stack/torrents
      hostPathType: DirectoryOrCreate
      globalMounts:
        - path: /data
```

The `tag: "5.2.0"` pin is the **only non-verbatim change** from the my-kluster source. The byte-equivalence diff in Plan 04-08 must explicitly tolerate this (it's a pre-approved diff because the production tag `latest` resolves to digest `sha256:2e0148...` which is `5.2.0` — no behavioural change, only an explicit pin replacing implicit drift).

Render and verify:
```bash
helm template arr-stack charts/arr-stack/ > /tmp/w2-full.yaml
# qbittorrent image must be 5.2.0, NOT latest
grep -A2 'name: qbittorrent$' /tmp/w2-full.yaml | head -20
```
  </action>
  <verify>
    <automated>bash -c 'helm lint charts/arr-stack/ && helm template arr-stack charts/arr-stack/ > /tmp/w2-full.yaml && ! grep -q "qbittorrent:latest" /tmp/w2-full.yaml && grep -q "qbittorrent:5.2.0" /tmp/w2-full.yaml && tools/scripts/check-renovate-annotations.sh charts/arr-stack/values.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - `helm lint charts/arr-stack/` exits 0
    - `helm template arr-stack charts/arr-stack/ > /tmp/w2-full.yaml` exits 0
    - qbittorrent NOT on `:latest`: `! grep -q 'image: lscr.io/linuxserver/qbittorrent:latest' /tmp/w2-full.yaml`
    - qbittorrent pinned to 5.2.0: `grep -q 'image: lscr.io/linuxserver/qbittorrent:5.2.0' /tmp/w2-full.yaml`
    - prowlarr renders: `grep -c '^  name: prowlarr$' /tmp/w2-full.yaml` >= 4
    - qbittorrent renders: `grep -c '^  name: qbittorrent$' /tmp/w2-full.yaml` >= 4
    - prowlarr's oauth2-proxy annotations stay commented out (byte-equivalence): `grep -A20 'prowlarr:$' charts/arr-stack/values.yaml | grep -c '^[[:space:]]*# nginx.ingress.kubernetes.io/auth-url'` == 1
    - All 4 Wave-2 aliases have `fullnameOverride`: `grep -c '^[[:space:]]*fullnameOverride:' charts/arr-stack/values.yaml` >= 4
    - All 4 Wave-2 repositories have annotations: `tools/scripts/check-renovate-annotations.sh charts/arr-stack/values.yaml` exits 0
    - 6 remaining aliases are still `{}` placeholders (W5 append-only contract): `for a in cleanuparr seerr flaresolverr jellyfin arrconf configarr; do grep -q "^${a}: {}$" charts/arr-stack/values.yaml || exit 1; done`
    - `! grep -q '^  name: arr-stack-' /tmp/w2-full.yaml` (no release-prefixed regression on any rendered resource)
    - `! grep -q 'tag: latest' charts/arr-stack/values.yaml` (Wave 5 check W3-equivalent: no `:latest` tags in values.yaml — note the 3 remaining `:latest` migrations land in Plan 04-04)
  </acceptance_criteria>
  <done>The 4 Wave-2 aliases (sonarr/radarr/prowlarr/qbittorrent) render to expected K8s manifests with preserved internal DNS, no release-prefix regression, and qbittorrent explicitly pinned to 5.2.0. The 6 remaining aliases are still empty placeholders awaiting Plans 04-04 and 04-05.</done>
</task>

</tasks>

<verification>
- `helm lint charts/arr-stack/` exits 0
- All 4 aliases render Deployments + Services + Ingresses with names matching the alias (no `arr-stack-<alias>` prefix)
- qbittorrent on `:5.2.0`, not `:latest`
- All 4 `repository:` lines have `# renovate: image=` annotations directly above them
- Plan 04-02's append-only contract preserved: cleanuparr/seerr/flaresolverr/jellyfin/arrconf/configarr still `{}`
</verification>

<success_criteria>
- The 4 *arr-family alias bodies are in place, byte-equivalent to my-kluster main except for the 3 deltas documented (fullnameOverride, renovate annotation, qbittorrent semver pin)
- `tools/scripts/check-renovate-annotations.sh` passes against the partially-populated values.yaml
- Internal DNS preserved (Sonarr's `http://sonarr.selfhost.svc.cluster.local:8989` still resolves at cutover)
</success_criteria>

<output>
After completion, create `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-03-SUMMARY.md` listing: (1) the 4 alias bodies appended, (2) the qbittorrent semver pin decision recorded (5.2.0, digest sha256:2e0148...), (3) any unexpected diff between the my-kluster source and the umbrella render (should be zero apart from the 3 documented deltas).
</output>
