---
phase: 04-umbrella-chart-migration-des-9-apps
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - charts/arr-stack/values.yaml
autonomous: true
requirements:
  - REQ-umbrella-deployment
  - REQ-renovate-image-tracking
  - REQ-helm-validation
  - REQ-config-as-code

must_haves:
  truths:
    - "values.yaml carries verbatim per-alias bodies for cleanuparr / seerr / flaresolverr / jellyfin ported from my-kluster main"
    - "All 4 aliases ship `global.fullnameOverride: <alias>`"
    - "Every `repository:` line in the 4 sections has a `# renovate: image=<repo>` annotation directly above it"
    - "cleanuparr pinned to `tag: 2.3.3` (was: latest) and flaresolverr pinned to `tag: v3.4.6` (was: latest) — concrete values from RESEARCH §State of the Art and the Wave 0 digest resolution"
    - "jellyfin section preserves NO oauth2-proxy annotations (Jellyfin handles its own auth) but keeps the `proxy-body-size: '0'` annotation (RESEARCH §Per-App Values Blocks)"
    - "flaresolverr renders WITHOUT an ingress (cluster-internal use only — comment from my-kluster source preserved)"
    - "After this plan, `! grep -q 'tag: latest' charts/arr-stack/values.yaml` (the C9 no-:latest invariant holds)"
  artifacts:
    - path: charts/arr-stack/values.yaml
      provides: "cleanuparr / seerr / flaresolverr / jellyfin alias bodies"
      contains: "fullnameOverride: cleanuparr"
      min_lines: 350  # cumulative with Plan 04-03
  key_links:
    - from: charts/arr-stack/values.yaml (cleanuparr image)
      to: ghcr.io/cleanuparr/cleanuparr:2.3.3
      via: "Pinned semver replaces production :latest"
      pattern: 'tag: "2.3.3"'
    - from: charts/arr-stack/values.yaml (flaresolverr image)
      to: ghcr.io/flaresolverr/flaresolverr:v3.4.6
      via: "Pinned semver replaces production :latest"
      pattern: 'tag: "v3.4.6"'
    - from: charts/arr-stack/values.yaml (jellyfin ingress)
      to: jellyfin.tgu.ovh
      via: "No oauth2-proxy — Jellyfin manages auth itself"
      pattern: "proxy-body-size"
---

<objective>
Populate `values.yaml` with the four remaining media-app aliases that close out the `:latest` migration: **cleanuparr** (pinned to 2.3.3), **seerr** (v3.2.0), **flaresolverr** (pinned to v3.4.6, no ingress), and **jellyfin** (10.11.8, no oauth2-proxy). After this plan, no `repository:` line in `values.yaml` carries `tag: latest` — closing C9 from spec.md.

Source-of-truth: verbatim helm.values from `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/<app>-app.yaml` for each app, with the same 3-delta pattern as Plan 04-03 (add fullnameOverride, add renovate annotations, pin :latest tags).

Purpose: This closes out the 8 media-app aliases. The umbrella chart now renders the full media stack; Plan 04-05 follows up with the 2 CronJob aliases.

Output: ~150 lines appended to `values.yaml`; 4 more app-template aliases populated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-CONTEXT.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-PATTERNS.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-01-SUMMARY.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-02-SUMMARY.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-03-SUMMARY.md
@CLAUDE.md
@.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/cleanuparr.yaml
@.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/seerr.yaml
@.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/flaresolverr.yaml
@.planning/phases/04-umbrella-chart-migration-des-9-apps/evidence/pre-cutover-argocd/jellyfin.yaml

<interfaces>
Source: `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/cleanuparr-app.yaml` (helm.values field) — pin tag `:latest` → `"2.3.3"`
Source: `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/seerr-app.yaml` — port verbatim (already `v3.2.0`)
Source: `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/flaresolverr-app.yaml` — pin tag `:latest` → `"v3.4.6"`, preserve "no ingress" comment
Source: `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/jellyfin-app.yaml` — port verbatim (already `10.11.8`), preserve `proxy-body-size` annotation and absence of oauth2-proxy

Resolved tag pins (from Plan 04-01 SUMMARY + RESEARCH §"State of the Art"):
- cleanuparr: digest sha256:9b8f7a5f... → semver 2.3.3
- flaresolverr: digest sha256:7962759d... → semver v3.4.6
</interfaces>
</context>

<executor_note>
`values.yaml` append-only contract still holds. This plan REPLACES `cleanuparr: {}` / `seerr: {}` / `flaresolverr: {}` / `jellyfin: {}` with full bodies, preserving `arrconf: {}` and `configarr: {}` for Plan 04-05.
</executor_note>

<tasks>

<task type="auto">
  <name>Task 4.1: Append cleanuparr + seerr alias bodies (cleanuparr pin :latest → 2.3.3)</name>
  <files>charts/arr-stack/values.yaml</files>
  <read_first>
    - `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/cleanuparr-app.yaml`
    - `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/seerr-app.yaml`
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Per-App Values Blocks → cleanuparr" and §"seerr"
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-01-SUMMARY.md` (resolved cleanuparr semver = 2.3.3 from digest sha256:9b8f7a5f...)
  </read_first>
  <action>
**cleanuparr body** (replace `cleanuparr: {}` with — note `tag: "2.3.3"` not `latest`):

```yaml
cleanuparr:
  global:
    fullnameOverride: cleanuparr
  controllers:
    main:
      containers:
        main:
          image:
            # renovate: image=ghcr.io/cleanuparr/cleanuparr
            repository: ghcr.io/cleanuparr/cleanuparr
            tag: "2.3.3"  # pinned per D-04-PIN-01 from evidence/current-image-tags.txt digest sha256:9b8f7a5f... (was: latest)
          env:
            TZ: "Europe/Paris"
  service:
    main:
      controller: main
      ports:
        http:
          port: 11011
  ingress:
    main:
      className: nginx
      annotations:
        cert-manager.io/cluster-issuer: "letsencrypt-prod"
        nginx.ingress.kubernetes.io/auth-url: "https://auth.tgu.ovh/oauth2/auth"
        nginx.ingress.kubernetes.io/auth-signin: "https://auth.tgu.ovh/oauth2/start?rd=https://cleanuparr.tgu.ovh"
      hosts:
        - host: cleanuparr.tgu.ovh
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: cleanuparr-tls
          hosts:
            - cleanuparr.tgu.ovh
  persistence:
    config:
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 1Gi
      globalMounts:
        - path: /config
```

**seerr body** (replace `seerr: {}` with):

```yaml
seerr:
  global:
    fullnameOverride: seerr
  controllers:
    main:
      containers:
        main:
          image:
            # renovate: image=ghcr.io/seerr-team/seerr
            repository: ghcr.io/seerr-team/seerr
            tag: "v3.2.0"
          env:
            TZ: "Europe/Paris"
  service:
    main:
      controller: main
      ports:
        http:
          port: 5055
  ingress:
    main:
      className: nginx
      annotations:
        cert-manager.io/cluster-issuer: "letsencrypt-prod"
        nginx.ingress.kubernetes.io/auth-url: "https://auth.tgu.ovh/oauth2/auth"
        nginx.ingress.kubernetes.io/auth-signin: "https://auth.tgu.ovh/oauth2/start?rd=https://seerr.tgu.ovh"
      hosts:
        - host: seerr.tgu.ovh
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: seerr-tls
          hosts:
            - seerr.tgu.ovh
  persistence:
    config:
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 2Gi
      globalMounts:
        - path: /app/config
```

Note: the seerr image repository `ghcr.io/seerr-team/seerr` is verbatim from `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/seerr-app.yaml`. RESEARCH Open Question #1 flagged this — confirmed by reading the my-kluster source directly.
  </action>
  <verify>
    <automated>bash -c 'helm lint charts/arr-stack/ && helm template arr-stack charts/arr-stack/ > /tmp/w3a.yaml && grep -q "cleanuparr:2.3.3" /tmp/w3a.yaml && ! grep -q "cleanuparr:latest" /tmp/w3a.yaml && grep -q "seerr-team/seerr:v3.2.0" /tmp/w3a.yaml && tools/scripts/check-renovate-annotations.sh charts/arr-stack/values.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - `helm lint charts/arr-stack/` exits 0
    - cleanuparr pinned to 2.3.3: `grep -q 'image: ghcr.io/cleanuparr/cleanuparr:2.3.3' /tmp/w3a.yaml`
    - cleanuparr NOT on :latest: `! grep -q 'image: ghcr.io/cleanuparr/cleanuparr:latest' /tmp/w3a.yaml`
    - seerr renders: `grep -q 'image: ghcr.io/seerr-team/seerr:v3.2.0' /tmp/w3a.yaml`
    - cleanuparr Service named `cleanuparr` (DNS preserved): `grep -c '^  name: cleanuparr$' /tmp/w3a.yaml` >= 4
    - seerr Service named `seerr`: `grep -c '^  name: seerr$' /tmp/w3a.yaml` >= 4
    - Renovate annotations: `grep -c '# renovate: image=ghcr.io/cleanuparr/cleanuparr' charts/arr-stack/values.yaml` == 1
    - `grep -c '# renovate: image=ghcr.io/seerr-team/seerr' charts/arr-stack/values.yaml` == 1
    - `tools/scripts/check-renovate-annotations.sh charts/arr-stack/values.yaml` exits 0
    - Append-only contract: `grep -q '^flaresolverr: {}$' charts/arr-stack/values.yaml && grep -q '^jellyfin: {}$' charts/arr-stack/values.yaml && grep -q '^arrconf: {}$' charts/arr-stack/values.yaml && grep -q '^configarr: {}$' charts/arr-stack/values.yaml`
  </acceptance_criteria>
  <done>cleanuparr and seerr aliases render to expected K8s manifests with DNS preserved, renovate annotations in place, cleanuparr explicitly pinned to 2.3.3.</done>
</task>

<task type="auto">
  <name>Task 4.2: Append flaresolverr (no ingress, pin v3.4.6) + jellyfin (no oauth2-proxy) bodies</name>
  <files>charts/arr-stack/values.yaml</files>
  <read_first>
    - `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/flaresolverr-app.yaml`
    - `git -C /home/moi/projets/perso/my-kluster show main:argocd/argocd-apps/jellyfin-app.yaml`
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-RESEARCH.md` §"Per-App Values Blocks → flaresolverr" and §"jellyfin"
    - `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-01-SUMMARY.md` (resolved flaresolverr semver = v3.4.6 from digest sha256:7962759d...)
    - `CLAUDE.md` "Conventions Helm — umbrella chart" (no `:latest` rule — this plan closes it for the last 3 apps)
  </read_first>
  <action>
**flaresolverr body** (replace `flaresolverr: {}` with — pin to v3.4.6, preserve "no ingress" comment block):

```yaml
flaresolverr:
  global:
    fullnameOverride: flaresolverr
  controllers:
    main:
      containers:
        main:
          image:
            # renovate: image=ghcr.io/flaresolverr/flaresolverr
            repository: ghcr.io/flaresolverr/flaresolverr
            tag: "v3.4.6"  # pinned per D-04-PIN-01 from evidence/current-image-tags.txt digest sha256:7962759d... (was: latest)
          env:
            TZ: "Europe/Paris"
            LOG_LEVEL: "info"
  service:
    main:
      controller: main
      ports:
        http:
          port: 8191
  # Pas d'ingress : accès interne uniquement
  # URL depuis les autres pods : http://flaresolverr.selfhost.svc.cluster.local:8191
```

(No `ingress:` block — flaresolverr is consumed only by Prowlarr in-cluster. The comment is preserved verbatim from the my-kluster source.)

**jellyfin body** (replace `jellyfin: {}` with — keep NO oauth2-proxy annotations + preserve `proxy-body-size: "0"`):

```yaml
jellyfin:
  global:
    fullnameOverride: jellyfin
  controllers:
    main:
      containers:
        main:
          image:
            # renovate: image=lscr.io/linuxserver/jellyfin
            repository: lscr.io/linuxserver/jellyfin
            tag: "10.11.8"
          env:
            TZ: "Europe/Paris"
            PUID: "1000"
            PGID: "1000"
  service:
    main:
      controller: main
      ports:
        http:
          port: 8096
  ingress:
    main:
      className: nginx
      annotations:
        cert-manager.io/cluster-issuer: "letsencrypt-prod"
        # Pas d'oauth2-proxy : Jellyfin gère sa propre authentification
        # ⚠️ Créer le compte admin immédiatement après le premier déploiement
        nginx.ingress.kubernetes.io/proxy-body-size: "0"
      hosts:
        - host: jellyfin.tgu.ovh
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - secretName: jellyfin-tls
          hosts:
            - jellyfin.tgu.ovh
  persistence:
    config:
      # Stockage local : BDD SQLite, cache de transcoding, métadonnées (thumbnails, etc.)
      type: persistentVolumeClaim
      accessMode: ReadWriteOnce
      size: 10Gi
      globalMounts:
        - path: /config
    media:
      # NAS NFS — bibliothèque partagée avec Radarr/Sonarr
      type: persistentVolumeClaim
      existingClaim: media-nas-pvc
      globalMounts:
        - path: /media
```

Note: Jellyfin's ingress annotations carry `proxy-body-size: "0"` (uncapped upload — needed for large media uploads via WebDAV) but NO oauth2-proxy headers. Both invariants are verifiable.

Render and verify the cumulative state:
```bash
helm template arr-stack charts/arr-stack/ > /tmp/w3-full.yaml
grep -c '^kind: Deployment$' /tmp/w3-full.yaml  # expect: 8 (Wave 2 aliases + cleanuparr + seerr + flaresolverr + jellyfin)
grep -c '^kind: Service$' /tmp/w3-full.yaml     # expect: 8
grep -c '^kind: Ingress$' /tmp/w3-full.yaml     # expect: 7 (all except flaresolverr)
```
  </action>
  <verify>
    <automated>bash -c 'helm lint charts/arr-stack/ && helm template arr-stack charts/arr-stack/ > /tmp/w3-full.yaml && grep -q "flaresolverr:v3.4.6" /tmp/w3-full.yaml && ! grep -q "flaresolverr:latest" /tmp/w3-full.yaml && grep -q "jellyfin:10.11.8" /tmp/w3-full.yaml && tools/scripts/check-renovate-annotations.sh charts/arr-stack/values.yaml && ! grep -q "tag: latest" charts/arr-stack/values.yaml'</automated>
  </verify>
  <acceptance_criteria>
    - `helm lint charts/arr-stack/` exits 0
    - flaresolverr pinned to v3.4.6: `grep -q 'image: ghcr.io/flaresolverr/flaresolverr:v3.4.6' /tmp/w3-full.yaml`
    - flaresolverr NOT on :latest: `! grep -q 'image: ghcr.io/flaresolverr/flaresolverr:latest' /tmp/w3-full.yaml`
    - jellyfin on 10.11.8: `grep -q 'image: lscr.io/linuxserver/jellyfin:10.11.8' /tmp/w3-full.yaml`
    - **The :latest invariant holds for all 8 media apps**: `! grep -q 'tag: latest' charts/arr-stack/values.yaml`
    - flaresolverr has NO Ingress: `awk '/^---$/{p=0} /name: flaresolverr$/{p=1} p && /^kind:/' /tmp/w3-full.yaml | grep -v 'kind: Ingress'` succeeds AND `grep -c 'name: flaresolverr' /tmp/w3-full.yaml` corresponds to Deployment+Service+ServiceAccount only (no Ingress)
    - jellyfin has Ingress with NO oauth2-proxy annotation: `awk '/jellyfin/{p=1} p && /---/{p=0} p' /tmp/w3-full.yaml | grep -c 'oauth2-proxy\|auth-url\|auth-signin'` == 0
    - jellyfin Ingress has `proxy-body-size: "0"`: `awk '/jellyfin/{p=1} p && /---/{p=0} p' /tmp/w3-full.yaml | grep -q 'proxy-body-size'`
    - Cumulative count: `grep -c '^kind: Deployment$' /tmp/w3-full.yaml` == 8 (all 8 media apps render)
    - `grep -c '^kind: Service$' /tmp/w3-full.yaml` == 8
    - `grep -c '^kind: Ingress$' /tmp/w3-full.yaml` == 7 (all except flaresolverr)
    - All 8 aliases have fullnameOverride: `grep -c '^[[:space:]]\{2,4\}fullnameOverride:' charts/arr-stack/values.yaml` == 8
    - `tools/scripts/check-renovate-annotations.sh charts/arr-stack/values.yaml` exits 0
    - Plan 04-05's targets still untouched: `grep -q '^arrconf: {}$' charts/arr-stack/values.yaml && grep -q '^configarr: {}$' charts/arr-stack/values.yaml`
    - No release-prefixed Services: `! grep -q '^  name: arr-stack-' /tmp/w3-full.yaml`
  </acceptance_criteria>
  <done>All 8 media-app aliases now render with pinned semver tags (no `:latest` in values.yaml), preserved DNS, preserved Jellyfin auth posture, preserved flaresolverr no-ingress shape. The :latest invariant from CLAUDE.md "Ce que tu NE dois PAS faire" is enforced across the 3 previously-affected apps (qbittorrent in Plan 04-03; cleanuparr + flaresolverr here).</done>
</task>

</tasks>

<verification>
- `helm lint charts/arr-stack/` exits 0
- `helm template` renders 8 Deployments, 8 Services, 7 Ingresses (flaresolverr is in-cluster only)
- `! grep -q 'tag: latest' charts/arr-stack/values.yaml` (C9 invariant satisfied)
- `tools/scripts/check-renovate-annotations.sh` passes
- Plan 04-05's targets (arrconf + configarr) still empty placeholders
</verification>

<success_criteria>
- Closure of the 8 media-app aliases
- Closure of the :latest pin migration (D-04-PIN-01) for the 3 apps that needed it (qbittorrent done in Plan 04-03; cleanuparr + flaresolverr done here)
- Internal DNS preserved for all 8 apps via `fullnameOverride`
- Renovate annotation contract holds across all 8 `repository:` lines
</success_criteria>

<output>
After completion, create `.planning/phases/04-umbrella-chart-migration-des-9-apps/04-04-SUMMARY.md` recording: (1) the cumulative render shape (8 Deployments, 8 Services, 7 Ingresses, 2 ConfigMaps), (2) the 3 :latest pin resolutions (qbittorrent 5.2.0, cleanuparr 2.3.3, flaresolverr v3.4.6), (3) confirmation that Jellyfin's auth posture is preserved (NO oauth2-proxy annotations, YES proxy-body-size).
</output>
