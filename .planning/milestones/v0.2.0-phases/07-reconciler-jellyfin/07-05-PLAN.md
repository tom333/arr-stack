---
phase: 07
plan: 05
type: execute
wave: 3
depends_on: [04]
files_modified:
  - charts/arr-stack/files/arrconf.yml
  - charts/arr-stack/values.yaml
  - charts/arr-stack/values.schema.json
  - tools/arrconf/tests/test_arrconf_yml_validates.py
autonomous: true
requirements: [REQ-app-coverage, REQ-yaml-autocomplete]
requirements_addressed: [REQ-app-coverage, REQ-yaml-autocomplete]

must_haves:
  truths:
    - "charts/arr-stack/files/arrconf.yml has a new `jellyfin:` top-level section with `main:` subsection containing the 4 reconcile blocks (libraries / users / server_config / plugins) — matches the canonical YAML in CONTEXT.md §26-71"
    - "charts/arr-stack/values.yaml line 458 `--apps` arg list is extended from `sonarr,radarr,prowlarr,qbittorrent,seerr` to `sonarr,radarr,prowlarr,qbittorrent,seerr,jellyfin` — closes D-06-CHART-ARGS-01 carry-forward (without this, the cluster CronJob silently skips Jellyfin reconcile)"
    - "tests/test_arrconf_yml_validates.py extended (or new test added) to assert RootConfig parses the live charts/arr-stack/files/arrconf.yml with the new jellyfin block — CI gate prevents broken YAML from shipping"
    - "helm lint charts/arr-stack/ passes (no schema violations against values.schema.json)"
    - "helm template charts/arr-stack/ -f examples/values-prod.yaml | kubeconform - passes (rendered manifests are k8s-valid)"
  artifacts:
    - path: "charts/arr-stack/files/arrconf.yml"
      provides: "jellyfin.main YAML section with the 4 reconcile blocks + comment block citing D-07-* decisions and Pitfalls 1/2/4/5/6"
      contains: "jellyfin:"
    - path: "charts/arr-stack/values.yaml"
      provides: "1-line edit to --apps list extending it with jellyfin (D-07-CHART-ARGS-01)"
      contains: "sonarr,radarr,prowlarr,qbittorrent,seerr,jellyfin"
    - path: "charts/arr-stack/values.schema.json"
      provides: "Optional schema extension for the arrconf --apps enum (or no-op if Phase 4 schema already covers it)"
      contains: "jellyfin"
    - path: "tools/arrconf/tests/test_arrconf_yml_validates.py"
      provides: "Test asserting RootConfig.load_config(charts/arr-stack/files/arrconf.yml).jellyfin['main'] parses cleanly with expected values"
      contains: "test_arrconf_yml_validates_jellyfin"
  key_links:
    - from: "charts/arr-stack/files/arrconf.yml jellyfin.main.users.admin.IsAdministrator"
      to: "tools/arrconf/arrconf/config.py JellyfinUserPolicy.IsAdministrator"
      via: "load_config parses the YAML into the pydantic model; Plan 07-04 reconciler reads it from instance.users.admin.IsAdministrator"
      pattern: "IsAdministrator:\\s*true"
    - from: "charts/arr-stack/values.yaml line 458 --apps list"
      to: "tools/arrconf/arrconf/__main__.py _selected_apps() + jellyfin dispatch branch"
      via: "CronJob args become argv to `arrconf apply --apps <list>`; _selected_apps splits the comma list; the branch only runs if 'jellyfin' is in the targets set"
      pattern: "'sonarr,radarr,prowlarr,qbittorrent,seerr,jellyfin'"
---

<objective>
Wave 3 of Phase 7 — chart cutover. Add the `jellyfin.main` section to `charts/arr-stack/files/arrconf.yml`, extend the cluster CronJob `--apps` arg list to include `jellyfin`, and add a CI validation test ensuring the live YAML parses against the pydantic schema (Plan 07-02).

**Critical anti-pattern to avoid (D-06-CHART-ARGS-01 carry-forward)**: Phase 6 missed adding `seerr` to the `--apps` list and the first cluster CronJob run silently skipped Seerr entirely (Plan 06-07 deviation, hotfix commit `ff39507`). Phase 7 must NOT repeat this — the 1-line edit to `values.yaml` line 458 is the single most important step in this plan.

**Comment block discipline** (Phase 6 D-06-CHART-ARGS-01 + Phase 5 D-05-PATHS-01 pattern): the new `jellyfin:` block in `arrconf.yml` should be preceded by a multi-line comment citing:
- The 4 D-07-* decisions (D-07-LIB-01, D-07-USERS-01, D-07-CONFIG-01, D-07-PLUGINS-01)
- The 6 critical Pitfalls (1, 2, 4, 5, 6, 7)
- Note that `AuthenticationProviderId` + `PasswordResetProviderId` are INTENTIONALLY ABSENT from YAML (Pitfall 6 — reconciler re-injects from cluster GET)

Plan 07-06 (cluster apply) depends on this plan landing the chart changes AND triggering the auto-tag → image-build → my-kluster bump chain.

Output: 3 chart files modified + 1 test file extended. No code changes in tools/arrconf/arrconf/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-reconciler-jellyfin/07-CONTEXT.md
@.planning/phases/07-reconciler-jellyfin/07-RESEARCH.md
@.planning/phases/07-reconciler-jellyfin/07-PATTERNS.md
@.planning/phases/07-reconciler-jellyfin/07-VALIDATION.md
@.planning/phases/07-reconciler-jellyfin/07-04-PLAN.md
@.planning/phases/06-reconciler-seerr/06-07-SUMMARY.md
@charts/arr-stack/files/arrconf.yml
@charts/arr-stack/values.yaml
@charts/arr-stack/values.schema.json
@tools/arrconf/tests/test_arrconf_yml_validates.py

<interfaces>
<!-- The exact line in values.yaml to edit (1-line scope) -->

From charts/arr-stack/values.yaml line 458 (CURRENT):
```yaml
            - "sonarr,radarr,prowlarr,qbittorrent,seerr"   # D-05-ARGS-01: Phase 5 adds qbittorrent ; Phase 6 adds seerr (D-06-SCOPE-01)
```

EDIT TO (1-line atomic change):
```yaml
            - "sonarr,radarr,prowlarr,qbittorrent,seerr,jellyfin"   # D-05-ARGS-01: Phase 5 adds qbittorrent ; Phase 6 adds seerr (D-06-SCOPE-01) ; Phase 7 adds jellyfin (D-07-INSTANCE-01)
```

From charts/arr-stack/files/arrconf.yml (existing Seerr block at lines 368-434 — analog for jellyfin block, will be appended after the Seerr block ending at line 434):
```yaml
# Phase 6 (D-06-SCOPE-01): Seerr reconciler ...
seerr:
  main:
    base_url: http://seerr.selfhost.svc.cluster.local:5055
    sonarr_service:
      ...
    main_settings:
      enable: true
      defaultPermissions: 32
      ...
```

The new jellyfin block (D-07-INSTANCE-01) follows the same shape: comment header + `jellyfin:` + `main:` + 4 sub-sections.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chart YAML (operator-authored, committed) → ConfigMap → arrconf CronJob pod → arrconf YAML parser | Any malformed YAML here causes the entire arrconf CronJob to exit 2 (ConfigError) — not just the jellyfin branch. Test_arrconf_yml_validates is the CI gate that prevents shipping a broken chart. |
| values.yaml --apps list → CronJob argv → _selected_apps() set | The set is the actual dispatch filter. Missing "jellyfin" in --apps = silent skip (D-06-CHART-ARGS-01 carry-forward). Typo (e.g. "jelyfin") = typer.BadParameter exit 2 (Phase 3 CR-03 guard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-CHART-ARGS-MISS | Denial of Service (CronJob runs successfully but jellyfin block is silently skipped — Phase 6 D-06-CHART-ARGS-01 repeat) | charts/arr-stack/values.yaml line 458 | mitigate | Task 5.2 acceptance: `grep -E '\\-\\-apps.*sonarr.*radarr.*prowlarr.*qbittorrent.*seerr.*jellyfin' charts/arr-stack/values.yaml` returns 1+ matches. Plan 07-06 cluster verification will check actual CronJob args in the live cluster. |
| T-07-CHART-YAML-INVALID | Denial of Service (broken YAML in arrconf.yml causes arrconf to exit 2 ConfigError on the entire reconcile — not just jellyfin) | charts/arr-stack/files/arrconf.yml | mitigate | Task 5.3 test_arrconf_yml_validates_jellyfin asserts `load_config("charts/arr-stack/files/arrconf.yml")` returns a RootConfig with `cfg.jellyfin["main"]` populated. CI test_arrconf_yml_validates.py is the regression contract for Phase 7. |
| T-07-LIBRARY-PATH-DIVERGENCE | Tampering (operator updates Phase 5 D-05-PATHS-01 split paths but forgets the Jellyfin libraries → media not visible in Jellyfin) | charts/arr-stack/files/arrconf.yml libraries.items[].paths | mitigate | The YAML literal contains both Séries paths [/media/series, /media/anime, /media/family] and Films paths [/media/films, /media/films-anime, /media/films-family]. Plan 07-06 Wave 4 cluster verification (post-apply diff) catches any divergence. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 5.1: Append jellyfin.main block to charts/arr-stack/files/arrconf.yml</name>
  <read_first>
    - charts/arr-stack/files/arrconf.yml (full file — esp. lines 368-434 for the Seerr block analog header + structure)
    - .planning/phases/07-reconciler-jellyfin/07-CONTEXT.md §25-71 (the canonical YAML)
    - .planning/phases/07-reconciler-jellyfin/07-PATTERNS.md § charts/arr-stack/files/arrconf.yml (lines 793-822) — comment-block pattern
    - tools/arrconf/arrconf/config.py (modified by Plan 07-02 — confirms field names match YAML keys: ui_culture not uiCulture etc.)
  </read_first>
  <files>charts/arr-stack/files/arrconf.yml</files>
  <action>
    Append a new top-level `jellyfin:` section to the END of `charts/arr-stack/files/arrconf.yml` (currently 434 lines, ending with the seerr.main.main_settings block). Use the Edit tool to append after the last line.

    The new content to append (mirror Phase 6 comment-block discipline, cite D-07-* + Pitfalls):

    ```yaml

    # Phase 7 (D-07-INSTANCE-01): Jellyfin reconciler — single-instance per ADR-7.
    # 4 resources managed (D-07-ORDER-01 ordering libraries → users → server_config → plugins):
    #   1. libraries      — POST /Library/VirtualFolders/Paths
    #                       (Pitfall 2: NOT idempotent; reconciler set-membership shim
    #                        skips paths already present in LibraryOptions.PathInfos)
    #                       D-07-LIB-01: merged 2 libraries multi-path (Séries + Films);
    #                       D-07-LIB-02: scope = Name + CollectionType + PathInfos only
    #                       (LibraryOptions/TypeOptions/fetchers stay operator-managed via UI).
    #   2. users.admin    — POST /Users/{id}/Policy
    #                       (Pitfall 4: POST not PUT — PUT returns 405)
    #                       (Pitfall 6 / D-06-OPENAPI-01 carry-forward: AuthenticationProviderId
    #                        and PasswordResetProviderId are OpenAPI-REQUIRED but operator-NEVER-configured;
    #                        reconciler re-injects from cluster GET — pattern mirror Seerr apiKey D-06-CREDS-01).
    #                       D-07-USERS-01: admin "moi" only (Id 82fd95db72904569b08d83271823ceaa);
    #                       emilie operator-managed via UI, prune=false hardcoded.
    #   3. server_config  — POST /System/Configuration
    #                       (Pitfall 1: full REPLACE — reconciler does GET → merge 7-field allowlist
    #                        into cluster 56-field dict → POST entire body)
    #                       (Pitfall 7: PluginRepositories diff is set-by-URL — operator UI reorder
    #                        does not cause false-positive update events)
    #                       D-07-CONFIG-01 allowlist: UICulture, MetadataCountryCode,
    #                       PreferredMetadataLanguage, ActivityLogRetentionDays,
    #                       LogFileRetentionDays, ServerName, PluginRepositories.
    #                       (TrickplayOptions, MetadataOptions[], CodecsUsed, etc. stay operator-managed.)
    #   4. plugins        — POST /Plugins/{id}/{version}/Enable
    #                       (Pitfall 5: VERSION required in path; /Plugins/{id}/Enable returns 405)
    #                       D-07-PLUGINS-01: activation-only (no install, no uninstall).
    #                       6 plugins allowlisted (TMDb, OMDb, MusicBrainz, AudioDb, Studio Images, Kodi Sync Queue).
    #
    # AuthenticationProviderId AND PasswordResetProviderId are INTENTIONALLY ABSENT below —
    # preserved at reconcile-time from cluster GET (Pitfall 6 / D-06-OPENAPI-01 carry-forward).
    #
    # ADR-8 (forceSave) does NOT apply to Jellyfin (spec.md §895): the JellyfinClient
    # does NOT inherit from _ArrV3Client — Jellyfin has no forceSave mechanism.
    jellyfin:
      main:
        base_url: http://jellyfin.selfhost.svc.cluster.local:8096
        libraries:
          enable: true
          prune: false  # D-07-LIB-01 hardcoded — reconciler refuses DELETE (Pitfall 3 protection)
          items:
            - name: "Séries"
              collection_type: tvshows
              paths:
                - /media/series
                - /media/anime
                - /media/family
            - name: "Films"
              collection_type: movies
              paths:
                - /media/films
                - /media/films-anime
                - /media/films-family
        users:
          enable: true
          prune: false  # D-07-USERS-01 hardcoded — emilie operator-managed, never touched
          admin:
            IsAdministrator: true
            EnableContentDeletion: true
            EnableRemoteAccess: true
            EnableLiveTvAccess: true
            EnableLiveTvManagement: false
            EnableMediaPlayback: true
            EnableAudioPlaybackTranscoding: true
            EnableVideoPlaybackTranscoding: true
            EnablePlaybackRemuxing: true
            EnableContentDownloading: true
            EnableSyncTranscoding: true
            EnableMediaConversion: true
            EnableAllDevices: true
            EnableAllChannels: true
            EnableAllFolders: true
            InvalidLoginAttemptCount: 0
            LoginAttemptsBeforeLockout: -1
            MaxActiveSessions: 0
            EnablePublicSharing: false
            EnableUserPreferenceAccess: true
            EnableRemoteControlOfOtherUsers: false
            EnableSharedDeviceControl: true
            EnablePlaylistManagement: true
            EnableCollectionManagement: false
            EnableSubtitleManagement: false
            SyncPlayAccess: "CreateAndJoinGroups"
            RemoteClientBitrateLimit: 0
        server_config:
          enable: true
          ui_culture: "fr"
          metadata_country_code: "FR"
          preferred_metadata_language: "fr"
          activity_log_retention_days: 30
          log_file_retention_days: 3
          server_name: "jellyfin"
          plugin_repositories:
            - Name: "Jellyfin Stable"
              Url: "https://repo.jellyfin.org/files/plugin/manifest.json"
              Enabled: true
        plugins:
          enable: true
          required:
            - name: "TMDb"
            - name: "OMDb"
            - name: "MusicBrainz"
            - name: "AudioDb"
            - name: "Studio Images"
            - name: "Kodi Sync Queue"
    ```

    Step — Verify YAML parse via Python:
    ```bash
    cd /home/moi/projets/perso/arr-stack && python -c "
    import yaml
    with open('charts/arr-stack/files/arrconf.yml') as f:
        data = yaml.safe_load(f)
    assert 'jellyfin' in data
    assert 'main' in data['jellyfin']
    assert data['jellyfin']['main']['base_url'] == 'http://jellyfin.selfhost.svc.cluster.local:8096'
    assert len(data['jellyfin']['main']['libraries']['items']) == 2
    assert data['jellyfin']['main']['libraries']['items'][0]['name'] == 'Séries'
    assert data['jellyfin']['main']['libraries']['items'][0]['paths'] == ['/media/series', '/media/anime', '/media/family']
    assert len(data['jellyfin']['main']['plugins']['required']) == 6
    assert data['jellyfin']['main']['users']['prune'] is False
    assert data['jellyfin']['main']['libraries']['prune'] is False
    print('arrconf.yml jellyfin block OK')
    "
    ```

    Step — Verify the YAML round-trips through arrconf RootConfig:
    ```bash
    cd /home/moi/projets/perso/arr-stack && cd tools/arrconf && python -c "
    from arrconf.config import load_config
    cfg = load_config('../../charts/arr-stack/files/arrconf.yml')
    assert 'main' in cfg.jellyfin
    j = cfg.jellyfin['main']
    assert j.base_url == 'http://jellyfin.selfhost.svc.cluster.local:8096'
    assert len(j.libraries.items) == 2
    assert j.libraries.items[0].name == 'Séries'
    assert j.libraries.items[0].paths == ['/media/series', '/media/anime', '/media/family']
    assert j.users.admin.IsAdministrator is True
    assert j.server_config.ui_culture == 'fr'
    assert len(j.plugins.required) == 6
    print('RootConfig parse OK')
    "
    ```

    Both smoke commands must succeed before commit.
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && python -c "
import yaml
with open('charts/arr-stack/files/arrconf.yml') as f:
    data = yaml.safe_load(f)
assert 'jellyfin' in data and 'main' in data['jellyfin']
assert data['jellyfin']['main']['base_url'].startswith('http://jellyfin')
assert len(data['jellyfin']['main']['libraries']['items']) == 2
assert data['jellyfin']['main']['libraries']['items'][0]['paths'] == ['/media/series', '/media/anime', '/media/family']
assert data['jellyfin']['main']['libraries']['items'][1]['paths'] == ['/media/films', '/media/films-anime', '/media/films-family']
assert len(data['jellyfin']['main']['plugins']['required']) == 6
assert data['jellyfin']['main']['users']['prune'] is False
assert data['jellyfin']['main']['libraries']['prune'] is False
print('OK')
" && cd tools/arrconf && python -c "
from arrconf.config import load_config
cfg = load_config('../../charts/arr-stack/files/arrconf.yml')
assert 'main' in cfg.jellyfin
j = cfg.jellyfin['main']
assert j.libraries.items[0].name == 'Séries'
assert j.users.admin.IsAdministrator is True
print('OK')
"</automated>
  </verify>
  <acceptance_criteria>
    1. `charts/arr-stack/files/arrconf.yml` contains a top-level `jellyfin:` section.
    2. `jellyfin.main.base_url` starts with `http://jellyfin`.
    3. `jellyfin.main.libraries.items` has exactly 2 entries with names "Séries" and "Films".
    4. "Séries" paths = `["/media/series", "/media/anime", "/media/family"]` exactly.
    5. "Films" paths = `["/media/films", "/media/films-anime", "/media/films-family"]` exactly.
    6. `jellyfin.main.plugins.required` has 6 entries (TMDb, OMDb, MusicBrainz, AudioDb, Studio Images, Kodi Sync Queue).
    7. `jellyfin.main.users.prune == false` AND `jellyfin.main.libraries.prune == false`.
    8. `jellyfin.main.users.admin` does NOT contain `AuthenticationProviderId` or `PasswordResetProviderId` keys (Pitfall 6 — intentionally absent).
    9. `load_config(charts/arr-stack/files/arrconf.yml)` returns a RootConfig with `cfg.jellyfin["main"]` populated and pydantic-validated.
    10. The comment block above `jellyfin:` cites D-07-LIB-01, D-07-USERS-01, D-07-CONFIG-01, D-07-PLUGINS-01 AND Pitfalls 1, 2, 4, 5, 6, 7.
  </acceptance_criteria>
  <done>jellyfin.main block added to arrconf.yml. RootConfig parse green. Comment block cites all required decisions + Pitfalls.</done>
</task>

<task type="auto">
  <name>Task 5.2: Extend charts/arr-stack/values.yaml --apps list (D-07-CHART-ARGS-01)</name>
  <read_first>
    - charts/arr-stack/values.yaml (full context around line 458 — the arrconf CronJob args)
    - .planning/phases/06-reconciler-seerr/06-07-SUMMARY.md § "D-06-CHART-ARGS-01 — seerr missing from `--apps` CronJob argument (hotfix shipped)" — the exact failure mode Phase 7 must avoid repeating
    - .planning/phases/07-reconciler-jellyfin/07-PATTERNS.md § charts/arr-stack/values.yaml (lines 825-839) — 1-line edit specification
  </read_first>
  <files>charts/arr-stack/values.yaml</files>
  <action>
    1-line atomic edit. Use the Edit tool to replace ONE line in `charts/arr-stack/values.yaml` (currently at line 458).

    Find this exact existing line:
    ```yaml
                - "sonarr,radarr,prowlarr,qbittorrent,seerr"   # D-05-ARGS-01: Phase 5 adds qbittorrent ; Phase 6 adds seerr (D-06-SCOPE-01)
    ```

    Replace with:
    ```yaml
                - "sonarr,radarr,prowlarr,qbittorrent,seerr,jellyfin"   # D-05-ARGS-01: Phase 5 adds qbittorrent ; Phase 6 adds seerr (D-06-SCOPE-01) ; Phase 7 adds jellyfin (D-07-INSTANCE-01)
    ```

    Verify no other lines reference the --apps list (defense-in-depth — if a duplicate exists somewhere, edit BOTH or fail):
    ```bash
    grep -nE '\\-\\-apps|"sonarr.*radarr.*prowlarr"' charts/arr-stack/values.yaml
    ```
    Expected: 2 lines (the `- "--apps"` literal and the value line right after it — single instance of the value).

    Step — Run helm lint to confirm the chart still validates against values.schema.json:
    ```bash
    cd /home/moi/projets/perso/arr-stack && helm lint charts/arr-stack/
    ```

    Step — Render the chart and verify rendered manifests pass kubeconform (catches K8s schema violations from the args change — unlikely, but defense):
    ```bash
    # If examples/values-prod.yaml is the canonical input (per repo convention):
    helm template charts/arr-stack/ -f examples/values-prod.yaml > /tmp/p7-rendered.yaml 2>/dev/null
    kubeconform -summary /tmp/p7-rendered.yaml || echo "kubeconform not installed; helm lint sufficient as gate"
    ```
  </action>
  <verify>
    <automated>grep -qE '\-\-apps' charts/arr-stack/values.yaml && grep -qE '"sonarr,radarr,prowlarr,qbittorrent,seerr,jellyfin"' charts/arr-stack/values.yaml && ! grep -qE '"sonarr,radarr,prowlarr,qbittorrent,seerr"\s' charts/arr-stack/values.yaml && cd /home/moi/projets/perso/arr-stack && helm lint charts/arr-stack/ 2>&1 | grep -E "0 chart\(s\) failed|^1 chart"</automated>
  </verify>
  <acceptance_criteria>
    1. `grep -E '"sonarr,radarr,prowlarr,qbittorrent,seerr,jellyfin"' charts/arr-stack/values.yaml` returns at least 1 match.
    2. `grep -E '"sonarr,radarr,prowlarr,qbittorrent,seerr"[^,]' charts/arr-stack/values.yaml` returns 0 matches (the OLD line was fully replaced, no double).
    3. `helm lint charts/arr-stack/` exits 0.
    4. The edited line is the SINGLE source of truth for the `--apps` value (`grep -c '"sonarr,radarr,prowlarr,qbittorrent,seerr,jellyfin"'` returns exactly 1).
  </acceptance_criteria>
  <done>values.yaml --apps list extended with jellyfin. D-07-CHART-ARGS-01 satisfied. helm lint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5.3: Extend test_arrconf_yml_validates.py + (optional) values.schema.json arrconf args enum</name>
  <read_first>
    - tools/arrconf/tests/test_arrconf_yml_validates.py (full file — existing test pattern; if test_seerr_block exists, mirror it)
    - charts/arr-stack/values.schema.json (full file — search for arrconf.args / --apps enum if any; Phase 4 may or may not have one)
    - .planning/phases/07-reconciler-jellyfin/07-PATTERNS.md § charts/arr-stack/values.schema.json (lines 843-849) — guidance on whether a schema edit is needed
  </read_first>
  <behavior>
    Test 1 — `test_arrconf_yml_validates_jellyfin`:
    Calls `load_config("charts/arr-stack/files/arrconf.yml")` and asserts:
    - `cfg.jellyfin["main"].base_url == "http://jellyfin.selfhost.svc.cluster.local:8096"`
    - `cfg.jellyfin["main"].libraries.items[0].name == "Séries"` AND `len(.paths) == 3`
    - `cfg.jellyfin["main"].libraries.items[1].name == "Films"` AND `len(.paths) == 3`
    - `cfg.jellyfin["main"].users.admin.IsAdministrator is True`
    - `cfg.jellyfin["main"].users.prune is False` (D-07-USERS-01 hardcoded)
    - `cfg.jellyfin["main"].libraries.prune is False` (D-07-LIB-01 hardcoded)
    - `cfg.jellyfin["main"].server_config.ui_culture == "fr"` AND `.metadata_country_code == "FR"`
    - `len(cfg.jellyfin["main"].plugins.required) == 6` AND `cfg.jellyfin["main"].plugins.required[0].name == "TMDb"`

    Test 2 (defensive) — `test_arrconf_yml_no_provider_ids_in_jellyfin_users`:
    Open the YAML file as raw text; grep for `AuthenticationProviderId` and `PasswordResetProviderId` strings; assert NEITHER appears (Pitfall 6 — these MUST NOT be in YAML; reconciler re-injects from cluster GET).
  </behavior>
  <files>
    tools/arrconf/tests/test_arrconf_yml_validates.py
    charts/arr-stack/values.schema.json
  </files>
  <action>
    Step 1 — Inspect the existing `tools/arrconf/tests/test_arrconf_yml_validates.py` to determine if it has a `test_arrconf_yml_validates_seerr` analog. If yes, mirror its shape. If no, add a fresh test using the load_config pattern from `tests/test_config.py::test_root_config_accepts_seerr_block`.

    Step 2 — Append (or insert into) `tools/arrconf/tests/test_arrconf_yml_validates.py`:

    ```python
    from pathlib import Path

    REPO_ROOT = Path(__file__).resolve().parents[3]  # tools/arrconf/tests → repo root
    ARRCONF_YML = REPO_ROOT / "charts" / "arr-stack" / "files" / "arrconf.yml"


    def test_arrconf_yml_validates_jellyfin() -> None:
        """Live chart YAML parses against the Phase 7 pydantic schema (D-07-INSTANCE-01).

        Regression contract — prevents shipping a chart-side jellyfin block that
        does not match the arrconf RootConfig schema. Mirrors the Seerr
        test_arrconf_yml_validates pattern.
        """
        from arrconf.config import load_config

        cfg = load_config(ARRCONF_YML)
        assert "main" in cfg.jellyfin, "charts/arr-stack/files/arrconf.yml missing jellyfin.main section"

        j = cfg.jellyfin["main"]
        assert j.base_url == "http://jellyfin.selfhost.svc.cluster.local:8096"

        # Libraries (D-07-LIB-01: 2 entries, multi-path)
        assert len(j.libraries.items) == 2
        assert j.libraries.items[0].name == "Séries"
        assert j.libraries.items[0].collection_type == "tvshows"
        assert j.libraries.items[0].paths == ["/media/series", "/media/anime", "/media/family"]
        assert j.libraries.items[1].name == "Films"
        assert j.libraries.items[1].paths == ["/media/films", "/media/films-anime", "/media/films-family"]
        assert j.libraries.prune is False  # D-07-LIB-01 hardcoded

        # Users (D-07-USERS-01: admin only, emilie protection)
        assert j.users.admin.IsAdministrator is True
        assert j.users.prune is False  # D-07-USERS-01 hardcoded — emilie protection

        # Server config (D-07-CONFIG-01: 7-field allowlist)
        assert j.server_config.ui_culture == "fr"
        assert j.server_config.metadata_country_code == "FR"
        assert j.server_config.activity_log_retention_days == 30
        assert len(j.server_config.plugin_repositories) == 1
        assert j.server_config.plugin_repositories[0].Url == (
            "https://repo.jellyfin.org/files/plugin/manifest.json"
        )

        # Plugins (D-07-PLUGINS-01: 6 plugins, activation-only)
        assert len(j.plugins.required) == 6
        plugin_names = [p.name for p in j.plugins.required]
        assert "TMDb" in plugin_names
        assert "Kodi Sync Queue" in plugin_names


    def test_arrconf_yml_no_provider_ids_in_jellyfin_users() -> None:
        """Pitfall 6 / D-06-OPENAPI-01 carry-forward — defensive grep.

        AuthenticationProviderId + PasswordResetProviderId MUST be re-injected
        from cluster GET, NEVER from YAML. If a future operator edits the chart
        to add them, the reconciler would land them in the POST body and stomp
        the cluster auth provider configuration.
        """
        text = ARRCONF_YML.read_text()
        # Locate the jellyfin block (everything from `jellyfin:` to EOF or next top-level key).
        # Simple substring check — these 2 strings must NOT appear in the file at all.
        assert "AuthenticationProviderId" not in text, (
            "Pitfall 6: AuthenticationProviderId leaked into YAML — reconciler re-injects this from cluster GET; YAML must not specify it."
        )
        assert "PasswordResetProviderId" not in text, (
            "Pitfall 6: PasswordResetProviderId leaked into YAML — reconciler re-injects this from cluster GET; YAML must not specify it."
        )
    ```

    Step 3 — (Optional) Check `charts/arr-stack/values.schema.json` for an arrconf `--apps` enum. Per Phase 7 RESEARCH/PATTERNS analysis: Phase 4 schema already covers the arrconf chart alias-level fields (controllers/global/ingress) but does NOT validate the `args` array content. So this file is a NO-OP for Phase 7 (PATTERNS.md §847). If grep confirms no enum exists:
    ```bash
    grep -nE '"args".*"items"|"enum".*"sonarr"' charts/arr-stack/values.schema.json
    ```
    Expected: no match (Phase 4 schema does not enum-restrict args). No edit needed.

    If grep DOES find an args enum, extend it with `"jellyfin"` (alphabetical insertion). This is a fallback only.

    Step 4 — Run the new tests:
    ```bash
    cd tools/arrconf && pytest -x tests/test_arrconf_yml_validates.py
    ```

    Step 5 — Full gate (ruff + mypy + pytest):
    ```bash
    cd tools/arrconf && ruff check && mypy . && pytest -x --cov=arrconf --cov-fail-under=70
    ```

    Step 6 — Commit all 3 (or 4) files atomically:
    ```bash
    git add charts/arr-stack/files/arrconf.yml \
            charts/arr-stack/values.yaml \
            tools/arrconf/tests/test_arrconf_yml_validates.py
    # If values.schema.json edited:
    # git add charts/arr-stack/values.schema.json
    git commit -m "feat(chart): jellyfin.main section + --apps list + validation test (Phase 7 Plan 05)"
    ```
  </action>
  <verify>
    <automated>cd tools/arrconf && pytest -x tests/test_arrconf_yml_validates.py::test_arrconf_yml_validates_jellyfin tests/test_arrconf_yml_validates.py::test_arrconf_yml_no_provider_ids_in_jellyfin_users && cd /home/moi/projets/perso/arr-stack && helm lint charts/arr-stack/ 2>&1 | grep -qE "0 chart\(s\) failed|1 chart\(s\) linted"</automated>
  </verify>
  <acceptance_criteria>
    1. `tools/arrconf/tests/test_arrconf_yml_validates.py::test_arrconf_yml_validates_jellyfin` passes.
    2. `tools/arrconf/tests/test_arrconf_yml_validates.py::test_arrconf_yml_no_provider_ids_in_jellyfin_users` passes.
    3. `helm lint charts/arr-stack/` exits 0.
    4. `grep "AuthenticationProviderId" charts/arr-stack/files/arrconf.yml` returns 0 lines (Pitfall 6 defensive — Task 5.1 enforced absence, Task 5.3 grep gate locks it in).
    5. The Pitfall 6 grep gate test would FAIL if a future operator adds `AuthenticationProviderId:` to the YAML — CI prevents the leak.
    6. All chart files + test file committed in one atomic commit.
  </acceptance_criteria>
  <done>CI validation test for chart YAML green + helm lint green + Pitfall 6 grep gate active. Phase 7 code + chart wiring is shippable to GHCR/my-kluster (Plan 07-06 handles the cluster deploy).</done>
</task>

</tasks>

<verification>
Wave 3 (Plan 07-05) closure checks:

- [ ] `charts/arr-stack/files/arrconf.yml` has a `jellyfin:` block with `main.libraries / users / server_config / plugins`
- [ ] `charts/arr-stack/values.yaml` line 458 contains `"sonarr,radarr,prowlarr,qbittorrent,seerr,jellyfin"` exactly once (no remnant of the old 5-app line)
- [ ] `tools/arrconf/tests/test_arrconf_yml_validates.py` has 2 new tests (yml validates + no providerids grep gate)
- [ ] `helm lint charts/arr-stack/` exits 0
- [ ] `cd tools/arrconf && pytest -x` all green
- [ ] Files committed in 1 atomic commit
- [ ] D-07-CHART-ARGS-01 satisfied (Phase 6 D-06-CHART-ARGS-01 not repeated)
</verification>

<success_criteria>
1. Chart YAML wired to the new reconciler code (Plan 07-04).
2. CronJob `--apps` list includes `jellyfin` — cluster CronJob will dispatch the Jellyfin branch on next run.
3. CI regression contract installed (`test_arrconf_yml_validates_jellyfin`) — broken YAML cannot ship.
4. Pitfall 6 leak prevention codified in CI (`test_arrconf_yml_no_provider_ids_in_jellyfin_users`).
</success_criteria>

<output>
After completion, create `.planning/phases/07-reconciler-jellyfin/07-05-SUMMARY.md` documenting:
- Line count delta on arrconf.yml + values.yaml + test_arrconf_yml_validates.py
- Comment block citations (D-07-* + Pitfalls covered)
- Whether values.schema.json was edited or left as NO-OP
- helm lint output (single-line)
- Wave 4 readiness signal — Plan 07-06 has the full chart ready for cluster apply via auto-tag → image build → my-kluster bump chain
</output>
