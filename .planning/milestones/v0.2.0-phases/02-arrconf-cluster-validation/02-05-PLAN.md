---
phase: 02-arrconf-cluster-validation
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - my-kluster/charts/arrconf/values.yaml  # PR2 flips arrconfDryRun: true -> false
  - arr-stack/snapshots/post-phase2-pr2-2026-05-08/
  - arr-stack/snapshots/drift-test-2026-05-08/
  - arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr2-job-logs-2026-05-08.log
  - arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/drift-demo-2026-05-08.log
  - arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
autonomous: false
requirements:
  - REQ-drift-detection
must_haves:
  truths:
    - "PR2 opened in my-kluster: ONLY change is `arrconfDryRun: true -> false` in values.yaml (D-28 atomic flip)"
    - "After PR2 merge: ArgoCD self-heals the env change; next CronJob run writes to Sonarr (dry-run disabled)"
    - "Sonarr's tag table contains `arrconf-managed` after the first apply Job (D-02 carry-forward + REQ-managed-tag from Phase 1 echoed at cluster level)"
    - "Sonarr's qBittorrent download_client gains the arrconf-managed tag id (ROADMAP success criterion #4)"
    - "Re-snapshot `snapshots/post-phase2-pr2-<date>/` shows the managed tag present (D-30 #3)"
    - "Apply-mode logs contain EXACT event names from Phase 1 reconciler source (W-06): `managed_tag_created` (sonarr.py line 60 — APPLY-mode marker) AND `plan_action` (differ.py); dry-run markers `would_create_managed_tag` and `dry_run_skip` are ABSENT"
    - "Drift demo runbook executed: UI/API mutation of a download_client field -> forced cron Job -> JSON logs show `plan_action` with action=update + reconcile completes -> Sonarr field reverted to YAML-declared value (REQ-drift-detection + ROADMAP success criterion #5)"
    - "Drift demo logs persisted to `arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/drift-demo-<date>.log` (D-30 #4 evidence trail)"
    - "**REQUIRED** (W-01 fix — was Optional): re-snapshot `snapshots/drift-test-<date>/` for the audit trail (D-30 #4); this is the only forensic before/after pair for REQ-drift-detection"
    - "Phase 1 HUMAN-UAT item #3 (live round-trip) resolved by the PR2 apply observation"
    - "Drift demo dispositive check (W-04 fix): `[ \"$RESTORED_PRIORITY\" = \"$ORIGINAL_PRIORITY\" ]` — value-equality assertion on the actual Sonarr API priority field, not regex substring matching of log events"
  artifacts:
    - path: "arr-stack/snapshots/post-phase2-pr2-2026-05-08/sonarr/tag.json"
      provides: "Post-PR2 tag list — MUST include an entry for label `arrconf-managed`"
      contains: "arrconf-managed"
    - path: "arr-stack/snapshots/post-phase2-pr2-2026-05-08/sonarr/downloadclient.json"
      provides: "Post-PR2 download_client list — qBittorrent entry MUST have a non-empty tags array"
    - path: "arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr2-job-logs-2026-05-08.log"
      provides: "Captured kubectl logs from the first PR2 (apply mode) Job — proof of `managed_tag_created` event (W-06 verified)"
    - path: "arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/drift-demo-2026-05-08.log"
      provides: "Drift demo evidence — JSON log lines showing plan_action with action=update (REQ-drift-detection)"
    - path: "arr-stack/snapshots/drift-test-2026-05-08/"
      provides: "REQUIRED forensic snapshot for REQ-drift-detection (W-01 fix — bumped from Optional to Required)"
  key_links:
    - from: "Cluster CronJob `arrconf`"
      to: "Sonarr API /api/v3/tag"
      via: "POST during first apply run (creates arrconf-managed tag — sonarr.py _ensure_managed_tag line 58)"
      pattern: "POST.*api/v3/tag"
    - from: "Cluster CronJob `arrconf`"
      to: "Sonarr API /api/v3/downloadclient/{id}"
      via: "PUT to revert UI-induced drift (sonarr.py _execute UPDATE branch line 100)"
      pattern: "PUT.*api/v3/downloadclient"
    - from: "evidence/drift-demo-<date>.log"
      to: "REQ-drift-detection acceptance"
      via: "JSON log lines with event=plan_action AND value-equality assertion `RESTORED_PRIORITY == ORIGINAL_PRIORITY` (W-04 dispositive)"
      pattern: "plan_action"
---

<objective>
Wave 4: PR2 apply mode + drift demo + final snapshots. (W-NEW-02 fix: wave bumped 3 -> 4 because upstream waves 02-03 and 02-04 each bumped by one to match their depends_on edges. Logically this is still the "PR2 apply + drift demo" value stream.) This plan opens **PR2** in my-kluster (a one-line change: `arrconfDryRun: true -> false`), observes that arrconf actually writes the `arrconf-managed` tag to Sonarr (ROADMAP #4), then runs the drift detection runbook (induce a UI mutation -> force cron Job -> verify rollback in logs and Sonarr — ROADMAP #5 + REQ-drift-detection). Final snapshots and evidence files are committed in arr-stack.

This plan addresses checker findings:
- **B-03** (Task 3.1 conflated 4 deliverables into one verify): split into 5.1a (PR2 open/merge — checkpoint, dispositive=ARRCONF_DRY_RUN==false), 5.1b (smoke-job + apply-mode log capture — autonomous, dispositive=W-06 event names), 5.1c (post-PR2 snapshot + Sonarr API tag verification — autonomous, dispositive=tag.json contains arrconf-managed AND `curl ... | jq '.tags | length > 0'`). Each gets its own automated verify block.
- **W-01** (drift-test snapshot was Optional): bumped to REQUIRED — it is the only forensic before/after pair for REQ-drift-detection. Updated must_haves + Task 5.2 acceptance.
- **W-04** (drift demo regex matched substring): added value-equality assertion `[ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ]` as the dispositive check after the kubectl logs grep.
- **W-06** (event names inferred): replaces inferred regex with EXACT apply-mode event names captured from sonarr.py + differ.py: `managed_tag_created`, `plan_action`. Dry-run markers (`would_create_managed_tag`, `dry_run_skip`) MUST be ABSENT in apply-mode logs.

Purpose: Close the Phase 2 success criteria. The dry-run guarantee is already proven in Wave 3 (Plan 02-04); Wave 4 (this plan) proves the **apply** path works AND that the **reconcile loop closes drift**.

Output: PR2 merged; arrconf-managed tag present in Sonarr; drift demo evidence captured (with REQUIRED snapshot pair); Phase 2 ROADMAP success criteria #4 and #5 SATISFIED; Phase 1 HUMAN-UAT #3 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-VALIDATION.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-04-PLAN.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-04-SUMMARY.md
@/home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/reconcilers/sonarr.py
@/home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py

<interfaces>
<!-- Cluster-side interfaces this plan exercises. The drift demo is a runbook, not new code. -->

# PR2 atomic change in my-kluster/charts/arrconf/values.yaml:
# - arrconfDryRun: true
# + arrconfDryRun: false
# (Nothing else changes. Atomic revert via `git revert <PR2-sha>`.)

# === Apply-mode log signature (W-06 — exact event names from sonarr.py + differ.py) ===
# DISPOSITIVE for apply mode (these MUST appear in PR2 logs):
#   - "managed_tag_created"  (sonarr.py line 60 — emitted ONLY when dry_run=false AND tag missing)
#     OR "managed_tag_found" (sonarr.py line 53 — if tag was created in a prior aborted run)
#   - "plan_action"          (differ.py — at least one with action="add" or "update", not all "no-op")
# DISPOSITIVE for dry-run-disabled (these MUST NOT appear in PR2 logs):
#   - "would_create_managed_tag"  (only emitted when dry_run=true)
#   - "dry_run_skip"              (only emitted when dry_run=true and action != NO_OP)

# Forced apply Job (after PR2 merge):
# kubectl create job --from=cronjob/arrconf arrconf-pr2-smoke-$(date +%s) -n selfhost

# === Drift demo invocation pattern (RESEARCH.md Pattern 4) ===
# 1. kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
# 2. curl -s http://localhost:8989/api/v3/downloadclient/1 -H "X-Api-Key: $SONARR_API_KEY" > /tmp/dc-before.json
# 3. ORIGINAL_PRIORITY=$(jq -r '.priority' /tmp/dc-before.json)   # capture for W-04 dispositive check
# 4. jq '.priority = 5' /tmp/dc-before.json > /tmp/dc-drifted.json
# 5. curl -X PUT ... -d @/tmp/dc-drifted.json
# 6. kubectl create job --from=cronjob/arrconf arrconf-drift-demo-$(date +%s) -n selfhost
# 7. kubectl logs job/arrconf-drift-demo-... -n selfhost > evidence/drift-demo-<date>.log
# 8. RESTORED_PRIORITY=$(curl -s ... | jq -r '.priority')
# 9. [ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ]   # W-04 fix — DISPOSITIVE check, not regex substring
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5.1a: Open PR2 (atomic flip arrconfDryRun: false) + verify ArgoCD self-heal (B-03 split 1/3)</name>
  <files>
    my-kluster/charts/arrconf/values.yaml
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-04-SUMMARY.md (Wave 3 outcome — confirm zero writes happened)
    - /home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml (verify currently `arrconfDryRun: true`)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md "D-28 PR2"
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md "Pattern 3" (T+7 -> T+9 sequence)
  </read_first>
  <what-built>
    Wave 3 deployed the chart in dry-run mode and proved zero writes. This is the FIRST of three split tasks (B-03 fix): opens the atomic flip PR, waits for operator merge, verifies ArgoCD self-heals the env value into the live CronJob. Tasks 5.1b + 5.1c then run the smoke job + capture logs + verify Sonarr API state — each with its own automated verify gate.
  </what-built>
  <action>
    Step 1 — Open PR2 in my-kluster with the atomic flip:
        cd /home/moi/projets/perso/my-kluster
        git checkout main && git pull origin main
        git checkout -b phase-2-arrconf-pr2

        # Atomic edit: change ONLY this single line.
        sed -i 's/^arrconfDryRun: true$/arrconfDryRun: false/' charts/arrconf/values.yaml

        git diff --stat charts/arrconf/values.yaml
        # Expected: 1 file changed, 1 insertion(+), 1 deletion(-)

    Step 2 — Commit + push + open the PR:
        git add charts/arrconf/values.yaml
        git commit -m "feat(arrconf): Phase 2 PR2 — flip arrconfDryRun: true -> false (D-28)"
        git push -u origin phase-2-arrconf-pr2

        gh pr create \
          --repo tom333/my-kluster \
          --title "feat(arrconf): Phase 2 PR2 — flip arrconfDryRun: false (apply mode)" \
          --body "Atomic single-line change. Wave 3 proved zero writes during dry-run. See arr-stack 02-CONTEXT.md D-28."

    Step 3 — Wait for operator to merge. After merge, sync ArgoCD:
        argocd app sync arrconf
        argocd app wait arrconf --health --timeout 180

    Step 4 — Verify the env value flipped in the live CronJob (DISPOSITIVE for this task per B-03):
        DRY_RUN_VAL=$(kubectl get cronjob arrconf -n selfhost \
          -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].env}' \
          | jq -r '[.[] | select(.name=="ARRCONF_DRY_RUN")][0].value')
        echo "ARRCONF_DRY_RUN value: $DRY_RUN_VAL (expected: false)"
        [ "$DRY_RUN_VAL" = "false" ] || { echo "ERROR: ArgoCD did not self-heal the env value"; exit 1; }
  </action>
  <how-to-verify>
    1. PR2 diff is 1 line: `git -C /home/moi/projets/perso/my-kluster diff main..phase-2-arrconf-pr2 --stat charts/arrconf/values.yaml` shows exactly 1 file, 1 insertion, 1 deletion
    2. PR2 merged: `gh pr view <pr-number> --repo tom333/my-kluster --json state --jq .state` returns `MERGED`
    3. **B-03 dispositive**: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].env}' | jq '[.[] | select(.name=="ARRCONF_DRY_RUN")][0].value == "false"'` returns `true`
  </how-to-verify>
  <acceptance_criteria>
    - my-kluster PR2 merged with exactly 1 line changed (`arrconfDryRun: true -> false`)
    - **B-03 dispositive single-acceptance**: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].env}' | jq '[.[] | select(.name=="ARRCONF_DRY_RUN")][0].value == "false"'` returns `true`
  </acceptance_criteria>
  <verify>
    <automated>kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].env}' | jq -e '[.[] | select(.name=="ARRCONF_DRY_RUN")][0].value == "false"'</automated>
  </verify>
  <resume-signal>Type "approved: PR2 merged, ArgoCD self-healed, ARRCONF_DRY_RUN=false in CronJob" OR describe what failed (PR conflict, ArgoCD did not self-heal within 3 min, env still shows true).</resume-signal>
  <done>PR2 merged atomically; ArgoCD self-healed the env change; live CronJob has ARRCONF_DRY_RUN=false; ready for Task 5.1b smoke job.</done>
</task>

<task type="auto">
  <name>Task 5.1b: Force apply-mode smoke Job + capture logs with verified event names (B-03 split 2/3, W-06)</name>
  <files>
    arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr2-job-logs-2026-05-08.log
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/reconcilers/sonarr.py (W-06 — verified apply-mode events: `managed_tag_created`, `managed_tag_found`)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py (W-06 — verified event: `plan_action`)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md "Pattern 4" + "Pitfall 4" + "Pitfall 10"
  </read_first>
  <action>
    Step 1 — Force a smoke job (apply mode):
        SMOKE_JOB="arrconf-pr2-smoke-$(date +%s)"
        kubectl create job --from=cronjob/arrconf "$SMOKE_JOB" -n selfhost
        kubectl wait --for=condition=complete "job/$SMOKE_JOB" -n selfhost --timeout=180s

        EVIDENCE="/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr2-job-logs-$(date +%F).log"
        kubectl logs "job/$SMOKE_JOB" -n selfhost > "$EVIDENCE"

    Step 2 — Verify apply-mode signal using EXACT event names from sonarr.py + differ.py (W-06):
        # DISPOSITIVE for apply mode: `managed_tag_created` event (sonarr.py line 60).
        # If tag already existed, `managed_tag_found` is acceptable instead.
        TAG_CREATED=$(jq -c 'select(.event == "managed_tag_created")' "$EVIDENCE" | wc -l)
        TAG_FOUND=$(jq -c 'select(.event == "managed_tag_found")' "$EVIDENCE" | wc -l)
        echo "managed_tag_created events: $TAG_CREATED ; managed_tag_found events: $TAG_FOUND"
        if [ "$TAG_CREATED" -lt 1 ] && [ "$TAG_FOUND" -lt 1 ]; then
          echo "ERROR: neither managed_tag_created nor managed_tag_found event present"
          exit 1
        fi

        # DISPOSITIVE for plan execution: at least one plan_action event (differ.py)
        PLAN_EVENTS=$(jq -c 'select(.event == "plan_action")' "$EVIDENCE" | wc -l)
        echo "plan_action events: $PLAN_EVENTS (expected >= 1)"
        [ "$PLAN_EVENTS" -ge 1 ] || { echo "ERROR: no plan_action events"; exit 1; }

        # DISPOSITIVE that dry-run path is NOT taken: would_create_managed_tag and dry_run_skip MUST be absent.
        DRY_RUN_MARKERS=$(jq -c 'select(.event == "would_create_managed_tag" or .event == "dry_run_skip")' "$EVIDENCE" | wc -l)
        echo "Dry-run markers (must be 0): $DRY_RUN_MARKERS"
        [ "$DRY_RUN_MARKERS" -eq 0 ] || { echo "CRITICAL: dry-run path event found in apply-mode logs — env flip didn't take effect"; exit 1; }

    Step 3 — Cleanup:
        kubectl delete "job/$SMOKE_JOB" -n selfhost --ignore-not-found

    Step 4 — Commit evidence in arr-stack:
        cd /home/moi/projets/perso/arr-stack
        git add .planning/phases/02-arrconf-cluster-validation/evidence/pr2-job-logs-*.log
        git commit -m "evidence(02): capture PR2 apply-mode cron Job logs (W-06 verified events)"
  </action>
  <acceptance_criteria>
    - `find /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/ -name 'pr2-job-logs-*.log' -size +0c` returns at least 1 file
    - `head -1 .../evidence/pr2-job-logs-*.log | jq -e . > /dev/null` exits 0 (valid JSON Lines)
    - **W-06 dispositive (apply marker)**: `jq -c 'select(.event == "managed_tag_created" or .event == "managed_tag_found")' .../evidence/pr2-job-logs-*.log | wc -l` >= 1
    - **W-06 dispositive (plan)**: `jq -c 'select(.event == "plan_action")' .../evidence/pr2-job-logs-*.log | wc -l` >= 1
    - **W-06 dispositive (dry-run absent)**: `jq -c 'select(.event == "would_create_managed_tag" or .event == "dry_run_skip")' .../evidence/pr2-job-logs-*.log | wc -l` == 0
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s | grep -q 'evidence(02):'` exits 0
  </acceptance_criteria>
  <verify>
    <automated>EVIDENCE=$(ls -t /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/pr2-job-logs-*.log 2&gt;/dev/null | head -1) &amp;&amp; test -n "$EVIDENCE" &amp;&amp; head -1 "$EVIDENCE" | jq -e . &gt; /dev/null &amp;&amp; [ "$(jq -c 'select(.event == "managed_tag_created" or .event == "managed_tag_found")' "$EVIDENCE" | wc -l)" -ge 1 ] &amp;&amp; [ "$(jq -c 'select(.event == "plan_action")' "$EVIDENCE" | wc -l)" -ge 1 ] &amp;&amp; [ "$(jq -c 'select(.event == "would_create_managed_tag" or .event == "dry_run_skip")' "$EVIDENCE" | wc -l)" = "0" ]</automated>
  </verify>
  <done>Smoke job ran in apply mode; logs captured to pr2-job-logs-<date>.log; W-06 verified events present (managed_tag_created/found + plan_action) and dry-run markers absent; evidence committed in arr-stack.</done>
</task>

<task type="auto">
  <name>Task 5.1c: Re-snapshot post-PR2 + Sonarr API tag verification (B-03 split 3/3)</name>
  <files>
    arr-stack/snapshots/post-phase2-pr2-2026-05-08/
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
    - /home/moi/projets/perso/arr-stack/snapshots/post-phase2-pr1-2026-05-08/sonarr/tag.json (Wave 3 baseline — empty `[]` expected; will gain `arrconf-managed` here)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md "D-30 #3"
  </read_first>
  <action>
    Step 1 — Re-snapshot post-PR2 (D-30 #3):
        cd /home/moi/projets/perso/arr-stack
        # Re-establish port-forwards + env vars as in Plan 02-01 Task 1.1 if needed.
        SNAPSHOT_DIR="snapshots/post-phase2-pr2-$(date +%F)"
        tools/snapshot/snapshot.sh --output "$SNAPSHOT_DIR/"

    Step 2 — Verify the cluster effect via BOTH file existence AND Sonarr API (B-03 dispositive — both checks required):
        # Verify the snapshot tag.json includes arrconf-managed (FILE-existence proof):
        jq -e 'map(.label) | any(. == "arrconf-managed")' "$SNAPSHOT_DIR/sonarr/tag.json"

        # Port-forward + Sonarr API verification (RUNTIME proof):
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        PF_PID=$!
        sleep 2

        SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"

        # qBittorrent download_client must have a non-empty tags array:
        TAGS_LEN=$(curl -s "http://localhost:8989/api/v3/downloadclient" -H "X-Api-Key: $SONARR_API_KEY" | jq '.[0].tags | length')
        echo "qBit download_client tags length: $TAGS_LEN (expected > 0)"
        [ "$TAGS_LEN" -gt 0 ] || { echo "ERROR: download_client has no tags — apply mode did not run"; kill $PF_PID 2>/dev/null; exit 1; }

        # Tag list MUST contain arrconf-managed:
        curl -s "http://localhost:8989/api/v3/tag" -H "X-Api-Key: $SONARR_API_KEY" \
          | jq -e 'map(.label) | any(. == "arrconf-managed")'

        kill $PF_PID 2>/dev/null || true

    Step 3 — Commit snapshot in arr-stack:
        git add snapshots/post-phase2-pr2-*/
        git commit -m "snapshot(02): post-phase2-pr2 apply-mode capture (D-30 #3) — arrconf-managed tag verified"
  </action>
  <acceptance_criteria>
    - **B-03 dispositive (file existence)**: `test -f /home/moi/projets/perso/arr-stack/snapshots/post-phase2-pr2-2026-05-08/sonarr/tag.json` exits 0
    - **B-03 dispositive (snapshot content)**: `jq -e 'map(.label) | any(. == "arrconf-managed")' /home/moi/projets/perso/arr-stack/snapshots/post-phase2-pr2-2026-05-08/sonarr/tag.json` exits 0
    - **B-03 dispositive (runtime API)**: `curl -s "http://localhost:8989/api/v3/downloadclient" -H "X-Api-Key: $SONARR_API_KEY" | jq -e '.[0].tags | length > 0'` exits 0 (during execution; result verified via snapshot file too)
    - `find /home/moi/projets/perso/arr-stack/snapshots/post-phase2-pr2-2026-05-08/sonarr -type f -name '*.json' | wc -l` >= 5
    - `test -f .../snapshots/post-phase2-pr2-2026-05-08/sonarr/downloadclient.json` exits 0 with `.[0].tags | length > 0`
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s | grep -q 'snapshot(02):.*pr2'` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack &amp;&amp; test -f snapshots/post-phase2-pr2-2026-05-08/sonarr/tag.json &amp;&amp; jq -e 'map(.label) | any(. == "arrconf-managed")' snapshots/post-phase2-pr2-2026-05-08/sonarr/tag.json &gt; /dev/null &amp;&amp; jq -e '.[0].tags | length &gt; 0' snapshots/post-phase2-pr2-2026-05-08/sonarr/downloadclient.json &gt; /dev/null</automated>
  </verify>
  <done>post-phase2-pr2 snapshot committed; tag.json contains arrconf-managed (FILE proof); downloadclient.json shows non-empty tags (FILE proof corroborates API call); ROADMAP success criterion #4 SATISFIED.</done>
</task>

<task type="auto">
  <name>Task 5.2: Drift demo runbook — induce mutation, force reconcile, capture evidence (REQ-drift-detection; W-01 + W-04)</name>
  <files>
    arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/drift-demo-2026-05-08.log
    arr-stack/snapshots/drift-test-2026-05-08/  # REQUIRED per W-01 fix (was Optional)
    arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-RESEARCH.md "Pattern 4: Drift detection demo" + "Code Example 4" + "Pitfall 4" + "Pitfall 10"
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-PATTERNS.md §13 ("evidence/drift-demo-<date>.log")
    - /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-CONTEXT.md "D-30 #4"
    - /home/moi/projets/perso/arr-stack/.planning/REQUIREMENTS.md REQ-drift-detection
    - /home/moi/projets/perso/my-kluster/charts/arrconf/files/arrconf.yml (declared download_client priority — what arrconf will revert to)
    - /home/moi/projets/perso/arr-stack/tools/arrconf/arrconf/differ.py (W-06 — verified `plan_action` event with action="update")
  </read_first>
  <action>
    Step 1 — Establish access:
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        PF_PID=$!
        sleep 2
        SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"

    Step 2 — Capture pre-drift state (CAPTURE ORIGINAL_PRIORITY for W-04 dispositive check):
        TMPDIR=$(mktemp -d)
        DC_ID=$(curl -s http://localhost:8989/api/v3/downloadclient -H "X-Api-Key: $SONARR_API_KEY" | jq '.[0].id')
        curl -s "http://localhost:8989/api/v3/downloadclient/$DC_ID" -H "X-Api-Key: $SONARR_API_KEY" \
          | jq '.' > "$TMPDIR/dc-before.json"

        ORIGINAL_PRIORITY=$(jq -r '.priority' "$TMPDIR/dc-before.json")
        echo "Original priority: $ORIGINAL_PRIORITY"   # likely 1; persist this for the W-04 dispositive comparison

    Step 3 — Induce drift via curl PUT (priority is safe per Assumption A6):
        DRIFTED_PRIORITY=5
        if [ "$ORIGINAL_PRIORITY" = "5" ]; then DRIFTED_PRIORITY=10; fi   # avoid no-op

        jq --argjson dp "$DRIFTED_PRIORITY" '.priority = $dp' "$TMPDIR/dc-before.json" > "$TMPDIR/dc-drifted.json"

        curl -s -X PUT "http://localhost:8989/api/v3/downloadclient/$DC_ID" \
          -H "X-Api-Key: $SONARR_API_KEY" \
          -H "Content-Type: application/json" \
          -d @"$TMPDIR/dc-drifted.json" \
          -o /dev/null -w "HTTP %{http_code}\n"

        # Confirm drift landed in Sonarr:
        DRIFTED_OBSERVED=$(curl -s "http://localhost:8989/api/v3/downloadclient/$DC_ID" -H "X-Api-Key: $SONARR_API_KEY" | jq '.priority')
        echo "Drifted priority observed in Sonarr: $DRIFTED_OBSERVED"
        if [ "$DRIFTED_OBSERVED" != "$DRIFTED_PRIORITY" ]; then
          echo "ERROR: drift did not land — investigate"
          exit 1
        fi

    Step 4 — Force a cron Job to trigger reconcile:
        DRIFT_JOB="arrconf-drift-demo-$(date +%s)"
        kubectl create job --from=cronjob/arrconf "$DRIFT_JOB" -n selfhost
        kubectl wait --for=condition=complete "job/$DRIFT_JOB" -n selfhost --timeout=180s

        EVIDENCE="/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/drift-demo-$(date +%F).log"
        kubectl logs "job/$DRIFT_JOB" -n selfhost > "$EVIDENCE"

    Step 5 — Verify the logs show drift detection (W-06 verified event names):
        # plan_action with action="update" is the dispositive event signature for drift correction:
        UPDATE_PLANS=$(jq -c 'select(.event == "plan_action" and .action == "update")' "$EVIDENCE" | wc -l)
        echo "plan_action update events: $UPDATE_PLANS (expected >= 1)"
        if [ "$UPDATE_PLANS" -lt 1 ] ; then
          echo "ERROR: no plan_action update event in drift-demo logs; inspect $EVIDENCE manually"
          exit 1
        fi

    Step 6 — W-04 DISPOSITIVE CHECK: Sonarr value reverted to ORIGINAL_PRIORITY:
        RESTORED_PRIORITY=$(curl -s "http://localhost:8989/api/v3/downloadclient/$DC_ID" -H "X-Api-Key: $SONARR_API_KEY" | jq -r '.priority')
        echo "Post-reconcile priority observed in Sonarr: $RESTORED_PRIORITY"
        # W-04 fix: dispositive value-equality assertion — not regex substring matching.
        if [ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ]; then
          echo "REQ-drift-detection SATISFIED — Sonarr value reverted from $DRIFTED_PRIORITY back to $ORIGINAL_PRIORITY"
        else
          echo "ERROR: drift was NOT corrected — REQ-drift-detection FAILS"
          echo "  Original: $ORIGINAL_PRIORITY"
          echo "  Drifted:  $DRIFTED_PRIORITY"
          echo "  Restored: $RESTORED_PRIORITY"
          exit 1
        fi

        # Persist ORIGINAL_PRIORITY + RESTORED_PRIORITY in evidence for audit:
        cat >> "$EVIDENCE" <<EOF

# === DRIFT-DEMO DISPOSITIVE COMPARISON (W-04 fix) ===
# original_priority: $ORIGINAL_PRIORITY
# drifted_priority:  $DRIFTED_PRIORITY
# restored_priority: $RESTORED_PRIORITY
# match (restored == original): YES
EOF

    Step 7 — REQUIRED drift-test snapshot (D-30 #4, W-01 fix — bumped from Optional):
        cd /home/moi/projets/perso/arr-stack
        DRIFT_SNAPSHOT="snapshots/drift-test-$(date +%F)"
        tools/snapshot/snapshot.sh --output "$DRIFT_SNAPSHOT/"

        # Verify the drift-test snapshot has at least sonarr/ populated:
        find "$DRIFT_SNAPSHOT/sonarr" -type f -name '*.json' | head -1 | grep -q . || {
          echo "ERROR: drift-test snapshot missing sonarr/ JSON files"; exit 1;
        }

    Step 8 — Cleanup:
        kubectl delete "job/$DRIFT_JOB" -n selfhost --ignore-not-found
        kill $PF_PID 2>/dev/null || true
        rm -rf "$TMPDIR"

    Step 9 — Mark Phase 1 HUMAN-UAT item #3 resolved:
        # Edit /home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
        # Change item #3 result from `[pending]` to:
        #   [passed: live round-trip via PR2 apply Job + drift demo on 2026-05-08; arrconf reconciled real Sonarr instance and corrected UI-induced drift; restored=$ORIGINAL_PRIORITY (W-04 dispositive); evidence in .planning/phases/02-arrconf-cluster-validation/evidence/]

    Step 10 — Commit drift demo evidence + drift-test snapshot + UAT update:
        cd /home/moi/projets/perso/arr-stack
        git add .planning/phases/02-arrconf-cluster-validation/evidence/drift-demo-*.log \
                snapshots/drift-test-*/ \
                .planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md
        git commit -m "evidence(02): drift demo runbook — REQ-drift-detection SATISFIED (W-01 snapshot, W-04 dispositive)"
  </action>
  <acceptance_criteria>
    - Evidence file `drift-demo-<date>.log` exists, non-empty, valid JSON Lines (`head -1 ... | jq -e .` exits 0)
    - **W-06 dispositive**: `jq -c 'select(.event == "plan_action" and .action == "update")' .../evidence/drift-demo-*.log | wc -l` >= 1
    - **W-04 DISPOSITIVE (post-execution)**: After Step 6, `[ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ]` exits 0 — Sonarr's download_client[0].priority returned to the original value (queried via curl after the Job)
    - **W-01 REQUIRED**: `test -d /home/moi/projets/perso/arr-stack/snapshots/drift-test-2026-05-08` exits 0
    - **W-01 REQUIRED**: `find /home/moi/projets/perso/arr-stack/snapshots/drift-test-2026-05-08/sonarr -type f -name '*.json' | wc -l` >= 1
    - Phase 1 HUMAN-UAT.md item #3 marked `[passed: ...]`
    - All evidence + UAT update committed in arr-stack git history
  </acceptance_criteria>
  <verify>
    <automated>EVIDENCE=$(ls -t /home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/evidence/drift-demo-*.log 2&gt;/dev/null | head -1) &amp;&amp; test -n "$EVIDENCE" &amp;&amp; head -1 "$EVIDENCE" | jq -e . &gt; /dev/null &amp;&amp; [ "$(jq -c 'select(.event == "plan_action" and .action == "update")' "$EVIDENCE" | wc -l)" -ge 1 ] &amp;&amp; test -d /home/moi/projets/perso/arr-stack/snapshots/drift-test-2026-05-08 &amp;&amp; [ "$(find /home/moi/projets/perso/arr-stack/snapshots/drift-test-2026-05-08/sonarr -type f -name '*.json' 2&gt;/dev/null | wc -l)" -ge 1 ] &amp;&amp; grep -E '^### 3\.|^## 3\. Live' /home/moi/projets/perso/arr-stack/.planning/phases/01-arrconf-poc-json-schema/01-HUMAN-UAT.md -A 5 | grep -q 'passed' &amp;&amp; grep -q 'match (restored == original): YES' "$EVIDENCE"</automated>
  </verify>
  <done>Drift induced via API mutation; reconcile Job ran; logs captured to evidence/ with W-06 verified `plan_action`+`action=update` event; **Sonarr field restored to YAML value (W-04 dispositive `RESTORED_PRIORITY == ORIGINAL_PRIORITY`)**; drift-test snapshot REQUIRED+committed (W-01); Phase 1 HUMAN-UAT #3 closed; ROADMAP success criterion #5 + REQ-drift-detection SATISFIED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| my-kluster repo -> ArgoCD -> cluster | PR2 merge causes ArgoCD to self-heal env value into the live CronJob spec |
| Cluster CronJob `arrconf` -> Sonarr API | Now performs writes (POST tag, PUT downloadclient); the `arrconf-managed` tag is the audit boundary marking arrconf-owned resources |
| Operator workstation -> Sonarr (curl PUT for drift induction) | Trusted local action with API key from secret; bounded to a safe field (`priority`) per Assumption A6 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering (T-drift-pr1-pr2) | The PR2 flip itself (atomic single-line change) | mitigate | Task 5.1a Step 1 — git diff verification (exactly 1 line changed); `git revert` is one command. |
| T-02-17 | Confused Deputy (T-scope-bypass) | First apply Job creates arrconf-managed tag + tags qBit DC | mitigate | Phase 1 D-12 ScopeViolationError already protects forbidden endpoints; the only writes Phase 2 plans for are Sonarr tag CREATE + downloadclient UPDATE (tag-id append). Task 5.1c snapshot diff confirms no other resources changed. |
| T-02-18 | Repudiation (audit trail) | Drift demo evidence | mitigate | Task 5.2 saves `kubectl logs` to evidence/ + appends a structured DISPOSITIVE COMPARISON block (W-04) recording original/drifted/restored priorities; commits in arr-stack as a permanent audit record. |
| T-02-19 | Tampering | UI-induced drift on `priority` field used as test stimulus | accept | Assumption A6 — `priority` has no functional impact with a single download_client; arrconf reverts within one cron cycle. |
| T-02-20 | Information Disclosure (T-secret-leak) | API key transit during drift demo curl commands | mitigate | API key is read from the cluster Secret via base64 decode and passed only via header `-H "X-Api-Key: ..."`; never logged, never echoed. The cleanup step removes the temp dir. |
| T-02-21 | Information Disclosure (T-secret-leak) | Evidence file content | mitigate | Phase 1 SecretStr masking ensures structlog never emits API key values; the JSON log lines contain event names + diff fields + ids, never credentials. |
| T-02-22 | Tampering (T-image-supply-chain) | First write actually originates from the verified image | accept | GHCR public anonymous pull is ADR-3 acceptance. cosign signing = OPSV2-03. |
| T-02-23 | Confused Deputy (W-04 — false-positive drift detection) | Drift demo regex matched substring `update` could match benign events | mitigate | W-04 fix: Step 6 of Task 5.2 adds dispositive `[ "$RESTORED_PRIORITY" = "$ORIGINAL_PRIORITY" ]` value-equality check — not regex substring matching. The check queries Sonarr API directly for the actual field value; only true reconcile success passes. |
| T-02-24 | Repudiation (W-01 — missing forensic snapshot) | Drift demo without before/after snapshot pair | mitigate | W-01 fix: drift-test snapshot is now REQUIRED (was Optional). Task 5.2 Step 7 acceptance criteria explicitly assert the directory exists with sonarr/ JSON files. Diff against pre-drift snapshot is the documented forensic trail for REQ-drift-detection. |
</threat_model>

<verification>
End-of-plan gate (Phase 2 complete when all pass):
1. PR2 merged in my-kluster; live CronJob env `ARRCONF_DRY_RUN=false` (Task 5.1a)
2. First apply Job ran with W-06 verified events (`managed_tag_created` OR `managed_tag_found` + `plan_action`); dry-run markers ABSENT (Task 5.1b)
3. `snapshots/post-phase2-pr2-<date>/` committed; tag.json has `arrconf-managed`; downloadclient.json shows non-empty tags (Task 5.1c)
4. Evidence file `pr2-job-logs-<date>.log` committed (Task 5.1b)
5. Drift demo executed; evidence file `drift-demo-<date>.log` has W-06 `plan_action+action=update` event AND DISPOSITIVE COMPARISON block (W-04) showing `match (restored == original): YES` (Task 5.2)
6. **W-04 DISPOSITIVE**: `RESTORED_PRIORITY == ORIGINAL_PRIORITY` confirmed via curl after the Job (Task 5.2 Step 6)
7. **W-01 REQUIRED**: `snapshots/drift-test-<date>/` committed with sonarr/ JSON files (Task 5.2)
8. Phase 1 HUMAN-UAT #3 marked passed (Task 5.2)
</verification>

<success_criteria>
- ROADMAP success criterion #4 (Sonarr UI shows arrconf-managed download_client after apply mode) — SATISFIED by Task 5.1c (verified via API + snapshot)
- ROADMAP success criterion #5 (Drift detection: UI mod hors-Git overwritten next run; JSON logs visible) — SATISFIED by Task 5.2
- REQ-drift-detection — SATISFIED by Task 5.2 (mutation -> reconcile -> revert -> JSON logs as evidence + W-04 dispositive value-equality + W-01 forensic snapshot)
- D-28 (Two-PR protocol — PR2 atomic flip) — SATISFIED by Task 5.1a
- D-30 #3 (post-phase2-pr2-<date>/ committed) — SATISFIED by Task 5.1c
- D-30 #4 (drift-test-<date>/ committed) — REQUIRED + SATISFIED (W-01 fix)
- Phase 1 HUMAN-UAT #3 (live round-trip) — CLOSED
- D-02 carry-forward (arrconf-managed tag is the audit boundary) — SATISFIED + observed in cluster
- B-03 fix (Task 3.1 split into 3 atomic verifies) — SATISFIED
- W-01 fix (drift-test snapshot REQUIRED) — SATISFIED
- W-04 fix (dispositive value-equality check, not regex substring) — SATISFIED
- W-06 fix (event names verified against reconciler source) — SATISFIED
</success_criteria>

<output>
After completion, create `/home/moi/projets/perso/arr-stack/.planning/phases/02-arrconf-cluster-validation/02-05-SUMMARY.md` documenting:
- PR2 number + merge timestamp + 1-line diff confirmation
- First apply Job: name, completion time, exit code
- Tag id assigned to arrconf-managed in Sonarr (from snapshot tag.json)
- Drift demo: original value, drifted value, restored value, time-to-revert (cron cycle wall-clock)
- Cited >= 2 JSON log lines from drift-demo-<date>.log demonstrating the W-06 verified `plan_action` event names
- Cited the DISPOSITIVE COMPARISON block from drift-demo-<date>.log (W-04 fix)
- drift-test-<date>/ snapshot file count (W-01 REQUIRED proof)
- Phase 1 HUMAN-UAT #3 closure timestamp
- Phase 2 ROADMAP success criteria status (all 5 checked)
- Cross-repo file inventory: snapshots committed, evidence committed (arr-stack); 1 PR2 merged (my-kluster)
- Recommended next step: `/gsd-verify-phase 02` to formalize phase completion
</output>
</content>
</invoke>