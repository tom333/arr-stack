---
phase: 02.2-v0-1-4-forcesave-fix
verified: 2026-05-10T16:45:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/10
  gaps_closed:
    - "merge_fields_for_put omit-credential branch now guarded by 'if v == \"\" or v is None:' — non-empty YAML credential passes through to PUT body for rotation (CR-01)"
  gaps_remaining: []
  regressions: []
---

# Phase 02.2: v0.1.5 Hotfix Verification Report

**Phase Goal:** Ship the v0.1.5 hotfix that closes D-02.2-AUTH-REGRESSION — credential survival during arrconf reconcile. The v0.1.4 forceSave fix worked for priority restoration but caused a credential regression (qBit password overwritten with the `"********"` API mask). v0.1.5 introduces an omit-by-privacy-metadata strategy in `merge_fields_for_put` that prevents credential-bearing fields from being included in PUT bodies, letting Sonarr/qBit preserve their stored values. Plan 13 (v0.1.6) closes CR-01 by guarding the omit-credential branch for non-empty rotation values.

**Verified:** 2026-05-10T16:45:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (Plan 13, v0.1.6)

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `merge_fields_for_put` consults cluster-side `privacy` metadata and omits `password`/`userName` entries from PUT body | VERIFIED | `differ.py:140` — `if cur_privacy in ("password", "userName"):` branch. Parallel lookup `cur_privacy_by_name` built from `FieldKV.privacy`. |
| 2 | `merge_field_omitted_credential` structlog event fires per omission with `{name, privacy}` payload (no value) | VERIFIED | `differ.py:143-147` — event emitted with `name=des_f["name"], privacy=cur_privacy`. Cluster smoke log confirms event fires × 2 in production. |
| 3 | D-31 contract preserved: non-credential empty-YAML fields still trigger cluster-value-preserve substitution | VERIFIED | `test_merge_preserves_cluster_value_when_yaml_empty` passes. Fields with `privacy=None` fall through to empty-value-preserve branch unchanged. |
| 4 | 2 RED tests (Plan 07) now GREEN; pre-existing 15 + new 4 = 19 tests all pass | VERIFIED | `uv run pytest tests/test_differ.py -q` → `20 passed in 0.12s` (post-Plan-13; was 19 before Plan 13). |
| 5 | ADR-8.1 appended to spec.md §11 (append-only, ADR-8 intact) | VERIFIED | `grep -c '^### ADR-' spec.md` → 8; `grep -c '^#### ADR-8\.1' spec.md` → 1. Commit `6bb9e9b` verified in git log. |
| 6 | v0.1.5 git tag exists and GHCR image `0.1.5` published | VERIFIED | `git tag -l v0.1.5` → `v0.1.5`; `git ls-remote --tags origin` → `dae80955...refs/tags/v0.1.5`. CI run `25597994626` conclusion=success. |
| 7 | my-kluster deployed v0.1.5 (ArgoCD synced) | VERIFIED | Renovate PR #1372 merged at `2026-05-09T20:23:22Z`; ArgoCD revision matches merge commit; live CronJob `spec.image = ghcr.io/tom333/arr-stack-arrconf:0.1.5`. |
| 8 | CronJob unsuspended and composite dispositive satisfied: `merge_field_omitted_credential >= 1` + `preserved-creds == 0` + Test API HTTP 200 | VERIFIED | `drift-demo-credential-aware-2026-05-10.log` captures all 6 composite signals GREEN. |
| 9 | Drift demo ran FULLY AUTOMATED: priority restored + credentials survived | VERIFIED | `drift-demo-credential-aware-2026-05-10.log`: `priority restored, manual_nudge_used: NO`, `RESTORED==ORIGINAL`. |
| 10 | `merge_fields_for_put` omit-credential branch is safe when user provides a non-empty YAML credential value (CR-01) | VERIFIED | `differ.py:139` — `v = des_f.get("value")` hoisted above privacy check. `differ.py:141` — `if v == "" or v is None:` inner guard restricts omission to empty-placeholder case. Non-empty desired appends `des_f` directly and continues (`differ.py:151-152`). `test_merge_fields_passes_through_non_empty_credential_value_for_rotation` (line 387) asserts `"password" in fields_by_name` and `fields_by_name["password"]["value"] == "new_value"` — passes in live run. `uv run pytest tests/test_differ.py -q` → `20 passed`. v0.1.6 tagged (`b863c0a8`) and pushed to origin. |

**Score:** 10/10 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tools/arrconf/arrconf/differ.py` | omit-by-privacy-metadata branch with CR-01 guard | VERIFIED | `v = des_f.get("value")` at L139, privacy branch at L140, inner guard `if v == "" or v is None:` at L141. |
| `tools/arrconf/tests/test_differ.py` | 20 tests (19 prior + 1 rotation-path test) | VERIFIED | `grep -c '^def test_'` → 20. `test_merge_fields_passes_through_non_empty_credential_value_for_rotation` at L387. `pytest` → 20 passed. |
| `tools/arrconf/tests/test_reconcilers_sonarr.py` | `test_update_omits_privacy_credential_fields_from_put_body` | VERIFIED | New name present. |
| `spec.md` | ADR-8.1 sub-section appended at §11 | VERIFIED | L905. 8 `### ADR-` entries. 1 `#### ADR-8.1` entry. |
| `git tag v0.1.5` (annotated, pushed) | Tag on origin | VERIFIED | `dae80955` on origin. |
| `git tag v0.1.6` (annotated, pushed) | Tag on origin for CR-01 closure | VERIFIED | `b863c0a8c2a5ef468eb597c62a91f2981df1ee70` on origin. Commit `a0f792b`. |
| `ghcr.io/tom333/arr-stack-arrconf:0.1.5` | Public image, anonymously pullable | VERIFIED | digest `sha256:63c05f754b58...11ac01`. |
| `evidence/cluster-recovery-2026-05-10.log` | Pre-reconcile Test API HTTP 200 | VERIFIED | File exists. All signals confirmed. |
| `evidence/drift-demo-credential-aware-2026-05-10.log` | Composite dispositive | VERIFIED | All 6 composite signals GREEN. |
| `evidence/visual-gate-reconfirm-2026-05-10.log` | Operator visual gate closed | VERIFIED | Operator approval at `2026-05-10T04:01:31Z`. |
| `snapshots/before-phase-2.2-hotfix-2026-05-09/sonarr/` | 17 JSON, anti-leak clean | VERIFIED | 17 files confirmed. |
| `snapshots/before-phase-2.2-hotfix-2026-05-09/qbittorrent/` | 8 JSON, anti-leak clean | VERIFIED | 8 files confirmed. |
| `snapshots/post-phase2.2-hotfix-2026-05-10/sonarr/` | 17 JSON post-recovery | VERIFIED | 17 files confirmed. |
| `snapshots/drift-test-phase2.2-hotfix-2026-05-10/sonarr/` | 17 JSON W-01 drift pre-snapshot | VERIFIED | 17 files confirmed. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `merge_fields_for_put` — non-empty credential path | `merged_fields.append(des_f)` | `v = des_f.get("value")` hoisted; inner guard `if v == "" or v is None:` restricts omit | VERIFIED | `differ.py:139-152`. Pass-through path at L151-152 confirmed. |
| `merge_fields_for_put` — empty credential path | `merge_field_omitted_credential` event | `if v == "" or v is None:` guard at L141 → `log.info(...)` → `continue` | VERIFIED | Event still fires on empty/None desired. Existing tests pass. |
| `test_merge_fields_passes_through_non_empty_credential_value_for_rotation` | `merge_fields_for_put` rotation path | Asserts `"password" in fields_by_name` and `value == "new_value"` | VERIFIED | L387. `pytest` → 20 passed. |
| Plan 07 RED tests | Plan 08 GREEN code | TDD cycle confirmed | VERIFIED | Both original RED tests still pass. |
| `test_merge_preserves_cluster_value_when_yaml_empty` | D-31 contract preservation | Empty-YAML field with `privacy=None` hits preserve branch | VERIFIED | `1 passed` live. |
| v0.1.5 `merge_fields_for_put` | Cluster smoke `merge_field_omitted_credential` | Image built → deployed by ArgoCD → fired by CronJob | VERIFIED | Cluster smoke log confirms event fires in production. |
| Drift demo | REQ-drift-detection correction half | Priority-restore + credential-survive in one automated run | VERIFIED | `manual_nudge_used: NO`, `RESTORED==ORIGINAL`. |
| ADR-8.1 in spec.md | `differ.py:merge_fields_for_put` | ADR cites `merge_field_omitted_credential` event name | VERIFIED | grep match confirmed. |

---

### Data-Flow Trace (Level 4)

`merge_fields_for_put` is a pure function (no I/O). Level 4 tracing is satisfied by the cluster composite dispositive (Plan 12) and the new rotation-path test (Plan 13):

| Data Path | Source | Produces Expected Output | Status |
|-----------|--------|------------------------|--------|
| `FieldKV.privacy` → `cur_privacy_by_name` lookup | Pydantic model `current.fields` | Privacy metadata available at omit-branch decision point | VERIFIED |
| `merge_field_omitted_credential` event → cluster JSON logs | `log.info(...)` at differ.py:143 | Events appear in smoke job pod logs | VERIFIED |
| Priority drift → UPDATE plan → merge → PUT body → Sonarr | reconcile() → merge_fields_for_put() → client.put() | Priority restored, credential NOT in PUT body | VERIFIED |
| Non-empty YAML credential → rotation path → PUT body | `v = des_f.get("value")` → inner guard → `merged_fields.append(des_f)` | `value == "new_value"` in merged body | VERIFIED — test assertion + 20 passed |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 20 differ tests all pass | `uv run pytest tests/test_differ.py -q` | `20 passed in 0.12s` | PASS |
| Full suite 68 tests pass | `uv run pytest -q` | `68 passed in 0.80s` | PASS |
| D-31 contract test passes | `uv run pytest tests/test_differ.py::test_merge_preserves_cluster_value_when_yaml_empty -q` | `1 passed` | PASS |
| Plan 02 forceSave contract intact | `uv run pytest -k forceSave -q` | passes | PASS |
| CR-01 rotation test passes | `uv run pytest tests/test_differ.py::test_merge_fields_passes_through_non_empty_credential_value_for_rotation -q` | `1 passed` | PASS |
| v0.1.6 tag on origin | `git ls-remote --tags origin \| grep v0.1.6` | `b863c0a8c2a5ef468eb597c62a91f2981df1ee70 refs/tags/v0.1.6` | PASS |
| `merge_field_omitted_credential` fires in cluster | `drift-demo-credential-aware-2026-05-10.log` | count=2, `merge_field_preserved` for creds=0 | PASS |
| Sonarr Test API HTTP 200 post-reconcile | `cluster-recovery-2026-05-10.log` | HTTP 200 + isValid:true | PASS |
| Drift demo fully automated | `drift-demo-credential-aware-2026-05-10.log` | `manual_nudge_used: NO`, `RESTORED==ORIGINAL` | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| REQ-drift-detection | Plans 07, 08, 12, 13 | Drift detected AND corrected automated; credentials survive correction; rotation path safe | SATISFIED | `REQUIREMENTS.md` `[x] REQ-drift-detection`. Drift demo log confirms automated correction + credential survival. Plan 13 closes CR-01 rotation-path correctness gap. |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `tools/arrconf/arrconf/differ.py` | 140 | Credential privacy enumeration covers only `("password", "userName")` — misses `"apiKey"` and `"token"` | Warning | Phase 3 indexer reconcilers reusing this function (D-33) may encounter `apiKey`/`token` fields falling through to preserve-cluster branch. CR-01 is closed; WR-01 from `02.2-REVIEW.md` remains as a non-blocking warning for Phase 3. |
| `tools/arrconf/tests/test_differ.py` | 87-131 | Pre-existing tests use `password`-named fields without `privacy` metadata | Info | Future maintainer adding `privacy: "password"` to those fixtures would silently invert tested contract. WR-02 from `02.2-REVIEW.md`. Non-blocking. |

CR-01 (previously a Blocker) is now CLOSED. Remaining entries are Warning/Info only.

---

### Human Verification Required

All operator verification items were completed within Plan 12. No remaining human verification items.

---

### Gaps Summary

No gaps. All 10 truths verified.

Plan 13 closed the single blocker (CR-01): `merge_fields_for_put` now guards the omit-credential branch with `if v == "" or v is None:`, hoisted above the privacy check. A non-empty YAML credential value passes through to the PUT body for rotation. The 20th test (`test_merge_fields_passes_through_non_empty_credential_value_for_rotation`) asserts this contract directly. All 68 tests pass, ruff and mypy clean, v0.1.6 tagged and pushed to origin.

---

_Verified: 2026-05-10T16:45:00Z_
_Verifier: Claude (gsd-verifier)_
