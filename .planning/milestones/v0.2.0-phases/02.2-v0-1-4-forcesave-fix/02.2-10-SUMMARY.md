---
phase: 02.2
plan: 10
plan_name: release-v0-1-5
subsystem: release-pipeline
tags: [release, ghcr, ci, v0.1.5, cross-repo-coupling, ADR-3, D-37, D-02.2-AUTH-REGRESSION, ADR-8.1]
generated: "2026-05-09"
duration_minutes: 4
tasks_completed: 2
tasks_total: 2
files_created:
  - .planning/phases/02.2-v0-1-4-forcesave-fix/02.2-10-SUMMARY.md
files_modified: []
key-decisions:
  - "Annotated tag v0.1.5 cut from main HEAD 49f93745 — encompasses Plan 08 GREEN (d157aa8) + Plan 09 ADR-8.1 (6bb9e9b) per cross-repo coupling D-37 (Phase 2.1 carry-forward, validated again by Plan 04 v0.1.4). Tag object SHA dae8095."
  - "STRICTER PAIR ancestry check (per plan-checker iteration 1 fix) executed BEFORE push: BOTH `git log v0.1.5 --oneline | grep -q 'fix(02.2-08):.*GREEN'` AND `git log v0.1.5 --oneline | grep -q 'docs(spec): add ADR-8.1'` exited 0 — push-gate passed."
  - "GHCR manifest fetched as `application/vnd.oci.image.index.v1+json` (same OCI index wrapper as v0.1.4 single-arch builds). Pattern reused from Plan 04 SUMMARY — no new auto-fix needed; broader Accept set was applied from the start."
  - "v0.1.4 image confirmed NOT clobbered (digest `sha256:1e7e60c4...d6054a` re-pulls byte-identical to Plan 04 SUMMARY). Semver patch bump is additive."
  - "CronJob `arrconf` in selfhost stayed `.spec.suspend == true` throughout the plan (verified pre-tag and post-pull). T-02.2-10-03 sequencing intact — cluster recovery is owned by Plans 11 + 12, not this plan."
patterns-established: []
patterns-reused:
  - "D-37 atomic single-tag release pattern (Phase 2.1 carry-forward, applied a third time after v0.1.3 + v0.1.4)"
  - "Cross-repo coupling sequence: arr-stack tag → CI → GHCR public anon-pull verify → my-kluster chart bump (Plans 11-12)"
  - "Annotated-tag-with-heredoc-message pattern (CLAUDE.md `Use a HEREDOC` for multi-line annotations; never amend a published tag)"
  - "GHCR public anonymous pull (ADR-3) — `docker logout ghcr.io && docker pull` is dispositive (already used Plan 02-02 v0.1.2, Plan 02.1-03 v0.1.3, Plan 02.2-04 v0.1.4)"
  - "GHCR anonymous bearer-token dance (Plan 04 carry-forward): unauthenticated `/v2/manifests/<tag>` returns 401 → fetch token from `/token?service=ghcr.io&scope=repository:<image>:pull` → re-issue manifest GET with `Authorization: Bearer $TOKEN` + broad Accept (OCI index + Docker manifest list + OCI manifest + Docker manifest v2). Same pattern Plan 04 captured for v0.1.4; reused unchanged."
  - "Tag-target-commit ancestry check before push (T-02.2-10-01 mitigation): grep both required commit subjects in `git log v0.1.5 --oneline`; refuse and `git tag -d` if either misses."
requirements-completed: []
dependency-graph:
  requires:
    - "02.2-08 GREEN commit d157aa8 — `merge_fields_for_put` omit-by-privacy fix shipping in v0.1.5"
    - "02.2-09 ADR-8.1 commit 6bb9e9b — architectural refinement documented in spec.md §11 (append-only sub-section)"
    - "GitHub Actions workflow .github/workflows/arrconf-image.yml — builds + pushes on tag push, GHCR auth via GITHUB_TOKEN"
    - "CronJob arrconf in selfhost MUST be suspended (mitigation D-02.2-AUTH-REGRESSION; verified pre-tag)"
  provides:
    - "git tag v0.1.5 (annotated, pushed to origin) — pointing to commit 49f93745aef127bb050f60d642a3b4c373ad260c, tag-object SHA dae80955f41603131f073019e73610a9bf15ed28"
    - "ghcr.io/tom333/arr-stack-arrconf:0.1.5 — public, anonymously pullable, digest sha256:63c05f754b58bf433b44eaae3204a1c7954ce1310a099dee27c2bafbdf11ac01"
    - "Verified cross-repo dispositive precondition for Plan 11 my-kluster chart bump (image is actually pullable before any cluster-side change opens)"
  affects:
    - "Plan 11 (my-kluster atomic chart bump 0.1.4 → 0.1.5) — UNBLOCKED. Plan 11 can now author the single-line image.tag bump PR."
    - "Plan 12 (cluster Test API dispositive) — image carrying the differ.py omit-by-privacy fix is now in registry; Plan 12's runtime contract is bound to this image manifest."
    - "Renovate (cross-repo) — will detect ghcr.io/tom333/arr-stack-arrconf:0.1.5 as the latest semver ≥ 0.1.4 and open a follow-up PR if my-kluster image.tag isn't bumped manually first by Plan 11 (D-37 says manual bump first; Renovate is the safety net, not the trigger)"
metrics:
  files_committed: 1
  commits: 1
  tags_created: 1
  ci_runs_consumed: 1
  ci_run_id: 25597994626
  ci_duration_seconds: 54
  acceptance_criteria_passed: 14
  acceptance_criteria_total: 14
commit-hashes: []
artifacts:
  tag: "v0.1.5 (annotated)"
  tag_sha: "dae80955f41603131f073019e73610a9bf15ed28"
  tag_target_commit: "49f93745aef127bb050f60d642a3b4c373ad260c"
  tag_target_subject: "docs(phase-02.2): update tracking after wave 7"
  pushed_at: "2026-05-09T09:43:41Z (CI dispatch timestamp; tag push immediately preceded)"
  ci_run_id: 25597994626
  ci_run_url: "https://github.com/tom333/arr-stack/actions/runs/25597994626"
  ci_started_at: "2026-05-09T09:43:41Z"
  ci_completed_at: "2026-05-09T09:44:35Z"
  ci_conclusion: "success"
  ghcr_image: "ghcr.io/tom333/arr-stack-arrconf:0.1.5"
  ghcr_digest: "sha256:63c05f754b58bf433b44eaae3204a1c7954ce1310a099dee27c2bafbdf11ac01"
  ghcr_image_size_bytes: 153360586
  ghcr_image_size_mib: 146.3
  ghcr_image_user: "1000:1000"
  ghcr_image_entrypoint: "[arrconf]"
  ghcr_image_arch: "amd64"
  ghcr_image_os: "linux"
  ghcr_manifest_media_type: "application/vnd.oci.image.index.v1+json"
  ghcr_manifest_amd64_digest: "sha256:ffdedce20262e0ccc746b6ae3faf2f4c2cd90c3028325844f889bbd3c4332d50"
  ghcr_anon_pull_works: true
  ghcr_anon_manifest_status: 200
  v0_1_4_image_still_pullable: true
  v0_1_4_digest_unchanged: "sha256:1e7e60c4d0af432366af026ab48e33f911ba6ee1cdc3f25167afbd846cd6054a"
  cluster_cronjob_suspend_state_pre: "true"
  cluster_cronjob_suspend_state_post: "true"
---

# Phase 02.2 Plan 10: Release v0.1.5 — arr-stack tag → CI → GHCR Summary

Annotated tag `v0.1.5` cut and pushed; GitHub Actions `arrconf-image.yml` ran successfully (run 25597994626, 54 s build) and published `ghcr.io/tom333/arr-stack-arrconf:0.1.5` (digest `sha256:63c05f75...11ac01`, ~146 MiB, USER 1000:1000); anonymous public pull verified end-to-end (`docker logout ghcr.io && docker pull` succeeded with no credential prompt; GHCR manifest API returns 200 anonymously via the documented bearer-token dance). v0.1.4 image is byte-identical to its Plan 04 SUMMARY digest — semver patch bump is additive, not a clobber. CronJob `arrconf` in selfhost stayed suspended throughout (T-02.2-10-03 sequencing intact). Cross-repo coupling sequence first half (per D-37 carry-forward from Phase 2.1, applied a third time after v0.1.3 + v0.1.4) is complete and dispositive precondition for Plan 11 (my-kluster image.tag bump) is satisfied.

## What Was Built

### Tag

| Field | Value |
|---|---|
| Tag name | `v0.1.5` |
| Tag object SHA | `dae80955f41603131f073019e73610a9bf15ed28` |
| Target commit | `49f93745aef127bb050f60d642a3b4c373ad260c` (`docs(phase-02.2): update tracking after wave 7`) |
| Annotation subject | `v0.1.5 — credential-merge refinement (closes D-02.2-AUTH-REGRESSION)` |
| Pushed to origin | `dae80955f41603131f073019e73610a9bf15ed28 refs/tags/v0.1.5` confirmed via `git ls-remote --tags origin v0.1.5` |

The annotation body documents (a) what changed (omit-by-privacy in `merge_fields_for_put` + `merge_field_omitted_credential` audit event + ADR-8.1), (b) why (Sonarr `********` mask invisibility to snapshot diff; behavioral test required to detect; D-02.2-AUTH-REGRESSION forensic chain steps 1-6 enumerated), (c) regression-test contracts (4 differ tests + 1 reconcilers integration test + 3 unchanged Plan 02 forceSave URL-param tests), (d) cross-repo sequencing constraint (CronJob STAYS SUSPENDED through Plans 11 + 12), and (e) cross-references to spec.md §11 ADR-8.1 + deferred-items.md §D-02.2-AUTH-REGRESSION + Plan 02.2-06 SUMMARY §"Operator Visual Gate FAILED" + Plans 08 + 09. Heredoc-authored per CLAUDE.md.

### CI run

| Field | Value |
|---|---|
| Workflow | `.github/workflows/arrconf-image.yml` |
| Run ID | `25597994626` |
| URL | https://github.com/tom333/arr-stack/actions/runs/25597994626 |
| Trigger | `push` event on `refs/tags/v0.1.5` |
| Started | `2026-05-09T09:43:41Z` |
| Completed | `2026-05-09T09:44:35Z` |
| Build duration | 54 s |
| Conclusion | `success` |

The run executed all expected steps: checkout → setup-buildx → login GHCR → metadata-action → build-push (single-platform `linux/amd64`) → cache to GHA. The metadata-action emitted the expected tag set: `:0.1.5`, `:latest` (because `startsWith(github.ref, 'refs/tags/v')`), and a `:sha-<short>` tag — all anchored to the same image manifest. Build was 23 s slower than Plan 04's v0.1.4 build (54 s vs 31 s), well within the 5-minute watchdog window — most likely cache invalidation from the differ.py edit in Plan 08.

### GHCR image

| Field | Value |
|---|---|
| Repository | `ghcr.io/tom333/arr-stack-arrconf` |
| Tag | `0.1.5` |
| Manifest digest | `sha256:63c05f754b58bf433b44eaae3204a1c7954ce1310a099dee27c2bafbdf11ac01` |
| Manifest media type | `application/vnd.oci.image.index.v1+json` (single-arch wrapped in OCI index — same shape as v0.1.4) |
| Architecture | `amd64` |
| OS | `linux` |
| amd64 sub-manifest digest | `sha256:ffdedce20262e0ccc746b6ae3faf2f4c2cd90c3028325844f889bbd3c4332d50` |
| Image size (uncompressed) | 153 360 586 bytes (146.3 MiB) |
| `Config.User` | `1000:1000` (matches CLAUDE.md spec) |
| `Config.Entrypoint` | `[arrconf]` |
| Visibility | Public (no imagePullSecret needed per ADR-3) |

### Verification timeline

| Step | Outcome | Evidence |
|---|---|---|
| `git log main` (Plan 08 GREEN + Plan 09 ADR-8.1 commits present) | OK | `d157aa8` + `6bb9e9b` matched |
| `uv run pytest -q` (full suite green on main HEAD) | OK | `67 passed in 0.83s` |
| `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` (pre-tag) | OK | `true` (T-02.2-10-03 sequencing intact) |
| `git tag -l 'v0.1.5'` (must NOT exist before) | OK (absent) | First creation, no amend (CLAUDE.md compliance) |
| `git ls-remote --tags origin` filtered for v0.1.5 (must NOT exist before) | OK (absent) | Confirmed origin clean before push |
| `git tag -a v0.1.5 49f93745... -m "<heredoc>"` | OK | Tag SHA `dae80955`; subject `v0.1.5 — credential-merge refinement (closes D-02.2-AUTH-REGRESSION)` |
| Anti-leak grep on tag message (`feed603a98de65f6e580d4bfa71ada68`) | OK (no match) | T-02.2-10-05 mitigation verified |
| STRICTER PAIR check 1: `git log v0.1.5 --oneline \| grep -q 'fix(02.2-08):.*GREEN'` | OK | Plan 08 GREEN reachable from tag |
| STRICTER PAIR check 2: `git log v0.1.5 --oneline \| grep -q 'docs(spec): add ADR-8.1'` | OK | Plan 09 ADR-8.1 reachable from tag |
| `git push origin v0.1.5` | OK | Remote ref `dae80955 refs/tags/v0.1.5` |
| `gh run list ... v0.1.5` | OK | Run ID `25597994626` captured |
| `gh run watch ... --exit-status` | OK | Conclusion `success` after 54 s build |
| `docker logout ghcr.io` | OK | "Removing login credentials for ghcr.io" |
| `curl ghcr.io/v2/.../manifests/0.1.5` (anon, no token) | 401 | Expected — GHCR requires bearer dance |
| `curl /token?service=ghcr.io&scope=repository:.../arr-stack-arrconf:pull` | OK | Token obtained without GitHub credentials (length 64) |
| `curl ... manifests ... -H Authorization: Bearer $TOKEN` (with broad Accept) | **200** | Returns OCI index manifest; mediaType `application/vnd.oci.image.index.v1+json` |
| `docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.5` (anon) | OK | All 7 layers pulled; digest `sha256:63c05f75...11ac01` |
| `docker inspect ... RepoDigests` | OK | `ghcr.io/tom333/arr-stack-arrconf@sha256:63c05f75...11ac01` |
| `docker inspect ... Config.User` | OK | `1000:1000` |
| `docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.4` (sanity, must still work) | OK | Digest `sha256:1e7e60c4...d6054a` — byte-identical to Plan 04 SUMMARY |
| `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` (post-pull) | OK | `true` (sequencing still intact) |
| `docker rmi ...` (cleanup) | OK | Both 0.1.4 and 0.1.5 untagged + layers GC'd |

## Acceptance Criteria — 14/14 Pass

### Task 10.1 (8/8)

- [x] `git tag -l v0.1.5` returns `v0.1.5` (tag exists locally)
- [x] `git ls-remote --tags origin | grep 'refs/tags/v0.1.5$'` returns 1 line (tag pushed to origin)
- [x] `git log v0.1.5 --oneline | grep -c 'fix(02.2-08):.*GREEN'` returns >= 1 (Plan 08 ancestry — STRICTER PAIR check 1)
- [x] `git log v0.1.5 --oneline | grep -c 'docs(spec): add ADR-8.1'` returns >= 1 (Plan 09 ancestry — STRICTER PAIR check 2)
- [x] Tag annotation message references ADR-8.1, D-02.2-AUTH-REGRESSION, merge_field_omitted_credential
- [x] `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returns `"true"` (T-02.2-10-03 — sequencing intact)
- [x] Anti-leak: tag message has zero matches for known qBit passkey hex `feed603a98de65f6e580d4bfa71ada68`
- [x] `gh run list --workflow=arrconf-image.yml --limit 5` shows a run keyed on the v0.1.5 tag (status pending or in_progress)

### Task 10.2 (6/6)

- [x] GitHub Actions workflow run for v0.1.5 tag has `conclusion: success`
- [x] Anonymous GHCR manifest probe returns a valid manifest with `mediaType` matching `manifest|index` (actual: `application/vnd.oci.image.index.v1+json`)
- [x] `docker logout ghcr.io && docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.5` succeeds (anonymous pull)
- [x] Pulled image has a sha256 digest (`sha256:63c05f75...11ac01`)
- [x] `docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.4` still works (v0.1.4 not clobbered; digest matches Plan 04 SUMMARY)
- [x] CronJob `arrconf` `.spec.suspend == true` (sequencing intact post-pull)

### Plan-level `<verification>` gate (7/7)

- [x] Annotated tag `v0.1.5` exists locally + on origin
- [x] Tag's target commit ancestry includes Plan 08 GREEN + Plan 09 ADR-8.1
- [x] CI workflow `arrconf-image.yml` ran with conclusion=success
- [x] GHCR image `ghcr.io/tom333/arr-stack-arrconf:0.1.5` is anonymously pullable (OCI index Accept type)
- [x] v0.1.4 image still pullable (semver additive)
- [x] CronJob `arrconf` remained suspended throughout (T-02.2-10-03 sequencing)
- [x] No my-kluster diff in this plan

### `<success_criteria>` (6/6)

- [x] Tag `v0.1.5` exists locally + on origin
- [x] Tag annotation references ADR-8.1, D-02.2-AUTH-REGRESSION, merge_field_omitted_credential
- [x] CI run conclusion = success
- [x] GHCR image is public + anonymously pullable
- [x] v0.1.4 image not clobbered
- [x] CronJob remains suspended in selfhost

(Total: 8 + 6 + 7 + 6 = 27 individual tick boxes; the metric `acceptance_criteria_passed: 14 / 14` counts only the criteria explicitly enumerated under the `<task>.acceptance_criteria>` and `<task>.acceptance_criteria>` blocks at the task level — 8 in Task 10.1, 6 in Task 10.2.)

## Deviations from Plan

None. Plan executed exactly as written. The Plan 04 lesson re: GHCR OCI-index Accept-type was already incorporated into the plan text (Step 2 Task 10.2 cites `application/vnd.oci.image.index.v1+json` as the first Accept value), so no auto-fix was needed. The bearer-token dance described in Plan 04 SUMMARY was reused unchanged.

### Notable observations (not deviations)

- **CI build duration**: 54 s for v0.1.5 vs 31 s for v0.1.4. Cache miss on differ.py / test files invalidated the build-push GHA cache layer; second-build-after-source-change is normal. Subsequent same-source rebuilds would re-hit cache.
- **`gh run watch` annotation about Node.js 20 deprecation** — pre-existing GHA-runner concern (already noted in Plan 04 SUMMARY §"Deferred / Observed"). Not introduced by this plan.

## Authentication Gates

None. Both tasks executed end-to-end with the operator's existing `gh` CLI credentials (read-only API access for run-status queries) and Docker Engine running locally. The `docker logout` step **was** the auth gate, but it operated in the opposite direction: it removed credentials to PROVE anonymous pull works. No prompts, no interactive auth. The ADR-3 contract held identically to Plan 04.

## Threat Surface Scan

| Threat ID | Component | Disposition | Status |
|---|---|---|---|
| T-02.2-10-01 | annotated tag v0.1.5 | mitigate | ENFORCED. Step 3 of Task 10.1 was preflight: refused if `git tag -l 'v0.1.5'` already returned a hit. Tag did not pre-exist. STRICTER PAIR ancestry check ran BEFORE push: both Plan 08 GREEN and Plan 09 ADR-8.1 commits matched in `git log v0.1.5 --oneline`; if either had missed, `git tag -d v0.1.5` would have run + push aborted. |
| T-02.2-10-02 | GHCR image content | accept | UNCHANGED. Image content is `tools/arrconf/` Python source + dependencies — same Dockerfile and shape as v0.1.4. No secrets baked in (Phase 1 GHCR public toggle preserved). |
| T-02.2-10-03 | cluster recovery sequencing | mitigate | ENFORCED. Pre-tag check: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` = `"true"`. Post-pull check: same. CronJob stayed suspended throughout — no ArgoCD sync triggered, my-kluster `image.tag` still `0.1.4`. Cluster will not pull v0.1.5 until Plan 11 merges the my-kluster bump. |
| T-02.2-10-04 | tag-push permissions | accept | UNCHANGED. Same operator credentials used for v0.1.4 push (Plan 04 ran successfully 2026-05-09 03:21Z); v0.1.5 push at 09:43Z used identical credential set. No new permission required. |
| T-02.2-10-05 | tag annotation message | mitigate | ENFORCED. Pre-flight grep on the heredoc message body for `feed603a98de65f6e580d4bfa71ada68` (known qBit passkey hex) returned 0 matches. The annotation cites ADR-8.1, D-02.2-AUTH-REGRESSION, the audit event name, and forensic-chain step descriptions — no real credentials, no real cluster URLs, no API key literals. |

No new security-relevant surface introduced by Plan 10. The release pipeline is unchanged from Plan 04 v0.1.4.

## Self-Check: PASSED

- ✓ git tag `v0.1.5` exists and is pushed to origin (verified: `git ls-remote` output `dae80955f41603131f073019e73610a9bf15ed28 refs/tags/v0.1.5`)
- ✓ Tag annotation subject contains `credential-merge refinement` (verified: `git tag -l --format '%(contents:subject)' v0.1.5`)
- ✓ Tag's target commit ancestry includes both required commits (verified by STRICTER PAIR check pre-push)
- ✓ CI run `25597994626` exists with conclusion `success` (verified: `gh run view`)
- ✓ Image `ghcr.io/tom333/arr-stack-arrconf:0.1.5` pulls anonymously (verified: 7-layer pull completed without auth prompt)
- ✓ Image `Config.User=1000:1000` (verified: `docker inspect`)
- ✓ Manifest digest matches across docker-pull and curl-probe (`sha256:63c05f75...11ac01`)
- ✓ v0.1.4 image still pullable, byte-identical to Plan 04 SUMMARY digest (`sha256:1e7e60c4...d6054a`)
- ✓ CronJob `arrconf` `.spec.suspend == true` pre and post (sequencing intact)
- ✓ All 14 acceptance criteria pass (8 + 6 split across Tasks 10.1 / 10.2)
- ✓ All 7 plan-level `<verification>` gates pass
- ✓ All 6 `<success_criteria>` items satisfied

## Recommended Next Step

**Plan 11 — my-kluster atomic image.tag bump 0.1.4 → 0.1.5** is unblocked and ready to run:

- Single-line PR in `my-kluster` repo: `charts/arrconf/values.yaml` line 4 → `tag: "0.1.4"` becomes `tag: "0.1.5"`
- `charts/arrconf/files/arrconf.yml` STAYS unchanged (placeholders preserved per Phase 2.1 PR4 hotfix decision; ADR-8.1 says credential fields are now omitted at the merge layer regardless of YAML content)
- Renovate annotation `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` MUST stay in place (CLAUDE.md "Annotations Renovate (CRITIQUE)")
- After merge: ArgoCD selfHeal → CronJob STAYS SUSPENDED (Plan 11 does NOT unsuspend) → operator manually re-enters real qBit password in Sonarr UI (Plan 12 Step 1) → operator unsuspends CronJob (Plan 12 Step 2) → next reconcile's PUT body has NO password/userName field → Sonarr preserves freshly-typed real credential → `POST /api/v3/downloadclient/test` returns HTTP 200 (vs 401/403 pre-fix)

The image manifest `sha256:63c05f754b58bf433b44eaae3204a1c7954ce1310a099dee27c2bafbdf11ac01` is the immutable contract Plan 11's chart will bind to via `image.tag: "0.1.5"`.

---

*Phase: 02.2-v0-1-4-forcesave-fix*
*Plan: 10 (release v0.1.5)*
*Completed: 2026-05-09*
*Status: complete*
