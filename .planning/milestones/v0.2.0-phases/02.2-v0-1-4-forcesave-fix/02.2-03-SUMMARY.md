---
phase: 02.2
plan: 03
plan_name: adr-8-trusted-controller
subsystem: spec-docs
tags: [docs, adr, ADR-8, trusted-controller, forcesave, arr-v3]
generated: "2026-05-09"
duration_minutes: 1
tasks_completed: 1
tasks_total: 1
files_created: []
files_modified:
  - spec.md
key-decisions:
  - "ADR-8 inserted between ADR-7's `Alternatives rejetées` block and the §11/§12 `---` separator — preserves §12 Références layout (no re-numbering of any prior ADR; ADRs 1-7 remain at lines 748..851 unchanged)"
  - "Prose authored in French per project convention (ADRs 1-7 are all in French; English would have been a stylistic regression)"
  - "Cross-reference triple captured in body: code (`_ArrV3Client.put()` in `tools/arrconf/arrconf/client_base.py`), prior finding (`D-02.1-06`), Phase 2.1 contract (`D-31/D-32`) — gives a Phase 3+ contributor a self-contained jump table from spec to implementation"
  - "Conséquences section explicitly scopes qBittorrent (Phase 5) and Jellyfin (Phase 7) OUT of forceSave — load-bearing for Phase 5/7 contributors who would otherwise inherit a *arr-v3 URL param into non-*arr semantics"
  - "Alternatives rejetées contains all four design options surfaced during planning: Option B (mask-in-body trigger), event-coupled trigger, reconciler-layer override, class-flag (Option a) — gives a future ADR author all the context to reopen the decision if needed"
patterns-established:
  - "ADR `Conséquences` section with explicit `OUT OF SCOPE` callout for adjacent subsystems — pattern carries forward to any ADR whose decision is locally true but globally false"
patterns-reused:
  - "ADR-1..7 format template: `### ADR-N — <title>` + bold-prefixed subheadings (Contexte / Décision / Raisons / Conséquences / Alternatives rejetées) — Phase 02.2 ADR-8 mirrors verbatim"
  - "Append-only ADR convention (CLAUDE.md `Conventions développement` carries this implicitly via `ADRs are append-only` in Phase 2.1 LEARNINGS) — no renumbering of ADR-1..7"
requirements-completed: []
dependency-graph:
  requires:
    - "02.2-02: GREEN commit `4a24c5f` — `_ArrV3Client.put()` mixin and structlog event `put_force_save_used` in `tools/arrconf/arrconf/client_base.py` (referenced literally in ADR-8 prose)"
    - "02.1-04 closure: `D-02.1-06` deferred-items entry — the architectural finding ADR-8 retroactively documents"
  provides:
    - "spec.md §11 ADR-8 — durable architectural record for Phase 3+ contributors. Phase 3 (Radarr/Prowlarr inheriting from `_ArrV3Client`), Phase 5 (qBittorrent — explicitly out of scope), Phase 7 (Jellyfin — explicitly out of scope) all get a self-contained reference"
  affects:
    - "Plan 04 (release v0.1.4) — independent of this plan; ready to ship as Wave 3 once Plans 02 + 03 are on main"
    - "Phase 3 RadarrClient/ProwlarrClient — inherit forceSave behavior by construction; ADR-8 is the contract documenting why this is safe"
    - "Phase 5 qBittorrentClient — ADR-8 Conséquences explicitly excludes qBit from forceSave (parent stays `ArrApiClient`, not `_ArrV3Client`)"
    - "Phase 7 JellyfinClient — same explicit exclusion as Phase 5"
metrics:
  files_committed: 1
  commits: 1
  lines_added_to_spec: 26
  lines_removed_from_spec: 0
  acceptance_criteria_passed: 12
  acceptance_criteria_total: 12
commit-hashes:
  - "571d9a1 — docs(spec): add ADR-8 — arrconf trusted controller forceSave bypass (D-02.2-04)"
---

# Phase 02.2 Plan 03: ADR-8 trusted-controller documentation Summary

Single docs commit appending ADR-8 to `spec.md` §11. Documents retroactively the architectural stance — "arrconf is a trusted controller — bypasses *arr UI-grade pre-save validation via `?forceSave=true` on UPDATE PUTs" — that Plan 02 implemented in `_ArrV3Client.put()`. Captures the load-bearing constraint for Phase 3+ contributors with bidirectional traceability: ADR ↔ code (`_ArrV3Client.put()`) ↔ finding (`D-02.1-06`) ↔ implementation (Plan 02 GREEN commit `4a24c5f`).

## What Was Built

### Commit

| # | Hash | Subject | Files | Delta |
|---|------|---------|-------|-------|
| 1 | `571d9a1` | `docs(spec): add ADR-8 — arrconf trusted controller forceSave bypass (D-02.2-04)` | `spec.md` | +26 / -0 |

### ADR-8 anchor location

```
spec.md  748: ### ADR-1 — Script Python custom plutôt que Buildarr/Terraform/Flemmarr
spec.md  766: ### ADR-2 — Helm dependencies sur app-template (Option A)
spec.md  781: ### ADR-3 — Image arrconf hébergée sur GHCR public
spec.md  796: ### ADR-4 — Repo séparé plutôt qu'extension de my-kluster
spec.md  814: ### ADR-5 — configarr conservé pour son scope
spec.md  829: ### ADR-6 — Snapshot baseline avant toute écriture
spec.md  851: ### ADR-7 — Single instance Sonarr/Radarr + tags (pas multi-instance)
spec.md  879: ### ADR-8 — arrconf is a trusted controller — bypasses *arr UI-grade pre-save validation   ← NEW
```

ADR-8 is the 8th entry; ADR-1..7 are byte-identical to pre-edit (lines unchanged). `## 12. Références` follows immediately after the ADR-8 block via the preserved `---` separator.

### One-line load-bearing claim

> arrconf est un **trusted controller** — tout client *arr v3 (Sonarr/Radarr/Prowlarr) envoie systématiquement `?forceSave=true` sur les PUT UPDATE. L'implémentation vit au layer HTTP client (`_ArrV3Client.put()` dans `tools/arrconf/arrconf/client_base.py`).

## Acceptance Criteria — 12/12 Pass

| # | Criterion | Result |
|---|-----------|--------|
| 1 | `grep -c '^### ADR-8' spec.md` returns `1` | ✅ 1 |
| 2 | `grep -c '^### ADR-' spec.md` returns `8` (no renumbering) | ✅ 8 |
| 3 | `grep -q '^### ADR-1 ' spec.md && grep -q '^### ADR-7 ' spec.md` exits 0 | ✅ both present |
| 4 | spec.md contains literal `trusted controller` | ✅ |
| 5 | spec.md contains literal `forceSave=true` | ✅ |
| 6 | spec.md contains literal `_ArrV3Client` (links ADR to code) | ✅ |
| 7 | spec.md contains literal `D-02.1-06` (links to Phase 2.1 finding) | ✅ |
| 8 | `grep -A 30 '^### ADR-8' spec.md \| grep -q 'qBittorrent'` exits 0 | ✅ Conséquences mentions qBit scope-out |
| 9 | `grep -A 30 '^### ADR-8' spec.md \| grep -q 'Jellyfin'` exits 0 | ✅ Conséquences mentions Jellyfin scope-out |
| 10 | `grep -A 60 '^### ADR-8' spec.md \| grep -q 'Alternatives rejetées'` exits 0 | ✅ |
| 11 | `grep -A 30 '^### ADR-8' spec.md \| grep -qE '(Contexte\|Décision\|Raisons\|Conséquences\|Alternatives)'` | ✅ all five French headers present |
| 12 | `git log -1 --pretty=%s \| grep -q 'docs(spec):.*ADR-8'` exits 0 | ✅ commit subject matches |

Plan-level `<verification>` block (7 gates): all 7 pass. Plan-level `<success_criteria>` (3 items): all 3 satisfied (D-02.2-04 closed; Phase 3+ have self-contained reference; bidirectional traceability ADR ↔ code ↔ finding ↔ implementation in place).

## Deviations from Plan

None — plan executed exactly as written. The PATTERNS.md §6 ADR-8 prose was adopted verbatim per the planner's pre-locked recommendation.

## Authentication Gates

None — pure docs commit; no API calls, no secret handling.

## Threat Surface Scan

No new security-relevant surface introduced. spec.md is a static documentation file; the change adds prose, not code paths. T-02.2-08 (the disposition `mitigate` from the plan threat register) is satisfied by the ADR existence + bidirectional links to D-02.1-06 and `_ArrV3Client`. T-02.2-09 (`accept` — future contributor silently removes ADR) inherits the project's append-only-ADR convention; mitigation is git history audit.

## Self-Check: PASSED

- ✓ `spec.md` contains `### ADR-8 — arrconf is a trusted controller` exactly once (verified at line 879)
- ✓ Total ADR count is 8 (verified by grep — ADR-1 through ADR-8 all present, no renumbering of ADR-1..7)
- ✓ Commit `571d9a1` exists in git log with subject `docs(spec): add ADR-8 — arrconf trusted controller forceSave bypass (D-02.2-04)`
- ✓ All 12 acceptance criteria pass (verified by automated `<verify>` block)
- ✓ Load-bearing literals present: `trusted controller`, `forceSave=true`, `_ArrV3Client`, `D-02.1-06` (verified)
- ✓ Conséquences explicitly scopes qBittorrent + Jellyfin OUT of forceSave (verified)
- ✓ Alternatives rejetées contains 4 entries (Option B, événements `merge_field_preserved`, reconciler `_execute` override, class flag Option a)

## Recommended Next Step

**Plan 04 — Release v0.1.4** can run now (Wave 3, independent of this plan):

- Tag arr-stack `v0.1.4` (annotated)
- Verify CI `arrconf-image.yml` builds and pushes `ghcr.io/tom333/arr-stack-arrconf:0.1.4` to GHCR
- Verify GHCR public anonymous pull (`docker logout ghcr.io && docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.4`)
- Open atomic single-line PR in my-kluster (`charts/arrconf/values.yaml`: `tag: "0.1.3"` → `"0.1.4"`)
- Cross-repo coupling sequence per D-37 carry-forward (Phase 2.1 LEARNINGS)
- ArgoCD selfHeal → CronJob unsuspend → forced smoke job → post-deploy snapshot

Plan 04 depends on Plan 02's GREEN commit `4a24c5f` (on main) and benefits from but does NOT block on Plan 03's ADR commit `571d9a1` — the ADR is documentation, the release ships the implementation.

After Plan 04: Plan 05 (drift demo runbook re-execution WITHOUT operator manual nudge — closes REQ-drift-detection's correction half automatically) and Plan 06 (phase closure: STATE.md + ROADMAP.md + Phase 02.2 marked complete).
