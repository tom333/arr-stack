---
phase: 02.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - snapshots/before-phase-2.2-2026-05-09/
autonomous: true
requirements: []
must_haves:
  truths:
    - "Pre-deploy baseline snapshot captured for sonarr + qbittorrent BEFORE any v0.1.4 code change ships (ADR-6 discipline)"
    - "Snapshot is committed in arr-stack git history (NEVER .gitignore'd per CLAUDE.md)"
    - "Snapshot includes a populated sonarr/downloadclient.json that confirms current cluster state running v0.1.3 with tags=[1] (Phase 2.1 closure carry-forward)"
    - "Snapshot directory tree exists with at least 5 sonarr JSON files and qbittorrent JSON files (full snapshot.sh output, not partial)"
  artifacts:
    - path: "snapshots/before-phase-2.2-2026-05-09/sonarr/downloadclient.json"
      provides: "Pre-deploy v0.1.3 cluster state for qBit downloadclient — used by Plan 06 W-04 dispositive comparison (ORIGINAL_PRIORITY anchor)"
      contains: "qBittorrent"
    - path: "snapshots/before-phase-2.2-2026-05-09/sonarr/tag.json"
      provides: "Pre-deploy tag list — must already include arrconf-managed (carry-forward from Phase 2.1)"
      contains: "arrconf-managed"
  key_links:
    - from: "tools/snapshot/snapshot.sh"
      to: "snapshots/before-phase-2.2-2026-05-09/"
      via: "snapshot.sh --apps sonarr,qbittorrent --output ..."
      pattern: "snapshot.sh.*before-phase-2.2"
---

<objective>
Pre-deploy baseline snapshot of Sonarr + qBittorrent state BEFORE the v0.1.4 forceSave fix ships. ADR-6 requires a snapshot baseline before any code change touching cluster scope; re-touching the downloadclient scope counts. This snapshot is the dispositive ORIGINAL_PRIORITY anchor for the W-04 drift demo dispositive in Plan 06.

Purpose: Capture the cluster's v0.1.3 steady state (tag id=1 attached to qBit downloadclient per Phase 2.1 closure) as forensic baseline. Provides the before-pair for the after-pair captured in Plan 06.

Output: `snapshots/before-phase-2.2-2026-05-09/sonarr/` and `snapshots/before-phase-2.2-2026-05-09/qbittorrent/` directories committed in arr-stack git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/STATE.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Capture pre-deploy baseline snapshot for sonarr + qbittorrent (ADR-6)</name>
  <files>
    snapshots/before-phase-2.2-2026-05-09/
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/tools/snapshot/snapshot.sh (CLI shape — `--apps APP1,APP2 --output PATH`)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md ("Worktree isolation can leak files into the main working tree" — cwd-drift gotcha)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/deferred-items.md (D-02.1-01, D-02.1-02 — manual jq redaction patterns for config_host + qbit tracker passkeys)
    - /home/moi/projets/perso/arr-stack/snapshots/post-phase2.1-2026-05-09/sonarr/downloadclient.json (Phase 2.1 closure baseline — confirm tags=[1] is current cluster state before this snapshot starts)
    - /home/moi/projets/perso/arr-stack/CLAUDE.md "Workflow snapshot" section
  </read_first>
  <action>
    Step 1 — Verify the cluster prerequisites are met (port-forwards available, env vars set):
        cd /home/moi/projets/perso/arr-stack
        # Confirm SONARR_API_KEY is available (read from cluster secret if missing):
        if [ -z "$SONARR_API_KEY" ]; then
          export SONARR_API_KEY="$(kubectl get secret arrconf-env -n selfhost -o jsonpath='{.data.SONARR_API_KEY}' | base64 -d)"
        fi
        # qBit credentials may be missing per D-02.1-04 — apply Phase 2.1's `127.0.0.0/8` whitelist workaround if needed.
        # See 02.1-LEARNINGS.md "qBit `WebUI\AuthSubnetWhitelist=192.168.88.0/24` does NOT cover cluster pod IPs"

    Step 2 — Establish port-forwards (Sonarr + qBittorrent):
        kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
        SONARR_PF=$!
        kubectl -n selfhost port-forward svc/qbittorrent 8080:8080 &
        QBIT_PF=$!
        sleep 3

    Step 3 — Run the baseline snapshot (literal output dir name `before-phase-2.2-2026-05-09` per CONTEXT.md "Specifics §Snapshot pair naming"):
        SNAPSHOT_DIR="snapshots/before-phase-2.2-2026-05-09"
        tools/snapshot/snapshot.sh --apps sonarr,qbittorrent --output "$SNAPSHOT_DIR/"

    Step 4 — Apply manual redaction workaround per D-02.1-01 + D-02.1-02 (snapshot.sh hardening is deferred):
        # config_host.json: redact apiKey, password, username
        if [ -f "$SNAPSHOT_DIR/sonarr/config_host.json" ]; then
          jq '.apiKey = "***REDACTED***" | .password = "***REDACTED***" | .username = "***REDACTED***"' \
            "$SNAPSHOT_DIR/sonarr/config_host.json" > "$SNAPSHOT_DIR/sonarr/config_host.json.tmp" \
            && mv "$SNAPSHOT_DIR/sonarr/config_host.json.tmp" "$SNAPSHOT_DIR/sonarr/config_host.json"
        fi
        # qbittorrent torrents_info.json: redact /announce/<32+ hex> tracker passkeys
        if [ -f "$SNAPSHOT_DIR/qbittorrent/torrents_info.json" ]; then
          jq 'walk(if type == "string" then gsub("/announce/[a-fA-F0-9]{16,}"; "/announce/***REDACTED-PASSKEY***") else . end)' \
            "$SNAPSHOT_DIR/qbittorrent/torrents_info.json" > "$SNAPSHOT_DIR/qbittorrent/torrents_info.json.tmp" \
            && mv "$SNAPSHOT_DIR/qbittorrent/torrents_info.json.tmp" "$SNAPSHOT_DIR/qbittorrent/torrents_info.json"
        fi

    Step 5 — Anti-leak audit (paranoid grep before commit):
        # No raw API keys, no plaintext passwords, no tracker passkeys
        ! grep -rE '(apiKey|api_key|API_KEY)" *: *"[^*]' "$SNAPSHOT_DIR/" || { echo "LEAK: apiKey not redacted"; exit 1; }
        ! grep -rE '/announce/[a-fA-F0-9]{16,}' "$SNAPSHOT_DIR/" || { echo "LEAK: passkey present"; exit 1; }

    Step 6 — Sanity check: tags=[1] is present on qBit downloadclient (Phase 2.1 carry-forward — should already be there):
        jq -e '.[0].tags | length > 0' "$SNAPSHOT_DIR/sonarr/downloadclient.json" \
          || { echo "ERROR: tags missing on qBit downloadclient — cluster state has regressed since Phase 2.1 closure"; exit 1; }

    Step 7 — Sanity check: arrconf-managed tag is present:
        jq -e 'map(.label) | any(. == "arrconf-managed")' "$SNAPSHOT_DIR/sonarr/tag.json" \
          || { echo "ERROR: arrconf-managed tag missing — cluster state has regressed since Phase 2.1 closure"; exit 1; }

    Step 8 — Cleanup port-forwards:
        kill $SONARR_PF $QBIT_PF 2>/dev/null || true

    Step 9 — Commit (CLAUDE.md "Ne pas ignorer `snapshots/`"):
        git add snapshots/before-phase-2.2-2026-05-09/
        git commit -m "snapshot(02.2): pre-deploy baseline before v0.1.4 forceSave fix (ADR-6)"

    Step 10 — Worktree-leak check (Phase 2.1 LEARNINGS surprise):
        # If executed inside a worktree, snapshot files may leak to main working tree.
        # Verify md5s match if any candidates exist outside the worktree, then leave a note in the SUMMARY.
        find /home/moi/projets/perso/arr-stack -maxdepth 2 -name "before-phase-2.2-2026-05-09" -type d 2>/dev/null
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && test -d snapshots/before-phase-2.2-2026-05-09/sonarr && test -d snapshots/before-phase-2.2-2026-05-09/qbittorrent && [ "$(find snapshots/before-phase-2.2-2026-05-09/sonarr -type f -name '*.json' | wc -l)" -ge 5 ] && jq -e '.[0].tags | length > 0' snapshots/before-phase-2.2-2026-05-09/sonarr/downloadclient.json > /dev/null && jq -e 'map(.label) | any(. == "arrconf-managed")' snapshots/before-phase-2.2-2026-05-09/sonarr/tag.json > /dev/null && ! grep -rE '/announce/[a-fA-F0-9]{16,}' snapshots/before-phase-2.2-2026-05-09/ && git log -1 --pretty=%s | grep -q 'snapshot(02.2):.*pre-deploy'</automated>
  </verify>
  <acceptance_criteria>
    - `test -d snapshots/before-phase-2.2-2026-05-09/sonarr` exits 0
    - `test -d snapshots/before-phase-2.2-2026-05-09/qbittorrent` exits 0
    - `find snapshots/before-phase-2.2-2026-05-09/sonarr -type f -name '*.json' | wc -l` >= 5
    - `jq -e '.[0].tags | length > 0' snapshots/before-phase-2.2-2026-05-09/sonarr/downloadclient.json` exits 0 (Phase 2.1 closure carry-forward)
    - `jq -e 'map(.label) | any(. == "arrconf-managed")' snapshots/before-phase-2.2-2026-05-09/sonarr/tag.json` exits 0 (Phase 2.1 closure carry-forward)
    - `! grep -rE '/announce/[a-fA-F0-9]{16,}' snapshots/before-phase-2.2-2026-05-09/` (no leaked tracker passkeys)
    - `! grep -rE '(apiKey|api_key|API_KEY)" *: *"[^*]' snapshots/before-phase-2.2-2026-05-09/` (no raw API keys)
    - `git -C /home/moi/projets/perso/arr-stack log -1 --pretty=%s | grep -q 'snapshot(02.2):.*pre-deploy'` exits 0
  </acceptance_criteria>
  <done>Pre-deploy snapshot captured + redacted + audited + committed; cluster state confirms v0.1.3 carry-forward (tag attached + arrconf-managed present); ready for Plan 02 code change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation -> Sonarr API (read-only) | Snapshot performs only GETs against Sonarr/qBit; no writes |
| Local snapshot files -> arr-stack git history | Snapshot artifacts are committed permanently; redaction must happen BEFORE commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-01 | Information Disclosure | snapshot.sh leaks Sonarr config_host apiKey/password (D-02.1-01) | mitigate | Step 4 applies manual `jq` redaction substituting `***REDACTED***` for apiKey/password/username; Step 5 grep audit gates the commit |
| T-02.2-02 | Information Disclosure | qBit torrents_info.json leaks tracker passkeys (D-02.1-02) | mitigate | Step 4 applies `jq walk` regex substitution `/announce/[a-fA-F0-9]{16,}` -> `/announce/***REDACTED-PASSKEY***`; Step 5 grep audit gates the commit |
| T-02.2-03 | Tampering | Snapshot captured during in-flight CronJob run gives stale state | accept | CronJob runs every 4h; if a run is in flight the snapshot's idempotent GET returns the same converged state. Cluster is single-tenant homelab; no concurrent operator activity expected |
</threat_model>

<verification>
End-of-plan gate (Plan 01 complete when all pass):
1. Snapshot directory exists with sonarr/ + qbittorrent/ subtrees
2. Sonarr JSON file count >= 5 (full snapshot, not partial)
3. tags=[1] confirmed on qBit downloadclient (Phase 2.1 carry-forward intact)
4. arrconf-managed tag confirmed present
5. No leaked apiKeys, no leaked tracker passkeys
6. Git commit exists with `snapshot(02.2):` prefix
</verification>

<success_criteria>
- ADR-6 discipline observed (snapshot before code change)
- Forensic baseline captured for Plan 06 W-04 dispositive ORIGINAL_PRIORITY anchor
- Cluster state regression check passes (v0.1.3 closure intact)
- All snapshot artifacts redacted + committed
</success_criteria>

<output>
After completion, create `/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-01-SUMMARY.md` documenting:
- Snapshot directory path + JSON file count per app
- Confirmed Phase 2.1 carry-forward state: `downloadclient.json[0].tags == [1]`, `tag.json contains arrconf-managed`
- Redaction workarounds applied (D-02.1-01, D-02.1-02 with one-line per-file diff summary)
- Commit SHA
- Cluster prerequisites verification (qBit auth workaround if applied + reverted)
- Recommended next step: Plan 02 (test + fix in arr-stack)
</output>
