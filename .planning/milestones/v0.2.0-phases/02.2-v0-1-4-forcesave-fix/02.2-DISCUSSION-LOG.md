# Phase 02.2: v0.1.4 forceSave fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-09
**Phase:** 02.2-v0-1-4-forcesave-fix
**Areas discussed:** Trigger condition, Implementation site & Phase 3 generality, Verification depth, ADR for spec.md §11 + audit logging

---

## Trigger condition

| Option | Description | Selected |
|--------|-------------|----------|
| A — Unconditional on UPDATE | Every UPDATE PUT carries `?forceSave=true`. Matches Sonarr's React UI Save behavior. ADD/DELETE unaffected. | ✓ |
| B — Conditional on mask in body | Scan body for `***REDACTED***` / `********` (or privacy=password fields); add forceSave only when present. | |
| C — Conditional on `merge_field_preserved` events fired | If merge helper emitted any `merge_field_preserved` event for this PUT, pass forceSave=true. | |

**User's choice:** A — Unconditional on UPDATE (Recommended)
**Notes:** Trade-off acknowledged — if Sonarr ever introduces a meaningful pre-save check, arrconf bypasses it. Counter: arrconf already merges cluster state into the body (D-31/D-32), so pre-save validation re-runs are a tax with no signal value in arrconf's controller context.

---

## Implementation site & Phase 3 generality

| Option | Description | Selected |
|--------|-------------|----------|
| client_base.py.put() default — always on | Add `forceSave=true` as the default `params` in client-layer; *arr-v3 subclasses inherit. Test asserts URL param at HTTP layer. | ✓ |
| Reconciler _execute UPDATE branch — explicit per call | `client.put(..., params={'forceSave': 'true'})` only in `sonarr.py:_execute`. Phase 3 reconcilers copy the line. | |
| Helper in differ.py alongside merge_fields_for_put | Tighter coupling to D-33 generalization; helper returns `{'params': {'forceSave': 'true'}}`. | |

**User's choice:** client_base.py.put() default — always on (Recommended for Phase 3)
**Notes:** Phase 3 reconcilers (Radarr, Prowlarr) inherit forceSave automatically by construction. Eliminates the "don't-forget-this-kwarg" failure mode for new reconcilers. Architectural shape (base-class default + override, vs `_ArrV3Client` mixin/intermediate, vs Sonarr-only override now then promote in Phase 3) is planner's call — captured under Claude's Discretion in CONTEXT.md.

---

## Verification depth

| Option | Description | Selected |
|--------|-------------|----------|
| Full re-run: smoke + W-01/W-04 drift demo dispositive | Pre-deploy snapshot + v0.1.4 release + smoke + full drift demo runbook with W-01 forensic snapshot before, mutate priority, W-04 dispositive value-equality after — WITHOUT operator manual nudge. | ✓ |
| Smoke only — defer drift re-run to Phase 3 | v0.1.4 + unit test + post-deploy smoke confirming PUT 200. Phase 3's first Radarr reconcile naturally exercises the same code path. | |
| Unit-only — trust the test, skip cluster work | v0.1.4 with new unit test only; don't redeploy to cluster in this phase. | |

**User's choice:** Full re-run: smoke + W-01/W-04 drift demo dispositive (Recommended)
**Notes:** Closes REQ-drift-detection's correction half cleanly — Phase 2.1 closed it via manual nudge; Phase 02.2 closes it via fully-automated reconcile. Adds ~30-45 min cluster work but locks the architectural finding shut before Phase 3.

---

## ADR for spec.md §11 + audit logging

| Option | Description | Selected |
|--------|-------------|----------|
| ADR-8 in spec.md §11 + `put_force_save_used` log event | Document trusted-controller stance in spec.md §11; emit structured log event in `client_base.py.put()` matching `merge_field_preserved` audit pattern. | ✓ |
| Decision records only (D-02.2-XX), no spec.md ADR, no new log event | Capture rationale only in CONTEXT.md / LEARNINGS.md; rely on existing `plan_action action=update` event. | |
| ADR-8 only, skip the new log event | Document architecturally; keep logging minimal. | |
| Log event only, skip the ADR | Audit-only; defer architectural documentation. | |

**User's choice:** ADR-8 in spec.md §11 + `put_force_save_used` log event (Recommended)
**Notes:** Captures the constraint for Phase 3+ (Radarr/Prowlarr inherit), Phase 5 (qBittorrent — explicitly out of scope for forceSave), Phase 6 (Seerr — TBD), Phase 7 (Jellyfin — TBD). Cluster JSON logs gain auditable proof of every forceSave bypass alongside existing `plan_action action=update` events.

---

## Claude's Discretion

- Architectural shape of the inheritance tree: base-class default with overrides, vs `_ArrV3Client` intermediate/mixin, vs SonarrClient-only override now + promote in Phase 3 — planner's call based on cohesion + minimum surface area.
- Final test name — D-02.1-06 proposed `test_update_uses_forceSave_when_masked_credential_in_body` but Option A unconditional makes the broader assertion correct; planner picks final name.
- `put_force_save_used` event payload shape — at minimum `{path, id}`; whether to include `diff_fields` is planner's call.
- Test coverage scope — at minimum the new unit test asserting forceSave on UPDATE; whether to also assert NOT present on ADD/DELETE is planner's call.
- Whether v0.1.4 also rolls in the D-02.1-05 helper backfill — default NO (single-purpose); planner escalates if composition is cleaner.

## Deferred Ideas

- `merge_fields_for_put` backfill for cluster-only fields (D-02.1-05) — defer to Phase 3 or v0.1.5
- snapshot.sh redaction enhancements (D-02.1-01, D-02.1-02) — defer to dedicated snapshot.sh hardening pass
- `QBT_USER`/`QBT_PASS` in `arrconf-env` Secret (D-02.1-04) — defer to Phase 5
- Drop the `arrconf-managed` orphan tag in Sonarr — no cleanup needed; mentioned for v0.1.4 smoke baseline awareness
- Test asserting forceSave NOT present on ADD/DELETE — defensive coverage, planner's call
- Spec.md §11 ADR-8 wording details — author's call
- README.md trusted-controller stance documentation — optional, defer to docs pass if scope tight
