---
phase: 02.2
plan: 11
type: execute
wave: 9
depends_on:
  - 02.2-10
files_modified:
  - my-kluster/charts/arrconf/values.yaml
gap_closure: true
autonomous: false
requirements: []
must_haves:
  truths:
    - "PR opened in my-kluster with EXACTLY one line changed: image.tag '0.1.4' -> '0.1.5' in charts/arrconf/values.yaml (D-37 atomic single-line diff)"
    - "charts/arrconf/files/arrconf.yml is UNCHANGED (placeholders username: ''/password: '' STAY per Phase 2.1 PR4 hotfix decision — D-02.1-05; v0.1.5 fix is in arrconf code, not chart values)"
    - "charts/arrconf/templates/cronjob.yaml is UNCHANGED"
    - "PR is reviewed and merged by the operator (checkpoint:human-action — automated checks pass first)"
    - "After merge: ArgoCD self-heals to v0.1.5 image; CronJob spec shows the new image tag — BUT CronJob STAYS SUSPENDED (do NOT unsuspend in this plan; Plan 12 owns the unsuspend after operator UI password re-entry)"
    - "Critical sequencing: the cluster pulls v0.1.5 (via ArgoCD sync) but does NOT execute it until Plan 12 unsuspends the CronJob — this prevents v0.1.4 (still in scheduled history) from re-overwriting any operator UI fix during the merge window"
  artifacts:
    - path: "my-kluster/charts/arrconf/values.yaml (post-merge)"
      provides: "Cluster runs ghcr.io/tom333/arr-stack-arrconf:0.1.5 after ArgoCD sync (CronJob spec.image updated; pods will spawn from this image when CronJob is unsuspended in Plan 12)"
      contains: "tag: \"0.1.5\""
  key_links:
    - from: "my-kluster PR (image.tag bump 0.1.4 → 0.1.5)"
      to: "ArgoCD selfHeal -> live CronJob spec.image"
      via: "automated.selfHeal: true on the arrconf ArgoCD Application"
      pattern: "image:\\s*ghcr.io/tom333/arr-stack-arrconf:0.1.5"
    - from: "kubectl patch cronjob arrconf -n selfhost suspend (already true since 06:48Z)"
      to: "Hotfix-via-PR safety pattern + cluster recovery race prevention"
      via: "Suspend CronJob during merge → ArgoCD sync → STAYS suspended → Plan 12 operator UI password re-entry → Plan 12 unsuspend"
      pattern: "suspend.*true"
---

<objective>
Cross-repo coupling sequence's second half: open the my-kluster PR bumping `charts/arrconf/values.yaml` `image.tag: "0.1.4" -> "0.1.5"`, wait for operator review + merge, verify ArgoCD self-heals the new image into the CronJob spec — but DO NOT unsuspend the CronJob (that's Plan 12's job, after operator UI password re-entry).

This plan is structurally a near-byte-clone of Plan 05 (v0.1.4 my-kluster bump) — D-37 atomic single-line diff pattern carried forward. The key DIFFERENCE from Plan 05: Plan 05 unsuspended the CronJob at the end. **This plan does NOT.** The cluster recovery sequencing (gap_summary section + D-02.2-AUTH-REGRESSION required-action #4) requires the password to be operator-re-entered in Sonarr UI BEFORE the next reconcile fires — otherwise v0.1.4 (still in the scheduled-not-yet-run history) or v0.1.5 (now scheduled) would re-overwrite the just-fixed credential. Plan 12 sequences: (a) confirm v0.1.5 pod can spawn (manual smoke); (b) operator re-enters password in Sonarr UI; (c) THEN unsuspend CronJob.

The `username: ''`/`password: ''` placeholders in `charts/arrconf/files/arrconf.yml` STAY (per Phase 2.1 PR4 hotfix decision documented in CONTEXT.md L24, 02.1-LEARNINGS D-36 reversal, and Plan 05 ATOMIC pattern). The v0.1.5 fix is at the arrconf code layer (differ.py), NOT the chart values layer — so the my-kluster diff is exactly one line.

Purpose: Make the cluster pull v0.1.5 image without disturbing the suspended-CronJob safety net. The cluster transitions from "running v0.1.4 code, suspended" to "running v0.1.5 code, still suspended" — ready for Plan 12 to operator-recover the credential and unsuspend.

Output: my-kluster PR merged; ArgoCD shows arrconf Application Synced + Healthy with the v0.1.5 image; CronJob STAYS suspended (verified pre-Plan-12-handoff).

Wave 9 — depends on Plan 10 (v0.1.5 image actually pullable from GHCR).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-05-PLAN.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-05-SUMMARY.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/deferred-items.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-03-PLAN.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml

<interfaces>
<!-- Single-line change in my-kluster — D-37 atomic diff pattern -->

# Existing values.yaml — POST-PLAN-05 CURRENT STATE:
image:
  # renovate: image=ghcr.io/tom333/arr-stack-arrconf
  repository: ghcr.io/tom333/arr-stack-arrconf
  tag: "0.1.4"
  pullPolicy: IfNotPresent

# DESIRED diff (1 line):
-    tag: "0.1.4"
+    tag: "0.1.5"

# UNCHANGED in this PR (Phase 2.1 PR4 carry-forward — placeholders stay):
# - charts/arrconf/files/arrconf.yml (username: ''/password: '' placeholders STAY)
# - charts/arrconf/templates/cronjob.yaml
# - charts/arrconf/Chart.yaml
# - any other my-kluster file

# CronJob suspend command (already applied since 2026-05-09T06:48:11Z — DO NOT change here):
# kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'  # MUST be "true" pre+post

# ArgoCD verification (Plan 05 SUMMARY pattern — argocd CLI may be unavailable, kubectl-on-Application is dispositive):
# kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status} {.status.health.status} rev={.status.sync.revision}'
# Expected: "Synced Healthy rev=<MERGE_COMMIT_SHA>"

# Live CronJob image verification (post-sync):
# kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'
# Expected: ghcr.io/tom333/arr-stack-arrconf:0.1.5

# Sequencing constraint — CronJob STAYS suspended through this plan:
# Plan 12 owns the unsuspend (after operator UI password re-entry).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| my-kluster git → ArgoCD selfHeal → cluster CronJob spec | A merged PR triggers ArgoCD sync within ~1 min; the live CronJob spec.image updates without operator intervention. |
| my-kluster PR diff → reviewer | The diff is one line. Reviewer must verify atomic-diff invariant (no chart values touched, no template touched). |
| Cluster CronJob (suspended) → Sonarr API | NO TRAFFIC: suspended state means no scheduled execution. Plan 12 unsuspends. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-11-01 | T (Tampering) | PR scope creep | mitigate | Pre-commit verification: `git diff main -- charts/` MUST show changes ONLY in `charts/arrconf/values.yaml` AND those changes MUST be exactly the line `tag: "0.1.4"` → `tag: "0.1.5"`. Acceptance criterion enforces this via grep+wc-l. The PR title contains "atomic" + "0.1.5" so the reviewer can spot any expansion. |
| T-02.2-11-02 | D (Denial of service / cluster recovery race) | CronJob unsuspend timing | mitigate | This plan does NOT unsuspend the CronJob. Pre-flight + post-merge checks both verify `spec.suspend == "true"`. Plan 12 owns the unsuspend AFTER operator UI password re-entry. If a future contributor reads this plan + Plan 05 and assumes Plan 11 should unsuspend (Plan 05's pattern), the explicit must_haves and acceptance criteria block it. |
| T-02.2-11-03 | I (Information disclosure) | values.yaml diff | mitigate | The diff is `tag: "0.1.4"` → `tag: "0.1.5"`. No credentials, no API keys, no hostnames. Renovate annotation (line above `repository:`) untouched. CLAUDE.md "Annotations Renovate (CRITIQUE)" preserved. |
| T-02.2-11-04 | E (Elevation of privilege) | ArgoCD sync window | accept | Same accepted scope as Plan 05: `automated.selfHeal: true` is the established sync pattern. The CronJob suspend semantics survive ArgoCD sync (suspend is a `.spec.suspend: true` field; sync does not toggle it unless the source values do — confirmed by Plan 05 SUMMARY 2026-05-09T05:37 merge → 06:48 suspend was external). |
| T-02.2-11-05 | T (Tampering) | git stash side-effects | mitigate | Phase 2 STATE.md notes 2 my-kluster stashes (pre-arrconf-Phase2-tracked + pre-arrconf-Phase2). Pre-flight: `cd ~/projets/perso/my-kluster && git stash list` — record the count. Post-PR: same count expected. If Plan 11 accidentally pops a stash, that's a pollution risk (would touch unrelated files). Acceptance criterion enforces stash-count invariance. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 11.1: Open atomic my-kluster PR (image.tag bump 0.1.4 → 0.1.5)</name>
  <files>
    my-kluster/charts/arrconf/values.yaml
  </files>
  <read_first>
    - /home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml (current state — verify line 4 is `tag: "0.1.4"`)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-05-PLAN.md (Plan 05 precedent — same shape; THIS plan deviates by NOT unsuspending the CronJob)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-05-SUMMARY.md (Plan 05 outcome: PR #1371 merged 2026-05-09T05:37:20Z; ArgoCD selfHeal Synced+Healthy on revision matching merge commit)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md "Cross-repo coupling: arr-stack tag → CI → GHCR → my-kluster PR → ArgoCD → cluster smoke" + "Suspend CronJob during hotfix windows"
    - /home/moi/projets/perso/arr-stack/CLAUDE.md "Annotations Renovate (CRITIQUE)" + "Ne pas déployer directement"
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/deferred-items.md §D-02.2-AUTH-REGRESSION required-action #3 + #4 (sequencing constraint)
  </read_first>
  <action>
    Step 1 — Pre-flight in arr-stack (verify Plan 10 GHCR image is pullable; CronJob suspended):
        cd /home/moi/projets/perso/arr-stack
        git ls-remote --tags origin | grep -q 'refs/tags/v0.1.5$' || { echo "ERROR: v0.1.5 tag not on origin — Plan 10 incomplete"; exit 1; }
        docker logout ghcr.io || true
        docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.5 || { echo "ERROR: v0.1.5 not pullable from GHCR"; exit 1; }
        SUSPEND=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}' 2>/dev/null || echo "")
        [ "$SUSPEND" = "true" ] || { echo "ERROR: CronJob NOT suspended (got: '$SUSPEND') — Phase 2.2 hotfix sequencing violated"; exit 1; }

    Step 2 — Pre-flight in my-kluster (snapshot stash state for T-02.2-11-05):
        cd /home/moi/projets/perso/my-kluster
        git fetch origin
        STASH_COUNT_PRE=$(git stash list | wc -l)
        echo "my-kluster stash count pre-PR: $STASH_COUNT_PRE"
        if [ -n "$(git status --porcelain)" ]; then
          echo "ERROR: my-kluster working tree dirty"
          git status
          exit 1
        fi
        git checkout main && git pull origin main
        CURRENT_TAG=$(grep '^[[:space:]]*tag:[[:space:]]*' charts/arrconf/values.yaml | head -1)
        echo "$CURRENT_TAG" | grep -qF '"0.1.4"' || { echo "ERROR: expected tag: \"0.1.4\" in values.yaml, got: $CURRENT_TAG"; exit 1; }

    Step 3 — Create branch + apply atomic edit:
        cd /home/moi/projets/perso/my-kluster
        BRANCH="hotfix/arrconf-v0.1.5"
        git checkout -b "$BRANCH"
        sed -i 's|^\([[:space:]]*\)tag: "0.1.4"$|\1tag: "0.1.5"|' charts/arrconf/values.yaml

        DIFF_FILES=$(git diff --name-only)
        [ "$DIFF_FILES" = "charts/arrconf/values.yaml" ] || { echo "ERROR: T-02.2-11-01 — diff touches other files: $DIFF_FILES"; exit 1; }

        DIFF_LINE_ADDS=$(git diff -- charts/arrconf/values.yaml | grep -c '^+[^+]')
        DIFF_LINE_DELS=$(git diff -- charts/arrconf/values.yaml | grep -c '^-[^-]')
        [ "$DIFF_LINE_ADDS" = "1" ] && [ "$DIFF_LINE_DELS" = "1" ] || { echo "ERROR: not exactly +1/-1; got +$DIFF_LINE_ADDS / -$DIFF_LINE_DELS"; exit 1; }

        NEW_TAG=$(grep '^[[:space:]]*tag:[[:space:]]*' charts/arrconf/values.yaml | head -1)
        echo "$NEW_TAG" | grep -qF '"0.1.5"' || { echo "ERROR: bump did not produce 0.1.5"; exit 1; }

        rtk proxy grep -B 1 'tag: "0.1.5"' charts/arrconf/values.yaml | grep -q 'renovate: image=' || {
          echo "ERROR: Renovate annotation lost above tag: line"
          exit 1
        }

        for unchanged in charts/arrconf/files/arrconf.yml charts/arrconf/templates/cronjob.yaml charts/arrconf/Chart.yaml; do
          if git diff --name-only | grep -qF "$unchanged"; then
            echo "ERROR: $unchanged was modified — atomic diff violated"
            exit 1
          fi
        done

    Step 4 — Commit + push branch:
        cd /home/moi/projets/perso/my-kluster
        git add charts/arrconf/values.yaml
        git commit -m "hotfix(arrconf): bump image.tag 0.1.4 → 0.1.5 (D-02.2-AUTH-REGRESSION atomic, CronJob STAYS suspended pending Plan 12 operator recovery)"
        git push -u origin "$BRANCH"

    Step 5 — Open PR via gh:
        cd /home/moi/projets/perso/my-kluster
        PR_URL=$(gh pr create \
          --title "hotfix(arrconf): bump image.tag 0.1.4 → 0.1.5 (D-02.2-AUTH-REGRESSION atomic)" \
          --body "$(cat <<'EOF'
## Summary

- Bump `charts/arrconf/values.yaml` `image.tag: "0.1.4" → "0.1.5"` (atomic single-line diff per D-37)
- v0.1.5 closes the credential-overwrite regression D-02.2-AUTH-REGRESSION discovered by arr-stack Plan 02.2-06 visual UAT (2026-05-09)

## Cross-repo links

- arr-stack tag: `v0.1.5` (annotated, target commit contains both Plan 08 GREEN fix and Plan 09 ADR-8.1)
- GHCR image: `ghcr.io/tom333/arr-stack-arrconf:0.1.5` (verified anonymously pullable)
- arr-stack ADR: `spec.md` §11 ADR-8.1 (refinement of ADR-8 documenting v0.1.5 mitigation)
- arr-stack incident: `.planning/phases/02.2-v0-1-4-forcesave-fix/deferred-items.md` §D-02.2-AUTH-REGRESSION

## What v0.1.5 changes (in arr-stack code, not chart)

- `tools/arrconf/arrconf/differ.py:merge_fields_for_put` now consults cluster-side `privacy` field metadata. Entries whose `privacy in ("password", "userName")` are OMITTED from the PUT body. Sonarr preserves stored values when fields are absent — safer than passing through the API mask `"********"` which v0.1.4's `?forceSave=true` would accept verbatim.
- New structlog event `merge_field_omitted_credential` ({name, privacy} — metadata only)
- Plan 02 forceSave URL-param contract UNCHANGED (3 tests still pass) — `?forceSave=true` continues to apply for non-credential drift correction. Two-layer defense.

## What this PR does NOT change

- `charts/arrconf/files/arrconf.yml` (placeholders STAY per Phase 2.1 PR4 hotfix decision — D-02.1-05; v0.1.5 fix is in code, not chart values)
- `charts/arrconf/templates/cronjob.yaml`
- `charts/arrconf/Chart.yaml`
- Any other my-kluster file

## CRITICAL — cluster recovery sequencing

The CronJob `arrconf` in selfhost is SUSPENDED (since 2026-05-09T06:48:11Z, D-02.2-AUTH-REGRESSION mitigation) and MUST stay suspended through this PR's merge + ArgoCD sync window.

After this PR merges and ArgoCD syncs:
1. CronJob spec.image will be `ghcr.io/tom333/arr-stack-arrconf:0.1.5` ✅
2. CronJob `.spec.suspend` will still be `true` ✅
3. **Operator must manually re-enter the qBit password in Sonarr UI** (Settings → Download Clients → qBittorrent → Password field → Save) BEFORE the next reconcile fires. The cluster's current stored password is the literal `"********"` (the regression artifact); a v0.1.5 reconcile fired RIGHT NOW would still see the broken state.
4. After step 3 succeeds AND Sonarr UI "Test" button returns success, run `kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":false}}'` to unsuspend.
5. Force a smoke job to verify v0.1.5 + recovered credential work end-to-end.

Steps 3–5 are owned by **arr-stack Plan 02.2-12** (cluster recovery checkpoint + credential-aware drift demo dispositive).

## Test plan

- [ ] PR diff is exactly one line in exactly one file (charts/arrconf/values.yaml)
- [ ] Renovate annotation preserved on the line above `repository:`
- [ ] After merge: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'` returns `ghcr.io/tom333/arr-stack-arrconf:0.1.5`
- [ ] After merge: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returns `"true"` (CronJob STAYS suspended)
- [ ] After merge: `kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status} {.status.health.status}'` returns `Synced Healthy`
- [ ] DO NOT unsuspend in this PR — that's Plan 02.2-12's job
EOF
)" \
          --base main \
          --head "$BRANCH")
        echo "PR opened: $PR_URL"
        echo "$PR_URL" > /tmp/plan-11-pr-url.txt

    Step 6 — Stash invariance check (T-02.2-11-05):
        cd /home/moi/projets/perso/my-kluster
        STASH_COUNT_POST=$(git stash list | wc -l)
        [ "$STASH_COUNT_POST" = "$STASH_COUNT_PRE" ] || { echo "ERROR: T-02.2-11-05 — stash count changed"; exit 1; }
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/my-kluster && git fetch origin && PR_URL=$(cat /tmp/plan-11-pr-url.txt 2>/dev/null) && [ -n "$PR_URL" ] && PR_NUM=$(echo "$PR_URL" | grep -oE '[0-9]+$') && gh pr view "$PR_NUM" --json state,changedFiles,additions,deletions -q '.changedFiles == 1 and .additions == 1 and .deletions == 1' | grep -q true && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}' 2>/dev/null)" = "true" ]</automated>
  </verify>
  <acceptance_criteria>
    - PR exists in my-kluster: `gh pr view <N> --json state -q '.state'` returns `OPEN` or `MERGED`
    - PR diff: exactly 1 changedFile, 1 addition, 1 deletion (T-02.2-11-01)
    - The single changed file is `charts/arrconf/values.yaml`
    - Renovate annotation `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` is on the line directly above `repository:` (CLAUDE.md gate)
    - `charts/arrconf/files/arrconf.yml`, `charts/arrconf/templates/cronjob.yaml`, `charts/arrconf/Chart.yaml` are NOT in the PR's changedFiles list
    - my-kluster stash count unchanged pre vs post (T-02.2-11-05)
    - CronJob `arrconf` `.spec.suspend == "true"` throughout (T-02.2-11-02)
    - PR URL written to `/tmp/plan-11-pr-url.txt` for Task 11.2 to consume
  </acceptance_criteria>
  <done>PR open and ready for operator review. Task 11.2 awaits operator merge.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 11.2: Operator review + merge of my-kluster PR (DO NOT unsuspend CronJob)</name>
  <files>
  </files>
  <action>
    This is a checkpoint:human-action task — operator MUST complete the steps in &lt;how-to-verify&gt; below manually (PR review/merge requires human GitHub UI action, ArgoCD sync verification requires cluster credentials).

    No autonomous execution by Claude. The orchestrator pauses on this task; the operator follows the &lt;how-to-verify&gt; steps and signals via &lt;resume-signal&gt; when complete.

    Critical sequencing constraint: the CronJob `arrconf` in selfhost MUST stay suspended throughout this task and through Task 11.3. Plan 12 (Wave 10) owns the unsuspend operation, ONLY after the operator UI password re-entry + Sonarr Test API HTTP 200 dispositive succeeds. If the CronJob gets unsuspended during this checkpoint window (e.g. ArgoCD inadvertently toggling it via a values.yaml drift), the operator must immediately re-suspend per the steps in &lt;how-to-verify&gt; #9.
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack &amp;&amp; PR_URL=$(cat /tmp/plan-11-pr-url.txt 2>/dev/null) &amp;&amp; PR_NUM=$(echo "$PR_URL" | grep -oE '[0-9]+$') &amp;&amp; gh pr view "$PR_NUM" --repo tom333/my-kluster --json state -q '.state' | grep -qE '^(MERGED)$' &amp;&amp; [ "$(kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status}')" = "Synced" ] &amp;&amp; [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}')" = "ghcr.io/tom333/arr-stack-arrconf:0.1.5" ] &amp;&amp; [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')" = "true" ]</automated>
  </verify>
  <done>Operator has merged the PR; ArgoCD shows Synced + Healthy on the merge commit SHA; CronJob spec.image is `ghcr.io/tom333/arr-stack-arrconf:0.1.5`; CronJob spec.suspend is still `true`. Task 11.3 captures this dispositive in evidence.</done>
  <what-built>
    Atomic single-line PR in my-kluster bumping `charts/arrconf/values.yaml` `image.tag` from `"0.1.4"` to `"0.1.5"`. PR URL captured in `/tmp/plan-11-pr-url.txt`. All automated checks (atomic diff, Renovate annotation preserved, CronJob suspended, stash invariant) PASSED in Task 11.1.
  </what-built>
  <how-to-verify>
    1. Open the PR URL printed by Task 11.1 (`cat /tmp/plan-11-pr-url.txt`).
    2. Verify the "Files changed" tab shows EXACTLY one file (`charts/arrconf/values.yaml`) with EXACTLY one line changed (`tag: "0.1.4"` → `tag: "0.1.5"`).
    3. Verify the PR title is `hotfix(arrconf): bump image.tag 0.1.4 → 0.1.5 (D-02.2-AUTH-REGRESSION atomic)`.
    4. Verify the PR body contains the "CRITICAL — cluster recovery sequencing" section explaining steps 1–5.
    5. Verify CronJob `arrconf` is still suspended (no surprise reconcile during your review window):
       ```bash
       kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'   # MUST output: true
       ```
    6. **MERGE the PR** via your preferred method (squash or merge-commit per Plan 05 LEARNINGS — both honored as non-deviation). Do NOT close-without-merge.
    7. After merge: WAIT for ArgoCD sync (~1–2 min). Verify ArgoCD picked up the merge:
       ```bash
       kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status} {.status.health.status} rev={.status.sync.revision}'
       # Expected: Synced Healthy rev=<MERGE_COMMIT_SHA>
       ```
       If `argocd` CLI is available and you prefer it, `argocd app get arrconf` works; both are dispositive (Plan 05 LEARNINGS).
    8. Verify the CronJob spec was updated by ArgoCD selfHeal:
       ```bash
       kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'
       # Expected: ghcr.io/tom333/arr-stack-arrconf:0.1.5
       ```
    9. **CONFIRM CronJob STAYS SUSPENDED** (this is the load-bearing check — Plan 12 owns the unsuspend):
       ```bash
       kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'   # MUST output: true (still)
       ```
       If the CronJob got unsuspended somehow (e.g. ArgoCD source values were edited inadvertently — they shouldn't have been), IMMEDIATELY re-suspend:
       ```bash
       kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":true}}'
       ```
       and report this anomaly in your `approved` signal so it's recorded.
    10. **DO NOT unsuspend the CronJob.** Plan 12 sequences the operator UI password re-entry FIRST, then unsuspend. If you unsuspend now, the cluster's current stored password is still the literal `"********"` (the regression), and the next v0.1.5 reconcile would observe the broken cluster state — defeating the recovery sequence.
  </how-to-verify>
  <resume-signal>
    Type "approved" once: PR merged, ArgoCD Synced+Healthy on the merge commit SHA, CronJob spec.image is 0.1.5, AND CronJob spec.suspend is still true.

    OR describe what went wrong (e.g. "merge conflict on main", "ArgoCD stuck pending", "CronJob got unsuspended unexpectedly — re-suspended manually") so it gets recorded in the SUMMARY.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 11.3: Post-merge automated dispositive (CronJob image=0.1.5 + spec.suspend=true)</name>
  <files>
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-05-SUMMARY.md "live CronJob image=ghcr.io/tom333/arr-stack-arrconf:0.1.4 (DISPOSITIVE)" (Plan 05 dispositive shape — same logic, different version)
  </read_first>
  <action>
    Step 1 — Capture the dispositive cluster state. Verify all three load-bearing properties:
        # (1) ArgoCD synced on the new merge commit:
        ARGO_STATE=$(kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status} {.status.health.status} rev={.status.sync.revision}')
        echo "ArgoCD: $ARGO_STATE"
        echo "$ARGO_STATE" | grep -q '^Synced Healthy' || { echo "ERROR: ArgoCD not Synced+Healthy"; exit 1; }

        # (2) CronJob spec.image bumped to 0.1.5:
        CRON_IMAGE=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}')
        echo "CronJob image: $CRON_IMAGE"
        [ "$CRON_IMAGE" = "ghcr.io/tom333/arr-stack-arrconf:0.1.5" ] || { echo "ERROR: image not 0.1.5"; exit 1; }

        # (3) CronJob STAYS suspended (T-02.2-11-02 dispositive):
        SUSPEND=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')
        echo "CronJob suspend: $SUSPEND"
        [ "$SUSPEND" = "true" ] || {
          echo "ERROR: CronJob unsuspended during merge window — DEFENDED-AGAINST RACE OCCURRED"
          echo "Re-suspending immediately:"
          kubectl patch cronjob arrconf -n selfhost -p '{"spec":{"suspend":true}}'
          echo "Plan 12 must investigate why this happened before continuing"
          exit 1
        }

        # (4) No fired Jobs from the CronJob since the merge (suspended state — should be 0):
        FIRED=$(kubectl get jobs -n selfhost -l job-name --field-selector status.successful=1 -o jsonpath='{.items[?(@.metadata.ownerReferences[0].name=="arrconf")].metadata.name}' 2>/dev/null || echo "")
        # The above selector is fragile across kubectl versions; fallback: check job count by owner reference name:
        TOTAL_JOBS=$(kubectl get jobs -n selfhost -o json | jq -r '.items[] | select(.metadata.ownerReferences[]?.name == "arrconf") | .metadata.name' 2>/dev/null | wc -l)
        echo "Jobs owned by arrconf CronJob: $TOTAL_JOBS (informational; suspend should keep this at the historical count)"

    Step 2 — Record the dispositive snapshot for Plan 12:
        DISPO_FILE=".planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-merge-v0.1.5-dispositive-$(date +%F).log"
        cd /home/moi/projets/perso/arr-stack
        mkdir -p .planning/phases/02.2-v0-1-4-forcesave-fix/evidence

        cat > "$DISPO_FILE" <<EOF
=== Plan 02.2-11 Post-Merge Dispositive ===
Timestamp: $(date -u +%FT%TZ)
Merge commit:  $(echo "$ARGO_STATE" | grep -oE 'rev=[a-f0-9]+' | cut -d= -f2)

ArgoCD state: $ARGO_STATE
CronJob spec.image: $CRON_IMAGE
CronJob spec.suspend: $SUSPEND
Jobs-owned-by-arrconf count (info): $TOTAL_JOBS

Sequencing dispositive:
  - v0.1.5 image deployed: YES
  - CronJob suspended (sequencing intact): YES
  - Plan 12 next: operator UI password re-entry → unsuspend → credential-aware drift demo

This file is consumed by Plan 12 as anchor for the "before-unsuspend" baseline.
EOF
        git add "$DISPO_FILE"
        git commit -m "evidence(02.2-11): post-merge dispositive — v0.1.5 deployed, CronJob STILL suspended (Plan 12 handoff anchor)"
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && [ "$(kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status}')" = "Synced" ] && [ "$(kubectl get application arrconf -n argocd -o jsonpath='{.status.health.status}')" = "Healthy" ] && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}')" = "ghcr.io/tom333/arr-stack-arrconf:0.1.5" ] && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}')" = "true" ] && DISPO_FILE=$(ls -t .planning/phases/02.2-v0-1-4-forcesave-fix/evidence/post-merge-v0.1.5-dispositive-*.log 2>/dev/null | head -1) && [ -n "$DISPO_FILE" ] && grep -q 'CronJob spec.suspend: true' "$DISPO_FILE" && git log -1 --pretty=%s | grep -q 'evidence(02.2-11): post-merge dispositive'</automated>
  </verify>
  <acceptance_criteria>
    - ArgoCD Application `arrconf`: `status.sync.status == "Synced"`, `status.health.status == "Healthy"`, `status.sync.revision` matches the my-kluster merge commit
    - CronJob `arrconf` `spec.jobTemplate.spec.template.spec.containers[0].image == "ghcr.io/tom333/arr-stack-arrconf:0.1.5"`
    - CronJob `arrconf` `spec.suspend == "true"` (T-02.2-11-02 dispositive — Plan 12 owns the unsuspend)
    - Evidence file `evidence/post-merge-v0.1.5-dispositive-YYYY-MM-DD.log` committed in arr-stack with the four dispositive lines (ArgoCD state, image, suspend, jobs-owned count)
    - Commit recorded with subject `evidence(02.2-11): post-merge dispositive — v0.1.5 deployed, CronJob STILL suspended (Plan 12 handoff anchor)`
  </acceptance_criteria>
  <done>v0.1.5 image is live in cluster CronJob spec; CronJob remains suspended; Plan 12 has its handoff anchor file. The cluster recovery race has NOT occurred.</done>
</task>

</tasks>

<verification>
- my-kluster PR atomic (1 file, 1 add, 1 del); merged
- ArgoCD Synced + Healthy on merge commit SHA
- CronJob spec.image = 0.1.5
- CronJob spec.suspend = true (sequencing intact through merge window — T-02.2-11-02 dispositive)
- Evidence file committed in arr-stack as Plan 12 handoff
- No my-kluster stash drift (T-02.2-11-05)
- Renovate annotation preserved (CLAUDE.md gate)
</verification>

<success_criteria>
- [ ] my-kluster PR opened, reviewed, merged
- [ ] ArgoCD Synced + Healthy on merge commit
- [ ] CronJob image = 0.1.5
- [ ] CronJob remains suspended (do NOT unsuspend in this plan)
- [ ] Post-merge dispositive evidence file committed
- [ ] Cluster recovery race prevented (sequencing intact)
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-11-SUMMARY.md` recording:
- PR URL + merge commit SHA
- ArgoCD revision + sync timestamp
- Live CronJob image + suspend state
- Stash invariance verification (pre/post counts equal)
- Evidence file path
- Forward link to Plan 12 (cluster recovery checkpoint + credential-aware dispositive)
- Note any deviations from Plan 05 (the sibling that DID unsuspend) and confirm THIS plan correctly did NOT
</output>
