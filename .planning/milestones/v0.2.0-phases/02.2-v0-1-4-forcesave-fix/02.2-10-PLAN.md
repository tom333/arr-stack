---
phase: 02.2
plan: 10
type: execute
wave: 8
depends_on:
  - 02.2-08
  - 02.2-09
files_modified: []
gap_closure: true
autonomous: false
requirements: []
must_haves:
  truths:
    - "Annotated git tag `v0.1.5` exists in arr-stack and is pushed to origin (D-37 atomic single-tag pattern)"
    - "GitHub Actions workflow `arrconf-image.yml` runs against the tagged commit and successfully publishes `ghcr.io/tom333/arr-stack-arrconf:0.1.5`"
    - "GHCR public anonymous pull works: `docker logout ghcr.io && docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.5` succeeds with no auth"
    - "Pre-existing v0.1.4 image is NOT clobbered (semver patch bump, additive)"
    - "Tagged commit contains BOTH the GREEN fix (Plan 08) AND the ADR-8.1 refinement (Plan 09) — verifiable via the STRICTER PAIR check (plan-checker iteration 1 WARNING #9 fix; the count >= 2 was satisfiable by Plan 08 alone since both Plan 08 commits contain '02.2-08'): BOTH `git log v0.1.5 --oneline | grep -q "fix(02.2-08):.*GREEN"` AND `git log v0.1.5 --oneline | grep -q "docs(spec): add ADR-8.1"` exit 0"
    - "Tagged commit does NOT contain any new my-kluster diff (this plan modifies arr-stack only)"
  artifacts:
    - path: "git tag v0.1.5 (annotated, pushed)"
      provides: "Triggers the GitHub Actions image build → GHCR publish chain (D-37 atomic single-tag pattern)"
    - path: "ghcr.io/tom333/arr-stack-arrconf:0.1.5"
      provides: "Container image consumed by my-kluster Plan 11 image.tag bump"
  key_links:
    - from: "git tag v0.1.5"
      to: "GitHub Actions workflow run"
      via: ".github/workflows/arrconf-image.yml on tag push (existing workflow, unchanged)"
      pattern: "arrconf-image.yml"
    - from: "GitHub Actions workflow"
      to: "GHCR ghcr.io/tom333/arr-stack-arrconf:0.1.5"
      via: "docker buildx + push to GHCR"
      pattern: "ghcr.io/tom333/arr-stack-arrconf"
    - from: "ghcr.io/tom333/arr-stack-arrconf:0.1.5"
      to: "Anonymous public pull (ADR-3)"
      via: "GHCR package visibility = public"
      pattern: "docker pull.*0.1.5"
---

<objective>
Release v0.1.5: cut an annotated git tag whose target commit contains BOTH the credential-merge fix (Plan 08 GREEN) AND the ADR-8.1 refinement (Plan 09); push to origin; wait for GitHub Actions to build + push the image to GHCR; verify anonymous public pull works. This plan is a near-byte-clone of Plan 04 (v0.1.4 release) — the cross-repo coupling sequence's first half (arr-stack tag → CI → GHCR) per D-37 atomic single-tag pattern carried forward from Phase 2.1.

The tag is the dispositive trigger: only after GHCR public pull verifies (`docker logout ghcr.io && docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.5`) does Plan 11 open the my-kluster PR. This ordering prevents the bad intermediate state where the cluster runs the new image tag but the image isn't actually pullable yet — same constraint as Plan 04 enforced for v0.1.4.

**Critical sequencing constraint** (gap_summary anti-race): the CronJob `arrconf` in selfhost is currently SUSPENDED (since 2026-05-09T06:48:11Z, D-02.2-AUTH-REGRESSION mitigation). It MUST stay suspended throughout this plan. This plan does NOT touch the cluster — only arr-stack git + GHCR. The cluster recovery sequencing (deploy v0.1.5 → operator re-enters password → unsuspend CronJob) is owned by Plans 11 + 12.

Purpose: Ship the v0.1.5 image to a public registry as the unit consumed by Plan 11 (my-kluster image.tag bump 0.1.4 → 0.1.5).

Output: Annotated git tag `v0.1.5` pushed to origin; GHCR image `ghcr.io/tom333/arr-stack-arrconf:0.1.5` exists, public, anonymously pullable.

Wave 8 — depends on Plan 08 (GREEN fix commit on main) and Plan 09 (ADR-8.1 commit on main) both being merged. CI runs against the tagged commit which must include both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/moi/projets/perso/arr-stack/.planning/PROJECT.md
@/home/moi/projets/perso/arr-stack/.planning/ROADMAP.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-PATTERNS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-04-PLAN.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-04-SUMMARY.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md
@/home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-03-PLAN.md
@/home/moi/projets/perso/arr-stack/CLAUDE.md
@/home/moi/projets/perso/arr-stack/.github/workflows/arrconf-image.yml

<interfaces>
<!-- D-37 atomic single-tag pattern (Phase 2.1 carry-forward, validated again by Plan 04). -->

# Required pre-conditions on origin/main:
# - Plan 08 GREEN commit landed (subject: fix(02.2-08): omit privacy=password|userName ...)
# - Plan 09 ADR-8.1 commit landed (subject: docs(spec): add ADR-8.1 — v0.1.5 credential-merge refinement ...)
# - Working tree clean

# Tag command (Plan 04 precedent — same shape, different version):
git tag -a v0.1.5 -m "<structured message>"
git push origin v0.1.5

# CI verification:
gh run list --workflow=arrconf-image.yml --limit 5
gh run watch <run-id>

# GHCR public-pull verification (anonymous):
docker logout ghcr.io
docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.5

# Plan 04 pattern: GHCR anonymous manifest probe must use OCI index Accept type:
curl -fsSL \
  -H "Accept: application/vnd.oci.image.index.v1+json" \
  -H "Accept: application/vnd.docker.distribution.manifest.v2+json" \
  "https://ghcr.io/v2/tom333/arr-stack-arrconf/manifests/0.1.5"

# CronJob in selfhost MUST stay suspended throughout (do NOT unsuspend in this plan):
kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'   # expected: "true"
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local arr-stack worktree → origin GitHub | Git push of an annotated tag triggers a CI workflow that builds + pushes a public OCI image. The tag is reachable via the public repo HEAD. |
| GitHub Actions runner → ghcr.io | CI uses GITHUB_TOKEN (workflow-scoped) to push the image. No long-lived secret leaks. |
| ghcr.io public registry → any consumer (my-kluster pull, dev workstation) | Anonymous pull is the contract per ADR-3. |
| arrconf CronJob in selfhost → arr-stack git | NO TRANSITIVE TRUST: cluster is suspended; this plan must not unsuspend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.2-10-01 | T (Tampering) | annotated tag v0.1.5 | mitigate | The tag's target commit is checked: it must include BOTH `fix(02.2-08):` and `docs(spec): add ADR-8.1` in its ancestry. Verified via `git log v0.1.5 --oneline | grep -c '02.2-08\|02.2-09'` >= 2 BEFORE pushing. CLAUDE.md "Ne pas amender un tag de release publié" — refuse if v0.1.5 already exists. |
| T-02.2-10-02 | I (Information disclosure) | GHCR image | accept | Image content is `tools/arrconf/` Python source + dependencies. No secrets baked in (verified by Phase 1 GHCR public toggle in 02-02-SUMMARY.md). The same scope applies to v0.1.5 — same Dockerfile, same source-tree exposure. |
| T-02.2-10-03 | D (Denial of service) | cluster recovery sequencing | mitigate | The CronJob in selfhost stays suspended throughout. This plan does NOT trigger an ArgoCD sync; my-kluster's `image.tag` is still `0.1.4` after this plan ships v0.1.5 image. The cluster will not pull v0.1.5 until Plan 11 merges the my-kluster image.tag bump. Verified pre-flight: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` MUST equal `"true"`. |
| T-02.2-10-04 | E (Elevation of privilege) | tag-push permissions | accept | The user has push access to origin (verified by Plan 04 release succeeding 2026-05-09 CI run 25590328939). No new permission required for v0.1.5. |
| T-02.2-10-05 | I (Information disclosure) | tag annotation message | mitigate | The structured message (Step 3 below) cites ADR-8.1, D-02.2-AUTH-REGRESSION, and the new audit event name — NO real credentials, NO real cluster URLs, NO API keys. Pre-flight grep on the message body: `echo "$TAG_MSG" | grep -qE '<sonarr_api_key>|feed603a98de65f6e580d4bfa71ada68'` MUST return 1 (no match). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 10.1: Verify Plan 08 + 09 on main, cut annotated v0.1.5 tag, push, verify CI run starts</name>
  <files>
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.github/workflows/arrconf-image.yml (the workflow that builds and pushes on tag push — confirm trigger pattern `on: push: tags: ['v*']` and image-tag stripping logic)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-04-PLAN.md (v0.1.4 release precedent — same shape)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-04-SUMMARY.md (v0.1.4 release outcome — D-37 pattern observed; OCI index Accept type pattern recorded)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.1-field-merge-fix/02.1-LEARNINGS.md "Cross-repo coupling: arr-stack tag → CI → GHCR → my-kluster PR → ArgoCD → cluster smoke"
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-CONTEXT.md "Decisions §Inherited from Phase 2.1 §D-37"
    - /home/moi/projets/perso/arr-stack/CLAUDE.md "Ne pas amender un tag de release publié"
  </read_first>
  <action>
    Step 1 — Verify Plans 08 + 09 are merged into main:
        cd /home/moi/projets/perso/arr-stack
        git checkout main && git pull origin main

        # Confirm Plan 08 GREEN commit landed:
        git log --oneline | grep -q 'fix(02.2-08):.*omit privacy.*GREEN' || { echo "ERROR: Plan 08 GREEN commit not on main"; exit 1; }

        # Confirm Plan 09 ADR-8.1 commit landed:
        git log --oneline | grep -q 'docs(spec): add ADR-8.1' || { echo "ERROR: Plan 09 ADR-8.1 commit not on main"; exit 1; }

        # Confirm clean working tree:
        if [ -n "$(git status --porcelain)" ]; then
          echo "ERROR: dirty working tree"
          git status
          exit 1
        fi

        # Sanity: tests still green at HEAD:
        cd tools/arrconf
        uv run pytest -q 2>&1 | tail -5
        uv run pytest -q 2>&1 | grep -qE "passed" || { echo "ERROR: tests failing on main HEAD"; exit 1; }
        cd /home/moi/projets/perso/arr-stack

    Step 2 — Verify cluster CronJob remains suspended (T-02.2-10-03 sequencing):
        SUSPEND=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}' 2>/dev/null || echo "")
        if [ "$SUSPEND" != "true" ]; then
          echo "ERROR: CronJob arrconf is NOT suspended (got: '$SUSPEND'). Phase 02.2 hotfix sequencing requires CronJob suspended throughout."
          echo "Run: kubectl patch cronjob arrconf -n selfhost -p '{\"spec\":{\"suspend\":true}}'"
          exit 1
        fi
        echo "CronJob suspend state: $SUSPEND (correct — stays suspended through Plan 11)"

    Step 3 — Confirm v0.1.5 tag does NOT already exist (CLAUDE.md: never amend a published tag):
        if git tag -l 'v0.1.5' | grep -q .; then
          echo "ERROR: v0.1.5 tag already exists — refuse to amend (CLAUDE.md)"
          exit 1
        fi
        # Also fetch from origin in case it was pushed elsewhere:
        git fetch --tags origin
        if git tag -l 'v0.1.5' | grep -q .; then
          echo "ERROR: v0.1.5 tag exists on origin — refuse to amend"
          exit 1
        fi

    Step 4 — Cut the annotated tag with a structured message:
        TAG_MSG=$(cat <<'EOF'
v0.1.5 — credential-merge refinement (closes D-02.2-AUTH-REGRESSION)

What changed (additive over v0.1.4):
- merge_fields_for_put now consults cluster-side privacy field metadata.
  Entries whose privacy is "password" or "userName" are OMITTED from the PUT
  body entirely (Sonarr preserves stored values when fields are absent —
  safer than passing through the API mask "********" which v0.1.4's
  ?forceSave=true would accept verbatim).
- New structured log event merge_field_omitted_credential ({name, privacy})
  for cluster audit trails. Payload is metadata-only — no credential value.
- ADR-8.1 added to spec.md §11 documenting the v0.1.5 mitigation as a
  refinement of the trusted-controller stance (ADR-8 unchanged, append-only).

Why:
The Plan 02.2-06 visual UAT (2026-05-09) caught a credential-overwrite
regression that v0.1.4's automated dispositives could not detect. Sonarr's
GET-side serialization makes the regression invisible to snapshot diff: the
GET returns "********" both when the stored value is the real password AND
when the stored value is the literal mask token. Detection required a
behavioral test (Sonarr Test API → 401/403). The forensic chain:

1. Phase 2.1 helper preserves cluster's stored value at empty-YAML (D-31)
2. For privacy=password fields, cluster's stored value AS RETURNED BY GET
   is the API mask "********"
3. v0.1.4 ?forceSave=true bypasses Sonarr's pre-save validation
4. Sonarr accepts "********" as the literal password value
5. GET continues returning "********" → regression invisible to diff
6. Sonarr Test API → POSTs "********" to qBit → 401/403

v0.1.5 breaks the chain at step 1 → 2 by omitting credential fields entirely.
ADR-8 (?forceSave=true) remains valid for non-credential drift correction;
ADR-8.1 (omit by privacy) covers the credential class. Two-layer defense.

Tests (regression contracts):
- tools/arrconf/tests/test_differ.py: 4 tests (3 new in v0.1.5 + 1 audit-event
  guard); coverage on differ.py >= 95%
- tools/arrconf/tests/test_reconcilers_sonarr.py:
  test_update_omits_privacy_credential_fields_from_put_body (rewrite of the
  old test_update_preserves_redacted_credentials_in_put_body — assertions
  intentionally inverted to reflect the new contract)
- Plan 02 forceSave URL-param tests (3 tests) UNCHANGED and passing — ADR-8
  stance preserved

Cross-repo coupling sequence (D-37 atomic single-tag):
Tag v0.1.5 → CI builds GHCR image → ONE PR in my-kluster bumping
charts/arrconf/values.yaml image.tag: "0.1.4" → "0.1.5". YAML stays unchanged.
Cluster CronJob STAYS SUSPENDED through merge → ArgoCD sync (Plan 11) → operator
re-enters qBit password in Sonarr UI (Plan 12) → unsuspend CronJob (Plan 12).

See:
- spec.md §11 ADR-8.1
- .planning/phases/02.2-v0-1-4-forcesave-fix/deferred-items.md §D-02.2-AUTH-REGRESSION
- .planning/phases/02.2-v0-1-4-forcesave-fix/02.2-06-SUMMARY.md §"Operator Visual Gate FAILED"
- .planning/phases/02.2-v0-1-4-forcesave-fix/02.2-08-PLAN.md (GREEN code)
- .planning/phases/02.2-v0-1-4-forcesave-fix/02.2-09-PLAN.md (ADR-8.1)
EOF
)

        # Anti-leak audit on the tag message (T-02.2-10-05):
        if echo "$TAG_MSG" | grep -qF 'feed603a98de65f6e580d4bfa71ada68'; then
          echo "ERROR: known qBit passkey hex in tag message — refuse"
          exit 1
        fi

        git tag -a v0.1.5 -m "$TAG_MSG"

        # Verify annotation:
        git show v0.1.5 --no-patch --pretty=format:'%H%n%n%B' | head -20

    Step 5 — T-02.2-10-01 ancestry check before push:
        # The tag's target commit must contain both Plan 08 and Plan 09 commits in its history.
        TAG_COMMIT=$(git rev-list -n 1 v0.1.5)
        echo "v0.1.5 target commit: $TAG_COMMIT"

        # Plan 08 GREEN must be reachable from the tag:
        if ! git log v0.1.5 --oneline | grep -q 'fix(02.2-08):.*GREEN'; then
          echo "ERROR: Plan 08 GREEN not in tag ancestry — refuse to push"
          git tag -d v0.1.5
          exit 1
        fi

        # Plan 09 ADR-8.1 must be reachable from the tag:
        if ! git log v0.1.5 --oneline | grep -q 'docs(spec): add ADR-8.1'; then
          echo "ERROR: Plan 09 ADR-8.1 not in tag ancestry — refuse to push"
          git tag -d v0.1.5
          exit 1
        fi

    Step 6 — Push the tag and start CI:
        git push origin v0.1.5

        # Wait briefly for CI to register:
        sleep 10
        echo "Latest workflow runs:"
        gh run list --workflow=arrconf-image.yml --limit 5
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && git tag -l v0.1.5 | grep -q '^v0.1.5$' && git log v0.1.5 --oneline | grep -q 'fix(02.2-08):.*GREEN' && git log v0.1.5 --oneline | grep -q 'docs(spec): add ADR-8.1' && git ls-remote --tags origin | grep -q 'refs/tags/v0.1.5$' && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}' 2>/dev/null)" = "true" ]</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l v0.1.5` returns `v0.1.5` (tag exists locally)
    - `git ls-remote --tags origin | grep 'refs/tags/v0.1.5$'` returns 1 line (tag pushed to origin)
    - `git log v0.1.5 --oneline | grep -c 'fix(02.2-08):.*GREEN'` returns >= 1 (Plan 08 ancestry)
    - `git log v0.1.5 --oneline | grep -c 'docs(spec): add ADR-8.1'` returns >= 1 (Plan 09 ancestry)
    - Tag annotation message references ADR-8.1, D-02.2-AUTH-REGRESSION, merge_field_omitted_credential
    - `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returns `"true"` (T-02.2-10-03 — sequencing intact)
    - Anti-leak: tag message has zero matches for known qBit passkey hex `feed603a98de65f6e580d4bfa71ada68`
    - `gh run list --workflow=arrconf-image.yml --limit 5` shows a run keyed on the v0.1.5 tag (status pending or in_progress)
  </acceptance_criteria>
  <done>v0.1.5 annotated tag pushed; CI run started. Task 10.2 waits for completion + verifies GHCR pull.</done>
</task>

<task type="auto">
  <name>Task 10.2: Wait for CI completion + verify GHCR public anonymous pull (OCI index Accept type)</name>
  <files>
  </files>
  <read_first>
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-04-SUMMARY.md (v0.1.4 release outcome — same Accept type pattern; CI ran in 31s, image digest sha256:..., ~146 MiB)
    - /home/moi/projets/perso/arr-stack/.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-PATTERNS.md "GHCR anonymous manifest probe must include OCI index Accept type"
  </read_first>
  <action>
    Step 1 — Locate the v0.1.5 CI run and wait for completion:
        cd /home/moi/projets/perso/arr-stack
        # Find the run keyed on the v0.1.5 tag:
        RUN_ID=$(gh run list --workflow=arrconf-image.yml --json databaseId,headBranch,event,status,createdAt --limit 10 | jq -r '.[] | select(.event == "push" and (.headBranch == "v0.1.5" or .headBranch == "refs/tags/v0.1.5")) | .databaseId' | head -1)
        if [ -z "$RUN_ID" ]; then
          echo "ERROR: no CI run found for v0.1.5 — wait + retry"
          sleep 30
          RUN_ID=$(gh run list --workflow=arrconf-image.yml --json databaseId,headBranch,event,status --limit 10 | jq -r '.[] | select(.event == "push" and (.headBranch == "v0.1.5" or .headBranch == "refs/tags/v0.1.5")) | .databaseId' | head -1)
          [ -n "$RUN_ID" ] || { echo "ERROR: still no CI run for v0.1.5 — investigate"; exit 1; }
        fi
        echo "Watching CI run $RUN_ID"

        # Wait for completion (Plan 04 took 31s; allow 5 minutes):
        gh run watch "$RUN_ID" --exit-status --interval 5
        # gh run watch exits 0 only if the run succeeded.

    Step 2 — Verify GHCR anonymous pull works (per Plan 04 / 02.1-LEARNINGS pattern):
        # Drop any cached creds:
        docker logout ghcr.io || true

        # Anonymous OCI index probe (Plan 04 pattern, 02.2-PATTERNS records this):
        MANIFEST=$(rtk proxy curl -fsSL \
          -H "Accept: application/vnd.oci.image.index.v1+json" \
          -H "Accept: application/vnd.docker.distribution.manifest.v2+json" \
          "https://ghcr.io/v2/tom333/arr-stack-arrconf/manifests/0.1.5" 2>&1)

        echo "$MANIFEST" | rtk proxy jq -e '(.mediaType // "") | test("manifest|index")' > /dev/null || {
          echo "ERROR: manifest probe failed or unexpected mediaType:"
          echo "$MANIFEST" | head -20
          exit 1
        }

        # Anonymous docker pull:
        docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.5

        # Sanity: image digest exists:
        DIGEST=$(docker inspect ghcr.io/tom333/arr-stack-arrconf:0.1.5 --format '{{index .RepoDigests 0}}' 2>/dev/null || echo "")
        echo "Pulled image digest: $DIGEST"
        echo "$DIGEST" | grep -q '@sha256:' || { echo "ERROR: no sha256 digest"; exit 1; }

    Step 3 — Verify v0.1.4 image is NOT clobbered (must still be pullable):
        docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.4 || { echo "ERROR: v0.1.4 image lost"; exit 1; }
        echo "v0.1.4 still pullable — semver patch additive (T-02.2-10-01 sanity)"

    Step 4 — Verify CronJob still suspended (T-02.2-10-03):
        SUSPEND=$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}' 2>/dev/null || echo "")
        [ "$SUSPEND" = "true" ] || { echo "ERROR: CronJob unexpectedly unsuspended during this plan"; exit 1; }
  </action>
  <verify>
    <automated>cd /home/moi/projets/perso/arr-stack && RUN_ID=$(gh run list --workflow=arrconf-image.yml --json databaseId,headBranch,event,status,conclusion --limit 10 | jq -r '.[] | select(.event == "push" and (.headBranch == "v0.1.5" or .headBranch == "refs/tags/v0.1.5") and .conclusion == "success") | .databaseId' | head -1) && [ -n "$RUN_ID" ] && rtk proxy curl -fsSL -H "Accept: application/vnd.oci.image.index.v1+json" -H "Accept: application/vnd.docker.distribution.manifest.v2+json" "https://ghcr.io/v2/tom333/arr-stack-arrconf/manifests/0.1.5" | rtk proxy jq -e '(.mediaType // "") | test("manifest|index")' > /dev/null && [ "$(kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}' 2>/dev/null)" = "true" ]</automated>
  </verify>
  <acceptance_criteria>
    - GitHub Actions workflow run for v0.1.5 tag has `conclusion: success`
    - Anonymous GHCR manifest probe (with OCI index Accept type per Plan 04 pattern) returns a valid manifest with `mediaType` matching `manifest|index`
    - `docker logout ghcr.io && docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.5` succeeds (anonymous pull)
    - Pulled image has a sha256 digest
    - `docker pull ghcr.io/tom333/arr-stack-arrconf:0.1.4` still works (v0.1.4 not clobbered)
    - CronJob `arrconf` `.spec.suspend == true` (sequencing intact)
  </acceptance_criteria>
  <done>v0.1.5 image is anonymously pullable from GHCR. Plan 11 (my-kluster image.tag bump) can now proceed.</done>
</task>

</tasks>

<verification>
- Annotated tag `v0.1.5` exists locally + on origin
- Tag's target commit ancestry includes Plan 08 GREEN + Plan 09 ADR-8.1
- CI workflow `arrconf-image.yml` ran with conclusion=success
- GHCR image `ghcr.io/tom333/arr-stack-arrconf:0.1.5` is anonymously pullable (OCI index Accept type)
- v0.1.4 image still pullable (semver additive)
- CronJob `arrconf` remained suspended throughout (T-02.2-10-03 sequencing)
- No my-kluster diff in this plan
</verification>

<success_criteria>
- [ ] Tag `v0.1.5` exists locally + on origin
- [ ] Tag annotation references ADR-8.1, D-02.2-AUTH-REGRESSION, merge_field_omitted_credential
- [ ] CI run conclusion = success
- [ ] GHCR image is public + anonymously pullable
- [ ] v0.1.4 image not clobbered
- [ ] CronJob remains suspended in selfhost
</success_criteria>

<output>
After completion, create `.planning/phases/02.2-v0-1-4-forcesave-fix/02.2-10-SUMMARY.md` recording:
- Tag commit hash (`git rev-list -n 1 v0.1.5`)
- CI run ID + conclusion
- GHCR image digest (sha256:...)
- Manifest mediaType
- Confirmation that CronJob stayed suspended
- Forward link to Plan 11 (my-kluster image.tag bump)
</output>
