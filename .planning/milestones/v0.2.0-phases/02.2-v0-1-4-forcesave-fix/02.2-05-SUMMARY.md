---
phase: 02.2
plan: 05
plan_name: my-kluster-image-bump
subsystem: cross-repo-coupling
tags: [my-kluster, helm, argocd, cross-repo-coupling, hotfix-window, D-37, ADR-3]
generated: "2026-05-09"
duration_minutes: 130
tasks_completed: 2
tasks_total: 2
files_created: []
files_modified: []
key-decisions:
  - "Atomic single-line diff pattern (D-37) observed end-to-end: my-kluster PR #1371 changed exactly 1 file (charts/arrconf/values.yaml) with +1/-1 line counts — no incidental drift on files/arrconf.yml (placeholders STAY per Phase 2.1 PR4) or templates/cronjob.yaml. The sub-30s GitHub-API confirmation of file scope matches the local diff verification done at PR-author time."
  - "Phase 2.1 hotfix-window discipline replayed verbatim: CronJob suspended at 2026-05-09T03:31:29Z (Task 5.1 Step 1) BEFORE the my-kluster PR opened, kept suspended through the operator review/merge/sync window (~2h6m), then unsuspended after the dispositive live-image check passed. spec.suspend transitioned true → false; lastScheduleTime stayed pinned at 01:00:00Z (the 4h-ago fire) — confirming no in-flight reconcile leaked through the window."
  - "Operator merged via 'Merge commit' (merge commit bba9010636ec24c97e2419138e5974fe25a357d5) rather than the plan-recommended 'squash' — non-deviation: the audit anchor is preserved (single feature branch, single PR description), the file-scope guarantee is identical (+1/-1 across 1 file), and the merge commit message includes the PR title `feat(arrconf): bump image v0.1.3 -> v0.1.4 (forceSave fix)`. Future hotfix PRs can use either merge style."
patterns-established:
  - "Cross-repo coupling sequence (full): arr-stack annotated tag → arrconf-image.yml CI run → GHCR public anonymous-pullable image → my-kluster atomic chart bump PR → operator review → merge → ArgoCD selfHeal → live CronJob spec image roll-forward → unsuspend. Total wall-clock from tag push (Plan 04 03:21:50Z) to live image confirmed (Plan 05 ~05:38Z) ≈ 2h17m, of which ~2h was the operator review/merge window — the automation cycle itself was sub-minute on each segment."
  - "ArgoCD revision-equality sanity check via kubectl: `kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.revision}'` returns the my-kluster main HEAD SHA — equality with the PR merge commit SHA is a one-shot proof that selfHeal applied the desired diff, independent of the argocd CLI being available locally. Useful workaround when the argocd CLI is unavailable on the operator workstation (this session: argocd CLI not on PATH, kubectl-only path was dispositive)."
patterns-reused:
  - "Phase 2.1 LEARNINGS hotfix-via-PR pattern (suspend → merge → sync → verify → unsuspend) — replayed verbatim, 1:1 with the v0.1.3 deployment cycle"
  - "D-37 atomic single-line diff (Phase 2.1 carry-forward) — second half of the sequence (Plan 04 was the first half: tag → CI → GHCR; this plan is: chart bump → ArgoCD → live)"
  - "CLAUDE.md 'Annotations Renovate (CRITIQUE)' enforcement — `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` annotation on charts/arrconf/values.yaml line 2 stayed in place; Renovate continues to track from 0.1.4 forward"
  - "Phase 2.1 PR4 hotfix decision honored — `username: ''` / `password: ''` placeholders in charts/arrconf/files/arrconf.yml STAY (D-02.1-05 reversal). Verified via `gh pr view --json files`: only charts/arrconf/values.yaml is in the file scope of PR #1371."
requirements-completed: []
dependency-graph:
  requires:
    - "02.2-04 — ghcr.io/tom333/arr-stack-arrconf:0.1.4 image (digest sha256:1e7e60c4d0af432366af026ab48e33f911ba6ee1cdc3f25167afbd846cd6054a) anonymously pullable from GHCR"
    - "Phase 2.1 LEARNINGS hotfix-via-PR pattern + Phase 2.1 PR4 hotfix decision (placeholders STAY)"
    - "ArgoCD `arrconf` Application with automated.selfHeal: true (configured in Phase 2 PR1, unchanged through Phase 2.1)"
    - "kubectl + gh CLI access on operator workstation (read+write to selfhost ns CronJob; merge auth on my-kluster repo)"
  provides:
    - "Cluster runs ghcr.io/tom333/arr-stack-arrconf:0.1.4 (live CronJob spec confirmed) — Plan 06 dispositive precondition satisfied"
    - "my-kluster main HEAD = bba9010636ec24c97e2419138e5974fe25a357d5 (post-PR-1371 merge); charts/arrconf/values.yaml line 4: `tag: \"0.1.4\"`"
    - "CronJob `arrconf` in selfhost ns: spec.suspend=false, schedule unchanged (`0 */4 * * *`, concurrencyPolicy=Forbid), next fire window = next quarter-hour offset of 4h cycle from 01:00:00Z lastScheduleTime"
    - "ArgoCD `arrconf` Application: status.sync.status=Synced, status.health.status=Healthy, status.sync.revision=bba9010636... (matches merge commit SHA dispositively)"
  affects:
    - "Plan 06 (FULLY AUTOMATED drift demo W-04) — UNBLOCKED. Plan 06 can now create a forced smoke job from the live v0.1.4-image CronJob, induce a `priority` mutation in Sonarr UI, and observe `put_force_save_used` event + RESTORED == ORIGINAL value-equality WITHOUT operator manual `?forceSave=true` curl nudge."
    - "Renovate (cross-repo) — my-kluster's image.tag is now 0.1.4; future GHCR releases (Phase 3 → v0.1.5+) will trigger Renovate PRs against my-kluster automatically (D-37 says manual bump first; Renovate is the safety net, not the trigger — confirmed by absence of Renovate-authored PR before this manual one)."
    - "Phase 3 (Radarr/Prowlarr) — the `_ArrV3Client` mixin shipped in v0.1.4 is now in cluster runtime; Phase 3's Radarr+Prowlarr reconcilers can extend `_ArrV3Client` and inherit `?forceSave=true` on UPDATE PUTs without re-deriving the architectural decision (ADR-8 already-applied evidence)."
metrics:
  files_committed: 0
  arr_stack_commits: 0
  my_kluster_commits: 2
  my_kluster_pr_number: 1371
  my_kluster_pr_additions: 1
  my_kluster_pr_deletions: 1
  my_kluster_pr_changed_files: 1
  acceptance_criteria_passed: 12
  acceptance_criteria_total: 12
commit-hashes: []
artifacts:
  my_kluster_pr_number: 1371
  my_kluster_pr_url: "https://github.com/tom333/my-kluster/pull/1371"
  my_kluster_pr_title: "feat(arrconf): bump image v0.1.3 -> v0.1.4 (forceSave fix)"
  my_kluster_pr_branch: "phase-2.2-arrconf-v0.1.4"
  my_kluster_pr_state: "MERGED"
  my_kluster_pr_merged_at: "2026-05-09T05:37:20Z"
  my_kluster_pr_merge_style: "merge_commit"
  my_kluster_merge_commit: "bba9010636ec24c97e2419138e5974fe25a357d5"
  my_kluster_diff_stat: "1 file changed, 1 insertion(+), 1 deletion(-)"
  my_kluster_files_in_diff: ["charts/arrconf/values.yaml"]
  files_arrconf_yml_in_diff: false
  templates_cronjob_yaml_in_diff: false
  cronjob_suspend_at: "2026-05-09T03:31:29Z (approx — Task 5.1 Step 1)"
  cronjob_unsuspend_at: "2026-05-09T~05:38Z (post-Task 5.2 dispositive image check)"
  hotfix_window_duration_minutes: 127
  cronjob_image_post_sync: "ghcr.io/tom333/arr-stack-arrconf:0.1.4"
  cronjob_image_pull_policy: "IfNotPresent"
  cronjob_suspend_post: "false"
  cronjob_schedule: "0 */4 * * *"
  cronjob_concurrency_policy: "Forbid"
  cronjob_last_schedule_time_post: "2026-05-09T01:00:00Z (pre-suspend; no fire during window)"
  argocd_app_sync_status: "Synced"
  argocd_app_health_status: "Healthy"
  argocd_app_revision: "bba9010636ec24c97e2419138e5974fe25a357d5"
---

# Phase 02.2 Plan 05: my-kluster image.tag bump 0.1.3 → 0.1.4 Summary

Cross-repo coupling sequence's second half completed: my-kluster PR #1371 merged at `2026-05-09T05:37:20Z` (merge commit `bba9010636ec24c97e2419138e5974fe25a357d5`) with the atomic 1-line diff (`charts/arrconf/values.yaml` line 4: `tag: "0.1.3"` → `tag: "0.1.4"`); ArgoCD `arrconf` Application self-healed to the new manifest revision (status: Synced + Healthy, revision matches merge commit SHA dispositively); live CronJob in `selfhost` ns now runs `image: ghcr.io/tom333/arr-stack-arrconf:0.1.4`; CronJob unsuspended (spec.suspend=false). Hotfix-window discipline observed: ~2h6m suspend window kept the cluster reconcile loop frozen during operator review/merge/sync; lastScheduleTime stayed pinned at the 01:00:00Z fire (4h before suspend) confirming no fire leaked through. D-37 atomic single-line diff pattern observed verbatim from Phase 2.1.

## What Was Built

### my-kluster PR #1371

| Field | Value |
|---|---|
| Number | 1371 |
| URL | https://github.com/tom333/my-kluster/pull/1371 |
| Title | `feat(arrconf): bump image v0.1.3 -> v0.1.4 (forceSave fix)` |
| Source branch | `phase-2.2-arrconf-v0.1.4` |
| Target branch | `main` |
| State (post-merge) | `MERGED` |
| Merged at | `2026-05-09T05:37:20Z` |
| Merge style | merge commit (operator chose merge over plan-recommended squash — non-deviation, file scope identical) |
| Merge commit SHA | `bba9010636ec24c97e2419138e5974fe25a357d5` |
| Files changed | 1 (`charts/arrconf/values.yaml`) |
| Additions | 1 |
| Deletions | 1 |
| Diff stat | `1 file changed, 1 insertion(+), 1 deletion(-)` |

The diff body (verified via `git show bba9010636 -- charts/arrconf/values.yaml` post-fetch on local clone):
```
-    tag: "0.1.3"
+    tag: "0.1.4"
```

`charts/arrconf/files/arrconf.yml` is **NOT** in the file scope (verified via `gh pr view 1371 --json files`) — placeholders `username: ''` / `password: ''` STAY per the Phase 2.1 PR4 hotfix decision documented in CONTEXT.md L24 and 02.1-LEARNINGS D-36 reversal. `charts/arrconf/templates/cronjob.yaml` is also not in the file scope. `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` annotation on line 2 of values.yaml stays in place (CLAUDE.md `Annotations Renovate (CRITIQUE)` honored).

### Hotfix-window discipline

| Field | Value |
|---|---|
| CronJob suspended at | `2026-05-09T03:31:29Z` (Task 5.1 Step 1, kubectl patch) |
| my-kluster PR opened at | ~03:35Z (Task 5.1 Step 3) |
| my-kluster PR merged at | `2026-05-09T05:37:20Z` (operator action, between checkpoint resume signals) |
| Live image confirmed v0.1.4 at | ~05:38Z (Task 5.2 Step 4 dispositive check) |
| CronJob unsuspended at | ~05:38Z (Task 5.2 Step 5) |
| Hotfix window total duration | ~2h6m |
| CronJob fires during window | 0 (lastScheduleTime stayed pinned at `2026-05-09T01:00:00Z`) |

The Phase 2.1 LEARNINGS hotfix-via-PR pattern was replayed 1:1: suspend BEFORE merge so no in-flight reconcile fires during the ArgoCD sync window; verify the live image rolled forward AFTER sync but BEFORE unsuspend; unsuspend only on dispositive image-equality check. The `Forbid` concurrencyPolicy was redundant insurance (no fire was scheduled during the window anyway), but kept consistent with the Phase 2 cluster baseline.

### ArgoCD post-sync state

| Field | Value | Source |
|---|---|---|
| Application name | `arrconf` (namespace: argocd) | kubectl get application |
| Sync status | `Synced` | `.status.sync.status` |
| Health status | `Healthy` | `.status.health.status` |
| Sync revision | `bba9010636ec24c97e2419138e5974fe25a357d5` | `.status.sync.revision` |
| Revision == PR merge commit | ✅ TRUE | dispositive selfHeal proof |

The argocd CLI was unavailable on the operator workstation this session (`command not found: argocd`); the kubectl-on-Application equivalent path provided dispositive evidence. Pattern recorded for future hotfix-window plans where the argocd CLI is missing.

### Live CronJob post-sync

| Field | Value |
|---|---|
| Image | `ghcr.io/tom333/arr-stack-arrconf:0.1.4` |
| imagePullPolicy | `IfNotPresent` |
| Schedule | `0 */4 * * *` (unchanged, every 4h) |
| concurrencyPolicy | `Forbid` (unchanged) |
| spec.suspend | `false` |
| lastScheduleTime | `2026-05-09T01:00:00Z` (pre-suspend; not advanced by the hotfix window) |

The image string matches Plan 04's published artifact byte-for-byte. The implicit digest binding is via the GHCR tag immutability pattern (the `0.1.4` tag points to digest `sha256:1e7e60c4d0af432366af026ab48e33f911ba6ee1cdc3f25167afbd846cd6054a` per Plan 04-04 SUMMARY); next CronJob fire pulls this manifest from GHCR (anonymous-public-pull verified in Plan 04 Task 4.2). Plan 06 will exercise the runtime path with a forced smoke job.

## Acceptance Criteria — 12/12 Pass

### Task 5.1 (8/8)

- [x] `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returned `true` post-Step 1 (CronJob suspended)
- [x] Branch `phase-2.2-arrconf-v0.1.4` exists in my-kluster origin (verified via `gh pr view 1371 --json headRefName`)
- [x] `git diff main..phase-2.2-arrconf-v0.1.4 --stat` showed exactly 1 file, 1 line changed (`gh pr view 1371 --json additions,deletions,changedFiles` → 1/1/1)
- [x] `grep -q '^    tag: "0.1.4"$' /home/moi/projets/perso/my-kluster/charts/arrconf/values.yaml` exits 0 on PR branch (post-merge: confirmed in main HEAD via `git show bba9010636 -- charts/arrconf/values.yaml`)
- [x] `git diff main..phase-2.2-arrconf-v0.1.4 -- charts/arrconf/files/arrconf.yml | wc -l` returns `0` (`gh pr view 1371 --json files` returns only `charts/arrconf/values.yaml`; placeholders stayed)
- [x] `git diff main..phase-2.2-arrconf-v0.1.4 -- charts/arrconf/templates/cronjob.yaml | wc -l` returns `0` (same source, templates stayed)
- [x] PR exists in my-kluster: `gh pr view 1371 --json number` returned `1371`
- [x] PR title contains `feat(arrconf):` and `v0.1.4`: `feat(arrconf): bump image v0.1.3 -> v0.1.4 (forceSave fix)`

### Task 5.2 (4/4)

- [x] my-kluster PR shows state=MERGED: `gh pr view 1371 --repo tom333/my-kluster --json state` → `MERGED`
- [x] ArgoCD arrconf Application is Synced + Healthy: `kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status} {.status.health.status}'` → `Synced Healthy`; revision matches PR merge commit
- [x] Live CronJob spec image matches v0.1.4: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.jobTemplate.spec.template.spec.containers[0].image}'` returned `ghcr.io/tom333/arr-stack-arrconf:0.1.4` (DISPOSITIVE)
- [x] CronJob is unsuspended: `kubectl get cronjob arrconf -n selfhost -o jsonpath='{.spec.suspend}'` returned `false`

### Plan-level `<verification>` gate (6/6)

- [x] CronJob was suspended before PR merge (Task 5.1 Step 1 at 03:31:29Z, PR merged at 05:37:20Z — 2h6m gap)
- [x] PR was a single 1-line diff (Task 5.1 Step 2: 1 file, +1/-1)
- [x] PR was reviewed and merged by operator (Task 5.2 checkpoint resume signal: "approved")
- [x] ArgoCD synced + healthy with v0.1.4 image rolled forward
- [x] CronJob unsuspended after sync confirmed (Task 5.2 Step 5)
- [x] Live CronJob spec shows the new image tag

### `<success_criteria>` (4/4)

- [x] D-37 atomic-PR pattern observed (single-line diff: 1 file, +1/-1)
- [x] Hotfix-window discipline observed (suspend → merge → sync → unsuspend, 2h6m window, 0 fires)
- [x] Phase 2.1 PR4 hotfix decision honored (placeholders stay; `files/arrconf.yml` not in PR file scope)
- [x] Cluster ready for Plan 06 smoke + drift demo (live image v0.1.4, CronJob unsuspended, ArgoCD Healthy)

## Cross-Repo Coupling Sequence (full sequence observed end-to-end)

```
arr-stack repo                          my-kluster repo                     cluster (selfhost ns)
─────────────                           ───────────────                     ────────────────────
Plan 04 Task 4.1: tag v0.1.4 →
                                                                            (CronJob still suspended
                                                                             from Phase 2.1 fix-window)
Plan 04 Task 4.1: CI run 25590328939 (31s)
                                        →
                                        ghcr.io/tom333/arr-stack-arrconf:0.1.4
                                        (digest sha256:1e7e60c4...d6054a)

Plan 04 Task 4.2: docker pull
  (anonymous, no creds) — DISPOSITIVE precondition for Plan 05

============= Plan 05 Wave 4 START =============

Plan 05 Task 5.1 Step 1:                                                   kubectl patch suspend=true
                                                                            (03:31:29Z; lastSchedule=01:00Z)

Plan 05 Task 5.1 Step 2:                git checkout -b phase-2.2-arrconf-v0.1.4
                                        sed -i '...0.1.3.../...0.1.4.../'
                                        git diff --stat: 1 file 1+/1-

Plan 05 Task 5.1 Step 3:                git commit + push + gh pr create #1371

Plan 05 Task 5.2 (operator action):
                                        gh pr merge 1371 --merge
                                        merge commit bba9010636 at 05:37:20Z

                                                                            ArgoCD selfHeal triggered
                                                                            Application revision →
                                                                              bba9010636
                                                                            CronJob spec image →
                                                                              ghcr.io/.../:0.1.4

Plan 05 Task 5.2 Step 4:                                                    kubectl get cronjob → :0.1.4
                                                                            DISPOSITIVE check passed

Plan 05 Task 5.2 Step 5:                                                    kubectl patch suspend=false
                                                                            (~05:38Z)

============= Plan 05 Wave 4 END =============

Plan 06 Wave 5: forced smoke job will exercise the live v0.1.4 image runtime path
```

## Deviations from Plan

### Auto-fixed Issues

None. Both tasks executed end-to-end without auto-fix being triggered.

### Operator-choice deviations (non-deviations)

**1. PR merge style: merge commit instead of plan-recommended squash**

- **Where:** Task 5.2 Step 2 (operator step, between checkpoint resume signals)
- **What plan said:** `gh pr merge <PR-NUMBER> --repo tom333/my-kluster --squash --delete-branch`
- **What happened:** Operator merged via merge commit (merge commit SHA `bba9010636ec24c97e2419138e5974fe25a357d5`)
- **Impact:** None. The audit anchor is preserved (single PR ref, single merge commit, PR title in commit message). The dispositive file-scope guarantee is identical (+1/-1 across 1 file). ArgoCD selfHeal is media-style-agnostic (it tracks revision SHAs, not commit semantics).
- **Why classify as non-deviation:** The acceptance criteria do not constrain merge style; they constrain `state == MERGED` (passed) and revision propagation (passed). Future hotfix PRs can use either style. Recorded for reference; no action required.
- **Branch deletion status:** Not verified (operator may or may not have run `--delete-branch`). Non-blocking — branch is not on the path of any future plan.

### Plan-text drift (non-deviation)

**Plan Task 5.2 Step 6 example:** The plan suggested `argocd app get arrconf | grep -E "(Status|Health|Sync)"` as the post-sync check. In practice the argocd CLI was not installed on the operator workstation (`command not found: argocd`); the kubectl-on-Application equivalent (`kubectl get application arrconf -n argocd -o jsonpath='{.status.sync.status} {.status.health.status} rev={.status.sync.revision}'`) provided dispositive evidence (Synced/Healthy/revision==merge-commit). Same intent, equivalent dispositive guarantee, no behavioral difference. Pattern recorded under `patterns-established` for reuse.

## Authentication Gates

**1. Task 5.2 — Operator manual review + merge (checkpoint:human-action):**
- **What:** PR review + merge requires GitHub UI auth and is by design out of scope for autonomous Claude execution.
- **What automation was attempted:** None — the plan correctly classified this as `human-action` rather than attempting CLI automation. The `gh pr merge` step is operator-executed; Claude only verifies the post-condition.
- **Resolution:** Operator returned the documented resume signal: "approved: PR merged, ArgoCD synced to v0.1.4, CronJob unsuspended". Continuation agent (this run) verified all 4 dispositive post-conditions (PR state, live image, suspend state, ArgoCD health) before writing this SUMMARY.
- **Gate duration:** ~2h6m (Task 5.1 PR-open at ~03:35Z → Task 5.2 resume at ~05:38Z). Within Phase 2.1 hotfix-window pattern norms (Phase 2.1 PR3+PR4 had a similar duration).

## Threat Surface Scan

| Flag | Threat ID | Source | Disposition | Evidence |
|------|-----------|--------|-------------|----------|
| T-02.2-13 | Tampering (PR includes unintended changes) | Plan threat register | mitigate (ENFORCED) | `gh pr view 1371 --json files` returned only `charts/arrconf/values.yaml`; `gh pr view 1371 --json additions,deletions,changedFiles` returned 1/1/1; explicit pre-push diff checks in Task 5.1 Step 2 confirmed `files/arrconf.yml` and `templates/cronjob.yaml` were untouched |
| T-02.2-14 | DoS (CronJob fires during merge window between bad transient state) | Plan threat register | mitigate (ENFORCED) | Suspend at 03:31:29Z (Task 5.1 Step 1); unsuspend at ~05:38Z (Task 5.2 Step 5); lastScheduleTime stayed at 01:00:00Z throughout — no fire leaked. Phase 2.1 LEARNINGS pattern reused verbatim. |
| T-02.2-15 | EoP (GHCR image hijack pulls untrusted code) | Plan threat register | accept | Plan 04 Task 4.2 anonymous-pull verification was the gate (digest `sha256:1e7e60c4...d6054a` confirmed from GHCR namespace `tom333`). cosign signing (OPSV2-03) is the future stronger guarantee. No new surface in Plan 05. |
| T-02.2-16 | Repudiation (audit trail confusion) | Plan threat register | mitigate (PARTIAL) | PR title + branch name + commit message all reference `v0.1.4` and `D-02.1-06`. Merge style was merge-commit (not plan-recommended squash), but the merge commit message includes the PR title — single audit anchor preserved. Branch deletion status not verified. Non-blocking. |

No new security-relevant surface introduced by Plan 05. The CronJob spec is unchanged structurally (only `image` field updated by ArgoCD selfHeal); no new RBAC, no new env vars, no new mounts, no new ports.

## Self-Check: PASSED

- ✓ my-kluster PR #1371 verified MERGED (state=MERGED, mergedAt=2026-05-09T05:37:20Z, mergeCommit=bba9010636ec24c97e2419138e5974fe25a357d5 via `gh pr view`)
- ✓ Merge commit verified in my-kluster local clone post-fetch (`git show bba9010636` shows the +1/-1 diff on charts/arrconf/values.yaml)
- ✓ Live CronJob image verified `:0.1.4` (DISPOSITIVE, via `kubectl get cronjob arrconf -n selfhost -o jsonpath='{...image}'` → `ghcr.io/tom333/arr-stack-arrconf:0.1.4`)
- ✓ CronJob spec.suspend verified `false` (via `kubectl get cronjob ... -o jsonpath='{.spec.suspend}'`)
- ✓ ArgoCD Application Synced + Healthy verified, revision matches merge commit SHA (via `kubectl get application arrconf -n argocd -o jsonpath`)
- ✓ PR file scope verified: only `charts/arrconf/values.yaml` (via `gh pr view 1371 --json files`); placeholders STAY confirmed by absence of files/arrconf.yml in scope
- ✓ All 12 acceptance criteria pass (8 + 4 split across Tasks 5.1 / 5.2)
- ✓ All 6 plan-level `<verification>` gates pass
- ✓ All 4 `<success_criteria>` items satisfied

No artifacts MISSING.

## Deferred / Observed (not deviations, not in scope to fix here)

- **argocd CLI not installed on operator workstation.** The kubectl-on-Application equivalent path was used dispositively. Future plans that include `argocd app sync` / `argocd app wait` steps should either (a) document the kubectl fallback inline, or (b) add an `argocd CLI` install step to my-kluster CLAUDE.md prerequisites. Logged for future-tooling track. Non-blocking for Plan 05 outcome.
- **PR branch deletion status not verified post-merge.** `gh pr merge --delete-branch` flag was recommended by the plan; operator chose merge-commit style and may or may not have used `--delete-branch`. The branch is not on the critical path of any future plan; ignoring.
- **Renovate did not auto-PR before this manual one.** Confirms D-37 expectation: manual bump is the trigger, Renovate is the safety net. Non-blocking; recorded as confirmation of the cross-repo coupling design (not a defect).
- **CronJob image-pull policy is `IfNotPresent`** (carried over from Phase 2 chart authoring). The next CronJob fire (next 4h offset from 01:00Z, so 05:00Z was already past — next is 09:00Z) will pull the v0.1.4 manifest from GHCR if not cached on the node. No node-warming forced this session; Plan 06's forced smoke job is the natural exercise of this path.

## Recommended Next Step

**Plan 06 — Cluster smoke + drift demo FULLY AUTOMATED** is unblocked and ready to run:

- Live image is `ghcr.io/tom333/arr-stack-arrconf:0.1.4` (confirmed dispositive)
- CronJob is unsuspended; `kubectl create job --from=cronjob/arrconf arrconf-v014-smoke -n selfhost` is the entry point
- Forced smoke job will exercise the v0.1.4 runtime path: expected log events
  - `merge_field_preserved` (for username + password fields, carry-forward from Phase 2.1)
  - `put_force_save_used` (NEW in v0.1.4, dispositive proof the `_ArrV3Client.put()` mixin shipped in cluster)
  - `plan_action action=update` (drift detection)
  - PUT 200 (no more HTTP 400 from Sonarr's pre-save validation against `********` mask)
- Drift demo runbook re-run WITHOUT operator manual `?forceSave=true` curl nudge:
  - W-01 forensic snapshot BEFORE drift induction
  - Mutate `priority` field on the qBittorrent download_client in Sonarr UI (or via API curl)
  - Force smoke job
  - W-04 dispositive value-equality: post-snapshot `priority == ORIGINAL_priority` (closes REQ-drift-detection correction half cleanly, replacing Phase 2.1's manual-nudge closure)

The differential dispositive proof against Phase 2.1 closure is the value-add of Plan 06 — Phase 2.1 closed REQ-drift-detection with a manual-nudge workaround; Phase 2.2 closes it with the architecturally-correct ADR-8 trusted-controller stance, fully automated. The image manifest `sha256:1e7e60c4d0af432366af026ab48e33f911ba6ee1cdc3f25167afbd846cd6054a` is the immutable contract Plan 06's smoke job will exercise.
