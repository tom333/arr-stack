---
phase: 00-bootstrap-repo-snapshot-raw
plan: 03
type: execute
wave: 2
depends_on:
  - 00-01
  - 00-02
files_modified:
  - snapshots/baseline-2026-05-07/sonarr/
  - snapshots/baseline-2026-05-07/radarr/
  - snapshots/baseline-2026-05-07/prowlarr/
  - snapshots/baseline-2026-05-07/qbittorrent/
  - snapshots/baseline-2026-05-07/seerr/
  - snapshots/baseline-2026-05-07/jellyfin/
autonomous: false
requirements:
  - REQ-baseline-snapshot
tags: [snapshot, baseline, audit, cluster, manual-verify]

must_haves:
  truths:
    - "Le user a démarré les 6 port-forwards et exporté les 7 env vars (checkpoint manuel)"
    - "snapshot.sh a été exécuté et a produit JSON pour les 6 apps (modulo Jellyfin partiel toléré si A3 false)"
    - "Tous les .json générés sont du JSON valide (find + jq empty)"
    - "Aucune écriture observée pendant le run (kubectl logs diff = pas de POST/PUT/DELETE)"
    - "Audit anti-leak passé (pas de matches 'apiKey/password/token/webhookUrl' avec valeur non-vide), OU redact appliqué et vérifié"
    - "Tous les fichiers committés dans Git, NE PAS dans .gitignore (vérifié par git check-ignore)"
  artifacts:
    - path: "snapshots/baseline-2026-05-07/sonarr/system_status.json"
      provides: "Sanity Sonarr — confirme que l'app a répondu"
      contains: "version"
    - path: "snapshots/baseline-2026-05-07/radarr/system_status.json"
      provides: "Sanity Radarr"
    - path: "snapshots/baseline-2026-05-07/prowlarr/system_status.json"
      provides: "Sanity Prowlarr"
    - path: "snapshots/baseline-2026-05-07/qbittorrent/app_buildinfo.json"
      provides: "Sanity qBittorrent"
    - path: "snapshots/baseline-2026-05-07/seerr/status.json"
      provides: "Sanity Seerr"
    - path: "snapshots/baseline-2026-05-07/jellyfin/system_info_public.json"
      provides: "Sanity Jellyfin (toujours accessible même sans admin bootstrap)"
  key_links:
    - from: "snapshots/baseline-2026-05-07/"
      to: "git index"
      via: "git add + git commit"
      pattern: "snapshots/baseline-2026-05-07"
    - from: ".gitignore"
      to: "snapshots/baseline-2026-05-07/"
      via: "absence de pattern (vérifié par git check-ignore)"
      pattern: "(?!snapshots)"
---

<objective>
Exécuter le snapshot baseline réel contre le cluster `my-kluster`, auditer le résultat pour absence de fuite de secrets, vérifier que le run est resté read-only (kubectl logs diff), et committer le tout dans Git.

Ce plan a `autonomous: false` car il contient 4 vérifications manuelles obligatoires :
1. Setup port-forwards + env vars (humain seul peut le faire — son cluster, ses creds)
2. Vérification kubectl logs *arr post-run = read-only confirmé (humain examine les logs)
3. Audit anti-leak du premier dump (humain décide redact vs commit-as-is vs remove)
4. Validation finale README run-instructions (humain confirme que la doc colle au comportement réel)

Purpose: Closure de la Phase 0. Une fois ce plan committé, la baseline ADR-6 niveau 1 existe — toutes les phases suivantes peuvent re-snapshot et diff.
Output: Dossier `snapshots/baseline-2026-05-07/` committé (~50-80 fichiers, 6 dossiers app), commit Git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-bootstrap-repo-snapshot-raw/00-CONTEXT.md
@.planning/phases/00-bootstrap-repo-snapshot-raw/00-RESEARCH.md
@.planning/phases/00-bootstrap-repo-snapshot-raw/00-01-PLAN.md
@.planning/phases/00-bootstrap-repo-snapshot-raw/00-02-PLAN.md
@CLAUDE.md
@tools/snapshot/snapshot.sh
@tools/snapshot/README.md

<interfaces>
<!-- Cluster K8s "my-kluster" namespace selfhost — services confirmés VERIFIED dans 00-RESEARCH.md -->

Services attendus (kubectl get svc -n selfhost) :
- svc/sonarr      port 8989
- svc/radarr      port 7878
- svc/prowlarr    port 9696
- svc/qbittorrent port 8080
- svc/seerr       port 5055
- svc/jellyfin    port 8096

Versions runtime VERIFIED en research :
- linuxserver/sonarr 4.0.17
- linuxserver/radarr 6.1.1
- linuxserver/prowlarr 2.3.5
- linuxserver/qbittorrent latest (≥ 5.0)
- seerr v3.2.0
- linuxserver/jellyfin 10.11.8

Sources des API keys (NG5 / REQ-bootstrap-exception) :
- SONARR_API_KEY / RADARR_API_KEY → my-kluster/secrets/configarr-secret.yaml
- PROWLARR_API_KEY / SEERR_API_KEY / JELLYFIN_API_KEY → à générer via UI si jamais fait
- QBT_USER / QBT_PASS → admin/<password> qBittorrent (à connaître via my-kluster ou bootstrap)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Setup port-forwards + env vars</name>
  <what-built>
Plans 01 et 02 sont commités. `tools/snapshot/snapshot.sh` est exécutable, `tools/snapshot/README.md` documente la procédure.
  </what-built>
  <how-to-verify>
**Pourquoi humain seul** : ces opérations dépendent du contexte kubectl actif sur ta machine et des creds de TON cluster. Aucune CLI/API ne peut le faire à ta place sans tes secrets.

**Étape A — Vérifier le contexte kubectl** :
```bash
kubectl config current-context
# Attendu : microk8s (ou ton context my-kluster)

kubectl get svc -n selfhost
# Attendu : sonarr, radarr, prowlarr, qbittorrent, seerr, flaresolverr, jellyfin, cleanuparr
```

**Étape B — Lancer les 6 port-forwards** dans un terminal séparé (laisser tourner) :
```bash
kubectl -n selfhost port-forward svc/sonarr      8989:8989 &
kubectl -n selfhost port-forward svc/radarr      7878:7878 &
kubectl -n selfhost port-forward svc/prowlarr    9696:9696 &
kubectl -n selfhost port-forward svc/qbittorrent 8080:8080 &
kubectl -n selfhost port-forward svc/seerr       5055:5055 &
kubectl -n selfhost port-forward svc/jellyfin    8096:8096 &
```

Vérifier au moins une connectivité :
```bash
curl -s --max-time 5 http://localhost:8989/api/v3/system/status -H "X-Api-Key: $SONARR_API_KEY" | jq -r .version 2>/dev/null
# Attendu : 4.0.17 (ou la version effectivement déployée)
```

**Étape C — Exporter les 7 env vars** (pattern recommandé : .env local non committé) :

```bash
# Pré-requis : .env.example a été créé en Plan 01 (template committé sans valeurs)
# Raccourci recommandé :
cp .env.example .env             # crée .env depuis le template (.env est gitignored)
$EDITOR .env                     # remplir les 7 valeurs <REMPLACER>

# OU à la main si tu préfères :
cat > .env <<'EOF'
export SONARR_API_KEY="<from my-kluster/secrets/configarr-secret.yaml>"
export RADARR_API_KEY="<idem>"
export PROWLARR_API_KEY="<from Prowlarr UI Settings → General>"
export QBT_USER="admin"
export QBT_PASS="<from your qBittorrent admin password>"
export SEERR_API_KEY="<from Seerr UI Settings → General>"
export JELLYFIN_API_KEY="<from Jellyfin Dashboard → API Keys>"
EOF

set -a; source .env; set +a

# Vérifier que rien n'est vide :
for v in SONARR_API_KEY RADARR_API_KEY PROWLARR_API_KEY QBT_USER QBT_PASS SEERR_API_KEY JELLYFIN_API_KEY; do
  test -n "${!v}" || { echo "MISSING: $v"; }
done | tee /tmp/env-check.log
test ! -s /tmp/env-check.log && echo "All 7 env vars set" || echo "Some env vars missing — fix before continuing"
```

**Étape D — Smoke test une app** (optionnel, recommandé) :
```bash
./tools/snapshot/snapshot.sh --apps sonarr --dry-run
# Attendu : log [dry-run] sonarr GET ... → ... pour ~17 endpoints, exit 0
```
  </how-to-verify>
  <resume-signal>Type "ok port-forwards et env vars en place" ou décrire les problèmes (port-forward fail, API key manquante, qBit password inconnu).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Exécuter snapshot.sh complet et valider la sortie</name>
  <files>snapshots/baseline-2026-05-07/{sonarr,radarr,prowlarr,qbittorrent,seerr,jellyfin}/*</files>
  <action>
Exécuter le script de snapshot complet (les 6 apps) et valider la sortie automatiquement.

**Étape A — Pré-snapshot : capturer les logs *arr** pour comparaison post-run (D-06, ROADMAP critère #3) :
```bash
mkdir -p /tmp/arr-snapshot-logs
kubectl logs -n selfhost deploy/sonarr --tail=200 > /tmp/arr-snapshot-logs/sonarr-pre.log 2>&1 || true
kubectl logs -n selfhost deploy/radarr --tail=200 > /tmp/arr-snapshot-logs/radarr-pre.log 2>&1 || true
kubectl logs -n selfhost deploy/prowlarr --tail=200 > /tmp/arr-snapshot-logs/prowlarr-pre.log 2>&1 || true
echo "Pre-logs captured ($(wc -l /tmp/arr-snapshot-logs/*.log | tail -1))"
```

**Étape B — Lancer le snapshot complet** :
```bash
./tools/snapshot/snapshot.sh 2>&1 | tee /tmp/snapshot-run.log
SCRIPT_EXIT=$?
echo "snapshot.sh exit code: $SCRIPT_EXIT"
```

Acceptance criteria pour SCRIPT_EXIT :
- `0` : succès (≥ 1 app OK). Continuer.
- `1` : toutes les apps ont fail entièrement → debugger avant de continuer (port-forwards down ? API keys invalides ?). NE PAS COMMIT.
- `2` : erreur args / env vars manquantes → fix l'env et relancer.

**Étape C — Validation automatique de la sortie** :

```bash
# 1) Tous les dossiers app présents (modulo Jellyfin partiel toléré)
for app in sonarr radarr prowlarr qbittorrent seerr jellyfin; do
  test -d "snapshots/baseline-2026-05-07/$app" \
    && echo "✓ $app dir exists" \
    || echo "✗ $app dir MISSING"
done

# 2) Tous les .json sont du JSON valide (jq empty exit 0 = valide)
INVALID=$(find snapshots/baseline-2026-05-07/ -name '*.json' -exec jq empty {} \; 2>&1 | wc -l)
test "$INVALID" -eq 0 && echo "✓ All JSON files valid" || { echo "✗ $INVALID invalid JSON file(s)"; find snapshots/baseline-2026-05-07/ -name '*.json' -exec jq empty {} \; 2>&1 | head -10; }

# 3) Compte sanity (théorique ~84 : sonarr=17 + radarr=18 + prowlarr=14 + qbit=9 + seerr=16 + jellyfin=10)
COUNT=$(find snapshots/baseline-2026-05-07/ -type f \( -name '*.json' -o -name '*.txt' \) | wc -l)
echo "Total snapshot files: $COUNT (théorique ~84)"
if   [ "$COUNT" -ge 75 ]; then
  echo "✓ Count nominal (≥75 — toutes apps répondent)"
elif [ "$COUNT" -ge 60 ]; then
  echo "⚠ Count partiel (60-74) — A3 (Jellyfin admin pas bootstrap) et/ou A5 (Seerr divergence) probables — VÉRIFIER manuellement quelles apps sont dégradées :"
  for app in sonarr radarr prowlarr qbittorrent seerr jellyfin; do
    SUB=$(find "snapshots/baseline-2026-05-07/$app/" -type f 2>/dev/null | wc -l)
    echo "  $app: $SUB fichier(s)"
  done
  echo "Si ce delta est attendu (cf assumptions A3/A5 dans 00-CONTEXT.md), continuer. Sinon STOP et investiguer."
else
  echo "✗ Count critique (<60) — au moins 2 apps complètement KO. STOP, investiguer les logs avant de committer."
  exit 1
fi

# 4) Sanity status endpoint per app
for sanity in \
  "sonarr/system_status.json" \
  "radarr/system_status.json" \
  "prowlarr/system_status.json" \
  "qbittorrent/app_buildinfo.json" \
  "seerr/status.json" \
  "jellyfin/system_info_public.json"; do
  f="snapshots/baseline-2026-05-07/$sanity"
  test -f "$f" && jq -e '.' "$f" >/dev/null \
    && echo "✓ sanity $sanity" \
    || echo "✗ sanity $sanity (missing or invalid)"
done

# 5) Aucun fichier .cookies dans snapshots/ (T-00-T2)
LEAKED_COOKIES=$(find snapshots/ -name '*.cookies' -o -name '*.cookie-jar' | wc -l)
test "$LEAKED_COOKIES" -eq 0 && echo "✓ No cookie leak" || echo "✗ Cookie files in snapshots/ — DELETE before commit"
```

Cette validation doit être propre avant de passer au Task 3 (audit anti-leak humain). Si invalides JSON ou compte trop bas, debug d'abord (port-forward mort en cours de run ? quelle app ?) et relance partielle :

```bash
# Re-snapshot d'une app spécifique si elle a fail
./tools/snapshot/snapshot.sh --apps prowlarr
```
  </action>
  <verify>
    <automated>test -d snapshots/baseline-2026-05-07/sonarr && test -d snapshots/baseline-2026-05-07/radarr && test -d snapshots/baseline-2026-05-07/prowlarr && test -d snapshots/baseline-2026-05-07/qbittorrent && test -d snapshots/baseline-2026-05-07/seerr && test -d snapshots/baseline-2026-05-07/jellyfin && find snapshots/baseline-2026-05-07/ -name '*.json' -exec jq empty {} \; 2>&1 | wc -l | xargs -I {} test {} -eq 0 && test "$(find snapshots/baseline-2026-05-07/ -type f \( -name '*.json' -o -name '*.txt' \) | wc -l)" -ge 60 && test "$(find snapshots/ -name '*.cookies' -o -name '*.cookie-jar' | wc -l)" -eq 0</automated>
  </verify>
  <done>
- Les 6 dossiers app existent dans `snapshots/baseline-2026-05-07/` (Jellyfin partiel toléré si A3 false)
- Tous les `.json` sont du JSON valide (jq empty exit 0)
- ≥ 60 fichiers au total (sanity count : 17+18+14+9+16+10 ≈ 84 attendus, on tolère un peu de marge pour endpoints qui auraient légitimement 404)
- Aucun fichier `.cookies` n'a fuité dans `snapshots/`
- Sanity status JSON pour les 6 apps confirme connectivité
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Vérifier no-write (logs *arr) + audit anti-leak du dump</name>
  <what-built>
Le snapshot a tourné, validé automatiquement (Task 2). On a :
- 6 dossiers app dans `snapshots/baseline-2026-05-07/`
- ~70-80 fichiers `.json` + `.txt`, tous JSON-valides
- Logs *arr pré-run capturés dans `/tmp/arr-snapshot-logs/*-pre.log`
- Le log d'exécution dans `/tmp/snapshot-run.log`
  </what-built>
  <how-to-verify>
**Pourquoi humain** : (a) examen visuel des logs Sonarr/Radarr requis pour ROADMAP critère #3 ("aucune écriture observée"), (b) décision case-by-case sur le redact ou pas du dump (option A vs B vs C dans `tools/snapshot/README.md`).

**Étape A — Vérification no-write (ROADMAP critère #3, T-00-T4)** :

```bash
# Re-capturer les logs post-run
kubectl logs -n selfhost deploy/sonarr   --tail=400 > /tmp/arr-snapshot-logs/sonarr-post.log
kubectl logs -n selfhost deploy/radarr   --tail=400 > /tmp/arr-snapshot-logs/radarr-post.log
kubectl logs -n selfhost deploy/prowlarr --tail=400 > /tmp/arr-snapshot-logs/prowlarr-post.log

# Diff : ne devrait montrer que des GET (pas de POST/PUT/DELETE)
for app in sonarr radarr prowlarr; do
  echo "=== $app diff ==="
  diff /tmp/arr-snapshot-logs/$app-pre.log /tmp/arr-snapshot-logs/$app-post.log \
    | grep -iE '\b(POST|PUT|DELETE|PATCH)\b' \
    && echo "⚠ $app : WRITES DETECTED" \
    || echo "✓ $app : read-only OK"
done
```

**Acceptance** : aucun match POST/PUT/DELETE/PATCH dans les diffs des 3 *arr apps. Si un match apparaît :
- Vérifier si c'est un faux positif (ex. log line mentionnant "POST" dans une URL d'indexer config qu'on aurait GET).
- Si vraie écriture détectée : **STOP, NE PAS COMMIT**, investiguer côté script (T-00-T4 violé).

**Étape B — Audit anti-leak (T-00-T3, D-11, Security Domain critique)** :

Vue d'ensemble des champs sensibles potentiellement présents :

```bash
# Recherche large
grep -irE '"(apiKey|password|token|sessionKey|webhookUrl)":\s*"[^"]+"' snapshots/baseline-2026-05-07/ \
  | grep -v '"apiKey":\s*""' \
  | grep -v '"apiKey":\s*null' \
  | tee /tmp/leak-audit.log

LEAK_LINES=$(wc -l < /tmp/leak-audit.log)
echo ""
echo "Audit found $LEAK_LINES potential secret occurrence(s)."
```

Si `LEAK_LINES > 0` : ouvrir `/tmp/leak-audit.log`, identifier les fichiers concernés (probablement `*/config_host.json`, `qbittorrent/app_preferences.json`, `*/notification.json`, `seerr/settings_notifications_*.json`).

**Décider une option** (documentée dans `tools/snapshot/README.md` § "Audit anti-leak") :

**Option A — Redact (RECOMMANDÉ Phase 0)** :
```bash
JQ_REDACT='walk(if type == "object" then with_entries(if (.key | test("(?i)^(apiKey|password|token|webhookUrl|sessionKey|web_ui_password_hash|proxy_password)$")) and .value != null and .value != "" then .value = "<redacted>" else . end) else . end)'

for f in snapshots/baseline-2026-05-07/*/*.json; do
  jq --sort-keys "$JQ_REDACT" "$f" > "$f.tmp" && mv "$f.tmp" "$f"
done

# Re-vérifier
grep -irE '"(apiKey|password|token|webhookUrl)":\s*"[^"]+"' snapshots/baseline-2026-05-07/ \
  | grep -v '<redacted>' \
  | grep -v '"apiKey":\s*""' \
  | grep -v '"apiKey":\s*null' \
  | wc -l
# Attendu : 0
```

**Option B — Garder tel quel** (si le repo reste privé ET les creds sont déjà connus de toi seul) : noter explicitement dans le SUMMARY pourquoi tu acceptes ce risque.

**Option C — Retirer le fichier concerné** :
```bash
rm snapshots/baseline-2026-05-07/sonarr/config_host.json  # exemple
# Documenter dans le SUMMARY qu'un endpoint a été retiré et pourquoi.
```

**Étape C — Vérification .gitignore** :
```bash
git check-ignore snapshots/baseline-2026-05-07/sonarr/system_status.json
echo "exit code: $?"
# Attendu : exit 1 (= NON ignoré). Si exit 0 → .gitignore Plan 01 a été modifié par accident, fix d'abord.
```

**Étape D — Confirmer au planner** :
- Logs *arr : read-only ✓ (ou listing des matches faux-positifs si présents)
- Audit anti-leak : 0 leak après option choisie (A / B / C)
- .gitignore : snapshots/ NON ignoré ✓

Documente le choix d'option (A/B/C) et le nombre de fichiers redacted/retirés pour le SUMMARY.
  </how-to-verify>
  <resume-signal>Type "no-write OK + audit OK option [A/B/C] avec N fichiers redacted/retirés" ou décrire les problèmes (write détecté ? leak non couvert ?).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: git add + commit baseline + verify final state</name>
  <files>snapshots/baseline-2026-05-07/**/*</files>
  <action>
Commit final de la baseline + vérifications de fermeture de phase.

**Étape A — Confirmer encore une fois `.gitignore` et `git check-ignore`** :
```bash
# Sanity D-04 : .gitignore ne contient pas de pattern qui ignorerait snapshots/
! grep -qE '^snapshots/?$' .gitignore && echo "✓ .gitignore ne bloque pas snapshots/"

# Sanity D-04 : Git ne considère pas le baseline comme ignoré
git check-ignore snapshots/baseline-2026-05-07/sonarr/system_status.json && \
  { echo "✗ FATAL: snapshot file is git-ignored — fix .gitignore before commit"; exit 1; } || \
  echo "✓ snapshots/ files NOT git-ignored"
```

**Étape B — Re-vérifier no-leak après les éventuels redacts** :
```bash
# Repasse l'audit pour confirmer que la décision Task 3 a bien été appliquée
LEAK_FINAL=$(grep -irE '"(apiKey|password|token|sessionKey|webhookUrl)":\s*"[^"]+"' snapshots/baseline-2026-05-07/ \
  | grep -v '<redacted>' \
  | grep -v '"apiKey":\s*""' \
  | grep -v '"apiKey":\s*null' \
  | wc -l)
test "$LEAK_FINAL" -eq 0 && echo "✓ Final audit clean ($LEAK_FINAL leaks)" || { echo "✗ STILL $LEAK_FINAL leak(s) — DO NOT COMMIT"; exit 1; }
```

**Étape C — git add explicite (PAS `git add -A`)** :
```bash
git add snapshots/baseline-2026-05-07/

# Vérifier ce qui est staged
git status --short snapshots/
# Doit montrer ~60-90 lignes "A snapshots/baseline-2026-05-07/<app>/<file>"

# Confirmer aucun .cookies / .env staged par accident
git status --short | grep -E '\.(cookies|cookie-jar|env)' && \
  { echo "✗ FATAL: secret file staged"; git status --short; exit 1; } || \
  echo "✓ No secret files staged"
```

**Étape D — Commit avec HEREDOC pour formatting** :
```bash
git commit -m "$(cat <<'EOF'
snapshot(00): baseline raw read-only de 6 apps avant toute écriture

ADR-6 niveau 1 — premier snapshot lossless capturé via tools/snapshot/snapshot.sh
(Bash + curl + jq, indépendant d'arrconf qui arrive Phase 1).

Apps couvertes : sonarr, radarr, prowlarr, qbittorrent, seerr, jellyfin.

Snapshot read-only confirmé par diff des logs sonarr/radarr/prowlarr
pré/post-run (aucun POST/PUT/DELETE/PATCH dans les logs *arr).

Audit anti-leak : voir 00-03-SUMMARY.md (option A/B/C documentée).

Closes Phase 0 success criteria #1 (6 apps × ~10 endpoints chacune),
#2 (committés Git, pas .gitignore), #3 (no-write logs).
EOF
)"
```

**Étape E — Vérification finale post-commit** :
```bash
# 1) Le commit existe
git log --oneline -1 -- snapshots/

# 2) Aucun fichier untracked oublié
git status --short snapshots/
# Doit être vide

# 3) Aucun fichier ignoré par accident
git ls-files snapshots/baseline-2026-05-07/ | wc -l
# Doit être ≥ 60 (correspondre au compte total)

# 4) Renovate config validée + README cohérents (Plan 01 + Plan 02)
test -f README.md && test -f renovate.json && test -f .gitignore && test -x tools/snapshot/snapshot.sh && test -f tools/snapshot/README.md && echo "✓ Plan 01 + 02 outputs intact"
```

**Étape F — Récapituler les 5 success criteria de la roadmap Phase 0** :
```bash
cat <<'EOF'
=== Phase 0 success criteria check ===

1. snapshot.sh produit JSON pour les 6 apps : OK (~$(find snapshots/baseline-2026-05-07/ -name '*.json' -o -name '*.txt' | wc -l) fichiers)
2. Tous committés Git, pas dans .gitignore : OK (git ls-files vs git check-ignore)
3. Aucune écriture observée (logs *arr) : OK (Task 3 humain — read-only confirmé)
4. renovate.json initial committé : OK (Plan 01)
5. README minimal présent : OK (Plan 01 README.md + Plan 02 tools/snapshot/README.md)
EOF
```
  </action>
  <verify>
    <automated>test "$(grep -irE '"(apiKey|password|token|sessionKey|webhookUrl)":\s*"[^"]+"' snapshots/baseline-2026-05-07/ | grep -v '<redacted>' | grep -v '"apiKey":\s*""' | grep -v '"apiKey":\s*null' | wc -l)" -eq 0 && ! git check-ignore snapshots/baseline-2026-05-07/sonarr/system_status.json && test "$(git ls-files snapshots/baseline-2026-05-07/ | wc -l)" -ge 60 && git log --oneline -1 -- snapshots/baseline-2026-05-07/ | grep -qiE 'snapshot|baseline' && test -f README.md && test -f renovate.json && test -f .gitignore && test -x tools/snapshot/snapshot.sh && test -f tools/snapshot/README.md</automated>
  </verify>
  <done>
- Audit final anti-leak = 0 match (D-11, T-00-T3 mitigated)
- `git check-ignore` exit 1 sur les fichiers snapshot (D-04 respecté)
- `git ls-files snapshots/baseline-2026-05-07/` retourne ≥ 60 fichiers
- Commit existe avec message référant Phase 0 / ADR-6
- Pas de fichier `.cookies`, `.env`, etc. staged
- Plans 01 et 02 outputs intacts (sanité finale)
- Les 5 success criteria de ROADMAP Phase 0 marqués comme atteints
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Validation finale README run-instructions + closure phase</name>
  <what-built>
Phase 0 complète : repo scaffolded (Plan 01), script + doc (Plan 02), baseline committée (Plan 03 Tasks 1-4).
  </what-built>
  <how-to-verify>
**Pourquoi humain** : seul un humain peut suivre les instructions du README "from scratch" et confirmer qu'elles correspondent au vrai comportement (la doc est-elle utile à toi-dans-3-mois ?).

**Test "fresh user" — re-faire un snapshot en suivant uniquement les README** :

1. Ouvrir `README.md` racine. Suivre la section "Snapshot rapide" :
   - Le pointer vers `tools/snapshot/README.md` est-il clair ?
   - Le bloc bash est-il copy-pasteable ?

2. Ouvrir `tools/snapshot/README.md`. Suivre les sections "Prérequis" → "Usage" :
   - Les commandes `kubectl port-forward` matchent-elles les services réels ?
   - La table des env vars couvre-t-elle exactement ce que `snapshot.sh --help` affiche ?
   - Les exemples (`--apps sonarr`, `--output ...`) marchent-ils ?

3. Re-snapshot rapide :
   ```bash
   ./tools/snapshot/snapshot.sh --apps sonarr --output /tmp/test-resnap-$(date +%s)
   # Attendu : ~17 fichiers JSON, exit 0, output dans /tmp
   # Si fail : la doc README a un bug à corriger.
   ```

4. Section troubleshooting : couvre-t-elle les cas que tu as réellement rencontrés (qBit referer, Jellyfin admin, port-forward mort) ?

**Si la doc est OK** : valider et closure Phase 0.

**Si la doc nécessite des ajustements mineurs** : appliquer (typos, command corrections) avant closure et créer un nouveau commit `docs(00): clarify README run-instructions`. (NE PAS amender le commit Plan 02 — convention GSD préfère systématiquement de nouveaux commits ; un amend est toléré uniquement si le commit n'a pas encore été pushé ET qu'il s'agit de la même session de travail, ce qui n'est pas garanti ici.)
  </how-to-verify>
  <resume-signal>Type "doc OK Phase 0 closed" ou "doc à corriger sur ces points : ..." (lister les corrections nécessaires avant closure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workstation env (port-forwards + env vars) → snapshot.sh process | Identique Plan 02. API keys et cookies en mémoire pendant le run. |
| snapshot.sh → filesystem `snapshots/baseline-2026-05-07/` | Les dumps **réels** sont écrits ici. Ce plan introduit le risque concret de leak vers Git. |
| `snapshots/` → Git index → GitHub repo | Une fois committé, **public sur GitHub** (repo `tom333/arr-stack` selon ADR-3 si visibilité publique, ou privé selon décision Phase 4). Tout secret committé = compromis durablement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-T1 | Information Disclosure | Process listing leak (env vars exposed via `ps`) | mitigate | API keys déjà chargées dans l'env shell par le user (étape A Task 1). `ps -ef` ne montre pas les env (sauf `/proc/<pid>/environ` accessible au seul owner du process). Documenté dans README. |
| T-00-T2 | Information Disclosure | Cookie qBittorrent committed by accident | mitigate | (a) Plan 01 .gitignore bloque `*.cookies`, `*.cookie-jar`. (b) snapshot.sh utilise `mktemp -d` cleanup via trap. (c) Task 2 vérification automatique : `find snapshots/ -name '*.cookies' \| wc -l == 0`. (d) Task 4 `git status` post-add : aucun fichier secret staged. |
| T-00-T3 | Information Disclosure | Premier dump contient des secrets API (apiKey field, webhooks, qBit passwords) | mitigate | **Triple barrière**: (a) Task 3 audit grep manuel obligatoire, (b) Task 3 décision explicite redact/keep/remove (option A/B/C documentée dans 00-03-SUMMARY), (c) Task 4 re-audit final automatique (LEAK_FINAL == 0 sinon exit 1). Le commit ne peut PAS arriver si l'audit retourne des matches non-redactés. |
| T-00-T4 | Tampering / Elevation | Une écriture API a eu lieu pendant le run (bug dans script) | mitigate | (a) Plan 02 garantit le no-write par construction (grep no-X-POST/PUT/DELETE). (b) Task 2 capture des logs *arr avant run. (c) Task 3 humain compare logs pré/post — diff ne doit montrer que des GET. Si write détecté : STOP, NE PAS COMMIT. |

Note importante : T-00-T1/T2/T3/T4 du contexte de planning sont mitigés respectivement par : Plan 01 (.gitignore défense en profondeur), Plan 02 (script read-only par construction), Plan 03 Task 3 (audit humain), Plan 03 Task 4 (re-audit final automatique avant commit).
</threat_model>

<verification>
**Verification automatique finale (regroupement)** :

```bash
# 1) 5 success criteria ROADMAP Phase 0
test -d snapshots/baseline-2026-05-07/sonarr || exit 1   # critère 1
test "$(git ls-files snapshots/baseline-2026-05-07/ | wc -l)" -ge 60 || exit 1  # critère 2 (Git tracked)
! git check-ignore snapshots/baseline-2026-05-07/sonarr/system_status.json || exit 1  # critère 2 (pas ignoré)
# critère 3 (no-write logs *arr) = manuel Task 3 — pas automatable sans kubectl context du user
test -f renovate.json && jq -e '.extends | index("config:recommended")' renovate.json >/dev/null || exit 1  # critère 4
test -f README.md && grep -qi snapshot README.md || exit 1  # critère 5

# 2) Anti-leak final
test "$(grep -irE '"(apiKey|password|token|sessionKey|webhookUrl)":\s*"[^"]+"' snapshots/baseline-2026-05-07/ | grep -v '<redacted>' | grep -v '"apiKey":\s*""' | grep -v '"apiKey":\s*null' | wc -l)" -eq 0 || exit 1

# 3) Commit présent
git log --oneline -1 -- snapshots/baseline-2026-05-07/ | grep -qiE 'snapshot|baseline' || exit 1

# 4) Plans 01+02 outputs intacts
test -f README.md && test -f renovate.json && test -f .gitignore && test -x tools/snapshot/snapshot.sh && test -f tools/snapshot/README.md || exit 1

echo "✓ Phase 0 verification clean"
```

**Verifications manuelles (4 documentées)** :

1. **Logs *arr no-write** (Task 3) : humain examine `diff /tmp/arr-snapshot-logs/<app>-pre.log /tmp/arr-snapshot-logs/<app>-post.log` pour absence de POST/PUT/DELETE.
2. **Audit anti-leak** (Task 3) : humain examine `/tmp/leak-audit.log`, choisit option A/B/C, applique.
3. **Setup port-forwards + env vars** (Task 1) : humain démarre les port-forwards et exporte les env vars.
4. **README run-instructions** (Task 5) : humain re-suit les README et confirme qu'ils sont utilisables fresh.
</verification>

<success_criteria>
- [ ] Tous les success criteria de ROADMAP Phase 0 (5/5) validés (cf section verification)
- [ ] `snapshots/baseline-2026-05-07/` contient ≥ 60 fichiers, tous JSON-valides (modulo Jellyfin partiel toléré si A3 false)
- [ ] No-write confirmé par diff de logs *arr humain (Task 3)
- [ ] Audit anti-leak passé : 0 match restant après option A/B/C documentée dans SUMMARY (T-00-T3 mitigated)
- [ ] Commit Git présent référant Phase 0 / ADR-6
- [ ] `git check-ignore` confirme que `snapshots/` n'est PAS ignoré (D-04 respecté)
- [ ] Plans 01 + 02 outputs intacts (README.md, renovate.json, .gitignore, snapshot.sh exécutable, tools/snapshot/README.md)
- [ ] Run-instructions README validés "fresh" par humain (Task 5)
- [ ] Aucun fichier `.cookies` / `.env` committé
</success_criteria>

<output>
Après complétion, créer `.planning/phases/00-bootstrap-repo-snapshot-raw/00-03-SUMMARY.md` listant :
- Date d'exécution effective + nom de la baseline (`snapshots/baseline-<date>/`)
- Compte de fichiers par app (sonarr=N, radarr=N, …) — détecter les apps partielles
- Statut Jellyfin (full / partiel admin-bootstrap-pas-fait — A3 confirmation)
- Résumé du diff logs *arr (no-write OK)
- **Décision audit anti-leak** : option A (redact, N fichiers modifiés) / option B (keep-as-is, justification) / option C (remove, fichiers retirés)
- Hash du commit final
- Confirmation des 5 success criteria ROADMAP Phase 0
- État des assumptions A1-A5 (confirmed / falsified — particulièrement A3 Jellyfin bootstrap)
- Pointer vers Phase 1 (`/gsd-plan-phase 1` — arrconf POC + JSON Schema) en next step

Mettre à jour `.planning/STATE.md` (peut être fait par `gsd-progress` après ce SUMMARY) :
- Phase 0 : status → "Complete"
- Last activity : "Phase 0 baseline snapshot committed"
- Resume file : pointer vers 00-03-SUMMARY.md
</output>
