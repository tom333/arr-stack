---
phase: 00-bootstrap-repo-snapshot-raw
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/snapshot/snapshot.sh
  - tools/snapshot/README.md
autonomous: true
requirements:
  - REQ-baseline-snapshot
tags: [snapshot, bash, curl, jq, multi-app-auth, read-only]

must_haves:
  truths:
    - "tools/snapshot/snapshot.sh existe, est exécutable, et utilise set -euo pipefail + IFS=$'\\n\\t' + trap"
    - "Le script supporte 6 apps (sonarr, radarr, prowlarr, qbittorrent, seerr, jellyfin) avec leurs 3 stratégies d'auth distinctes"
    - "Le script ne contient AUCUN -X POST/PUT/DELETE/PATCH (read-only par construction)"
    - "Le script lit les API keys uniquement depuis l'env (jamais de fichier secret)"
    - "Le script supporte les flags --apps / --output / --dry-run / --help"
    - "Le script écrit dans snapshots/baseline-2026-05-07/<app>/<resource>.json par défaut, avec jq --sort-keys"
    - "Le script tolère per-endpoint failure (warning) et per-app failure (warning si au moins 1 app OK)"
    - "tools/snapshot/README.md documente : prérequis port-forwards, env vars exactes, exemples d'usage, troubleshooting (Jellyfin admin bootstrap, qBittorrent referer, port-forward mort)"
  artifacts:
    - path: "tools/snapshot/snapshot.sh"
      provides: "Script principal Bash multi-app read-only"
      contains: "set -euo pipefail"
      min_lines: 200
    - path: "tools/snapshot/README.md"
      provides: "Doc opérationnelle (port-forwards, env vars, exemples, troubleshooting)"
      contains: "kubectl port-forward"
      min_lines: 80
  key_links:
    - from: "tools/snapshot/snapshot.sh"
      to: "env vars (SONARR_API_KEY etc.)"
      via: "${VAR:?error message}"
      pattern: "\\$\\{[A-Z_]+:\\?"
    - from: "tools/snapshot/snapshot.sh"
      to: "snapshots/baseline-2026-05-07/<app>/"
      via: "mkdir -p + curl -o + jq --sort-keys"
      pattern: "mkdir -p.*snapshots"
    - from: "tools/snapshot/snapshot.sh"
      to: "qBittorrent /api/v2/auth/login"
      via: "curl --cookie-jar + Referer header"
      pattern: "Referer:"
    - from: "tools/snapshot/README.md"
      to: "tools/snapshot/snapshot.sh"
      via: "exemples ./snapshot.sh --apps sonarr"
      pattern: "snapshot\\.sh"
---

<objective>
Implémenter le script Bash `tools/snapshot/snapshot.sh` (read-only, multi-auth, multi-app) qui capture lossless l'état des 6 apps avec leurs ~80 endpoints au total, et écrire la doc opérationnelle `tools/snapshot/README.md` (port-forwards, env vars, troubleshooting).

Purpose: Livrer le filet de sécurité ADR-6 niveau 1 (Bash standalone, indépendant d'arrconf qui arrive Phase 1+). Le script doit garantir le caractère **read-only par construction** (aucune écriture API possible) et la **déterminisme** des outputs (jq --sort-keys → diffs git lisibles).
Output: Deux fichiers committés (`snapshot.sh` exécutable + `README.md`). PAS d'exécution réelle dans ce plan — le run + commit du baseline est Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-bootstrap-repo-snapshot-raw/00-CONTEXT.md
@.planning/phases/00-bootstrap-repo-snapshot-raw/00-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Phase 0 = greenfield. Aucune interface interne — uniquement contrats externes (APIs *arr / qBit / Seerr / Jellyfin). -->
<!-- Toutes les références ci-dessous sont VERIFIED dans 00-RESEARCH.md ("Standard Stack" → "Endpoints à snapshot par app"). -->

### Endpoints par app (résumé — détail complet dans 00-RESEARCH.md)

**Sonarr (`X-Api-Key`, `/api/v3/`)** — 17 endpoints :
downloadclient, indexer, notification, rootfolder, tag, qualityprofile (forensic), customformat (forensic), config/host, config/naming (forensic), config/mediamanagement, config/ui, config/indexer, config/downloadclient, importlist, remotepathmapping, metadata, system/status

**Radarr (`X-Api-Key`, `/api/v3/`)** — Même que Sonarr + `config/metadata`

**Prowlarr (`X-Api-Key`, `/api/v1/`)** — 14 endpoints :
indexer, indexer/categories, indexerstats, indexerstatus, indexerproxy, applications, appprofile, downloadclient, notification, tag, config/host, config/ui, config/downloadclient, system/status

**qBittorrent (cookie SID via `/api/v2/auth/login` form + Referer header, `/api/v2/`)** — 9 endpoints (mix JSON + text) :
JSON : app/buildInfo, app/preferences, torrents/categories, torrents/tags, torrents/info, transfer/info
TXT : app/version, app/webapiVersion, app/defaultSavePath

**Seerr (`X-Api-Key`, `/api/v1/`)** — ~16 endpoints :
settings/main, settings/network, settings/public, settings/sonarr, settings/radarr, settings/jellyfin, settings/plex, settings/notifications/{email,discord,telegram,webhook}, settings/jobs, user, request, request/count, status

**Jellyfin (`Authorization: MediaBrowser Token="<key>"`, racine `/`)** — 10 endpoints :
System/Info, System/Info/Public, System/Configuration, System/Info/Storage, Library/VirtualFolders, System/Configuration/MetadataOptions/Default, Users, Plugins, Devices, ScheduledTasks

### Auth strategies

| App | Stratégie | Détail |
|-----|-----------|--------|
| sonarr / radarr / prowlarr / seerr | Header `X-Api-Key: ${KEY}` | Standard *arr / Servarr |
| qbittorrent | POST login form + cookie SID + Referer | `--data-urlencode "username=..." --data-urlencode "password=..." -H "Referer: ${QBT_URL}"` puis `--cookie-jar` partagé |
| jellyfin | Header `Authorization: MediaBrowser Token="${KEY}"` (10.11+) | Fallback `X-Emby-Token: ${KEY}` overridable via env `JELLYFIN_AUTH_HEADER` |

### URLs par défaut (overridables via env)

```bash
SONARR_URL="${SONARR_URL:-http://localhost:8989}"
RADARR_URL="${RADARR_URL:-http://localhost:7878}"
PROWLARR_URL="${PROWLARR_URL:-http://localhost:9696}"
QBT_URL="${QBT_URL:-http://localhost:8080}"
SEERR_URL="${SEERR_URL:-http://localhost:5055}"
JELLYFIN_URL="${JELLYFIN_URL:-http://localhost:8096}"
```

### Output schema

```
snapshots/baseline-2026-05-07/
├── sonarr/<resource>.json         (jq --sort-keys '.')
├── radarr/<resource>.json
├── prowlarr/<resource>.json
├── qbittorrent/
│   ├── <resource>.json            (JSON endpoints)
│   └── <resource>.txt             (text endpoints : app/version, app/webapiVersion, app/defaultSavePath)
├── seerr/<resource>.json
└── jellyfin/<resource>.json
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implémenter tools/snapshot/snapshot.sh</name>
  <files>tools/snapshot/snapshot.sh, tools/snapshot/.gitkeep (à supprimer si présent)</files>
  <action>
Créer `tools/snapshot/snapshot.sh` exécutable (`chmod +x`) implémentant tous les patterns de 00-RESEARCH.md.

**Pré-action — cleanup placeholder** : si `tools/snapshot/.gitkeep` existe (créé par Plan 01 pour tracker le dossier vide), le supprimer maintenant — le dossier est désormais non-vide grâce à `snapshot.sh`. Commande : `rm -f tools/snapshot/.gitkeep`. Le commit doit inclure cette suppression.


**Structure recommandée (~250-350 lignes)** :

```bash
#!/usr/bin/env bash
# arr-stack snapshot.sh — read-only baseline capture for 6 apps.
# See tools/snapshot/README.md for prerequisites (port-forwards, env vars).
# ADR-6 niveau 1 : Bash standalone, indépendant d'arrconf (qui arrive Phase 1).

set -euo pipefail
IFS=$'\n\t'

# ─── Globals ────────────────────────────────────────────────────────────────

readonly SCRIPT_NAME="$(basename "$0")"
readonly SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
readonly REPO_ROOT="$(cd "${SCRIPT_DIR}/../.." && pwd)"
readonly DEFAULT_DATE="2026-05-07"  # baseline date (verrouillée Phase 0)
readonly KNOWN_APPS=(sonarr radarr prowlarr qbittorrent seerr jellyfin)

# Workspace temp (cookies qBittorrent + tampons curl)
WORK_DIR=""
trap 'cleanup' EXIT INT TERM

cleanup() {
  if [[ -n "${WORK_DIR}" && -d "${WORK_DIR}" ]]; then
    rm -rf "${WORK_DIR}"
  fi
}

# ─── Refus root ─────────────────────────────────────────────────────────────

if [[ ${EUID} -eq 0 ]]; then
  echo "ERROR: do not run ${SCRIPT_NAME} as root" >&2
  exit 2
fi

# ─── Args parsing ───────────────────────────────────────────────────────────

usage() {
  cat <<EOF
Usage: ${SCRIPT_NAME} [options]

Capture un snapshot raw read-only des 6 apps arr-stack (sonarr, radarr, prowlarr,
qbittorrent, seerr, jellyfin) via leurs APIs REST.

Options:
  --apps APP1,APP2     Liste des apps à snapshotter (default: toutes les 6)
  --output PATH        Dossier output (default: snapshots/baseline-${DEFAULT_DATE})
  --dry-run            Liste les GET sans écrire les fichiers
  -h, --help           Affiche cette aide

Prérequis (voir tools/snapshot/README.md):
  - kubectl port-forward actif vers chaque service selfhost
  - Env vars : SONARR_API_KEY, RADARR_API_KEY, PROWLARR_API_KEY,
              QBT_USER, QBT_PASS, SEERR_API_KEY, JELLYFIN_API_KEY

Exit codes: 0 = succès (≥1 app OK), 1 = toutes les apps ont fail, 2 = erreur args
EOF
}

TARGET_APPS=("${KNOWN_APPS[@]}")
OUTPUT_DIR=""
DRY_RUN=0

while [[ $# -gt 0 ]]; do
  case "$1" in
    --apps)
      IFS=',' read -ra TARGET_APPS <<< "$2"
      # Validation whitelist (anti command injection — ASVS V5)
      for app in "${TARGET_APPS[@]}"; do
        case "$app" in
          sonarr|radarr|prowlarr|qbittorrent|seerr|jellyfin) ;;
          *) echo "ERROR: unknown app '$app' (allowed: ${KNOWN_APPS[*]})" >&2; exit 2 ;;
        esac
      done
      shift 2 ;;
    --output)
      OUTPUT_DIR="$2"
      # Anti path-traversal : refuser '..' (ASVS V5)
      if [[ "$OUTPUT_DIR" == *..* ]]; then
        echo "ERROR: '..' not allowed in --output path" >&2; exit 2
      fi
      shift 2 ;;
    --dry-run) DRY_RUN=1; shift ;;
    -h|--help) usage; exit 0 ;;
    *) echo "ERROR: unknown flag '$1'" >&2; usage >&2; exit 2 ;;
  esac
done

OUTPUT_DIR="${OUTPUT_DIR:-${REPO_ROOT}/snapshots/baseline-${DEFAULT_DATE}}"

# ─── Working dir ────────────────────────────────────────────────────────────

WORK_DIR="$(mktemp -d -t arr-snapshot.XXXXXX)"

# ─── URL defaults ───────────────────────────────────────────────────────────

SONARR_URL="${SONARR_URL:-http://localhost:8989}"
RADARR_URL="${RADARR_URL:-http://localhost:7878}"
PROWLARR_URL="${PROWLARR_URL:-http://localhost:9696}"
QBT_URL="${QBT_URL:-http://localhost:8080}"
SEERR_URL="${SEERR_URL:-http://localhost:5055}"
JELLYFIN_URL="${JELLYFIN_URL:-http://localhost:8096}"

# Jellyfin auth — 10.11+ default, X-Emby-Token override possible
JELLYFIN_AUTH_HEADER="${JELLYFIN_AUTH_HEADER:-Authorization: MediaBrowser Token=\"${JELLYFIN_API_KEY:-}\"}"

# ─── Logging helpers ────────────────────────────────────────────────────────

log()  { echo "[$(date +%H:%M:%S)] $*"; }
warn() { echo "[$(date +%H:%M:%S)] WARN: $*" >&2; }
err()  { echo "[$(date +%H:%M:%S)] ERROR: $*" >&2; }

# ─── Core helper : snapshot_get ─────────────────────────────────────────────

# Usage: snapshot_get <app> <full_url> <auth_header_or_-> <output_file_relative_to_app_dir> [--cookie <jar>]
# Returns 0 si OK, 1 si fail (HTTP != 200 ou JSON invalide).
snapshot_get() {
  local app="$1" url="$2" auth="$3" out_rel="$4"; shift 4
  local extra_args=("$@")
  local out_dir="${OUTPUT_DIR}/${app}"
  local out_file="${out_dir}/${out_rel}"

  if (( DRY_RUN )); then
    log "  [dry-run] ${app} GET ${url} → ${out_rel}"
    return 0
  fi

  mkdir -p "$out_dir"

  local curl_args=(
    --silent --show-error
    --max-time 30
    --retry 2 --retry-delay 1
    -H "Accept: application/json"
    -w "%{http_code}"
    -o "${WORK_DIR}/raw"
  )
  if [[ "$auth" != "-" ]]; then
    curl_args+=(-H "$auth")
  fi
  curl_args+=("${extra_args[@]}" "$url")

  local http_code
  http_code=$(curl "${curl_args[@]}" || echo "000")

  if [[ "$http_code" != "200" ]]; then
    warn "  ✗ ${app} ${out_rel} → HTTP ${http_code}"
    return 1
  fi

  # JSON ? sort-keys + write. Sinon (qBit text endpoints) : write raw.
  if [[ "$out_rel" == *.json ]]; then
    if jq --sort-keys '.' "${WORK_DIR}/raw" > "$out_file" 2>/dev/null; then
      log "  ✓ ${app} ${out_rel}"
      return 0
    else
      warn "  ✗ ${app} ${out_rel} → invalid JSON, fallback raw"
      cp "${WORK_DIR}/raw" "${out_file%.json}.raw"
      return 1
    fi
  else
    cp "${WORK_DIR}/raw" "$out_file"
    log "  ✓ ${app} ${out_rel}"
    return 0
  fi
}

# ─── Per-app snapshotters ───────────────────────────────────────────────────

snapshot_arr_app() {
  local app="$1" base_url="$2" api_key="$3" api_version="$4"
  local auth="X-Api-Key: ${api_key}"
  local v="$api_version"
  local local_failures=0

  local endpoints=(
    "/api/${v}/downloadclient:downloadclient.json"
    "/api/${v}/indexer:indexer.json"
    "/api/${v}/notification:notification.json"
    "/api/${v}/rootfolder:rootfolder.json"
    "/api/${v}/tag:tag.json"
    "/api/${v}/qualityprofile:qualityprofile.json"
    "/api/${v}/customformat:customformat.json"
    "/api/${v}/config/host:config_host.json"
    "/api/${v}/config/naming:config_naming.json"
    "/api/${v}/config/mediamanagement:config_mediamanagement.json"
    "/api/${v}/config/ui:config_ui.json"
    "/api/${v}/config/indexer:config_indexer.json"
    "/api/${v}/config/downloadclient:config_downloadclient.json"
    "/api/${v}/importlist:importlist.json"
    "/api/${v}/remotepathmapping:remotepathmapping.json"
    "/api/${v}/metadata:metadata.json"
    "/api/${v}/system/status:system_status.json"
  )
  if [[ "$app" == "radarr" ]]; then
    endpoints+=("/api/${v}/config/metadata:config_metadata.json")
  fi

  log "▶ ${app} (${base_url})"
  for entry in "${endpoints[@]}"; do
    local endpoint="${entry%%:*}"
    local filename="${entry##*:}"
    snapshot_get "$app" "${base_url}${endpoint}" "$auth" "$filename" || local_failures=$((local_failures + 1))
  done

  if (( local_failures > 0 )); then
    warn "${app} : ${local_failures} endpoint(s) failed"
    return 1
  fi
  return 0
}

snapshot_prowlarr() {
  local base_url="$1" api_key="$2"
  local auth="X-Api-Key: ${api_key}"
  local local_failures=0

  local endpoints=(
    "/api/v1/indexer:indexer.json"
    "/api/v1/indexer/categories:indexer_categories.json"
    "/api/v1/indexerstats:indexerstats.json"
    "/api/v1/indexerstatus:indexerstatus.json"
    "/api/v1/indexerproxy:indexerproxy.json"
    "/api/v1/applications:applications.json"
    "/api/v1/appprofile:appprofile.json"
    "/api/v1/downloadclient:downloadclient.json"
    "/api/v1/notification:notification.json"
    "/api/v1/tag:tag.json"
    "/api/v1/config/host:config_host.json"
    "/api/v1/config/ui:config_ui.json"
    "/api/v1/config/downloadclient:config_downloadclient.json"
    "/api/v1/system/status:system_status.json"
  )

  log "▶ prowlarr (${base_url})"
  for entry in "${endpoints[@]}"; do
    local endpoint="${entry%%:*}"
    local filename="${entry##*:}"
    snapshot_get "prowlarr" "${base_url}${endpoint}" "$auth" "$filename" || local_failures=$((local_failures + 1))
  done

  (( local_failures > 0 )) && { warn "prowlarr : ${local_failures} endpoint(s) failed"; return 1; }
  return 0
}

snapshot_qbittorrent() {
  local base_url="$1" user="$2" pass="$3"
  local cookie_jar="${WORK_DIR}/qbt.cookies"
  local local_failures=0

  log "▶ qbittorrent (${base_url})"

  # Login (POST is read-from-server, not a write to torrents — read-only intent preserved).
  if (( ! DRY_RUN )); then
    local http_code
    http_code=$(curl --silent --show-error \
      --cookie-jar "$cookie_jar" \
      -H "Referer: ${base_url}" \
      --data-urlencode "username=${user}" \
      --data-urlencode "password=${pass}" \
      -w "%{http_code}" \
      -o /dev/null \
      "${base_url}/api/v2/auth/login" || echo "000")

    if [[ "$http_code" != "200" ]]; then
      warn "qbittorrent : login failed (HTTP ${http_code}) — vérifier QBT_USER/PASS et port-forward"
      return 1
    fi
  fi

  local json_endpoints=(
    "/api/v2/app/buildInfo:app_buildinfo.json"
    "/api/v2/app/preferences:app_preferences.json"
    "/api/v2/torrents/categories:torrents_categories.json"
    "/api/v2/torrents/tags:torrents_tags.json"
    "/api/v2/torrents/info:torrents_info.json"
    "/api/v2/transfer/info:transfer_info.json"
  )
  local text_endpoints=(
    "/api/v2/app/version:app_version.txt"
    "/api/v2/app/webapiVersion:app_webapi_version.txt"
    "/api/v2/app/defaultSavePath:app_default_save_path.txt"
  )

  for entry in "${json_endpoints[@]}" "${text_endpoints[@]}"; do
    local endpoint="${entry%%:*}"
    local filename="${entry##*:}"
    snapshot_get "qbittorrent" "${base_url}${endpoint}" "-" "$filename" --cookie "$cookie_jar" \
      || local_failures=$((local_failures + 1))
  done

  (( local_failures > 0 )) && { warn "qbittorrent : ${local_failures} endpoint(s) failed"; return 1; }
  return 0
}

snapshot_seerr() {
  local base_url="$1" api_key="$2"
  local auth="X-Api-Key: ${api_key}"
  local local_failures=0

  local endpoints=(
    "/api/v1/settings/main:settings_main.json"
    "/api/v1/settings/network:settings_network.json"
    "/api/v1/settings/public:settings_public.json"
    "/api/v1/settings/sonarr:settings_sonarr.json"
    "/api/v1/settings/radarr:settings_radarr.json"
    "/api/v1/settings/jellyfin:settings_jellyfin.json"
    "/api/v1/settings/plex:settings_plex.json"
    "/api/v1/settings/notifications/email:settings_notifications_email.json"
    "/api/v1/settings/notifications/discord:settings_notifications_discord.json"
    "/api/v1/settings/notifications/telegram:settings_notifications_telegram.json"
    "/api/v1/settings/notifications/webhook:settings_notifications_webhook.json"
    "/api/v1/settings/jobs:settings_jobs.json"
    "/api/v1/user:user.json"
    "/api/v1/request:request.json"
    "/api/v1/request/count:request_count.json"
    "/api/v1/status:status.json"
  )

  log "▶ seerr (${base_url})"
  for entry in "${endpoints[@]}"; do
    local endpoint="${entry%%:*}"
    local filename="${entry##*:}"
    snapshot_get "seerr" "${base_url}${endpoint}" "$auth" "$filename" || local_failures=$((local_failures + 1))
  done

  (( local_failures > 0 )) && { warn "seerr : ${local_failures} endpoint(s) failed (Q1 compat — non-bloquant Phase 0)"; return 1; }
  return 0
}

snapshot_jellyfin() {
  local base_url="$1" auth_header="$2"
  local local_failures=0

  local endpoints=(
    "/System/Info:system_info.json"
    "/System/Info/Public:system_info_public.json"
    "/System/Configuration:system_configuration.json"
    "/System/Info/Storage:system_storage.json"
    "/Library/VirtualFolders:library_virtualfolders.json"
    "/System/Configuration/MetadataOptions/Default:metadata_options_default.json"
    "/Users:users.json"
    "/Plugins:plugins.json"
    "/Devices:devices.json"
    "/ScheduledTasks:scheduled_tasks.json"
  )

  log "▶ jellyfin (${base_url})"
  for entry in "${endpoints[@]}"; do
    local endpoint="${entry%%:*}"
    local filename="${entry##*:}"
    snapshot_get "jellyfin" "${base_url}${endpoint}" "$auth_header" "$filename" || local_failures=$((local_failures + 1))
  done

  if (( local_failures > 0 )); then
    warn "jellyfin : ${local_failures} endpoint(s) failed (NG5 admin bootstrap ? — voir tools/snapshot/README.md)"
    return 1
  fi
  return 0
}

# ─── Main loop ──────────────────────────────────────────────────────────────

log "snapshot.sh start — output: ${OUTPUT_DIR} (dry-run=${DRY_RUN})"
log "target apps: ${TARGET_APPS[*]}"

declare -i FAILED_APPS=0
declare -i TOTAL_APPS=${#TARGET_APPS[@]}

for app in "${TARGET_APPS[@]}"; do
  case "$app" in
    sonarr)
      : "${SONARR_API_KEY:?SONARR_API_KEY env var is required (see tools/snapshot/README.md)}"
      snapshot_arr_app "sonarr" "$SONARR_URL" "$SONARR_API_KEY" "v3" || FAILED_APPS=$((FAILED_APPS + 1)) ;;
    radarr)
      : "${RADARR_API_KEY:?RADARR_API_KEY env var is required}"
      snapshot_arr_app "radarr" "$RADARR_URL" "$RADARR_API_KEY" "v3" || FAILED_APPS=$((FAILED_APPS + 1)) ;;
    prowlarr)
      : "${PROWLARR_API_KEY:?PROWLARR_API_KEY env var is required}"
      snapshot_prowlarr "$PROWLARR_URL" "$PROWLARR_API_KEY" || FAILED_APPS=$((FAILED_APPS + 1)) ;;
    qbittorrent)
      : "${QBT_USER:?QBT_USER env var is required}"
      : "${QBT_PASS:?QBT_PASS env var is required}"
      snapshot_qbittorrent "$QBT_URL" "$QBT_USER" "$QBT_PASS" || FAILED_APPS=$((FAILED_APPS + 1)) ;;
    seerr)
      : "${SEERR_API_KEY:?SEERR_API_KEY env var is required}"
      snapshot_seerr "$SEERR_URL" "$SEERR_API_KEY" || FAILED_APPS=$((FAILED_APPS + 1)) ;;
    jellyfin)
      : "${JELLYFIN_API_KEY:?JELLYFIN_API_KEY env var is required}"
      snapshot_jellyfin "$JELLYFIN_URL" "$JELLYFIN_AUTH_HEADER" || FAILED_APPS=$((FAILED_APPS + 1)) ;;
  esac
done

# ─── Final report ───────────────────────────────────────────────────────────

if (( FAILED_APPS == TOTAL_APPS )); then
  err "all ${TOTAL_APPS} app(s) failed entirely"
  exit 1
fi

local_ok=$((TOTAL_APPS - FAILED_APPS))
log "snapshot complete : ${local_ok}/${TOTAL_APPS} app(s) OK, ${FAILED_APPS} with warnings"
log "output: ${OUTPUT_DIR}"
log ""
log "next: review for secret leaks before commit (see tools/snapshot/README.md § 'Audit anti-leak')"
exit 0
```

**Vérifications après écriture** :

1. `chmod +x tools/snapshot/snapshot.sh` (exécutable).
2. **Sanity grep no-write** (D-06 — garde-fou critique) :
   ```bash
   grep -nE '\-X[[:space:]]*(POST|PUT|DELETE|PATCH)' tools/snapshot/snapshot.sh | grep -v '^#'
   ```
   Doit ne rien retourner (exit 1 de grep). Le seul POST autorisé est `/api/v2/auth/login` qBittorrent (read-from-server, pas une écriture sur torrents) ; il est implémenté **sans** `-X POST` (curl utilise POST automatiquement quand on envoie `--data-urlencode`).
3. **Sanity bashisms** : `bash -n tools/snapshot/snapshot.sh` (parse only, pas d'exécution).
4. **Sanity strict mode** : `grep -q 'set -euo pipefail' tools/snapshot/snapshot.sh && grep -q "trap" tools/snapshot/snapshot.sh`.
5. **Sanity env-only** (D-05) : aucun pattern de lecture de fichier de secrets (jamais `cat .*key`, jamais `source .*secret`).
6. **Smoke test --help** : `tools/snapshot/snapshot.sh --help` doit afficher l'usage et exit 0 (pas besoin d'env vars pour --help).
7. **Smoke test args** : `tools/snapshot/snapshot.sh --apps invalid` doit exit 2 avec message d'erreur whitelist (anti-injection).
8. **Smoke test dry-run sans env** : `tools/snapshot/snapshot.sh --apps sonarr --dry-run` doit fail proprement avec `SONARR_API_KEY env var is required` (pas de crash bash, juste exit non-zero avec message clair).
  </action>
  <verify>
    <automated>chmod +x tools/snapshot/snapshot.sh && bash -n tools/snapshot/snapshot.sh && grep -q 'set -euo pipefail' tools/snapshot/snapshot.sh && grep -q "trap" tools/snapshot/snapshot.sh && ! (grep -nE '\-X[[:space:]]*(POST|PUT|DELETE|PATCH)' tools/snapshot/snapshot.sh | grep -v '^[[:space:]]*#') && ./tools/snapshot/snapshot.sh --help >/dev/null && (./tools/snapshot/snapshot.sh --apps invalid 2>&1; test $? -eq 2) && (env -u SONARR_API_KEY ./tools/snapshot/snapshot.sh --apps sonarr --dry-run 2>&1 | grep -qi "SONARR_API_KEY")</automated>
  </verify>
  <done>
- `tools/snapshot/snapshot.sh` existe, exécutable (`chmod +x`)
- Parse propre (`bash -n` exit 0)
- Contient `set -euo pipefail` + `trap` + refus root + whitelist apps
- AUCUN `-X POST/PUT/DELETE/PATCH` (sauf commentaires) — read-only par construction
- `--help` fonctionne sans env vars
- `--apps invalid` exit 2 avec message d'erreur whitelist
- Lecture API key obligatoire via `${VAR:?...}` — fail explicite si manquant
- Couvre les 6 apps avec leurs 3 patterns d'auth distincts (X-Api-Key / cookie SID / Authorization MediaBrowser)
- ~250-350 lignes, organisé en sections (globals, args, helpers, per-app, main)
  </done>
</task>

<task type="auto">
  <name>Task 2: Écrire tools/snapshot/README.md (doc opérationnelle)</name>
  <files>tools/snapshot/README.md</files>
  <action>
Créer `tools/snapshot/README.md` (~120-180 lignes) couvrant prérequis, env vars, usage, troubleshooting. Le contenu DOIT couvrir tous les scénarios de pitfalls 1-7 du 00-RESEARCH.md.

**Structure recommandée** :

```markdown
# tools/snapshot

Script Bash standalone pour capturer un snapshot **read-only, lossless, déterministe** de la config actuelle des 6 apps arr-stack (sonarr, radarr, prowlarr, qbittorrent, seerr, jellyfin) via leurs APIs REST. Aucune dépendance Python (arrconf arrive Phase 1+).

> **Discipline (ADR-6)** : exécuter ce script AVANT tout test risqué (nouveau reconciler, montée de version, debug en cluster). Tous les snapshots sont versionnés Git — voir [`spec.md`](../../spec.md) §6.5.

## Prérequis

### 1. Outils CLI

- `bash` 5.x, `curl` 8.x, `jq` 1.7+, `kubectl` 1.28+, `git`. Tous standards Linux.

### 2. Port-forwards vers le cluster

Ouvrir un terminal séparé et lancer (laisser tourner pendant le snapshot) :

\`\`\`bash
kubectl -n selfhost port-forward svc/sonarr      8989:8989 &
kubectl -n selfhost port-forward svc/radarr      7878:7878 &
kubectl -n selfhost port-forward svc/prowlarr    9696:9696 &
kubectl -n selfhost port-forward svc/qbittorrent 8080:8080 &
kubectl -n selfhost port-forward svc/seerr       5055:5055 &
kubectl -n selfhost port-forward svc/jellyfin    8096:8096 &
\`\`\`

Pour stopper en bloc — utilise UNE des options ci-dessous (par sécurité décroissante) :

\`\`\`bash
# Option A (la plus sûre) — kill uniquement les jobs lancés depuis CE shell
jobs -p | xargs -r kill 2>/dev/null

# Option B — trap auto-cleanup si tu lances tout dans un script ou subshell
( trap 'jobs -p | xargs -r kill' EXIT
  kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
  # ... autres apps ...
  ./tools/snapshot/snapshot.sh )

# Option C (DANGER — kill tout port-forward de la machine, y compris autres clusters)
# Ne l'utilise que si tu sais qu'aucun autre kubectl port-forward ne tourne :
# pkill -f "kubectl.*port-forward"
\`\`\`

### 3. Variables d'environnement

Le script lit **uniquement** depuis l'env (jamais de fichier de secrets — convention CLAUDE.md).

| Variable | Source | Comment l'obtenir |
|----------|--------|-------------------|
| `SONARR_API_KEY` | Sonarr UI → Settings → General | Déjà dans `my-kluster/secrets/configarr-secret.yaml` |
| `RADARR_API_KEY` | Radarr UI → Settings → General | idem |
| `PROWLARR_API_KEY` | Prowlarr UI → Settings → General | À générer si jamais fait (NG5) |
| `QBT_USER` / `QBT_PASS` | qBittorrent UI → Tools → Options → Web UI | Le compte admin par défaut est `admin/adminadmin` au boot, à changer |
| `SEERR_API_KEY` | Seerr UI → Settings → General | À générer si jamais fait (NG5) |
| `JELLYFIN_API_KEY` | Jellyfin UI → Dashboard → API Keys | À générer si admin pas encore bootstrap (NG5) |

Pattern d'export recommandé (utilise un `.env` local, jamais committé — `.gitignore` le bloque) :

\`\`\`bash
# ./.env (au root du repo, ou autre chemin)
export SONARR_API_KEY="abc123…"
export RADARR_API_KEY="def456…"
export PROWLARR_API_KEY="ghi789…"
export QBT_USER="admin"
export QBT_PASS="…"
export SEERR_API_KEY="jkl012…"
export JELLYFIN_API_KEY="mno345…"
\`\`\`

\`\`\`bash
# Sourcing (depuis le shell, pas le script)
set -a; source .env; set +a
\`\`\`

> **⚠** Ne JAMAIS \`echo $SONARR_API_KEY\` dans un terminal partagé / log / screen sharing.

### 4. URLs (overridables)

Par défaut le script vise localhost (cf port-forwards ci-dessus). Override possible :

\`\`\`bash
export SONARR_URL=http://10.0.0.5:8989    # ex. accès Tailscale direct
export QBT_URL=http://my-host:18080       # port custom
# etc.
\`\`\`

### 5. Auth Jellyfin (10.11+)

Par défaut : \`Authorization: MediaBrowser Token="<key>"\` (10.11+). Si `EnableLegacyAuthorization=true` dans `system.xml` (versions < 10.11), fallback :

\`\`\`bash
export JELLYFIN_AUTH_HEADER="X-Emby-Token: ${JELLYFIN_API_KEY}"
\`\`\`

## Usage

\`\`\`bash
# Capture complète (les 6 apps) → snapshots/baseline-2026-05-07/
./tools/snapshot/snapshot.sh

# Une seule app (smoke test rapide)
./tools/snapshot/snapshot.sh --apps sonarr

# Plusieurs apps
./tools/snapshot/snapshot.sh --apps sonarr,radarr,prowlarr

# Output personnalisé (avant test risqué)
./tools/snapshot/snapshot.sh --output snapshots/before-phase-3-2026-05-15/

# Dry-run (liste les GET sans écrire les fichiers)
./tools/snapshot/snapshot.sh --dry-run

# Aide
./tools/snapshot/snapshot.sh --help
\`\`\`

## Audit anti-leak (CRITIQUE avant commit du PREMIER dump)

> Le premier snapshot peut contenir des champs sensibles dans certains endpoints. **Lire avant `git add snapshots/`.**

Les endpoints suivants sont connus pour exposer des creds :
- `sonarr/config_host.json`, `radarr/config_host.json`, `prowlarr/config_host.json` → champ `apiKey` (l'API key de l'app elle-même)
- `*/notification.json` → webhooks Discord/Slack/Telegram avec leurs URLs/tokens
- `qbittorrent/app_preferences.json` → `web_ui_password_hash`, `proxy_username`, `proxy_password` (peut être plain text)
- `seerr/settings_notifications_*.json` → API tokens des services notif

Audit pattern :

\`\`\`bash
# Exit code 0 = pas de match (OK). Sortie non-vide = potentiel leak à examiner.
grep -irE '"(apiKey|password|token|sessionKey|webhookUrl)":\s*"[^"]+"' snapshots/baseline-2026-05-07/ \
  | grep -v '"apiKey":\s*""' \
  | grep -v '"apiKey":\s*null'
\`\`\`

Si matches non-vides : décision case-by-case :

1. **Option A — Redact les valeurs** (recommandé pour le commit Phase 0) :
   \`\`\`bash
   JQ_REDACT='walk(if type == "object" then with_entries(if (.key | test("(?i)apiKey|password|token|webhookUrl|sessionKey")) and .value != null and .value != "" then .value = "<redacted>" else . end) else . end)'
   for f in snapshots/baseline-2026-05-07/*/*.json; do
     jq --sort-keys "$JQ_REDACT" "$f" > "$f.tmp" && mv "$f.tmp" "$f"
   done
   \`\`\`

2. **Option B — Garder tel quel** : seulement si le repo reste privé ET les creds sont déjà connus de toi seul.

3. **Option C — Retirer le fichier concerné** : `rm snapshots/.../config_host.json` puis re-snapshot Plan 03 sans cet endpoint (modifier le script).

Documenter le choix dans le SUMMARY de Plan 03.

## Vérifier qu'aucune écriture n'a eu lieu (logs *arr)

Avant le snapshot, capture des logs :

\`\`\`bash
kubectl logs -n selfhost deploy/sonarr --tail=200 > /tmp/sonarr-pre.log
kubectl logs -n selfhost deploy/radarr --tail=200 > /tmp/radarr-pre.log
\`\`\`

Lancer le snapshot, puis :

\`\`\`bash
kubectl logs -n selfhost deploy/sonarr --tail=400 > /tmp/sonarr-post.log
diff /tmp/sonarr-pre.log /tmp/sonarr-post.log | grep -iE '\b(POST|PUT|DELETE)\b' || echo "READ-ONLY OK"
\`\`\`

Pas de matches sur POST/PUT/DELETE → le snapshot a respecté ADR-6.

## Troubleshooting

### qBittorrent login fail (HTTP 403)
qBittorrent rejette les requêtes sans `Referer` matchant l'URL (anti-CSRF v5.0). Le script l'envoie automatiquement (\`-H "Referer: ${QBT_URL}"\`). Vérifier que `QBT_URL` correspond exactement au host:port utilisé.

### Jellyfin tous endpoints 401/403
Probablement `EnableLegacyAuthorization=false` côté Jellyfin 10.11.8 alors que le script tente `X-Emby-Token`. Vérifier que `JELLYFIN_AUTH_HEADER` n'a pas été override en `X-Emby-Token` par accident — le default Authorization MediaBrowser est correct pour 10.11+.

### Jellyfin /Library/VirtualFolders 403 mais /System/Info/Public 200
Le compte admin Jellyfin n'est pas encore bootstrap (NG5). C'est attendu si Phase 0 tourne avant Phase 7. Le script log un warning et continue — pas un fail bloquant.

### Port-forward meurt en cours de run
\`kubectl port-forward\` n'est pas robuste. Si une app fail systématiquement en milieu de run (`Failed to connect`, `Empty reply`), relancer le port-forward concerné et relancer \`./tools/snapshot/snapshot.sh --apps <app>\`.

### Seerr endpoint 404
Q1 ouverte (compat Seerr ↔ Overseerr). Si un endpoint diverge, le snapshot est juste vide pour cette ressource — non bloquant en Phase 0. Validation réelle = Phase 6.

### Diff git illisible entre 2 snapshots
Tous les `.json` doivent passer par `jq --sort-keys` (le script le fait). Si tu vois du bruit massif, vérifier que le snapshot a bien tourné jusqu'au bout (pas d'écriture partielle d'un fichier raw).

## Format output

\`\`\`
snapshots/baseline-YYYY-MM-DD/
├── sonarr/<resource>.json        (tri jq --sort-keys, déterministe)
├── radarr/<resource>.json
├── prowlarr/<resource>.json
├── qbittorrent/
│   ├── <resource>.json
│   └── <resource>.txt            (app/version, app/webapiVersion, app/defaultSavePath)
├── seerr/<resource>.json
└── jellyfin/<resource>.json
\`\`\`

Tous fichiers ASCII / UTF-8, taille typique 1-200 KB par fichier, total < 5 MB pour les 6 apps.

## Voir aussi

- [`spec.md`](../../spec.md) §6.5 (workflow snapshot complet 4 niveaux)
- [`spec.md`](../../spec.md) §11 ADR-6 (décision verrouillée)
- [`CLAUDE.md`](../../CLAUDE.md) (workflow snapshot CRITIQUE — discipline avant tests risqués)
- [`.planning/phases/00-bootstrap-repo-snapshot-raw/00-RESEARCH.md`](../../.planning/phases/00-bootstrap-repo-snapshot-raw/00-RESEARCH.md) (recherche détaillée)
```

Le README doit couvrir explicitement les pitfalls Jellyfin 10.11 (Pitfall 2), qBittorrent Referer (Pitfall 3), port-forward mort (Pitfall 4), Jellyfin admin bootstrap (Pitfall 5), audit anti-leak (Security Domain).
  </action>
  <verify>
    <automated>test -f tools/snapshot/README.md && grep -q "kubectl port-forward" tools/snapshot/README.md && grep -q "SONARR_API_KEY" tools/snapshot/README.md && grep -q "QBT_USER" tools/snapshot/README.md && grep -q "JELLYFIN_API_KEY" tools/snapshot/README.md && grep -qi "anti-leak\|audit\|redact" tools/snapshot/README.md && grep -q "Authorization: MediaBrowser" tools/snapshot/README.md && grep -qi "referer" tools/snapshot/README.md && test "$(wc -l < tools/snapshot/README.md)" -ge 80</automated>
  </verify>
  <done>
- `tools/snapshot/README.md` existe, ≥ 80 lignes
- Documente les port-forwards `kubectl` pour les 6 apps
- Documente les 7 env vars (SONARR/RADARR/PROWLARR_API_KEY, QBT_USER/PASS, SEERR/JELLYFIN_API_KEY)
- Couvre les 4 pitfalls clés : qBittorrent Referer, Jellyfin 10.11+ auth, port-forward mort, Jellyfin admin bootstrap
- Documente l'audit anti-leak (jq walk redact pattern) avec exemple bash exécutable
- Liens vers spec.md, CLAUDE.md, 00-RESEARCH.md
- Couvre `--apps`, `--output`, `--dry-run`, `--help` flags
- Section "Vérifier qu'aucune écriture n'a eu lieu" (kubectl logs diff) documentée
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workstation shell → snapshot.sh process | Env vars (API keys) traversent ici. Risque : leak via process listing (`ps`), via `.bash_history`, via logs si `set -x` actif. |
| snapshot.sh → APIs *arr/qBit/Seerr/Jellyfin (via port-forward) | Curl envoie API keys en headers. TLS interne K8s assure le transport. |
| snapshot.sh → filesystem (`snapshots/<output>`) | Les dumps peuvent contenir des secrets API (apiKey field, webhooks, qBit passwords). Ne pas committer sans audit. |
| snapshot.sh → mktemp -d cookie jar | Fichier qBit `.cookies` doit être éphémère et pas leak vers `snapshots/` ou Git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-T1 | Information Disclosure | Process listing leak via CLI args | mitigate | API keys lues via env vars (`${VAR:?...}`), JAMAIS via flags CLI. `ps -ef` ne montre pas les env. Documenté dans README "Ne JAMAIS echo $SONARR_API_KEY". |
| T-00-T2 | Information Disclosure | Cookies qBittorrent committés par accident | mitigate | (a) Cookie jar dans `mktemp -d` (mode 700), (b) `trap rm -rf "$WORK_DIR" EXIT INT TERM` cleanup, (c) `.gitignore` (Plan 01) ignore `*.cookies` / `*.cookie-jar` en défense en profondeur. |
| T-00-T3 | Information Disclosure | Dump API contient des secrets (apiKey field, webhooks, passwords) | mitigate (deferred to Plan 03) | Le script lui-même ne redact pas (Phase 0 minimaliste). L'audit anti-leak + redact `jq walk` est documenté dans `tools/snapshot/README.md` ET implémenté comme step manuel dans Plan 03 AVANT `git add`. |
| T-00-T4 | Tampering / Elevation of Privilege | Bug qui ferait POST/PUT/DELETE par accident | mitigate | (a) Aucun `-X POST/PUT/DELETE/PATCH` dans le script (sauf `/api/v2/auth/login` qBittorrent qui est read-from-server pas une écriture sur torrents — implémenté sans `-X POST` explicite). (b) Garde-fou automatique : `grep -nE '\-X[[:space:]]*(POST\|PUT\|DELETE\|PATCH)' tools/snapshot/snapshot.sh` doit ne rien retourner. (c) Refus root (`if [[ $EUID -eq 0 ]]; then exit 2`). (d) Validation manuelle Plan 03 via diff de logs *arr. |
| T-00-T5 | Tampering | Command injection via `--apps` | mitigate | Whitelist explicite : `case "$app" in sonarr\|radarr\|prowlarr\|qbittorrent\|seerr\|jellyfin) ;; *) exit 2 ;; esac`. |
| T-00-T6 | Tampering | Path traversal via `--output` | mitigate | Refus de `..` dans le path : `if [[ "$OUTPUT_DIR" == *..* ]]; then exit 2`. |
| T-00-T7 | DoS | Endpoint hang bloque le snapshot indéfiniment | mitigate | `--max-time 30` sur tous les `curl`. Si une app entière hang, échec localisé (warning), les autres apps continuent. |
</threat_model>

<verification>
Après les 2 tâches :

1. Script existe + exécutable + parse propre :
   ```bash
   test -x tools/snapshot/snapshot.sh && bash -n tools/snapshot/snapshot.sh
   ```

2. Bash strict mode + trap + refus root :
   ```bash
   grep -q 'set -euo pipefail' tools/snapshot/snapshot.sh
   grep -q "trap " tools/snapshot/snapshot.sh
   grep -q '\$EUID' tools/snapshot/snapshot.sh
   ```

3. Read-only sanity (D-06, T-00-T4) :
   ```bash
   ! (grep -nE '\-X[[:space:]]*(POST|PUT|DELETE|PATCH)' tools/snapshot/snapshot.sh | grep -v '^[[:space:]]*#')
   ```

4. Env-only credentials (D-05) :
   ```bash
   grep -E '\$\{[A-Z_]+:\?' tools/snapshot/snapshot.sh | wc -l
   # Doit être ≥ 7 (au moins 7 env vars vérifiées)
   ```

5. CLI flags fonctionnent :
   ```bash
   ./tools/snapshot/snapshot.sh --help >/dev/null
   ! ./tools/snapshot/snapshot.sh --apps invalid 2>/dev/null
   ```

6. README opérationnel :
   ```bash
   wc -l tools/snapshot/README.md  # ≥ 80
   grep -q "kubectl port-forward" tools/snapshot/README.md
   grep -q "Authorization: MediaBrowser" tools/snapshot/README.md
   grep -qi "audit\|redact" tools/snapshot/README.md
   ```

7. Endpoints couverts (≥ 70 entrées au total) :
   ```bash
   grep -cE ':[a-z_]+\.(json|txt)$' tools/snapshot/snapshot.sh
   # Devrait être ≥ 70 (17 sonarr + 18 radarr + 14 prowlarr + 9 qbit + 16 seerr + 10 jellyfin)
   ```
</verification>

<success_criteria>
- [ ] `tools/snapshot/snapshot.sh` exécutable, ≥ 250 lignes, bash strict mode + trap (D-19)
- [ ] Couvre les 6 apps avec leurs 3 patterns d'auth (D-12, D-15, X-Api-Key)
- [ ] AUCUN `-X POST/PUT/DELETE/PATCH` (D-06, T-00-T4 mitigation)
- [ ] Lecture API keys via `${VAR:?...}` uniquement (D-05, T-00-T1)
- [ ] CLI flags `--apps` (whitelist anti-injection T-00-T5), `--output` (anti-traversal T-00-T6), `--dry-run`, `--help` (D-18)
- [ ] `tools/snapshot/README.md` ≥ 80 lignes, documente prérequis port-forwards + 7 env vars + 4 troubleshooting + audit anti-leak (D-08, D-11)
- [ ] Output via `jq --sort-keys '.'` pour tous les `.json` (D-16)
- [ ] Tolérance per-endpoint et per-app failure (D-13, D-14)
- [ ] `bash -n tools/snapshot/snapshot.sh` exit 0
</success_criteria>

<output>
Après complétion, créer `.planning/phases/00-bootstrap-repo-snapshot-raw/00-02-SUMMARY.md` listant :
- `tools/snapshot/snapshot.sh` taille en lignes + exécutabilité
- Nombre d'endpoints couverts par app (sonarr=17, radarr=18, prowlarr=14, qbittorrent=9, seerr=16, jellyfin=10)
- Output du grep no-write sanity (doit être vide)
- Output du `bash -n` sanity (doit exit 0)
- Confirmation `tools/snapshot/README.md` couvre tous les pitfalls 1-5 + audit anti-leak
- Pointer vers Plan 03 (run + audit + commit baseline) en next step
</output>
