---
phase: 05-reconciler-qbittorrent-split-tv-anime-family
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - tools/arrconf/tests/conftest.py
  - tools/arrconf/tests/fixtures/qbittorrent/categories.json
  - tools/arrconf/tests/fixtures/qbittorrent/preferences.json
  - tools/arrconf/tests/fixtures/qbittorrent/auth_login_ok.txt
  - tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json
  - tools/arrconf/tests/fixtures/sonarr/edge_cases/series_with_tv_tag.json
  - tools/arrconf/tests/fixtures/sonarr/remotepathmapping.json
  - tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json
  - tools/arrconf/tests/fixtures/radarr/remotepathmapping.json
autonomous: true
requirements: [REQ-app-coverage]
requirements_addressed: [REQ-app-coverage]

must_haves:
  truths:
    - "All test fixtures needed by Plans 04, 05, 06 exist on disk and are loadable via the existing _load_fixture helper"
    - "Fixtures are sanitized — no api keys, no passwords, no real PII (CLAUDE.md no-secret discipline + Phase 2.2 P06 RECOVERY lesson)"
    - "conftest.py exposes 8 new fixtures (the 7 listed in PATTERNS lines 698–729 + qbit_login_response_body) each decorated with @pytest.fixture and returning the loaded JSON or text"
    - "Wave 0 of validation strategy (05-VALIDATION.md §'Wave 0 Requirements') is closed: every MISSING reference is replaced by a real file"
  artifacts:
    - path: "tools/arrconf/tests/fixtures/qbittorrent/categories.json"
      provides: "Snapshot of GET /api/v2/torrents/categories — 3 pre-existing entries (cleanuparr-unlinked, radarr, sonarr) all with savePath: ''"
      contains: "cleanuparr-unlinked"
    - path: "tools/arrconf/tests/fixtures/qbittorrent/preferences.json"
      provides: "Trimmed snapshot of GET /api/v2/app/preferences — allowlist keys + 5 peripheral keys for realism"
      contains: "auto_tmm_enabled"
    - path: "tools/arrconf/tests/fixtures/qbittorrent/auth_login_ok.txt"
      provides: "Body fixture for qBit login success — single line 'Ok.'"
      contains: "Ok."
    - path: "tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json"
      provides: "Snapshot of GET /api/v3/series — 8 series, all tags=[] (D-05-MIG-01 starting state)"
    - path: "tools/arrconf/tests/fixtures/sonarr/edge_cases/series_with_tv_tag.json"
      provides: "Idempotence-proof fixture — 8 series, all tags=[2] (post-D-05-MIG-01 state)"
    - path: "tools/arrconf/tests/fixtures/sonarr/remotepathmapping.json"
      provides: "Snapshot of GET /api/v3/remotepathmapping — 1 existing entry (/data/complete/ → /data/torrents/complete/)"
    - path: "tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json"
      provides: "Snapshot of GET /api/v3/movie — 11 movies, all tags=[]"
    - path: "tools/arrconf/tests/fixtures/radarr/remotepathmapping.json"
      provides: "Snapshot of GET /api/v3/remotepathmapping — 1 entry mirroring Sonarr"
    - path: "tools/arrconf/tests/conftest.py"
      provides: "8 new @pytest.fixture functions"
      contains: "qbit_categories_fixture"
  key_links:
    - from: "snapshots/before-phase-5-2026-05-14/qbittorrent/categories.json"
      to: "tools/arrconf/tests/fixtures/qbittorrent/categories.json"
      via: "Manual copy + sanitize"
      pattern: "cleanuparr-unlinked"
    - from: "snapshots/before-phase-5-2026-05-14/sonarr/series.json"
      to: "tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json"
      via: "Copy + verify tags==[]"
      pattern: '"tags": \[\]'
---

<objective>
Stand up the 9 fixture files plus the conftest.py extensions so Plans 04 (qbittorrent reconciler tests), 05 (Sonarr split reconciler tests), and 06 (Radarr mirror) can run without scavenger-hunting for example payloads. This plan is a pure test scaffolding step — no production code touched.

Per the Phase 5 Validation Strategy (`05-VALIDATION.md §"Wave 0 Requirements"`): every MISSING reference must be replaced by a real file before Plan 04's first reconciler test runs.

This plan runs in PARALLEL with Plan 02 because their `files_modified` lists are disjoint. Both depend only on Plan 01.

Purpose: Honor the planner "interface-first task ordering" rule by shipping all upstream test inputs BEFORE the consuming tests are written.

Output: 8 NEW fixture files + 1 MODIFIED conftest.py with 8 new fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-CONTEXT.md
@.planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-RESEARCH.md
@.planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-PATTERNS.md
@.planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-VALIDATION.md
@tools/arrconf/tests/conftest.py
@tools/arrconf/tests/fixtures/sonarr/downloadclient.json
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live snapshot → committed fixture | Snapshot data captured against prod cluster moves into the repo. Sanitization layer is critical. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SECRET | Information disclosure (credential leak via fixture commit) | tests/fixtures/* | mitigate | Every fixture file undergoes a redaction grep before commit. tests/fixtures/qbittorrent/preferences.json is the SANITIZED & TRIMMED allowlist + 5 peripheral keys; it omits server-side fields like `web_ui_password` even though the live GET response carries a hashed value. |
| T-05-CONTENT | Tampering (fixtures drift from live cluster reality, masking real bugs) | tests/fixtures/* | accept | Fixtures are snapshots in time. Drift is acceptable — fixtures test the wire-shape, not live cluster monitoring. Phase 4 LEARNINGS "Suspended CronJobs mask config-schema regressions" stays mitigated by the separate cluster-side SC#4 manual gate in Plan 08. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 3.1: Materialize 8 fixture files from Wave 0 snapshot (sanitize + commit)</name>
  <files>tools/arrconf/tests/fixtures/qbittorrent/categories.json, tools/arrconf/tests/fixtures/qbittorrent/preferences.json, tools/arrconf/tests/fixtures/qbittorrent/auth_login_ok.txt, tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json, tools/arrconf/tests/fixtures/sonarr/edge_cases/series_with_tv_tag.json, tools/arrconf/tests/fixtures/sonarr/remotepathmapping.json, tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json, tools/arrconf/tests/fixtures/radarr/remotepathmapping.json</files>
  <read_first>
    - snapshots/before-phase-5-2026-05-14/qbittorrent/categories.json (source — live capture from Plan 01)
    - snapshots/before-phase-5-2026-05-14/qbittorrent/preferences.json (source — ~7 KiB; we trim down)
    - snapshots/before-phase-5-2026-05-14/sonarr/series.json (source — 8 series)
    - snapshots/before-phase-5-2026-05-14/sonarr/remotepathmapping.json (source — 1 entry)
    - snapshots/before-phase-5-2026-05-14/radarr/movie.json (source — 11 movies)
    - snapshots/before-phase-5-2026-05-14/radarr/remotepathmapping.json (source)
    - tools/arrconf/tests/fixtures/sonarr/downloadclient.json (analog — established sanitization shape)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-PATTERNS.md §"tests/fixtures/" rows (lines 41–49)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-RESEARCH.md §"Live Cluster State Captured" (the exact data shapes)
  </read_first>
  <action>
    For each fixture file, copy from the Wave 0 snapshot dir and sanitize. Use jq for shape-preserving edits.

    1. tools/arrconf/tests/fixtures/qbittorrent/categories.json — copy verbatim from `snapshots/before-phase-5-2026-05-14/qbittorrent/categories.json`. The 3 entries carry no secrets. Expected shape (dict keyed by name): { "cleanuparr-unlinked": { "name": "cleanuparr-unlinked", "savePath": "" }, "radarr": { ... }, "sonarr": { ... } }.

    2. tools/arrconf/tests/fixtures/qbittorrent/preferences.json — TRIM the snapshot's ~80-key payload to the 4 allowlisted keys + 5 peripheral keys via jq:
       `jq '{ category_changed_tmm_enabled, torrent_changed_tmm_enabled, auto_tmm_enabled, save_path, temp_path, max_active_downloads, max_active_uploads, queueing_enabled, locale }' snapshots/before-phase-5-2026-05-14/qbittorrent/preferences.json > tools/arrconf/tests/fixtures/qbittorrent/preferences.json`
       Confirm no key like `web_ui_password`/`web_ui_username` appears: `jq '. | keys' tools/arrconf/tests/fixtures/qbittorrent/preferences.json`. Per Pitfall 4 (RESEARCH.md line 858), booleans must remain JSON-typed: `jq -e '.auto_tmm_enabled | type == "boolean"'` must exit 0.

    3. tools/arrconf/tests/fixtures/qbittorrent/auth_login_ok.txt — write single-line `Ok.` (3 ASCII bytes + final newline). Used by respx login mocks.

    4. tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json — copy from `snapshots/before-phase-5-2026-05-14/sonarr/series.json`. Optionally drop heavy poster/banner payloads with `jq 'map(del(.images))'` to keep the fixture under 50 KiB. Verify shape: `jq -e '. | length == 8 and (all(.tags == []))' tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json` must exit 0.

    5. tools/arrconf/tests/fixtures/sonarr/edge_cases/series_with_tv_tag.json — copy of the above with `.tags` mutated to `[2]` on all 8 entries:
       `mkdir -p tools/arrconf/tests/fixtures/sonarr/edge_cases/`
       `jq 'map(.tags = [2])' tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json > tools/arrconf/tests/fixtures/sonarr/edge_cases/series_with_tv_tag.json`
       Verify: `jq -e '. | length == 8 and (all(.tags == [2]))' tools/arrconf/tests/fixtures/sonarr/edge_cases/series_with_tv_tag.json` must exit 0.

    6. tools/arrconf/tests/fixtures/sonarr/remotepathmapping.json — copy from snapshot. Expected: a single-entry list with host `qbittorrent.selfhost.svc.cluster.local`, remotePath `/data/complete/`, localPath `/data/torrents/complete/`.

    7. tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json — copy from `snapshots/before-phase-5-2026-05-14/radarr/movie.json`. Same sanitization. Verify: `jq -e '. | length == 11 and (all(.tags == []))' tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json` must exit 0.

    8. tools/arrconf/tests/fixtures/radarr/remotepathmapping.json — copy from snapshot. Single-entry list mirroring Sonarr.

    Final anti-leak audit on ALL 8 files: `grep -rE '(api[_-]?key|password|apikey|x-api-key|authorization)([^A-Za-z0-9_]|$).*[A-Za-z0-9_-]{16,}' tools/arrconf/tests/fixtures/qbittorrent/ tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json tools/arrconf/tests/fixtures/sonarr/edge_cases/series_with_tv_tag.json tools/arrconf/tests/fixtures/sonarr/remotepathmapping.json tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json tools/arrconf/tests/fixtures/radarr/remotepathmapping.json` MUST return no matches.

    Per Phase 2.2 P06 RECOVERY lesson: `mv` is aliased interactively. Use `jq ... > tmp && mv -f tmp dest` or rely on direct `>` redirection (preferred; no `mv` at all).
  </action>
  <verify>
    <automated>test -f tools/arrconf/tests/fixtures/qbittorrent/categories.json && test -f tools/arrconf/tests/fixtures/qbittorrent/preferences.json && test -f tools/arrconf/tests/fixtures/qbittorrent/auth_login_ok.txt && test -f tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json && test -f tools/arrconf/tests/fixtures/sonarr/edge_cases/series_with_tv_tag.json && test -f tools/arrconf/tests/fixtures/sonarr/remotepathmapping.json && test -f tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json && test -f tools/arrconf/tests/fixtures/radarr/remotepathmapping.json && jq -e '."cleanuparr-unlinked"' tools/arrconf/tests/fixtures/qbittorrent/categories.json >/dev/null && jq -e '. | length == 8 and (all(.tags == []))' tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json && jq -e '. | length == 11 and (all(.tags == []))' tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json && jq -e 'all(.tags == [2])' tools/arrconf/tests/fixtures/sonarr/edge_cases/series_with_tv_tag.json</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files listed in `<files>` exist on disk.
    - `jq -e '."cleanuparr-unlinked"' tools/arrconf/tests/fixtures/qbittorrent/categories.json` returns a non-null value.
    - `cat tools/arrconf/tests/fixtures/qbittorrent/auth_login_ok.txt` outputs `Ok.` (with trailing newline).
    - `jq -e 'has("auto_tmm_enabled")' tools/arrconf/tests/fixtures/qbittorrent/preferences.json` returns true AND `jq -e 'has("web_ui_password")' tools/arrconf/tests/fixtures/qbittorrent/preferences.json` returns false.
    - `jq -e '. | length == 8 and (all(.tags == []))' tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json` exits 0.
    - `jq -e '. | length == 8 and (all(.tags == [2]))' tools/arrconf/tests/fixtures/sonarr/edge_cases/series_with_tv_tag.json` exits 0.
    - `jq -e '. | length == 11 and (all(.tags == []))' tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json` exits 0.
    - `jq -e '.[0].remotePath | endswith("/")' tools/arrconf/tests/fixtures/sonarr/remotepathmapping.json` exits 0 (Pitfall 6 trailing-slash invariant).
    - Anti-leak grep returns no matches (exit code 1): `grep -rE '(api[_-]?key|password|apikey|authorization)([^A-Za-z0-9_]|$).*[A-Za-z0-9_-]{16,}' tools/arrconf/tests/fixtures/qbittorrent/ tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json tools/arrconf/tests/fixtures/sonarr/edge_cases/ tools/arrconf/tests/fixtures/radarr/`.
  </acceptance_criteria>
  <done>8 fixture files committed, audit-clean, ready for Plans 04–06 to consume via conftest.py fixtures.</done>
</task>

<task type="auto">
  <name>Task 3.2: Extend conftest.py with 8 new @pytest.fixture functions</name>
  <files>tools/arrconf/tests/conftest.py</files>
  <read_first>
    - tools/arrconf/tests/conftest.py (full file — read lines 1–100 for the `_load_fixture(...)` helper pattern + existing fixture shape)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-PATTERNS.md §"tests/conftest.py (MODIFIED — extend with qBit + series fixtures)" (lines 686–729)
  </read_first>
  <action>
    Open `tools/arrconf/tests/conftest.py` and append 8 new fixture functions at the bottom of the file (after existing fixtures). Mirror the existing pattern exactly — `@pytest.fixture` decorator, single-line return-`_load_fixture(...)` body, type annotations:

    - `qbit_categories_fixture() -> dict[str, Any]:` returns `_load_fixture("qbittorrent/categories.json")`. Docstring: "qBit GET /api/v2/torrents/categories — dict keyed by category name. Baseline: 3 pre-existing entries (cleanuparr-unlinked, radarr, sonarr) all with empty savePath (R-04 mitigation context)."

    - `qbit_preferences_fixture() -> dict[str, Any]:` returns `_load_fixture("qbittorrent/preferences.json")`. Docstring mentions Q2 allowlist resolution.

    - `qbit_login_response_body() -> str:` reads the .txt file: `(Path(__file__).parent / "fixtures" / "qbittorrent" / "auth_login_ok.txt").read_text().rstrip("\n")`. Used by respx login mocks per Pitfall 1.

    - `sonarr_series_with_no_tags_fixture() -> list[dict[str, Any]]:` returns `_load_fixture("sonarr/series_with_no_tags.json")`.

    - `sonarr_series_with_tv_tag_fixture() -> list[dict[str, Any]]:` returns `_load_fixture("sonarr/edge_cases/series_with_tv_tag.json")`.

    - `sonarr_remotepathmapping_fixture() -> list[dict[str, Any]]:` returns `_load_fixture("sonarr/remotepathmapping.json")`.

    - `radarr_movie_with_no_tags_fixture() -> list[dict[str, Any]]:` returns `_load_fixture("radarr/movie_with_no_tags.json")`.

    - `radarr_remotepathmapping_fixture() -> list[dict[str, Any]]:` returns `_load_fixture("radarr/remotepathmapping.json")`.

    If `_load_fixture` does not exist in the current conftest.py, use the existing helper (PATTERNS lines 692–695 documents the shape). The standard shape is `(Path(__file__).parent / "fixtures" / name).read_text() |> json.loads`.

    Validate conftest still parses + the fixtures collect:
    - `cd tools/arrconf && uv run pytest --collect-only -q` must exit 0.
    - `cd tools/arrconf && uv run ruff check tests/conftest.py` must exit 0.
    - `cd tools/arrconf && uv run mypy tests/conftest.py` must exit 0 (relax mypy strictness with `# type: ignore[no-any-return]` if `_load_fixture` returns Any — only as last resort).
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run pytest --collect-only -q >/dev/null && python3 -c "import re,pathlib; t=pathlib.Path('tests/conftest.py').read_text(); names=['qbit_categories_fixture','qbit_preferences_fixture','qbit_login_response_body','sonarr_series_with_no_tags_fixture','sonarr_series_with_tv_tag_fixture','sonarr_remotepathmapping_fixture','radarr_movie_with_no_tags_fixture','radarr_remotepathmapping_fixture']; missing=[n for n in names if not re.search(r'def\s+'+n+r'\b', t)]; assert not missing, f'missing: {missing}'" && uv run ruff check tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd tools/arrconf && uv run pytest --collect-only -q` exits 0.
    - All 8 fixture function names exist in `tools/arrconf/tests/conftest.py` (verified by the python3 one-liner in the `<automated>` block — exits 0 only when no name is missing).
    - `cd tools/arrconf && uv run ruff check tests/conftest.py` exits 0.
    - At least one of the new fixtures is consumable by a probe test (sanity check via `cd tools/arrconf && uv run pytest --fixtures 2>&1 | grep -c qbit_categories_fixture` returns at least 1).
  </acceptance_criteria>
  <done>8 new pytest fixtures available in conftest.py; downstream plans (04/05/06) can request them by name without any further plumbing.</done>
</task>

</tasks>

<verification>
End-of-plan gate:

```bash
# Fixture invariants
cd tools/arrconf && uv run pytest --collect-only -q
jq -e '. | length == 8' tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json
jq -e '. | length == 11' tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json
# Anti-leak
grep -rE '(api[_-]?key|password|apikey)([^A-Za-z0-9_]|$).*[A-Za-z0-9_-]{16,}' tools/arrconf/tests/fixtures/qbittorrent/ tools/arrconf/tests/fixtures/sonarr/series_with_no_tags.json tools/arrconf/tests/fixtures/sonarr/edge_cases/ tools/arrconf/tests/fixtures/radarr/movie_with_no_tags.json tools/arrconf/tests/fixtures/radarr/remotepathmapping.json && echo LEAK && exit 1 || true
# Full suite stays green
cd tools/arrconf && uv run pytest -x -q
```
</verification>

<success_criteria>
- 8 fixture files materialized, sanitized, committed.
- 8 new conftest.py fixtures, all collectable by pytest.
- No regression in existing arrconf tests.
- Wave 0 Requirements list in 05-VALIDATION.md is fully closed (every MISSING item is now present).
</success_criteria>

<output>
After completion, create `.planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-03-SUMMARY.md` capturing:
- Fixture file sizes (each should be < 50 KiB).
- Anti-leak grep result (must be "no matches").
- Any deviation from the snapshot (e.g., if the live cluster's `series.json` had unexpected entries beyond the 8 RESEARCH says, document the deviation).
</output>
