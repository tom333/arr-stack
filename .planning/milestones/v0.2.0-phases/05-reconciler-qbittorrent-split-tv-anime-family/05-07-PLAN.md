---
phase: 05-reconciler-qbittorrent-split-tv-anime-family
plan: 07
type: execute
wave: 4
depends_on: [02, 06]
files_modified:
  - charts/arr-stack/files/arrconf.yml
  - charts/arr-stack/files/configarr.yml
  - charts/arr-stack/values.yaml
  - charts/arr-stack/values.schema.json
  - schemas/arrconf-schema.json
  - tools/arrconf/tests/test_arrconf_yml_validates.py
  - tools/arrconf/tests/test_configarr_three_profiles.py
autonomous: true
requirements: [REQ-app-coverage]
requirements_addressed: [REQ-app-coverage]

must_haves:
  truths:
    - "charts/arr-stack/files/arrconf.yml declares qbittorrent.main with 6 explicit categories matching D-05-PATHS-01: sonarr-tv→/data/series, sonarr-anime→/data/anime, sonarr-family→/data/family, radarr-movies→/data/films, radarr-anime→/data/films-anime, radarr-family→/data/films-family (Pitfall 3 — explicit savePath, never empty)"
    - "arrconf.yml sonarr.main declares 3 tags (tv, anime, family), 3 root folders (/media/series existing + /media/anime + /media/family), 3 download_clients tagged [tv]/[anime]/[family], 4 remote_path_mappings (existing /data/complete/ + /data/series/ + /data/anime/ + /data/family/), series_tags{enable:true, default_tag:tv}"
    - "arrconf.yml radarr.main declares 3 tags (movies, anime, family per D-05-SPLIT-02), 3 root folders (/media/films KEPT existing + /media/films-anime + /media/films-family per D-05-PATHS-01), 3 download_clients, 4 remote_path_mappings (existing + /data/films/ + /data/films-anime/ + /data/films-family/), movie_tags{enable:true, default_tag:movies}"
    - "All remotePath and localPath values end with `/` (Pitfall 6 trailing-slash invariant)"
    - "arrconf.yml validates against the schema generated by `arrconf schema-gen` AND against pydantic load_config — verified by test_arrconf_yml_validates"
    - "configarr.yml declares 3 quality profiles per instance (MULTi.VF unchanged + Anime new + Family new). Family is a byte-equivalent CLONE of MULTi.VF per D-05-FAM-01."
    - "configarr.yml `custom_formats[].assign_scores_to[]` lists target all 3 profile names per entry; VOSTFR has explicit per-profile scores: MULTi.VF=-10000, Anime=+50, Family=-10000 (Pattern 7 / Q5 explicit-score syntax)"
    - "charts/arr-stack/values.yaml `arrconf.controllers.main.containers.main.args` last element bumps from sonarr,radarr,prowlarr to sonarr,radarr,prowlarr,qbittorrent (D-05-ARGS-01 — exact 1-line diff)"
    - "helm lint + helm template + kubeconform all exit 0 (REQ-helm-validation preserved)"
  artifacts:
    - path: "charts/arr-stack/files/arrconf.yml"
      provides: "Declarative Phase 5 arrconf scope — qbittorrent + sonarr/radarr split + remote_path_mappings + series_tags/movie_tags"
      contains: "qbittorrent:"
    - path: "charts/arr-stack/files/configarr.yml"
      provides: "3 quality profiles per instance + Phase-5-aware assign_scores_to"
      contains: "Anime"
    - path: "charts/arr-stack/values.yaml"
      provides: "Bumped arrconf CronJob args to include qbittorrent"
      contains: "sonarr,radarr,prowlarr,qbittorrent"
    - path: "tools/arrconf/tests/test_arrconf_yml_validates.py"
      provides: "NEW pytest module — loads charts/arr-stack/files/arrconf.yml via pydantic, asserts no ValidationError; cross-validates against the JSON schema via jsonschema"
      contains: "test_arrconf_yml_validates_against_pydantic"
    - path: "tools/arrconf/tests/test_configarr_three_profiles.py"
      provides: "NEW pytest module — loads configarr.yml as YAML, asserts 3 quality profiles per instance + Family clone shape"
      contains: "test_family_clone_of_multivf"
  key_links:
    - from: "charts/arr-stack/files/arrconf.yml qbittorrent.main.categories"
      to: "tools/arrconf/arrconf/reconcilers/qbittorrent.py reconcile path (Plan 04)"
      via: "ConfigMap mount + arrconf reads YAML + applies"
      pattern: "qbittorrent.*main.*categories.*sonarr-tv.*/data/series"
    - from: "charts/arr-stack/values.yaml arrconf.controllers.main.containers.main.args"
      to: "CronJob spec passed to arrconf binary in the pod"
      via: "Helm template render — args become argv on container start"
      pattern: 'apply.*--apps.*sonarr,radarr,prowlarr,qbittorrent'
---

<objective>
Land the chart-side declarative Phase 5 surface. Three files: arrconf.yml (the giant one — qbittorrent block + sonarr/radarr extensions), configarr.yml (3 quality profiles per instance per D-05-CONFIGARR-01 + D-05-FAM-01), and values.yaml (single-line bump to arrconf CronJob args per D-05-ARGS-01).

Per CLAUDE.md "Conventions Helm — umbrella chart": every new image needs a `# renovate: image=` annotation (Phase 5 introduces no new images — qBit was already pinned in Phase 4, configarr/arrconf already annotated; this plan therefore needs NO new annotations). Helm lint + kubeconform + values.schema.json must all stay green per REQ-helm-validation.

Two new test modules ensure the chart YAMLs round-trip through pydantic (arrconf.yml) and through a YAML-shape assertion (configarr.yml). These tests are CI's belt-and-suspenders against the chart drifting from the code.

Purpose: Land the "declarative source of truth" for Phase 5. Once committed and tagged, the my-kluster cluster will pick up the changes on its next ArgoCD sync. The actual cluster apply is Plan 08.

Output: 3 MODIFIED chart files + 1 REGENERATED JSON Schema + 1 REGENERATED values.schema.json + 2 NEW test modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-CONTEXT.md
@.planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-RESEARCH.md
@.planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-PATTERNS.md
@CLAUDE.md
@charts/arr-stack/files/arrconf.yml
@charts/arr-stack/files/configarr.yml
@charts/arr-stack/values.yaml
@charts/arr-stack/values.schema.json
@tools/arrconf/arrconf/config.py
@schemas/arrconf-schema.json
@.planning/phases/04-umbrella-chart-migration-des-9-apps/04-LEARNINGS.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chart YAML → cluster runtime config | arrconf.yml + configarr.yml become ConfigMaps mounted into the respective CronJob pods. Operator-controlled. |
| values.yaml `args` → container argv | The 1-line change to arrconf args expands the CronJob's reconcile scope; the Wave 0 Secret bootstrap is the prerequisite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-CATPATH | Tampering (chart YAML drift from code — e.g. typoed category save_path lands in /data/complete) | charts/arr-stack/files/arrconf.yml | mitigate | test_arrconf_yml_validates loads the file via pydantic (extra='forbid' rejects typos). CI runs this on every PR. Pitfall 3 invariant: every category MUST declare an explicit savePath — never blank. |
| T-05-CATPATH | Tampering (Pitfall 6 — trailing slash on remotePath/localPath dropped from YAML) | arrconf.yml remote_path_mappings entries | mitigate | The test asserts that for every entry, `remotePath.endswith("/") and localPath.endswith("/")`. If pydantic adds a model-level validator in a future plan, the test stays as belt-and-suspenders. |
| T-05-CONTENT | Tampering (configarr Family profile drifts from MULTi.VF) | charts/arr-stack/files/configarr.yml | mitigate | test_family_clone_of_multivf does a YAML-level deep-equal comparison between the MULTi.VF and Family quality_profile blocks (with the `name:` field excluded). |
| T-05-CONTENT | Frontière violation (configarr.yml gains a quality_profile that arrconf is supposed to never touch) | configarr.yml | accept | configarr explicitly OWNS quality_profiles per ADR-5; arrconf reconcilers raise ScopeViolationError on quality endpoints. The frontière is enforced at the code layer, not the chart layer. |
| T-05-CONTENT | Tampering (Renovate annotation removed during chart edit) | charts/arr-stack/values.yaml | accept | Phase 5 introduces NO new images. The 1-line diff to `arrconf.controllers.main.containers.main.args` doesn't touch annotations. CI check-renovate-annotations.sh (Phase 4 helper) still passes. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 7.1: Extend arrconf.yml with qbittorrent block + Sonarr/Radarr Phase 5 sections</name>
  <files>charts/arr-stack/files/arrconf.yml</files>
  <read_first>
    - charts/arr-stack/files/arrconf.yml (full file — current 44-line shape, sonarr.main download_clients only; the analog/baseline)
    - tools/arrconf/arrconf/config.py (the pydantic schemas — the YAML must conform)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-PATTERNS.md §"charts/arr-stack/files/arrconf.yml (MODIFIED)" (lines 778–802)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-RESEARCH.md §"Pattern 4: Sonarr/Radarr download client tag-routing — wire-up" (lines 534–603 — the canonical Sonarr block YAML)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-RESEARCH.md §"Q13 — Radarr root folder naming" (lines 1044–1074 — D-05-PATHS-01: Radarr uses /media/films, qBit category radarr-movies uses /data/films)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-CONTEXT.md `<decisions>` §D-05-PATHS-01 + §D-05-SPLIT-02
    - .planning/phases/04-umbrella-chart-migration-des-9-apps/04-LEARNINGS.md (Phase 4 ConfigMap mount pattern — confirm the file is read by both arrconf-configmap.yaml + the CronJob volume mount; no shape change needed here)
  </read_first>
  <action>
    Read the current arrconf.yml end-to-end before editing. Then rewrite the file to declare the Phase 5 scope. Use ruyaml (the project's YAML library) or hand-edit — both are acceptable since the file is operator-readable.

    Structure to produce (yaml-language-server modeline + sonarr.main + radarr.main + prowlarr.main UNCHANGED structurally + qbittorrent.main NEW):

    ```yaml
    # yaml-language-server: $schema=../../../schemas/arrconf-schema.json

    sonarr:
      main:
        base_url: http://sonarr.selfhost.svc.cluster.local:8989
        tags:
          prune: false
          items:
            - label: tv
            - label: anime
            - label: family
        root_folders:
          prune: false
          items:
            - path: /media/series
            - path: /media/anime
            - path: /media/family
        download_clients:
          prune: false
          items:
            - name: qBittorrent - TV
              enable: true
              protocol: torrent
              priority: 1
              implementation: QBittorrent
              configContract: QBittorrentSettings
              fields:
                - { name: host, value: qbittorrent.selfhost.svc.cluster.local }
                - { name: port, value: 8080 }
                - { name: useSsl, value: false }
                - { name: urlBase, value: "" }
                - { name: username, value: "" }
                - { name: password, value: "" }
                - { name: tvCategory, value: sonarr-tv }
                - { name: tvImportedCategory, value: "" }
                - { name: recentTvPriority, value: 0 }
                - { name: olderTvPriority, value: 0 }
                - { name: initialState, value: 0 }
                - { name: sequentialOrder, value: false }
                - { name: firstAndLast, value: false }
                - { name: contentLayout, value: 0 }
              tags: [tv]
              removeCompletedDownloads: true
              removeFailedDownloads: true
            - name: qBittorrent - Anime
              # ...same fields with tvCategory=sonarr-anime, tags: [anime]
            - name: qBittorrent - Family
              # ...same fields with tvCategory=sonarr-family, tags: [family]
        remote_path_mappings:
          prune: false
          items:
            - host: qbittorrent.selfhost.svc.cluster.local
              remotePath: /data/complete/
              localPath: /data/torrents/complete/
            - host: qbittorrent.selfhost.svc.cluster.local
              remotePath: /data/series/
              localPath: /data/torrents/series/
            - host: qbittorrent.selfhost.svc.cluster.local
              remotePath: /data/anime/
              localPath: /data/torrents/anime/
            - host: qbittorrent.selfhost.svc.cluster.local
              remotePath: /data/family/
              localPath: /data/torrents/family/
        series_tags:
          enable: true
          default_tag: tv

    radarr:
      main:
        base_url: http://radarr.selfhost.svc.cluster.local:7878
        tags:
          prune: false
          items:
            - label: movies   # D-05-SPLIT-02: Radarr default tag = `movies` (NOT `tv`)
            - label: anime
            - label: family
        root_folders:
          prune: false
          items:
            - path: /media/films            # KEPT existing per D-05-PATHS-01
            - path: /media/films-anime
            - path: /media/films-family
        download_clients:
          prune: false
          items:
            - name: qBittorrent - Movies
              # ...same fields with movieCategory=radarr-movies (Radarr's field name; verify against existing Phase 3 fixture if needed)
              tags: [movies]
            - name: qBittorrent - Anime
              tags: [anime]
            - name: qBittorrent - Family
              tags: [family]
        remote_path_mappings:
          prune: false
          items:
            - host: qbittorrent.selfhost.svc.cluster.local
              remotePath: /data/complete/
              localPath: /data/torrents/complete/
            - host: qbittorrent.selfhost.svc.cluster.local
              remotePath: /data/films/
              localPath: /data/torrents/films/
            - host: qbittorrent.selfhost.svc.cluster.local
              remotePath: /data/films-anime/
              localPath: /data/torrents/films-anime/
            - host: qbittorrent.selfhost.svc.cluster.local
              remotePath: /data/films-family/
              localPath: /data/torrents/films-family/
        movie_tags:
          enable: true
          default_tag: movies               # D-05-SPLIT-02

    prowlarr:
      main:
        base_url: http://prowlarr.selfhost.svc.cluster.local:9696
        # ... existing Phase 3 prowlarr config UNCHANGED ...

    qbittorrent:
      main:
        base_url: http://qbittorrent.selfhost.svc.cluster.local:8080
        categories:
          prune: false                       # NEVER set true — R-04, breaks cleanuparr
          items:
            - name: sonarr-tv
              savePath: /data/series         # D-05-PATHS-01
            - name: sonarr-anime
              savePath: /data/anime
            - name: sonarr-family
              savePath: /data/family
            - name: radarr-movies
              savePath: /data/films          # D-05-PATHS-01 — `/data/films` NOT `/data/movies`
            - name: radarr-anime
              savePath: /data/films-anime
            - name: radarr-family
              savePath: /data/films-family
        preferences:
          enable: false                      # opt-in OFF by default per Q2 / D-03-04
          # values: { ... }                  # operator opts in by setting enable: true and listing the 4 allowlisted keys
    ```

    Cross-check against the existing arrconf.yml's exact `fields:` shape for download_clients (Phase 1/2.1 set the pattern with empty username/password — keep them empty so the merge_fields_for_put helper preserves the cluster-stored values per Phase 2.1 / D-02.1-05). Verify by reading the existing download_clients block.

    For Radarr download_clients: verify the field name for category — Sonarr uses `tvCategory`; Radarr uses `movieCategory`. Check the existing Phase 3 radarr fixture or the live cluster snapshot.

    Validate the result locally:
    ```
    cd tools/arrconf && uv run python -c "from arrconf.config import load_config; cfg = load_config('../../charts/arr-stack/files/arrconf.yml'); print('main' in cfg.qbittorrent, len(cfg.qbittorrent['main'].categories.items), len(cfg.sonarr['main'].tags.items), len(cfg.radarr['main'].remote_path_mappings.items))"
    ```
    Expected output: `True 6 3 4` (qbit main exists, 6 categories, 3 sonarr tags, 4 sonarr RPM entries).
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run python -c "from arrconf.config import load_config; cfg = load_config('../../charts/arr-stack/files/arrconf.yml'); assert 'main' in cfg.qbittorrent and len(cfg.qbittorrent['main'].categories.items) == 6, f'qbit categories: {len(cfg.qbittorrent[\"main\"].categories.items)}'; assert len(cfg.sonarr['main'].tags.items) == 3; assert len(cfg.radarr['main'].tags.items) == 3; assert len(cfg.sonarr['main'].remote_path_mappings.items) == 4; assert len(cfg.radarr['main'].remote_path_mappings.items) == 4; assert cfg.sonarr['main'].series_tags.default_tag == 'tv'; assert cfg.radarr['main'].movie_tags.default_tag == 'movies'; rpms = list(cfg.sonarr['main'].remote_path_mappings.items) + list(cfg.radarr['main'].remote_path_mappings.items); assert all(r.remotePath.endswith('/') and r.localPath.endswith('/') for r in rpms), 'trailing-slash invariant violated'"</automated>
  </verify>
  <acceptance_criteria>
    - `charts/arr-stack/files/arrconf.yml` parses via pydantic without ValidationError (the verify command exits 0).
    - The file contains exactly 6 qbittorrent categories with these names: `sonarr-tv`, `sonarr-anime`, `sonarr-family`, `radarr-movies`, `radarr-anime`, `radarr-family`. Verify: `grep -c '  - name: sonarr-tv\|  - name: sonarr-anime\|  - name: sonarr-family\|  - name: radarr-movies\|  - name: radarr-anime\|  - name: radarr-family' charts/arr-stack/files/arrconf.yml` returns at least `6`.
    - The Radarr root folders include `/media/films` (KEPT per D-05-PATHS-01) AND `/media/films-anime` AND `/media/films-family`. Verify: `grep -cE 'path: /media/films($|-)' charts/arr-stack/files/arrconf.yml` returns at least `3`.
    - The qbittorrent radarr-movies category has savePath `/data/films` (NOT `/data/movies`). Verify: `grep -B 1 'savePath: /data/films$' charts/arr-stack/files/arrconf.yml | grep 'name: radarr-movies'`.
    - Sonarr tags are `tv, anime, family` AND Radarr tags are `movies, anime, family` (D-05-SPLIT-02). Verify the verify command's assertions pass.
    - All remotePath and localPath values end with `/` (Pitfall 6) — covered by the verify command's `all(r.remotePath.endswith('/') ...)` assertion.
  </acceptance_criteria>
  <done>Phase 5 arrconf.yml shipped; parses via pydantic; live cluster reality (D-05-PATHS-01) honored; trailing-slash invariant preserved.</done>
</task>

<task type="auto">
  <name>Task 7.2: Extend configarr.yml with Anime + Family quality profiles per instance (D-05-CONFIGARR-01 + D-05-FAM-01)</name>
  <files>charts/arr-stack/files/configarr.yml</files>
  <read_first>
    - charts/arr-stack/files/configarr.yml (full file — read lines 1–330 to absorb the existing MULTi.VF profile structure for both sonarr.main and radarr.main; the analog blocks)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-PATTERNS.md §"charts/arr-stack/files/configarr.yml (MODIFIED)" (lines 805–832)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-RESEARCH.md §"Pattern 7" (lines 752–804) — the configarr block sketch + Q5 explicit-score syntax
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-RESEARCH.md §"Q9" (lines 1018–1026) — TRaSH-Guides anime template availability
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-CONTEXT.md `<decisions>` §D-05-FAM-01 + §D-05-CONFIGARR-01 + §D-05-CONFIGARR-02
  </read_first>
  <action>
    1. Read the existing MULTi.VF quality_profile block for sonarr.main (lines 187–210 per PATTERNS.md). Copy it verbatim twice: once renamed to `Family` (D-05-FAM-01 — IDENTICAL scoring) and once renamed to `Anime` (D-05-CONFIGARR-02 — base structure with anime-specific scoring deltas via assign_scores_to).

    2. For the Anime profile (Pattern 7 + RESEARCH lines 766–781 / Q9 resolution):
       - Same `qualities:` block as MULTi.VF (1080p HD, no Remux, no 4K — our existing scope).
       - Consider using `include: - template: sonarr-v4-quality-profile-1080p-french-anime-multi` per Q9 if the recyclarr template resolves cleanly. If that template include fails on first lint, fall back to hand-rolled — the planner records the choice in the SUMMARY.
       - The differentiation vs MULTi.VF is ONLY at the `assign_scores_to` level: VOSTFR scores +50 for Anime (vs -10000 for MULTi.VF/Family).

    3. For the Family profile (D-05-FAM-01): byte-equivalent clone of MULTi.VF EXCEPT for the `name:` field. The clone is intentional — the Family differentiation is path/tag-only, not quality semantics.

    4. Extend the `custom_formats[].assign_scores_to[]` lists (Pattern 7 lines 783–803 + RESEARCH §"configarr — Quality Profiles + assign_scores_to" lines 267–298). Use the EXPLICIT per-profile score syntax (Q5 resolution). Critical entries:
       - `fr-vff, fr-vfi, fr-vfq, fr-multi`: target all 3 profiles; default score from custom_format inherits.
       - `fr-vostfr`: explicit per-profile — `MULTi.VF: -10000`, `Anime: +50`, `Family: -10000` (the load-bearing differentiation).
       - `fr-mhd, fr-x265-hd`: target all 3 profiles; default scoring.

    5. Mirror the same 3-profile shape in `radarr.main` (Q9/A6 caveat — the Radarr French Anime template may not exist; hand-roll the Radarr `Anime` profile if so; document in SUMMARY).

    6. Validate the YAML structure (no schema layer for configarr.yml — manual review + the test in Task 7.4):
       ```
       cd tools/arrconf && uv run python -c "from ruamel.yaml import YAML; y = YAML(typ='safe'); doc = y.load(open('../../charts/arr-stack/files/configarr.yml')); profs_son = [p['name'] for p in doc['sonarr']['main']['quality_profiles']]; profs_rad = [p['name'] for p in doc['radarr']['main']['quality_profiles']]; print('sonarr profiles:', profs_son); print('radarr profiles:', profs_rad); assert 'MULTi.VF' in profs_son and 'Anime' in profs_son and 'Family' in profs_son and 'MULTi.VF' in profs_rad and 'Anime' in profs_rad and 'Family' in profs_rad"
       ```
       (Use the existing YAML library shipped with arrconf — likely `ruyaml` or `ruamel.yaml`; adapt the import if needed.)
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run python -c "
import sys
try:
    from ruamel.yaml import YAML
except ImportError:
    import yaml as _y
    YAML = None
if YAML:
    y = YAML(typ='safe'); doc = y.load(open('../../charts/arr-stack/files/configarr.yml'))
else:
    doc = _y.safe_load(open('../../charts/arr-stack/files/configarr.yml'))
son = [p['name'] for p in doc['sonarr']['main']['quality_profiles']]
rad = [p['name'] for p in doc['radarr']['main']['quality_profiles']]
assert sorted(son) == ['Anime', 'Family', 'MULTi.VF'], f'sonarr profiles: {son}'
assert sorted(rad) == ['Anime', 'Family', 'MULTi.VF'], f'radarr profiles: {rad}'
print('configarr.yml: 3 profiles per instance OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - The configarr.yml verify command exits 0 (3 profiles per instance, names exact).
    - `grep -c '^      - name: Anime$\|^      - name: Family$\|^      - name: MULTi.VF$' charts/arr-stack/files/configarr.yml` returns at least `6` (3 names × 2 instances).
    - `grep -c 'fr-vostfr' charts/arr-stack/files/configarr.yml` returns at least `2` (Sonarr + Radarr custom_formats blocks both reference fr-vostfr per Pattern 7).
    - `grep -E 'score: 50' charts/arr-stack/files/configarr.yml | wc -l` returns at least `2` (the per-profile VOSTFR=+50 override for Anime appears once per instance).
    - `grep -E 'score: -10000' charts/arr-stack/files/configarr.yml | wc -l` returns at least `4` (VOSTFR=-10000 appears for MULTi.VF AND Family in each of the 2 instances).
  </acceptance_criteria>
  <done>configarr.yml ships 3 profiles per instance with the explicit-score per-profile assign_scores_to syntax. Family is a clone of MULTi.VF.</done>
</task>

<task type="auto">
  <name>Task 7.3: Bump charts/arr-stack/values.yaml arrconf args (D-05-ARGS-01) + regenerate schemas</name>
  <files>charts/arr-stack/values.yaml, charts/arr-stack/values.schema.json, schemas/arrconf-schema.json</files>
  <read_first>
    - charts/arr-stack/values.yaml (lines 423–478 — the `arrconf` alias block; line 455 is the target — the args list)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-PATTERNS.md §"charts/arr-stack/values.yaml (MODIFIED — single line)" (lines 836–851)
    - charts/arr-stack/values.schema.json (the existing schema — regenerate via losisin plugin per CLAUDE.md/Phase 4)
    - schemas/arrconf-schema.json (regenerated in Plan 02; verify it still matches arrconf schema-gen output now that the YAML is committed)
  </read_first>
  <action>
    1. Open `charts/arr-stack/values.yaml`. Find the `arrconf:` alias and within `controllers.main.containers.main.args`, change the LAST element from `"sonarr,radarr,prowlarr"` to `"sonarr,radarr,prowlarr,qbittorrent"`. This is the SINGLE-LINE change D-05-ARGS-01 mandates.

    2. Verify the diff is minimal:
       ```
       git diff charts/arr-stack/values.yaml | wc -l
       ```
       Should show a typical 5-line diff (3 context + 1 removed + 1 added).

    3. Regenerate values.schema.json. Per Phase 4, the project uses the `losisin/helm-values-schema-json` plugin or similar. Check `Chart.yaml` / project tooling. If unsure, run:
       ```
       cd charts/arr-stack && helm schema || helm-values-schema-json -input values.yaml -output values.schema.json
       ```
       (Adjust to the project's exact incantation — refer to Phase 4 02-LEARNINGS or check `chart-lint.yml` workflow for the canonical command.)

    4. Regenerate schemas/arrconf-schema.json (Plan 02 already did this for the schema changes; re-run idempotently to verify):
       ```
       cd tools/arrconf && uv run arrconf schema-gen --output ../../schemas/arrconf-schema.json
       ```
       If the file changes (it shouldn't if Plan 02's regeneration was correct), commit the diff. CI test_schema_gen blocks if not.

    5. Confirm `arrconf.yml` validates against the regenerated arrconf-schema.json:
       ```
       cd tools/arrconf && uv run python -c "
       import json, jsonschema, yaml
       schema = json.load(open('../../schemas/arrconf-schema.json'))
       doc = yaml.safe_load(open('../../charts/arr-stack/files/arrconf.yml'))
       jsonschema.validate(doc, schema)
       print('arrconf.yml validates against arrconf-schema.json: OK')
       "
       ```
  </action>
  <verify>
    <automated>grep -c 'sonarr,radarr,prowlarr,qbittorrent' charts/arr-stack/values.yaml && cd tools/arrconf && uv run arrconf schema-gen --output /tmp/schema-check.json && diff -q /tmp/schema-check.json ../../schemas/arrconf-schema.json && uv run python -c "import json, jsonschema, yaml; jsonschema.validate(yaml.safe_load(open('../../charts/arr-stack/files/arrconf.yml')), json.load(open('../../schemas/arrconf-schema.json')))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sonarr,radarr,prowlarr,qbittorrent' charts/arr-stack/values.yaml` returns at least `1`.
    - `grep -c 'sonarr,radarr,prowlarr"$\|sonarr,radarr,prowlarr$' charts/arr-stack/values.yaml` returns `0` (the OLD args string is GONE — no leftover).
    - `git diff --stat charts/arr-stack/values.yaml` shows a minimal change (1 file changed, 1 insertion(+), 1 deletion(-) per D-05-ARGS-01's "single-line" promise).
    - `arrconf schema-gen` output matches the committed `schemas/arrconf-schema.json` byte-for-byte (the verify command's `diff -q` exits 0 — Phase 1 D-29 CI test_schema_gen gate stays green).
    - `arrconf.yml` validates against `schemas/arrconf-schema.json` via jsonschema (the verify command's last line exits 0).
  </acceptance_criteria>
  <done>D-05-ARGS-01 single-line bump landed; values.schema.json regenerated; arrconf-schema.json idempotence gate preserved; arrconf.yml validates against the published schema.</done>
</task>

<task type="auto">
  <name>Task 7.4: Author test_arrconf_yml_validates.py + test_configarr_three_profiles.py + helm-lint smoke</name>
  <files>tools/arrconf/tests/test_arrconf_yml_validates.py, tools/arrconf/tests/test_configarr_three_profiles.py</files>
  <read_first>
    - tools/arrconf/tests/test_config.py (an analog for how the existing tests load YAML via pydantic)
    - tools/arrconf/tests/conftest.py (potentially uses _load_fixture; we may need a path-resolver helper for the chart YAMLs)
    - charts/arr-stack/files/arrconf.yml (the YAML under test from Task 7.1)
    - charts/arr-stack/files/configarr.yml (the YAML under test from Task 7.2)
    - .planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-VALIDATION.md (task IDs 05-05-01 and 05-06-01/02 — the validation strategy already mapped these)
  </read_first>
  <action>
    1. Create `tools/arrconf/tests/test_arrconf_yml_validates.py`. Tests:
       - `test_arrconf_yml_validates_against_pydantic` — `load_config(<absolute path to charts/arr-stack/files/arrconf.yml>)` returns a `RootConfig` instance; `cfg.qbittorrent["main"]` exists; the 6 expected category names appear; etc. Use the same assertions from Task 7.1's verify command, hardened.
       - `test_arrconf_yml_validates_against_json_schema` — load the JSON schema, load the YAML, call `jsonschema.validate(doc, schema)`; no exception.
       - `test_arrconf_yml_all_remote_path_mappings_end_with_slash` — explicit assertion of Pitfall 6 invariant.
       - `test_arrconf_yml_radarr_movies_category_uses_films_path` — D-05-PATHS-01 dispositive: assert the `radarr-movies` category's savePath is `/data/films` (NOT `/data/movies`).

       Use `Path(__file__).resolve().parents[2] / "charts" / "arr-stack" / "files" / "arrconf.yml"` to find the YAML — note: tools/arrconf/tests is 2 dirs deep from the repo root, so `parents[2]` is the repo root. Verify with `os.path.exists` in the first test.

    2. Create `tools/arrconf/tests/test_configarr_three_profiles.py`. Tests:
       - `test_three_profiles_per_instance` — load configarr.yml as YAML, assert Sonarr `quality_profiles` names == [MULTi.VF, Anime, Family] (or sorted equivalent), same for Radarr.
       - `test_family_clone_of_multivf` — deep-equal compare the Sonarr MULTi.VF profile block and Family profile block, EXCLUDING the `name:` field. Use `copy.deepcopy(p); p.pop('name')` for both, then `assert family_dict == multivf_dict`. Same for Radarr.
       - `test_vostfr_score_differs_per_profile` — find the custom_formats entry for `fr-vostfr`, walk its `assign_scores_to`, assert that MULTi.VF and Family have `score: -10000` while Anime has `score: 50`.
       - `test_no_quality_profile_named_anime_or_family_before_phase_5_baseline` — read the snapshot file `snapshots/before-phase-5-2026-05-14/sonarr/qualityprofile.json` if present; assert it does NOT contain a profile named `Anime` or `Family` (R-06 mitigation — this would conflict with the new profile names). If the snapshot doesn't exist (Plan 01 didn't capture qualityprofile.json), skip the test with pytest.skip and a TODO note.

    3. Run both test files:
       ```
       cd tools/arrconf && uv run pytest tests/test_arrconf_yml_validates.py tests/test_configarr_three_profiles.py -x -v
       cd tools/arrconf && uv run pytest -x -q
       ```

    4. Run helm lint + kubeconform smoke (chart-side gate; CLAUDE.md "En local, sans cluster" section + REQ-helm-validation):
       ```
       helm dependency update charts/arr-stack/
       helm lint charts/arr-stack/
       helm template charts/arr-stack/ -f examples/values-prod.yaml | kubeconform -
       ```
       The Helm 4 multi-alias workaround from CLAUDE.md (untar + cp) MAY be needed. Refer to chart-lint.yml in `.github/workflows/`. Document if the local workaround was needed in the SUMMARY.

    5. Lint:
       ```
       cd tools/arrconf && uv run ruff check tests/test_arrconf_yml_validates.py tests/test_configarr_three_profiles.py
       ```
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run pytest tests/test_arrconf_yml_validates.py tests/test_configarr_three_profiles.py -x -q && uv run pytest -x -q && uv run ruff check tests/test_arrconf_yml_validates.py tests/test_configarr_three_profiles.py</automated>
  </verify>
  <acceptance_criteria>
    - File `tools/arrconf/tests/test_arrconf_yml_validates.py` exists with at least 4 test functions.
    - File `tools/arrconf/tests/test_configarr_three_profiles.py` exists with at least 4 test functions.
    - `cd tools/arrconf && uv run pytest tests/test_arrconf_yml_validates.py tests/test_configarr_three_profiles.py -x -q` exits 0.
    - `cd tools/arrconf && uv run pytest -x -q` exits 0 (full-suite no regression).
    - `helm lint charts/arr-stack/` exits 0 — recorded in SUMMARY with output excerpt.
    - `helm template charts/arr-stack/ -f examples/values-prod.yaml | kubeconform -` exits 0 — recorded in SUMMARY with output excerpt.
    - `cd tools/arrconf && uv run ruff check tests/test_arrconf_yml_validates.py tests/test_configarr_three_profiles.py` exits 0.
  </acceptance_criteria>
  <done>Chart YAMLs are validated by pytest in CI; helm lint + kubeconform stay green; D-05-PATHS-01 + D-05-FAM-01 + Q5 VOSTFR-per-profile invariant under regression test.</done>
</task>

</tasks>

<verification>
End-of-plan gate:

```bash
# All arrconf tests (config, CLI, fixtures, reconcilers, chart YAMLs)
cd tools/arrconf && uv run pytest -x -q
cd tools/arrconf && uv run pytest --cov=arrconf --cov-fail-under=70
# Schema gates
cd tools/arrconf && uv run arrconf schema-gen --output /tmp/s.json && diff -q /tmp/s.json ../../schemas/arrconf-schema.json
# Helm gates
helm dependency update charts/arr-stack/
helm lint charts/arr-stack/
helm template charts/arr-stack/ -f examples/values-prod.yaml | kubeconform -
# Renovate annotations preserved
bash tools/scripts/check-renovate-annotations.sh
# Byte-equivalence sanity (Phase 4 helper — confirms no surprising delta to the rendered chart)
bash tools/scripts/byte-equivalence-diff.sh || true   # informational only — Phase 5 will of course produce a meaningful delta
```
</verification>

<success_criteria>
- arrconf.yml ships the 6 qBit categories, 3 sonarr tags, 3 radarr tags (movies/anime/family), 3 root folders per instance, 3 download clients per instance, 4 RPM entries per instance, series_tags + movie_tags blocks.
- configarr.yml ships 3 quality profiles per instance (MULTi.VF + Anime + Family); Family is a byte-equivalent clone of MULTi.VF.
- values.yaml has the 1-line D-05-ARGS-01 bump.
- All schemas regenerated and idempotent.
- helm lint + kubeconform + values.schema.json gates stay green.
- 4 new pytest tests cover chart YAML invariants.
</success_criteria>

<output>
After completion, create `.planning/phases/05-reconciler-qbittorrent-split-tv-anime-family/05-07-SUMMARY.md` capturing:
- Total arrconf.yml line count delta (~150 lines added).
- configarr.yml profile choices: did the Anime template `sonarr-v4-quality-profile-1080p-french-anime-multi` resolve in configarr, or did we hand-roll?
- helm lint + kubeconform output.
- Confirmation that `check-renovate-annotations.sh` still passes.
- The 1-line diff to values.yaml (the single-line D-05-ARGS-01 change).
</output>
