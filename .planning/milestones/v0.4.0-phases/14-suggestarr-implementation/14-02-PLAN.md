---
phase: 14-suggestarr-implementation
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - charts/arr-stack/values.yaml
autonomous: false
requirements:
  - REQ-suggestarr-integration
tags:
  - helm
  - sidecar
  - categories-routing
  - live-cluster-discovery
user_setup:
  - service: suggestarr
    why: "Live-cluster discovery: Jellyfin library ItemIds + Sonarr/Radarr profileId values must be captured from the running cluster — they are not knowable at plan time. These values are NOT shipped via a ConfigMap (per 13-RESEARCH lines 488, 492-494: SuggestArr's config persists in its SQLite/YAML inside the PVC and is configured via the web UI post-deploy). The captured values are written to evidence files under .planning/phases/14-suggestarr-implementation/evidence/ and re-surfaced in 14-HUMAN-UAT.md as operator-pastable web-UI values."
    env_vars: []
    dashboard_config:
      - task: "Run discovery kubectl port-forwards + curl commands per CONTEXT canonical_refs §Cluster discovery commands; capture outputs into 4 evidence files (jellyfin-virtual-folders.txt, sonarr-quality-profiles.txt, radarr-quality-profiles.txt, derived-routing-values.md)"
        location: "Operator workstation with kubeconfig pointing at my-kluster"
      - task: "TMDB_API_KEY obtained from https://www.themoviedb.org/settings/api"
        location: "Operator personal TMDB account — value will be sealed into my-kluster arrconf-env SealedSecret in a SEPARATE my-kluster PR (D-13). Plan 03's 14-HUMAN-UAT.md documents the SealedSecret update procedure (Pre-deploy gate)."
      - task: "Post-deploy: paste the evidence-file values into SuggestArr's web UI (Settings → Jellyfin Libraries + Settings → Seer Integration)"
        location: "Port-forwarded SuggestArr web UI at http://localhost:5000 — full step-by-step in 14-HUMAN-UAT.md Scenario 3"

must_haves:
  truths:
    - "values.yaml suggestarr: block exists with controller type Deployment, image ciuse99/suggestarr:v2.7.3 pinned, Renovate annotation # renovate: image=docker.io/ciuse99/suggestarr (D-09), per-container env remap JELLYFIN_API_KEY→JELLYFIN_TOKEN + SEERR_API_KEY→SEER_TOKEN + TMDB_API_KEY direct (D-01), 1Gi PVC at /app/config/config_files, NO Ingress block (D-14) — SC#1 deployment artifact present"
    - "NO ConfigMap template exists in templates/ for SuggestArr (per 13-RESEARCH line 488 — SuggestArr ignores file-based config; web UI is the only config interface). Plan 02 explicitly does NOT create files/suggestarr-config.yml nor templates/suggestarr-configmap.yaml"
    - "helm template charts/arr-stack/ -f examples/values-prod.yaml | grep 'kind: Deployment' shows the suggestarr Deployment renders (SC#4 precondition — chart renders before ArgoCD sync)"
    - "helm template charts/arr-stack/ -f examples/values-prod.yaml | grep 'kind: PersistentVolumeClaim' shows the 1Gi PVC renders"
    - "helm template charts/arr-stack/ -f examples/values-prod.yaml does NOT emit a ConfigMap named suggestarr-config (negative assertion — SC#1 boundary)"
    - "helm lint charts/arr-stack/ exits 0 after suggestarr: block added (SC#4 precondition)"
    - "Task 2.1 captures 4 evidence files in .planning/phases/14-suggestarr-implementation/evidence/ — they feed 14-HUMAN-UAT.md (Plan 03) as operator-pastable values, NOT a ConfigMap mount"
  artifacts:
    - path: "charts/arr-stack/values.yaml"
      provides: "suggestarr: alias values block — controller, image, env remap, PVC, NO Ingress, NO ConfigMap mount"
      contains: "alias-equivalent: suggestarr key"
      pattern: "docker.io/ciuse99/suggestarr"
    - path: ".planning/phases/14-suggestarr-implementation/evidence/jellyfin-virtual-folders.txt"
      provides: "Live-captured Jellyfin VirtualFolders → 10 entries (ItemId + Name) per arrconf.yml categories"
      contains: "Name: ItemId"
    - path: ".planning/phases/14-suggestarr-implementation/evidence/sonarr-quality-profiles.txt"
      provides: "Live-captured Sonarr quality profiles for routing reference"
      contains: "id: name"
    - path: ".planning/phases/14-suggestarr-implementation/evidence/radarr-quality-profiles.txt"
      provides: "Live-captured Radarr quality profiles for routing reference"
      contains: "id: name"
    - path: ".planning/phases/14-suggestarr-implementation/evidence/derived-routing-values.md"
      provides: "Operator-pastable derived values: 10 JELLYFIN_LIBRARIES entries (Name + ItemId + is_anime flag) + 4 SEER_ANIME_PROFILE_CONFIG keys (profileId + rootFolder) with provenance comments mirroring arrconf.yml — consumed verbatim by 14-HUMAN-UAT.md Scenario 3"
      contains: "SEER_ANIME_PROFILE_CONFIG"
      min_lines: 40
  key_links:
    - from: "values.yaml suggestarr.controllers.main.containers.main.env[*].secretKeyRef"
      to: "Kubernetes Secret arrconf-env (existing SealedSecret, my-kluster managed)"
      via: "secretKeyRef.name=arrconf-env + key={JELLYFIN_API_KEY, SEERR_API_KEY, TMDB_API_KEY}"
      pattern: "name: arrconf-env"
    - from: "values.yaml suggestarr.persistence.config.type=persistentVolumeClaim"
      to: "SuggestArr's SQLite + config YAML (web-UI-managed) at /app/config/config_files"
      via: "1Gi PVC RWO — config persists across pod restarts; web UI is the only interface (13-RESEARCH line 488)"
      pattern: "/app/config/config_files"
    - from: ".planning/phases/14-suggestarr-implementation/evidence/derived-routing-values.md"
      to: ".planning/phases/14-suggestarr-implementation/14-HUMAN-UAT.md Scenario 3 (web-UI config)"
      via: "operator reads the evidence file at UAT time, pastes 10 Jellyfin ItemIds + 4 SEER_ANIME_PROFILE_CONFIG keys into the SuggestArr web UI Settings panels"
      pattern: "SEER_ANIME_PROFILE_CONFIG|JELLYFIN_LIBRARIES"
---

<objective>
Configure SuggestArr in the umbrella chart: populate the `suggestarr:` block in `values.yaml` (controller + image + env remap + PVC, NO Ingress, NO ConfigMap mount). Capture the 4 evidence files (Jellyfin ItemIds + Sonarr/Radarr profile IDs + derived routing values) for operator consumption at UAT time.

Purpose: This plan lands the chart-side mechanics. The Categories-routing CONFIG itself (SEER_ANIME_PROFILE_CONFIG + JELLYFIN_LIBRARIES) does NOT ship as a Kubernetes ConfigMap — per 13-RESEARCH §"Phase 14 Implementation Guidance" line 488 ("No ConfigMap needed for SuggestArr: config persists in the SQLite DB / YAML inside the PVC. The web UI is the configuration interface.") and lines 492-494 ("SEER_ANIME_PROFILE_CONFIG ... is set via the SuggestArr web UI (Settings → Seer Integration), NOT via environment variables.").

The original revision-1 of this plan invented a `files/suggestarr-config.yml` ConfigMap mount that SuggestArr silently ignores at runtime. This revision aligns with the research: the routing config is configured by the operator via SuggestArr's web UI POST-DEPLOY, using the evidence files captured here as paste-ready source values. The web-UI configuration sequence is documented in Plan 03's 14-HUMAN-UAT.md (Scenario 3).

Output: 1 file modified (values.yaml) + 4 evidence files written.

After this plan, `helm template -f examples/values-prod.yaml` emits a Deployment + Service + PVC for SuggestArr (NO ConfigMap named suggestarr-config) — ready for ArgoCD sync. Post-sync, the operator follows 14-HUMAN-UAT.md to enter the routing config in the web UI.

Critical: 4 IDs are NOT knowable at plan-author-time — they MUST be captured live during plan execution via kubectl port-forwards. Task 2.1 is an operator-driven checkpoint that captures them into evidence files; Task 2.2 is fully autonomous (writes only values.yaml; the evidence files for UAT consumption are already written by Task 2.1).

Out of scope (per D-14 + CONTEXT § Out of Scope):
- NO Ingress (web UI internal-only — no nginx, no oauth2-proxy, no Tailscale).
- NO `CRON_TIMES` env override (D-03: default daemon polling).
- NO arrconf.yml `suggestarr:` block (Claude's Discretion — sidecar-only, arrconf.yml stays reconciler-scoped).
- NO my-kluster SealedSecret YAML in this repo (D-13 — separate operator-driven PR; Plan 03's UAT documents the procedure).
- **NO `templates/suggestarr-configmap.yaml`** — explicitly deleted from the original revision-1 scope (revision-2 alignment with 13-RESEARCH line 488).
- **NO `files/suggestarr-config.yml`** — same reason. The routing config lives in the operator's head and in `.planning/phases/14-suggestarr-implementation/evidence/derived-routing-values.md`, NOT in a chart file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-suggestarr-implementation/14-CONTEXT.md
@.planning/phases/13-suggestarr-research-spike/13-RESEARCH.md
@.planning/phases/14-suggestarr-implementation/14-01-SUMMARY.md
@CLAUDE.md
@charts/arr-stack/values.yaml
@charts/arr-stack/files/arrconf.yml

<interfaces>
<!-- Decisions implemented (cross-referenced for executor traceability): -->
<!-- D-01: Per-container env remap (JELLYFIN_API_KEY → JELLYFIN_TOKEN, SEERR_API_KEY → SEER_TOKEN, TMDB_API_KEY direct) -->
<!-- D-04: JELLYFIN_LIBRARIES lists ALL 10 categories — RECORDED in evidence/derived-routing-values.md, ENTERED by operator in SuggestArr web UI post-deploy (D-04 revised per revision-2) -->
<!-- D-05: SEER_ANIME_PROFILE_CONFIG.anime_tv.profileId ← arrconf.yml seerr.main.sonarr_service.activeAnimeProfileId — RECORDED in evidence, ENTERED via web UI (D-05 revised per revision-2) -->
<!-- D-06: default_tv.profileId ← activeProfileId ; default_movie.profileId ← radarr_service.activeProfileId — RECORDED in evidence, ENTERED via web UI -->
<!-- D-07: rootFolder values ← activeAnimeDirectory / activeDirectory (sonarr + radarr) — RECORDED in evidence, ENTERED via web UI -->
<!--      WARNING: radarr_service.activeAnimeDirectory does NOT exist in arrconf.yml today. -->
<!--      Resolution: derive anime_movie.rootFolder from arrconf.yml::categories[] where kind=movies AND profile=anime → /media/films-zoe. Recorded in derived-routing-values.md. -->
<!-- D-08: Family-bucket suggestions fall into default_tv/default_movie (accepted limitation, NOT engineered around) -->
<!-- D-09: # renovate: image=docker.io/ciuse99/suggestarr (registry-explicit) -->
<!-- D-12: Atomic PR — Plans 01/02/03 land together -->
<!-- D-14: NO Ingress, NO TMDB watchlist, NO CRON_TIMES tune, NO ConfigMap (revision-2 addition) -->

<!-- arrconf.yml source-of-truth fields (verbatim from charts/arr-stack/files/arrconf.yml lines 148-181): -->
```yaml
seerr:
  main:
    sonarr_service:
      activeProfileId: 6              # → default_tv.profileId (D-06) — operator enters in web UI
      activeDirectory: /media/series  # → default_tv.rootFolder (D-07) — operator enters in web UI
      activeAnimeProfileId: 8         # → anime_tv.profileId (D-05) — operator enters in web UI
      activeAnimeDirectory: /media/anime  # → anime_tv.rootFolder (D-07) — operator enters in web UI
    radarr_service:
      activeProfileId: 6              # → default_movie.profileId (D-06) — operator enters in web UI
      activeDirectory: /media/films   # → default_movie.rootFolder (D-07) — operator enters in web UI
      # activeAnimeProfileId  → ABSENT (no Radarr anime equivalent on Seerr per D-06-SEERR-RADARR-LIMIT in Phase 6)
      # activeAnimeDirectory  → ABSENT — derive anime_movie.rootFolder from categories[] (kind=movies, profile=anime → /media/films-zoe)
```

<!-- arrconf.yml categories[] (verbatim from lines 3-53, 10 entries): -->
```yaml
categories:
  - { name: series,                 kind: series, profile: general, base_path: /media/series }
  - { name: series-emilie,          kind: series, profile: general, base_path: /media/series-emilie }
  - { name: series-thomas,          kind: series, profile: general, base_path: /media/series-thomas }
  - { name: series-garcons,         kind: series, profile: family,  base_path: /media/series-garcons }
  - { name: series-zoe,             kind: series, profile: anime,   base_path: /media/series-zoe }
  - { name: films,                  kind: movies, profile: general, base_path: /media/films }
  - { name: nouveaux-films,         kind: movies, profile: general, base_path: /media/nouveaux-films }
  - { name: films-enfants,          kind: movies, profile: family,  base_path: /media/films-enfants }
  - { name: films-animation-enfants, kind: movies, profile: family,  base_path: /media/films-animation-enfants }
  - { name: films-zoe,              kind: movies, profile: anime,   base_path: /media/films-zoe }
```

<!-- arrconf: values.yaml pattern reference for the simpler Deployment+PVC shape (NOT the ConfigMap-as-persistence pattern — we explicitly do NOT mirror that for SuggestArr): -->
<!-- See values.yaml lines 426-480 for arrconf: block as a structural reference; seerr: lines 293-340 for a deployment-style alias closer to suggestarr's shape. -->

<!-- 13-RESEARCH.md lines 488 + 492-494 — load-bearing for revision-2 (verbatim): -->
<!-- Line 488: "No ConfigMap needed for SuggestArr: config persists in the SQLite DB / YAML inside the PVC. The web UI is the configuration interface. No files/suggestarr.yml arrconf-style config file." -->
<!-- Lines 492-494: "SEER_ANIME_PROFILE_CONFIG population — This is set via the SuggestArr web UI (Settings → Seer Integration), NOT via environment variables. It persists in the SQLite DB / config.yaml. The operator performs this step post-deployment via the web UI at http://suggestarr.selfhost.svc.cluster.local:5000." -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2.1: CHECKPOINT — live cluster discovery (Jellyfin library ItemIds + Sonarr/Radarr profileIds) → 4 evidence files for UAT consumption</name>
  <files>
    .planning/phases/14-suggestarr-implementation/evidence/jellyfin-virtual-folders.txt,
    .planning/phases/14-suggestarr-implementation/evidence/sonarr-quality-profiles.txt,
    .planning/phases/14-suggestarr-implementation/evidence/radarr-quality-profiles.txt,
    .planning/phases/14-suggestarr-implementation/evidence/derived-routing-values.md
  </files>
  <read_first>
    - .planning/phases/14-suggestarr-implementation/14-CONTEXT.md §"Canonical References" → "Cluster discovery commands" block (verbatim curl + python3 snippets)
    - .planning/phases/14-suggestarr-implementation/14-CONTEXT.md §"Revision Note" (explains why the 4th evidence file replaces the deleted suggestarr-config.yml)
    - .planning/phases/13-suggestarr-research-spike/13-PHASE14-PREFLIGHT.md §"Questions to resolve" Q3 + Q4
    - .planning/phases/13-suggestarr-research-spike/13-RESEARCH.md lines 488 + 492-494 (SuggestArr config is web-UI-managed, NOT file-based — load-bearing for the 4th evidence file's purpose)
    - charts/arr-stack/files/arrconf.yml (lines 148-181 for Seerr block source-of-truth values; lines 3-53 for the 10 categories[])
  </read_first>
  <what-built>NOTHING by Claude. This is an OPERATOR-RUN discovery step. Claude has no kubeconfig — the operator runs the kubectl port-forwards.</what-built>
  <action>
    OPERATOR-DRIVEN. The executor agent posts these commands to the operator and pauses. The operator runs them on their workstation (kubeconfig pointing at my-kluster's `selfhost` namespace) and pastes the resulting files into the evidence directory.

    Step A — Create the evidence directory:
    ```bash
    mkdir -p .planning/phases/14-suggestarr-implementation/evidence
    ```

    Step B — Capture three raw evidence files via kubectl port-forward + curl. The CONTEXT § "Canonical References" → "Cluster discovery commands" block ships these verbatim — paste-and-go:

    ```bash
    # Setup — extract API keys from the running SealedSecret (read-only)
    SECRET_JSON=$(kubectl -n selfhost get secret arrconf-env -o json)
    JELLYFIN_API_KEY=$(echo "$SECRET_JSON" | python3 -c "import json,sys,base64;d=json.load(sys.stdin)['data'];print(base64.b64decode(d['JELLYFIN_API_KEY']).decode())")
    SONARR_API_KEY=$(echo "$SECRET_JSON" | python3 -c "import json,sys,base64;d=json.load(sys.stdin)['data'];print(base64.b64decode(d['SONARR_API_KEY']).decode())")
    RADARR_API_KEY=$(echo "$SECRET_JSON" | python3 -c "import json,sys,base64;d=json.load(sys.stdin)['data'];print(base64.b64decode(d['RADARR_API_KEY']).decode())")

    # Q3 — Jellyfin VirtualFolders → 10 ItemIds expected (one per category)
    kubectl -n selfhost port-forward svc/jellyfin 8096:8096 &
    JELLY_PF_PID=$!
    sleep 2
    curl -sS -H "X-Emby-Token: $JELLYFIN_API_KEY" http://localhost:8096/Library/VirtualFolders | \
      python3 -c "import json,sys;[print(f\"{i['Name']}: {i['ItemId']}\") for i in json.load(sys.stdin)]" \
      | tee .planning/phases/14-suggestarr-implementation/evidence/jellyfin-virtual-folders.txt
    kill $JELLY_PF_PID 2>/dev/null

    # Q4 — Sonarr quality profiles (Anime profile id + default series profile id)
    kubectl -n selfhost port-forward svc/sonarr 8989:8989 &
    SONARR_PF_PID=$!
    sleep 2
    curl -sS -H "X-Api-Key: $SONARR_API_KEY" http://localhost:8989/api/v3/qualityprofile | \
      python3 -c "import json,sys;[print(f\"{p['id']}: {p['name']}\") for p in json.load(sys.stdin)]" \
      | tee .planning/phases/14-suggestarr-implementation/evidence/sonarr-quality-profiles.txt
    kill $SONARR_PF_PID 2>/dev/null

    # Q4' — Radarr quality profiles (default movies profile id)
    kubectl -n selfhost port-forward svc/radarr 7878:7878 &
    RADARR_PF_PID=$!
    sleep 2
    curl -sS -H "X-Api-Key: $RADARR_API_KEY" http://localhost:7878/api/v3/qualityprofile | \
      python3 -c "import json,sys;[print(f\"{p['id']}: {p['name']}\") for p in json.load(sys.stdin)]" \
      | tee .planning/phases/14-suggestarr-implementation/evidence/radarr-quality-profiles.txt
    kill $RADARR_PF_PID 2>/dev/null
    ```

    **Expected output shapes:**

    Jellyfin (10 lines, one per category from arrconf.yml):
    ```
    Séries: 8a5c...
    Séries - Émilie: ...
    Séries - Thomas: ...
    Séries - Garçons: ...
    Séries - Zoé: ...
    Films: ...
    Nouveaux Films: ...
    Films - Enfants: ...
    Films - Animation Enfants: ...
    Films - Zoé: ...
    ```

    Sonarr (≥ 2 lines):
    ```
    6: HD - 720p/1080p    ← matches arrconf.yml seerr.main.sonarr_service.activeProfileId
    8: Anime              ← matches arrconf.yml seerr.main.sonarr_service.activeAnimeProfileId
    ```

    Radarr (≥ 1 line):
    ```
    6: HD - 720p/1080p    ← matches arrconf.yml seerr.main.radarr_service.activeProfileId
    ```

    Step C — **Write the 4th evidence file** `derived-routing-values.md`. This is the document the operator opens at UAT time (14-HUMAN-UAT.md Scenario 3) and pastes from into the SuggestArr web UI. It replaces the deleted `files/suggestarr-config.yml` ConfigMap entirely.

    Create `.planning/phases/14-suggestarr-implementation/evidence/derived-routing-values.md` with this structure (operator fills the `<LIVE_DISCOVERY>` placeholders from Steps B's three raw evidence files — this file is internal Phase 14 documentation, NOT a chart resource):

    ```markdown
    # Phase 14 — SuggestArr web-UI routing config values (paste-ready)

    > These values were discovered live in my-kluster's `selfhost` namespace at $(date -Iseconds).
    > The operator pastes them into the SuggestArr web UI POST-DEPLOY per 14-HUMAN-UAT.md Scenario 3.
    > SuggestArr persists them to its PVC-backed SQLite + YAML config — NO ConfigMap in the chart.
    > Source: 13-RESEARCH.md lines 488 + 492-494 (web UI is the only configuration interface).

    ## Provenance

    - Jellyfin ItemIds (10): captured live from `GET /Library/VirtualFolders` — see `jellyfin-virtual-folders.txt`
    - Sonarr profileIds (2): captured live from `GET /api/v3/qualityprofile` — see `sonarr-quality-profiles.txt`
    - Radarr profileId (1): captured live from `GET /api/v3/qualityprofile` — see `radarr-quality-profiles.txt`
    - profileId/rootFolder source-of-truth: `charts/arr-stack/files/arrconf.yml` seerr.main.{sonarr_service,radarr_service}
    - `is_anime` mapping: derived from `arrconf.yml` `categories[].profile == "anime"`

    ## JELLYFIN_LIBRARIES — paste into SuggestArr web UI: Settings → Jellyfin → Libraries

    | Library name (Jellyfin)        | ItemId (paste-ready)                          | is_anime | Notes (D-08 family-bucket caveat) |
    |--------------------------------|-----------------------------------------------|----------|------------------------------------|
    | Séries                         | `<LIVE_DISCOVERY: from jellyfin-virtual-folders.txt 'Séries:'>` | false    | default_tv routing |
    | Séries - Émilie                | `<LIVE_DISCOVERY: from jellyfin-virtual-folders.txt 'Séries - Émilie:'>` | false    | default_tv routing |
    | Séries - Thomas                | `<LIVE_DISCOVERY: from jellyfin-virtual-folders.txt 'Séries - Thomas:'>` | false    | default_tv routing |
    | Séries - Garçons               | `<LIVE_DISCOVERY: from jellyfin-virtual-folders.txt 'Séries - Garçons:'>` | false    | D-08 family-bucket → falls into default_tv |
    | Séries - Zoé                   | `<LIVE_DISCOVERY: from jellyfin-virtual-folders.txt 'Séries - Zoé:'>` | true     | anime_tv routing |
    | Films                          | `<LIVE_DISCOVERY: from jellyfin-virtual-folders.txt 'Films:'>` | false    | default_movie routing |
    | Nouveaux Films                 | `<LIVE_DISCOVERY: from jellyfin-virtual-folders.txt 'Nouveaux Films:'>` | false    | default_movie routing |
    | Films - Enfants                | `<LIVE_DISCOVERY: from jellyfin-virtual-folders.txt 'Films - Enfants:'>` | false    | D-08 family-bucket → falls into default_movie |
    | Films - Animation Enfants      | `<LIVE_DISCOVERY: from jellyfin-virtual-folders.txt 'Films - Animation Enfants:'>` | false    | D-08 family-bucket → falls into default_movie |
    | Films - Zoé                    | `<LIVE_DISCOVERY: from jellyfin-virtual-folders.txt 'Films - Zoé:'>` | true     | anime_movie routing |

    ## SEER_ANIME_PROFILE_CONFIG — paste into SuggestArr web UI: Settings → Seer Integration → Profile Config

    | Key            | profileId                          | rootFolder                                 | Source-of-truth (provenance)                                                  |
    |----------------|------------------------------------|--------------------------------------------|--------------------------------------------------------------------------------|
    | `anime_tv`     | `8` (verify via Sonarr evidence)   | `/media/anime` (verify via arrconf.yml)    | arrconf.yml seerr.main.sonarr_service.activeAnimeProfileId + activeAnimeDirectory (D-05/D-07) |
    | `anime_movie`  | `6` (reuses default_movie — D-06-SEERR-RADARR-LIMIT) | `/media/films-zoe` (derived from categories[] kind=movies+profile=anime) | Radarr has no anime profile in arrconf.yml; rootFolder derived per D-07 deviation note |
    | `default_tv`   | `6` (verify via Sonarr evidence)   | `/media/series` (verify via arrconf.yml)   | arrconf.yml seerr.main.sonarr_service.activeProfileId + activeDirectory (D-06/D-07) |
    | `default_movie`| `6` (verify via Radarr evidence)   | `/media/films` (verify via arrconf.yml)    | arrconf.yml seerr.main.radarr_service.activeProfileId + activeDirectory (D-06/D-07) |

    > **D-07 deviation handling**: `arrconf.yml::seerr.main.radarr_service` has NO `activeAnimeDirectory` field. The operator pastes `/media/films-zoe` for `anime_movie.rootFolder`, derived from `categories[]` where `kind=movies` and `profile=anime`. If a future arrconf.yml revision adds `radarr_service.activeAnimeDirectory`, update this row and re-paste in the web UI.

    > **Drift mitigation**: if the operator later updates any of these values in `arrconf.yml`, they MUST also update SuggestArr's web UI manually (single-tenant manual sync — same drift posture as Phase 6/10's Seerr↔arrconf relationship).

    ## D-08 family-bucket caveat — operator awareness

    `series-garcons`, `films-enfants`, `films-animation-enfants` watch-history events route to the GENERAL buckets (default_tv → `/media/series`; default_movie → `/media/films`), NOT to their dedicated paths. This is SuggestArr's native binary anime/non-anime split. The operator may notice this when validating Scenario 3 — it is the accepted limitation per CONTEXT D-08, NOT a Phase 14 defect.
    ```

    IMPORTANT — `<LIVE_DISCOVERY: ...>` placeholders in `derived-routing-values.md` MUST be replaced with the literal ItemId strings from `jellyfin-virtual-folders.txt`. The operator does this paste step at the same time they create the 3 raw evidence files (or immediately after).

    **Verification before resume-signal:**
    1. Each of the 3 raw evidence files is non-empty (≥ 1 line each).
    2. Jellyfin file has 10 entries — one per arrconf.yml category. If fewer, the operator must investigate (some category dir may not yet exist as a Jellyfin virtual folder; check Phase 9 follow-up `phase-09 initContainer NFS uid=1000 cluster-time write test`).
    3. Sonarr profile id `8` exists with name matching `Anime` (the configarr-created profile per Phase 5).
    4. Sonarr profile id `6` exists (or whatever `activeProfileId` is in arrconf.yml today).
    5. Radarr profile id `6` exists.
    6. `derived-routing-values.md` exists and contains NO unresolved `<LIVE_DISCOVERY: ...>` tokens (all 10 Jellyfin ItemIds pasted in).

    If any check fails, surface the discrepancy and let the operator decide (rerun discovery, update arrconf.yml first, or proceed with documented deviation tracked in 14-02-SUMMARY.md).
  </action>
  <how-to-verify>
    Same verification block as in <action>. The orchestrator waits for the operator to type `captured` and confirms all 4 evidence files exist before resuming Task 2.2.
  </how-to-verify>
  <verify>
    <automated>
      test -s .planning/phases/14-suggestarr-implementation/evidence/jellyfin-virtual-folders.txt &&
      test -s .planning/phases/14-suggestarr-implementation/evidence/sonarr-quality-profiles.txt &&
      test -s .planning/phases/14-suggestarr-implementation/evidence/radarr-quality-profiles.txt &&
      test -s .planning/phases/14-suggestarr-implementation/evidence/derived-routing-values.md &&
      test "$(grep -v '^#' .planning/phases/14-suggestarr-implementation/evidence/jellyfin-virtual-folders.txt | grep -c ':')" -ge 10 &&
      grep -Eq "^8:" .planning/phases/14-suggestarr-implementation/evidence/sonarr-quality-profiles.txt &&
      ! grep -q "LIVE_DISCOVERY" .planning/phases/14-suggestarr-implementation/evidence/derived-routing-values.md
    </automated>
  </verify>
  <resume-signal>
    Type "captured" once all four evidence files exist and the operator has verified the 10 Jellyfin entries + 2 Sonarr profile IDs + 1 Radarr profile ID match the arrconf.yml expectations, AND `derived-routing-values.md` has no remaining `<LIVE_DISCOVERY: ...>` placeholders (or has flagged any mismatches as deviations to track in 14-02-SUMMARY.md).
  </resume-signal>
  <done>The four evidence files exist in `.planning/phases/14-suggestarr-implementation/evidence/`, are non-empty, contain the literal IDs from the running cluster, and 14-HUMAN-UAT.md (Plan 03) Scenario 3 has everything it needs — operator-pastable values for SuggestArr's web UI Settings panels post-deploy.</done>
  <acceptance_criteria>
    - `.planning/phases/14-suggestarr-implementation/evidence/jellyfin-virtual-folders.txt` exists, non-empty.
    - `.planning/phases/14-suggestarr-implementation/evidence/sonarr-quality-profiles.txt` exists, non-empty.
    - `.planning/phases/14-suggestarr-implementation/evidence/radarr-quality-profiles.txt` exists, non-empty.
    - `.planning/phases/14-suggestarr-implementation/evidence/derived-routing-values.md` exists, non-empty.
    - `grep -v '^#' jellyfin-virtual-folders.txt | grep -c ":"` ≥ 10 (10 colon-prefixed entries minimum; may be more if legacy v0.2.0 dirs still exist) — comments excluded.
    - `grep -E "^8:" .planning/phases/14-suggestarr-implementation/evidence/sonarr-quality-profiles.txt` matches (the `Anime` profile per D-05).
    - `grep -q "LIVE_DISCOVERY" .planning/phases/14-suggestarr-implementation/evidence/derived-routing-values.md` returns NO match (all placeholders resolved).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2.2: Add suggestarr: block to values.yaml (image + env remap + PVC, NO Ingress, NO ConfigMap mount)</name>
  <files>
    charts/arr-stack/values.yaml
  </files>
  <read_first>
    - .planning/phases/14-suggestarr-implementation/evidence/jellyfin-virtual-folders.txt (Task 2.1 output — confirms live cluster is reachable; values are NOT injected into values.yaml — they live in derived-routing-values.md for UAT)
    - .planning/phases/14-suggestarr-implementation/evidence/derived-routing-values.md (Task 2.1 output — operator-pastable routing values for the web UI; this task does NOT consume them, it only confirms the file exists for downstream Plan 03 / UAT use)
    - charts/arr-stack/values.yaml (existing 10 alias blocks — pattern reference for the new suggestarr: block. arrconf: lines 426-480 shows the cronjob+ConfigMap pattern (NOT what we want for SuggestArr). seerr: lines 293-340 shows the simpler Deployment+PVC pattern (closer to suggestarr's shape).)
    - charts/arr-stack/files/arrconf.yml (lines 148-181 — verify Seerr source-of-truth values for documentation comments; lines 3-53 — verify the 10 categories[])
    - .planning/phases/14-suggestarr-implementation/14-CONTEXT.md §"Decisions" D-01 through D-09 + §"Revision Note" (load-bearing: explains the deletion of ConfigMap mechanism)
    - .planning/phases/13-suggestarr-research-spike/13-RESEARCH.md §"Phase 14 Implementation Guidance" lines 414-490 (values.yaml shape, probes, port 5000) + lines 488 / 492-494 (NO ConfigMap; web UI is the config interface — load-bearing for revision-2)

    **Autonomy note**: This task is FULLY AUTONOMOUS once Task 2.1's 4 evidence files exist on disk. Task 2.2 reads only `derived-routing-values.md` as confirmation that Task 2.1 completed; it does NOT extract values from any evidence file into values.yaml (the routing values are operator-entered in the SuggestArr web UI post-deploy per 14-HUMAN-UAT.md Scenario 3).
  </read_first>
  <action>
    **Step A — DELETE / SKIP the ConfigMap mechanism entirely.**

    This task does NOT create:
    - `charts/arr-stack/templates/suggestarr-configmap.yaml` — does NOT exist in the chart.
    - `charts/arr-stack/files/suggestarr-config.yml` — does NOT exist in the chart.

    Per 13-RESEARCH line 488, SuggestArr ignores file-based config. Its config lives in its PVC-backed SQLite + YAML, configured via the web UI post-deploy. The original revision-1 of this plan created these two files; revision-2 removes them.

    If either file exists in the workspace from a prior partial execution, DELETE it before running Step B:
    ```bash
    rm -f charts/arr-stack/templates/suggestarr-configmap.yaml
    rm -f charts/arr-stack/files/suggestarr-config.yml
    ```

    **Step B — Add `suggestarr:` block to `charts/arr-stack/values.yaml`:**

    Append AFTER the `configarr:` block (currently ends around line 528). Append-only matches the established convention and preserves git diff clarity.

    ```yaml
    # Wave 2 (Plan 14-02) — SuggestArr Helm sidecar (D-01..D-14 from .planning/phases/14-suggestarr-implementation/14-CONTEXT.md)
    # Routing config (SEER_ANIME_PROFILE_CONFIG + JELLYFIN_LIBRARIES) is NOT shipped via ConfigMap —
    # per 13-RESEARCH lines 488 + 492-494, SuggestArr's config persists in its PVC-backed SQLite + YAML
    # and is configured via the web UI POST-DEPLOY. See 14-HUMAN-UAT.md Scenario 3 for the operator step-by-step.
    suggestarr:
      global:
        nameOverride: suggestarr
        fullnameOverride: suggestarr
      serviceAccount:
        suggestarr: {}
      controllers:
        main:
          type: deployment   # SuggestArr is a daemon per 13-RESEARCH §Architecture Decision (Option C CronJob is non-viable)
          replicas: 1
          containers:
            main:
              image:
                # renovate: image=docker.io/ciuse99/suggestarr
                repository: ciuse99/suggestarr
                tag: "v2.7.3"  # Re-pin if Phase 14 kickoff is after 2026-06-22 per 13-RESEARCH §Metadata "Valid until"
                pullPolicy: IfNotPresent
              env:
                - name: TZ
                  value: "Europe/Paris"
                - name: LOG_LEVEL
                  value: "info"
                - name: SUGGESTARR_PORT
                  value: "5000"
                # D-01 per-container env remap — arrconf-env keys are reused under SuggestArr's expected names
                - name: JELLYFIN_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: arrconf-env
                      key: JELLYFIN_API_KEY
                - name: SEER_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: arrconf-env
                      key: SEERR_API_KEY
                - name: TMDB_API_KEY
                  valueFrom:
                    secretKeyRef:
                      name: arrconf-env
                      key: TMDB_API_KEY   # D-02: operator adds this key to arrconf-env in my-kluster SealedSecret (separate PR per D-13) BEFORE arr-stack PR merges
              probes:
                liveness:
                  enabled: true
                  custom: true
                  spec:
                    httpGet:
                      path: /api/health/live
                      port: 5000
                    initialDelaySeconds: 30
                    periodSeconds: 30
                readiness:
                  enabled: true
                  custom: true
                  spec:
                    httpGet:
                      path: /api/health/ready
                      port: 5000
                    initialDelaySeconds: 30
                    periodSeconds: 30
              resources:
                limits:
                  cpu: 250m
                  memory: 256Mi
                requests:
                  cpu: 50m
                  memory: 128Mi
      service:
        main:
          controller: main
          ports:
            http:
              port: 5000
      # D-14: NO ingress block — web UI stays cluster-internal-only.
      #       The operator accesses the web UI via `kubectl -n selfhost port-forward svc/suggestarr 5000:5000`
      #       to perform the post-deploy SEER_ANIME_PROFILE_CONFIG + JELLYFIN_LIBRARIES configuration
      #       per 14-HUMAN-UAT.md Scenario 3.
      persistence:
        # 1Gi PVC for SQLite + YAML config persistence per 13-RESEARCH §Phase 14 Implementation Guidance.
        # This is the ONLY place SuggestArr's routing config lives — there is NO ConfigMap-shipped routing file.
        config:
          type: persistentVolumeClaim
          accessMode: ReadWriteOnce
          size: 1Gi
          globalMounts:
            - path: /app/config/config_files
    ```

    YAML SYNTAX CRITICAL: the `env:` block above uses the LIST FORM (each entry is `- name: KEY` + either `value:` or `valueFrom:`). DO NOT mix with the map form (`KEY: value` pairs). The list form above is correct and must be used verbatim.

    The `persistence:` block has ONLY ONE entry (`config:` — the PVC). It does NOT have a `suggestarr-config:` entry (that was the original revision-1 ConfigMap mount, deleted in revision-2 per 13-RESEARCH line 488).
  </action>
  <verify>
    <automated>
      cd /data/projets/perso/arr-stack &&
      test ! -f charts/arr-stack/templates/suggestarr-configmap.yaml &&
      test ! -f charts/arr-stack/files/suggestarr-config.yml &&
      grep -q "^suggestarr:" charts/arr-stack/values.yaml &&
      grep -q "# renovate: image=docker.io/ciuse99/suggestarr" charts/arr-stack/values.yaml &&
      grep -q "ciuse99/suggestarr" charts/arr-stack/values.yaml &&
      grep -q "v2.7.3" charts/arr-stack/values.yaml &&
      grep -q "JELLYFIN_TOKEN" charts/arr-stack/values.yaml &&
      grep -q "SEER_TOKEN" charts/arr-stack/values.yaml &&
      grep -q "TMDB_API_KEY" charts/arr-stack/values.yaml &&
      helm lint charts/arr-stack/ &&
      helm template charts/arr-stack/ -f examples/values-prod.yaml | grep -q "name: suggestarr$" &&
      ! ( helm template charts/arr-stack/ -f examples/values-prod.yaml | grep -E "kind: ConfigMap" -A2 | grep -q "name: suggestarr-config" )
    </automated>
  </verify>
  <acceptance_criteria>
    - `charts/arr-stack/templates/suggestarr-configmap.yaml` does NOT exist (revision-2: ConfigMap mechanism deleted).
    - `charts/arr-stack/files/suggestarr-config.yml` does NOT exist (revision-2: ConfigMap source file deleted).
    - `charts/arr-stack/values.yaml` has a top-level `suggestarr:` key.
    - `charts/arr-stack/values.yaml` has the Renovate annotation `# renovate: image=docker.io/ciuse99/suggestarr` IMMEDIATELY above the `repository:` line (CLAUDE.md critical rule — never separate annotation from repository).
    - `charts/arr-stack/values.yaml` references `arrconf-env` Secret keys `JELLYFIN_API_KEY`, `SEERR_API_KEY`, `TMDB_API_KEY` via `secretKeyRef` (D-01).
    - `charts/arr-stack/values.yaml` does NOT contain an `ingress:` sub-block under `suggestarr:` (D-14).
    - `charts/arr-stack/values.yaml` `suggestarr.persistence` has exactly ONE entry (`config:`), NOT a second `suggestarr-config:` entry (revision-2: ConfigMap mount deleted).
    - `charts/arr-stack/values.yaml` does NOT bump `arrconf.image.tag` (currently `0.7.0` — must stay `0.7.0`).
    - `helm lint charts/arr-stack/` exits 0.
    - `helm template charts/arr-stack/ -f examples/values-prod.yaml` succeeds, output includes `name: suggestarr` (Deployment) AND does NOT include a ConfigMap named `suggestarr-config`.
    - `tools/arrconf/**` is NOT modified by this task (D-11 NO co-bump): `git diff --name-only -- tools/arrconf/` returns EMPTY.
    - `charts/arr-stack/files/arrconf.yml` is NOT modified by this task: `git diff --name-only -- charts/arr-stack/files/arrconf.yml` returns EMPTY.
  </acceptance_criteria>
  <done>
    The `suggestarr:` block in `values.yaml` is committed. `helm lint` and `helm template` both pass. No ConfigMap-related artifacts exist (revision-2 alignment with 13-RESEARCH line 488). Ready for Plan 03 (integration test + UAT).

    **Runtime precondition (operator-side, NOT enforced by this task)**: `TMDB_API_KEY` must exist in the `arrconf-env` SealedSecret BEFORE ArgoCD syncs this chart. The my-kluster PR adding the key merges FIRST per D-13 ordering rule. See 14-HUMAN-UAT.md "Pre-deploy gate" for the full procedure. If the key is absent at sync time, the SuggestArr pod will fail to start with `CreateContainerConfigError`.
  </done>
</task>

</tasks>

<verification>
After Task 2.2:

```bash
cd /data/projets/perso/arr-stack

# 1. Chart still lints
helm lint charts/arr-stack/

# 2. Chart renders a SuggestArr Deployment with the correct image
helm template charts/arr-stack/ -f examples/values-prod.yaml | yq 'select(.kind=="Deployment" and .metadata.name=="suggestarr") | .spec.template.spec.containers[0].image' | grep -q "ciuse99/suggestarr:v2.7.3"

# 3. Chart renders the 1Gi PVC for SuggestArr
helm template charts/arr-stack/ -f examples/values-prod.yaml | yq 'select(.kind=="PersistentVolumeClaim" and (.metadata.name | test("suggestarr"))) | .spec.resources.requests.storage' | grep -q "1Gi"

# 4. Chart renders the secret bindings (3 secretKeyRef entries pointing at arrconf-env)
helm template charts/arr-stack/ -f examples/values-prod.yaml | yq 'select(.kind=="Deployment" and .metadata.name=="suggestarr") | .spec.template.spec.containers[0].env[] | select(.valueFrom.secretKeyRef.name=="arrconf-env") | .name' | sort -u | wc -l | grep -q "^3$"

# 5. NO ConfigMap named suggestarr-config rendered (revision-2 negative assertion)
! ( helm template charts/arr-stack/ -f examples/values-prod.yaml | yq 'select(.kind=="ConfigMap" and .metadata.name=="suggestarr-config")' | grep -q "." )

# 6. NO suggestarr-configmap.yaml template file exists
test ! -f charts/arr-stack/templates/suggestarr-configmap.yaml

# 7. NO suggestarr-config.yml chart file exists
test ! -f charts/arr-stack/files/suggestarr-config.yml

# 8. NO arrconf.image.tag bump
grep -A4 "^arrconf:" charts/arr-stack/values.yaml | grep -A2 "image:" | grep -q "tag: \"0.7.0\""

# 9. NO arrconf.yml writes
test -z "$(git diff --name-only -- charts/arr-stack/files/arrconf.yml)"

# 10. NO tools/arrconf/** writes (production code untouched)
test -z "$(git diff --name-only -- tools/arrconf/arrconf/)"
```

All 10 must exit 0.
</verification>

<success_criteria>
- `values.yaml` `suggestarr:` block exists with Deployment + image pin + env remap + 1Gi PVC, NO Ingress, NO ConfigMap mount.
- `templates/suggestarr-configmap.yaml` does NOT exist (revision-2 alignment with 13-RESEARCH line 488).
- `files/suggestarr-config.yml` does NOT exist (revision-2 alignment with 13-RESEARCH line 488).
- 4 evidence files exist in `.planning/phases/14-suggestarr-implementation/evidence/` (3 raw + 1 derived-routing-values.md) — they feed 14-HUMAN-UAT.md Scenario 3 as operator-pastable web-UI values.
- Renovate annotation `# renovate: image=docker.io/ciuse99/suggestarr` present (D-09).
- `helm lint` + `helm template` both pass.
- `helm template` does NOT emit a ConfigMap named `suggestarr-config`.
- No co-bump (D-11): `arrconf.image.tag` stays `0.7.0`, `tools/arrconf/arrconf/**` untouched.
- No `arrconf.yml` write (Phase 14 reads it, never writes).
- D-13 SealedSecret YAML NOT added in this repo (Plan 03 UAT documents the my-kluster procedure).
</success_criteria>

<output>
After completion, create `.planning/phases/14-suggestarr-implementation/14-02-SUMMARY.md` covering:
- 1 file modified (values.yaml) + 4 evidence files added.
- Revision-2 note: explicit confirmation that NO ConfigMap template or source file was created, per 13-RESEARCH line 488. Compare to original revision-1 scope (which incorrectly invented a ConfigMap mount).
- D-07 deviation handling: `radarr_service.activeAnimeDirectory` was absent in arrconf.yml — `anime_movie.rootFolder` recorded as `/media/films-zoe` in `derived-routing-values.md` (derived from categories[] kind=movies+profile=anime).
- D-06-SEERR-RADARR-LIMIT inheritance: `anime_movie.profileId` documented as reusing `default_movie.profileId` (Radarr has no anime profile in current arrconf.yml) in `derived-routing-values.md`.
- Captured ItemIds + profile IDs from Task 2.1 evidence (10 + 2 + 1 = 13 values written into `derived-routing-values.md`, NOT into a chart file).
- Any drift detected between arrconf.yml seerr.main and live cluster (e.g., profile id 8 != "Anime" name).
- `helm template` output excerpt showing the new Deployment + PVC + Service (and the ABSENCE of a `suggestarr-config` ConfigMap).
- Confirmation: no arrconf bump, no arrconf.yml write, no SealedSecret YAML added, no ConfigMap template added.
- Handoff to Plan 03: Scenario 3 of 14-HUMAN-UAT.md will consume `derived-routing-values.md` for the web-UI configuration step.
</output>
