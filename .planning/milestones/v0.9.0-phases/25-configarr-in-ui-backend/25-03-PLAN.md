---
phase: 25-configarr-in-ui-backend
plan: 03
type: execute
wave: 2
depends_on: [25-01, 25-02]
files_modified:
  - tools/arrconf-ui/arrconf_ui/configarr_diff.py
  - tools/arrconf-ui/arrconf_ui/app.py
  - tools/arrconf-ui/tests/test_configarr_endpoints.py
autonomous: true
requirements: [CFGUI-01, CFGUI-03]
must_haves:
  truths:
    - "GET /api/configarr/config returns the parsed configarr.yml; api_key shows the literal '!env SONARR_API_KEY' (SC#2)"
    - "PUT /api/configarr/config writes back without corrupting !env/!secret tags; on tag loss it rolls back and returns 500 (D-09)"
    - "POST /api/configarr/diff returns a configarr-shape structured diff with !env literals preserved, never resolved (SC#4)"
    - "GET /api/configarr/schema returns the committed configarr-schema.json with readOnly markers"
    - "No *arr API URL is constructed or dialed anywhere in arrconf-ui source (SC#3 boundary assertion)"
  artifacts:
    - path: "tools/arrconf-ui/arrconf_ui/configarr_diff.py"
      provides: "configarr-shape structured diff (per-quality-profile, per-custom-format), tag-literal preserving (D-05/D-06)"
      contains: "def configarr_diff"
      min_lines: 40
    - path: "tools/arrconf-ui/arrconf_ui/app.py"
      provides: "4 /api/configarr/* endpoints symmetric to arrconf endpoints + D-09 anti-leak runtime guard"
      contains: "/api/configarr/config"
    - path: "tools/arrconf-ui/tests/test_configarr_endpoints.py"
      provides: "endpoint tests incl. GET-literal, PUT-roundtrip, D-09 rollback, diff-no-resolve, schema 404"
      contains: "!env SONARR_API_KEY"
  key_links:
    - from: "tools/arrconf-ui/arrconf_ui/app.py GET /api/configarr/config"
      to: "configarr_io._tagged_to_literal"
      via: "read_yaml → _tagged_to_literal → ConfigarrRootConfig.model_validate"
      pattern: "_tagged_to_literal"
    - from: "tools/arrconf-ui/arrconf_ui/app.py PUT /api/configarr/config"
      to: "D-09 byte-presence guard"
      via: "re-read after write_yaml_atomic, assert !env/!secret counts, rollback write_bytes on loss"
      pattern: "write_bytes"
    - from: "tools/arrconf-ui/arrconf_ui/configarr_diff.py"
      to: "tag-literal before/after snapshots"
      via: "diff runs on _tagged_to_literal output + payload, never model_dump"
      pattern: "configarr_diff"
---

<objective>
Register the 4 `/api/configarr/*` endpoints (GET/PUT config, POST diff, GET schema) symmetric to the existing arrconf endpoints, build the configarr-shape structured diff, and layer the D-09 anti-leak runtime guard on the PUT write path (re-read after write, assert every `!env`/`!secret` tag is byte-present, roll back + 500 on loss).

Purpose: CFGUI-01 + CFGUI-03 — the operator can load/edit/save configarr.yml through arrconf-ui with the same safety as arrconf.yml plus zero secret-leak risk. SC#2 (GET/PUT round-trip), SC#3 (no *arr URL in source), SC#4 (diff shows `!env` literals).
Output: `configarr_diff.py`, +4 endpoints in `app.py`, `test_configarr_endpoints.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-configarr-in-ui-backend/25-RESEARCH.md
@.planning/phases/25-configarr-in-ui-backend/25-PATTERNS.md
@.planning/phases/25-configarr-in-ui-backend/25-01-PLAN.md
@.planning/phases/25-configarr-in-ui-backend/25-02-PLAN.md

<interfaces>
<!-- Consume Plan 01 + Plan 02 outputs. -->

From arrconf_ui.configarr_io (Plan 01):
```python
def _tagged_to_literal(node: Any) -> Any: ...   # reconstructs "!env SONARR_API_KEY"; USE for every GET + diff before-snapshot
```
From arrconf_ui.locator (Plan 01):
```python
def configarr_yml_path() -> Path: ...
def configarr_schema_json_path() -> Path: ...
```
From arrconf_ui.configarr_config (Plan 02):
```python
class ConfigarrRootConfig(BaseModel): ...   # extra="forbid"; validates the real file
```
From arrconf_ui.io (REUSE AS-IS):
```python
def read_yaml(path) -> Any        # ruyaml CommentedMap, TaggedScalars intact
def write_yaml_atomic(path, data) # tmp + fsync + os.replace
```

Existing arrconf endpoints to clone (tools/arrconf-ui/arrconf_ui/app.py):
- get_config lines 67-82 (422-on-disk-invalid branch reused verbatim, lines 73-81)
- put_config lines 84-130 (shallow-merge-into-ruyaml-tree lines 107-118 — preserves comments AND leaves TaggedScalar nodes untouched)
- post_diff lines 132-141 (stateless, MUST NOT write)
- get_schema lines 143-152 (read committed schema json, 404 if missing)
- 422 normalization (lines 77, 93): json.loads(json.dumps(e.errors(), default=str))

configarr-diff reference (DO NOT import — D-05): tools/arrconf-ui/arrconf_ui/diff.py — reusable helpers:
- _list_to_index(items, key) (lines 31-33) — index list by stable key
- _flatten_paths / _changed_field_paths (lines 36-58) — dotted-path leaf diff
- has_changes(diff) (lines 102-112) — adapt key iteration

D-09 guard (RESEARCH lines 363-373):
```python
before_bytes = configarr_yml_path().read_bytes()
expected_env = before_bytes.decode().count("!env")
expected_secret = before_bytes.decode().count("!secret")
# ... shallow-merge editable keys into target ruyaml tree, write_yaml_atomic ...
after_text = configarr_yml_path().read_text("utf-8")
if after_text.count("!env") < expected_env or after_text.count("!secret") < expected_secret:
    configarr_yml_path().write_bytes(before_bytes)   # rollback
    raise HTTPException(500, "anti-leak guard: !env/!secret tag lost on write")
```

Editable top-level keys for PUT shallow-merge: ("trashGuideUrl", "recyclarrConfigUrl", "customFormatDefinitions", "sonarr", "radarr")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (D-05/D-06, SC#4): configarr-shape structured diff, tag-literal preserving</name>
  <files>tools/arrconf-ui/arrconf_ui/configarr_diff.py</files>
  <read_first>
    - tools/arrconf-ui/arrconf_ui/diff.py (STRUCTURE reference only, D-05 — _list_to_index lines 31-33, _flatten_paths/_changed_field_paths lines 36-58, has_changes lines 102-112, module docstring lines 13-20)
    - .planning/phases/25-configarr-in-ui-backend/25-PATTERNS.md lines 90-101 (configarr_diff assignment + D-06 CRITICAL note)
    - .planning/phases/25-configarr-in-ui-backend/25-RESEARCH.md lines 148-173 (quality_profiles/custom_formats shapes to group by)
    - charts/arr-stack/files/configarr.yml lines 193-300 (quality_profiles + custom_formats real shape)
  </read_first>
  <behavior>
    - Test 1 (per-profile grouping): diffing two configs where one quality_profile score changed groups the change under that profile name; unchanged profiles present with empty change-sets (predictable shape for the frontend, diff.py docstring contract).
    - Test 2 (per-custom-format grouping): a changed custom_format (matched by name/trash_id) appears in the diff; unchanged ones empty.
    - Test 3 (SC#4 tag preservation): diffing a before-snapshot containing `api_key: "!env SONARR_API_KEY"` (literal, from _tagged_to_literal) against an unchanged after shows the literal `!env SONARR_API_KEY` if surfaced, NEVER a resolved value and NEVER bare `SONARR_API_KEY`.
    - Test 4 (has_changes): returns False when before==after, True when any leaf differs.
  </behavior>
  <action>
    Write `tools/arrconf-ui/arrconf_ui/configarr_diff.py` — a NEW module, do NOT import `arrconf_ui.diff` (D-05: it is hard-coded to arrconf's `categories` + APP_SECTIONS sonarr/radarr/prowlarr/qbittorrent/seerr/jellyfin and cannot diff configarr's shape). Copy/adapt the reusable helpers from diff.py: `_list_to_index(items, key)` (index lists by stable key — `quality_profiles[]` by `name`, `custom_formats[]` by `name` or `trash_id`, `customFormatDefinitions[]` by `trash_id`), `_flatten_paths`/`_changed_field_paths` (dotted-path leaf diff) reusable as-is for field-level comparison within a grouped item.

    Expose `configarr_diff(before: dict, after: dict) -> dict` producing per-quality-profile and per-custom-format semantic grouping (D-05), iterating the configarr top-level keys (trashGuideUrl, recyclarrConfigUrl, customFormatDefinitions, sonarr.<instance>.quality_profiles, sonarr.<instance>.custom_formats, radarr.<instance>....). Keep the empty-change-sets-still-present contract (frontend hides them). Expose `has_changes(diff) -> bool` adapting the key iteration.

    CRITICAL (D-06/SC#4): the diff MUST operate on the tag-literal data. The `before` snapshot is `_tagged_to_literal(read_yaml(...))`; the `after` is the PUT/POST payload (already tag-literal strings). NEVER route any value through `model_dump`/a resolver — any dump that touches `api_key` drops the `!env` tag (RESEARCH Anti-Patterns line 292). This module never resolves env vars and never reads os.environ.
  </action>
  <verify>
    <automated>cd tools/arrconf-ui && uv run pytest tests/ -q -k configarr_diff && uv run ruff format --check . && uv run ruff check . && uv run mypy .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def configarr_diff' tools/arrconf-ui/arrconf_ui/configarr_diff.py`
    - `grep -q 'def has_changes' tools/arrconf-ui/arrconf_ui/configarr_diff.py`
    - D-05 boundary: `grep -c 'from arrconf_ui.diff\|import diff' tools/arrconf-ui/arrconf_ui/configarr_diff.py` returns 0 (no reuse of arrconf diff)
    - D-05 boundary: `grep -c 'APP_SECTIONS\|categories' tools/arrconf-ui/arrconf_ui/configarr_diff.py` returns 0
    - SC#4: no env resolution — `grep -c 'os.environ\|getenv\|model_dump' tools/arrconf-ui/arrconf_ui/configarr_diff.py` returns 0
    - tests pass; triade clean
  </acceptance_criteria>
  <done>configarr_diff groups changes per-profile and per-custom-format, preserves `!env` literals in the diff, and reuses no arrconf-shape diff code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (CFGUI-01/CFGUI-03, SC#2/SC#3/D-09): 4 endpoints + anti-leak runtime guard + tests</name>
  <files>tools/arrconf-ui/arrconf_ui/app.py, tools/arrconf-ui/tests/test_configarr_endpoints.py</files>
  <read_first>
    - tools/arrconf-ui/arrconf_ui/app.py (the 4 arrconf endpoints lines 67-152 to clone; _read_current lines 38-56 — the anti-pattern NOT to copy)
    - .planning/phases/25-configarr-in-ui-backend/25-PATTERNS.md lines 62-86 (endpoint clone assignment), lines 186-188 (atomic write + D-09 guard)
    - .planning/phases/25-configarr-in-ui-backend/25-RESEARCH.md lines 333-373 (endpoint code examples + D-09 byte-presence guard)
    - tools/arrconf-ui/tests/test_app_endpoints.py (endpoint test patterns: TestClient fixture, GET-200, PUT-writes-diff, invalid-422-no-write, POST-diff-no-write)
    - tools/arrconf-ui/tests/conftest.py (sandboxed_configarr_yml fixture from Plan 01)
    - tools/arrconf-ui/arrconf_ui/configarr_io.py, configarr_config.py, configarr_diff.py (the modules to wire)
  </read_first>
  <behavior>
    - Test 1 (SC#2 GET literal): `GET /api/configarr/config` returns 200 and `body["sonarr"]["main"]["api_key"] == "!env SONARR_API_KEY"` (NOT bare `SONARR_API_KEY` — Pitfall 1 regression).
    - Test 2 (GET radarr literal): `body["radarr"]["main"]["api_key"] == "!env RADARR_API_KEY"`.
    - Test 3 (SC#2 PUT round-trip): PUT an editable change (e.g. a quality_profile score), 200, then the sandboxed file `read_text` still contains `!env SONARR_API_KEY` and `!env RADARR_API_KEY`; response includes a diff + has_changes.
    - Test 4 (invalid payload → 422, no write): PUT with an unmodeled top key (`whisparr: {}`) returns 422 and the on-disk file is byte-unchanged.
    - Test 5 (D-09 rollback): simulate a tag-dropping write (monkeypatch write_yaml_atomic to write content missing one `!env`) → endpoint returns 500 AND the file is rolled back to the original bytes (still contains both tags).
    - Test 6 (POST /diff no write): POST /api/configarr/diff returns a diff and the file is byte-unchanged.
    - Test 7 (GET /schema): returns the committed configarr-schema.json; 404 when the schema path is missing.
  </behavior>
  <action>
    Add 4 endpoints to `create_app()` in `tools/arrconf-ui/arrconf_ui/app.py`, symmetric to the arrconf ones. Import at module top: `from arrconf_ui.configarr_io import _tagged_to_literal`, `from arrconf_ui.configarr_config import ConfigarrRootConfig`, `from arrconf_ui.configarr_diff import configarr_diff, has_changes as configarr_has_changes` (alias to avoid clashing with the arrconf `has_changes`), and `from arrconf_ui.locator import configarr_yml_path, configarr_schema_json_path`.

    GET `/api/configarr/config`: `read_yaml(configarr_yml_path())` → `_tagged_to_literal(raw)` (NOT `_read_current` — that JSON-coerces and drops tags, Pitfall 1) → `ConfigarrRootConfig.model_validate(literal)`; on ValidationError return 422 with `{"detail": json.loads(json.dumps(e.errors(), default=str)), "raw": literal}` (operator may have hand-edited); on success return the literal dict so api_key keeps `!env SONARR_API_KEY` (do NOT round-trip through model_dump if that would drop the tag — return the `_tagged_to_literal` dict, or model_dump with the literal already a plain str). 404 if file missing.

    PUT `/api/configarr/config`: `ConfigarrRootConfig.model_validate(payload)` (422 on invalid, no write). Capture `before_bytes = configarr_yml_path().read_bytes()` and expected `!env`/`!secret` counts. Compute the diff: `before = _tagged_to_literal(read_yaml(configarr_yml_path()))`, `diff = configarr_diff(before, payload)`. Shallow-merge editable keys into the on-disk ruyaml tree (clone app.py:107-118): `target = read_yaml(configarr_yml_path())`; `for k in ("trashGuideUrl","recyclarrConfigUrl","customFormatDefinitions","sonarr","radarr"): if k in payload: target[k] = payload[k]`; `write_yaml_atomic(configarr_yml_path(), target)`. Then the D-09 guard: re-read text, if `after_text.count("!env") < expected_env or after_text.count("!secret") < expected_secret` → `configarr_yml_path().write_bytes(before_bytes)` rollback + `raise HTTPException(500, "anti-leak guard: !env/!secret tag lost on write")`. On success return `{"diff": diff, "has_changes": configarr_has_changes(diff)}`.

    POST `/api/configarr/diff`: stateless, MUST NOT write. `before = _tagged_to_literal(read_yaml(configarr_yml_path()))`; return `{"diff": configarr_diff(before, payload), "has_changes": configarr_has_changes(...)}`.

    GET `/api/configarr/schema`: read `configarr_schema_json_path()`, 404 if missing, return parsed JSON.

    SC#3 BOUNDARY SELF-CHECK: none of these handlers construct or dial a Sonarr/Radarr URL. `base_url` is read/echoed only. No httpx/requests client targeting a *arr anywhere in arrconf-ui source.

    Write `tools/arrconf-ui/tests/test_configarr_endpoints.py` with the 7 tests, using `TestClient(create_app())` and the `sandboxed_configarr_yml` (+ `sandboxed_configarr_schema`) fixtures from conftest.py (Plan 01).
  </action>
  <verify>
    <automated>cd tools/arrconf-ui && uv run pytest tests/ -q && uv run ruff format --check . && uv run ruff check . && uv run mypy .</automated>
  </verify>
  <acceptance_criteria>
    - `cd tools/arrconf-ui && uv run pytest tests/ -q` exits 0 (all configarr + existing arrconf tests pass)
    - all 4 routes registered: `grep -c '/api/configarr/' tools/arrconf-ui/arrconf_ui/app.py` returns ≥4
    - GET uses the tag-literal helper, NOT _read_current: `grep -q '_tagged_to_literal' tools/arrconf-ui/arrconf_ui/app.py`
    - D-09 guard present: `grep -q 'write_bytes' tools/arrconf-ui/arrconf_ui/app.py` AND `grep -q 'anti-leak' tools/arrconf-ui/arrconf_ui/app.py`
    - test asserts the literal: `grep -q '!env SONARR_API_KEY' tools/arrconf-ui/tests/test_configarr_endpoints.py`
    - SC#3 boundary (whole arrconf-ui source): NO *arr URL dialed — `grep -rEn 'httpx\.(get|post|put|delete|Client)|requests\.(get|post)' tools/arrconf-ui/arrconf_ui/` returns no match targeting a sonarr/radarr/prowlarr host (the only HTTP in arrconf-ui is the FastAPI server itself + the test TestClient)
    - SC#3: `grep -rEc 'sonarr\.selfhost|radarr\.selfhost|:8989|:7878' tools/arrconf-ui/arrconf_ui/` returns 0 (no hardcoded *arr endpoint in source — base_url comes from the file at runtime, never dialed)
    - triade clean
  </acceptance_criteria>
  <done>The 4 configarr endpoints work symmetrically to arrconf; GET surfaces `!env` literals; PUT round-trips tags safely with the D-09 rollback guard; POST diff is stateless; no *arr URL is dialed anywhere in arrconf-ui source. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (LAN) → PUT /api/configarr/config | untrusted edited payload crosses into a file write; validated by ConfigarrRootConfig (422) then anti-leak guarded (500 + rollback) |
| configarr.yml → GET/diff response | `!env`/`!secret` must surface as literals, never resolved (SC#4) |
| arrconf-ui ↔ *arr APIs | HARD boundary (ADR-5/SC#3): arrconf-ui touches a file ONLY; configarr (separate in-cluster CronJob) is the sole thing that dials Sonarr/Radarr |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08 | Information Disclosure | GET /api/configarr/config | mitigate | uses `_tagged_to_literal`, not `_read_current` (Pitfall 1); Test 1/2 assert api_key surfaces `!env NAME` literal, not bare var name, never a resolved secret |
| T-25-09 | Tampering / Info Disclosure | PUT write path tag drop | mitigate | D-09 byte-presence guard: capture before-bytes + `!env`/`!secret` counts, re-read after write, rollback `write_bytes(before)` + HTTP 500 on any loss; Test 5 forces the failure path |
| T-25-10 | Tampering | invalid payload corrupts file | mitigate | `ConfigarrRootConfig.model_validate` 422s before any write; Test 4 asserts file byte-unchanged on 422 |
| T-25-11 | Information Disclosure | diff resolves env var | mitigate | diff runs on `_tagged_to_literal` data + payload, never `model_dump`/os.environ; Test 3 asserts literal preserved (SC#4) |
| T-25-12 | Elevation of Privilege | arrconf-ui dials a *arr | mitigate | SC#3 boundary: no httpx/requests client targets a *arr host; `base_url` read/echoed only. acceptance grep asserts 0 hardcoded *arr endpoints + 0 *arr HTTP calls in source |
| T-25-13 | Denial of Service | malformed huge payload | accept | LAN-trusted single-operator tool (v0.7.0 decision); no payload-size DoS surface worth mitigating |
</threat_model>

<verification>
- All 4 endpoints respond; GET surfaces `!env` literals (SC#2).
- PUT round-trips tags; D-09 rollback fires on tag loss (Test 5).
- POST diff is stateless and tag-literal preserving (SC#4).
- SC#3: no *arr URL dialed anywhere in arrconf-ui source (grep gates).
- All tests pass; triade clean.
</verification>

<success_criteria>
CFGUI-01 + CFGUI-03 satisfied: the operator can load/edit/save configarr.yml through 4 endpoints symmetric to the arrconf ones, with `!env`/`!secret` tags preserved on read (SC#2), the diff (SC#4), and the D-09-guarded write. SC#3 boundary holds: arrconf-ui dials no *arr.
</success_criteria>

<output>
After completion, create `.planning/phases/25-configarr-in-ui-backend/25-03-SUMMARY.md`
</output>
