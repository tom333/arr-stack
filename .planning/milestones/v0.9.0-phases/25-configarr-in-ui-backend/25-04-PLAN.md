---
phase: 25-configarr-in-ui-backend
plan: 04
type: execute
wave: 3
depends_on: [25-02, 25-03]
files_modified:
  - tools/arrconf-ui/tests/test_configarr_ci_gate.py
  - .github/workflows/tests.yml
autonomous: true
requirements: [CFGUI-07]
must_haves:
  truths:
    - "A CI gate validates the committed configarr.yml via ConfigarrRootConfig.model_validate (pydantic-only, D-08 Option C)"
    - "A CI step regenerates configarr-schema.json and fails on drift (reproducibility gate)"
    - "The gate runs in the existing arrconf-ui-backend job (tools/arrconf-ui working-directory); no *arr container, no configarr invocation in CI"
  artifacts:
    - path: "tools/arrconf-ui/tests/test_configarr_ci_gate.py"
      provides: "pytest gate asserting the REAL configarr.yml validates against ConfigarrRootConfig (CFGUI-07 authority)"
      contains: "model_validate"
    - path: ".github/workflows/tests.yml"
      provides: "schema reproducibility step for configarr-schema.json in the arrconf-ui-backend job"
      contains: "configarr-schema.json"
  key_links:
    - from: "tools/arrconf-ui/tests/test_configarr_ci_gate.py"
      to: "charts/arr-stack/files/configarr.yml (the REAL committed file)"
      via: "_tagged_to_literal(read_yaml(...)) → ConfigarrRootConfig.model_validate"
      pattern: "configarr.yml"
    - from: ".github/workflows/tests.yml arrconf-ui-backend job"
      to: "schemas/configarr-schema.json"
      via: "regenerate + git diff --exit-code"
      pattern: "git diff --exit-code"
---

<objective>
Wire the CFGUI-07 CI gate (pydantic-only, D-08 Option C per the resolved user decision): a pytest that validates the REAL committed `configarr.yml` against `ConfigarrRootConfig`, plus a schema-reproducibility step in the existing `arrconf-ui-backend` job that regenerates `configarr-schema.json` and fails on drift.

Purpose: CFGUI-07 — the authoritative structural gate is the pydantic `extra="forbid"` model run in CI against the written file. D-08 RESOLVED → Option C: configarr v1.28.0 has NO offline validate mode, so the project does NOT spin up *arr containers and does NOT invoke configarr in CI. The pydantic model IS the gate.
Output: `test_configarr_ci_gate.py`, a schema-reproducibility step added to `tests.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-configarr-in-ui-backend/25-RESEARCH.md
@.planning/phases/25-configarr-in-ui-backend/25-PATTERNS.md
@.planning/phases/25-configarr-in-ui-backend/25-02-PLAN.md
@.planning/phases/25-configarr-in-ui-backend/25-03-PLAN.md

<interfaces>
<!-- D-08 RESOLVED: Option C, pydantic-only. NO configarr invocation, NO *arr containers in CI. -->

Existing arrconf-ui-backend job (.github/workflows/tests.yml, defaults working-directory: tools/arrconf-ui):
```yaml
  arrconf-ui-backend:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: tools/arrconf-ui
    steps:
      - uses: actions/checkout@v4
      - name: Setup uv ...
      - name: Install dependencies (frozen ...)
        run: uv sync --frozen
      - name: Format check (ruff)
      - name: Lint (ruff)
      - name: Type check (mypy)
        run: uv run mypy .
      - name: Run tests
        run: uv run pytest -q
```

Schema-reproducibility analog (the arrconf job step to mirror, tests.yml ~line 52-59):
```yaml
      - name: Verify schema reproducibility (D-15)
        working-directory: ${{ github.workspace }}
        run: |
          cd tools/arrconf
          uv run arrconf schema-gen --output ../../schemas/arrconf-schema.json
          cd ../..
          git diff --exit-code -- schemas/arrconf-schema.json || (echo "::error::drift"; exit 1)
```

From arrconf_ui.configarr_schema_gen (Plan 02): `python -m arrconf_ui.configarr_schema_gen` regenerates schemas/configarr-schema.json reproducibly.
From arrconf_ui.configarr_config (Plan 02): `ConfigarrRootConfig`.
From arrconf_ui.configarr_io (Plan 01): `_tagged_to_literal`.
From arrconf_ui.locator (Plan 01): `configarr_yml_path`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (CFGUI-07, D-08 Option C): pydantic CI gate test against the REAL configarr.yml</name>
  <files>tools/arrconf-ui/tests/test_configarr_ci_gate.py</files>
  <read_first>
    - .planning/phases/25-configarr-in-ui-backend/25-CONTEXT.md (D-07/D-08 RESOLVED — Option C pydantic-only; do NOT invoke configarr or spin *arr containers)
    - .planning/phases/25-configarr-in-ui-backend/25-RESEARCH.md lines 375-396 (BLOCKER/ESCALATION — why no offline configarr validate exists)
    - .planning/phases/25-configarr-in-ui-backend/25-PATTERNS.md lines 150-159 (CI gate assignment — Option C)
    - tools/arrconf-ui/arrconf_ui/configarr_config.py, configarr_io.py, locator.py (the modules to validate with)
    - charts/arr-stack/files/configarr.yml (the REAL file the gate validates — NOT a sandbox copy here; the gate proves the committed file is valid)
  </read_first>
  <behavior>
    - Test 1 (authoritative gate, CFGUI-07): `ConfigarrRootConfig.model_validate(_tagged_to_literal(read_yaml(configarr_yml_path())))` succeeds against the REAL committed `charts/arr-stack/files/configarr.yml` — this is the pydantic CI authority. No exception, no skip.
    - Test 2 (tag survives into the gate): the validated input dict carries `!env SONARR_API_KEY` and `!env RADARR_API_KEY` literals (sanity that the gate runs on tag-literal data, not stripped data).
  </behavior>
  <action>
    Write `tools/arrconf-ui/tests/test_configarr_ci_gate.py`. Unlike the sandboxed endpoint tests, this gate validates the REAL committed file (resolve via `configarr_yml_path()` directly — no monkeypatch). It is the CFGUI-07 authority: if a future hand-edit to `configarr.yml` introduces an unmodeled key or a shape error, `extra="forbid"` makes this test fail in CI.

    Import `read_yaml` from `arrconf_ui.io`, `_tagged_to_literal` from `arrconf_ui.configarr_io`, `ConfigarrRootConfig` from `arrconf_ui.configarr_config`, `configarr_yml_path` from `arrconf_ui.locator`. Assert `model_validate` succeeds and the input literal contains both `!env` tags.

    Add a module docstring stating: D-08 RESOLVED Option C — pydantic is the authoritative gate; configarr v1.28.0 has no offline validate mode (RESEARCH escalation block); do NOT add configarr/`*arr`-container invocation here. This test runs inside the existing `arrconf-ui-backend` `uv run pytest -q` step — no new CI runner needed for the validation itself (Task 2 only adds the schema-reproducibility step).
  </action>
  <verify>
    <automated>cd tools/arrconf-ui && uv run pytest tests/test_configarr_ci_gate.py -q && uv run ruff format --check . && uv run ruff check . && uv run mypy .</automated>
  </verify>
  <acceptance_criteria>
    - `cd tools/arrconf-ui && uv run pytest tests/test_configarr_ci_gate.py -q` exits 0
    - gate validates the REAL file: `grep -q 'configarr_yml_path' tools/arrconf-ui/tests/test_configarr_ci_gate.py` AND `grep -q 'model_validate' tools/arrconf-ui/tests/test_configarr_ci_gate.py`
    - D-08 Option C honored — NO configarr/*arr-container invocation in CI: `grep -rEc 'configarr.*--dry-run|docker run|sonarrEnabled|radarrEnabled' tools/arrconf-ui/tests/test_configarr_ci_gate.py` returns 0
    - triade clean
  </acceptance_criteria>
  <done>A pytest gate validates the real committed configarr.yml against ConfigarrRootConfig and runs in the existing arrconf-ui-backend pytest step. No configarr/*arr invocation.</done>
</task>

<task type="auto">
  <name>Task 2 (CFGUI-07): schema-reproducibility step in tests.yml arrconf-ui-backend job</name>
  <files>.github/workflows/tests.yml</files>
  <read_first>
    - .github/workflows/tests.yml (the arrconf-ui-backend job ~lines 86-118 AND the arrconf "Verify schema reproducibility (D-15)" step ~lines 52-59 to mirror)
    - .planning/phases/25-configarr-in-ui-backend/25-PATTERNS.md lines 150-159 (CI gate + reproducibility assignment)
    - tools/arrconf-ui/arrconf_ui/configarr_schema_gen.py (the `python -m arrconf_ui.configarr_schema_gen` entry from Plan 02)
  </read_first>
  <action>
    Add a `Verify configarr schema reproducibility (CFGUI-07)` step to the EXISTING `arrconf-ui-backend` job in `.github/workflows/tests.yml`, placed after the `Run tests` step. Mirror the arrconf D-15 step structure but for configarr:
    ```yaml
      - name: Verify configarr schema reproducibility (CFGUI-07)
        working-directory: ${{ github.workspace }}
        run: |
          cd tools/arrconf-ui
          uv run python -m arrconf_ui.configarr_schema_gen
          cd ../..
          git diff --exit-code -- schemas/configarr-schema.json \
            || (echo "::error::schemas/configarr-schema.json drift — run 'cd tools/arrconf-ui && uv run python -m arrconf_ui.configarr_schema_gen' and commit"; exit 1)
    ```
    Do NOT add any configarr-tool or *arr-container step (D-08 Option C). The pydantic gate itself runs via the existing `uv run pytest -q` step (Task 1 ships the test). This step ONLY guards schema drift. Do NOT touch the arrconf D-15 step or the chart-lint workflow. This phase touches only tools/arrconf-ui/** + schemas/ + .github/workflows/ — do NOT bump charts/arr-stack/values.yaml#arrconf.image.tag (CLAUDE.md exception rule).
  </action>
  <verify>
    <automated>cd /data/projets/perso/arr-stack && python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/tests.yml')); assert 'configarr-schema.json' in open('.github/workflows/tests.yml').read(); print('yaml parses + step present')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/tests.yml` parses as valid YAML (the python3 yaml.safe_load above exits 0)
    - new step present in the arrconf-ui-backend job: `grep -q 'Verify configarr schema reproducibility' .github/workflows/tests.yml`
    - reproducibility check wired: `grep -q 'git diff --exit-code -- schemas/configarr-schema.json' .github/workflows/tests.yml`
    - D-08 Option C: NO configarr/*arr invocation added — `grep -c 'raydak-labs/configarr\|sonarrEnabled\|docker run' .github/workflows/tests.yml` returns 0
    - co-bump NOT triggered: `git diff --name-only` does NOT include charts/arr-stack/values.yaml
    - arrconf D-15 step untouched: `grep -c 'schema-gen --output ../../schemas/arrconf-schema.json' .github/workflows/tests.yml` still returns 1
  </acceptance_criteria>
  <done>The arrconf-ui-backend CI job regenerates configarr-schema.json and fails on drift; no configarr/*arr container is invoked; no chart co-bump triggered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed configarr.yml → CI | a hand-edited file must be structurally validated before it ships to the in-cluster configarr CronJob; the pydantic gate is the only offline authority (no configarr offline validate exists) |
| model → committed schema | the schema artifact must not silently drift from ConfigarrRootConfig |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-14 | Tampering | malformed configarr.yml ships | mitigate | CFGUI-07 pydantic gate (Task 1) validates the REAL committed file in CI; `extra="forbid"` fails the build on any unmodeled/malformed key |
| T-25-15 | Repudiation / drift | schema diverges from model | mitigate | reproducibility step regenerates + `git diff --exit-code` (Task 2) |
| T-25-16 | Information Disclosure | resolved secret reaches CI logs | accept | the gate runs on `_tagged_to_literal` data; `!env NAME` is a variable name, no secret resolved; CI does not read SONARR_API_KEY/RADARR_API_KEY env (arrconf-ui never does) |
| T-25-17 | (residual) weak structural gate vs configarr semantics | configarr-specific semantic rules | accept | D-08 RESOLVED Option C, user-acknowledged: configarr v1.28.0 has no offline validate; pydantic structural validation is the accepted authority for Phase 25 (semantic CF/profile validation deferred — would require live *arr, out of scope) |
</threat_model>

<verification>
- CFGUI-07 pydantic gate validates the real configarr.yml (Task 1).
- Schema-reproducibility step present and wired in the arrconf-ui-backend job (Task 2).
- No configarr-tool / *arr-container invocation in CI (D-08 Option C).
- No chart co-bump triggered (touches only tools/arrconf-ui/** + schemas/ + .github/).
- tests.yml parses; triade clean.
</verification>

<success_criteria>
CFGUI-07 satisfied per the resolved D-08 Option C: the authoritative CI gate is `ConfigarrRootConfig.model_validate` against the committed configarr.yml (running in the existing arrconf-ui-backend pytest step), plus a schema-reproducibility drift check. No configarr invocation, no *arr containers, no chart co-bump.
</success_criteria>

<output>
After completion, create `.planning/phases/25-configarr-in-ui-backend/25-04-SUMMARY.md`
</output>
