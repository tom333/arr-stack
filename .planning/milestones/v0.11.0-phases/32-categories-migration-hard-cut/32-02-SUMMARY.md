---
phase: 32-categories-migration-hard-cut
plan: "02"
subsystem: arrconf-python
tags: [catmig, generate, intent-config, arrconf-yml, read-only, ci-guard]
dependency_graph:
  requires: [IntentConfig.categories, IntentConfig.apps, RootConfig.no-categories, generators-list-input]
  provides: [generate_arrconf_yml, intent-yml-categories, generated-read-only-arrconf-yml, ci-idempotence-arrconf]
  affects:
    - tools/arrconf/arrconf/generators/intent.py
    - tools/arrconf/arrconf/__main__.py
    - charts/arr-stack/files/intent.yml
    - charts/arr-stack/files/arrconf.yml
    - .github/workflows/tests.yml
    - .planning/ROADMAP.md
tech_stack:
  added: []
  patterns: [CATMIG-02, CATMIG-03, D-32-04, D-32-05, SC4-determinism-by-construction]
key_files:
  created:
    - tools/arrconf/tests/test_generate_arrconf_yml.py
    - tools/arrconf/tests/fixtures/phase10-baseline-plans.json
  modified:
    - tools/arrconf/arrconf/generators/intent.py
    - tools/arrconf/arrconf/__main__.py
    - charts/arr-stack/files/intent.yml
    - charts/arr-stack/files/arrconf.yml
    - .github/workflows/tests.yml
    - .planning/ROADMAP.md
    - tools/arrconf/tests/_arrconf_helpers.py
    - tools/arrconf/tests/test_arrconf_yml_validates.py
    - tools/arrconf/tests/test_phase10_idempotence_sweep.py
decisions:
  - "generate_arrconf_yml pure function added to generators/intent.py with _sort_dict recursive key sorter (SC#4 determinism by construction)"
  - "arrconf.yml is now 100% GENERATED read-only from intent.yml (CATMIG-02 hard cut complete)"
  - "categories-derived resources still materialize at apply-time from intent_cfg.categories (NOT inlined in arrconf.yml — RootConfig sections are prune-stubs)"
  - "test_phase10_idempotence_sweep updated: categories sourced from intent.yml via production_categories fixture"
  - "12 previously-skipped tests in test_arrconf_yml_validates.py re-enabled with categories from intent.yml"
  - "ROADMAP SC#2 wording already correct at plan start (updated during planning pass) — no edit required"
metrics:
  duration: "~13 minutes"
  completed: "2026-06-04"
  tasks: 3
  files_changed: 9
---

# Phase 32 Plan 02: generate_arrconf_yml emission + intent.yml lift (CATMIG-02/03) Summary

`arrconf generate` now emits `arrconf.yml` as a GENERATED read-only apps pass-through from `intent.yml`. The 10 production categories have been lifted into `intent.yml` (the single hand-edited file). `arrconf.yml` carries no categories; categories-derived resources materialize at apply-time from `intent_cfg.categories`. The CI generate-idempotence guard explicitly covers `arrconf.yml`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add generate_arrconf_yml pure generator (TDD) | 34ced90 | generators/intent.py, tests/test_generate_arrconf_yml.py |
| 2 | Wire generate() to emit arrconf.yml + invert qbit_manage coupling | d311ba4 | __main__.py, tests/test_generate_arrconf_yml.py (ruff fix) |
| 3 | Lift categories+apps into intent.yml, regenerate arrconf.yml, update CI, fix tests | c97ad71 | intent.yml, arrconf.yml, tests.yml, ROADMAP.md, 4 test files + fixture |

## Verification Results

- Python triade (ruff format --check, ruff check, mypy arrconf): all pass (exit 0)
- pytest: 547 passed, 2 skipped, 3 pre-existing order-flaky failures (confirmed pre-existing)
- generate --check: exits 0 (all 3 generated files idempotent: cross-seed, arrconf.yml, qbit_manage)
- arrconf.yml header: `# GENERATED by 'arrconf generate' from intent.yml — DO NOT EDIT BY HAND`
- arrconf.yml loads via RootConfig: `LOADS_OK`
- values.yaml arrconf.image.tag: 0.23.0 (set by Plan 01 — NOT modified here, WARNING 3 honored)
- ROADMAP SC#2 wording: already correct at plan start (set during planning revision pass)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] _register_radarr_routes in _arrconf_helpers.py accessed cfg.categories**
- **Found during:** Task 3 (running phase10 sweep test)
- **Issue:** `_register_radarr_routes` accessed `cfg.categories` (old `RootConfig.categories` attribute removed in Plan 01). Plan scope listed helper as needing update but did not specify the fix.
- **Fix:** Added `categories: list[MediaCategory] | None = None` param to `_register_radarr_routes` (mirror of existing `_register_sonarr_routes` pattern). Updated `dry_run_all_apps` call to pass `cats`.
- **Files modified:** `tools/arrconf/tests/_arrconf_helpers.py`
- **Commit:** c97ad71

**2. [Rule 1 - Bug] _register_qbittorrent_routes did not mock /app/preferences**
- **Found during:** Task 3 (running phase10 sweep test)
- **Issue:** `_arrconf_helpers._register_qbittorrent_routes` only mocked `/torrents/categories`. The generated `arrconf.yml` now includes `preferences.enable: true` (it was in the old arrconf.yml but categories blocked parsing — Plan 01 skipped tests covered this). With the categories-free arrconf.yml loading successfully, `_reconcile_preferences` is called and hits the unmocked `GET /app/preferences` endpoint.
- **Fix:** Added `preferences.json` fixture mock to `_register_qbittorrent_routes`. The fixture already existed (`tests/fixtures/qbittorrent/preferences.json`).
- **Files modified:** `tools/arrconf/tests/_arrconf_helpers.py`
- **Commit:** c97ad71

**3. [Rule 1 - Bug] phase10-baseline-plans.json required regeneration**
- **Found during:** Task 3 (categories now sourced from intent.yml, plan shape changed)
- **Issue:** The old baseline was generated with categories from RootConfig (Plan 01 transition state with empty categories). Now categories from intent.yml produce different ADD plans (10 new resources each in the Phase 9 fixture environment). The baseline needed regeneration.
- **Fix:** Deleted the old baseline, let the test bootstrap regenerate it, committed the new fixture. `test_phase10_baseline_fixture_exists_or_generate` skipped on first run (bootstrap mode) then passed on second run.
- **Files modified:** `tools/arrconf/tests/fixtures/phase10-baseline-plans.json`
- **Commit:** c97ad71

### test_phase10_idempotence_sweep Resolution

The sweep test was updated to source categories from `intent.yml` (new `production_categories` fixture). The `test_sweep` (SC#3 dispositive) passes with 0 UPDATE/DELETE on 2nd run — FP fixes survive the CATMIG-02 transition. Decision: test updated (not retired) because the idempotence proof is still valid and valuable.

### test_arrconf_yml_validates.py Re-enablement

12 tests skipped in Plan 01 with `_SKIP_UNTIL_PLAN02` are now fully enabled:
- Tests using `load_config(ARRCONF_YML)` + generators: updated to load categories from `_load_production_categories()` (intent.yml)
- Tests checking `cfg.categories`: updated to use `cats` from intent.yml
- `test_arrconf_yml_has_10_categories` → renamed to `test_intent_yml_has_10_categories` (categories moved)
- `test_arrconf_yml_categories_ruyaml_roundtrip` → renamed to `test_intent_yml_categories_ruyaml_roundtrip`
- `test_arrconf_yml_validates_against_json_schema` (was skipped in Plan 01 commit fef6dc7): re-enabled and passes (arrconf.yml no longer has categories:, JSON schema validates cleanly)

All 14 tests in `test_arrconf_yml_validates.py` pass.

### ROADMAP SC#2 Wording

The ROADMAP SC#2 was already correctly worded at plan start (the planning revision pass had already applied the fix). No edit was needed. `grep -c "matérialisées au **apply-time**"` returns 1 and `grep -c "PAS inlinées dans \`arrconf.yml\`"` returns 1.

## Known Stubs

None — all data is wired and functional. `arrconf.yml` is fully generated from `intent.yml`. Categories materialize at apply-time from `intent_cfg.categories`.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: information_disclosure | charts/arr-stack/files/intent.yml | intent.yml now contains `apps:` block with Seerr animeTags int IDs (3) and profile IDs (6, 8) — these are internal API IDs, not credentials. Existing repo secret-scan posture unchanged. No literal secrets; env interpolation `${...}` preserved verbatim (T-32-04 mitigation verified). |

## Self-Check

Checking created files exist:
- generators/intent.py: FOUND
- test_generate_arrconf_yml.py: FOUND
- phase10-baseline-plans.json: FOUND
- intent.yml (modified): FOUND
- arrconf.yml (regenerated): FOUND
- 32-02-SUMMARY.md: FOUND

Checking commits exist:
- 34ced90: FOUND (feat: generate_arrconf_yml pure generator)
- d311ba4: FOUND (feat: wire generate() + invert qbit_manage coupling)
- c97ad71: FOUND (feat: lift categories+apps, regenerate arrconf.yml)

Key properties verified:
- arrconf.yml first line: `# GENERATED by 'arrconf generate' from intent.yml — DO NOT EDIT BY HAND`
- arrconf.yml categories count: 0 (no categories in generated file)
- values.yaml tag: 0.23.0 (unchanged, owned by Plan 01)
- ROADMAP SC#2: matérialisées au apply-time — 1 match

## Self-Check: PASSED
