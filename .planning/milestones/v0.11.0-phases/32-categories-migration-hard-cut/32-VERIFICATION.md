---
phase: 32-categories-migration-hard-cut
verified: 2026-06-04T11:00:00Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
---

# Phase 32: Categories Migration Hard Cut — Verification Report

**Phase Goal:** `categories[]` vit uniquement dans `intent.yml` ; `arrconf.yml` devient 100% généré et read-only.
**Verified:** 2026-06-04T11:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `intent.yml` contains 10 production categories (name/kind/profile/display/base_path); `arrconf.yml` has no hand-edited `categories:` section; `RootConfig` rejects `categories:` (extra=forbid) | ✓ VERIFIED | `grep -c "^categories:" charts/.../intent.yml` = 1; 10 entries confirmed with all fields; `grep -c "^categories:" arrconf.yml` = 0; `RootConfig.model_validate({"categories":...})` raises ValidationError extra_forbidden |
| 2 | `arrconf generate` produces `arrconf.yml` = apps pass-through from intent; categories-derived resources materialize at apply-time from `intent_cfg.categories`, NOT inlined | ✓ VERIFIED | `arrconf generate --check` exits 0; `generate_arrconf_yml` called at line 1167 of `__main__.py`; all 5 generators take `list[MediaCategory]` (not RootConfig); ROADMAP SC#2 wording confirmed "matérialisées au **apply-time**" |
| 3 | CI `generate-idempotence` job covers `arrconf.yml`; editing `intent.yml` without regenerating fails | ✓ VERIFIED | `.github/workflows/tests.yml` line 202 error hint explicitly names `arrconf.yml`; `--output-dir` covers all 3 generated files including `arrconf.yml`; `--check` mode verified live exits 0 |
| 4 | `arrconf.yml` carries `# GENERATED` header and is byte-for-byte reproducible by `arrconf generate` | ✓ VERIFIED | First line: `# GENERATED by 'arrconf generate' from intent.yml — DO NOT EDIT BY HAND`; `arrconf generate --check` exits 0 (no drift); `_sort_dict` recursive key sorter wired at line 220 of `generators/intent.py` |

**Score:** 4/4 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tools/arrconf/arrconf/intent_config.py` | `IntentConfig.categories` + `IntentConfig.apps` fields | ✓ VERIFIED | `categories: list[MediaCategory]` at line 183; `apps: dict[str, Any]` at line 187; `extra="forbid"` preserved |
| `tools/arrconf/arrconf/config.py` | `RootConfig` without `categories` field; legacy guard removed | ✓ VERIFIED | `grep -n "categories: list\[MediaCategory\]"` returns 0 matches; `_LEGACY_CATEGORY_NAMES` / `_check_no_legacy_categories` — 0 matches |
| `tools/arrconf/arrconf/generators/categories.py` | 5 generators taking `list[MediaCategory]` | ✓ VERIFIED | `grep -cE "def generate_...\\(categories: list\\[MediaCategory\\]"` = 5; `grep -n "cfg: RootConfig"` = 0 |
| `tools/arrconf/arrconf/generators/intent.py` | `generate_arrconf_yml` pure function + `_ARRCONF_HEADER` constant + `_sort_dict` | ✓ VERIFIED | All 3 present; `_sort_dict(dict(intent_cfg.apps))` wired in dump at line 220 |
| `charts/arr-stack/files/intent.yml` | 10 categories + `apps:` block | ✓ VERIFIED | `grep -c "^categories:"` = 1; `grep -c "^apps:"` = 1; 10 name entries confirmed; all have name/kind/profile/display/base_path |
| `charts/arr-stack/files/arrconf.yml` | GENERATED header; no `categories:`; no `yaml-language-server` modeline; loads via RootConfig | ✓ VERIFIED | Header confirmed; 0 `categories:` top-level; 0 `yaml-language-server` lines; `load_config(arrconf.yml)` prints `LOADS_OK` |
| `schemas/arrconf-schema.json` | No `categories` in RootConfig schema | ✓ VERIFIED | `"categories"` not in top-level `properties` |
| `schemas/intent-schema.json` | Has `categories` and `apps` properties | ✓ VERIFIED | Both present in `properties` |
| `.github/workflows/tests.yml` | `generate-idempotence` job with `arrconf.yml` in error hint | ✓ VERIFIED | Line 202 names `arrconf.yml` explicitly |
| `tools/arrconf/tests/test_apply_requires_intent.py` | Guard test: absent intent + sonarr target → exit 2 + `intent_required_for_categories` | ✓ VERIFIED | File exists; asserts `result.exit_code == 2`; asserts `"intent_required_for_categories" in result.output` |
| `tools/arrconf/tests/test_generate_arrconf_yml.py` | Byte-identical, key-sort, and RootConfig round-trip tests | ✓ VERIFIED | 6 test functions confirmed including `test_generate_arrconf_yml_byte_identical_determinism`, `test_generate_arrconf_yml_keys_are_sorted`, `test_generate_arrconf_yml_rootconfig_round_trip` |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `__main__.py apply()/diff()` | `intent_cfg.categories` | `generate_sonarr_resources(cats)`, `generate_radarr_resources(cats)`, `generate_qbit_categories(cats)`, `generate_jellyfin_libraries(cats)` | ✓ WIRED | 4 direct `(cats)` calls confirmed; 5th call (`generate_anime_tag_labels`) at line 81 via `_resolve_seerr_anime_tag_ids(categories, ...)` — spirit satisfied per plan note |
| `__main__.py apply()/diff()` | `intent_required_for_categories` guard | `log.error(...)` + `raise typer.Exit(code=2)` | ✓ WIRED | 2 matches in `__main__.py` at lines 279 (apply) and 911 (diff) |
| `__main__.py generate()` | `generate_arrconf_yml(intent_cfg)` | write/check loop at line 1167 | ✓ WIRED | Confirmed; `load_config(output_dir / "arrconf.yml")` removed (0 matches) |
| `__main__.py generate() qbit_manage block` | `intent_cfg.categories` | `generate_qbit_categories(intent_cfg.categories)` at line 1185 | ✓ WIRED | D-32-05 coupling inverted; no longer reads arrconf.yml to get categories |
| `diff_cmd.py diff_{sonarr,radarr,qbittorrent,jellyfin}` | `categories: list[MediaCategory]` param | function signatures | ✓ WIRED | `grep -cE "categories: list\\[MediaCategory\\]"` = 5 |
| `__main__.py diff()` | `diff_xxx(..., cats)` | 4 diff call sites passing `cats` | ✓ WIRED | `grep -cE "diff_(sonarr|radarr|qbittorrent|jellyfin)\\(.*cats\\)"` = 4 |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `charts/arr-stack/files/arrconf.yml` | apps pass-through | `intent_cfg.apps` in `generate_arrconf_yml()` | Yes — verbatim lift of production app config | ✓ FLOWING |
| `generators/categories.py` | `categories: list[MediaCategory]` | `intent_cfg.categories` at call site | Yes — 10 production entries from intent.yml | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `arrconf generate --check` exits 0 (idempotent) | `uv run arrconf generate --check --intent .../intent.yml --output-dir .../files/` | 3x `generate_ok`; exit 0 | ✓ PASS |
| `RootConfig` rejects `categories:` | `RootConfig.model_validate({"categories": [...]})` | ValidationError `extra_forbidden` | ✓ PASS |
| Generated `arrconf.yml` loads via RootConfig | `load_config(Path("arrconf.yml"))` | `LOADS_OK` | ✓ PASS |
| Full pytest suite | `uv run pytest -q` | 547 passed, 2 skipped, 3 pre-existing order-flaky failures (all pass in isolation) | ✓ PASS |
| ruff + mypy triade | `ruff format --check && ruff check && mypy arrconf` | All exit 0 | ✓ PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| CATMIG-01 | Plan 01 | `categories[]` absorbed by `intent.yml` schema; no longer in `arrconf.yml` | ✓ SATISFIED | `IntentConfig.categories` field added; `RootConfig.categories` removed; extra=forbid enforces hard cut |
| CATMIG-02 | Plan 02 | `arrconf generate` emits `arrconf.yml` from intent; no hand-edited `categories[]` residual | ✓ SATISFIED | `generate_arrconf_yml` pure function; `arrconf.yml` GENERATED header; 0 `categories:` in output |
| CATMIG-03 | Plan 02 | `arrconf.yml` generated + read-only; `generate-idempotence` CI guard covers it | ✓ SATISFIED | CI job line 202 names `arrconf.yml`; `--check` exits 0; GENERATED header signals read-only |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | — |

No stubs, placeholder returns, or disconnected wiring found. All generators are substantive and wired to real data sources. The 12 previously-skipped tests in `test_arrconf_yml_validates.py` are re-enabled and passing.

---

### Human Verification Required

None. All success criteria are verifiable programmatically and confirmed above.

---

### Gaps Summary

No gaps. All 4 must-have truths are verified with direct codebase evidence. The phase goal — `categories[]` lives only in `intent.yml`; `arrconf.yml` is 100% generated and read-only — is fully achieved.

**Additional checks verified:**
- Missing-intent guard: `apply()`/`diff()` emit `intent_required_for_categories` and exit 2 when a categories-driven app is targeted but intent.yml is absent (2 matches in `__main__.py`; dedicated test asserts exit code 2).
- CR-01 sanity check: `audit.py` has `categories: list[MediaCategory] | None = None` param on all relevant functions — wired.
- Values co-bump: `arrconf.image.tag` = `0.23.0`; renovate annotation preserved.
- ROADMAP SC#2 wording: "matérialisées au **apply-time**" and "PAS inlinées dans `arrconf.yml`" — both confirmed present.
- 3 pre-existing order-flaky test failures confirmed as pre-existing (pass in isolation per MEMORY note).

---

_Verified: 2026-06-04T11:00:00Z_
_Verifier: Claude (gsd-verifier)_
