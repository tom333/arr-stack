---
phase: 34-ui-over-intent
plan: 03
type: execute
wave: 3
depends_on: ["34-02"]
files_modified:
  - tools/arrconf-ui/web/src/lib/ProfileCard.svelte
  - tools/arrconf-ui/web/src/lib/ProfileDefinitionsEditor.svelte
  - tools/arrconf-ui/web/src/lib/ConfigarrRawEditor.svelte
  - tools/arrconf-ui/web/src/i18n/fr.ts
  - tools/arrconf-ui/web/src/App.svelte
autonomous: true
requirements: [UI-01, UI-03]
must_haves:
  truths:
    - "The intent.yml tab renders ordered sections: categories, sagas, apps, tools, profile_definitions, configarr — each preceded by its SectionDoc"
    - "profile_definitions renders one collapsible ProfileCard per profile; each card has a raw body YAML textarea, a CF chip list with per-CF score override, and a per-profile TRaSH CF/QP picker"
    - "Selecting custom formats in a profile's picker appends {trash_ids, score} refs to THAT profile's custom_formats (per-profile, not cross-profile)"
    - "configarr renders as a single raw YAML textarea (opaque pass-through) with the verbatim helper text"
    - "Editing any intent section updates intentState so diffCount/save reflect the change; categories reuses the existing CategoriesEditor unchanged"
  artifacts:
    - path: "tools/arrconf-ui/web/src/lib/ProfileCard.svelte"
      provides: "single profile card: body textarea + CF chips + score inputs + TrashCFPicker/TrashQPPicker"
      contains: "TrashCFPicker"
    - path: "tools/arrconf-ui/web/src/lib/ProfileDefinitionsEditor.svelte"
      provides: "add/delete profiles, render ProfileCard per key"
      contains: "Ajouter un profil"
    - path: "tools/arrconf-ui/web/src/lib/ConfigarrRawEditor.svelte"
      provides: "opaque YAML textarea for intent.configarr"
      contains: "pass-through"
  key_links:
    - from: "tools/arrconf-ui/web/src/lib/ProfileCard.svelte"
      to: "TrashCFPicker (existing v0.9.0)"
      via: "CustomFormatRef[] ↔ CustomFormatEntry[] shape transform"
      pattern: "assign_scores_to"
    - from: "tools/arrconf-ui/web/src/App.svelte"
      to: "ProfileDefinitionsEditor + ConfigarrRawEditor + CategoriesEditor + AppSection"
      via: "intent tab section rendering"
      pattern: "ProfileDefinitionsEditor"
---

<objective>
Complete the intent.yml editing form by replacing the temporary placeholder from plan 34-02 with the real ordered sections. Build the three special-cased components — ProfileDefinitionsEditor + ProfileCard (with the per-profile TRaSH CF/QP picker re-mounted from v0.9.0) and ConfigarrRawEditor (opaque pass-through) — add the FR i18n strings, and wire all six sections into App.svelte's intent tab.

Purpose: integrates the TRaSH picker into the intent edit flow (UI-03) and makes intent.yml fully editable through the UI (UI-01). The picker writes per-profile `{trash_ids, score}` refs (D-33-06 shape), NOT expanded CF bodies — arrconf-ui never dials a *arr API (ADR-5).
Output: 3 new Svelte components, FR i18n additions, App.svelte section wiring. Build + svelte-check + frontend tests green. No new npm packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-ui-over-intent/34-CONTEXT.md
@.planning/phases/34-ui-over-intent/34-RESEARCH.md
@.planning/phases/34-ui-over-intent/34-PATTERNS.md
@.planning/phases/34-ui-over-intent/34-UI-SPEC.md

<interfaces>
<!-- Types from plan 34-02 (types.ts). Components consume these. -->
type CustomFormatRef = { trash_ids: string[]; score: number | null };
type ProfileDefinition = { body: Record<string, unknown>; custom_formats: CustomFormatRef[] };
type IntentPayload = { categories, sagas, apps, tools, profile_definitions: Record<string, ProfileDefinition>, configarr };

<!-- EXISTING TrashCFPicker prop shape (v0.9.0 — reused UNCHANGED). Verified source. -->
type Props = {
  app: 'sonarr' | 'radarr';
  existingCustomFormats: CustomFormatEntry[];  // { trash_ids: string[]; assign_scores_to: { name: string; score?: number }[] }
  localDefinitions: { trash_id: string; name: string }[];
  profileNames: string[];
  onChange: (next: CustomFormatEntry[]) => void;
};

<!-- CRITICAL shape transform (RESEARCH.md Pattern 5 / Pitfall 3): -->
<!-- intent CustomFormatRef {trash_ids, score} ↔ picker CustomFormatEntry {trash_ids, assign_scores_to:[{name, score?}]} -->
<!-- to picker:   assign_scores_to: [{ name: profileName, ...(score !== null ? {score} : {}) }] -->
<!-- from picker: { trash_ids, score: assign_scores_to[0]?.score ?? null } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProfileCard.svelte (body editor + CF chips + score + TRaSH picker adapter)</name>
  <files>tools/arrconf-ui/web/src/lib/ProfileCard.svelte</files>
  <read_first>
    - tools/arrconf-ui/web/src/lib/TrashCFPicker.svelte (whole — exact prop shape, CF chip display lines 107-120, chip CSS 195-202, array-add CSS 304-319)
    - tools/arrconf-ui/web/src/lib/TrashQPPicker.svelte (whole — prop shape for the QP picker mount)
    - tools/arrconf-ui/web/src/lib/SectionDoc.svelte (lines 42-68 — <details> collapsible card pattern + CSS)
    - .planning/phases/34-ui-over-intent/34-PATTERNS.md (ProfileCard section lines 750-849; shape adapter lines 801-818)
    - .planning/phases/34-ui-over-intent/34-RESEARCH.md (Pattern 5 lines 312-348; Pitfall 3 lines 462-480)
    - .planning/phases/34-ui-over-intent/34-UI-SPEC.md (ProfileDefinitionsEditor lines 192-213; Destructive Actions lines 320-326)
  </read_first>
  <action>
    Props: `{ profileName: string; profile: ProfileDefinition; app: 'sonarr' | 'radarr'; localDefinitions: { trash_id: string; name: string }[]; onChange: (next: ProfileDefinition) => void; onDelete: () => void }`.

    Render a `<details>` card matching SectionDoc structure (lines 42-68) but `--panel` bg + `--border` border (not doc-bg) per UI-SPEC line 846. Summary shows `profile: {profileName}` and a delete `✕` button at header right (`--ink-muted`, `--destructive` on hover, `aria-label="Supprimer le profil {profileName}"`). Delete uses INLINE confirm (UI-SPEC line 325): clicking ✕ swaps the header to `Supprimer {profileName} ?` with `[Annuler la suppression]` / `[Supprimer le profil]` — no modal. Confirm calls `onDelete()`.

    Body section: label "Bloc corps du profil (YAML brut)" (14px IBM Plex Sans weight 600). A `<textarea>` (IBM Plex Mono 12px, bg `--code-bg`, `1px solid var(--border)`, focus ring `--accent`) bound to a YAML-serialized view of `profile.body`. On input, parse back to an object; on parse failure keep raw text and show inline error but do not crash. For serialize/parse use the same approach as ConfigarrRawEditor (task 2) — JSON.stringify(value, null, 2) as the minimal viable representation if no YAML lib is bundled; check whether a YAML serializer is already imported anywhere in web/src before adding one (no new npm packages).

    custom_formats section: render CF chips (copy TrashCFPicker chip markup lines 107-120 + CSS 195-202) — one chip per trash_id. Each chip: label (resolve via localDefinitions/labelFor; fall back to the id), a `✕` delete button (`aria-label="Retirer le format {name}"`), and a compact `<input type="number">` (width 5em, IBM Plex Mono, placeholder `défaut`) bound to that ref's `score` (empty → null). Deleting a chip removes that ref; changing score updates it.

    Mount the EXISTING TrashCFPicker below the chip list. Transform intent refs → picker entries (Pattern 5):
    ```typescript
    const pickerCFs = $derived(profile.custom_formats.map((ref) => ({
      trash_ids: ref.trash_ids,
      assign_scores_to: [{ name: profileName, ...(ref.score !== null ? { score: ref.score } : {}) }],
    })));
    function handleCFChange(next) {
      const refs = next.map((e) => ({ trash_ids: e.trash_ids, score: e.assign_scores_to[0]?.score ?? null }));
      onChange({ ...profile, custom_formats: refs });
    }
    ```
    Pass `app={app}`, `existingCustomFormats={pickerCFs}`, `localDefinitions={localDefinitions}`, `profileNames={[profileName]}`, `onChange={handleCFChange}`.

    Mount the EXISTING TrashQPPicker below the CF picker for this profile (feeds the profile body / QP selection). Wire its onChange to merge selected QP fields into `profile.body` and call `onChange({ ...profile, body: merged })`. Follow TrashQPPicker's documented prop shape exactly as read from source — do not invent props.

    Use ONLY CSS variables; weights 400/600 only (new code); no new font sizes.
  </action>
  <verify>
    <automated>cd tools/arrconf-ui/web && npx svelte-check --threshold error</automated>
  </verify>
  <acceptance_criteria>
    - ProfileCard.svelte contains `TrashCFPicker`, `TrashQPPicker`, `assign_scores_to`, `Bloc corps du profil`
    - ProfileCard.svelte contains the per-CF score input: `type="number"` and placeholder via `défaut`
    - ProfileCard.svelte contains `aria-label` with `Retirer le format` and `Supprimer le profil`
    - the reverse transform exists: `grep -c "assign_scores_to\[0\]" tools/arrconf-ui/web/src/lib/ProfileCard.svelte` >= 1
    - ProfileCard.svelte does NOT call any api function that dials a *arr URL (`grep -c "base_url\|http" tools/arrconf-ui/web/src/lib/ProfileCard.svelte` returns 0)
    - `cd tools/arrconf-ui/web && npx svelte-check --threshold error` exits 0
  </acceptance_criteria>
  <done>ProfileCard renders body editor + CF chips with score override + per-profile TRaSH CF/QP pickers, with the CustomFormatRef↔CustomFormatEntry transform; inline delete confirm; no *arr API call.</done>
</task>

<task type="auto">
  <name>Task 2: ProfileDefinitionsEditor + ConfigarrRawEditor + i18n strings</name>
  <files>tools/arrconf-ui/web/src/lib/ProfileDefinitionsEditor.svelte, tools/arrconf-ui/web/src/lib/ConfigarrRawEditor.svelte, tools/arrconf-ui/web/src/i18n/fr.ts</files>
  <read_first>
    - tools/arrconf-ui/web/src/lib/CategoriesEditor.svelte (whole — props/state lines 1-17, add/delete lines 39-46, list render)
    - tools/arrconf-ui/web/src/i18n/fr.ts (lines 28-95 — SECTION_DOCS shape + existing string constants)
    - tools/arrconf-ui/web/src/lib/ProfileCard.svelte (the component built in task 1 — its prop contract)
    - .planning/phases/34-ui-over-intent/34-PATTERNS.md (ProfileDefinitionsEditor lines 709-746; ConfigarrRawEditor lines 852-892; i18n section lines 433-484)
    - .planning/phases/34-ui-over-intent/34-UI-SPEC.md (ConfigarrRawEditor lines 214-223; Copywriting Contract lines 289-318)
  </read_first>
  <action>
    ProfileDefinitionsEditor.svelte — props `{ profiles: Record<string, ProfileDefinition>; localDefinitions: ...; onChange: (next: Record<string, ProfileDefinition>) => void }`. Render one `<ProfileCard>` per key (pass profileName, profile, onChange that replaces that key, onDelete that removes the key). Infer `app` per profile: if the profile name or its routing maps to sonarr → 'sonarr', radarr → 'radarr'; if ambiguous, render a small `app` select (UI-SPEC line 209). Add-profile control: an inline text input (placeholder `Nom du profil (ex: MULTi.VF)`) + button `+ Ajouter un profil` (array-add style: `--accent-soft` bg, `--accent` border, IBM Plex Sans 14px weight 400). addProfile(name) → `onChange({ ...profiles, [name]: { body: {}, custom_formats: [] } })` (guard empty/duplicate name). Follow CategoriesEditor add/delete pattern (lines 39-46).

    ConfigarrRawEditor.svelte — props `{ value: Record<string, unknown>; onChange: (next: Record<string, unknown>) => void }`. Label "Bloc configarr pass-through (YAML brut)" (14px IBM Plex Sans weight 600 `--ink`). A `<textarea class="raw-editor">` (CSS per PATTERNS.md lines 858-876: IBM Plex Mono 12px, bg `--code-bg`, `1px solid var(--border)`, border-radius 3px, min-height 200px, resize vertical, focus ring `--accent`+accent-soft). Helper below: "Ce bloc est émis verbatim dans configarr.yml. arrconf-ui ne le valide pas." (12px `--ink-faint` italic). Serialize `value` for display and parse on change. Use JSON.stringify(value, null, 2) as the minimal representation if no YAML serializer is already bundled (no new npm packages); on parse error keep the raw text + inline error, never crash. This block is opaque pass-through (D-33-07/08) — never structured.

    i18n/fr.ts — add the six SECTION_DOCS entries (keys `intent.categories`, `intent.sagas`, `intent.apps`, `intent.tools`, `intent.profile_definitions`, `intent.configarr`) with the titles from the Copywriting Contract (UI-SPEC lines 313-318) and a short body each. Add the string constants (copy verbatim from UI-SPEC Copywriting Contract):
    ```typescript
    export const READONLY_BADGE_TEXT = 'généré — lecture seule';
    export const MATERIALIZATION_EMPTY_TEXT = 'Aucune modification';
    export const PROFILE_BODY_LABEL = 'Bloc corps du profil (YAML brut)';
    export const CONFIGARR_RAW_LABEL = 'Bloc configarr pass-through (YAML brut)';
    export const CONFIGARR_RAW_HELPER = "Ce bloc est émis verbatim dans configarr.yml. arrconf-ui ne le valide pas.";
    export const ADD_PROFILE_TEXT = '+ Ajouter un profil';
    export const PROFILE_NAME_PLACEHOLDER = 'Nom du profil (ex: MULTi.VF)';
    export const SCORE_OVERRIDE_PLACEHOLDER = 'défaut';
    ```
    Wire components to use these constants where the matching literal appears (badge, helper, labels) — keep a single source of truth in fr.ts.
  </action>
  <verify>
    <automated>cd tools/arrconf-ui/web && npx svelte-check --threshold error</automated>
  </verify>
  <acceptance_criteria>
    - ProfileDefinitionsEditor.svelte contains `ProfileCard`, `Ajouter un profil`, and an add-profile guard against duplicate keys
    - ConfigarrRawEditor.svelte contains `pass-through`, `raw-editor`, `--code-bg`, and the verbatim helper text
    - fr.ts contains all six `intent.` SECTION_DOCS keys (grep each: `intent.categories`, `intent.sagas`, `intent.apps`, `intent.tools`, `intent.profile_definitions`, `intent.configarr`)
    - fr.ts contains `READONLY_BADGE_TEXT`, `CONFIGARR_RAW_HELPER`, `ADD_PROFILE_TEXT`, `SCORE_OVERRIDE_PLACEHOLDER`
    - `cd tools/arrconf-ui/web && npx svelte-check --threshold error` exits 0
  </acceptance_criteria>
  <done>ProfileDefinitionsEditor adds/deletes profiles and renders ProfileCard children; ConfigarrRawEditor edits the opaque block; all FR strings + SECTION_DOCS present.</done>
</task>

<task type="auto">
  <name>Task 3: Wire six intent sections into App.svelte intent tab</name>
  <files>tools/arrconf-ui/web/src/App.svelte</files>
  <read_first>
    - tools/arrconf-ui/web/src/App.svelte (whole — the intent tab branch + temporary <pre>/marker left by plan 34-02)
    - tools/arrconf-ui/web/src/lib/CategoriesEditor.svelte (props — reused as-is for intent.categories)
    - tools/arrconf-ui/web/src/lib/AppSection.svelte (props — schema-driven sections for sagas/apps/tools)
    - tools/arrconf-ui/web/src/lib/SectionDoc.svelte (props — precedes each section)
    - .planning/phases/34-ui-over-intent/34-PATTERNS.md (App.svelte section lines 488-577)
    - .planning/phases/34-ui-over-intent/34-UI-SPEC.md (Page Structure lines 119-152; Section ordering lines 181-190)
  </read_first>
  <action>
    Replace the temporary intent placeholder (the `<pre>` + `34-03` marker from plan 34-02) with the ordered sections inside the `{#if activeConfig === 'intent'}` branch. The MaterializationDiffPanel stays at the top of this branch when `showDiffPanel`. Order (UI-SPEC lines 181-189), each preceded by `<SectionDoc doc={SECTION_DOCS['intent.<key>']} />`:

    1. categories — `<CategoriesEditor categories={intentState.categories} onChange={(c) => updateIntent('categories', c)} />` (reused unchanged).
    2. sagas — `<AppSection ... />` schema-driven (use the existing AppSection contract; bind to intentState.sagas, schema node from intent schema).
    3. apps — schema-driven AppSection (pass-through dict).
    4. tools — schema-driven AppSection (cross_seed + qbit_manage sub-blocks).
    5. profile_definitions — `<ProfileDefinitionsEditor profiles={intentState.profile_definitions} localDefinitions={...} onChange={(p) => updateIntent('profile_definitions', p)} />`.
    6. configarr — `<ConfigarrRawEditor value={intentState.configarr} onChange={(c) => updateIntent('configarr', c)} />`.

    Add `function updateIntent<K extends keyof IntentPayload>(key: K, val: IntentPayload[K]) { intentState = { ...intentState!, [key]: val }; }` so every section edit flows into intentState (driving diffCount + save). For `localDefinitions` (custom local CF defs available to the picker), source them from the existing place the configarr tab sourced them (read App.svelte / TrashCFPicker usage); if none, pass `[]`.

    Import the new components (ProfileDefinitionsEditor, ConfigarrRawEditor) and SECTION_DOCS from i18n. Keep the inspect-tab branches and MaterializationDiffPanel wiring from plan 34-02 intact. Remove the `34-03: mount intent form sections here` marker.
    Use ONLY CSS variables; weights 400/600 in any new styles.
  </action>
  <verify>
    <automated>cd tools/arrconf-ui/web && npx svelte-check --threshold error && npm run build && npm test --silent || true; npm test 2>/dev/null; echo "build+check done"</automated>
  </verify>
  <acceptance_criteria>
    - App.svelte contains `CategoriesEditor`, `ProfileDefinitionsEditor`, `ConfigarrRawEditor`, and `SECTION_DOCS['intent.categories']` (or equivalent SECTION_DOCS keyed access for all six)
    - App.svelte contains an `updateIntent` helper feeding intentState
    - `grep -c "34-03: mount intent form sections here" tools/arrconf-ui/web/src/App.svelte` returns 0 (marker removed)
    - all six SECTION_DOCS intent keys referenced: `grep -oE "intent\.(categories|sagas|apps|tools|profile_definitions|configarr)" tools/arrconf-ui/web/src/App.svelte | sort -u | wc -l` returns 6
    - `cd tools/arrconf-ui/web && npx svelte-check --threshold error` exits 0 and `npm run build` exits 0
    - frontend test job passes (run the project's frontend test command; if a vitest/jest suite exists it exits 0)
  </acceptance_criteria>
  <done>The intent tab renders all six ordered sections with SectionDocs; edits flow through updateIntent into intentState; build + svelte-check + frontend tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → arrconf-ui backend | LAN-trusted; intent edits (incl. profile bodies + configarr block) posted as JSON |
| TRaSH picker → baked catalogue | picker reads the build-baked TRaSH catalogue via /api/trash/* (no runtime HTTP, no *arr API) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-10 | Elevation | ProfileCard / picker dialing a *arr quality_profiles/custom_formats API | mitigate | Picker reuses /api/trash/* (baked catalogue read) only; ProfileCard writes per-profile {trash_ids, score} refs, never expands CF bodies or constructs a *arr URL. ADR-5 boundary preserved (grep-asserted no base_url/http in ProfileCard). |
| T-34-11 | Spoofing | XSS via profile body / configarr raw textarea content | mitigate | Svelte escapes interpolated text; textareas bind to `value`/text nodes; no `{@html}`. configarr block is opaque text round-tripped to backend, validated nowhere client-side but never executed. |
| T-34-12 | Tampering | malformed YAML in raw editors corrupting intent | mitigate | Parse errors kept inline (raw text retained, no crash); backend re-validates the full payload with IntentConfig (extra="forbid") on save — invalid intent → 422, nothing written. |
| T-34-13 | Spoofing/Repudiation | no auth | accept | LAN-trusted single-operator design (CONTEXT.md). Out of scope. |
</threat_model>

<verification>
- `cd tools/arrconf-ui/web && npx svelte-check --threshold error` exits 0
- `cd tools/arrconf-ui/web && npm run build` exits 0
- frontend test suite passes (CI arrconf-ui-frontend job parity)
- No `{@html}` and no *arr URL construction in any new component (grep returns nothing)
- Picker reuses /api/trash/* unchanged (ADR-5 intact)
</verification>

<success_criteria>
- intent.yml tab fully editable: six ordered sections + SectionDocs (UI-01 complete)
- TRaSH CF/QP picker integrated per-profile inside ProfileCard, writing per-profile refs (UI-03 complete)
- configarr edited as opaque raw block (D-33-07/08 honored)
- Build + svelte-check + frontend tests green; ADR-5 preserved
</success_criteria>

<output>
After completion, create `.planning/phases/34-ui-over-intent/34-03-SUMMARY.md`
</output>
