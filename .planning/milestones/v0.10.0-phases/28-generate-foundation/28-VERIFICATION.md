---
phase: 28-generate-foundation
verified: 2026-05-31T03:24:03Z
status: passed
score: 4/4 must-haves verified
overrides_applied: 0
re_verification: false
---

# Phase 28: Generate Foundation — Verification Report

**Phase Goal:** L'opérateur édite un seul `intent.yml` et `arrconf generate` transforme l'intention en configs verbeuses committées, avec un garde-fou CI qui bloque tout drift.
**Verified:** 2026-05-31T03:24:03Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `intent.yml` is the sole hand-edited source; generated configs are read-only header-marked and never hand-edited | VERIFIED | `charts/arr-stack/files/intent.yml` carries `# HAND-EDITED` header + schema modeline. `cross-seed/config.js` carries `// GENERATED by 'arrconf generate' from intent.yml — DO NOT EDIT BY HAND`. `arrconf.yml`/`configarr.yml` remain hand-edited per D-01 (generating them is INTENT-CFGARR-01, deferred to v2). |
| 2 | `arrconf generate` transforms `intent.yml` into verbose configs via a pure function reusing `arrconf/generators/` pattern | VERIFIED | `generators/intent.py::generate_cross_seed(CrossSeedConfig) -> str` — pure function, no I/O (0 `open()` calls confirmed), `json.dumps(sort_keys=True)` for determinism. Exported from `arrconf.generators.__init__` in `__all__`. `generate` subcommand in `__main__.py` calls `load_intent` + `generate_cross_seed`, decoupled from `apply` (lines 881/888 only inside `def generate`). |
| 3 | CI fails when committed configs drift from `intent.yml` | VERIFIED | `generate-idempotence` job in `tests.yml` (lines 172-202) runs `arrconf generate --check`. Path trigger `charts/arr-stack/files/**` added to both `on.pull_request.paths` (line 10) and `on.push.paths` (line 19). Guard absent from `chart-lint.yml` (D-09 confirmed). `arrconf generate --check` exits 0 on committed tree (verified live). |
| 4 | ADR-10 documents the intention layer, absorber vs deployer-only boundary, and ADR-5 extension | VERIFIED | `spec.md §11` lines 956-968: ADR-10 block present before `## 12. Références`. Covers all three D-12 mandatory points: (a) layer-above-arrconf+configarr, (b) absorber/déployer-seulement boundary, (c) ADR-5 extension ("seul appliqueur TRaSH", quality_profiles/custom_formats/quality_definitions/media_naming listed). `.planning/PROJECT.md` line 261: ADR-10 row in decisions table with full summary. |

**Score:** 4/4 truths verified

### Requirements Coverage

| Requirement | Phase | Description | Status | Evidence |
|-------------|-------|-------------|--------|----------|
| INTENT-01 | 28 | Seul `intent.yml` hand-edited; configs verbeuses générées, read-only | SATISFIED | `intent_config.py` IntentConfig model with `extra="forbid"` (3×), `load_intent()` with ConfigError wrapping; `intent-schema-gen` subcommand; `schemas/intent-schema.json` committed; intent.yml + cross-seed/config.js seeded. |
| INTENT-02 | 28 | `arrconf generate` via pure function reusing `generators/` pattern | SATISFIED | `generators/intent.py::generate_cross_seed` pure function; exported in `__all__`; `generate` CLI wired in `__main__.py`. |
| INTENT-03 | 28 | CI fails on drift (`arrconf generate && git diff --exit-code`) | SATISFIED | `generate-idempotence` job in `tests.yml`; `--check` flag on `generate` subcommand exits 1 on drift, 0 in sync; path trigger `charts/arr-stack/files/**` ensures intent.yml edits trigger guard. |
| INTENT-04 | 28 | New ADR documents intention layer + absorber/déployer boundary + ADR-5 extension | SATISFIED | ADR-10 in `spec.md §11` (lines 956-968) + `PROJECT.md` decisions table (line 261). |

All 4 Phase 28 requirements satisfied. Requirements SAGAS-01..04, XSEED-01..03, QBM-01..03 are scoped to Phases 29-31 per REQUIREMENTS.md traceability table.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tools/arrconf/arrconf/intent_config.py` | IntentConfig/ToolsConfig/CrossSeedConfig/SagaEntry models + `load_intent()` | VERIFIED | 117 lines; 3× `extra="forbid"` (CrossSeedConfig, ToolsConfig, IntentConfig); 1× `extra="allow"` (SagaEntry, with phase-29 comment); `load_intent` raises ConfigError on missing/parse/validation failure. |
| `tools/arrconf/arrconf/generators/intent.py` | Pure function `generate_cross_seed(CrossSeedConfig) -> str` | VERIFIED | 50 lines; `_HEADER` constant with exact D-11 text; `json.dumps(sort_keys=True, indent="\t")`; empty list fields omitted; camelCase key mapping; 0 I/O calls. |
| `tools/arrconf/arrconf/generators/__init__.py` | `generate_cross_seed` in `__all__` | VERIFIED | `from arrconf.generators.intent import generate_cross_seed` + `"generate_cross_seed"` in `__all__`. |
| `tools/arrconf/arrconf/__main__.py` | `generate` `@app.command` with `--intent`/`--output-dir`/`--check` | VERIFIED | Lines 855-901; `--check` exits 1 on drift, 0 in sync; ConfigError → exit 2; never called from `apply`. |
| `tools/arrconf/arrconf/schema_gen.py` | `write_intent_schema()` function | VERIFIED | `IntentConfig.model_json_schema(...)` call with `json.dumps(sort_keys=True)` determinism. |
| `schemas/intent-schema.json` | JSON Schema for intent.yml | VERIFIED | 3.0K file; references `IntentConfig` and `CrossSeedConfig`; regenerates byte-identically. |
| `charts/arr-stack/files/intent.yml` | Seeded hand-edited intent with cross_seed block | VERIFIED | 16 lines; `$schema` modeline pointing at `schemas/intent-schema.json`; 2× `PLACEHOLDER` for secrets (torznab apikey + qBit password); `sagas: []`. |
| `charts/arr-stack/files/cross-seed/config.js` | Generated CommonJS config with read-only header | VERIFIED | 14 lines; `// GENERATED by 'arrconf generate' from intent.yml — DO NOT EDIT BY HAND`; `module.exports = {...}` with alphabetically-sorted keys; PLACEHOLDER in values (no real secrets). |
| `tools/arrconf/tests/test_intent_config.py` | Unit tests for load_intent + model validation | VERIFIED | 7 tests passing (load valid, missing path, unknown key, CrossSeedConfig defaults, SagaEntry extra-allow, empty intent). |
| `tools/arrconf/tests/test_generate_cross_seed.py` | Unit tests for generate_cross_seed | VERIFIED | 5 tests passing (minimal, deterministic, sort-keys, empty-omit, camelCase). |
| `tools/arrconf/tests/test_generate_cmd.py` | CLI tests for generate subcommand | VERIFIED | 6 tests passing (write, help, missing-intent exit-2, --check pass, --check drift). |
| `.github/workflows/tests.yml` | generate-idempotence job + charts/arr-stack/files/** path trigger | VERIFIED | `generate-idempotence:` job at line 172; path trigger `charts/arr-stack/files/**` at lines 10 and 19; guard absent from `chart-lint.yml`. |
| `charts/arr-stack/values.yaml` | arrconf image tag co-bumped to 0.18.0 | VERIFIED | Line 451: `tag: "0.18.0"`; `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` annotation preserved (line 449). |
| `spec.md §11` | ADR-10 block after ADR-9, before §12 | VERIFIED | Lines 956-968: ADR-10 with all three D-12 content items; sits before `## 12. Références` (line 970). |
| `.planning/PROJECT.md` | ADR-10 one-line entry in decisions table | VERIFIED | Line 261: full ADR-10 summary row with absorber/déployer boundary and ADR-5 extension reference. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `__main__.py` | `intent_config.py` | `intent-schema-gen` subcommand calls `write_intent_schema` | WIRED | Lines 42 (import) + 838-852 (command). |
| `__main__.py` | `generators/intent.py` | `generate` subcommand calls `generate_cross_seed` | WIRED | Lines 36 (import) + 888 (call inside `def generate`). |
| `__main__.py` | `intent_config.py` | `generate` subcommand calls `load_intent` | WIRED | Lines 37 (import) + 881 (call inside `def generate`). |
| `generators/__init__.py` | `generators/intent.py` | re-exports `generate_cross_seed` in `__all__` | WIRED | Line 21 (import) + `__all__` entry. |
| `generators/intent.py` | `intent_config.py` | imports `CrossSeedConfig` | WIRED | Line 21: `from arrconf.intent_config import CrossSeedConfig`. |
| `charts/arr-stack/files/intent.yml` | `charts/arr-stack/files/cross-seed/config.js` | `arrconf generate` | WIRED | `generate --check` exits 0 (verified live) — committed config.js is in sync with intent.yml. |
| `charts/arr-stack/files/intent.yml` | `schemas/intent-schema.json` | yaml-language-server modeline | WIRED | Line 1: `# yaml-language-server: $schema=../../../schemas/intent-schema.json`. |
| `tests.yml` | `schemas/intent-schema.json` | intent-schema reproducibility CI step | WIRED | Lines 63-70: runs `intent-schema-gen --output ../../schemas/intent-schema.json` + `git diff --exit-code`. |
| `tests.yml` | `charts/arr-stack/files/intent.yml` | `on.pull_request.paths` + `on.push.paths` | WIRED | `charts/arr-stack/files/**` trigger present at lines 10 and 19. |
| `spec.md §11 ADR-10` | `spec.md §11 ADR-5` | explicit extension reference | WIRED | Line 964: "Extension d'ADR-5" + "configarr reste le **seul appliqueur TRaSH**". |
| `apply` command | `generate` subcommand | must NOT be wired (D-06 decoupling) | CONFIRMED ABSENT | `load_intent` and `generate_cross_seed` only called at lines 881/888, both inside `def generate`. Zero cross-calls from `apply`. |

### Data-Flow Trace (Level 4)

Not applicable — Phase 28 delivers pure-function generators and CLI plumbing, not components that render dynamic data from a data source. The `generate` command reads `intent.yml` (operator-edited file) and writes `config.js` (committed file). The data flow is: operator YAML → `load_intent` → pydantic model → `generate_cross_seed` → JS string → filesystem. This was verified end-to-end by running `arrconf generate --check` live (exit 0).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `arrconf generate --check` on committed tree exits 0 | `uv run arrconf generate --check --intent ../../charts/arr-stack/files/intent.yml --output-dir ../../charts/arr-stack/files/` | exit 0, `generate_ok` logged | PASS |
| 18 new phase-28 tests pass | `pytest tests/test_intent_config.py tests/test_generate_cross_seed.py tests/test_generate_cmd.py -q` | 18 passed in 0.32s | PASS |
| image tag co-bumped to 0.18.0 | `grep 'tag.*0.18' charts/arr-stack/values.yaml` | `tag: "0.18.0"` at line 451 | PASS |
| intent-schema.json deterministic | Two consecutive runs byte-identical (per `schema_gen.py` json.dumps sort_keys=True) | PASS (by construction — same guarantee as existing schema-gen) | PASS |
| `generate-idempotence` absent from `chart-lint.yml` | `grep generate-idempotence .github/workflows/chart-lint.yml` | ABSENT | PASS |

### Pre-existing Test Failures (Not Phase 28 Regressions)

Three tests fail in the full suite run but are confirmed pre-existing and unrelated to Phase 28:

- `test_phase10_idempotence_sweep.py::test_sweep` — FAILS in isolation. Root cause: `qbittorrent.py::_reconcile_preferences` (added in 12-B/12-C era) calls `GET /app/preferences` which the sweep test never mocked. Test last modified at commit `e875e2b` (Phase 12-C); qbittorrent.py `_reconcile_preferences` predates that commit. Zero Phase 28 files were involved.
- `test_phase10_idempotence_sweep.py::test_phase10_baseline_fixture_exists_or_generate` — same root cause as above (ordering state from respx leak with preferences endpoint).
- `test_reconcilers_jellyfin.py::test_reconcile_jellyfin_step_order_invariant` — order-dependent respx state leak, pre-existing.

None of these tests reference `intent`, `generate`, `CrossSeedConfig`, or any Phase 28 code path. They are not Phase 28 regressions.

### Anti-Patterns Found

None in Phase 28 deliverables.

- `generators/intent.py`: 0 `open()` calls (pure function confirmed), no TODO/FIXME, `sort_keys=True` guarantees determinism.
- `intent_config.py`: no placeholder implementations, no silent exception swallowing, `extra="forbid"` on all non-SagaEntry models.
- `charts/arr-stack/files/cross-seed/config.js`: PLACEHOLDER values only (no real API keys/passwords), read-only header present.
- `charts/arr-stack/files/intent.yml`: PLACEHOLDER values only (torznab apikey, qBit password), `sagas: []` stub correctly empty.

### Human Verification Required

None — all must-haves are verifiable programmatically. The `generate --check` behavioral check was run live and returned exit 0.

---

## Summary

Phase 28 goal fully achieved. All 4 ROADMAP success criteria are met:

1. **SC#1 (INTENT-01):** `intent.yml` is the sole hand-edited file with `$schema` modeline. `cross-seed/config.js` has the mandatory `// GENERATED ... DO NOT EDIT BY HAND` header. `arrconf.yml`/`configarr.yml` remain hand-edited per D-01 (their generation is INTENT-CFGARR-01, explicitly deferred to v2). The ROADMAP reference to "4 target configs" describes the tranche-1 end-state spanning Phases 28-31, not the P28 scope.

2. **SC#2 (INTENT-02):** `generate_cross_seed` is a pure function with determinism guaranteed by `json.dumps(sort_keys=True)`, no I/O, exported from `arrconf.generators`. The generator is an extension of the existing `categories.py` idiom (D-03 proving slice for JS literal output format).

3. **SC#3 (INTENT-03):** `generate-idempotence` CI job with `arrconf generate --check`, path trigger `charts/arr-stack/files/**` in both PR and push paths, isolated from `chart-lint.yml` auto-tagger (D-09). Confirmed: `generate --check` exits 0 on committed tree.

4. **SC#4 (INTENT-04):** ADR-10 in `spec.md §11` covers all three D-12 content points (intention layer above arrconf+configarr, absorber vs déployer-seulement boundary, ADR-5 extension with configarr as sole TRaSH applier). `PROJECT.md` decisions table updated.

Co-bump discipline honored: `arrconf.image.tag` bumped `0.17.0 → 0.18.0` for the Python plans (01-03); charts-only/docs-only plans (04-06) correctly did not bump.

---

_Verified: 2026-05-31T03:24:03Z_
_Verifier: Claude (gsd-verifier)_
