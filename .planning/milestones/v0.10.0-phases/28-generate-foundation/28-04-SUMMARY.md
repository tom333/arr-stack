---
phase: 28-generate-foundation
plan: "04"
subsystem: charts
tags: [intent, cross-seed, generate, charts-only]
dependency_graph:
  requires: [28-03]
  provides: [charts/arr-stack/files/intent.yml, charts/arr-stack/files/cross-seed/config.js]
  affects: [INTENT-01, INTENT-03]
tech_stack:
  added: []
  patterns: [arrconf generate, intent.yml hand-edited source, CommonJS module.exports, PLACEHOLDER secrets]
key_files:
  created:
    - charts/arr-stack/files/intent.yml
    - charts/arr-stack/files/cross-seed/config.js
  modified: []
decisions:
  - "PLACEHOLDER used for torznab apikey and qBit password (T-28-09 mitigated; real secrets injected at runtime via K8s Secret)"
  - "charts-only change — no arrconf.image.tag co-bump (intent.yml and config.js live under charts/arr-stack/files/, not tools/arrconf/**)"
  - "generate --check exits 0 confirming committed config.js is byte-identical to fresh generator output"
metrics:
  duration: "~5 minutes"
  completed: "2026-05-31"
  tasks_completed: 2
  tasks_total: 2
  files_created: 2
  files_modified: 0
---

# Phase 28 Plan 04: Seed intent.yml + generate cross-seed/config.js Summary

**One-liner:** Seeded `intent.yml` with one `tools.cross_seed` block (PLACEHOLDER secrets) + generated committed `cross-seed/config.js` via `arrconf generate`; `generate --check` exits 0 on committed tree.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create seeded intent.yml | 0ce144e | charts/arr-stack/files/intent.yml |
| 2 | Generate and commit cross-seed/config.js | b6f5175 | charts/arr-stack/files/cross-seed/config.js |

## What Was Built

**Task 1 — intent.yml:**
- Created `charts/arr-stack/files/intent.yml` as the hand-edited source of truth for `arrconf generate`
- `yaml-language-server` modeline pointing at `schemas/intent-schema.json` (mirrors `arrconf.yml` line 1 convention)
- One `tools.cross_seed` block: torznab URL + qBit client connection string + link_dirs + link_type + action
- All secrets replaced with literal `PLACEHOLDER` (T-28-09 mitigated per CLAUDE.md "Ne pas committer de secrets")
- `sagas: []` stub present for schema completeness (D-05)

**Task 2 — cross-seed/config.js:**
- Generated via `arrconf generate --intent ... --output-dir ...` (not hand-written — D-11 enforced)
- Read-only header: `// GENERATED by 'arrconf generate' from intent.yml — DO NOT EDIT BY HAND`
- CommonJS `module.exports = {...};` with tab-indented, sorted keys (`json.dumps(sort_keys=True)`)
- Contains PLACEHOLDER values only — no real credentials committed
- `arrconf generate --check` exits 0 confirming committed file is byte-identical to fresh generator output (INTENT-03)

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

- `torznab` URL uses `apikey=PLACEHOLDER` — real key flows via K8s Secret at runtime (P30 concern)
- `torrent_clients` password is `PLACEHOLDER` — real password flows via K8s Secret at runtime (P30 concern)

These are intentional placeholders per CLAUDE.md "Ne pas committer de secrets" and T-28-09 mitigation. Plan 30 (cross-seed Helm deploy) will resolve how runtime secrets are injected.

## Threat Flags

None. All new content is under `charts/arr-stack/files/` (public but secret-free). T-28-09 (Information Disclosure) mitigated by PLACEHOLDER pattern.

## Self-Check: PASSED

- `charts/arr-stack/files/intent.yml` exists: YES
- `charts/arr-stack/files/cross-seed/config.js` exists: YES
- Commits exist: 0ce144e (intent.yml), b6f5175 (config.js)
- `arrconf generate --check` exits 0: YES
- No real secrets committed: YES (PLACEHOLDER verified)
- No `arrconf.image.tag` bump: YES (arrconf.image.tag remains "0.18.0" from prior wave plans)
