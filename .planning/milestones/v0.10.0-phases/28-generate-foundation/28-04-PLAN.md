---
phase: 28-generate-foundation
plan: 04
type: execute
wave: 4
depends_on: [28-03]
files_modified:
  - charts/arr-stack/files/intent.yml
  - charts/arr-stack/files/cross-seed/config.js
autonomous: true
requirements: [INTENT-01, INTENT-03]
must_haves:
  truths:
    - "A seeded intent.yml with a real cross_seed block exists, sibling to arrconf.yml"
    - "A non-empty generated cross-seed/config.js is committed with the read-only header"
    - "arrconf generate --check exits 0 on the committed tree (no drift)"
    - "No real secrets are committed — torznab/client URLs use PLACEHOLDER"
  artifacts:
    - path: "charts/arr-stack/files/intent.yml"
      provides: "Hand-edited intent source with tools.cross_seed + sagas: []"
      contains: "cross_seed"
    - path: "charts/arr-stack/files/cross-seed/config.js"
      provides: "Generated CommonJS config (read-only header + module.exports)"
      contains: "module.exports"
  key_links:
    - from: "charts/arr-stack/files/intent.yml"
      to: "charts/arr-stack/files/cross-seed/config.js"
      via: "arrconf generate"
      pattern: "module.exports"
    - from: "charts/arr-stack/files/intent.yml"
      to: "schemas/intent-schema.json"
      via: "yaml-language-server modeline"
      pattern: "intent-schema.json"
---

<objective>
Seed the proving slice (D-02): create a real, hand-edited `charts/arr-stack/files/intent.yml` carrying one `tools.cross_seed` block (and `sagas: []`), then run `arrconf generate` to produce the committed `charts/arr-stack/files/cross-seed/config.js`. This gives the CI idempotence guard (Plan 05) real bytes to diff.

Purpose: Prove `generate` end-to-end on committed bytes. charts-only change → NO image-tag co-bump (these files live under `charts/arr-stack/files/`, not `tools/arrconf/**`).
Output: seeded `intent.yml` + generated `config.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-generate-foundation/28-CONTEXT.md
@.planning/phases/28-generate-foundation/28-PATTERNS.md
@.planning/phases/28-generate-foundation/28-03-SUMMARY.md

<analog-source>
intent.yml header/modeline convention: charts/arr-stack/files/arrconf.yml (line 1, yaml-language-server $schema modeline).
Seed intent.yml content: 28-PATTERNS.md lines 538-553.
Generated config.js shape: 28-PATTERNS.md lines 568-575.
</analog-source>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the seeded intent.yml</name>
  <files>charts/arr-stack/files/intent.yml</files>
  <read_first>
    - charts/arr-stack/files/arrconf.yml (READ line 1 — the `# yaml-language-server: $schema=...` modeline convention to mirror)
    - .planning/phases/28-generate-foundation/28-PATTERNS.md (lines 538-553 — the seed intent.yml content)
    - CLAUDE.md ("Ce que tu NE dois PAS faire" — no secrets committed)
  </read_first>
  <action>
Create `charts/arr-stack/files/intent.yml` with EXACT content (PLACEHOLDER in place of real API keys/passwords — CLAUDE.md "Ne pas committer de secrets"):

```yaml
# yaml-language-server: $schema=../../../schemas/intent-schema.json
# HAND-EDITED — source of truth for 'arrconf generate'

tools:
  cross_seed:
    torznab:
      - "http://prowlarr.selfhost.svc.cluster.local:9696/1/api?apikey=PLACEHOLDER"
    torrent_clients:
      - "qbittorrent:http://admin:PLACEHOLDER@qbittorrent.selfhost.svc.cluster.local:8080"
    link_dirs:
      - "/data/torrents/cross-seed"
    link_type: hardlink
    action: inject

sagas: []
```

The `$schema` modeline points at `schemas/intent-schema.json` (committed by Plan 01). This file is under `charts/arr-stack/files/` → NO co-bump, does NOT trigger arrconf-image.yml.
  </action>
  <verify>
    <automated>test -f charts/arr-stack/files/intent.yml && grep -q PLACEHOLDER charts/arr-stack/files/intent.yml && grep -q "intent-schema.json" charts/arr-stack/files/intent.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f charts/arr-stack/files/intent.yml` succeeds
    - `grep -c PLACEHOLDER charts/arr-stack/files/intent.yml` returns >= 2 (torznab + qbit credentials)
    - `grep -q "apikey=PLACEHOLDER" charts/arr-stack/files/intent.yml` (no real key)
    - `grep -q "sagas: \[\]" charts/arr-stack/files/intent.yml`
  </acceptance_criteria>
  <done>Seeded intent.yml exists with a cross_seed block, placeholders for secrets, sagas: [], and the schema modeline.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and commit cross-seed/config.js</name>
  <files>charts/arr-stack/files/cross-seed/config.js</files>
  <read_first>
    - charts/arr-stack/files/intent.yml (the seed from Task 1)
    - .planning/phases/28-generate-foundation/28-PATTERNS.md (lines 568-575 — expected config.js shape)
  </read_first>
  <action>
Run the generator to produce the committed config: from the repo root, `cd tools/arrconf && uv run arrconf generate --intent ../../charts/arr-stack/files/intent.yml --output-dir ../../charts/arr-stack/files/`. This writes `charts/arr-stack/files/cross-seed/config.js`. Verify it byte-matches by re-running `arrconf generate --check` (must exit 0). Commit the generated `config.js`. Do NOT hand-edit the file (the header forbids it; the CI guard enforces it). charts-only change → NO co-bump.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run arrconf generate --check --intent ../../charts/arr-stack/files/intent.yml --output-dir ../../charts/arr-stack/files/ && echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `test -f charts/arr-stack/files/cross-seed/config.js` succeeds
    - `head -1 charts/arr-stack/files/cross-seed/config.js` contains `// GENERATED by 'arrconf generate'`
    - `grep -q "module.exports" charts/arr-stack/files/cross-seed/config.js`
    - `arrconf generate --check ...` exits 0 (prints CLEAN) — committed file in sync with intent
  </acceptance_criteria>
  <done>cross-seed/config.js committed; generate --check exits 0 (no drift).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed intent.yml/config.js → public GitHub repo | These files are world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09 | Information Disclosure | secrets in intent.yml / config.js | mitigate | torznab apikey and qBit password use literal `PLACEHOLDER`; real secrets injected at runtime via pod env (P30 concern), never committed (CLAUDE.md "Ne pas committer de secrets") |
| T-28-10 | Tampering | hand-edited generated config.js | mitigate | Read-only header on config.js + CI idempotence guard (Plan 05) blocks any hand-edit drift before merge |
</threat_model>

<verification>
- `grep -q "apikey=PLACEHOLDER" charts/arr-stack/files/intent.yml` (no real secret)
- `arrconf generate --check` exits 0 on the committed tree
- config.js has the read-only header and module.exports literal
</verification>

<success_criteria>
- Seeded intent.yml with a real cross_seed block (placeholders for secrets) committed sibling to arrconf.yml (INTENT-01)
- Non-empty generated config.js committed, read-only header present, in sync with intent (INTENT-03 has real bytes to diff)
</success_criteria>

<output>
After completion, create `.planning/phases/28-generate-foundation/28-04-SUMMARY.md`
</output>
