---
phase: 28-generate-foundation
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - tools/arrconf/arrconf/generators/intent.py
  - tools/arrconf/arrconf/generators/__init__.py
  - tools/arrconf/tests/test_generate_cross_seed.py
  - charts/arr-stack/values.yaml
autonomous: true
requirements: [INTENT-02]
must_haves:
  truths:
    - "generate_cross_seed(cfg) returns a deterministic JS module.exports literal string"
    - "Two calls with the same config produce byte-identical output"
    - "The output carries the read-only header and omits empty list fields"
  artifacts:
    - path: "tools/arrconf/arrconf/generators/intent.py"
      provides: "Pure function generate_cross_seed(CrossSeedConfig) -> str"
      contains: "def generate_cross_seed"
      min_lines: 25
    - path: "tools/arrconf/tests/test_generate_cross_seed.py"
      provides: "Unit tests: minimal, all-fields, determinism, sort-keys, header, empty-omission"
      contains: "def test_generate_cross_seed"
  key_links:
    - from: "tools/arrconf/arrconf/generators/__init__.py"
      to: "tools/arrconf/arrconf/generators/intent.py"
      via: "re-export generate_cross_seed in __all__"
      pattern: "generate_cross_seed"
    - from: "tools/arrconf/arrconf/generators/intent.py"
      to: "tools/arrconf/arrconf/intent_config.py"
      via: "imports CrossSeedConfig"
      pattern: "from arrconf.intent_config import CrossSeedConfig"
---

<objective>
Implement the pure-function generator `generate_cross_seed(cfg: CrossSeedConfig) -> str` that renders a cross-seed `config.js` (CommonJS `module.exports = {...}` literal) with a read-only header. Export it from `generators/__init__.py`. This is the deliberate proving slice (D-03): emit the novel JS literal format right in the foundation.

Purpose: Prove the generate framework supports literal-JS emission, not just YAML, de-risking Phase 30. Pure function (no I/O) per INTENT-02 — write/check stays in the CLI layer (Plan 03).
Output: `generators/intent.py`, updated `generators/__init__.py`, unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-generate-foundation/28-CONTEXT.md
@.planning/phases/28-generate-foundation/28-PATTERNS.md
@.planning/phases/28-generate-foundation/28-01-SUMMARY.md

<interfaces>
<!-- Consumes the contract from Plan 01 (intent_config.py). -->

From tools/arrconf/arrconf/intent_config.py (Plan 01):
```python
class CrossSeedConfig(BaseModel):
    torznab: list[str]; torrent_clients: list[str]; link_dirs: list[str]
    link_type: str = "hardlink"; action: str = "inject"
```

This plan produces:
```python
# tools/arrconf/arrconf/generators/intent.py
def generate_cross_seed(cfg: CrossSeedConfig) -> str: ...  # pure, returns JS string
```
</interfaces>

<analog-source>
Pure-function generator idiom (no I/O, mypy-strict, from __future__ import annotations): tools/arrconf/arrconf/generators/categories.py (lines 1-26 header, 126-132 generate_qbit_categories).
__init__ export surface: tools/arrconf/arrconf/generators/__init__.py (lines 1-25).
Deterministic json.dumps(sort_keys=True): tools/arrconf/arrconf/schema_gen.py line 33.
Test idiom (no respx, pure): tools/arrconf/tests/test_generators_categories.py.
</analog-source>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create generators/intent.py (generate_cross_seed pure function)</name>
  <files>tools/arrconf/arrconf/generators/intent.py</files>
  <read_first>
    - tools/arrconf/arrconf/generators/categories.py (READ lines 1-26 for the module-docstring + `from __future__ import annotations` + import convention; lines 126-132 for the pure-function contract shape)
    - tools/arrconf/arrconf/intent_config.py (the CrossSeedConfig input type from Plan 01)
    - tools/arrconf/arrconf/schema_gen.py (line 33 — the json.dumps(sort_keys=True) determinism reference)
  </read_first>
  <behavior>
    - generate_cross_seed(CrossSeedConfig(torznab=["http://a"])) starts with the // GENERATED header
    - output contains "module.exports = " and ends with ";\n"
    - empty list fields (torznab=[], torrent_clients=[], link_dirs=[]) are OMITTED from the JS object
    - link_type and action are ALWAYS present (they have non-empty defaults)
    - JS object keys are alphabetically sorted (idempotence)
    - two calls with identical input produce byte-identical strings
    - camelCase key mapping: torrent_clients -> torrentClients, link_dirs -> linkDirs, link_type -> linkType
  </behavior>
  <action>
Create `tools/arrconf/arrconf/generators/intent.py`. Header: `from __future__ import annotations`, then `import json`, `from typing import Final`, `from arrconf.intent_config import CrossSeedConfig`.

Module-level constant — EXACT text (D-11, JS `//` comment, single line):
```python
_HEADER: Final[str] = (
    "// GENERATED by 'arrconf generate' from intent.yml"
    " — DO NOT EDIT BY HAND\n"
)
```

Implement `generate_cross_seed(cfg: CrossSeedConfig) -> str` exactly as the RESEARCH/PATTERNS skeleton (28-PATTERNS.md lines 170-186): build a `data: dict[str, object] = {}`; add `data["torznab"] = cfg.torznab` only if `cfg.torznab` is non-empty; `data["torrentClients"] = cfg.torrent_clients` if non-empty; `data["linkDirs"] = cfg.link_dirs` if non-empty; ALWAYS set `data["linkType"] = cfg.link_type` and `data["action"] = cfg.action`. Render:
```python
body = json.dumps(data, indent="\t", sort_keys=True, ensure_ascii=False)
return f"{_HEADER}module.exports = {body};\n"
```
NO file I/O, NO open(), NO writes anywhere — pure function (anti-pattern guard from RESEARCH). Docstring must note the determinism guarantee (json.dumps sort_keys=True → byte-identical across runs) and that this is the P28 proving slice (D-03). Module docstring names Phase 28 / INTENT-02 / XSEED-02.

This file is under `tools/arrconf/**` → co-bump applies (Task 3).
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run python -c "from arrconf.intent_config import CrossSeedConfig; from arrconf.generators.intent import generate_cross_seed; s=generate_cross_seed(CrossSeedConfig(torznab=['http://a'])); assert s.startswith('// GENERATED'); assert 'module.exports = ' in s; assert s.endswith(';\n'); assert 'torrentClients' not in s; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - command above prints OK
    - `grep -q "def generate_cross_seed" tools/arrconf/arrconf/generators/intent.py`
    - `grep -q "sort_keys=True" tools/arrconf/arrconf/generators/intent.py`
    - `grep -c "open(" tools/arrconf/arrconf/generators/intent.py` returns 0 (no I/O)
  </acceptance_criteria>
  <done>generate_cross_seed renders a deterministic, header-prefixed JS literal; no I/O in the module.</done>
</task>

<task type="auto">
  <name>Task 2: Export generate_cross_seed from generators/__init__.py</name>
  <files>tools/arrconf/arrconf/generators/__init__.py</files>
  <read_first>
    - tools/arrconf/arrconf/generators/__init__.py (READ lines 1-25 — the current import + __all__ block to extend)
  </read_first>
  <action>
In `tools/arrconf/arrconf/generators/__init__.py`, add `from arrconf.generators.intent import generate_cross_seed` after the existing `from arrconf.generators.categories import (...)` block, and add `"generate_cross_seed"` to the `__all__` list (keep alphabetical position consistent with the existing entries). Update the module docstring to mention Phase 28 (cross-seed generator) alongside the existing Phase 10 wording. Leave all existing imports unchanged.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run python -c "from arrconf.generators import generate_cross_seed; print(generate_cross_seed.__name__)"</automated>
  </verify>
  <acceptance_criteria>
    - command above prints `generate_cross_seed`
    - `grep -q '"generate_cross_seed"' tools/arrconf/arrconf/generators/__init__.py`
  </acceptance_criteria>
  <done>generate_cross_seed is importable from arrconf.generators and listed in __all__.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests + co-bump arrconf image tag</name>
  <files>tools/arrconf/tests/test_generate_cross_seed.py, charts/arr-stack/values.yaml</files>
  <read_first>
    - tools/arrconf/tests/test_generators_categories.py (READ — the pure-function, no-respx test idiom; structure to mirror)
    - tools/arrconf/arrconf/generators/intent.py (the module under test from Task 1)
    - charts/arr-stack/values.yaml (READ line 451 — the arrconf image tag; co-bump target)
  </read_first>
  <behavior>
    - test_generate_cross_seed_minimal: header present, "module.exports = " present, ends ";\n"
    - test_generate_cross_seed_deterministic: two calls equal
    - test_generate_cross_seed_sort_keys: parse the JS object body, assert keys == sorted(keys)
    - test_generate_cross_seed_omits_empty: empty torznab/torrent_clients/link_dirs absent from output; linkType/action present
    - test_generate_cross_seed_camelcase: torrent_clients renders as "torrentClients", link_dirs as "linkDirs"
  </behavior>
  <action>
Create `tools/arrconf/tests/test_generate_cross_seed.py`. Header: `from __future__ import annotations`, module docstring "Unit tests for arrconf.generators.intent (Phase 28 INTENT-02/XSEED-02). No HTTP — generator is pure Python.". Import `CrossSeedConfig` from `arrconf.intent_config` and `generate_cross_seed` from `arrconf.generators.intent`. Write one test per case in `<behavior>` above (five tests). For the sort-keys test, extract the object via `re.search(r"module\.exports = (\{.*\});", output, re.DOTALL)` then `json.loads` and assert `list(data.keys()) == sorted(data.keys())` (mirror 28-PATTERNS.md lines 364-373).

CO-BUMP (CLAUDE.md): this plan touched `tools/arrconf/**`. Ensure `charts/arr-stack/values.yaml` line 451 reads `tag: "0.18.0"`. If Plan 01 already set it to `"0.18.0"`, leave it unchanged (idempotent — target end-state is 0.18.0, one minor bump for the whole generate feature). Do not touch the `# renovate:` annotation.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run pytest tests/test_generate_cross_seed.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd tools/arrconf && uv run pytest tests/test_generate_cross_seed.py -q` exits 0 with >= 5 tests passing
    - `grep -c "def test_generate_cross_seed" tools/arrconf/tests/test_generate_cross_seed.py` returns >= 5
    - `grep -q 'tag: "0.18.0"' charts/arr-stack/values.yaml`
  </acceptance_criteria>
  <done>Generator unit tests pass; arrconf.image.tag is 0.18.0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CrossSeedConfig values → rendered config.js | Operator-declared string values are serialized into a JS literal that cross-seed will `require()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering | JS literal rendering | mitigate | Values are serialized via `json.dumps` (proper quoting/escaping), NEVER f-string concatenation of raw values — a quote or `}` inside a torznab URL cannot break `module.exports` parsing |
| T-28-05 | Elevation of Privilege | code injection via intent values | mitigate | json.dumps escapes control chars and quotes; output is a JSON-valid object body, so no JS expression injection is possible from intent.yml string fields |
</threat_model>

<verification>
- `cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf` (triade)
- `cd tools/arrconf && uv run pytest tests/test_generate_cross_seed.py -q` passes
- Determinism: two `generate_cross_seed` calls byte-identical (covered by test)
</verification>

<success_criteria>
- generate_cross_seed is a pure function emitting a deterministic, header-prefixed JS module.exports literal (INTENT-02 / D-03)
- json.dumps used for rendering (no f-string interpolation of raw values) — injection-safe
- Exported from arrconf.generators; tests pass; tag co-bumped to 0.18.0
</success_criteria>

<output>
After completion, create `.planning/phases/28-generate-foundation/28-02-SUMMARY.md`
</output>
