---
phase: 28-generate-foundation
plan: 05
type: execute
wave: 5
depends_on: [28-03, 28-04]
files_modified:
  - .github/workflows/tests.yml
autonomous: true
requirements: [INTENT-03]
must_haves:
  truths:
    - "A generate-idempotence CI job runs arrconf generate --check on PR and push"
    - "The job fails (exit non-zero) when committed configs drift from intent.yml"
    - "PRs that modify only charts/arr-stack/files/** trigger the guard"
  artifacts:
    - path: ".github/workflows/tests.yml"
      provides: "generate-idempotence job + charts/arr-stack/files/** path trigger"
      contains: "generate-idempotence"
  key_links:
    - from: ".github/workflows/tests.yml"
      to: "charts/arr-stack/files/intent.yml"
      via: "on.pull_request.paths includes charts/arr-stack/files/**"
      pattern: "charts/arr-stack/files"
    - from: ".github/workflows/tests.yml"
      to: "arrconf generate --check"
      via: "generate-idempotence job step"
      pattern: "generate --check"
---

<objective>
Add the CI idempotence guard (D-07/D-09): a new `generate-idempotence` job in `tests.yml` that runs `arrconf generate --check`, plus the `charts/arr-stack/files/**` path trigger so the guard fires when an operator edits `intent.yml` or a generated config. Guard stays OUT of `chart-lint.yml` (which carries the auto-tagger).

Purpose: INTENT-03 — CI blocks any PR where committed configs diverge from intent. CI-only change → NO co-bump.
Output: updated `tests.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-generate-foundation/28-CONTEXT.md
@.planning/phases/28-generate-foundation/28-PATTERNS.md
@.planning/phases/28-generate-foundation/28-04-SUMMARY.md

<analog-source>
Existing job structure + uv setup steps to copy verbatim: .github/workflows/tests.yml lines 20-37 (the `test` job: runs-on/permissions/defaults + Setup uv + Install dependencies).
Idempotence-guard step shape: tests.yml lines 52-59 (schema-gen) and 28-PATTERNS.md lines 496-522 (the generate-idempotence job).
Why it stays out of chart-lint.yml: 28-CONTEXT.md D-09 (mathieudutour auto-tagger isolation).
</analog-source>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add charts/arr-stack/files/** path trigger to tests.yml</name>
  <files>.github/workflows/tests.yml</files>
  <read_first>
    - .github/workflows/tests.yml (READ the top `on:` block — the `pull_request.paths` and `push.paths` lists; confirm current entries are tools/arrconf/**, schemas/**, examples/**)
  </read_first>
  <action>
In `.github/workflows/tests.yml`, add `'charts/arr-stack/files/**'` to BOTH the `on.pull_request.paths` list AND the `on.push.paths` list (matching the quoting/indentation style of the existing entries). This is mandatory (RESEARCH Pitfall 2 / PATTERNS gotcha #4): without it, a PR that only edits `intent.yml` or `cross-seed/config.js` skips the guard entirely. Do not remove any existing path entry.
  </action>
  <verify>
    <automated>grep -c "charts/arr-stack/files" .github/workflows/tests.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "charts/arr-stack/files" .github/workflows/tests.yml` returns >= 2 (pull_request + push paths)
    - existing `tools/arrconf` path entry still present: `grep -q "tools/arrconf" .github/workflows/tests.yml`
  </acceptance_criteria>
  <done>tests.yml triggers on charts/arr-stack/files/** for both PR and push.</done>
</task>

<task type="auto">
  <name>Task 2: Add the generate-idempotence job</name>
  <files>.github/workflows/tests.yml</files>
  <read_first>
    - .github/workflows/tests.yml (READ lines 20-37 — the `test` job header + Setup uv + Install dependencies steps to copy verbatim; READ lines 52-59 for the guard-step shape)
    - .planning/phases/28-generate-foundation/28-PATTERNS.md (lines 496-522 — the generate-idempotence job to add)
  </read_first>
  <action>
Add a new job `generate-idempotence` to `tests.yml`, parallel to the existing `test` job. Copy the `runs-on: ubuntu-24.04`, `permissions: {contents: read}`, `defaults.run.working-directory: tools/arrconf`, `actions/checkout@v4`, Setup uv (`astral-sh/setup-uv@v4`, version "0.11.x", python-version "3.13", enable-cache true), and `uv sync --frozen` steps VERBATIM from the `test` job (tests.yml lines 20-37). Then add the guard step EXACTLY:

```yaml
      - name: Verify generate idempotence (INTENT-03)
        working-directory: ${{ github.workspace }}
        run: |
          cd tools/arrconf
          uv run arrconf generate --check \
            --intent ../../charts/arr-stack/files/intent.yml \
            --output-dir ../../charts/arr-stack/files/ \
            || (echo "::error::Generated configs drift from intent.yml — run 'cd tools/arrconf && uv run arrconf generate --intent ../../charts/arr-stack/files/intent.yml --output-dir ../../charts/arr-stack/files/' and commit"; exit 1)
```

`--check` means the job writes nothing — read-only, idempotent on the workspace. Do NOT add this to `chart-lint.yml` (D-09 — keep it away from the mathieudutour auto-tagger).
  </action>
  <verify>
    <automated>grep -q "generate-idempotence" .github/workflows/tests.yml && grep -q "generate --check" .github/workflows/tests.yml && ! grep -q "generate-idempotence" .github/workflows/chart-lint.yml && echo OK || echo FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "generate-idempotence:" .github/workflows/tests.yml` (job defined)
    - `grep -q "arrconf generate --check" .github/workflows/tests.yml`
    - `grep -q "generate-idempotence" .github/workflows/chart-lint.yml` returns NO match (guard stays out of chart-lint — D-09); the verify command prints OK
  </acceptance_criteria>
  <done>generate-idempotence job runs arrconf generate --check; guard absent from chart-lint.yml.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR diff → CI gate | Untrusted PR content (incl. hand-edited generated files) checked before merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-11 | Tampering | drift slipping past CI | mitigate | generate-idempotence job runs on PR + push; path trigger includes charts/arr-stack/files/** so intent.yml edits always fire the guard |
| T-28-12 | Elevation of Privilege | guard interacting with auto-tagger | mitigate | Guard placed in tests.yml (read-only, `permissions: contents: read`), NOT chart-lint.yml — isolated from mathieudutour auto-tagger (D-09) |
</threat_model>

<verification>
- `grep -q "generate-idempotence:" .github/workflows/tests.yml`
- `grep -c "charts/arr-stack/files" .github/workflows/tests.yml` >= 2
- chart-lint.yml unchanged (no generate-idempotence reference)
</verification>

<success_criteria>
- generate-idempotence CI job runs `arrconf generate --check` on PR + push and fails on drift (INTENT-03)
- charts/arr-stack/files/** path trigger added so intent.yml edits fire the guard
- Guard kept out of chart-lint.yml (D-09)
</success_criteria>

<output>
After completion, create `.planning/phases/28-generate-foundation/28-05-SUMMARY.md`
</output>
