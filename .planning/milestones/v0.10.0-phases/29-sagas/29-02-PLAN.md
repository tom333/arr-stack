---
phase: 29-sagas
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - tools/arrconf/arrconf/resources/radarr/collection.py
  - tools/arrconf/arrconf/resources/radarr/__init__.py
  - tools/arrconf/arrconf/reconcilers/radarr.py
  - tools/arrconf/arrconf/__main__.py
  - tools/arrconf/tests/test_reconcilers_radarr_collections.py
  - tools/arrconf/tests/fixtures/radarr/collection.json
  - charts/arr-stack/values.yaml
autonomous: true
requirements: [SAGAS-02]
must_haves:
  truths:
    - "reconcile_radarr_collections matches kind=movies sagas to Radarr collections by tmdbId"
    - "PUT fires only on field drift; a second reconcile run with no drift produces 0 plan_actions (strict idempotence)"
    - "a saga whose tmdb_collection is absent from Radarr logs a warning and is skipped (no POST-create)"
    - "profile name resolves to qualityProfileId via read-only GET /qualityprofile; ConfigError if not found"
    - "apply invokes reconcile_radarr_collections only when intent_cfg has sagas and radarr is a target"
  artifacts:
    - path: "tools/arrconf/arrconf/resources/radarr/collection.py"
      provides: "CollectionResource pydantic schema (tmdbId match key, read-only excludes)"
      contains: "class CollectionResource"
    - path: "tools/arrconf/arrconf/reconcilers/radarr.py"
      provides: "reconcile_radarr_collections function"
      contains: "def reconcile_radarr_collections"
  key_links:
    - from: "tools/arrconf/arrconf/reconcilers/radarr.py"
      to: "Radarr /api/v3/collection"
      via: "GET list indexed by tmdbId, PUT /collection/{id} on drift"
      pattern: "collection"
    - from: "tools/arrconf/arrconf/__main__.py"
      to: "reconcile_radarr_collections"
      via: "saga branch guarded by intent_cfg + radarr target"
      pattern: "reconcile_radarr_collections"
---

<objective>
Build the Radarr Collections reconciler: a new `CollectionResource` schema, the `reconcile_radarr_collections()` function (GET-match by `tmdbId`, PUT-on-drift, log-skip absent collections), and wire it into `apply` behind the intent.yml saga branch. Strictly idempotent (2nd run = 0 plan_actions, D-07).

Purpose: SAGAS-02 — arrconf reconciles Radarr Collections from declared movie sagas.

Output: new resource schema + reconciler function + apply wiring + respx tests + co-bumped tag (idempotent if already 0.19.0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-sagas/29-CONTEXT.md
@.planning/phases/29-sagas/29-RESEARCH.md
@.planning/phases/29-sagas/29-PATTERNS.md
@.planning/phases/29-sagas/29-01-PLAN.md

<interfaces>
<!-- From 29-01: SagaEntry in intent_config.py (kind=movies has tmdb_collection:int, profile:str, root:str). -->
<!-- From 29-01: __main__.py apply() now has `intent_cfg: IntentConfig | None` loaded after load_config, guarded by intent_path.exists(). -->
<!-- Radarr CollectionResource (verified from Radarr source, 29-RESEARCH §VALIDATE D-03):
     fields: id:int, title:str, sortTitle:str, tmdbId:int (MATCH KEY), monitored:bool,
     rootFolderPath:str, qualityProfileId:int, searchOnAdd:bool, minimumAvailability:str enum,
     movies:list, missingMovies:int, tags:HashSet<int>, images:list.
     minimumAvailability JSON enum (camelCase): "tba","announced","inCinemas","released","deleted". Default "released". -->
<!-- API: GET /api/v3/collection (all, auto-discovered only — NO POST endpoint); PUT /api/v3/collection/{id} body=CollectionResource → 202. -->
<!-- Existing radarr.py helpers: _reconcile_movie_tags (lines 302-366) uses client._request("PUT", path, json=body); _execute dry_run idiom (130-166); _ensure_managed_tag (104-117). RadarrClient.get(path) → list. ConfigError already imported. -->
<!-- Existing radarr resource schema analog: resources/sonarr/root_folder.py (id + read-only excludes pattern). resources/radarr/__init__.py exports resource classes. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CollectionResource schema + reconcile_radarr_collections</name>
  <files>tools/arrconf/arrconf/resources/radarr/collection.py, tools/arrconf/arrconf/resources/radarr/__init__.py, tools/arrconf/arrconf/reconcilers/radarr.py, tools/arrconf/tests/fixtures/radarr/collection.json, tools/arrconf/tests/test_reconcilers_radarr_collections.py</files>
  <read_first>
    - tools/arrconf/arrconf/resources/sonarr/root_folder.py (analog: id + read-only `Field(default=None, exclude=True)` excludes, extra="allow")
    - tools/arrconf/arrconf/reconcilers/radarr.py (analogs: path constants ~lines 83-91; _reconcile_movie_tags lines 302-366 for client._request PUT idiom; _execute dry_run lines 130-166; structlog `log` at line 81; ConfigError import)
    - tools/arrconf/arrconf/resources/radarr/__init__.py (export list to extend)
    - 29-PATTERNS.md "resources/radarr/collection.py" + "reconcilers/radarr.py — new reconcile_radarr_collections() fn" (verbatim function body)
    - 29-RESEARCH.md "Pattern 1: Radarr Collection Reconcile" + Pitfall 1 (re-inject id) + Pitfall 6 (minimumAvailability casing — read cluster value, do not hardcode-guess) + Open Question A1
  </read_first>
  <behavior>
    - Test (no-op): GET /collection returns a collection matching all desired fields → reconcile returns [] and NO PUT fires (assert put route call_count == 0).
    - Test (drift→PUT): GET /collection returns a collection with monitored=false (drift) → reconcile PUTs once to /collection/{id}; returned actions contain "collection:updated:<name>"; PUT body includes "id" == cluster id (Pitfall 1).
    - Test (absent→skip): GET /collection returns [] (collection not auto-discovered) → reconcile logs "collection_absent_skip" and returns []; NO PUT fires.
    - Test (profile missing→ConfigError): GET /qualityprofile lacks the saga.profile name → reconcile raises ConfigError.
    - Test (idempotence): two consecutive reconcile calls against the no-op fixture both return [] (2nd run = 0 plan_actions).
    - Test (dry_run): drift present + dry_run=True → no PUT fires; actions contain "collection:dry_run:<name>".
    - Test (kind=series ignored): a series saga in the input list produces no Radarr calls.
  </behavior>
  <action>
    Create tools/arrconf/arrconf/resources/radarr/collection.py mirroring 29-PATTERNS.md "Core schema pattern": `model_config = ConfigDict(extra="allow")`; match key `tmdbId: int`; reconciled fields `monitored: bool=True`, `qualityProfileId: int=0`, `rootFolderPath: str=""`, `searchOnAdd: bool=True`, `minimumAvailability: str="released"`; read-only excludes `id/title/sortTitle/missingMovies/movies/images/tags` all `Field(default=None, exclude=True)`. Add `CollectionResource` to resources/radarr/__init__.py exports.

    In tools/arrconf/arrconf/reconcilers/radarr.py: add path constants `COLLECTION_PATH = "/collection"` and `QUALITY_PROFILE_PATH = "/qualityprofile"` (only if not already defined). Add `from arrconf.intent_config import SagaEntry` import. Implement `reconcile_radarr_collections(client: RadarrClient, sagas: list[SagaEntry], dry_run: bool) -> list[str]` verbatim per 29-PATTERNS.md "Core reconcile function pattern": filter movie_sagas; `client.get(QUALITY_PROFILE_PATH)` → qp_by_name; `client.get(COLLECTION_PATH)` → by_tmdb_id; for each movie saga lookup by tmdb_collection → if None warn+skip (D-03), else resolve profile (ConfigError if missing), build `desired` dict, compute drift_fields, no-op/dry_run/PUT branches. PUT via `client._request("PUT", f"{COLLECTION_PATH}/{cluster['id']}", json=body)` where `body = dict(cluster); body.update(desired); body["id"] = cluster["id"]` (Pitfall 1 re-inject).

    minimumAvailability (A1/Pitfall 6): default desired value is `"released"`. Add an inline comment noting that if a future live test shows Radarr returns PascalCase, the drift comparison naturally re-PUTs; do NOT hardcode a casing variant beyond the camelCase default — the cluster value flows through `body = dict(cluster)` untouched for fields we do not override.

    Create fixture tools/arrconf/tests/fixtures/radarr/collection.json from 29-RESEARCH.md "Sanitized fixture template" (tmdbId 645, qualityProfileId 3, rootFolderPath "/media/films", minimumAvailability "released", monitored true, searchOnAdd true).

    Write tools/arrconf/tests/test_reconcilers_radarr_collections.py covering all <behavior> cases, mirroring tests/test_reconcilers_radarr.py respx idiom (load fixture, `@pytest.mark.respx(base_url=f"{RADARR_BASE}/api/v3", assert_all_called=False)`, register GET /collection + GET /qualityprofile, PUT route via `url__regex`). NO live API calls.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run pytest tests/test_reconcilers_radarr_collections.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd tools/arrconf && uv run pytest tests/test_reconcilers_radarr_collections.py -q` exits 0 with ≥7 passing tests (incl. an idempotence test asserting 2nd run returns []).
    - `grep -q 'def reconcile_radarr_collections' tools/arrconf/arrconf/reconcilers/radarr.py` succeeds.
    - `grep -q 'class CollectionResource' tools/arrconf/arrconf/resources/radarr/collection.py` succeeds.
    - `grep -q 'body\["id"\] = cluster\["id"\]' tools/arrconf/arrconf/reconcilers/radarr.py` succeeds (Pitfall 1 re-inject present).
    - `cd tools/arrconf && uv run mypy arrconf` reports 0 errors.
  </acceptance_criteria>
  <done>CollectionResource + reconcile_radarr_collections implemented; tmdbId-matched PUT-on-drift; absent→skip; 2nd run = 0 actions; ≥7 respx tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Radarr Collections saga branch into apply + co-bump</name>
  <files>tools/arrconf/arrconf/__main__.py, charts/arr-stack/values.yaml</files>
  <read_first>
    - tools/arrconf/arrconf/__main__.py (analog: existing sonarr/radarr branches lines 240+ for RadarrClient construction `RadarrClient(base_url=instance.base_url, api_key=settings.X_api_key.get_secret_value())`; failures.append pattern; the 29-01 intent_cfg load block)
    - 29-PATTERNS.md "__main__.py" Step 3 saga branches (verbatim Radarr saga branch)
    - 29-RESEARCH.md "Pattern 4" ordering constraint (saga branches run AFTER existing app branches)
    - charts/arr-stack/values.yaml line 451 (tag)
  </read_first>
  <action>
    In tools/arrconf/arrconf/__main__.py, AFTER all existing app branches (after the Jellyfin block ~line 472) and BEFORE `if failures:` (~line 474), add the Radarr Collections saga branch from 29-PATTERNS.md Step 3 (only the Radarr portion in this plan; the Jellyfin saga branch lands in 29-03): `if intent_cfg is not None and intent_cfg.sagas:` then guard `if "radarr" in targets and "main" in root.radarr and settings.radarr_api_key:`; inside, lazily import `reconcile_radarr_collections`, construct `RadarrClient(base_url=root.radarr["main"].base_url, api_key=settings.radarr_api_key.get_secret_value())`, call `reconcile_radarr_collections(client, intent_cfg.sagas, dry_run=dry_run or settings.arrconf_dry_run)`, log `apply_complete app="radarr_collections"`, and on `(ApiClientError, ReconcileError, ConfigError)` log `app_failed` + `failures.append("radarr_collections")`. Remove the 29-01 temporary `log.debug("intent_loaded", ...)` line if present (it was a lint placeholder; intent_cfg is now genuinely consumed). Confirm the exact base_url attribute and settings field names match the existing radarr branch.

    Co-bump charts/arr-stack/values.yaml line 451 to `tag: "0.19.0"` (idempotent — already set by 29-01; leave as-is if already 0.19.0). Do not touch the renovate annotation.
  </action>
  <verify>
    <automated>cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf && uv run pytest tests/test_reconcilers_radarr_collections.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'reconcile_radarr_collections' tools/arrconf/arrconf/__main__.py` succeeds.
    - `grep -q 'radarr_collections' tools/arrconf/arrconf/__main__.py` appears in BOTH an apply_complete and a failures.append context.
    - `grep -q 'tag: "0.19.0"' charts/arr-stack/values.yaml` succeeds.
    - `cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf` all green.
  </acceptance_criteria>
  <done>Radarr Collections saga branch wired after existing app branches; failures tracked; tag at 0.19.0; triade green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arrconf → Radarr /api/v3 (env-injected RADARR_API_KEY) | arrconf writes collection config across the network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04 | Tampering / Integrity | PUT /collection/{id} body missing id | mitigate | Re-inject `body["id"] = cluster["id"]` (Pitfall 1) so Radarr routes the PUT to the correct collection; never blind-overwrite (Task 1). |
| T-29-05 | Elevation of Privilege (scope) | GET /qualityprofile for name→id | accept | Read-only GET on /qualityprofile for resolution is explicitly allowed by ADR-5; NO write to quality_profiles/custom_formats occurs. Verified: only PUT target is /collection. |
| T-29-06 | Denial of Service | unbounded PUTs / wrong-collection writes | mitigate | Match strictly by `tmdbId`; absent collections log-skip (no POST-create); drift-only PUT bounds writes to declared sagas (Task 1). |
| T-29-07 | Information Disclosure | ConfigError leaking secrets | accept | ConfigError messages contain profile names/paths only, no API keys; structured logs use env-var names, not values (existing pattern). |
</threat_model>

<verification>
- `cd tools/arrconf && uv run pytest tests/test_reconcilers_radarr_collections.py -q` — ≥7 pass incl. idempotence.
- `cd tools/arrconf && uv run ruff format --check . && uv run ruff check . && uv run mypy arrconf` — green.
- `grep -q 'tag: "0.19.0"' charts/arr-stack/values.yaml` — co-bump present.
</verification>

<success_criteria>
- SAGAS-02: arrconf reconciles Radarr Collections from movie sagas — GET-match by tmdbId, PUT-on-drift, idempotent (2nd run = 0 plan_actions), absent collections log-skip.
- No quality_profile WRITE (ADR-5 boundary held — read-only GET only).
</success_criteria>

<output>
After completion, create `.planning/phases/29-sagas/29-02-SUMMARY.md`.
</output>
