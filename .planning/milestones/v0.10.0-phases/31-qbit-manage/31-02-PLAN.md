---
phase: 31-qbit-manage
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - charts/arr-stack/Chart.yaml
  - charts/arr-stack/values.yaml
  - charts/arr-stack/templates/qbit-manage-configmap.yaml
  - .github/workflows/chart-lint.yml
  - README.md
autonomous: true
requirements: [QBM-03]
must_haves:
  truths:
    - "qbit_manage is deployed as a CronJob app-template alias (the 13th) in the umbrella chart, schedule 0 */4 * * * [D-07]"
    - "The generated qbit_manage/config.yml is mounted read-only at /config/config.yml via ConfigMap (.Files.Get) [D-09]"
    - "arrconf-env injects QBT_USER/QBT_PASS so the !ENV tags in config.yml resolve at runtime"
    - "The CronJob runs once and exits (QBT_RUN=true + QBT_SCHEDULE=0) — no daemon hang; one run executes all enabled sections [D-08]"
    - "helm template + kubeconform pass with the 13th alias unpacked; CI annotation guard bumped to 14"
  artifacts:
    - path: "charts/arr-stack/Chart.yaml"
      provides: "13th app-template alias qbit-manage"
      contains: "alias: qbit-manage"
    - path: "charts/arr-stack/templates/qbit-manage-configmap.yaml"
      provides: "ConfigMap rendering files/qbit_manage/config.yml"
      contains: "files/qbit_manage/config.yml"
    - path: "charts/arr-stack/values.yaml"
      provides: "qbit-manage CronJob block"
      contains: "qbit-manage:"
    - path: ".github/workflows/chart-lint.yml"
      provides: "qbit-manage in unpack loop + annotation guard bumped to 14"
      contains: "qbit-manage"
  key_links:
    - from: "charts/arr-stack/templates/qbit-manage-configmap.yaml"
      to: "charts/arr-stack/files/qbit_manage/config.yml"
      via: ".Files.Get"
      pattern: "files/qbit_manage/config.yml"
    - from: "charts/arr-stack/values.yaml qbit-manage block"
      to: "qbit-manage-config ConfigMap"
      via: "persistence.config configMap mount"
      pattern: "name: qbit-manage-config"
    - from: "charts/arr-stack/values.yaml qbit-manage block"
      to: "arrconf-env SealedSecret"
      via: "envFrom secretRef"
      pattern: "name: arrconf-env"
---

<objective>
Deploy qbit_manage as the 13th umbrella `app-template` CronJob alias: add the Chart.yaml dependency, the `qbit-manage` CronJob block in values.yaml, the ConfigMap template mounting the generated config.yml, and the Helm-4 multi-alias unpack-loop + Renovate annotation-guard updates in chart-lint.yml and README.

Purpose: Materialize QBM-03 (qbit_manage deployed as Helm CronJob alias). The generated `charts/arr-stack/files/qbit_manage/config.yml` from Plan 31-01 is mounted read-only; `arrconf-env` injects `QBT_USER`/`QBT_PASS` for the `!ENV` tags; `QBT_RUN=true` + `QBT_SCHEDULE=0` make the container run-once (no daemon hang).

Output: Chart.yaml 13th alias, values.yaml CronJob block, qbit-manage-configmap.yaml, chart-lint.yml + README updates.

Depends on Plan 31-01: the committed `charts/arr-stack/files/qbit_manage/config.yml` must exist for `.Files.Get` to render.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-qbit-manage/31-CONTEXT.md
@.planning/phases/31-qbit-manage/31-RESEARCH.md
@.planning/phases/31-qbit-manage/31-PATTERNS.md

<interfaces>
<!-- Exact analogs the executor copies. -->

Chart.yaml last alias (lines 54-57) — append the 13th after this:
```yaml
  - name: app-template
    alias: cross-seed
    version: 5.0.0
    repository: https://bjw-s-labs.github.io/helm-charts
```

charts/arr-stack/templates/configarr-configmap.yaml (full — verbatim analog):
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: configarr-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "arr-stack.labels" . | nindent 4 }}
data:
  config.yml: |
    {{- .Files.Get "files/configarr.yml" | nindent 4 }}
```

chart-lint.yml — current alias loop (lines 48-50) ends with `cross-seed`; current annotation guard (lines 132-135):
```python
          if total_matches < 12:
              print(f'ERROR: expected >= 12 image matches, got {total_matches}')
          ...
          print(f'OK: {total_matches} image matches (>= 12 required)')
```
The guard step `name:` is on line ~111. Confirm the exact step-name string by reading the file before editing.

README.md — current Helm-4 alias loop (lines 182-186) ends with `cross-seed`, comment says "12 aliases".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 13th Chart.yaml alias + ConfigMap template + values.yaml CronJob block</name>
  <files>charts/arr-stack/Chart.yaml, charts/arr-stack/templates/qbit-manage-configmap.yaml, charts/arr-stack/values.yaml</files>
  <read_first>
    - charts/arr-stack/Chart.yaml (lines 54-57 cross-seed alias — append 13th)
    - charts/arr-stack/templates/configarr-configmap.yaml (full — verbatim analog)
    - charts/arr-stack/values.yaml (lines 426-480 arrconf CronJob block — closest analog; confirm arrconf tag is already 0.20.0 from Plan 31-01)
    - .planning/phases/31-qbit-manage/31-PATTERNS.md (lines 474-647 Chart.yaml/values.yaml/configmap analogs)
    - .planning/phases/31-qbit-manage/31-RESEARCH.md (lines 467-515 CronJob block; Pitfalls 2-3)
  </read_first>
  <action>
Step A — `charts/arr-stack/Chart.yaml`: append the 13th alias after the existing `cross-seed` entry (verbatim copy, alias changed):
```yaml
  - name: app-template
    alias: qbit-manage
    version: 5.0.0
    repository: https://bjw-s-labs.github.io/helm-charts
```

Step B — create `charts/arr-stack/templates/qbit-manage-configmap.yaml` as a copy of `configarr-configmap.yaml` with name and `.Files.Get` path changed:
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: qbit-manage-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "arr-stack.labels" . | nindent 4 }}
data:
  config.yml: |
    {{- .Files.Get "files/qbit_manage/config.yml" | nindent 4 }}
```
Note: the `.Files.Get` path uses `qbit_manage` (underscore) — matches the directory written by Plan 31-01. The ConfigMap name `qbit-manage-config` (hyphen) matches the values.yaml mount reference below.

Step C — `charts/arr-stack/values.yaml`: add the `qbit-manage` CronJob block. Place it after the `configarr` block (adjacent to the other CronJob aliases). Copy from PATTERNS.md lines 555-601 / RESEARCH.md lines 470-515:
```yaml
qbit-manage:
  global:
    nameOverride: qbit-manage
    fullnameOverride: qbit-manage
  serviceAccount:
    qbit-manage: {}
  controllers:
    main:
      type: cronjob
      cronjob:
        schedule: "0 */4 * * *"
        concurrencyPolicy: Forbid
        successfulJobsHistory: 1
        failedJobsHistory: 2
        startingDeadlineSeconds: 600
      containers:
        main:
          image:
            # renovate: image=ghcr.io/stuffanthings/qbit_manage
            repository: ghcr.io/stuffanthings/qbit_manage
            tag: "v4.6.6"
            pullPolicy: IfNotPresent
          env:
            TZ: "Europe/Paris"
            QBT_RUN: "true"
            QBT_SCHEDULE: "0"
            QBT_DRY_RUN: "false"
          envFrom:
            - secretRef:
                name: arrconf-env
          resources:
            limits:
              cpu: 250m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 128Mi
  persistence:
    config:
      type: configMap
      name: qbit-manage-config
      globalMounts:
        - path: /config/config.yml
          subPath: config.yml
          readOnly: true
```
Critical details (do not omit):
- `# renovate: image=ghcr.io/stuffanthings/qbit_manage` annotation MUST be directly above `repository:` (lowercase `stuffanthings`).
- `QBT_RUN: "true"` + `QBT_SCHEDULE: "0"` are MANDATORY — without them the container runs in daemon mode and the CronJob pod never completes (RESEARCH Pitfall 2). Keep them quoted strings.
- Mount path is `/config/config.yml` (qbit_manage Docker convention), NOT `/app/config/`.
- `envFrom: arrconf-env` provides QBT_USER/QBT_PASS for the `!ENV` resolution.
- Do NOT touch the `arrconf` image tag — it was already co-bumped to 0.20.0 by Plan 31-01. This plan adds NO Python and needs NO further co-bump. Do NOT add a `pod.securityContext` (qbit_manage image runs as root by default; mirror RESEARCH note — omit it).

Verify the chart renders with the Helm-4 unpack workaround (Task 2 codifies it in CI; run it locally to verify the render before committing).
  </action>
  <verify>
    <automated>helm dependency build charts/arr-stack/ >/dev/null 2>&1 || true; tar -xzf charts/arr-stack/charts/app-template-5.0.0.tgz -C charts/arr-stack/charts/ 2>/dev/null; for a in sonarr radarr prowlarr qbittorrent cleanuparr seerr flaresolverr jellyfin suggestarr arrconf configarr cross-seed qbit-manage; do [ ! -d "charts/arr-stack/charts/$a" ] && cp -r charts/arr-stack/charts/app-template "charts/arr-stack/charts/$a"; done; helm lint charts/arr-stack/ && helm template charts/arr-stack/ -f examples/values-prod.yaml | kubeconform -ignore-missing-schemas -</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "alias: qbit-manage" charts/arr-stack/Chart.yaml`
    - `charts/arr-stack/templates/qbit-manage-configmap.yaml` exists
    - `grep -q "name: qbit-manage-config" charts/arr-stack/templates/qbit-manage-configmap.yaml`
    - `grep -q 'files/qbit_manage/config.yml' charts/arr-stack/templates/qbit-manage-configmap.yaml`
    - `grep -q "^qbit-manage:" charts/arr-stack/values.yaml`
    - `grep -q "# renovate: image=ghcr.io/stuffanthings/qbit_manage" charts/arr-stack/values.yaml`
    - `grep -q 'tag: "v4.6.6"' charts/arr-stack/values.yaml`
    - `grep -q 'QBT_RUN: "true"' charts/arr-stack/values.yaml` AND `grep -q 'QBT_SCHEDULE: "0"' charts/arr-stack/values.yaml`
    - `grep -q "/config/config.yml" charts/arr-stack/values.yaml`
    - `grep -q 'tag: "0.20.0"' charts/arr-stack/values.yaml` (arrconf tag unchanged from 31-01)
    - `helm lint charts/arr-stack/` exits 0 and `helm template ... | kubeconform -ignore-missing-schemas -` exits 0
  </acceptance_criteria>
  <done>13th alias added; qbit-manage-configmap.yaml renders files/qbit_manage/config.yml; values.yaml has the qbit-manage CronJob with renovate annotation, v4.6.6 pin, QBT_RUN/QBT_SCHEDULE run-once env, arrconf-env envFrom, /config/config.yml mount; arrconf tag still 0.20.0; helm lint + kubeconform green.</done>
</task>

<task type="auto">
  <name>Task 2: chart-lint.yml unpack loop + annotation guard (12→14) + README alias loop</name>
  <files>.github/workflows/chart-lint.yml, README.md</files>
  <read_first>
    - .github/workflows/chart-lint.yml (lines 48-50 alias loop; line ~111 the annotation-guard step `name:`; lines 123-135 annotation count guard)
    - README.md (lines 182-186 Vérification locale alias loop + comment)
    - .planning/phases/31-qbit-manage/31-PATTERNS.md (lines 651-714 chart-lint + README edits)
    - .planning/phases/31-qbit-manage/31-RESEARCH.md (Pitfall 4 lines 357-361, Pitfall 5 lines 363-367)
  </read_first>
  <action>
Edit 1 — `chart-lint.yml` alias loop (lines ~48-50): add `qbit-manage` at the end of the `for alias in ...` list. After edit the loop reads:
```bash
          for alias in sonarr radarr prowlarr qbittorrent cleanuparr seerr flaresolverr jellyfin suggestarr arrconf configarr cross-seed qbit-manage; do
            [ ! -d "charts/arr-stack/charts/$alias" ] && cp -r charts/arr-stack/charts/app-template "charts/arr-stack/charts/$alias"
          done
```
If there is a comment above the loop mentioning the alias count (e.g. "13 aliases"), bump it to 13.

Edit 2 — `chart-lint.yml` Renovate annotation count guard. Change the threshold from 12 to 14 (current count after this phase: 13 existing image annotations + the cross-seed initContainer extra annotation already counted = 14 total; RESEARCH Pitfall 4). Update ALL FOUR references — the guard, both messages, AND the step name string itself (so a stale step name is caught by CI/grep):
- the guard: `if total_matches < 14:`
- the ERROR message: `print(f'ERROR: expected >= 14 image matches, got {total_matches}')`
- the OK message: `print(f'OK: {total_matches} image matches (>= 14 required)')`
- the annotation-guard step `name:` on line ~111 — first read the exact current string, then update its parenthetical so it reads `customManagers regex synthetic test (>= 14 ...)`. The literal substring `customManagers regex synthetic test (>= 14` MUST be present after the edit. Do NOT leave the old `>= 12` (or any stale count) anywhere in the step name.
  </action>
  <verify>
    <automated>grep -q "configarr cross-seed qbit-manage" .github/workflows/chart-lint.yml && grep -q "total_matches < 14" .github/workflows/chart-lint.yml && ! grep -q "total_matches < 12" .github/workflows/chart-lint.yml && grep -q "customManagers regex synthetic test (>= 14" .github/workflows/chart-lint.yml && grep -q "configarr cross-seed qbit-manage" README.md && grep -q "13 aliases" README.md && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/chart-lint.yml')); print('yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "configarr cross-seed qbit-manage" .github/workflows/chart-lint.yml` (alias loop updated)
    - `grep -q "total_matches < 14" .github/workflows/chart-lint.yml` (guard bumped)
    - `! grep -q "total_matches < 12" .github/workflows/chart-lint.yml` (old guard removed)
    - `grep -q "customManagers regex synthetic test (>= 14" .github/workflows/chart-lint.yml` (step name + threshold updated, stale step name caught — WARNING 3)
    - `! grep -q ">= 12 " .github/workflows/chart-lint.yml` (no stale "12" threshold anywhere)
    - `grep -q "configarr cross-seed qbit-manage" README.md` (README loop updated)
    - `grep -q "13 aliases" README.md` (comment count bumped)
    - chart-lint.yml is valid YAML (`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/chart-lint.yml'))"`)
  </acceptance_criteria>
  <done>chart-lint.yml unpack loop includes qbit-manage; annotation guard bumped 12→14 across guard/messages/step-name (step name reads "customManagers regex synthetic test (>= 14 ...)"); README alias loop includes qbit-manage with "13 aliases" comment; chart-lint.yml still valid YAML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ConfigMap → pod | Read-only mount of the generated config.yml; no secret embedded |
| arrconf-env → qbit_manage container | SealedSecret injects QBT_USER/QBT_PASS at runtime; resolved by qbit_manage `!ENV` parser |
| CronJob → in-cluster qBittorrent svc | qbit_manage writes share_limits/tags but NEVER categories (cat_update:false enforced by Plan 31-01 config) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-06 | Information Disclosure | qbit-manage-configmap.yaml | mitigate | ConfigMap renders the committed config.yml which contains only `!ENV` references (no plaintext secrets — verified by Plan 31-01); secrets arrive at runtime via `envFrom: arrconf-env`, never in the ConfigMap data |
| T-31-07 | Denial of Service | CronJob lifecycle | mitigate | `QBT_RUN: "true"` + `QBT_SCHEDULE: "0"` force run-once-and-exit; `concurrencyPolicy: Forbid` + history limits prevent pile-up; without these the pod would hang (RESEARCH Pitfall 2) |
| T-31-08 | Tampering | qBit category ownership | mitigate | The mounted config.yml has `cat_update: false` + `cat: {}` (from Plan 31-01); qbit_manage cannot write categories — arrconf stays sole owner (QBM-02) |
| T-31-09 | Spoofing/Supply-chain | qbit_manage image | mitigate | Pinned `v4.6.6` (no `:latest`); `# renovate: image=...` annotation tracks future versions; CI annotation-count guard (bumped to 14) blocks accidental annotation removal |
| T-31-10 | Repudiation | Helm render correctness | accept | helm lint + kubeconform in CI (chart-lint.yml) validate the rendered manifests; multi-alias unpack loop ensures the 13th alias resolves |
</threat_model>

<verification>
- `helm lint charts/arr-stack/` exits 0 (after unpack workaround).
- `helm template charts/arr-stack/ -f examples/values-prod.yaml | kubeconform -ignore-missing-schemas -` exits 0 — the qbit-manage CronJob + ConfigMap render and validate.
- Rendered output contains a CronJob named `qbit-manage` with schedule `0 */4 * * *`, `QBT_RUN`/`QBT_SCHEDULE` env, `envFrom` arrconf-env, and a `/config/config.yml` volume mount from the `qbit-manage-config` ConfigMap.
- chart-lint.yml remains valid YAML; the unpack loop and `>= 14` guard (with the matching step name) are present.
- README Vérification locale loop lists `qbit-manage`.
</verification>

<success_criteria>
QBM-03 satisfied: qbit_manage deployed as the 13th `app-template` CronJob alias with the generated config.yml mounted read-only, run-once env, and arrconf-env credential injection; CI multi-alias unpack + annotation guard updated; a single `arrconf apply` + ArgoCD sync would deploy it.
</success_criteria>

<output>
After completion, create `.planning/phases/31-qbit-manage/31-02-SUMMARY.md`.
</output>
</plan>
</content>
