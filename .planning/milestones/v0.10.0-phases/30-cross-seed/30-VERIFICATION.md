---
phase: 30-cross-seed
verified: 2026-05-31T06:10:00Z
status: human_needed
score: 3/3
overrides_applied: 0
human_verification:
  - test: "Pod reaches Running state after ArgoCD sync"
    expected: "cross-seed pod STATUS=Running, READY=1/1; initContainer config-init completes; no unresolved ${...} tokens in /config/config.js at runtime"
    why_human: "Requires live cluster with cross-seed-config PVC, arrconf-env SealedSecret keys (PROWLARR_API_KEY, QBT_USER, QBT_PASS), and /media/data/torrents/cross-seed host dir — cannot verify programmatically"
  - test: "cross-seed authenticates to Prowlarr torznab without 401 Unauthorized"
    expected: "kubectl -n selfhost logs deploy/cross-seed shows torznab connection success, no 'invalid apikey' error"
    why_human: "Requires live Prowlarr instance with correctly-seeded API key in arrconf-env SealedSecret"
---

# Phase 30: cross-seed Verification Report

**Phase Goal:** cross-seed est consolidé dans l'umbrella chart avec sa config entièrement générée depuis intent.yml, remplaçant l'instance hors-stack existante.
**Verified:** 2026-05-31T06:10:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC1: intent.yml declares `tools.cross_seed` with distinct env tokens (not PLACEHOLDER); `arrconf generate --check` exits 0 | VERIFIED | `charts/arr-stack/files/intent.yml` has `${PROWLARR_API_KEY}` in torznab URL and `${QBT_USER}:${QBT_PASS}` in torrent_clients; `config.js` starts with `module.exports = {`; `arrconf generate --check` exits 0 with `generate_ok` event |
| 2 | SC2: cross-seed/config.js mounted via ConfigMap; initContainer resolves tokens; main container mounts at /config/config.js via subPath; tcpSocket probes on 2468; no PLACEHOLDER or real secrets in committed files | VERIFIED | ConfigMap renders correctly in `helm template`; initContainer Node.js script present; subPath mount `/config/config.js` verified; tcpSocket probes verified; 0 PLACEHOLDER occurrences; only `${...}` tokens in committed ConfigMap |
| 3 | SC3: cross-seed is the 12th app-template@5.0.0 alias in Chart.yaml; CI alias loop includes cross-seed; renovate threshold >= 12; single ArgoCD sync deploys consolidated version; operator runbook documents prerequisites and teardown | VERIFIED | Chart.yaml has 12 aliases (confirmed); chart-lint.yml alias loop includes cross-seed; renovate synthetic test passes (13 matches >= 12); 30-OPERATOR-RUNBOOK.md documents PVC, host dir, arrconf-env keys, and out-of-stack teardown |

**Score:** 3/3 truths verified (automated evidence)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `charts/arr-stack/files/intent.yml` | tools.cross_seed block with distinct env tokens | VERIFIED | `${PROWLARR_API_KEY}` and `${QBT_USER}:${QBT_PASS}` tokens present; no PLACEHOLDER |
| `charts/arr-stack/files/cross-seed/config.js` | Generated config.js with `module.exports = {` | VERIFIED | Starts with `// GENERATED by 'arrconf generate'` and `module.exports = {`; carries env tokens |
| `charts/arr-stack/templates/cross-seed-configmap.yaml` | ConfigMap template mounting config.js under `config.js` key | VERIFIED | Mirrors configarr-configmap.yaml pattern; name=cross-seed-config; uses `.Files.Get "files/cross-seed/config.js"` |
| `charts/arr-stack/Chart.yaml` | 12 app-template@5.0.0 aliases including cross-seed | VERIFIED | 12 aliases present; cross-seed is the 12th (after suggestarr) |
| `charts/arr-stack/values.yaml` (cross-seed block) | Deployment with initContainer, emptyDir, PVC, hostPath, tcpSocket probes | VERIFIED | All volumes present via advancedMounts; initContainer uses Node.js script; envFrom arrconf-env; tcpSocket on 2468 |
| `charts/arr-stack/values.schema.json` | cross-seed property in schema | VERIFIED | `"cross-seed": {"type": "object", "additionalProperties": true}` present |
| `.github/workflows/chart-lint.yml` | cross-seed in alias unpack loop; threshold >= 12 | VERIFIED | `for alias in ... cross-seed; do` present; `total_matches < 12` threshold |
| `README.md` | cross-seed in local-verification loop; "12 aliases" comment | VERIFIED | cross-seed in loop; "12 aliases du même chart" comment |
| `.planning/phases/30-cross-seed/30-OPERATOR-RUNBOOK.md` | Documents PVC creation, host dir, arrconf-env keys, teardown | VERIFIED | All four categories documented: pre-reqs, post-sync verification, teardown, rollback |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `intent.yml` tools.cross_seed | `cross-seed/config.js` | `arrconf generate` pure function | VERIFIED | `arrconf generate --check` exits 0; config.js matches intent |
| `cross-seed-configmap.yaml` | `files/cross-seed/config.js` | `.Files.Get` in template | VERIFIED | ConfigMap renders with full config.js content including module.exports |
| initContainer config-init | ConfigMap (config-cm volume) | advancedMounts subPath `/config-cm/config.js` | VERIFIED | volumeMounts verified in rendered Deployment |
| initContainer → emptyDir | main container `/config/config.js` | advancedMounts subPath `/config/config.js` | VERIFIED | emptyDir config-resolved wired correctly; subPath prevents PVC shadowing |
| main container | arrconf-env SealedSecret | `envFrom secretRef: name: arrconf-env` | VERIFIED | Both initContainer and main container have envFrom arrconf-env |
| initContainer Node.js script | `${PROWLARR_API_KEY}`, `${QBT_USER}`, `${QBT_PASS}` env vars | `process.env.*` substitution | VERIFIED | Script replaces all three tokens via regex in the rendered Deployment |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `cross-seed/config.js` (ConfigMap) | torznab URL, torrentClients URL | `intent.yml` via `arrconf generate` | Yes — tokens are `${PROWLARR_API_KEY}` / `${QBT_USER}:${QBT_PASS}` substituted at runtime | FLOWING (static tokens resolved at runtime via initContainer) |
| initContainer resolved config.js | Real API key + qBit credentials | `arrconf-env` SealedSecret at pod startup | Yes — cluster-side secret injection | FLOWING (cannot verify without live cluster) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `arrconf generate --check` exits 0 (drift gate) | `uv run arrconf generate --check --intent charts/arr-stack/files/intent.yml --output-dir charts/arr-stack/files/` | exit 0, `generate_ok` event logged | PASS |
| 0 PLACEHOLDER occurrences in intent.yml and config.js | `grep -r PLACEHOLDER charts/arr-stack/files/intent.yml charts/arr-stack/files/cross-seed/config.js` | grep exit 1 (0 matches) | PASS |
| helm lint exits 0 | `helm lint charts/arr-stack/ -f examples/values-prod.yaml` | 1 chart(s) linted, 0 chart(s) failed | PASS |
| helm template exits 0 (umbrella) | `helm template arr-stack charts/arr-stack/ -f charts/arr-stack/values.yaml` | exit 0, 76999 bytes rendered | PASS |
| cross-seed Deployment renders with initContainer | helm template output analysis (cross-seed subchart) | kind: Deployment, initContainers: [config-init], daemon args, tcpSocket on 2468, emptyDir, subPath | PASS |
| ConfigMap renders config.js with tokens | helm template umbrella, cross-seed-configmap.yaml block | module.exports with ${PROWLARR_API_KEY}, ${QBT_USER}, ${QBT_PASS}; no real secrets | PASS |
| check-renovate-annotations.sh exits 0 | `tools/scripts/check-renovate-annotations.sh charts/arr-stack/values.yaml` | "OK: all repository: lines have renovate annotations" | PASS |
| renovate synthetic test passes (>= 12) | Python test matching renovate.json regex against values.yaml | 13 matches >= 12 required | PASS |
| pytest 509 passed / 3 known failures | `cd tools/arrconf && uv run pytest -q` | 3 failed (test_sweep x2, test_reconcile_jellyfin_step_order_invariant — pre-existing respx order-flakiness per MEMORY.md), 509 passed | PASS |
| test_d_chart_yaml_has_exactly_12_aliases passes | `uv run pytest -k chart -v` | test_d_chart_yaml_has_exactly_12_aliases PASSED | PASS |
| mypy arrconf exits clean | `cd tools/arrconf && uv run mypy arrconf` | Success: no issues found in 60 source files | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| XSEED-01 | 30-01-PLAN.md | Opérateur déclare `tools.cross_seed` (torznab, link policy) dans `intent.yml` | SATISFIED | `charts/arr-stack/files/intent.yml` has full `tools.cross_seed` block with torznab, torrent_clients, link_dirs, link_type, action |
| XSEED-02 | 30-01-PLAN.md | `arrconf generate` émet un `cross-seed/config.js` valide (`module.exports = {...}`) | SATISFIED | `config.js` generated with correct syntax; `generate --check` exits 0; 5 generator tests pass |
| XSEED-03 | 30-02-PLAN.md, 30-03-PLAN.md | cross-seed déployé via alias Helm `app-template` (config.js monté), consolidant l'instance hors-stack | SATISFIED | 12th alias in Chart.yaml; ConfigMap template; values.yaml Deployment block; CI updated; operator runbook provided |

Note: REQUIREMENTS.md still shows XSEED-01/02/03 as "Pending" — this is a documentation artifact that the orchestrator updates after verification passes.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | No stubs, TODOs, PLACEHOLDER, or empty implementations found in phase-30 deliverables | — | — |

The `${PROWLARR_API_KEY}`, `${QBT_USER}`, `${QBT_PASS}` tokens in `config.js` are NOT stubs — they are intentional substitution tokens resolved at runtime by the initContainer. This is the design (D-01, D-02).

### Human Verification Required

#### 1. Pod reaches Running state (initContainer success)

**Test:** After cluster prerequisites are met (per 30-OPERATOR-RUNBOOK.md), trigger ArgoCD sync:
- Verify `cross-seed-config` PVC exists in `selfhost` namespace
- Verify `arrconf-env` SealedSecret contains `PROWLARR_API_KEY`, `QBT_USER`, `QBT_PASS` keys
- Verify `/media/data/torrents/cross-seed` host directory exists
- Then: `kubectl -n selfhost get pod -l app.kubernetes.io/name=cross-seed`

**Expected:** STATUS=Running, READY=1/1; initContainer config-init completed; `kubectl -n selfhost logs deploy/cross-seed -c config-init` shows no Node.js error

**Why human:** Requires live cluster with SealedSecret and PVC — cannot mock these in CI

#### 2. cross-seed authenticates to Prowlarr torznab

**Test:** After pod reaches Running: `kubectl -n selfhost logs deploy/cross-seed`

**Expected:** No "401 Unauthorized" or "invalid apikey" errors; cross-seed shows torznab indexing or "waiting for requests"

**Why human:** Requires live Prowlarr instance with real API key in arrconf-env SealedSecret; authentication result is a runtime behavior observable only in cluster logs

### Gaps Summary

No gaps found. All three Success Criteria are achieved in the codebase:

- SC1 (XSEED-01/02): `intent.yml` has distinct env tokens; `config.js` has correct `module.exports` syntax; `arrconf generate --check` passes.
- SC2: ConfigMap template renders config.js correctly; Deployment has initContainer with Node.js token substitution; main container gets config at `/config/config.js` via emptyDir subPath; tcpSocket probes on 2468; zero PLACEHOLDER/secrets in committed files.
- SC3: Chart.yaml has 12 aliases including cross-seed; CI alias loop and renovate threshold updated; operator runbook provides complete pre-req/teardown documentation.

Two human verification items remain (runtime pod behavior and torznab authentication) which require a live cluster and cannot be verified programmatically.

---

_Verified: 2026-05-31T06:10:00Z_
_Verifier: Claude (gsd-verifier)_
