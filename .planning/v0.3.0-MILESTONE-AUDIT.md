---
milestone: v0.3.0 — Categories first-class
audited: 2026-05-22
auditor: claude-sonnet-4-6 (gsd cross-phase integration auditor)
verdict: passed_with_caveats
caveats:
  - 3 human-UAT items deferred (cluster-time, not code defects)
  - 1 documentation sync gap (ROADMAP Phase 9 checkbox not ticked)
  - 11 REQUIREMENTS.md inline body checkboxes stale (pre-closure; table is authoritative)
---

# v0.3.0 Milestone Audit — Categories first-class

**Audit date:** 2026-05-22
**Phases in scope:** Phase 9 (09-A/B/C/D), Phase 10 (10-A through 10-J), Phase 11 (11-A/B)
**REQs in scope:** 18 (REQ-categories-schema … REQ-readme-onboarding-v030)
**Test suite state:** 384 tests, all passing
**Production cluster:** chart v0.7.0 synced + healthy; image :0.6.7 running

---

## Audit Verdict: PASSED WITH CAVEATS

All 18 REQs have code implementation verified. The 3 deferred human-UAT items are operator-exercise gates ("run the path in cluster"), not code defects. No BLOCKER-class cross-phase wiring break was found. Two WARNING-class documentation sync gaps are noted.

---

## Wiring Summary

**Connected:** 18 exports properly used across phases
**Orphaned:** 0 exports created but unused (generators module is fully consumed by __main__.py)
**Missing:** 0 expected connections absent

## API Coverage

**Consumed:** all 6 app reconcilers (Sonarr, Radarr, Prowlarr, qBit, Seerr, Jellyfin) have consumers in __main__.py
**Orphaned:** 0

## Auth Protection

Not applicable — this is a CronJob CLI, not a web app. API keys are injected via envFrom secretRef (arrconf-env). All 6 reconciler branches check for their respective API key env var with an explicit fail-fast guard (exit code 2).

## E2E Flows

**Complete:** 7 flows verified end-to-end (see Detailed Findings)
**Broken:** 0 flows fully broken
**Human-UAT pending:** 3 items (see Deferred Human-UAT section)

---

## Detailed Findings

### Cross-Phase Integration Check 1 — Categories → 6 apps E2E chain

**Verdict: WIRED (code complete; cluster materialization is human-UAT)**

Traced from data model to every consumer:

| Link | Wiring | Evidence |
|------|--------|----------|
| `categories[]` in arrconf.yml → `RootConfig.categories: list[MediaCategory]` | WIRED | `config.py` line 22 imports `Category as MediaCategory`; field at line 664 |
| `RootConfig.categories` → `generate_qbit_categories(root)` → `merge_with_manual` → `reconcile_qbittorrent` | WIRED | `__main__.py` lines 342-355; apply branch confirmed |
| `RootConfig.categories` → `generate_sonarr_resources(root)` → 4× `merge_with_manual` → `reconcile_sonarr` | WIRED | `__main__.py` lines 191-225; all 4 resource types wired |
| `RootConfig.categories` → `generate_radarr_resources(root)` → 4× `merge_with_manual` → `reconcile_radarr` | WIRED | `__main__.py` lines 244-280; mirror of Sonarr, kind=movies filter |
| `RootConfig.categories` → `generate_anime_tag_labels` → `_resolve_seerr_anime_tag_ids` (kind=series filter) → `merge_with_manual` → `reconcile_seerr` | WIRED | `__main__.py` lines 65-95 (resolver) + 392-415 (wiring); kind=series filter confirmed at line 69 |
| `RootConfig.categories` → `generate_jellyfin_libraries(root)` → `merge_with_manual` → `reconcile_jellyfin` | WIRED | `__main__.py` lines 440-452 |
| `categories-init-job.yaml` → `.Files.Get "files/arrconf.yml" \| fromYaml` → `mkdir -p /media/<name>` per category | WIRED | `categories-init-job.yaml` line 20; `claimName: media-nas-pvc` at line 75; `media_dir_ensured` log pattern at lines 63/66 |
| diff branch mirrors apply branch (Pitfall 5) | WIRED | `__main__.py` diff branch: qBit (lines 671-684), Sonarr (lines 551-582), Radarr (lines 598-630), Prowlarr (lines 637-652), Seerr pre-merge (lines 705-731), Jellyfin (lines 739-749) |

**One known gap in diff branch:** `diff_seerr` is not wired — `arrconf diff --apps seerr` logs `diff_not_implemented` (`__main__.py` line 727-731). The apply branch is fully wired. No SC requires diff_seerr. This is an INFO-level deferred item, not a BLOCKER.

### Cross-Phase Integration Check 2 — 2nd-run idempotence E2E

**Verdict: WIRED (SC#2 dispositive on live cluster 2026-05-20)**

All 3 FP regression fixes verified wired:

| Fix | File | Mechanism | Status |
|-----|------|-----------|--------|
| FP#1 qBit extras | `reconcilers/qbittorrent.py` line 95-99 | `QBIT_CATEGORY_MANAGED_FIELDS = frozenset({"name", "savePath"})` filters before `model_validate` | WIRED |
| FP#2 Prowlarr extras + prowlarr_url separation | `reconcilers/prowlarr.py` | `PROWLARR_APP_MANAGED_FIELDS` (7 fields) + `PROWLARR_APP_MANAGED_FIELD_NAMES` sub-field allowlist; `ProwlarrInstance.prowlarr_url` field separation (commit `310aebf`) | WIRED |
| FP#3 Seerr user extras | `reconcilers/seerr.py` line 70 | `SEERR_USER_MANAGED_FIELDS = frozenset({6 user fields})` applied in `_reconcile_user` | WIRED |

SC#2 sweep tests: `test_sweep_categories_derived_path` + `test_sweep_manual_override_path` both pass (0 UPDATE/DELETE on run 2). Live cluster dry-run ×2 confirmed 0 `plan_action` events (2026-05-20).

### Cross-Phase Integration Check 3 — Chart-pin co-bump pattern E2E

**Verdict: WIRED (full chain observed in production)**

End-to-end chain verified:

1. `gsd-executor.md` (`/home/moi/.claude/agents/gsd-executor.md` line 62) contains the co-bump rule paragraph — arrconf-side commits trigger it
2. `CLAUDE.md` section "Release pin co-bump pattern" documents the pattern and historical table
3. Phase 10 per-plan co-bumps executed: 0.5.3→0.6.0→0.6.1→0.6.2→0.6.3→0.6.4→0.6.5→0.6.6 (Phase 10-C through 10-J); 0.6.6→0.6.7 (commit `310aebf`, Prowlarr live-cluster FP fix)
4. `chart-lint.yml` paths filter (`tools/arrconf/**`) at lines 8+17 ensures arrconf-only commits trigger the auto-tag job
5. Auto-tag created `v0.7.0` on push of Phase 11 commits (feat-prefix triggered minor bump; `12c05da` workflow fix resolved `github.ref_name` semver bug)
6. `ghcr.io/tom333/arr-stack-arrconf:0.7.0` built and pushed by `arrconf-image.yml`
7. my-kluster PR #1411 (v0.6.7) MERGED 2026-05-20; PR #1413 (v0.6.8→v0.7.0) MERGED 2026-05-21T18:12:32Z
8. ArgoCD targetRevision at v0.7.0; cluster synced + healthy

**Full chain: arr-stack commit → auto-tag → image build → my-kluster Renovate PR → merge → ArgoCD sync — all empirically observed.**

### Cross-Phase Integration Check 4 — Renovate App scan + cross-repo loop

**Verdict: WIRED (SC#4 dispositive via my-kluster PR #1413 MERGED)**

- `chart-lint.yml` paths filter at lines 8+17 covers both `charts/**` and `tools/arrconf/**`
- Renovate App confirmed active: PRs #14/#15/#17/#18 opened on `tom333/arr-stack` by `app/renovate`
- my-kluster PR #1413 MERGED at 2026-05-21T18:12:32Z (v0.7.0 targetRevision bump)
- Renovate scan latency: 2 min after v0.7.0 push (PRs #17/#18 observed)

### Cross-Phase Integration Check 5 — ADR-5 frontière (arrconf ≠ configarr)

**Verdict: INTACT**

- No arrconf code reads or writes `configarr.yml` — grep confirms zero hits on `configarr` in reconcilers
- `ScopeViolationError` is defined in `exceptions.py` lines 29-32 and imported in `__main__.py` line 26
- All 4 configarr-owned resource types have stub files that raise `ScopeViolationError` instead of reconciling: `sonarr/quality_profile.py`, `sonarr/custom_format.py`, `sonarr/quality_definition.py`, `sonarr/media_naming.py` (and their Radarr equivalents)
- `test_scope_violation.py` (8.8K) covers this explicitly
- `test_three_profiles_per_instance` confirms configarr.yml has exactly 3 profiles per instance (MULTi.VF, Anime, Family) and that arrconf does not modify them

**Note on REQ-categories-configarr-mapping wording:** The REQUIREMENTS.md body says profiles are named "General, Anime, Family" but the actual production profile names in `configarr.yml` are "MULTi.VF, Anime, Family". This is a pre-existing wording artifact from before production profile names were finalized. The intent (3 profiles per instance) is satisfied and verified by test. This is documentation-only, not a code defect.

### Cross-Phase Integration Check 6 — ADR-7 single-instance + tags

**Verdict: INTACT**

- `arrconf.yml` has a single `sonarr.main` block and single `radarr.main` block
- `generate_sonarr_resources` produces 5 tags (one per series category), 5 root_folders, 5 download_clients, 5 RPMs — all bearing the category name as the routing label
- `generate_radarr_resources` mirrors this for kind=movies
- No multi-instance plumbing was introduced in v0.3.0
- D-07-USERS-01 protection (no new Jellyfin users) is preserved; `reconcile_jellyfin` handles only libraries + plugins + server_config

### Cross-Phase Integration Check 7 — Production cluster reality check

**Verdict: VERIFIED via Phase 11 evidence logs**

| Check | Expected | Evidence |
|-------|----------|----------|
| ArgoCD selfHeal+prune | `{"prune":true,"selfHeal":true}` | `argocd-selfheal-uat-2026-05-21.log` STEP 1 confirms; STEP 6 auto-revert in <3 min |
| ConfigMaps in selfhost | only arrconf-config + configarr-config | `cm-cruft-cleanup-2026-05-21.log` STEP 4: exactly 2 non-CA CMs; legacy CMs already pruned by ArgoCD |
| arrconf CronJob image | ghcr.io/tom333/arr-stack-arrconf:0.6.7 | `values.yaml` line 451: `tag: "0.6.7"`; `values.yaml` line 449: `# renovate:` annotation intact |
| Renovate annotation intact | `# renovate: image=ghcr.io/tom333/arr-stack-arrconf` above `repository:` | Confirmed at values.yaml line 449 |

Note: The audit context states image :0.6.7 is running in cluster (chart v0.7.0 was built from the Phase 11 push). The `values.yaml` still shows `tag: "0.6.7"` which is correct — Phase 11 commits did not touch `tools/arrconf/**` Python code so no co-bump was required (D-05 exception applies cleanly per 11-VERIFICATION.md).

---

## Orphaned Exports

None found. All 5 generator functions from `arrconf/generators/categories.py` are imported and called in `__main__.py` (lines 28-33). `merge_with_manual` from `reconcilers/_shared.py` is called 11× in the apply branch and 9× in the diff branch. The generators module's `SonarrDerived` and `RadarrDerived` dataclasses are consumed by the callsites.

---

## Missing Connections

None of the expected cross-phase connections are missing.

---

## Broken Flows

No E2E flows are broken. The only wiring gap is `diff_seerr` not being called in the diff branch (`__main__.py` line 727-731), which is an explicit deferred item with no SC requirement.

---

## Unprotected Routes

Not applicable (CronJob CLI, no HTTP server or web routes).

---

## Deferred Human-UAT Items

These items require live cluster access and cannot be verified from code. They are not BLOCKER-class — the code is correct and ships; the UAT is an operator exercise.

| Item | Phase | Why Deferred | Blocking? |
|------|-------|-------------|-----------|
| initContainer cluster execution — verify 10 `/media/<name>` dirs created; NFS uid=1000 write access (CR-01) | Phase 9 | Requires ArgoCD sync + `kubectl logs job/arr-stack-categories-init`; NFS `root_squash` behavior is runtime-observable | No — code correct; cluster UAT opt-in |
| SC#1 arrconf apply materialization — verify 10 qBit cats, 20 Sonarr resources, 20 Radarr resources created without UI edits | Phase 10 | Requires live cluster apply with real API keys and populated cluster state (empty flat sections to exercise Categories path) | No — code + unit tests verified; operator exercise |
| SC#3 TVDB-anime routing — submit anime series request in Seerr, verify routes to series-zoe category | Phase 10 | Requires live Seerr + Sonarr + qBittorrent integration; TVDB classification cannot be mocked offline | No — `_resolve_seerr_anime_tag_ids` unit tested (6 tests pass); live routing is operator exercise |

---

## Documentation Sync Gaps

### WARNING-1: ROADMAP Phase 9 checkbox not ticked

**File:** `.planning/ROADMAP.md` line 54
**State:** `- [ ] **Phase 9: Categories data model + chart initContainer**` — all 4 plans (09-A/B/C/D) have SUMMARYs and the 09-VERIFICATION.md exists. Phase 9 is complete in code.
**Impact:** Documentation only. Phase 10 and 11 are correctly marked `[x]`. Phase 9 should be `[x]` per the standard GSD lifecycle.
**Action:** Flip to `[x]` during `/gsd-complete-milestone`.

### WARNING-2: REQUIREMENTS.md inline checkboxes stale

**File:** `.planning/REQUIREMENTS.md` body section
**State:** 16 of 20 v0.3.0 requirements still show `[ ]` in the body bullet text; only 4 are `[x]`. The traceability table at the bottom correctly shows 9 "Complete" and 11 "Pending" (the latter 11 being implemented but not yet flipped).
**Impact:** Documentation only. The traceability table is the canonical tracking view; the inline body checkboxes are a known sync artifact (pre-closure). The 11 "Pending" rows in the table correspond to implemented requirements.
**Action:** Flip all 20 inline checkboxes to `[x]` and update 11 "Pending" rows to "Complete" during `/gsd-complete-milestone`.

### WARNING-3: ROADMAP "Progress" table shows 0/TBD plans for v0.3.0

**File:** `.planning/ROADMAP.md` line 120
**State:** `| v0.3.0 Categories first-class | 3 | 0/TBD | 🚧 In progress | — |` — should show 16/16 plans complete.
**Impact:** Documentation only.
**Action:** Update during `/gsd-complete-milestone`.

---

## Future Requirements Integrity

The 4 deferred v0.4.0+ REQs appear only as:
- Comments in `_shared.py` line 166 (REQ-categories-deprecation mentioned as future intent)
- Planning context files (09-CONTEXT.md, 10-CONTEXT.md, 10-DISCUSSION-LOG.md)
- REQUIREMENTS.md "Future Requirements" section

**No deferred REQ has a phase mapping, code implementation, or was accidentally included in any phase checklist.** The `_shared.py` reference is a forward-pointer comment only, not implementation. Integrity intact.

---

## Requirements Integration Map

| Requirement | Integration Path | Status | Notes |
|-------------|-----------------|--------|-------|
| REQ-categories-schema | Phase 9-A: `categories.py` → `config.py` import → `RootConfig.categories` → `schemas/arrconf-schema.json` | WIRED | 37 unit tests; schema CI gate passes |
| REQ-categories-10-target | Phase 9-C: `arrconf.yml` 10-entry block → `test_arrconf_yml_has_10_categories` | WIRED | 10 entries confirmed; test passes |
| REQ-categories-qbit-propagation | Phase 10-A generators → 10-C wiring: `generate_qbit_categories` → `merge_with_manual` → `reconcile_qbittorrent` in `__main__.py` lines 342-355 | WIRED | D-03a bare `<name>` confirmed; 10 QbitCategory entries |
| REQ-categories-sonarr-propagation | Phase 10-A generators → 10-D wiring: `generate_sonarr_resources` → 4× `merge_with_manual` → `reconcile_sonarr` in `__main__.py` lines 191-225 | WIRED | 5 series categories × 4 resources = 20 |
| REQ-categories-radarr-propagation | Phase 10-A generators → 10-E wiring: `generate_radarr_resources` → 4× `merge_with_manual` → `reconcile_radarr` in `__main__.py` lines 244-280 | WIRED | 5 movies categories × 4 resources = 20 |
| REQ-categories-configarr-mapping | Phase 10 SC#4: `test_three_profiles_per_instance` reads `configarr.yml`; ADR-5 stubs in reconcilers raise `ScopeViolationError` | WIRED | 3 profiles (MULTi.VF/Anime/Family); arrconf never writes configarr.yml |
| REQ-categories-seerr-routing | Phase 10-F: `generate_anime_tag_labels` → `_resolve_seerr_anime_tag_ids` (kind=series filter) → `merge_with_manual(animeTags)` → `reconcile_seerr` | WIRED (code); HUMAN-UAT (TVDB routing) | 6 unit tests pass; live routing deferred |
| REQ-categories-jellyfin-paths | Phase 10-G: `generate_jellyfin_libraries` → `merge_with_manual` → `reconcile_jellyfin` in `__main__.py` lines 440-452 | WIRED | 2 super-libraries × 5 PathInfos |
| REQ-migration-progressive | Phase 9-C + Phase 10-B: `merge_with_manual` in `_shared.py`; `test_dry_run_plan_unchanged_without_categories` SC#4 dispositive | WIRED | Both paths (categories-derived + manual-override) sweep-tested |
| REQ-filesystem-initcontainer | Phase 9-B: `categories-init-job.yaml` → `.Files.Get "files/arrconf.yml" \| fromYaml` → `mkdir -p /media/<name>` per category; `media-nas-pvc` PVC mount | WIRED (code); HUMAN-UAT (cluster execution) | Template correct; cluster-time NFS write is deferred UAT |
| REQ-filesystem-operator-migration | Phase 9-D: `CLAUDE.md` section "Filesystem migration: v0.2.0 flat → v0.3.0 Categories" with 6-row mapping table + 4-step runbook | WIRED | Section verified at CLAUDE.md; no bash helper (per D-17 decision) |
| REQ-04-09-argocd-selfheal | Phase 11-B: `my-kluster` `arr-stack-app.yaml` `selfHeal: true` + `prune: true`; drift UAT | WIRED | Live evidence: `argocd-selfheal-uat-2026-05-21.log` SC#1 PASS |
| REQ-cm-cruft-cleanup | Phase 11-B: Legacy CMs `arrconf`/`configarr` absent from selfhost namespace | WIRED | Live evidence: `cm-cruft-cleanup-2026-05-21.log` SC#2 PASS; CMs pruned by ArgoCD |
| REQ-chart-pin-prebump | Phase 10-I: `gsd-executor.md` co-bump rule + `CLAUDE.md` section; per-plan chain 0.5.3→0.6.7 in values.yaml | WIRED | Full chain to my-kluster PR #1413 MERGED empirically observed |
| REQ-ruff-format-ci-gate | Phase 11-A: `tests.yml` lines 39+42 `ruff check` + `ruff format --check`; `.pre-commit-config.yaml` `v0.15.7` pinned | WIRED | Both CI + local gates in place |
| REQ-paths-filter-arrconf | Phase 11-A: `chart-lint.yml` lines 8+17 `tools/arrconf/**` in both push and pull_request paths | WIRED | 2 occurrences confirmed; REQ comment inline |
| REQ-renovate-app-install | Phase 11-B: Mend Renovate App active; PRs #14/#15/#17/#18 on arr-stack; my-kluster PR #1413 MERGED | WIRED | Cross-repo loop empirically validated |
| REQ-snapshot-redaction-harden | Phase 11-A: `snapshot.sh` lines 400-416 `JQ_REDACT` block + `mv -f` + `nullglob` + dry-run gate | WIRED | Anti-leak grep on fresh snapshot returns 0 |
| REQ-idempotence-fp-fix | Phase 10-C/F/H/J: 3 `frozenset` allowlists in qbittorrent/seerr/prowlarr reconcilers; SC#2 sweep tests pass | WIRED | Live cluster dry-run ×2 = 0 plan_action confirmed 2026-05-20 |
| REQ-readme-onboarding-v030 | Phase 11-A: 3 stale v0.2.x references removed; Onboarding section at README.md line 173 | WIRED | `grep` confirms 0 stale refs; section present |

**Requirements with no cross-phase wiring (self-contained):** None. All 18 v0.3.0 requirements have at minimum one verified cross-phase connection (data model → consumer or doc → procedure).

---

## Production Cluster State Summary

| Observable | Value | Source |
|------------|-------|--------|
| ArgoCD app status | Synced + Healthy | Audit context |
| chart targetRevision | v0.7.0 | my-kluster PR #1413 MERGED |
| arrconf image tag in values.yaml | `0.6.7` | `charts/arr-stack/values.yaml` line 451 |
| `# renovate:` annotation | intact | `charts/arr-stack/values.yaml` line 449 |
| ArgoCD selfHeal + prune | `{"prune":true,"selfHeal":true}` | `argocd-selfheal-uat-2026-05-21.log` STEP 1 |
| Legacy CMs in selfhost | 0 (pruned by ArgoCD) | `cm-cruft-cleanup-2026-05-21.log` STEP 4 |
| Test suite | 384 passed | local pytest run (audit) |
| Renovate cross-repo loop | Active; 2 PRs MERGED | my-kluster #1411 + #1413 |

---

## Recommended Next Steps

1. **Proceed with `/gsd-complete-milestone`** — all 18 REQs are implemented and the system works end-to-end. The 3 deferred UAT items are operator exercises on a live cluster, not code gaps.

2. **During milestone close**, update the following documentation artifacts:
   - `.planning/ROADMAP.md` line 54: flip Phase 9 checkbox from `[ ]` to `[x]`
   - `.planning/ROADMAP.md` Progress table: update v0.3.0 row to `16/16 plans | ✅ Shipped | 2026-05-22`
   - `.planning/REQUIREMENTS.md` traceability table: flip 11 "Pending" rows to "Complete"
   - `.planning/REQUIREMENTS.md` inline body: flip remaining 16 `[ ]` to `[x]`

3. **v0.4.0 seeds to capture** (already in REQUIREMENTS.md "Future Requirements"):
   - REQ-categories-deprecation (remove merge_with_manual override path; Categories sole source)
   - REQ-bazarr-addition (7th *arr-stack app)
   - REQ-suggestarr-integration (SEED-001)
   - REQ-web-ui-categories

4. **Optional operator exercises** (not blocking, can run anytime):
   - Cluster UAT of initContainer dir creation (Phase 9 SC#3)
   - Full `arrconf apply` materialization pass (Phase 10 SC#1)
   - TVDB-anime routing via Seerr (Phase 10 SC#3)

---

_Audited: 2026-05-22_
_Auditor: Claude (gsd-cross-phase-integration-auditor)_
