# yaml-language-server: $schema=../schemas/arrconf-schema.json
# Phase 12 (v0.4.0): the v0.2.0 `download_clients.items` flat list is no longer
# valid YAML — `extra="forbid"` on Section models rejects the legacy shape.
# Generator-derived resources flow from the top-level `categories:` block.
sonarr:
  main:
    base_url: http://sonarr.selfhost.svc.cluster.local:8989
    download_clients:
      prune: false
